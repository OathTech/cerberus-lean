proc sum (n: pointer): eff loaded integer :=
  let strong s: pointer = create(Ivalignof('signed int'), 'signed int') in
  let strong a_533: loaded integer = bound(pure(Specified(0))) in
  store('signed int', s, conv_loaded_int('signed int', a_533)) ;
  let strong i: pointer = create(Ivalignof('signed int'), 'signed int') in
  let strong a_534: loaded integer = bound(pure(Specified(0))) in
  store('signed int', i, conv_loaded_int('signed int', a_534)) ;
  save while_531: unit (i: pointer:= i, s: pointer:= s) in
    let strong a_535: loaded integer =
      bound(
        let weak (a_537: loaded integer, a_538: loaded integer) =
          unseq(
            let weak (a_544: loaded integer, a_545: loaded integer) =
              unseq(
                let weak a_542: pointer = pure(i) in
                load('signed int', a_542)
              ,
                let weak a_543: pointer = pure(n) in
                load('signed int', a_543)
              ) in
            case (a_544, a_545) of
              | (Specified(a_546: integer), Specified(a_547: integer)) =>
                  pure(
                    if conv_int('signed int', a_546) <
                    conv_int('signed int', a_547) then
                      Specified(1)
                    else
                      Specified(0)
                  )
              | _: (loaded integer,loaded integer) =>
                  pure(Unspecified('signed int'))
            end
          ,
            pure(Specified(0))
          ) in
        pure(
          case (a_537, a_538) of
            | (Specified(a_539: integer), Specified(a_540: integer)) =>
                if conv_int('signed int', a_539) = conv_int('signed int', a_540) then
                  Specified(1)
                else
                  Specified(0)
            | _: (loaded integer,loaded integer) =>
                Unspecified('signed int')
          end
        )
      ) in
    let strong a_532: boolean =
      case a_535 of
        | Specified(a_536: integer) =>
            pure(if not(a_536 = 1) then True else False)
        | Unspecified(_: ctype) =>
            nd(pure(True), pure(False))
      end in
    if a_532 then
      let strong _: loaded integer =
        bound(
          let weak (a_549: pointer, a_557: loaded integer) =
            unseq(
              pure(s)
            ,
              let weak (a_550: loaded integer, a_551: loaded integer) =
                unseq(
                  let weak a_555: pointer = pure(s) in
                  load('signed int', a_555)
                ,
                  let weak a_556: pointer = pure(i) in
                  load('signed int', a_556)
                ) in
              pure(
                case (a_550, a_551) of
                  | (Specified(a_552: integer), Specified(a_553: integer)) =>
                      Specified(catch_exceptional_condition_add('signed int', __conv_int__('signed int', a_552), __conv_int__('signed int', a_553)))
                  | _: (loaded integer,loaded integer) =>
                      undef(<<UB036_exceptional_condition>>)
                end
              )
            ) in
          let weak _: unit =
            neg(store('signed int', a_549, conv_loaded_int('signed int', a_557))) in
          pure(conv_loaded_int('signed int', a_557))
        ) in
      pure(Unit) ;
      save __cerb_continue0: unit (i: pointer:= i, s: pointer:= s) in
        let strong _: loaded integer =
          bound(
            let weak (a_558: pointer, a_565: loaded integer) =
              unseq(
                pure(i)
              ,
                let weak (a_559: loaded integer, a_560: loaded integer) =
                  unseq(
                    let weak a_564: pointer = pure(i) in
                    load('signed int', a_564)
                  ,
                    pure(Specified(1))
                  ) in
                pure(
                  case (a_559, a_560) of
                    | (Specified(a_561: integer), Specified(a_562: integer)) =>
                        Specified(catch_exceptional_condition_add('signed int', __conv_int__('signed int', a_561), __conv_int__('signed int', a_562)))
                    | _: (loaded integer,loaded integer) =>
                        undef(<<UB036_exceptional_condition>>)
                  end
                )
              ) in
            let weak _: unit =
              neg(store('signed int', a_558,
              conv_loaded_int('signed int', a_565))) in
            pure(conv_loaded_int('signed int', a_565))
          ) in
        pure(Unit) ;
      pure(Unit) ;
      save continue_529: unit (i: pointer:= i, s: pointer:= s) in  pure(Unit) ;
      pure(Unit) ;
      run while_531(i, s)
    else
      pure(Unit) ;
  save break_530: unit (i: pointer:= i, s: pointer:= s) in  pure(Unit) ;
  pure(Unit) ;
  kill('signed int', i) ;
  pure(Unit) ;
  let strong a_567: loaded integer =
    bound(
      let weak a_566: pointer = pure(s) in
      load('signed int', a_566)
    ) in
  kill('signed int', s) ;
  run ret_528(conv_loaded_int('signed int', a_567)) ;
  kill('signed int', s) ;
  pure(Unit) ;
  save ret_528: loaded integer (a_568: loaded integer:= undef(<<UB088_reached_end_of_function>>)) in
    pure(a_568)

proc main (): eff loaded integer :=
  let strong a_525: loaded integer =
    bound(
      let strong ((a_515: loaded pointer,
      (a_516: ctype, a_517: [ctype], a_518: boolean, a_519: boolean)),
      a_521: loaded integer) =
        unseq(
          let strong a_514: loaded pointer = pure(Specified(Cfunction(sum))) in
          pure((a_514, cfunction(a_514)))
        ,
          pure(Specified(10))
        ) in
      if not(params_length(a_517) = 1) then
        pure(undef(<<UB038_number_of_args>>))
      else
        if a_518 \/ not(are_compatible ('signed int', a_516)) then
          pure(undef(<<UB041_function_not_compatible>>))
        else
          let strong a_520: pointer =
            let a_522: ctype = params_nth(a_517, 0) in
            if not(are_compatible ('signed int', a_522)) then
              pure(undef(<<UB041_function_not_compatible>>))
            else
              let weak a_523: pointer =
                create(Ivalignof('signed int'), 'signed int') in
              let weak _: unit =
                store('signed int', a_523, conv_loaded_int('signed int', a_521)) in
              pure(a_523) in
          let strong a_524: loaded integer =
            ccall('signed int (*) (signed int)', a_515, a_520) in
          kill('signed int', a_520) ;
          pure(a_524)
    ) in
  run ret_513(conv_loaded_int('signed int', a_525)) ;
  pure(Unit) ;
  save ret_513: loaded integer (a_526: loaded integer:= Specified(0)) in
    pure(a_526)

