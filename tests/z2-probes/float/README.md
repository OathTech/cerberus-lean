# float/ — CerbFloat.lean vs OCaml float semantics probes (Z2, 2026-09-03)

Engines/binaries as in `../mem/README.md`. Classes [AGENT].

| Probe | Mode | What it tests | fork oracle | upstream | Lean | Class | Proposed lane |
|---|---|---|---|---|---|---|---|
| `hexfloat_round.c` | nolibc | reader claim: `CerbFloat.lean:152-155` accumulates the hex mantissa as a Nat then `Float.ofNat` + `scaleB`, rounding before the exponent — three ties/near-ties at 14 hex digits | `Defined {value: "Specified(111)", stdout: "", stderr: "", blocked: "false"}` | same | same | AGREE — REFUTES the claim on all three shapes (ties-to-even and just-above-halfway round identically to `float_of_string`) | exec nolibc MATCH |
| `decimal_sweep.c` | libc | reader claim: Lean 4.32 `Float.ofScientific` (truncating `(m<<<k)/5^e` then 64-bit truncation before the 53-bit rounding) can misround 17-digit decimal literals vs `float_of_string` (strtod) — 200 generated literals (seed 2026; 160 random with exponents −300..300, 40 near-halfway) printed as IEEE bit patterns | 200 stdout lines, `Specified(0)` | identical | identical (full `Defined` line byte-equal) | AGREE on 200/200 — no divergence FOUND; the structural double-rounding risk stays a reading-level claim (INSTRUMENT: a larger sweep in Z4's measurement lane, or a correctly-rounded parser as a hardening) | libc_exec MATCH (pins 200 literals) |
| `nan_to_int_nopanicflag.c` | nolibc | `(int)NaN` (NaN = `inf - inf`): oracle `Z.of_float` raises; Lean `CerbFloat.truncToInt:301-302` panics — crash parity holds ONLY under `LEAN_ABORT_ON_PANIC=1` | exit 125 `Z.Overflow` `Raised by primitive operation at Cerb_frontend__Impl_mem.Concrete.ivfromfloat in file "memory/concrete/impl_mem.ml", line 2554` | same | with `LEAN_ABORT_ON_PANIC=1`: exit 134 `PANIC at CerbFloat.truncToInt CerbFloat:302:4: CerbFloat.truncToInt: nan/inf (OCaml Z.of_float raises Z.Overflow)` / `Aborted (core dumped)`. WITHOUT the flag: exit 0, the same PANIC text on stderr and then **`Defined {value: "Specified(0)", stdout: "", stderr: "", blocked: "false"}`** | both-crash EXC(a) under the flag (= charter Z-58/F2). **INSTRUMENT (fail-closed hygiene), CONFIRMED**: every `panic!` mirror of an OCaml `failwith`/`assert`/exception in the seams is a crash-to-VALUE conversion for any consumer that runs the binary without the env var (the lanes all set it; `Main.lean` never sets it itself). Fix (S): `Main.lean` refuses to run (or sets the runtime abort flag) unless abort-on-panic is in force | immaculate `MATCH \| L=CRASH` pair + a plant that runs the binary without the flag and expects the refusal |

Derived: 3 probes; 0 LEAN≠ORACLE (2 reader claims refuted / not evidenced), 1 INSTRUMENT finding confirmed.
