# mem/ — CerbMem.lean vs impl_mem.ml probes (Z2 seam audit, 2026-09-03)

Three engines per probe (`../run_z2.sh`): fork oracle (`_build/default/backend/
driver/main.exe --exec --batch --mode=exhaustive [--nolibc]`), un-forked
upstream (`deps/cerberus-upstream` @ `git-cn-pin-18-gb9aeedcb4`, same flags),
Lean (`cerberus-lean --batch` via the cabs-json bridge; libc mode adds `--libc
tests/libc/libc.core` + the 12 metadata jsons). Binaries: this worktree's
stamped build (`check_driver_fresh --check` OK: oracle bin `6f59798c…`, lean bin
`8c484a47…`). Engine lines below are VERBATIM (crash backtraces cut to the
exception line). Classes are [AGENT]; the rule is [USER 2026-09-03].

| Probe | Mode | What it tests | fork oracle | upstream | Lean | Class | Proposed lane |
|---|---|---|---|---|---|---|---|
| `aligned_alloc_zero.c` | libc | std.core:385 `size rem_t align` with align 0 (no UB045 guard) → `op_ival IntRem_t` = `Z.rem` (impl_mem.ml:2481-2482, :11) vs Lean total `Int.tmod` | exit 125 `Division_by_zero` `Raised at Z.rem … Called from Cerb_frontend__Impl_mem.Concrete.op_ival in file "memory/concrete/impl_mem.ml", line 2482` | same | exit 1 `Undefined {ub: "DUMMY(align_alloc)", stderr: "", loc: "unknown location"}` | **BUG-FIX** (one-sided oracle crash → Lean must fail-stop with the OCaml text, Q4; also a tray candidate: the oracle crashes on `aligned_alloc(0, n)`) | immaculate `ORACLE_CRASH` pair (Lean pin `UB:DUMMY(align_alloc)` → flips to `MATCH \| L=CRASH` on the fix) |
| `aligned_alloc_zero_zero.c` | libc | as above with size 0: Lean `0 tmod 0 = 0` passes the test, then `alloc(0,0)` with the `alignN.toNat.max 1` clamp (Z-13) | exit 125 `Division_by_zero` (same site) | same | exit 0 `Defined {value: "Specified(1)", stdout: "", stderr: "", blocked: "false"}` | **BUG-FIX** (Lean DEFINED where the oracle crashes — the worst class) | immaculate `ORACLE_CRASH` pair |
| `aligned_alloc_zero_nolibc.c` | nolibc | same proxy through the ailname redirect | exit 125 `Division_by_zero` | same | exit 1 `Undefined {ub: "DUMMY(align_alloc)", stderr: "", loc: "unknown location"}` | same row (mode-independent witness) | reporting-only |
| `aligned_alloc_bad_size.c` | libc | CONTROL: `undef(<<DUMMY(align_alloc)>>)` rendering | `Undefined {ub: "DUMMY(align_alloc)", stderr: "", loc: "<5:28--5:48>"}` | same | `Undefined {ub: "DUMMY(align_alloc)", stderr: "", loc: "unknown location"}` | AGREE on ub (loc is Z-01) | exec libc_exec UB_MATCH |
| `aligned_alloc_3.c` | libc | CONTROL: non-power-of-two align through `allocator` quomod/erem vs `alignDown` | `Defined {value: "Specified(11)", stdout: "", stderr: "", blocked: "false"}` | same | same | AGREE | libc_exec MATCH |
| `free_funptr.c` | libc | `free((void*)main)`: the void* conversion yields a Prov_none concrete pointer, so this is the Z-07 (D6) Prov_none arm again (the PVfunction arm of `kill` is unreachable from C: a function pointer stored/loaded through `void*` loses its PVfunction shape in `repr`/`abst`) | exit 1 `Error {msg: "MerrOther "attempted to kill with a pointer lacking a provenance""}` | same | exit 1 `Undefined {ub: "UB179a_non_matching_allocation_free", stderr: "", loc: "unknown location"}` | **BUG-FIX** = Z-07 witness | immaculate DIFF pair (Lean pin) → MATCH after Z-07 |
| `device_funptr_call.c` | nolibc | `((void(*)(void))0xABC)()`: 0xABC ∈ `device_ranges` → oracle mints Prov_device (impl_mem.ml:2164-2167); `Eccall` → `case_ptrval` `\| _ -> failwith "case_ptrval"` (:1814). Lean `casePtrval` has a fail-OPEN `onConcrete none addr` fallback (CerbMem.lean:1241) but never mints Prov_device today (Z-06) | exit 125 `Failure("case_ptrval")` `Called from Cerb_frontend__Core_eval.step_eval_pexpr` | same | exit 1 `Error {msg: "Illformed_program: tests/z2-probes/mem/device_funptr_call.c:9:18-43: does not point to a function"}` | **BUG-FIX** (failure-class: crash vs Error; Z-06's device mirror MUST land together with a `casePtrval` fail-stop mirror, else the fallback becomes a Defined-vs-crash divergence) | immaculate `ORACLE_CRASH` pair |
| `empty_struct.c` | nolibc | GNU empty struct → `alignof` 0 → OCaml `modulus max_offset 0` Division_by_zero vs Lean `% 0` | `Undefined {ub: "UB061_no_named_members", stderr: "", loc: "<6:1--6:12>"}` | same | `Undefined {ub: "UB061_no_named_members", stderr: "", loc: "tests/z2-probes/mem/empty_struct.c:6:1-12"}` | AGREE — REFUTES reachability: the shared front end rejects the shape (UB061), so the `% 0` normalisations in the layout family are unreachable-by-construction (declare) | exec nolibc UB_MATCH |
| `funptr_noparams_deref.c` | nolibc | `isWellAligned_ptrval` FunctionNoParams arm (OCaml falls to `alignof` `assert false`, Lean fails MerrOther — Z-21) via `(*fp)()` | exit 1 `error: constraint violation: called object type is not a function or function pointer` (front end) | same | exit 1 `Error {msg: "typechecking failed at tests/z2-probes/mem/funptr_noparams_deref.c:9:42-49"}` | both-reject — REFUTES reachability via the call shape | reporting-only (reject control; Z-74 evidence row) |
| `funptr_noparams_deref2.c` | nolibc | second shape `int (*g)() = *fp` | exit 1 `constraint violation: initializing 'signed int (*)()' with an expression of incompatible type 'function (NO PARAMS) returning signed int'` | same | exit 1 `Error {msg: "typechecking failed at …:6:48-51"}` | both-reject — the FunctionNoParams arm stays UNSETTLED (no accepted shape found) | reporting-only |
| `enum_underlying.c` | nolibc | `typeof_enum` registry vs Lean at sizeof/max_ival | exit 1 `constraint violation: integer constant not in the range of the representable values for its type` (enumerator > INT_MAX rejected, §6.6#4) | same | exit 1 `Error {msg: "desugaring failed at …:5:14-25"}` | both-reject (the GCC wide-enum extension is not admitted by the shared front end) | reporting-only |
| `enum_conv.c` | nolibc | `(enum E)4294967295u > 0` — unsigned-int underlying type via the registry (GCC rule) | `Defined {value: "Specified(1)", stdout: "", stderr: "", blocked: "false"}` | same | same | AGREE (CerberusImpl.typeof_enum is a real registry mirror, not the stub CerbMem.lean:1257-1259 still describes) | exec nolibc MATCH |
| `unspec_const_ptr.c` | nolibc | Z-19 (unspecified pointer ctype keeps qualifiers) — can the ctype text reach the verdict? | `Defined {value: "Unspecified('signed int')", stdout: "", stderr: "", blocked: "false"}` | same | same | AGREE — the cast's type wins; Z-19's text is not verdict-reachable by this route | exec nolibc MATCH |
| `atomic_member_stderr.c` | nolibc | `is_atomic_member_access` (impl_mem.ml:698-702) prints two diagnostic lines to the TOOL's stderr | `addr: 281474976710644 <--> alloc.base: 281474976710644` / `\|lvalue_ty\|: 4 <--> \|alloc\|: 8` / `Undefined {ub: "UB042_access_atomic_structUnion_member", stderr: "", loc: "<7:25--7:28>"}` | same | `Undefined {ub: "UB042_access_atomic_structUnion_member", stderr: "", loc: "tests/z2-probes/mem/atomic_member_stderr.c:7:25-28"}` | AGREE on the verdict; the two extra oracle lines are tool stderr (not the program's `stderr:` field) — INSTRUMENT note for any lane that byte-compares whole stderr | exec nolibc UB_MATCH |
| `ptr_lt_null.c` | nolibc | CONTROL: `lt_ptrval` null-arm MerrWIP text | `Error {msg: "Memory WIP: lt_ptrval ==> one null pointer"}` | same | same | AGREE | exec nolibc MATCH (Error class) |
| `malloc_oom_msg.c` | libc | allocator out-of-memory MESSAGE (4 × `malloc(1ULL<<46)`) — ORACLES ONLY (`--nolean`: Lean would materialise 2^46 bytes per region, the Z-30 OOM) | `Error {msg: "MerrOther "Concrete.allocator: failed (out of memory)""}` | same | not run | EXC(a) message text: Lean's text is `MerrOther "out of memory"` (CerbMem.lean:1861/1889) — mirror (S). Side note for Z-30: `allocate_region` writes NO bytemap bytes upstream (impl_mem.ml:1420-1435) while `allocateRegion` eagerly writes `List.replicate size` unspecified bytes | reporting-only until Z-30 |

Derived tallies: 16 probe runs; 5 LEAN≠ORACLE-class rows (`aligned_alloc_zero`,
`aligned_alloc_zero_zero`, `aligned_alloc_zero_nolibc`, `free_funptr`,
`device_funptr_call`) — 3 distinct findings; 8 AGREE (incl. 3 controls);
4 both-reject (reachability refuted or unsettled); 1 oracle-only.
