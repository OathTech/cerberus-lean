# fs/ — CerbFS.lean residual cross-check probes (Z2, 2026-09-03)

Z1 owns the op-by-op served/refused table (charter Z-27); these two probes
only pin residuals the charter's list MISSES (reader D's findings, probed).
Engines/binaries as in `../mem/README.md`; libc mode. Classes [AGENT].

| Probe | What it tests | fork oracle | upstream | Lean | Class | Proposed lane |
|---|---|---|---|---|---|---|
| `lseek_whence.c` | `CerbFS.lean:311-318` `fs_lseek`: an invalid `whence` (7) falls to `\| _ => entry.offset` = SUCCESS with the current offset; POSIX/SibylFS → `EINVAL` (-1) | `Defined {value: "Specified(9)", stdout: "", stderr: "", blocked: "false"}` (-1 + 10) | same | `Defined {value: "Specified(13)", stdout: "", stderr: "", blocked: "false"}` (3 + 10: the current offset) | **BUG-FIX** — a silent-absorption arm NOT on the charter's Z-27 residual list; add to Z1's table: `\| _ => (st, .inl (.other "EINVAL"))` | immaculate DIFF pair (libc) → MATCH after Z-27 |
| `closedir.c` | `CerbFS.lean:335-345`: `fs_opendir` never registers the fd, `fs_closedir = fs_close` → EBADF | exit 125 `Failure("internal error: can_advance: Step_error2 ==> tests/z2-probes/fs/closedir.c:4:18-40the value of a store(signed int*) didn't match the lvalue type: Specified(1)")` | same | exit 134 `PANIC at _private.LemLib.0.failwithIImpl LemLib:158:2: can_advance: Step_error2 ==> tests/z2-probes/fs/closedir.c:4:18-40the value of a store(signed int*) didn't match the lvalue type: Specified(3)` | both-crash (EXC(a); the embedded fd number differs, 1 vs 3 — the fd numbering itself is an fs-model literal, `CerbFS.lean:100 nextFd := 3`, vs SibylFS's — a Z-76-shape value for Z1's table). The EBADF outcome is NOT reachable through `opendir`/`closedir` from C: the libc's `DIR*` handling crashes both engines first | immaculate `MATCH \| L=CRASH` pair |

Residuals reader D found that the charter's Z-27 list does not name (for Z1's table): `fs_lseek` invalid whence (probed above); `fs_lseek` `SEEK_END` on an fd whose path no longer resolves (`:314-317` falls back to `entry.offset + offset`); `fs_write`/`fs_pwrite` `(lookupFile st entry.path).getD []` (`:187,:231`) — a write after `unlink` RE-CREATES the path, a read after `unlink` → ENOENT (POSIX keeps the inode open); `fs_opendir` fd not registered (`:335-337`); `fs_write` ignores its `size` argument (`:182`; SibylFS bounds by size — settles only if `|buf| ≠ size` can arise at `core_run.lem:1240-1245`); `fs_umask` stores the raw int, initial `0o022` literal (`:269-271,:102`); `fs_unlink` of a missing path returns 0 (`:301`; POSIX ENOENT); header `:44-45` says "zeroed fields except size" but `:70-71` are `mode := 0o644`, `nlink := 1`.
