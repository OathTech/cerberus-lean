proc lookup_size_shift (sz: pointer): eff loaded integer :=
  let strong a_833: loaded integer =
    bound(
      let weak a_832: pointer = pure(sz) in
      load('enum size', a_832)
    ) in
  case a_833 of
    | Specified(a_834: integer) =>
        let strong a_835: integer = pure(conv_int('enum size', a_834)) in
        if a_835 = 0 then run case_838() else pure(Unit) ;
        if a_835 = 1 then run case_837() else pure(Unit) ;
        if a_835 = 2 then run case_836() else pure(Unit) ;
        run default_839() ;
        run break_831() ;
        save case_838: unit () in
          let strong a_840: loaded integer = bound(pure(Specified(12))) in
          run ret_829(conv_loaded_int('signed int', a_840)) ;
        save case_837: unit () in
          let strong a_841: loaded integer = bound(pure(Specified(12))) in
          run ret_829(conv_loaded_int('signed int', a_841)) ;
        save case_836: unit () in
          let strong a_842: loaded integer = bound(pure(Specified(2))) in
          run ret_829(conv_loaded_int('signed int', a_842)) ;
        save default_839: unit () in
          let strong a_843: loaded integer = bound(pure(Specified(0))) in
          run ret_829(conv_loaded_int('signed int', a_843)) ;
        pure(Unit) ;
        pure(Unit)
    | Unspecified(_: ctype) =>
        pure(undef(<<UB036_exceptional_condition>>))
  end ;
  save break_831: unit () in  pure(Unit) ;
  pure(Unit) ;
  pure(Unit) ;
  save ret_829: loaded integer (a_844: loaded integer:= undef(<<UB088_reached_end_of_function>>)) in
    pure(a_844)

proc main (): eff loaded integer :=
  let strong choices: pointer =
    create(Ivalignof('unsigned char[4]'), 'unsigned char[4]') in
  let strong expected: pointer =
    create(Ivalignof('unsigned char[4]'), 'unsigned char[4]') in
  let strong out: pointer =
    create(Ivalignof('unsigned char[4]'), 'unsigned char[4]') in
  let strong i: pointer = create(Ivalignof('unsigned long'), 'unsigned long') in
  let strong acc: pointer = create(Ivalignof('signed long'), 'signed long') in
  let strong v: pointer = create(Ivalignof('signed int'), 'signed int') in
  let strong r: pointer = create(Ivalignof('signed int'), 'signed int') in
  let strong a_545: loaded array(integer) =
    bound(
      let weak (a_549: loaded integer, a_548: loaded integer,
      a_547: loaded integer, a_546: loaded integer) =
        unseq(
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(1))
        ) in
      pure(
        Specified(Array(conv_loaded_int('unsigned char', a_546),
        conv_loaded_int('unsigned char', a_547),
        conv_loaded_int('unsigned char', a_548),
        conv_loaded_int('unsigned char', a_549)))
      )
    ) in
  store_lock('unsigned char[4]', choices, a_545) ;
  let strong a_550: loaded array(integer) =
    bound(
      let weak (a_554: loaded integer, a_553: loaded integer,
      a_552: loaded integer, a_551: loaded integer) =
        unseq(
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(8))
        ) in
      pure(
        Specified(Array(conv_loaded_int('unsigned char', a_551),
        conv_loaded_int('unsigned char', a_552),
        conv_loaded_int('unsigned char', a_553),
        conv_loaded_int('unsigned char', a_554)))
      )
    ) in
  store_lock('unsigned char[4]', expected, a_550) ;
  store('unsigned char[4]', out, Unspecified('unsigned ichar[4]')) ;
  store('unsigned long', i, Unspecified('unsigned long')) ;
  store('signed long', acc, Unspecified('signed long')) ;
  store('signed int', v, Unspecified('signed int')) ;
  store('signed int', r, Unspecified('signed int')) ;
  let strong a_556: loaded integer =
    bound(
      let weak (a_558: loaded integer, a_559: loaded integer) =
        unseq(
          let weak (a_563: loaded integer, a_564: loaded integer) =
            unseq(
              pure(Specified(Ivsizeof('unsigned char[4]')))
            ,
              pure(Specified(4))
            ) in
          pure(
            case (a_563, a_564) of
              | (Specified(a_565: integer), Specified(a_566: integer)) =>
                  if not(conv_int('size_t', a_565) = conv_int('size_t', a_566)) then
                    Specified(1)
                  else
                    Specified(0)
              | _: (loaded integer,loaded integer) =>
                  Unspecified('signed int')
            end
          )
        ,
          pure(Specified(0))
        ) in
      pure(
        case (a_558, a_559) of
          | (Specified(a_560: integer), Specified(a_561: integer)) =>
              if conv_int('signed int', a_560) = conv_int('signed int', a_561) then
                Specified(1)
              else
                Specified(0)
          | _: (loaded integer,loaded integer) =>
              Unspecified('signed int')
        end
      )
    ) in
  let strong a_555: boolean =
    case a_556 of
      | Specified(a_557: integer) =>
          pure(if not(a_557 = 1) then True else False)
      | Unspecified(_: ctype) =>
          nd(pure(True), pure(False))
    end in
  if a_555 then
    let strong a_568: loaded integer = bound(pure(Specified(254))) in
    kill('unsigned ichar[4]', choices) ;
    kill('unsigned ichar[4]', expected) ;
    kill('unsigned ichar[4]', out) ;
    kill('unsigned long', i) ;
    kill('signed long', acc) ;
    kill('signed int', v) ;
    kill('signed int', r) ;
    run ret_536(conv_loaded_int('signed int', a_568)) ;
    pure(Unit)
  else
    pure(Unit) ;
  let strong _: loaded integer =
    bound(
      let weak (a_569: pointer, a_570: loaded integer) =
        unseq(pure(acc), pure(Specified(0))) in
      let weak _: unit =
        neg(store('signed long', a_569, conv_loaded_int('signed long', a_570))) in
      pure(conv_loaded_int('signed long', a_570))
    ) in
  pure(Unit) ;
  let strong _: loaded integer =
    bound(
      let weak (a_571: pointer, a_572: loaded integer) =
        unseq(pure(i), pure(Specified(0))) in
      let weak _: unit =
        neg(store('unsigned long', a_571,
        conv_loaded_int('unsigned long', a_572))) in
      pure(conv_loaded_int('unsigned long', a_572))
    ) in
  pure(Unit) ;
  save while_573: unit (choices: pointer:= choices, expected: pointer:= expected,
  out: pointer:= out, i: pointer:= i, acc: pointer:= acc, v: pointer:= v,
  r: pointer:= r) in
    let strong a_575: loaded integer =
      bound(
        let weak (a_577: loaded integer, a_578: loaded integer) =
          unseq(
            let weak (a_583: loaded integer, a_584: loaded integer) =
              unseq(
                let weak a_582: pointer = pure(i) in
                load('unsigned long', a_582)
              ,
                pure(Specified(4))
              ) in
            case (a_583, a_584) of
              | (Specified(a_585: integer), Specified(a_586: integer)) =>
                  pure(
                    if conv_int('unsigned long', a_585) <
                    conv_int('unsigned long', a_586) then
                      Specified(1)
                    else
                      Specified(0)
                  )
              | _: (loaded integer,loaded integer) =>
                  pure(Unspecified('signed int'))
            end
          ,
            pure(Specified(0))
          ) in
        pure(
          case (a_577, a_578) of
            | (Specified(a_579: integer), Specified(a_580: integer)) =>
                if conv_int('signed int', a_579) = conv_int('signed int', a_580) then
                  Specified(1)
                else
                  Specified(0)
            | _: (loaded integer,loaded integer) =>
                Unspecified('signed int')
          end
        )
      ) in
    let strong a_574: boolean =
      case a_575 of
        | Specified(a_576: integer) =>
            pure(if not(a_576 = 1) then True else False)
        | Unspecified(_: ctype) =>
            nd(pure(True), pure(False))
      end in
    if a_574 then
      let strong _: loaded integer =
        bound(
          let weak (a_588: pointer, a_623: loaded integer) =
            unseq(
              pure(acc)
            ,
              let weak (a_589: loaded integer, a_590: loaded integer) =
                unseq(
                  let weak a_594: pointer = pure(acc) in
                  load('signed long', a_594)
                ,
                  let weak (a_595: loaded integer, a_596: loaded integer) =
                    unseq(
                      let weak a_602: loaded integer =
                        let weak a_615: pointer =
                          let weak a_604: loaded pointer =
                            let weak (a_607: loaded pointer,
                            a_609: loaded integer) =
                              unseq(
                                let weak a_612: pointer = pure(choices) in
                                pure(
                                  Specified(array_shift(a_612, 'unsigned ichar', 0))
                                )
                              ,
                                let weak a_614: pointer = pure(i) in
                                load('unsigned long', a_614)
                              ) in
                            case (a_607, a_609) of
                              | (Specified(a_608: pointer),
                              Specified(a_610: integer)) =>
                                  pure(
                                    Specified(array_shift(a_608, 'unsigned ichar', a_610))
                                  )
                              | _: (loaded pointer,loaded integer) =>
                                  pure(
                                    undef(<<UB_CERB004_unspecified__pointer_add>>)
                                  )
                            end in
                          case a_604 of
                            | Specified(a_605: pointer) =>
                                let weak a_606: boolean =
                                  memop(PtrValidForDeref, 'unsigned char', a_605) in
                                pure(
                                  if a_606 then
                                    a_605
                                  else
                                    undef(<<UB043_indirection_invalid_value>>)
                                )
                            | Unspecified(_: ctype) =>
                                pure(undef(<<UB043_indirection_invalid_value>>))
                          end in
                        load('unsigned char', a_615) in
                      pure(conv_loaded_int('signed long', a_602))
                    ,
                      let weak (a_618: loaded integer, a_619: loaded integer) =
                        unseq(
                          pure(Specified(8))
                        ,
                          let weak a_617: pointer = pure(i) in
                          load('unsigned long', a_617)
                        ) in
                      pure(
                        case (a_618, a_619) of
                          | (Specified(a_620: integer),
                          Specified(a_621: integer)) =>
                              Specified(wrapI_mul('unsigned long', conv_int('unsigned long',
                              a_620), conv_int('unsigned long', a_621)))
                          | _: (loaded integer,loaded integer) =>
                              Unspecified('unsigned long')
                        end
                      )
                    ) in
                  pure(
                    case (a_595, a_596) of
                      | (_: loaded integer, Unspecified(_: ctype)) =>
                          undef(<<UB036_exceptional_condition>>)
                      | (Unspecified(_: ctype), _: loaded integer) =>
                          undef(<<UB036_exceptional_condition>>)
                      | (Specified(a_597: integer), Specified(a_598: integer)) =>
                          let a_599: integer = conv_int('signed long', a_597) in
                          let a_600: integer =
                            conv_int('unsigned long', a_598) in
                          if a_600 < 0 then
                            undef(<<UB051a_negative_shift>>)
                          else
                            if ctype_width('signed long') < a_600 \/
                            ctype_width('signed long') = a_600 then
                              undef(<<UB51b_shift_too_large>>)
                            else
                              if a_599 < 0 then
                                undef(<<UB052a_negative_left_shift>>)
                              else
                                let a_601: integer = a_599 * 2 ^ a_600 in
                                if is_representable_integer(a_601, 'signed long') then
                                  Specified(a_601)
                                else
                                  undef(<<UB052b_non_representable_left_shift>>)
                    end
                  )
                ) in
              pure(
                case (a_589, a_590) of
                  | (Specified(a_591: integer), Specified(a_592: integer)) =>
                      Specified(catch_exceptional_condition_add('signed long', __conv_int__('signed long', a_591), __conv_int__('signed long', a_592)))
                  | _: (loaded integer,loaded integer) =>
                      undef(<<UB036_exceptional_condition>>)
                end
              )
            ) in
          let weak _: unit =
            neg(store('signed long', a_588,
            conv_loaded_int('signed long', a_623))) in
          pure(conv_loaded_int('signed long', a_623))
        ) in
      pure(Unit) ;
      save __cerb_continue0: unit (choices: pointer:= choices,
      expected: pointer:= expected, out: pointer:= out, i: pointer:= i,
      acc: pointer:= acc, v: pointer:= v, r: pointer:= r) in
        let strong _: loaded integer =
          bound(
            let weak a_624: pointer = pure(i) in
            seq_rmw('unsigned long', a_624, a_625 => case a_625 of
              | Specified(a_626: integer) =>
                  Specified(conv_int('unsigned long',
                  wrapI_add('unsigned long', conv_int('unsigned long', a_626), 1)))
              | Unspecified(_: ctype) =>
                  Unspecified('unsigned long')
            end)
          ) in
        pure(Unit) ;
      pure(Unit) ;
      save continue_537: unit (choices: pointer:= choices,
      expected: pointer:= expected, out: pointer:= out, i: pointer:= i,
      acc: pointer:= acc, v: pointer:= v, r: pointer:= r) in
        pure(Unit) ;
      pure(Unit) ;
      run while_573(choices, expected, out, i, acc, v, r)
    else
      pure(Unit) ;
  save break_538: unit (choices: pointer:= choices, expected: pointer:= expected,
  out: pointer:= out, i: pointer:= i, acc: pointer:= acc, v: pointer:= v,
  r: pointer:= r) in
    pure(Unit) ;
  pure(Unit) ;
  let strong a_628: loaded integer =
    bound(
      let weak (a_630: loaded integer, a_631: loaded integer) =
        unseq(
          let weak (a_636: loaded integer, a_637: loaded integer) =
            unseq(
              let weak a_635: pointer = pure(acc) in
              load('signed long', a_635)
            ,
              pure(Specified(2147483648))
            ) in
          case (a_636, a_637) of
            | (Specified(a_638: integer), Specified(a_639: integer)) =>
                pure(
                  if conv_int('signed long', a_638) >=
                  conv_int('signed long', a_639) then
                    Specified(1)
                  else
                    Specified(0)
                )
            | _: (loaded integer,loaded integer) =>
                pure(Unspecified('signed int'))
          end
        ,
          pure(Specified(0))
        ) in
      pure(
        case (a_630, a_631) of
          | (Specified(a_632: integer), Specified(a_633: integer)) =>
              if conv_int('signed int', a_632) = conv_int('signed int', a_633) then
                Specified(1)
              else
                Specified(0)
          | _: (loaded integer,loaded integer) =>
              Unspecified('signed int')
        end
      )
    ) in
  let strong a_627: boolean =
    case a_628 of
      | Specified(a_629: integer) =>
          pure(if not(a_629 = 1) then True else False)
      | Unspecified(_: ctype) =>
          nd(pure(True), pure(False))
    end in
  if a_627 then
    let strong a_641: loaded integer = bound(pure(Specified(254))) in
    kill('unsigned ichar[4]', choices) ;
    kill('unsigned ichar[4]', expected) ;
    kill('unsigned ichar[4]', out) ;
    kill('unsigned long', i) ;
    kill('signed long', acc) ;
    kill('signed int', v) ;
    kill('signed int', r) ;
    run ret_536(conv_loaded_int('signed int', a_641)) ;
    pure(Unit)
  else
    pure(Unit) ;
  let strong _: loaded integer =
    bound(
      let weak (a_642: pointer, a_647: loaded integer) =
        unseq(
          pure(v)
        ,
          let weak a_643: loaded integer =
            let weak a_645: pointer = pure(acc) in
            load('signed long', a_645) in
          pure(conv_loaded_int('signed int', a_643))
        ) in
      let weak _: unit =
        neg(store('signed int', a_642, conv_loaded_int('signed int', a_647))) in
      pure(conv_loaded_int('signed int', a_647))
    ) in
  pure(Unit) ;
  let strong _: loaded integer =
    bound(
      let weak (a_648: pointer, a_664: loaded integer) =
        unseq(
          pure(r)
        ,
          let strong ((a_650: loaded pointer,
          (a_651: ctype, a_652: [ctype], a_653: boolean, a_654: boolean)),
          a_656: loaded integer) =
            unseq(
              let strong a_649: loaded pointer =
                pure(Specified(Cfunction(lookup_size_shift))) in
              pure((a_649, cfunction(a_649)))
            ,
              let weak a_657: loaded integer =
                let weak a_659: pointer = pure(v) in
                load('signed int', a_659) in
              pure(conv_loaded_int('enum size', a_657))
            ) in
          if not(params_length(a_652) = 1) then
            pure(undef(<<UB038_number_of_args>>))
          else
            if a_653 \/ not(are_compatible ('signed int', a_651)) then
              pure(undef(<<UB041_function_not_compatible>>))
            else
              let strong a_655: pointer =
                let a_661: ctype = params_nth(a_652, 0) in
                if not(are_compatible ('enum size', a_661)) then
                  pure(undef(<<UB041_function_not_compatible>>))
                else
                  let weak a_662: pointer =
                    create(Ivalignof('enum size'), 'enum size') in
                  let weak _: unit =
                    store('enum size', a_662,
                    conv_loaded_int('enum size', a_656)) in
                  pure(a_662) in
              let strong a_663: loaded integer =
                ccall('signed int (*) (enum size)', a_650, a_655) in
              kill('enum size', a_655) ;
              pure(a_663)
        ) in
      let weak _: unit =
        neg(store('signed int', a_648, conv_loaded_int('signed int', a_664))) in
      pure(conv_loaded_int('signed int', a_664))
    ) in
  pure(Unit) ;
  let strong _: loaded integer =
    bound(
      let weak (a_665: pointer, a_670: loaded integer) =
        unseq(
          pure(acc)
        ,
          let weak a_666: loaded integer =
            let weak a_668: pointer = pure(r) in
            load('signed int', a_668) in
          pure(conv_loaded_int('signed long', a_666))
        ) in
      let weak _: unit =
        neg(store('signed long', a_665, conv_loaded_int('signed long', a_670))) in
      pure(conv_loaded_int('signed long', a_670))
    ) in
  pure(Unit) ;
  let strong a_672: loaded integer =
    bound(
      let weak (a_674: loaded integer, a_675: loaded integer) =
        unseq(
          let weak (a_680: loaded integer, a_681: loaded integer) =
            unseq(
              let weak a_679: pointer = pure(acc) in
              load('signed long', a_679)
            ,
              pure(Specified(0))
            ) in
          case (a_680, a_681) of
            | (Specified(a_682: integer), Specified(a_683: integer)) =>
                pure(
                  if conv_int('signed long', a_682) <
                  conv_int('signed long', a_683) then
                    Specified(1)
                  else
                    Specified(0)
                )
            | _: (loaded integer,loaded integer) =>
                pure(Unspecified('signed int'))
          end
        ,
          pure(Specified(0))
        ) in
      pure(
        case (a_674, a_675) of
          | (Specified(a_676: integer), Specified(a_677: integer)) =>
              if conv_int('signed int', a_676) = conv_int('signed int', a_677) then
                Specified(1)
              else
                Specified(0)
          | _: (loaded integer,loaded integer) =>
              Unspecified('signed int')
        end
      )
    ) in
  let strong a_671: boolean =
    case a_672 of
      | Specified(a_673: integer) =>
          pure(if not(a_673 = 1) then True else False)
      | Unspecified(_: ctype) =>
          nd(pure(True), pure(False))
    end in
  if a_671 then
    let strong _: loaded integer =
      bound(
        let weak (a_685: pointer, a_692: loaded integer) =
          unseq(
            pure(acc)
          ,
            let weak (a_686: loaded integer, a_687: loaded integer) =
              unseq(
                let weak a_691: pointer = pure(acc) in
                load('signed long', a_691)
              ,
                pure(Specified(4294967296))
              ) in
            pure(
              case (a_686, a_687) of
                | (Specified(a_688: integer), Specified(a_689: integer)) =>
                    Specified(catch_exceptional_condition_add('signed long', __conv_int__('signed long', a_688), __conv_int__('signed long', a_689)))
                | _: (loaded integer,loaded integer) =>
                    undef(<<UB036_exceptional_condition>>)
              end
            )
          ) in
        let weak _: unit =
          neg(store('signed long', a_685, conv_loaded_int('signed long', a_692))) in
        pure(conv_loaded_int('signed long', a_692))
      ) in
    pure(Unit) ;
    pure(Unit)
  else
    pure(Unit) ;
  let strong _: loaded integer =
    bound(
      let weak (a_693: pointer, a_694: loaded integer) =
        unseq(pure(i), pure(Specified(0))) in
      let weak _: unit =
        neg(store('unsigned long', a_693,
        conv_loaded_int('unsigned long', a_694))) in
      pure(conv_loaded_int('unsigned long', a_694))
    ) in
  pure(Unit) ;
  save while_695: unit (choices: pointer:= choices, expected: pointer:= expected,
  out: pointer:= out, i: pointer:= i, acc: pointer:= acc, v: pointer:= v,
  r: pointer:= r) in
    let strong a_697: loaded integer =
      bound(
        let weak (a_699: loaded integer, a_700: loaded integer) =
          unseq(
            let weak (a_705: loaded integer, a_706: loaded integer) =
              unseq(
                let weak a_704: pointer = pure(i) in
                load('unsigned long', a_704)
              ,
                pure(Specified(4))
              ) in
            case (a_705, a_706) of
              | (Specified(a_707: integer), Specified(a_708: integer)) =>
                  pure(
                    if conv_int('unsigned long', a_707) <
                    conv_int('unsigned long', a_708) then
                      Specified(1)
                    else
                      Specified(0)
                  )
              | _: (loaded integer,loaded integer) =>
                  pure(Unspecified('signed int'))
            end
          ,
            pure(Specified(0))
          ) in
        pure(
          case (a_699, a_700) of
            | (Specified(a_701: integer), Specified(a_702: integer)) =>
                if conv_int('signed int', a_701) = conv_int('signed int', a_702) then
                  Specified(1)
                else
                  Specified(0)
            | _: (loaded integer,loaded integer) =>
                Unspecified('signed int')
          end
        )
      ) in
    let strong a_696: boolean =
      case a_697 of
        | Specified(a_698: integer) =>
            pure(if not(a_698 = 1) then True else False)
        | Unspecified(_: ctype) =>
            nd(pure(True), pure(False))
      end in
    if a_696 then
      let strong _: loaded integer =
        bound(
          let weak (a_710: pointer, a_732: loaded integer) =
            unseq(
              let weak a_711: loaded pointer =
                let weak (a_714: loaded pointer, a_716: loaded integer) =
                  unseq(
                    let weak a_719: pointer = pure(out) in
                    pure(Specified(array_shift(a_719, 'unsigned ichar', 0)))
                  ,
                    let weak a_721: pointer = pure(i) in
                    load('unsigned long', a_721)
                  ) in
                case (a_714, a_716) of
                  | (Specified(a_715: pointer), Specified(a_717: integer)) =>
                      pure(
                        Specified(array_shift(a_715, 'unsigned ichar', a_717))
                      )
                  | _: (loaded pointer,loaded integer) =>
                      pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
                end in
              case a_711 of
                | Specified(a_712: pointer) =>
                    let weak a_713: boolean =
                      memop(PtrValidForDeref, 'unsigned char', a_712) in
                    pure(
                      if a_713 then
                        a_712
                      else
                        undef(<<UB043_indirection_invalid_value>>)
                    )
                | Unspecified(_: ctype) =>
                    pure(undef(<<UB043_indirection_invalid_value>>))
              end
            ,
              let weak a_722: loaded integer =
                let weak (a_725: loaded integer, a_726: loaded integer) =
                  unseq(
                    let weak a_724: pointer = pure(acc) in
                    load('signed long', a_724)
                  ,
                    pure(Specified(256))
                  ) in
                pure(
                  case (a_725, a_726) of
                    | (Unspecified(_: ctype), Specified(a_728: integer)) =>
                        let a_730: integer = conv_int('signed long', a_728) in
                        if a_730 = 0 then
                          undef(<<UB045b_modulo_by_zero>>)
                        else
                          undef(<<UB036_exceptional_condition>>)
                    | (_: loaded integer, Unspecified(_: ctype)) =>
                        undef(<<UB045b_modulo_by_zero>>)
                    | (Specified(a_727: integer), Specified(a_728: integer)) =>
                        let a_729: integer = conv_int('signed long', a_727) in
                        let a_730: integer = conv_int('signed long', a_728) in
                        if a_730 = 0 then
                          undef(<<UB045b_modulo_by_zero>>)
                        else
                          if is_representable_integer(a_729 / a_730,
                          'signed long') then
                            Specified(a_729 rem_t a_730)
                          else
                            undef(<<UB045c_quotient_not_representable>>)
                  end
                ) in
              pure(conv_loaded_int('unsigned char', a_722))
            ) in
          let weak _: unit =
            neg(store('unsigned char', a_710,
            conv_loaded_int('unsigned char', a_732))) in
          pure(conv_loaded_int('unsigned char', a_732))
        ) in
      pure(Unit) ;
      let strong _: loaded integer =
        bound(
          let weak (a_733: pointer, a_741: loaded integer) =
            unseq(
              pure(acc)
            ,
              let weak (a_735: loaded integer, a_736: loaded integer) =
                unseq(
                  let weak a_734: pointer = pure(acc) in
                  load('signed long', a_734)
                ,
                  pure(Specified(256))
                ) in
              pure(
                case (a_735, a_736) of
                  | (Unspecified(_: ctype), Specified(a_738: integer)) =>
                      let a_740: integer = conv_int('signed long', a_738) in
                      if a_740 = 0 then
                        undef(<<UB045a_division_by_zero>>)
                      else
                        undef(<<UB036_exceptional_condition>>)
                  | (_: loaded integer, Unspecified(_: ctype)) =>
                      undef(<<UB045a_division_by_zero>>)
                  | (Specified(a_737: integer), Specified(a_738: integer)) =>
                      let a_739: integer = conv_int('signed long', a_737) in
                      let a_740: integer = conv_int('signed long', a_738) in
                      if a_740 = 0 then
                        undef(<<UB045a_division_by_zero>>)
                      else
                        if is_representable_integer(a_739 / a_740, 'signed long') then
                          Specified(a_739 / a_740)
                        else
                          undef(<<UB045c_quotient_not_representable>>)
                end
              )
            ) in
          let weak _: unit =
            neg(store('signed long', a_733,
            conv_loaded_int('signed long', a_741))) in
          pure(conv_loaded_int('signed long', a_741))
        ) in
      pure(Unit) ;
      save __cerb_continue1: unit (choices: pointer:= choices,
      expected: pointer:= expected, out: pointer:= out, i: pointer:= i,
      acc: pointer:= acc, v: pointer:= v, r: pointer:= r) in
        let strong _: loaded integer =
          bound(
            let weak a_742: pointer = pure(i) in
            seq_rmw('unsigned long', a_742, a_743 => case a_743 of
              | Specified(a_744: integer) =>
                  Specified(conv_int('unsigned long',
                  wrapI_add('unsigned long', conv_int('unsigned long', a_744), 1)))
              | Unspecified(_: ctype) =>
                  Unspecified('unsigned long')
            end)
          ) in
        pure(Unit) ;
      pure(Unit) ;
      save continue_539: unit (choices: pointer:= choices,
      expected: pointer:= expected, out: pointer:= out, i: pointer:= i,
      acc: pointer:= acc, v: pointer:= v, r: pointer:= r) in
        pure(Unit) ;
      pure(Unit) ;
      run while_695(choices, expected, out, i, acc, v, r)
    else
      pure(Unit) ;
  save break_540: unit (choices: pointer:= choices, expected: pointer:= expected,
  out: pointer:= out, i: pointer:= i, acc: pointer:= acc, v: pointer:= v,
  r: pointer:= r) in
    pure(Unit) ;
  pure(Unit) ;
  let strong a_746: loaded integer =
    bound(
      let weak (a_748: loaded integer, a_749: loaded integer) =
        unseq(
          let weak (a_753: loaded integer, a_754: loaded integer) =
            unseq(
              pure(Specified(Ivsizeof('unsigned char[4]')))
            ,
              pure(Specified(4))
            ) in
          pure(
            case (a_753, a_754) of
              | (Specified(a_755: integer), Specified(a_756: integer)) =>
                  if not(conv_int('size_t', a_755) = conv_int('size_t', a_756)) then
                    Specified(1)
                  else
                    Specified(0)
              | _: (loaded integer,loaded integer) =>
                  Unspecified('signed int')
            end
          )
        ,
          pure(Specified(0))
        ) in
      pure(
        case (a_748, a_749) of
          | (Specified(a_750: integer), Specified(a_751: integer)) =>
              if conv_int('signed int', a_750) = conv_int('signed int', a_751) then
                Specified(1)
              else
                Specified(0)
          | _: (loaded integer,loaded integer) =>
              Unspecified('signed int')
        end
      )
    ) in
  let strong a_745: boolean =
    case a_746 of
      | Specified(a_747: integer) =>
          pure(if not(a_747 = 1) then True else False)
      | Unspecified(_: ctype) =>
          nd(pure(True), pure(False))
    end in
  if a_745 then
    let strong a_758: loaded integer = bound(pure(Specified(255))) in
    kill('unsigned ichar[4]', choices) ;
    kill('unsigned ichar[4]', expected) ;
    kill('unsigned ichar[4]', out) ;
    kill('unsigned long', i) ;
    kill('signed long', acc) ;
    kill('signed int', v) ;
    kill('signed int', r) ;
    run ret_536(conv_loaded_int('signed int', a_758)) ;
    pure(Unit)
  else
    pure(Unit) ;
  let strong _: loaded integer =
    bound(
      let weak (a_759: pointer, a_760: loaded integer) =
        unseq(pure(i), pure(Specified(0))) in
      let weak _: unit =
        neg(store('unsigned long', a_759,
        conv_loaded_int('unsigned long', a_760))) in
      pure(conv_loaded_int('unsigned long', a_760))
    ) in
  pure(Unit) ;
  save while_761: unit (choices: pointer:= choices, expected: pointer:= expected,
  out: pointer:= out, i: pointer:= i, acc: pointer:= acc, v: pointer:= v,
  r: pointer:= r) in
    let strong a_763: loaded integer =
      bound(
        let weak (a_765: loaded integer, a_766: loaded integer) =
          unseq(
            let weak (a_771: loaded integer, a_772: loaded integer) =
              unseq(
                let weak a_770: pointer = pure(i) in
                load('unsigned long', a_770)
              ,
                pure(Specified(4))
              ) in
            case (a_771, a_772) of
              | (Specified(a_773: integer), Specified(a_774: integer)) =>
                  pure(
                    if conv_int('unsigned long', a_773) <
                    conv_int('unsigned long', a_774) then
                      Specified(1)
                    else
                      Specified(0)
                  )
              | _: (loaded integer,loaded integer) =>
                  pure(Unspecified('signed int'))
            end
          ,
            pure(Specified(0))
          ) in
        pure(
          case (a_765, a_766) of
            | (Specified(a_767: integer), Specified(a_768: integer)) =>
                if conv_int('signed int', a_767) = conv_int('signed int', a_768) then
                  Specified(1)
                else
                  Specified(0)
            | _: (loaded integer,loaded integer) =>
                Unspecified('signed int')
          end
        )
      ) in
    let strong a_762: boolean =
      case a_763 of
        | Specified(a_764: integer) =>
            pure(if not(a_764 = 1) then True else False)
        | Unspecified(_: ctype) =>
            nd(pure(True), pure(False))
      end in
    if a_762 then
      let strong a_777: loaded integer =
        bound(
          let weak (a_779: loaded integer, a_780: loaded integer) =
            unseq(
              let weak (a_784: loaded integer, a_785: loaded integer) =
                unseq(
                  let weak a_800: pointer =
                    let weak a_789: loaded pointer =
                      let weak (a_792: loaded pointer, a_794: loaded integer) =
                        unseq(
                          let weak a_797: pointer = pure(out) in
                          pure(
                            Specified(array_shift(a_797, 'unsigned ichar', 0))
                          )
                        ,
                          let weak a_799: pointer = pure(i) in
                          load('unsigned long', a_799)
                        ) in
                      case (a_792, a_794) of
                        | (Specified(a_793: pointer), Specified(a_795: integer)) =>
                            pure(
                              Specified(array_shift(a_793, 'unsigned ichar', a_795))
                            )
                        | _: (loaded pointer,loaded integer) =>
                            pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
                      end in
                    case a_789 of
                      | Specified(a_790: pointer) =>
                          let weak a_791: boolean =
                            memop(PtrValidForDeref, 'unsigned char', a_790) in
                          pure(
                            if a_791 then
                              a_790
                            else
                              undef(<<UB043_indirection_invalid_value>>)
                          )
                      | Unspecified(_: ctype) =>
                          pure(undef(<<UB043_indirection_invalid_value>>))
                    end in
                  load('unsigned char', a_800)
                ,
                  let weak a_812: pointer =
                    let weak a_801: loaded pointer =
                      let weak (a_804: loaded pointer, a_806: loaded integer) =
                        unseq(
                          let weak a_809: pointer = pure(expected) in
                          pure(
                            Specified(array_shift(a_809, 'unsigned ichar', 0))
                          )
                        ,
                          let weak a_811: pointer = pure(i) in
                          load('unsigned long', a_811)
                        ) in
                      case (a_804, a_806) of
                        | (Specified(a_805: pointer), Specified(a_807: integer)) =>
                            pure(
                              Specified(array_shift(a_805, 'unsigned ichar', a_807))
                            )
                        | _: (loaded pointer,loaded integer) =>
                            pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
                      end in
                    case a_801 of
                      | Specified(a_802: pointer) =>
                          let weak a_803: boolean =
                            memop(PtrValidForDeref, 'unsigned char', a_802) in
                          pure(
                            if a_803 then
                              a_802
                            else
                              undef(<<UB043_indirection_invalid_value>>)
                          )
                      | Unspecified(_: ctype) =>
                          pure(undef(<<UB043_indirection_invalid_value>>))
                    end in
                  load('unsigned char', a_812)
                ) in
              pure(
                case (a_784, a_785) of
                  | (Specified(a_786: integer), Specified(a_787: integer)) =>
                      if not(conv_int('signed int', a_786) =
                      conv_int('signed int', a_787)) then
                        Specified(1)
                      else
                        Specified(0)
                  | _: (loaded integer,loaded integer) =>
                      Unspecified('signed int')
                end
              )
            ,
              pure(Specified(0))
            ) in
          pure(
            case (a_779, a_780) of
              | (Specified(a_781: integer), Specified(a_782: integer)) =>
                  if conv_int('signed int', a_781) =
                  conv_int('signed int', a_782) then
                    Specified(1)
                  else
                    Specified(0)
              | _: (loaded integer,loaded integer) =>
                  Unspecified('signed int')
            end
          )
        ) in
      let strong a_776: boolean =
        case a_777 of
          | Specified(a_778: integer) =>
              pure(if not(a_778 = 1) then True else False)
          | Unspecified(_: ctype) =>
              nd(pure(True), pure(False))
        end in
      if a_776 then
        let strong a_822: loaded integer =
          bound(
            let weak a_813: loaded integer =
              let weak (a_815: loaded integer, a_816: loaded integer) =
                unseq(
                  pure(Specified(1))
                ,
                  let weak a_820: pointer = pure(i) in
                  load('unsigned long', a_820)
                ) in
              pure(
                case (a_815, a_816) of
                  | (Specified(a_817: integer), Specified(a_818: integer)) =>
                      Specified(wrapI_add('unsigned long', __conv_int__('unsigned long', a_817), __conv_int__('unsigned long', a_818)))
                  | _: (loaded integer,loaded integer) =>
                      Unspecified('unsigned long')
                end
              ) in
            pure(conv_loaded_int('signed int', a_813))
          ) in
        kill('unsigned ichar[4]', choices) ;
        kill('unsigned ichar[4]', expected) ;
        kill('unsigned ichar[4]', out) ;
        kill('unsigned long', i) ;
        kill('signed long', acc) ;
        kill('signed int', v) ;
        kill('signed int', r) ;
        run ret_536(conv_loaded_int('signed int', a_822)) ;
        pure(Unit)
      else
        pure(Unit) ;
      save __cerb_continue2: unit (choices: pointer:= choices,
      expected: pointer:= expected, out: pointer:= out, i: pointer:= i,
      acc: pointer:= acc, v: pointer:= v, r: pointer:= r) in
        let strong _: loaded integer =
          bound(
            let weak a_823: pointer = pure(i) in
            seq_rmw('unsigned long', a_823, a_824 => case a_824 of
              | Specified(a_825: integer) =>
                  Specified(conv_int('unsigned long',
                  wrapI_add('unsigned long', conv_int('unsigned long', a_825), 1)))
              | Unspecified(_: ctype) =>
                  Unspecified('unsigned long')
            end)
          ) in
        pure(Unit) ;
      pure(Unit) ;
      save continue_541: unit (choices: pointer:= choices,
      expected: pointer:= expected, out: pointer:= out, i: pointer:= i,
      acc: pointer:= acc, v: pointer:= v, r: pointer:= r) in
        pure(Unit) ;
      pure(Unit) ;
      run while_761(choices, expected, out, i, acc, v, r)
    else
      pure(Unit) ;
  save break_542: unit (choices: pointer:= choices, expected: pointer:= expected,
  out: pointer:= out, i: pointer:= i, acc: pointer:= acc, v: pointer:= v,
  r: pointer:= r) in
    pure(Unit) ;
  pure(Unit) ;
  let strong a_826: loaded integer = bound(pure(Specified(0))) in
  kill('unsigned ichar[4]', choices) ;
  kill('unsigned ichar[4]', expected) ;
  kill('unsigned ichar[4]', out) ;
  kill('unsigned long', i) ;
  kill('signed long', acc) ;
  kill('signed int', v) ;
  kill('signed int', r) ;
  run ret_536(conv_loaded_int('signed int', a_826)) ;
  kill('unsigned ichar[4]', choices) ;
  kill('unsigned ichar[4]', expected) ;
  kill('unsigned ichar[4]', out) ;
  kill('unsigned long', i) ;
  kill('signed long', acc) ;
  kill('signed int', v) ;
  kill('signed int', r) ;
  pure(Unit) ;
  save ret_536: loaded integer (a_827: loaded integer:= Specified(0)) in
    pure(a_827)

