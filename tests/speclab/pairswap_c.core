proc swap_pair (pair_p: pointer): eff unit :=
  let strong tmp: pointer =
    create(Ivalignof('unsigned long'), 'unsigned long') in
  let strong a_894: loaded integer =
    bound(
      let weak a_904: pointer =
        let weak a_895: loaded pointer =
          let weak (a_898: loaded pointer, a_900: loaded integer) =
            unseq(
              let weak a_903: pointer = pure(pair_p) in
              load('unsigned long*', a_903)
            ,
              pure(Specified(0))
            ) in
          case (a_898, a_900) of
            | (Specified(a_899: pointer), Specified(a_901: integer)) =>
                pure(Specified(array_shift(a_899, 'unsigned long', a_901)))
            | _: (loaded pointer,loaded integer) =>
                pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
          end in
        case a_895 of
          | Specified(a_896: pointer) =>
              let weak a_897: boolean =
                memop(PtrValidForDeref, 'unsigned long', a_896) in
              pure(
                if a_897 then
                  a_896
                else
                  undef(<<UB043_indirection_invalid_value>>)
              )
          | Unspecified(_: ctype) =>
              pure(undef(<<UB043_indirection_invalid_value>>))
        end in
      load('unsigned long', a_904)
    ) in
  store('unsigned long', tmp, conv_loaded_int('unsigned long', a_894)) ;
  let strong _: loaded integer =
    bound(
      let weak (a_905: pointer, a_925: loaded integer) =
        unseq(
          let weak a_906: loaded pointer =
            let weak (a_909: loaded pointer, a_911: loaded integer) =
              unseq(
                let weak a_914: pointer = pure(pair_p) in
                load('unsigned long*', a_914)
              ,
                pure(Specified(0))
              ) in
            case (a_909, a_911) of
              | (Specified(a_910: pointer), Specified(a_912: integer)) =>
                  pure(Specified(array_shift(a_910, 'unsigned long', a_912)))
              | _: (loaded pointer,loaded integer) =>
                  pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
            end in
          case a_906 of
            | Specified(a_907: pointer) =>
                let weak a_908: boolean =
                  memop(PtrValidForDeref, 'unsigned long', a_907) in
                pure(
                  if a_908 then
                    a_907
                  else
                    undef(<<UB043_indirection_invalid_value>>)
                )
            | Unspecified(_: ctype) =>
                pure(undef(<<UB043_indirection_invalid_value>>))
          end
        ,
          let weak a_924: pointer =
            let weak a_915: loaded pointer =
              let weak (a_918: loaded pointer, a_920: loaded integer) =
                unseq(
                  let weak a_923: pointer = pure(pair_p) in
                  load('unsigned long*', a_923)
                ,
                  pure(Specified(1))
                ) in
              case (a_918, a_920) of
                | (Specified(a_919: pointer), Specified(a_921: integer)) =>
                    pure(Specified(array_shift(a_919, 'unsigned long', a_921)))
                | _: (loaded pointer,loaded integer) =>
                    pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
              end in
            case a_915 of
              | Specified(a_916: pointer) =>
                  let weak a_917: boolean =
                    memop(PtrValidForDeref, 'unsigned long', a_916) in
                  pure(
                    if a_917 then
                      a_916
                    else
                      undef(<<UB043_indirection_invalid_value>>)
                  )
              | Unspecified(_: ctype) =>
                  pure(undef(<<UB043_indirection_invalid_value>>))
            end in
          load('unsigned long', a_924)
        ) in
      let weak _: unit =
        neg(store('unsigned long', a_905,
        conv_loaded_int('unsigned long', a_925))) in
      pure(conv_loaded_int('unsigned long', a_925))
    ) in
  pure(Unit) ;
  let strong _: loaded integer =
    bound(
      let weak (a_926: pointer, a_937: loaded integer) =
        unseq(
          let weak a_927: loaded pointer =
            let weak (a_930: loaded pointer, a_932: loaded integer) =
              unseq(
                let weak a_935: pointer = pure(pair_p) in
                load('unsigned long*', a_935)
              ,
                pure(Specified(1))
              ) in
            case (a_930, a_932) of
              | (Specified(a_931: pointer), Specified(a_933: integer)) =>
                  pure(Specified(array_shift(a_931, 'unsigned long', a_933)))
              | _: (loaded pointer,loaded integer) =>
                  pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
            end in
          case a_927 of
            | Specified(a_928: pointer) =>
                let weak a_929: boolean =
                  memop(PtrValidForDeref, 'unsigned long', a_928) in
                pure(
                  if a_929 then
                    a_928
                  else
                    undef(<<UB043_indirection_invalid_value>>)
                )
            | Unspecified(_: ctype) =>
                pure(undef(<<UB043_indirection_invalid_value>>))
          end
        ,
          let weak a_936: pointer = pure(tmp) in
          load('unsigned long', a_936)
        ) in
      let weak _: unit =
        neg(store('unsigned long', a_926,
        conv_loaded_int('unsigned long', a_937))) in
      pure(conv_loaded_int('unsigned long', a_937))
    ) in
  pure(Unit) ;
  kill('unsigned long', tmp) ;
  pure(Unit) ;
  save ret_893: unit (a_938: unit:= Unit) in  pure(a_938)

proc main (): eff loaded integer :=
  let strong choices: pointer =
    create(Ivalignof('unsigned char[16]'), 'unsigned char[16]') in
  let strong expected: pointer =
    create(Ivalignof('unsigned char[16]'), 'unsigned char[16]') in
  let strong pair: pointer =
    create(Ivalignof('unsigned long[2]'), 'unsigned long[2]') in
  let strong out: pointer =
    create(Ivalignof('unsigned char[16]'), 'unsigned char[16]') in
  let strong i: pointer = create(Ivalignof('unsigned long'), 'unsigned long') in
  let strong j: pointer = create(Ivalignof('unsigned long'), 'unsigned long') in
  let strong acc: pointer =
    create(Ivalignof('unsigned long'), 'unsigned long') in
  let strong a_548: loaded array(integer) =
    bound(
      let weak (a_564: loaded integer, a_563: loaded integer,
      a_562: loaded integer, a_561: loaded integer, a_560: loaded integer,
      a_559: loaded integer, a_558: loaded integer, a_557: loaded integer,
      a_556: loaded integer, a_555: loaded integer, a_554: loaded integer,
      a_553: loaded integer, a_552: loaded integer, a_551: loaded integer,
      a_550: loaded integer, a_549: loaded integer) =
        unseq(
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ) in
      pure(
        Specified(Array(conv_loaded_int('unsigned char', a_549),
        conv_loaded_int('unsigned char', a_550),
        conv_loaded_int('unsigned char', a_551),
        conv_loaded_int('unsigned char', a_552),
        conv_loaded_int('unsigned char', a_553),
        conv_loaded_int('unsigned char', a_554),
        conv_loaded_int('unsigned char', a_555),
        conv_loaded_int('unsigned char', a_556),
        conv_loaded_int('unsigned char', a_557),
        conv_loaded_int('unsigned char', a_558),
        conv_loaded_int('unsigned char', a_559),
        conv_loaded_int('unsigned char', a_560),
        conv_loaded_int('unsigned char', a_561),
        conv_loaded_int('unsigned char', a_562),
        conv_loaded_int('unsigned char', a_563),
        conv_loaded_int('unsigned char', a_564)))
      )
    ) in
  store_lock('unsigned char[16]', choices, a_548) ;
  let strong a_565: loaded array(integer) =
    bound(
      let weak (a_581: loaded integer, a_580: loaded integer,
      a_579: loaded integer, a_578: loaded integer, a_577: loaded integer,
      a_576: loaded integer, a_575: loaded integer, a_574: loaded integer,
      a_573: loaded integer, a_572: loaded integer, a_571: loaded integer,
      a_570: loaded integer, a_569: loaded integer, a_568: loaded integer,
      a_567: loaded integer, a_566: loaded integer) =
        unseq(
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ) in
      pure(
        Specified(Array(conv_loaded_int('unsigned char', a_566),
        conv_loaded_int('unsigned char', a_567),
        conv_loaded_int('unsigned char', a_568),
        conv_loaded_int('unsigned char', a_569),
        conv_loaded_int('unsigned char', a_570),
        conv_loaded_int('unsigned char', a_571),
        conv_loaded_int('unsigned char', a_572),
        conv_loaded_int('unsigned char', a_573),
        conv_loaded_int('unsigned char', a_574),
        conv_loaded_int('unsigned char', a_575),
        conv_loaded_int('unsigned char', a_576),
        conv_loaded_int('unsigned char', a_577),
        conv_loaded_int('unsigned char', a_578),
        conv_loaded_int('unsigned char', a_579),
        conv_loaded_int('unsigned char', a_580),
        conv_loaded_int('unsigned char', a_581)))
      )
    ) in
  store_lock('unsigned char[16]', expected, a_565) ;
  store('unsigned long[2]', pair, Unspecified('unsigned long[2]')) ;
  store('unsigned char[16]', out, Unspecified('unsigned ichar[16]')) ;
  store('unsigned long', i, Unspecified('unsigned long')) ;
  store('unsigned long', j, Unspecified('unsigned long')) ;
  store('unsigned long', acc, Unspecified('unsigned long')) ;
  let strong a_583: loaded integer =
    bound(
      let weak (a_585: loaded integer, a_586: loaded integer) =
        unseq(
          let weak (a_590: loaded integer, a_591: loaded integer) =
            unseq(
              pure(Specified(Ivsizeof('unsigned char[16]')))
            ,
              pure(Specified(16))
            ) in
          pure(
            case (a_590, a_591) of
              | (Specified(a_592: integer), Specified(a_593: integer)) =>
                  if not(conv_int('size_t', a_592) = conv_int('size_t', a_593)) then
                    Specified(1)
                  else
                    Specified(0)
              | _: (loaded integer,loaded integer) =>
                  Unspecified('signed int')
            end
          )
        ,
          pure(Specified(0))
        ) in
      pure(
        case (a_585, a_586) of
          | (Specified(a_587: integer), Specified(a_588: integer)) =>
              if conv_int('signed int', a_587) = conv_int('signed int', a_588) then
                Specified(1)
              else
                Specified(0)
          | _: (loaded integer,loaded integer) =>
              Unspecified('signed int')
        end
      )
    ) in
  let strong a_582: boolean =
    case a_583 of
      | Specified(a_584: integer) =>
          pure(if not(a_584 = 1) then True else False)
      | Unspecified(_: ctype) =>
          nd(pure(True), pure(False))
    end in
  if a_582 then
    let strong a_595: loaded integer = bound(pure(Specified(254))) in
    kill('unsigned ichar[16]', choices) ;
    kill('unsigned ichar[16]', expected) ;
    kill('unsigned long[2]', pair) ;
    kill('unsigned ichar[16]', out) ;
    kill('unsigned long', i) ;
    kill('unsigned long', j) ;
    kill('unsigned long', acc) ;
    run ret_535(conv_loaded_int('signed int', a_595)) ;
    pure(Unit)
  else
    pure(Unit) ;
  let strong _: loaded integer =
    bound(
      let weak (a_596: pointer, a_597: loaded integer) =
        unseq(pure(j), pure(Specified(0))) in
      let weak _: unit =
        neg(store('unsigned long', a_596,
        conv_loaded_int('unsigned long', a_597))) in
      pure(conv_loaded_int('unsigned long', a_597))
    ) in
  pure(Unit) ;
  save while_598: unit (choices: pointer:= choices, expected: pointer:= expected,
  pair: pointer:= pair, out: pointer:= out, i: pointer:= i, j: pointer:= j,
  acc: pointer:= acc) in
    let strong a_600: loaded integer =
      bound(
        let weak (a_602: loaded integer, a_603: loaded integer) =
          unseq(
            let weak (a_608: loaded integer, a_609: loaded integer) =
              unseq(
                let weak a_607: pointer = pure(j) in
                load('unsigned long', a_607)
              ,
                pure(Specified(2))
              ) in
            case (a_608, a_609) of
              | (Specified(a_610: integer), Specified(a_611: integer)) =>
                  pure(
                    if conv_int('unsigned long', a_610) <
                    conv_int('unsigned long', a_611) then
                      Specified(1)
                    else
                      Specified(0)
                  )
              | _: (loaded integer,loaded integer) =>
                  pure(Unspecified('signed int'))
            end
          ,
            pure(Specified(0))
          ) in
        pure(
          case (a_602, a_603) of
            | (Specified(a_604: integer), Specified(a_605: integer)) =>
                if conv_int('signed int', a_604) = conv_int('signed int', a_605) then
                  Specified(1)
                else
                  Specified(0)
            | _: (loaded integer,loaded integer) =>
                Unspecified('signed int')
          end
        )
      ) in
    let strong a_599: boolean =
      case a_600 of
        | Specified(a_601: integer) =>
            pure(if not(a_601 = 1) then True else False)
        | Unspecified(_: ctype) =>
            nd(pure(True), pure(False))
      end in
    if a_599 then
      let strong _: loaded integer =
        bound(
          let weak (a_613: pointer, a_614: loaded integer) =
            unseq(pure(acc), pure(Specified(0))) in
          let weak _: unit =
            neg(store('unsigned long', a_613,
            conv_loaded_int('unsigned long', a_614))) in
          pure(conv_loaded_int('unsigned long', a_614))
        ) in
      pure(Unit) ;
      let strong _: loaded integer =
        bound(
          let weak (a_615: pointer, a_616: loaded integer) =
            unseq(pure(i), pure(Specified(0))) in
          let weak _: unit =
            neg(store('unsigned long', a_615,
            conv_loaded_int('unsigned long', a_616))) in
          pure(conv_loaded_int('unsigned long', a_616))
        ) in
      pure(Unit) ;
      save while_617: unit (choices: pointer:= choices,
      expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
      i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
        let strong a_619: loaded integer =
          bound(
            let weak (a_621: loaded integer, a_622: loaded integer) =
              unseq(
                let weak (a_627: loaded integer, a_628: loaded integer) =
                  unseq(
                    let weak a_626: pointer = pure(i) in
                    load('unsigned long', a_626)
                  ,
                    pure(Specified(8))
                  ) in
                case (a_627, a_628) of
                  | (Specified(a_629: integer), Specified(a_630: integer)) =>
                      pure(
                        if conv_int('unsigned long', a_629) <
                        conv_int('unsigned long', a_630) then
                          Specified(1)
                        else
                          Specified(0)
                      )
                  | _: (loaded integer,loaded integer) =>
                      pure(Unspecified('signed int'))
                end
              ,
                pure(Specified(0))
              ) in
            pure(
              case (a_621, a_622) of
                | (Specified(a_623: integer), Specified(a_624: integer)) =>
                    if conv_int('signed int', a_623) =
                    conv_int('signed int', a_624) then
                      Specified(1)
                    else
                      Specified(0)
                | _: (loaded integer,loaded integer) =>
                    Unspecified('signed int')
              end
            )
          ) in
        let strong a_618: boolean =
          case a_619 of
            | Specified(a_620: integer) =>
                pure(if not(a_620 = 1) then True else False)
            | Unspecified(_: ctype) =>
                nd(pure(True), pure(False))
          end in
        if a_618 then
          let strong _: loaded integer =
            bound(
              let weak (a_632: pointer, a_678: loaded integer) =
                unseq(
                  pure(acc)
                ,
                  let weak (a_633: loaded integer, a_634: loaded integer) =
                    unseq(
                      let weak a_638: pointer = pure(acc) in
                      load('unsigned long', a_638)
                    ,
                      let weak (a_639: loaded integer, a_640: loaded integer) =
                        unseq(
                          let weak a_646: loaded integer =
                            let weak a_670: pointer =
                              let weak a_648: loaded pointer =
                                let weak (a_651: loaded pointer,
                                a_653: loaded integer) =
                                  unseq(
                                    let weak a_656: pointer = pure(choices) in
                                    pure(
                                      Specified(array_shift(a_656, 'unsigned ichar', 0))
                                    )
                                  ,
                                    let weak (a_658: loaded integer,
                                    a_659: loaded integer) =
                                      unseq(
                                        let weak (a_664: loaded integer,
                                        a_665: loaded integer) =
                                          unseq(
                                            pure(Specified(8))
                                          ,
                                            let weak a_663: pointer = pure(j) in
                                            load('unsigned long', a_663)
                                          ) in
                                        pure(
                                          case (a_664, a_665) of
                                            | (Specified(a_666: integer),
                                            Specified(a_667: integer)) =>
                                                Specified(wrapI_mul('unsigned long', conv_int('unsigned long',
                                                a_666), conv_int('unsigned long',
                                                a_667)))
                                            | _: (loaded integer,loaded integer) =>
                                                Unspecified('unsigned long')
                                          end
                                        )
                                      ,
                                        let weak a_669: pointer = pure(i) in
                                        load('unsigned long', a_669)
                                      ) in
                                    pure(
                                      case (a_658, a_659) of
                                        | (Specified(a_660: integer),
                                        Specified(a_661: integer)) =>
                                            Specified(wrapI_add('unsigned long', __conv_int__('unsigned long', a_660), __conv_int__('unsigned long', a_661)))
                                        | _: (loaded integer,loaded integer) =>
                                            Unspecified('unsigned long')
                                      end
                                    )
                                  ) in
                                case (a_651, a_653) of
                                  | (Specified(a_652: pointer),
                                  Specified(a_654: integer)) =>
                                      pure(
                                        Specified(array_shift(a_652, 'unsigned ichar', a_654))
                                      )
                                  | _: (loaded pointer,loaded integer) =>
                                      pure(
                                        undef(<<UB_CERB004_unspecified__pointer_add>>)
                                      )
                                end in
                              case a_648 of
                                | Specified(a_649: pointer) =>
                                    let weak a_650: boolean =
                                      memop(PtrValidForDeref, 'unsigned char',
                                      a_649) in
                                    pure(
                                      if a_650 then
                                        a_649
                                      else
                                        undef(<<UB043_indirection_invalid_value>>)
                                    )
                                | Unspecified(_: ctype) =>
                                    pure(
                                      undef(<<UB043_indirection_invalid_value>>)
                                    )
                              end in
                            load('unsigned char', a_670) in
                          pure(conv_loaded_int('unsigned long', a_646))
                        ,
                          let weak (a_673: loaded integer, a_674: loaded integer) =
                            unseq(
                              pure(Specified(8))
                            ,
                              let weak a_672: pointer = pure(i) in
                              load('unsigned long', a_672)
                            ) in
                          pure(
                            case (a_673, a_674) of
                              | (Specified(a_675: integer),
                              Specified(a_676: integer)) =>
                                  Specified(wrapI_mul('unsigned long', conv_int('unsigned long',
                                  a_675), conv_int('unsigned long', a_676)))
                              | _: (loaded integer,loaded integer) =>
                                  Unspecified('unsigned long')
                            end
                          )
                        ) in
                      pure(
                        case (a_639, a_640) of
                          | (_: loaded integer, Unspecified(_: ctype)) =>
                              undef(<<UB036_exceptional_condition>>)
                          | (Unspecified(_: ctype), _: loaded integer) =>
                              Unspecified('unsigned long')
                          | (Specified(a_641: integer),
                          Specified(a_642: integer)) =>
                              let a_643: integer =
                                conv_int('unsigned long', a_641) in
                              let a_644: integer =
                                conv_int('unsigned long', a_642) in
                              if a_644 < 0 then
                                undef(<<UB051a_negative_shift>>)
                              else
                                if ctype_width('unsigned long') < a_644 \/
                                ctype_width('unsigned long') = a_644 then
                                  undef(<<UB51b_shift_too_large>>)
                                else
                                  Specified(a_643 * 2 ^ a_644 rem_t
                                  (Ivmax('unsigned long') + 1))
                        end
                      )
                    ) in
                  pure(
                    case (a_633, a_634) of
                      | (Specified(a_635: integer), Specified(a_636: integer)) =>
                          Specified(wrapI_add('unsigned long', __conv_int__('unsigned long', a_635), __conv_int__('unsigned long', a_636)))
                      | _: (loaded integer,loaded integer) =>
                          Unspecified('unsigned long')
                    end
                  )
                ) in
              let weak _: unit =
                neg(store('unsigned long', a_632,
                conv_loaded_int('unsigned long', a_678))) in
              pure(conv_loaded_int('unsigned long', a_678))
            ) in
          pure(Unit) ;
          save __cerb_continue1: unit (choices: pointer:= choices,
          expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
          i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
            let strong _: loaded integer =
              bound(
                let weak a_679: pointer = pure(i) in
                seq_rmw('unsigned long', a_679, a_680 => case a_680 of
                  | Specified(a_681: integer) =>
                      Specified(conv_int('unsigned long',
                      wrapI_add('unsigned long', conv_int('unsigned long', a_681), 1)))
                  | Unspecified(_: ctype) =>
                      Unspecified('unsigned long')
                end)
              ) in
            pure(Unit) ;
          pure(Unit) ;
          save continue_538: unit (choices: pointer:= choices,
          expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
          i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
            pure(Unit) ;
          pure(Unit) ;
          run while_617(choices, expected, pair, out, i, j, acc)
        else
          pure(Unit) ;
      save break_539: unit (choices: pointer:= choices,
      expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
      i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
        pure(Unit) ;
      pure(Unit) ;
      let strong _: loaded integer =
        bound(
          let weak (a_682: pointer, a_695: loaded integer) =
            unseq(
              let weak a_683: loaded pointer =
                let weak (a_686: loaded pointer, a_688: loaded integer) =
                  unseq(
                    let weak a_691: pointer = pure(pair) in
                    pure(Specified(array_shift(a_691, 'unsigned long', 0)))
                  ,
                    let weak a_693: pointer = pure(j) in
                    load('unsigned long', a_693)
                  ) in
                case (a_686, a_688) of
                  | (Specified(a_687: pointer), Specified(a_689: integer)) =>
                      pure(
                        Specified(array_shift(a_687, 'unsigned long', a_689))
                      )
                  | _: (loaded pointer,loaded integer) =>
                      pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
                end in
              case a_683 of
                | Specified(a_684: pointer) =>
                    let weak a_685: boolean =
                      memop(PtrValidForDeref, 'unsigned long', a_684) in
                    pure(
                      if a_685 then
                        a_684
                      else
                        undef(<<UB043_indirection_invalid_value>>)
                    )
                | Unspecified(_: ctype) =>
                    pure(undef(<<UB043_indirection_invalid_value>>))
              end
            ,
              let weak a_694: pointer = pure(acc) in
              load('unsigned long', a_694)
            ) in
          let weak _: unit =
            neg(store('unsigned long', a_682,
            conv_loaded_int('unsigned long', a_695))) in
          pure(conv_loaded_int('unsigned long', a_695))
        ) in
      pure(Unit) ;
      save __cerb_continue0: unit (choices: pointer:= choices,
      expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
      i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
        let strong _: loaded integer =
          bound(
            let weak a_696: pointer = pure(j) in
            seq_rmw('unsigned long', a_696, a_697 => case a_697 of
              | Specified(a_698: integer) =>
                  Specified(conv_int('unsigned long',
                  wrapI_add('unsigned long', conv_int('unsigned long', a_698), 1)))
              | Unspecified(_: ctype) =>
                  Unspecified('unsigned long')
            end)
          ) in
        pure(Unit) ;
      pure(Unit) ;
      save continue_536: unit (choices: pointer:= choices,
      expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
      i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
        pure(Unit) ;
      pure(Unit) ;
      run while_598(choices, expected, pair, out, i, j, acc)
    else
      pure(Unit) ;
  save break_537: unit (choices: pointer:= choices, expected: pointer:= expected,
  pair: pointer:= pair, out: pointer:= out, i: pointer:= i, j: pointer:= j,
  acc: pointer:= acc) in
    pure(Unit) ;
  pure(Unit) ;
  bound(
    let strong ((a_700: loaded pointer,
    (a_701: ctype, a_702: [ctype], a_703: boolean, a_704: boolean)),
    a_706: loaded pointer) =
      unseq(
        let strong a_699: loaded pointer =
          pure(Specified(Cfunction(swap_pair))) in
        pure((a_699, cfunction(a_699)))
      ,
        let weak a_707: pointer = pure(pair) in
        pure(Specified(array_shift(a_707, 'unsigned long', 0)))
      ) in
    if not(params_length(a_702) = 1) then
      pure(undef(<<UB038_number_of_args>>))
    else
      if a_703 \/ not(are_compatible ('void', a_701)) then
        pure(undef(<<UB041_function_not_compatible>>))
      else
        let strong a_705: pointer =
          let a_709: ctype = params_nth(a_702, 0) in
          if not(are_compatible ('unsigned long*', a_709)) then
            pure(undef(<<UB041_function_not_compatible>>))
          else
            let weak a_710: pointer =
              create(Ivalignof('unsigned long*'), 'unsigned long*') in
            let weak _: unit = store('unsigned long*', a_710, a_706) in
            pure(a_710) in
        let strong a_711: unit =
          ccall('void (*) (unsigned long*)', a_700, a_705) in
        kill('unsigned long*', a_705) ;
        pure(a_711)
  ) ;
  pure(Unit) ;
  let strong _: loaded integer =
    bound(
      let weak (a_712: pointer, a_713: loaded integer) =
        unseq(pure(j), pure(Specified(0))) in
      let weak _: unit =
        neg(store('unsigned long', a_712,
        conv_loaded_int('unsigned long', a_713))) in
      pure(conv_loaded_int('unsigned long', a_713))
    ) in
  pure(Unit) ;
  save while_714: unit (choices: pointer:= choices, expected: pointer:= expected,
  pair: pointer:= pair, out: pointer:= out, i: pointer:= i, j: pointer:= j,
  acc: pointer:= acc) in
    let strong a_716: loaded integer =
      bound(
        let weak (a_718: loaded integer, a_719: loaded integer) =
          unseq(
            let weak (a_724: loaded integer, a_725: loaded integer) =
              unseq(
                let weak a_723: pointer = pure(j) in
                load('unsigned long', a_723)
              ,
                pure(Specified(2))
              ) in
            case (a_724, a_725) of
              | (Specified(a_726: integer), Specified(a_727: integer)) =>
                  pure(
                    if conv_int('unsigned long', a_726) <
                    conv_int('unsigned long', a_727) then
                      Specified(1)
                    else
                      Specified(0)
                  )
              | _: (loaded integer,loaded integer) =>
                  pure(Unspecified('signed int'))
            end
          ,
            pure(Specified(0))
          ) in
        pure(
          case (a_718, a_719) of
            | (Specified(a_720: integer), Specified(a_721: integer)) =>
                if conv_int('signed int', a_720) = conv_int('signed int', a_721) then
                  Specified(1)
                else
                  Specified(0)
            | _: (loaded integer,loaded integer) =>
                Unspecified('signed int')
          end
        )
      ) in
    let strong a_715: boolean =
      case a_716 of
        | Specified(a_717: integer) =>
            pure(if not(a_717 = 1) then True else False)
        | Unspecified(_: ctype) =>
            nd(pure(True), pure(False))
      end in
    if a_715 then
      let strong _: loaded integer =
        bound(
          let weak (a_729: pointer, a_742: loaded integer) =
            unseq(
              pure(acc)
            ,
              let weak a_741: pointer =
                let weak a_730: loaded pointer =
                  let weak (a_733: loaded pointer, a_735: loaded integer) =
                    unseq(
                      let weak a_738: pointer = pure(pair) in
                      pure(Specified(array_shift(a_738, 'unsigned long', 0)))
                    ,
                      let weak a_740: pointer = pure(j) in
                      load('unsigned long', a_740)
                    ) in
                  case (a_733, a_735) of
                    | (Specified(a_734: pointer), Specified(a_736: integer)) =>
                        pure(
                          Specified(array_shift(a_734, 'unsigned long', a_736))
                        )
                    | _: (loaded pointer,loaded integer) =>
                        pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
                  end in
                case a_730 of
                  | Specified(a_731: pointer) =>
                      let weak a_732: boolean =
                        memop(PtrValidForDeref, 'unsigned long', a_731) in
                      pure(
                        if a_732 then
                          a_731
                        else
                          undef(<<UB043_indirection_invalid_value>>)
                      )
                  | Unspecified(_: ctype) =>
                      pure(undef(<<UB043_indirection_invalid_value>>))
                end in
              load('unsigned long', a_741)
            ) in
          let weak _: unit =
            neg(store('unsigned long', a_729,
            conv_loaded_int('unsigned long', a_742))) in
          pure(conv_loaded_int('unsigned long', a_742))
        ) in
      pure(Unit) ;
      let strong _: loaded integer =
        bound(
          let weak (a_743: pointer, a_744: loaded integer) =
            unseq(pure(i), pure(Specified(0))) in
          let weak _: unit =
            neg(store('unsigned long', a_743,
            conv_loaded_int('unsigned long', a_744))) in
          pure(conv_loaded_int('unsigned long', a_744))
        ) in
      pure(Unit) ;
      save while_745: unit (choices: pointer:= choices,
      expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
      i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
        let strong a_747: loaded integer =
          bound(
            let weak (a_749: loaded integer, a_750: loaded integer) =
              unseq(
                let weak (a_755: loaded integer, a_756: loaded integer) =
                  unseq(
                    let weak a_754: pointer = pure(i) in
                    load('unsigned long', a_754)
                  ,
                    pure(Specified(8))
                  ) in
                case (a_755, a_756) of
                  | (Specified(a_757: integer), Specified(a_758: integer)) =>
                      pure(
                        if conv_int('unsigned long', a_757) <
                        conv_int('unsigned long', a_758) then
                          Specified(1)
                        else
                          Specified(0)
                      )
                  | _: (loaded integer,loaded integer) =>
                      pure(Unspecified('signed int'))
                end
              ,
                pure(Specified(0))
              ) in
            pure(
              case (a_749, a_750) of
                | (Specified(a_751: integer), Specified(a_752: integer)) =>
                    if conv_int('signed int', a_751) =
                    conv_int('signed int', a_752) then
                      Specified(1)
                    else
                      Specified(0)
                | _: (loaded integer,loaded integer) =>
                    Unspecified('signed int')
              end
            )
          ) in
        let strong a_746: boolean =
          case a_747 of
            | Specified(a_748: integer) =>
                pure(if not(a_748 = 1) then True else False)
            | Unspecified(_: ctype) =>
                nd(pure(True), pure(False))
          end in
        if a_746 then
          let strong _: loaded integer =
            bound(
              let weak (a_760: pointer, a_793: loaded integer) =
                unseq(
                  let weak a_761: loaded pointer =
                    let weak (a_764: loaded pointer, a_766: loaded integer) =
                      unseq(
                        let weak a_769: pointer = pure(out) in
                        pure(Specified(array_shift(a_769, 'unsigned ichar', 0)))
                      ,
                        let weak (a_771: loaded integer, a_772: loaded integer) =
                          unseq(
                            let weak (a_777: loaded integer,
                            a_778: loaded integer) =
                              unseq(
                                pure(Specified(8))
                              ,
                                let weak a_776: pointer = pure(j) in
                                load('unsigned long', a_776)
                              ) in
                            pure(
                              case (a_777, a_778) of
                                | (Specified(a_779: integer),
                                Specified(a_780: integer)) =>
                                    Specified(wrapI_mul('unsigned long', conv_int('unsigned long',
                                    a_779), conv_int('unsigned long', a_780)))
                                | _: (loaded integer,loaded integer) =>
                                    Unspecified('unsigned long')
                              end
                            )
                          ,
                            let weak a_782: pointer = pure(i) in
                            load('unsigned long', a_782)
                          ) in
                        pure(
                          case (a_771, a_772) of
                            | (Specified(a_773: integer),
                            Specified(a_774: integer)) =>
                                Specified(wrapI_add('unsigned long', __conv_int__('unsigned long', a_773), __conv_int__('unsigned long', a_774)))
                            | _: (loaded integer,loaded integer) =>
                                Unspecified('unsigned long')
                          end
                        )
                      ) in
                    case (a_764, a_766) of
                      | (Specified(a_765: pointer), Specified(a_767: integer)) =>
                          pure(
                            Specified(array_shift(a_765, 'unsigned ichar', a_767))
                          )
                      | _: (loaded pointer,loaded integer) =>
                          pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
                    end in
                  case a_761 of
                    | Specified(a_762: pointer) =>
                        let weak a_763: boolean =
                          memop(PtrValidForDeref, 'unsigned char', a_762) in
                        pure(
                          if a_763 then
                            a_762
                          else
                            undef(<<UB043_indirection_invalid_value>>)
                        )
                    | Unspecified(_: ctype) =>
                        pure(undef(<<UB043_indirection_invalid_value>>))
                  end
                ,
                  let weak a_783: loaded integer =
                    let weak (a_786: loaded integer, a_787: loaded integer) =
                      unseq(
                        let weak a_785: pointer = pure(acc) in
                        load('unsigned long', a_785)
                      ,
                        pure(Specified(256))
                      ) in
                    pure(
                      case (a_786, a_787) of
                        | (Unspecified(_: ctype), Specified(a_789: integer)) =>
                            let a_791: integer =
                              conv_int('unsigned long', a_789) in
                            if a_791 = 0 then
                              undef(<<UB045b_modulo_by_zero>>)
                            else
                              Unspecified('unsigned long')
                        | (_: loaded integer, Unspecified(_: ctype)) =>
                            undef(<<UB045b_modulo_by_zero>>)
                        | (Specified(a_788: integer), Specified(a_789: integer)) =>
                            let a_790: integer =
                              conv_int('unsigned long', a_788) in
                            let a_791: integer =
                              conv_int('unsigned long', a_789) in
                            if a_791 = 0 then
                              undef(<<UB045b_modulo_by_zero>>)
                            else
                              if is_representable_integer(a_790 / a_791,
                              'unsigned long') then
                                Specified(a_790 rem_t a_791)
                              else
                                undef(<<UB045c_quotient_not_representable>>)
                      end
                    ) in
                  pure(conv_loaded_int('unsigned char', a_783))
                ) in
              let weak _: unit =
                neg(store('unsigned char', a_760,
                conv_loaded_int('unsigned char', a_793))) in
              pure(conv_loaded_int('unsigned char', a_793))
            ) in
          pure(Unit) ;
          let strong _: loaded integer =
            bound(
              let weak (a_794: pointer, a_802: loaded integer) =
                unseq(
                  pure(acc)
                ,
                  let weak (a_796: loaded integer, a_797: loaded integer) =
                    unseq(
                      let weak a_795: pointer = pure(acc) in
                      load('unsigned long', a_795)
                    ,
                      pure(Specified(256))
                    ) in
                  pure(
                    case (a_796, a_797) of
                      | (Unspecified(_: ctype), Specified(a_799: integer)) =>
                          let a_801: integer =
                            conv_int('unsigned long', a_799) in
                          if a_801 = 0 then
                            undef(<<UB045a_division_by_zero>>)
                          else
                            Unspecified('unsigned long')
                      | (_: loaded integer, Unspecified(_: ctype)) =>
                          undef(<<UB045a_division_by_zero>>)
                      | (Specified(a_798: integer), Specified(a_799: integer)) =>
                          let a_800: integer =
                            conv_int('unsigned long', a_798) in
                          let a_801: integer =
                            conv_int('unsigned long', a_799) in
                          if a_801 = 0 then
                            undef(<<UB045a_division_by_zero>>)
                          else
                            if is_representable_integer(a_800 / a_801,
                            'unsigned long') then
                              Specified(a_800 / a_801)
                            else
                              undef(<<UB045c_quotient_not_representable>>)
                    end
                  )
                ) in
              let weak _: unit =
                neg(store('unsigned long', a_794,
                conv_loaded_int('unsigned long', a_802))) in
              pure(conv_loaded_int('unsigned long', a_802))
            ) in
          pure(Unit) ;
          save __cerb_continue3: unit (choices: pointer:= choices,
          expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
          i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
            let strong _: loaded integer =
              bound(
                let weak a_803: pointer = pure(i) in
                seq_rmw('unsigned long', a_803, a_804 => case a_804 of
                  | Specified(a_805: integer) =>
                      Specified(conv_int('unsigned long',
                      wrapI_add('unsigned long', conv_int('unsigned long', a_805), 1)))
                  | Unspecified(_: ctype) =>
                      Unspecified('unsigned long')
                end)
              ) in
            pure(Unit) ;
          pure(Unit) ;
          save continue_542: unit (choices: pointer:= choices,
          expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
          i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
            pure(Unit) ;
          pure(Unit) ;
          run while_745(choices, expected, pair, out, i, j, acc)
        else
          pure(Unit) ;
      save break_543: unit (choices: pointer:= choices,
      expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
      i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
        pure(Unit) ;
      pure(Unit) ;
      save __cerb_continue2: unit (choices: pointer:= choices,
      expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
      i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
        let strong _: loaded integer =
          bound(
            let weak a_806: pointer = pure(j) in
            seq_rmw('unsigned long', a_806, a_807 => case a_807 of
              | Specified(a_808: integer) =>
                  Specified(conv_int('unsigned long',
                  wrapI_add('unsigned long', conv_int('unsigned long', a_808), 1)))
              | Unspecified(_: ctype) =>
                  Unspecified('unsigned long')
            end)
          ) in
        pure(Unit) ;
      pure(Unit) ;
      save continue_540: unit (choices: pointer:= choices,
      expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
      i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
        pure(Unit) ;
      pure(Unit) ;
      run while_714(choices, expected, pair, out, i, j, acc)
    else
      pure(Unit) ;
  save break_541: unit (choices: pointer:= choices, expected: pointer:= expected,
  pair: pointer:= pair, out: pointer:= out, i: pointer:= i, j: pointer:= j,
  acc: pointer:= acc) in
    pure(Unit) ;
  pure(Unit) ;
  let strong a_810: loaded integer =
    bound(
      let weak (a_812: loaded integer, a_813: loaded integer) =
        unseq(
          let weak (a_817: loaded integer, a_818: loaded integer) =
            unseq(
              pure(Specified(Ivsizeof('unsigned char[16]')))
            ,
              pure(Specified(16))
            ) in
          pure(
            case (a_817, a_818) of
              | (Specified(a_819: integer), Specified(a_820: integer)) =>
                  if not(conv_int('size_t', a_819) = conv_int('size_t', a_820)) then
                    Specified(1)
                  else
                    Specified(0)
              | _: (loaded integer,loaded integer) =>
                  Unspecified('signed int')
            end
          )
        ,
          pure(Specified(0))
        ) in
      pure(
        case (a_812, a_813) of
          | (Specified(a_814: integer), Specified(a_815: integer)) =>
              if conv_int('signed int', a_814) = conv_int('signed int', a_815) then
                Specified(1)
              else
                Specified(0)
          | _: (loaded integer,loaded integer) =>
              Unspecified('signed int')
        end
      )
    ) in
  let strong a_809: boolean =
    case a_810 of
      | Specified(a_811: integer) =>
          pure(if not(a_811 = 1) then True else False)
      | Unspecified(_: ctype) =>
          nd(pure(True), pure(False))
    end in
  if a_809 then
    let strong a_822: loaded integer = bound(pure(Specified(255))) in
    kill('unsigned ichar[16]', choices) ;
    kill('unsigned ichar[16]', expected) ;
    kill('unsigned long[2]', pair) ;
    kill('unsigned ichar[16]', out) ;
    kill('unsigned long', i) ;
    kill('unsigned long', j) ;
    kill('unsigned long', acc) ;
    run ret_535(conv_loaded_int('signed int', a_822)) ;
    pure(Unit)
  else
    pure(Unit) ;
  let strong _: loaded integer =
    bound(
      let weak (a_823: pointer, a_824: loaded integer) =
        unseq(pure(i), pure(Specified(0))) in
      let weak _: unit =
        neg(store('unsigned long', a_823,
        conv_loaded_int('unsigned long', a_824))) in
      pure(conv_loaded_int('unsigned long', a_824))
    ) in
  pure(Unit) ;
  save while_825: unit (choices: pointer:= choices, expected: pointer:= expected,
  pair: pointer:= pair, out: pointer:= out, i: pointer:= i, j: pointer:= j,
  acc: pointer:= acc) in
    let strong a_827: loaded integer =
      bound(
        let weak (a_829: loaded integer, a_830: loaded integer) =
          unseq(
            let weak (a_835: loaded integer, a_836: loaded integer) =
              unseq(
                let weak a_834: pointer = pure(i) in
                load('unsigned long', a_834)
              ,
                pure(Specified(16))
              ) in
            case (a_835, a_836) of
              | (Specified(a_837: integer), Specified(a_838: integer)) =>
                  pure(
                    if conv_int('unsigned long', a_837) <
                    conv_int('unsigned long', a_838) then
                      Specified(1)
                    else
                      Specified(0)
                  )
              | _: (loaded integer,loaded integer) =>
                  pure(Unspecified('signed int'))
            end
          ,
            pure(Specified(0))
          ) in
        pure(
          case (a_829, a_830) of
            | (Specified(a_831: integer), Specified(a_832: integer)) =>
                if conv_int('signed int', a_831) = conv_int('signed int', a_832) then
                  Specified(1)
                else
                  Specified(0)
            | _: (loaded integer,loaded integer) =>
                Unspecified('signed int')
          end
        )
      ) in
    let strong a_826: boolean =
      case a_827 of
        | Specified(a_828: integer) =>
            pure(if not(a_828 = 1) then True else False)
        | Unspecified(_: ctype) =>
            nd(pure(True), pure(False))
      end in
    if a_826 then
      let strong a_841: loaded integer =
        bound(
          let weak (a_843: loaded integer, a_844: loaded integer) =
            unseq(
              let weak (a_848: loaded integer, a_849: loaded integer) =
                unseq(
                  let weak a_864: pointer =
                    let weak a_853: loaded pointer =
                      let weak (a_856: loaded pointer, a_858: loaded integer) =
                        unseq(
                          let weak a_861: pointer = pure(out) in
                          pure(
                            Specified(array_shift(a_861, 'unsigned ichar', 0))
                          )
                        ,
                          let weak a_863: pointer = pure(i) in
                          load('unsigned long', a_863)
                        ) in
                      case (a_856, a_858) of
                        | (Specified(a_857: pointer), Specified(a_859: integer)) =>
                            pure(
                              Specified(array_shift(a_857, 'unsigned ichar', a_859))
                            )
                        | _: (loaded pointer,loaded integer) =>
                            pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
                      end in
                    case a_853 of
                      | Specified(a_854: pointer) =>
                          let weak a_855: boolean =
                            memop(PtrValidForDeref, 'unsigned char', a_854) in
                          pure(
                            if a_855 then
                              a_854
                            else
                              undef(<<UB043_indirection_invalid_value>>)
                          )
                      | Unspecified(_: ctype) =>
                          pure(undef(<<UB043_indirection_invalid_value>>))
                    end in
                  load('unsigned char', a_864)
                ,
                  let weak a_876: pointer =
                    let weak a_865: loaded pointer =
                      let weak (a_868: loaded pointer, a_870: loaded integer) =
                        unseq(
                          let weak a_873: pointer = pure(expected) in
                          pure(
                            Specified(array_shift(a_873, 'unsigned ichar', 0))
                          )
                        ,
                          let weak a_875: pointer = pure(i) in
                          load('unsigned long', a_875)
                        ) in
                      case (a_868, a_870) of
                        | (Specified(a_869: pointer), Specified(a_871: integer)) =>
                            pure(
                              Specified(array_shift(a_869, 'unsigned ichar', a_871))
                            )
                        | _: (loaded pointer,loaded integer) =>
                            pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
                      end in
                    case a_865 of
                      | Specified(a_866: pointer) =>
                          let weak a_867: boolean =
                            memop(PtrValidForDeref, 'unsigned char', a_866) in
                          pure(
                            if a_867 then
                              a_866
                            else
                              undef(<<UB043_indirection_invalid_value>>)
                          )
                      | Unspecified(_: ctype) =>
                          pure(undef(<<UB043_indirection_invalid_value>>))
                    end in
                  load('unsigned char', a_876)
                ) in
              pure(
                case (a_848, a_849) of
                  | (Specified(a_850: integer), Specified(a_851: integer)) =>
                      if not(conv_int('signed int', a_850) =
                      conv_int('signed int', a_851)) then
                        Specified(1)
                      else
                        Specified(0)
                  | _: (loaded integer,loaded integer) =>
                      Unspecified('signed int')
                end
              )
            ,
              pure(Specified(0))
            ) in
          pure(
            case (a_843, a_844) of
              | (Specified(a_845: integer), Specified(a_846: integer)) =>
                  if conv_int('signed int', a_845) =
                  conv_int('signed int', a_846) then
                    Specified(1)
                  else
                    Specified(0)
              | _: (loaded integer,loaded integer) =>
                  Unspecified('signed int')
            end
          )
        ) in
      let strong a_840: boolean =
        case a_841 of
          | Specified(a_842: integer) =>
              pure(if not(a_842 = 1) then True else False)
          | Unspecified(_: ctype) =>
              nd(pure(True), pure(False))
        end in
      if a_840 then
        let strong a_886: loaded integer =
          bound(
            let weak a_877: loaded integer =
              let weak (a_879: loaded integer, a_880: loaded integer) =
                unseq(
                  pure(Specified(1))
                ,
                  let weak a_884: pointer = pure(i) in
                  load('unsigned long', a_884)
                ) in
              pure(
                case (a_879, a_880) of
                  | (Specified(a_881: integer), Specified(a_882: integer)) =>
                      Specified(wrapI_add('unsigned long', __conv_int__('unsigned long', a_881), __conv_int__('unsigned long', a_882)))
                  | _: (loaded integer,loaded integer) =>
                      Unspecified('unsigned long')
                end
              ) in
            pure(conv_loaded_int('signed int', a_877))
          ) in
        kill('unsigned ichar[16]', choices) ;
        kill('unsigned ichar[16]', expected) ;
        kill('unsigned long[2]', pair) ;
        kill('unsigned ichar[16]', out) ;
        kill('unsigned long', i) ;
        kill('unsigned long', j) ;
        kill('unsigned long', acc) ;
        run ret_535(conv_loaded_int('signed int', a_886)) ;
        pure(Unit)
      else
        pure(Unit) ;
      save __cerb_continue4: unit (choices: pointer:= choices,
      expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
      i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
        let strong _: loaded integer =
          bound(
            let weak a_887: pointer = pure(i) in
            seq_rmw('unsigned long', a_887, a_888 => case a_888 of
              | Specified(a_889: integer) =>
                  Specified(conv_int('unsigned long',
                  wrapI_add('unsigned long', conv_int('unsigned long', a_889), 1)))
              | Unspecified(_: ctype) =>
                  Unspecified('unsigned long')
            end)
          ) in
        pure(Unit) ;
      pure(Unit) ;
      save continue_544: unit (choices: pointer:= choices,
      expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
      i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
        pure(Unit) ;
      pure(Unit) ;
      run while_825(choices, expected, pair, out, i, j, acc)
    else
      pure(Unit) ;
  save break_545: unit (choices: pointer:= choices, expected: pointer:= expected,
  pair: pointer:= pair, out: pointer:= out, i: pointer:= i, j: pointer:= j,
  acc: pointer:= acc) in
    pure(Unit) ;
  pure(Unit) ;
  let strong a_890: loaded integer = bound(pure(Specified(0))) in
  kill('unsigned ichar[16]', choices) ;
  kill('unsigned ichar[16]', expected) ;
  kill('unsigned long[2]', pair) ;
  kill('unsigned ichar[16]', out) ;
  kill('unsigned long', i) ;
  kill('unsigned long', j) ;
  kill('unsigned long', acc) ;
  run ret_535(conv_loaded_int('signed int', a_890)) ;
  kill('unsigned ichar[16]', choices) ;
  kill('unsigned ichar[16]', expected) ;
  kill('unsigned long[2]', pair) ;
  kill('unsigned ichar[16]', out) ;
  kill('unsigned long', i) ;
  kill('unsigned long', j) ;
  kill('unsigned long', acc) ;
  pure(Unit) ;
  save ret_535: loaded integer (a_891: loaded integer:= Specified(0)) in
    pure(a_891)

