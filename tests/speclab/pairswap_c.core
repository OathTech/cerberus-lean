proc swap_pair (pair_p: pointer): eff unit :=
  let strong tmp: pointer =
    create(Ivalignof('unsigned long'), 'unsigned long') in
  let strong a_894: loaded integer =
    bound(
      let weak a_904: pointer =
        let weak a_895: loaded pointer =
          let weak (a_898: loaded pointer, a_900: loaded integer) =
            unseq(
              let weak a_903: pointer = pure(pair_p) in
              load('unsigned long*', a_903)
            ,
              pure(Specified(0))
            ) in
          case (a_898, a_900) of
            | (Specified(a_899: pointer), Specified(a_901: integer)) =>
                pure(Specified(array_shift(a_899, 'unsigned long', a_901)))
            | _: (loaded pointer,loaded integer) =>
                pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
          end in
        case a_895 of
          | Specified(a_896: pointer) =>
              let weak a_897: boolean =
                memop(PtrValidForDeref, 'unsigned long', a_896) in
              pure(
                if a_897 then
                  a_896
                else
                  undef(<<UB043_indirection_invalid_value>>)
              )
          | Unspecified(_: ctype) =>
              pure(undef(<<UB043_indirection_invalid_value>>))
        end in
      load('unsigned long', a_904)
    ) in
  store('unsigned long', tmp, conv_loaded_int('unsigned long', a_894)) ;
  let strong _: loaded integer =
    bound(
      let weak (a_905: pointer, a_925: loaded integer) =
        unseq(
          let weak a_906: loaded pointer =
            let weak (a_909: loaded pointer, a_911: loaded integer) =
              unseq(
                let weak a_914: pointer = pure(pair_p) in
                load('unsigned long*', a_914)
              ,
                pure(Specified(0))
              ) in
            case (a_909, a_911) of
              | (Specified(a_910: pointer), Specified(a_912: integer)) =>
                  pure(Specified(array_shift(a_910, 'unsigned long', a_912)))
              | _: (loaded pointer,loaded integer) =>
                  pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
            end in
          case a_906 of
            | Specified(a_907: pointer) =>
                let weak a_908: boolean =
                  memop(PtrValidForDeref, 'unsigned long', a_907) in
                pure(
                  if a_908 then
                    a_907
                  else
                    undef(<<UB043_indirection_invalid_value>>)
                )
            | Unspecified(_: ctype) =>
                pure(undef(<<UB043_indirection_invalid_value>>))
          end
        ,
          let weak a_924: pointer =
            let weak a_915: loaded pointer =
              let weak (a_918: loaded pointer, a_920: loaded integer) =
                unseq(
                  let weak a_923: pointer = pure(pair_p) in
                  load('unsigned long*', a_923)
                ,
                  pure(Specified(1))
                ) in
              case (a_918, a_920) of
                | (Specified(a_919: pointer), Specified(a_921: integer)) =>
                    pure(Specified(array_shift(a_919, 'unsigned long', a_921)))
                | _: (loaded pointer,loaded integer) =>
                    pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
              end in
            case a_915 of
              | Specified(a_916: pointer) =>
                  let weak a_917: boolean =
                    memop(PtrValidForDeref, 'unsigned long', a_916) in
                  pure(
                    if a_917 then
                      a_916
                    else
                      undef(<<UB043_indirection_invalid_value>>)
                  )
              | Unspecified(_: ctype) =>
                  pure(undef(<<UB043_indirection_invalid_value>>))
            end in
          load('unsigned long', a_924)
        ) in
      let weak _: unit =
        neg(store('unsigned long', a_905,
        conv_loaded_int('unsigned long', a_925))) in
      pure(conv_loaded_int('unsigned long', a_925))
    ) in
  pure(Unit) ;
  let strong _: loaded integer =
    bound(
      let weak (a_926: pointer, a_937: loaded integer) =
        unseq(
          let weak a_927: loaded pointer =
            let weak (a_930: loaded pointer, a_932: loaded integer) =
              unseq(
                let weak a_935: pointer = pure(pair_p) in
                load('unsigned long*', a_935)
              ,
                pure(Specified(1))
              ) in
            case (a_930, a_932) of
              | (Specified(a_931: pointer), Specified(a_933: integer)) =>
                  pure(Specified(array_shift(a_931, 'unsigned long', a_933)))
              | _: (loaded pointer,loaded integer) =>
                  pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
            end in
          case a_927 of
            | Specified(a_928: pointer) =>
                let weak a_929: boolean =
                  memop(PtrValidForDeref, 'unsigned long', a_928) in
                pure(
                  if a_929 then
                    a_928
                  else
                    undef(<<UB043_indirection_invalid_value>>)
                )
            | Unspecified(_: ctype) =>
                pure(undef(<<UB043_indirection_invalid_value>>))
          end
        ,
          let weak a_936: pointer = pure(tmp) in
          load('unsigned long', a_936)
        ) in
      let weak _: unit =
        neg(store('unsigned long', a_926,
        conv_loaded_int('unsigned long', a_937))) in
      pure(conv_loaded_int('unsigned long', a_937))
    ) in
  pure(Unit) ;
  kill('unsigned long', tmp) ;
  pure(Unit) ;
  save ret_893: unit (a_938: unit:= Unit) in  pure(a_938)

proc main (): eff loaded integer :=
  let strong choices: pointer =
    create(Ivalignof('unsigned char[16]'), 'unsigned char[16]') in
  let strong expected: pointer =
    create(Ivalignof('unsigned char[16]'), 'unsigned char[16]') in
  let strong pair: pointer =
    create(Ivalignof('unsigned long[2]'), 'unsigned long[2]') in
  let strong out: pointer =
    create(Ivalignof('unsigned char[16]'), 'unsigned char[16]') in
  let strong i: pointer = create(Ivalignof('unsigned long'), 'unsigned long') in
  let strong j: pointer = create(Ivalignof('unsigned long'), 'unsigned long') in
  let strong acc: pointer =
    create(Ivalignof('unsigned long'), 'unsigned long') in
  let strong a_556: loaded array(integer) =
    bound(
      let weak (a_572: loaded integer, a_571: loaded integer,
      a_570: loaded integer, a_569: loaded integer, a_568: loaded integer,
      a_567: loaded integer, a_566: loaded integer, a_565: loaded integer,
      a_564: loaded integer, a_563: loaded integer, a_562: loaded integer,
      a_561: loaded integer, a_560: loaded integer, a_559: loaded integer,
      a_558: loaded integer, a_557: loaded integer) =
        unseq(
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ) in
      pure(
        Specified(Array(conv_loaded_int('unsigned char', a_557),
        conv_loaded_int('unsigned char', a_558),
        conv_loaded_int('unsigned char', a_559),
        conv_loaded_int('unsigned char', a_560),
        conv_loaded_int('unsigned char', a_561),
        conv_loaded_int('unsigned char', a_562),
        conv_loaded_int('unsigned char', a_563),
        conv_loaded_int('unsigned char', a_564),
        conv_loaded_int('unsigned char', a_565),
        conv_loaded_int('unsigned char', a_566),
        conv_loaded_int('unsigned char', a_567),
        conv_loaded_int('unsigned char', a_568),
        conv_loaded_int('unsigned char', a_569),
        conv_loaded_int('unsigned char', a_570),
        conv_loaded_int('unsigned char', a_571),
        conv_loaded_int('unsigned char', a_572)))
      )
    ) in
  store_lock('unsigned char[16]', choices, a_556) ;
  let strong a_573: loaded array(integer) =
    bound(
      let weak (a_589: loaded integer, a_588: loaded integer,
      a_587: loaded integer, a_586: loaded integer, a_585: loaded integer,
      a_584: loaded integer, a_583: loaded integer, a_582: loaded integer,
      a_581: loaded integer, a_580: loaded integer, a_579: loaded integer,
      a_578: loaded integer, a_577: loaded integer, a_576: loaded integer,
      a_575: loaded integer, a_574: loaded integer) =
        unseq(
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(255))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ,
          pure(Specified(0))
        ) in
      pure(
        Specified(Array(conv_loaded_int('unsigned char', a_574),
        conv_loaded_int('unsigned char', a_575),
        conv_loaded_int('unsigned char', a_576),
        conv_loaded_int('unsigned char', a_577),
        conv_loaded_int('unsigned char', a_578),
        conv_loaded_int('unsigned char', a_579),
        conv_loaded_int('unsigned char', a_580),
        conv_loaded_int('unsigned char', a_581),
        conv_loaded_int('unsigned char', a_582),
        conv_loaded_int('unsigned char', a_583),
        conv_loaded_int('unsigned char', a_584),
        conv_loaded_int('unsigned char', a_585),
        conv_loaded_int('unsigned char', a_586),
        conv_loaded_int('unsigned char', a_587),
        conv_loaded_int('unsigned char', a_588),
        conv_loaded_int('unsigned char', a_589)))
      )
    ) in
  store_lock('unsigned char[16]', expected, a_573) ;
  store('unsigned long[2]', pair, Unspecified('unsigned long[2]')) ;
  store('unsigned char[16]', out, Unspecified('unsigned ichar[16]')) ;
  store('unsigned long', i, Unspecified('unsigned long')) ;
  store('unsigned long', j, Unspecified('unsigned long')) ;
  store('unsigned long', acc, Unspecified('unsigned long')) ;
  let strong a_590: loaded integer =
    bound(
      let weak (a_592: loaded integer, a_593: loaded integer) =
        unseq(
          let weak (a_597: loaded integer, a_598: loaded integer) =
            unseq(
              pure(Specified(Ivsizeof('unsigned char[16]')))
            ,
              pure(Specified(16))
            ) in
          pure(
            case (a_597, a_598) of
              | (Specified(a_599: integer), Specified(a_600: integer)) =>
                  if not(conv_int('size_t', a_599) = conv_int('size_t', a_600)) then
                    Specified(1)
                  else
                    Specified(0)
              | _: (loaded integer,loaded integer) =>
                  Unspecified('signed int')
            end
          )
        ,
          pure(Specified(0))
        ) in
      pure(
        case (a_592, a_593) of
          | (Specified(a_594: integer), Specified(a_595: integer)) =>
              if conv_int('signed int', a_594) = conv_int('signed int', a_595) then
                Specified(1)
              else
                Specified(0)
          | _: (loaded integer,loaded integer) =>
              Unspecified('signed int')
        end
      )
    ) in
  let strong a_553: boolean =
    case a_590 of
      | Specified(a_591: integer) =>
          pure(if not(a_591 = 1) then True else False)
      | Unspecified(_: ctype) =>
          nd(pure(True), pure(False))
    end in
  if a_553 then
    let strong a_602: loaded integer = bound(pure(Specified(254))) in
    kill('unsigned ichar[16]', choices) ;
    kill('unsigned ichar[16]', expected) ;
    kill('unsigned long[2]', pair) ;
    kill('unsigned ichar[16]', out) ;
    kill('unsigned long', i) ;
    kill('unsigned long', j) ;
    kill('unsigned long', acc) ;
    run ret_535(conv_loaded_int('signed int', a_602)) ;
    pure(Unit)
  else
    pure(Unit) ;
  let strong _: loaded integer =
    bound(
      let weak (a_603: pointer, a_604: loaded integer) =
        unseq(pure(j), pure(Specified(0))) in
      let weak _: unit =
        neg(store('unsigned long', a_603,
        conv_loaded_int('unsigned long', a_604))) in
      pure(conv_loaded_int('unsigned long', a_604))
    ) in
  pure(Unit) ;
  save while_551: unit (choices: pointer:= choices, expected: pointer:= expected,
  pair: pointer:= pair, out: pointer:= out, i: pointer:= i, j: pointer:= j,
  acc: pointer:= acc) in
    let strong a_605: loaded integer =
      bound(
        let weak (a_607: loaded integer, a_608: loaded integer) =
          unseq(
            let weak (a_613: loaded integer, a_614: loaded integer) =
              unseq(
                let weak a_612: pointer = pure(j) in
                load('unsigned long', a_612)
              ,
                pure(Specified(2))
              ) in
            case (a_613, a_614) of
              | (Specified(a_615: integer), Specified(a_616: integer)) =>
                  pure(
                    if conv_int('unsigned long', a_615) <
                    conv_int('unsigned long', a_616) then
                      Specified(1)
                    else
                      Specified(0)
                  )
              | _: (loaded integer,loaded integer) =>
                  pure(Unspecified('signed int'))
            end
          ,
            pure(Specified(0))
          ) in
        pure(
          case (a_607, a_608) of
            | (Specified(a_609: integer), Specified(a_610: integer)) =>
                if conv_int('signed int', a_609) = conv_int('signed int', a_610) then
                  Specified(1)
                else
                  Specified(0)
            | _: (loaded integer,loaded integer) =>
                Unspecified('signed int')
          end
        )
      ) in
    let strong a_552: boolean =
      case a_605 of
        | Specified(a_606: integer) =>
            pure(if not(a_606 = 1) then True else False)
        | Unspecified(_: ctype) =>
            nd(pure(True), pure(False))
      end in
    if a_552 then
      let strong _: loaded integer =
        bound(
          let weak (a_620: pointer, a_621: loaded integer) =
            unseq(pure(acc), pure(Specified(0))) in
          let weak _: unit =
            neg(store('unsigned long', a_620,
            conv_loaded_int('unsigned long', a_621))) in
          pure(conv_loaded_int('unsigned long', a_621))
        ) in
      pure(Unit) ;
      let strong _: loaded integer =
        bound(
          let weak (a_622: pointer, a_623: loaded integer) =
            unseq(pure(i), pure(Specified(0))) in
          let weak _: unit =
            neg(store('unsigned long', a_622,
            conv_loaded_int('unsigned long', a_623))) in
          pure(conv_loaded_int('unsigned long', a_623))
        ) in
      pure(Unit) ;
      save while_618: unit (choices: pointer:= choices,
      expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
      i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
        let strong a_624: loaded integer =
          bound(
            let weak (a_626: loaded integer, a_627: loaded integer) =
              unseq(
                let weak (a_632: loaded integer, a_633: loaded integer) =
                  unseq(
                    let weak a_631: pointer = pure(i) in
                    load('unsigned long', a_631)
                  ,
                    pure(Specified(8))
                  ) in
                case (a_632, a_633) of
                  | (Specified(a_634: integer), Specified(a_635: integer)) =>
                      pure(
                        if conv_int('unsigned long', a_634) <
                        conv_int('unsigned long', a_635) then
                          Specified(1)
                        else
                          Specified(0)
                      )
                  | _: (loaded integer,loaded integer) =>
                      pure(Unspecified('signed int'))
                end
              ,
                pure(Specified(0))
              ) in
            pure(
              case (a_626, a_627) of
                | (Specified(a_628: integer), Specified(a_629: integer)) =>
                    if conv_int('signed int', a_628) =
                    conv_int('signed int', a_629) then
                      Specified(1)
                    else
                      Specified(0)
                | _: (loaded integer,loaded integer) =>
                    Unspecified('signed int')
              end
            )
          ) in
        let strong a_619: boolean =
          case a_624 of
            | Specified(a_625: integer) =>
                pure(if not(a_625 = 1) then True else False)
            | Unspecified(_: ctype) =>
                nd(pure(True), pure(False))
          end in
        if a_619 then
          let strong _: loaded integer =
            bound(
              let weak (a_637: pointer, a_683: loaded integer) =
                unseq(
                  pure(acc)
                ,
                  let weak (a_638: loaded integer, a_639: loaded integer) =
                    unseq(
                      let weak a_643: pointer = pure(acc) in
                      load('unsigned long', a_643)
                    ,
                      let weak (a_644: loaded integer, a_645: loaded integer) =
                        unseq(
                          let weak a_651: loaded integer =
                            let weak a_675: pointer =
                              let weak a_653: loaded pointer =
                                let weak (a_656: loaded pointer,
                                a_658: loaded integer) =
                                  unseq(
                                    let weak a_661: pointer = pure(choices) in
                                    pure(
                                      Specified(array_shift(a_661, 'unsigned ichar', 0))
                                    )
                                  ,
                                    let weak (a_663: loaded integer,
                                    a_664: loaded integer) =
                                      unseq(
                                        let weak (a_669: loaded integer,
                                        a_670: loaded integer) =
                                          unseq(
                                            pure(Specified(8))
                                          ,
                                            let weak a_668: pointer = pure(j) in
                                            load('unsigned long', a_668)
                                          ) in
                                        pure(
                                          case (a_669, a_670) of
                                            | (Specified(a_671: integer),
                                            Specified(a_672: integer)) =>
                                                Specified(wrapI_mul('unsigned long', conv_int('unsigned long',
                                                a_671), conv_int('unsigned long',
                                                a_672)))
                                            | _: (loaded integer,loaded integer) =>
                                                Unspecified('unsigned long')
                                          end
                                        )
                                      ,
                                        let weak a_674: pointer = pure(i) in
                                        load('unsigned long', a_674)
                                      ) in
                                    pure(
                                      case (a_663, a_664) of
                                        | (Specified(a_665: integer),
                                        Specified(a_666: integer)) =>
                                            Specified(wrapI_add('unsigned long', __conv_int__('unsigned long', a_665), __conv_int__('unsigned long', a_666)))
                                        | _: (loaded integer,loaded integer) =>
                                            Unspecified('unsigned long')
                                      end
                                    )
                                  ) in
                                case (a_656, a_658) of
                                  | (Specified(a_657: pointer),
                                  Specified(a_659: integer)) =>
                                      pure(
                                        Specified(array_shift(a_657, 'unsigned ichar', a_659))
                                      )
                                  | _: (loaded pointer,loaded integer) =>
                                      pure(
                                        undef(<<UB_CERB004_unspecified__pointer_add>>)
                                      )
                                end in
                              case a_653 of
                                | Specified(a_654: pointer) =>
                                    let weak a_655: boolean =
                                      memop(PtrValidForDeref, 'unsigned char',
                                      a_654) in
                                    pure(
                                      if a_655 then
                                        a_654
                                      else
                                        undef(<<UB043_indirection_invalid_value>>)
                                    )
                                | Unspecified(_: ctype) =>
                                    pure(
                                      undef(<<UB043_indirection_invalid_value>>)
                                    )
                              end in
                            load('unsigned char', a_675) in
                          pure(conv_loaded_int('unsigned long', a_651))
                        ,
                          let weak (a_678: loaded integer, a_679: loaded integer) =
                            unseq(
                              pure(Specified(8))
                            ,
                              let weak a_677: pointer = pure(i) in
                              load('unsigned long', a_677)
                            ) in
                          pure(
                            case (a_678, a_679) of
                              | (Specified(a_680: integer),
                              Specified(a_681: integer)) =>
                                  Specified(wrapI_mul('unsigned long', conv_int('unsigned long',
                                  a_680), conv_int('unsigned long', a_681)))
                              | _: (loaded integer,loaded integer) =>
                                  Unspecified('unsigned long')
                            end
                          )
                        ) in
                      pure(
                        case (a_644, a_645) of
                          | (_: loaded integer, Unspecified(_: ctype)) =>
                              undef(<<UB036_exceptional_condition>>)
                          | (Unspecified(_: ctype), _: loaded integer) =>
                              Unspecified('unsigned long')
                          | (Specified(a_646: integer),
                          Specified(a_647: integer)) =>
                              let a_648: integer =
                                conv_int('unsigned long', a_646) in
                              let a_649: integer =
                                conv_int('unsigned long', a_647) in
                              if a_649 < 0 then
                                undef(<<UB051a_negative_shift>>)
                              else
                                if ctype_width('unsigned long') < a_649 \/
                                ctype_width('unsigned long') = a_649 then
                                  undef(<<UB51b_shift_too_large>>)
                                else
                                  Specified(a_648 * 2 ^ a_649 rem_t
                                  (Ivmax('unsigned long') + 1))
                        end
                      )
                    ) in
                  pure(
                    case (a_638, a_639) of
                      | (Specified(a_640: integer), Specified(a_641: integer)) =>
                          Specified(wrapI_add('unsigned long', __conv_int__('unsigned long', a_640), __conv_int__('unsigned long', a_641)))
                      | _: (loaded integer,loaded integer) =>
                          Unspecified('unsigned long')
                    end
                  )
                ) in
              let weak _: unit =
                neg(store('unsigned long', a_637,
                conv_loaded_int('unsigned long', a_683))) in
              pure(conv_loaded_int('unsigned long', a_683))
            ) in
          pure(Unit) ;
          save __cerb_continue1: unit (choices: pointer:= choices,
          expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
          i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
            let strong _: loaded integer =
              bound(
                let weak a_684: pointer = pure(i) in
                seq_rmw('unsigned long', a_684, a_685 => case a_685 of
                  | Specified(a_686: integer) =>
                      Specified(conv_int('unsigned long',
                      wrapI_add('unsigned long', conv_int('unsigned long', a_686), 1)))
                  | Unspecified(_: ctype) =>
                      Unspecified('unsigned long')
                end)
              ) in
            pure(Unit) ;
          pure(Unit) ;
          save continue_538: unit (choices: pointer:= choices,
          expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
          i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
            pure(Unit) ;
          pure(Unit) ;
          run while_618(choices, expected, pair, out, i, j, acc)
        else
          pure(Unit) ;
      save break_539: unit (choices: pointer:= choices,
      expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
      i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
        pure(Unit) ;
      pure(Unit) ;
      let strong _: loaded integer =
        bound(
          let weak (a_687: pointer, a_700: loaded integer) =
            unseq(
              let weak a_688: loaded pointer =
                let weak (a_691: loaded pointer, a_693: loaded integer) =
                  unseq(
                    let weak a_696: pointer = pure(pair) in
                    pure(Specified(array_shift(a_696, 'unsigned long', 0)))
                  ,
                    let weak a_698: pointer = pure(j) in
                    load('unsigned long', a_698)
                  ) in
                case (a_691, a_693) of
                  | (Specified(a_692: pointer), Specified(a_694: integer)) =>
                      pure(
                        Specified(array_shift(a_692, 'unsigned long', a_694))
                      )
                  | _: (loaded pointer,loaded integer) =>
                      pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
                end in
              case a_688 of
                | Specified(a_689: pointer) =>
                    let weak a_690: boolean =
                      memop(PtrValidForDeref, 'unsigned long', a_689) in
                    pure(
                      if a_690 then
                        a_689
                      else
                        undef(<<UB043_indirection_invalid_value>>)
                    )
                | Unspecified(_: ctype) =>
                    pure(undef(<<UB043_indirection_invalid_value>>))
              end
            ,
              let weak a_699: pointer = pure(acc) in
              load('unsigned long', a_699)
            ) in
          let weak _: unit =
            neg(store('unsigned long', a_687,
            conv_loaded_int('unsigned long', a_700))) in
          pure(conv_loaded_int('unsigned long', a_700))
        ) in
      pure(Unit) ;
      save __cerb_continue0: unit (choices: pointer:= choices,
      expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
      i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
        let strong _: loaded integer =
          bound(
            let weak a_701: pointer = pure(j) in
            seq_rmw('unsigned long', a_701, a_702 => case a_702 of
              | Specified(a_703: integer) =>
                  Specified(conv_int('unsigned long',
                  wrapI_add('unsigned long', conv_int('unsigned long', a_703), 1)))
              | Unspecified(_: ctype) =>
                  Unspecified('unsigned long')
            end)
          ) in
        pure(Unit) ;
      pure(Unit) ;
      save continue_536: unit (choices: pointer:= choices,
      expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
      i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
        pure(Unit) ;
      pure(Unit) ;
      run while_551(choices, expected, pair, out, i, j, acc)
    else
      pure(Unit) ;
  save break_537: unit (choices: pointer:= choices, expected: pointer:= expected,
  pair: pointer:= pair, out: pointer:= out, i: pointer:= i, j: pointer:= j,
  acc: pointer:= acc) in
    pure(Unit) ;
  pure(Unit) ;
  bound(
    let strong ((a_705: loaded pointer,
    (a_706: ctype, a_707: [ctype], a_708: boolean, a_709: boolean)),
    a_711: loaded pointer) =
      unseq(
        let strong a_704: loaded pointer =
          pure(Specified(Cfunction(swap_pair))) in
        pure((a_704, cfunction(a_704)))
      ,
        let weak a_712: pointer = pure(pair) in
        pure(Specified(array_shift(a_712, 'unsigned long', 0)))
      ) in
    if not(params_length(a_707) = 1) then
      pure(undef(<<UB038_number_of_args>>))
    else
      if a_708 \/ not(are_compatible ('void', a_706)) then
        pure(undef(<<UB041_function_not_compatible>>))
      else
        let strong a_710: pointer =
          let a_714: ctype = params_nth(a_707, 0) in
          if not(are_compatible ('unsigned long*', a_714)) then
            pure(undef(<<UB041_function_not_compatible>>))
          else
            let weak a_715: pointer =
              create(Ivalignof('unsigned long*'), 'unsigned long*') in
            let weak _: unit = store('unsigned long*', a_715, a_711) in
            pure(a_715) in
        let strong a_716: unit =
          ccall('void (*) (unsigned long*)', a_705, a_710) in
        kill('unsigned long*', a_710) ;
        pure(a_716)
  ) ;
  pure(Unit) ;
  let strong _: loaded integer =
    bound(
      let weak (a_717: pointer, a_718: loaded integer) =
        unseq(pure(j), pure(Specified(0))) in
      let weak _: unit =
        neg(store('unsigned long', a_717,
        conv_loaded_int('unsigned long', a_718))) in
      pure(conv_loaded_int('unsigned long', a_718))
    ) in
  pure(Unit) ;
  save while_549: unit (choices: pointer:= choices, expected: pointer:= expected,
  pair: pointer:= pair, out: pointer:= out, i: pointer:= i, j: pointer:= j,
  acc: pointer:= acc) in
    let strong a_719: loaded integer =
      bound(
        let weak (a_721: loaded integer, a_722: loaded integer) =
          unseq(
            let weak (a_727: loaded integer, a_728: loaded integer) =
              unseq(
                let weak a_726: pointer = pure(j) in
                load('unsigned long', a_726)
              ,
                pure(Specified(2))
              ) in
            case (a_727, a_728) of
              | (Specified(a_729: integer), Specified(a_730: integer)) =>
                  pure(
                    if conv_int('unsigned long', a_729) <
                    conv_int('unsigned long', a_730) then
                      Specified(1)
                    else
                      Specified(0)
                  )
              | _: (loaded integer,loaded integer) =>
                  pure(Unspecified('signed int'))
            end
          ,
            pure(Specified(0))
          ) in
        pure(
          case (a_721, a_722) of
            | (Specified(a_723: integer), Specified(a_724: integer)) =>
                if conv_int('signed int', a_723) = conv_int('signed int', a_724) then
                  Specified(1)
                else
                  Specified(0)
            | _: (loaded integer,loaded integer) =>
                Unspecified('signed int')
          end
        )
      ) in
    let strong a_550: boolean =
      case a_719 of
        | Specified(a_720: integer) =>
            pure(if not(a_720 = 1) then True else False)
        | Unspecified(_: ctype) =>
            nd(pure(True), pure(False))
      end in
    if a_550 then
      let strong _: loaded integer =
        bound(
          let weak (a_734: pointer, a_747: loaded integer) =
            unseq(
              pure(acc)
            ,
              let weak a_746: pointer =
                let weak a_735: loaded pointer =
                  let weak (a_738: loaded pointer, a_740: loaded integer) =
                    unseq(
                      let weak a_743: pointer = pure(pair) in
                      pure(Specified(array_shift(a_743, 'unsigned long', 0)))
                    ,
                      let weak a_745: pointer = pure(j) in
                      load('unsigned long', a_745)
                    ) in
                  case (a_738, a_740) of
                    | (Specified(a_739: pointer), Specified(a_741: integer)) =>
                        pure(
                          Specified(array_shift(a_739, 'unsigned long', a_741))
                        )
                    | _: (loaded pointer,loaded integer) =>
                        pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
                  end in
                case a_735 of
                  | Specified(a_736: pointer) =>
                      let weak a_737: boolean =
                        memop(PtrValidForDeref, 'unsigned long', a_736) in
                      pure(
                        if a_737 then
                          a_736
                        else
                          undef(<<UB043_indirection_invalid_value>>)
                      )
                  | Unspecified(_: ctype) =>
                      pure(undef(<<UB043_indirection_invalid_value>>))
                end in
              load('unsigned long', a_746)
            ) in
          let weak _: unit =
            neg(store('unsigned long', a_734,
            conv_loaded_int('unsigned long', a_747))) in
          pure(conv_loaded_int('unsigned long', a_747))
        ) in
      pure(Unit) ;
      let strong _: loaded integer =
        bound(
          let weak (a_748: pointer, a_749: loaded integer) =
            unseq(pure(i), pure(Specified(0))) in
          let weak _: unit =
            neg(store('unsigned long', a_748,
            conv_loaded_int('unsigned long', a_749))) in
          pure(conv_loaded_int('unsigned long', a_749))
        ) in
      pure(Unit) ;
      save while_732: unit (choices: pointer:= choices,
      expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
      i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
        let strong a_750: loaded integer =
          bound(
            let weak (a_752: loaded integer, a_753: loaded integer) =
              unseq(
                let weak (a_758: loaded integer, a_759: loaded integer) =
                  unseq(
                    let weak a_757: pointer = pure(i) in
                    load('unsigned long', a_757)
                  ,
                    pure(Specified(8))
                  ) in
                case (a_758, a_759) of
                  | (Specified(a_760: integer), Specified(a_761: integer)) =>
                      pure(
                        if conv_int('unsigned long', a_760) <
                        conv_int('unsigned long', a_761) then
                          Specified(1)
                        else
                          Specified(0)
                      )
                  | _: (loaded integer,loaded integer) =>
                      pure(Unspecified('signed int'))
                end
              ,
                pure(Specified(0))
              ) in
            pure(
              case (a_752, a_753) of
                | (Specified(a_754: integer), Specified(a_755: integer)) =>
                    if conv_int('signed int', a_754) =
                    conv_int('signed int', a_755) then
                      Specified(1)
                    else
                      Specified(0)
                | _: (loaded integer,loaded integer) =>
                    Unspecified('signed int')
              end
            )
          ) in
        let strong a_733: boolean =
          case a_750 of
            | Specified(a_751: integer) =>
                pure(if not(a_751 = 1) then True else False)
            | Unspecified(_: ctype) =>
                nd(pure(True), pure(False))
          end in
        if a_733 then
          let strong _: loaded integer =
            bound(
              let weak (a_763: pointer, a_796: loaded integer) =
                unseq(
                  let weak a_764: loaded pointer =
                    let weak (a_767: loaded pointer, a_769: loaded integer) =
                      unseq(
                        let weak a_772: pointer = pure(out) in
                        pure(Specified(array_shift(a_772, 'unsigned ichar', 0)))
                      ,
                        let weak (a_774: loaded integer, a_775: loaded integer) =
                          unseq(
                            let weak (a_780: loaded integer,
                            a_781: loaded integer) =
                              unseq(
                                pure(Specified(8))
                              ,
                                let weak a_779: pointer = pure(j) in
                                load('unsigned long', a_779)
                              ) in
                            pure(
                              case (a_780, a_781) of
                                | (Specified(a_782: integer),
                                Specified(a_783: integer)) =>
                                    Specified(wrapI_mul('unsigned long', conv_int('unsigned long',
                                    a_782), conv_int('unsigned long', a_783)))
                                | _: (loaded integer,loaded integer) =>
                                    Unspecified('unsigned long')
                              end
                            )
                          ,
                            let weak a_785: pointer = pure(i) in
                            load('unsigned long', a_785)
                          ) in
                        pure(
                          case (a_774, a_775) of
                            | (Specified(a_776: integer),
                            Specified(a_777: integer)) =>
                                Specified(wrapI_add('unsigned long', __conv_int__('unsigned long', a_776), __conv_int__('unsigned long', a_777)))
                            | _: (loaded integer,loaded integer) =>
                                Unspecified('unsigned long')
                          end
                        )
                      ) in
                    case (a_767, a_769) of
                      | (Specified(a_768: pointer), Specified(a_770: integer)) =>
                          pure(
                            Specified(array_shift(a_768, 'unsigned ichar', a_770))
                          )
                      | _: (loaded pointer,loaded integer) =>
                          pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
                    end in
                  case a_764 of
                    | Specified(a_765: pointer) =>
                        let weak a_766: boolean =
                          memop(PtrValidForDeref, 'unsigned char', a_765) in
                        pure(
                          if a_766 then
                            a_765
                          else
                            undef(<<UB043_indirection_invalid_value>>)
                        )
                    | Unspecified(_: ctype) =>
                        pure(undef(<<UB043_indirection_invalid_value>>))
                  end
                ,
                  let weak a_786: loaded integer =
                    let weak (a_789: loaded integer, a_790: loaded integer) =
                      unseq(
                        let weak a_788: pointer = pure(acc) in
                        load('unsigned long', a_788)
                      ,
                        pure(Specified(256))
                      ) in
                    pure(
                      case (a_789, a_790) of
                        | (Unspecified(_: ctype), Specified(a_792: integer)) =>
                            let a_794: integer =
                              conv_int('unsigned long', a_792) in
                            if a_794 = 0 then
                              undef(<<UB045b_modulo_by_zero>>)
                            else
                              Unspecified('unsigned long')
                        | (_: loaded integer, Unspecified(_: ctype)) =>
                            undef(<<UB045b_modulo_by_zero>>)
                        | (Specified(a_791: integer), Specified(a_792: integer)) =>
                            let a_793: integer =
                              conv_int('unsigned long', a_791) in
                            let a_794: integer =
                              conv_int('unsigned long', a_792) in
                            if a_794 = 0 then
                              undef(<<UB045b_modulo_by_zero>>)
                            else
                              if is_representable_integer(a_793 / a_794,
                              'unsigned long') then
                                Specified(a_793 rem_t a_794)
                              else
                                undef(<<UB045c_quotient_not_representable>>)
                      end
                    ) in
                  pure(conv_loaded_int('unsigned char', a_786))
                ) in
              let weak _: unit =
                neg(store('unsigned char', a_763,
                conv_loaded_int('unsigned char', a_796))) in
              pure(conv_loaded_int('unsigned char', a_796))
            ) in
          pure(Unit) ;
          let strong _: loaded integer =
            bound(
              let weak (a_797: pointer, a_805: loaded integer) =
                unseq(
                  pure(acc)
                ,
                  let weak (a_799: loaded integer, a_800: loaded integer) =
                    unseq(
                      let weak a_798: pointer = pure(acc) in
                      load('unsigned long', a_798)
                    ,
                      pure(Specified(256))
                    ) in
                  pure(
                    case (a_799, a_800) of
                      | (Unspecified(_: ctype), Specified(a_802: integer)) =>
                          let a_804: integer =
                            conv_int('unsigned long', a_802) in
                          if a_804 = 0 then
                            undef(<<UB045a_division_by_zero>>)
                          else
                            Unspecified('unsigned long')
                      | (_: loaded integer, Unspecified(_: ctype)) =>
                          undef(<<UB045a_division_by_zero>>)
                      | (Specified(a_801: integer), Specified(a_802: integer)) =>
                          let a_803: integer =
                            conv_int('unsigned long', a_801) in
                          let a_804: integer =
                            conv_int('unsigned long', a_802) in
                          if a_804 = 0 then
                            undef(<<UB045a_division_by_zero>>)
                          else
                            if is_representable_integer(a_803 / a_804,
                            'unsigned long') then
                              Specified(a_803 / a_804)
                            else
                              undef(<<UB045c_quotient_not_representable>>)
                    end
                  )
                ) in
              let weak _: unit =
                neg(store('unsigned long', a_797,
                conv_loaded_int('unsigned long', a_805))) in
              pure(conv_loaded_int('unsigned long', a_805))
            ) in
          pure(Unit) ;
          save __cerb_continue3: unit (choices: pointer:= choices,
          expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
          i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
            let strong _: loaded integer =
              bound(
                let weak a_806: pointer = pure(i) in
                seq_rmw('unsigned long', a_806, a_807 => case a_807 of
                  | Specified(a_808: integer) =>
                      Specified(conv_int('unsigned long',
                      wrapI_add('unsigned long', conv_int('unsigned long', a_808), 1)))
                  | Unspecified(_: ctype) =>
                      Unspecified('unsigned long')
                end)
              ) in
            pure(Unit) ;
          pure(Unit) ;
          save continue_542: unit (choices: pointer:= choices,
          expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
          i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
            pure(Unit) ;
          pure(Unit) ;
          run while_732(choices, expected, pair, out, i, j, acc)
        else
          pure(Unit) ;
      save break_543: unit (choices: pointer:= choices,
      expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
      i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
        pure(Unit) ;
      pure(Unit) ;
      save __cerb_continue2: unit (choices: pointer:= choices,
      expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
      i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
        let strong _: loaded integer =
          bound(
            let weak a_809: pointer = pure(j) in
            seq_rmw('unsigned long', a_809, a_810 => case a_810 of
              | Specified(a_811: integer) =>
                  Specified(conv_int('unsigned long',
                  wrapI_add('unsigned long', conv_int('unsigned long', a_811), 1)))
              | Unspecified(_: ctype) =>
                  Unspecified('unsigned long')
            end)
          ) in
        pure(Unit) ;
      pure(Unit) ;
      save continue_540: unit (choices: pointer:= choices,
      expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
      i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
        pure(Unit) ;
      pure(Unit) ;
      run while_549(choices, expected, pair, out, i, j, acc)
    else
      pure(Unit) ;
  save break_541: unit (choices: pointer:= choices, expected: pointer:= expected,
  pair: pointer:= pair, out: pointer:= out, i: pointer:= i, j: pointer:= j,
  acc: pointer:= acc) in
    pure(Unit) ;
  pure(Unit) ;
  let strong a_812: loaded integer =
    bound(
      let weak (a_814: loaded integer, a_815: loaded integer) =
        unseq(
          let weak (a_819: loaded integer, a_820: loaded integer) =
            unseq(
              pure(Specified(Ivsizeof('unsigned char[16]')))
            ,
              pure(Specified(16))
            ) in
          pure(
            case (a_819, a_820) of
              | (Specified(a_821: integer), Specified(a_822: integer)) =>
                  if not(conv_int('size_t', a_821) = conv_int('size_t', a_822)) then
                    Specified(1)
                  else
                    Specified(0)
              | _: (loaded integer,loaded integer) =>
                  Unspecified('signed int')
            end
          )
        ,
          pure(Specified(0))
        ) in
      pure(
        case (a_814, a_815) of
          | (Specified(a_816: integer), Specified(a_817: integer)) =>
              if conv_int('signed int', a_816) = conv_int('signed int', a_817) then
                Specified(1)
              else
                Specified(0)
          | _: (loaded integer,loaded integer) =>
              Unspecified('signed int')
        end
      )
    ) in
  let strong a_548: boolean =
    case a_812 of
      | Specified(a_813: integer) =>
          pure(if not(a_813 = 1) then True else False)
      | Unspecified(_: ctype) =>
          nd(pure(True), pure(False))
    end in
  if a_548 then
    let strong a_824: loaded integer = bound(pure(Specified(255))) in
    kill('unsigned ichar[16]', choices) ;
    kill('unsigned ichar[16]', expected) ;
    kill('unsigned long[2]', pair) ;
    kill('unsigned ichar[16]', out) ;
    kill('unsigned long', i) ;
    kill('unsigned long', j) ;
    kill('unsigned long', acc) ;
    run ret_535(conv_loaded_int('signed int', a_824)) ;
    pure(Unit)
  else
    pure(Unit) ;
  let strong _: loaded integer =
    bound(
      let weak (a_825: pointer, a_826: loaded integer) =
        unseq(pure(i), pure(Specified(0))) in
      let weak _: unit =
        neg(store('unsigned long', a_825,
        conv_loaded_int('unsigned long', a_826))) in
      pure(conv_loaded_int('unsigned long', a_826))
    ) in
  pure(Unit) ;
  save while_546: unit (choices: pointer:= choices, expected: pointer:= expected,
  pair: pointer:= pair, out: pointer:= out, i: pointer:= i, j: pointer:= j,
  acc: pointer:= acc) in
    let strong a_827: loaded integer =
      bound(
        let weak (a_829: loaded integer, a_830: loaded integer) =
          unseq(
            let weak (a_835: loaded integer, a_836: loaded integer) =
              unseq(
                let weak a_834: pointer = pure(i) in
                load('unsigned long', a_834)
              ,
                pure(Specified(16))
              ) in
            case (a_835, a_836) of
              | (Specified(a_837: integer), Specified(a_838: integer)) =>
                  pure(
                    if conv_int('unsigned long', a_837) <
                    conv_int('unsigned long', a_838) then
                      Specified(1)
                    else
                      Specified(0)
                  )
              | _: (loaded integer,loaded integer) =>
                  pure(Unspecified('signed int'))
            end
          ,
            pure(Specified(0))
          ) in
        pure(
          case (a_829, a_830) of
            | (Specified(a_831: integer), Specified(a_832: integer)) =>
                if conv_int('signed int', a_831) = conv_int('signed int', a_832) then
                  Specified(1)
                else
                  Specified(0)
            | _: (loaded integer,loaded integer) =>
                Unspecified('signed int')
          end
        )
      ) in
    let strong a_547: boolean =
      case a_827 of
        | Specified(a_828: integer) =>
            pure(if not(a_828 = 1) then True else False)
        | Unspecified(_: ctype) =>
            nd(pure(True), pure(False))
      end in
    if a_547 then
      let strong a_841: loaded integer =
        bound(
          let weak (a_843: loaded integer, a_844: loaded integer) =
            unseq(
              let weak (a_848: loaded integer, a_849: loaded integer) =
                unseq(
                  let weak a_864: pointer =
                    let weak a_853: loaded pointer =
                      let weak (a_856: loaded pointer, a_858: loaded integer) =
                        unseq(
                          let weak a_861: pointer = pure(out) in
                          pure(
                            Specified(array_shift(a_861, 'unsigned ichar', 0))
                          )
                        ,
                          let weak a_863: pointer = pure(i) in
                          load('unsigned long', a_863)
                        ) in
                      case (a_856, a_858) of
                        | (Specified(a_857: pointer), Specified(a_859: integer)) =>
                            pure(
                              Specified(array_shift(a_857, 'unsigned ichar', a_859))
                            )
                        | _: (loaded pointer,loaded integer) =>
                            pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
                      end in
                    case a_853 of
                      | Specified(a_854: pointer) =>
                          let weak a_855: boolean =
                            memop(PtrValidForDeref, 'unsigned char', a_854) in
                          pure(
                            if a_855 then
                              a_854
                            else
                              undef(<<UB043_indirection_invalid_value>>)
                          )
                      | Unspecified(_: ctype) =>
                          pure(undef(<<UB043_indirection_invalid_value>>))
                    end in
                  load('unsigned char', a_864)
                ,
                  let weak a_876: pointer =
                    let weak a_865: loaded pointer =
                      let weak (a_868: loaded pointer, a_870: loaded integer) =
                        unseq(
                          let weak a_873: pointer = pure(expected) in
                          pure(
                            Specified(array_shift(a_873, 'unsigned ichar', 0))
                          )
                        ,
                          let weak a_875: pointer = pure(i) in
                          load('unsigned long', a_875)
                        ) in
                      case (a_868, a_870) of
                        | (Specified(a_869: pointer), Specified(a_871: integer)) =>
                            pure(
                              Specified(array_shift(a_869, 'unsigned ichar', a_871))
                            )
                        | _: (loaded pointer,loaded integer) =>
                            pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
                      end in
                    case a_865 of
                      | Specified(a_866: pointer) =>
                          let weak a_867: boolean =
                            memop(PtrValidForDeref, 'unsigned char', a_866) in
                          pure(
                            if a_867 then
                              a_866
                            else
                              undef(<<UB043_indirection_invalid_value>>)
                          )
                      | Unspecified(_: ctype) =>
                          pure(undef(<<UB043_indirection_invalid_value>>))
                    end in
                  load('unsigned char', a_876)
                ) in
              pure(
                case (a_848, a_849) of
                  | (Specified(a_850: integer), Specified(a_851: integer)) =>
                      if not(conv_int('signed int', a_850) =
                      conv_int('signed int', a_851)) then
                        Specified(1)
                      else
                        Specified(0)
                  | _: (loaded integer,loaded integer) =>
                      Unspecified('signed int')
                end
              )
            ,
              pure(Specified(0))
            ) in
          pure(
            case (a_843, a_844) of
              | (Specified(a_845: integer), Specified(a_846: integer)) =>
                  if conv_int('signed int', a_845) =
                  conv_int('signed int', a_846) then
                    Specified(1)
                  else
                    Specified(0)
              | _: (loaded integer,loaded integer) =>
                  Unspecified('signed int')
            end
          )
        ) in
      let strong a_840: boolean =
        case a_841 of
          | Specified(a_842: integer) =>
              pure(if not(a_842 = 1) then True else False)
          | Unspecified(_: ctype) =>
              nd(pure(True), pure(False))
        end in
      if a_840 then
        let strong a_886: loaded integer =
          bound(
            let weak a_877: loaded integer =
              let weak (a_879: loaded integer, a_880: loaded integer) =
                unseq(
                  pure(Specified(1))
                ,
                  let weak a_884: pointer = pure(i) in
                  load('unsigned long', a_884)
                ) in
              pure(
                case (a_879, a_880) of
                  | (Specified(a_881: integer), Specified(a_882: integer)) =>
                      Specified(wrapI_add('unsigned long', __conv_int__('unsigned long', a_881), __conv_int__('unsigned long', a_882)))
                  | _: (loaded integer,loaded integer) =>
                      Unspecified('unsigned long')
                end
              ) in
            pure(conv_loaded_int('signed int', a_877))
          ) in
        kill('unsigned ichar[16]', choices) ;
        kill('unsigned ichar[16]', expected) ;
        kill('unsigned long[2]', pair) ;
        kill('unsigned ichar[16]', out) ;
        kill('unsigned long', i) ;
        kill('unsigned long', j) ;
        kill('unsigned long', acc) ;
        run ret_535(conv_loaded_int('signed int', a_886)) ;
        pure(Unit)
      else
        pure(Unit) ;
      save __cerb_continue4: unit (choices: pointer:= choices,
      expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
      i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
        let strong _: loaded integer =
          bound(
            let weak a_887: pointer = pure(i) in
            seq_rmw('unsigned long', a_887, a_888 => case a_888 of
              | Specified(a_889: integer) =>
                  Specified(conv_int('unsigned long',
                  wrapI_add('unsigned long', conv_int('unsigned long', a_889), 1)))
              | Unspecified(_: ctype) =>
                  Unspecified('unsigned long')
            end)
          ) in
        pure(Unit) ;
      pure(Unit) ;
      save continue_544: unit (choices: pointer:= choices,
      expected: pointer:= expected, pair: pointer:= pair, out: pointer:= out,
      i: pointer:= i, j: pointer:= j, acc: pointer:= acc) in
        pure(Unit) ;
      pure(Unit) ;
      run while_546(choices, expected, pair, out, i, j, acc)
    else
      pure(Unit) ;
  save break_545: unit (choices: pointer:= choices, expected: pointer:= expected,
  pair: pointer:= pair, out: pointer:= out, i: pointer:= i, j: pointer:= j,
  acc: pointer:= acc) in
    pure(Unit) ;
  pure(Unit) ;
  let strong a_890: loaded integer = bound(pure(Specified(0))) in
  kill('unsigned ichar[16]', choices) ;
  kill('unsigned ichar[16]', expected) ;
  kill('unsigned long[2]', pair) ;
  kill('unsigned ichar[16]', out) ;
  kill('unsigned long', i) ;
  kill('unsigned long', j) ;
  kill('unsigned long', acc) ;
  run ret_535(conv_loaded_int('signed int', a_890)) ;
  kill('unsigned ichar[16]', choices) ;
  kill('unsigned ichar[16]', expected) ;
  kill('unsigned long[2]', pair) ;
  kill('unsigned ichar[16]', out) ;
  kill('unsigned long', i) ;
  kill('unsigned long', j) ;
  kill('unsigned long', acc) ;
  pure(Unit) ;
  save ret_535: loaded integer (a_891: loaded integer:= Specified(0)) in
    pure(a_891)

