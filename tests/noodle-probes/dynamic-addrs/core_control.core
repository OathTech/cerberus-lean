-- CONTROL: same program without the zero-size alloc; free(x) must be UB179a.
proc main (): eff loaded integer :=
  let strong x_ptr: pointer = create(16, 'signed int') in
  store('signed int', x_ptr, Specified(1)) ;
  free(x_ptr) ;
  pure(Specified(0))
