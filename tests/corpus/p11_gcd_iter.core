proc gcd (a: pointer, b: pointer): eff loaded integer :=
  save while_550: unit () in
    let strong a_552: loaded integer =
      bound(
        let weak (a_554: loaded integer, a_555: loaded integer) =
          unseq(
            let weak (a_559: loaded integer, a_560: loaded integer) =
              unseq(
                let weak a_564: pointer = pure(b) in
                load('signed int', a_564)
              ,
                pure(Specified(0))
              ) in
            pure(
              case (a_559, a_560) of
                | (Specified(a_561: integer), Specified(a_562: integer)) =>
                    if not(conv_int('signed int', a_561) =
                    conv_int('signed int', a_562)) then
                      Specified(1)
                    else
                      Specified(0)
                | _: (loaded integer,loaded integer) =>
                    Unspecified('signed int')
              end
            )
          ,
            pure(Specified(0))
          ) in
        pure(
          case (a_554, a_555) of
            | (Specified(a_556: integer), Specified(a_557: integer)) =>
                if conv_int('signed int', a_556) = conv_int('signed int', a_557) then
                  Specified(1)
                else
                  Specified(0)
            | _: (loaded integer,loaded integer) =>
                Unspecified('signed int')
          end
        )
      ) in
    let strong a_551: boolean =
      case a_552 of
        | Specified(a_553: integer) =>
            pure(if not(a_553 = 1) then True else False)
        | Unspecified(_: ctype) =>
            nd(pure(True), pure(False))
      end in
    if a_551 then
      let strong t: pointer = create(Ivalignof('signed int'), 'signed int') in
      let strong a_565: loaded integer =
        bound(
          let weak (a_568: loaded integer, a_569: loaded integer) =
            unseq(
              let weak a_566: pointer = pure(a) in
              load('signed int', a_566)
            ,
              let weak a_567: pointer = pure(b) in
              load('signed int', a_567)
            ) in
          pure(
            case (a_568, a_569) of
              | (Unspecified(_: ctype), Specified(a_571: integer)) =>
                  let a_573: integer = conv_int('signed int', a_571) in
                  if a_573 = 0 then
                    undef(<<UB045b_modulo_by_zero>>)
                  else
                    undef(<<UB036_exceptional_condition>>)
              | (_: loaded integer, Unspecified(_: ctype)) =>
                  undef(<<UB045b_modulo_by_zero>>)
              | (Specified(a_570: integer), Specified(a_571: integer)) =>
                  let a_572: integer = conv_int('signed int', a_570) in
                  let a_573: integer = conv_int('signed int', a_571) in
                  if a_573 = 0 then
                    undef(<<UB045b_modulo_by_zero>>)
                  else
                    if is_representable_integer(a_572 / a_573, 'signed int') then
                      Specified(a_572 rem_t a_573)
                    else
                      undef(<<UB045c_quotient_not_representable>>)
            end
          )
        ) in
      store('signed int', t, conv_loaded_int('signed int', a_565)) ;
      let strong _: loaded integer =
        bound(
          let weak (a_574: pointer, a_576: loaded integer) =
            unseq(
              pure(a)
            ,
              let weak a_575: pointer = pure(b) in
              load('signed int', a_575)
            ) in
          let weak _: unit =
            neg(store('signed int', a_574, conv_loaded_int('signed int', a_576))) in
          pure(conv_loaded_int('signed int', a_576))
        ) in
      pure(Unit) ;
      let strong _: loaded integer =
        bound(
          let weak (a_577: pointer, a_579: loaded integer) =
            unseq(
              pure(b)
            ,
              let weak a_578: pointer = pure(t) in
              load('signed int', a_578)
            ) in
          let weak _: unit =
            neg(store('signed int', a_577, conv_loaded_int('signed int', a_579))) in
          pure(conv_loaded_int('signed int', a_579))
        ) in
      pure(Unit) ;
      kill('signed int', t) ;
      pure(Unit) ;
      save continue_548: unit () in  pure(Unit) ;
      pure(Unit) ;
      run while_550()
    else
      pure(Unit) ;
  save break_549: unit () in  pure(Unit) ;
  pure(Unit) ;
  let strong a_581: loaded integer =
    bound(
      let weak a_580: pointer = pure(a) in
      load('signed int', a_580)
    ) in
  run ret_547(conv_loaded_int('signed int', a_581)) ;
  pure(Unit) ;
  save ret_547: loaded integer (a_582: loaded integer:= undef(<<UB088_reached_end_of_function>>)) in
    pure(a_582)

proc main (): eff loaded integer :=
  let strong a_543: loaded integer =
    bound(
      let strong a_539: loaded integer =
        let weak (a_514: loaded integer, a_515: loaded integer) =
          unseq(
            let weak (a_519: loaded integer, a_520: loaded integer) =
              unseq(
                let strong ((a_525: loaded pointer,
                (a_526: ctype, a_527: [ctype], a_528: boolean, a_529: boolean)),
                a_532: loaded integer, a_533: loaded integer) =
                  unseq(
                    let strong a_524: loaded pointer =
                      pure(Specified(Cfunction(gcd))) in
                    pure((a_524, cfunction(a_524)))
                  ,
                    pure(Specified(12))
                  ,
                    pure(Specified(18))
                  ) in
                if not(params_length(a_527) = 2) then
                  pure(undef(<<UB038_number_of_args>>))
                else
                  if a_528 \/ not(are_compatible ('signed int', a_526)) then
                    pure(undef(<<UB041_function_not_compatible>>))
                  else
                    let strong a_530: pointer =
                      let a_534: ctype = params_nth(a_527, 0) in
                      if not(are_compatible ('signed int', a_534)) then
                        pure(undef(<<UB041_function_not_compatible>>))
                      else
                        let weak a_535: pointer =
                          create(Ivalignof('signed int'), 'signed int') in
                        let weak _: unit =
                          store('signed int', a_535,
                          conv_loaded_int('signed int', a_532)) in
                        pure(a_535) in
                    let strong a_531: pointer =
                      let a_536: ctype = params_nth(a_527, 1) in
                      if not(are_compatible ('signed int', a_536)) then
                        pure(undef(<<UB041_function_not_compatible>>))
                      else
                        let weak a_537: pointer =
                          create(Ivalignof('signed int'), 'signed int') in
                        let weak _: unit =
                          store('signed int', a_537,
                          conv_loaded_int('signed int', a_533)) in
                        pure(a_537) in
                    let strong a_538: loaded integer =
                      ccall('signed int (*) (signed int, signed int)', a_525,
                      a_530, a_531) in
                    let strong _: (unit,unit) =
                      unseq(
                        kill('signed int', a_530)
                      ,
                        kill('signed int', a_531)
                      ) in
                    pure(a_538)
              ,
                pure(Specified(6))
              ) in
            pure(
              case (a_519, a_520) of
                | (Specified(a_521: integer), Specified(a_522: integer)) =>
                    if conv_int('signed int', a_521) =
                    conv_int('signed int', a_522) then
                      Specified(1)
                    else
                      Specified(0)
                | _: (loaded integer,loaded integer) =>
                    Unspecified('signed int')
              end
            )
          ,
            pure(Specified(0))
          ) in
        pure(
          case (a_514, a_515) of
            | (Specified(a_516: integer), Specified(a_517: integer)) =>
                if conv_int('signed int', a_516) = conv_int('signed int', a_517) then
                  Specified(1)
                else
                  Specified(0)
            | _: (loaded integer,loaded integer) =>
                Unspecified('signed int')
          end
        ) in
      case a_539 of
        | Specified(a_540: integer) =>
            if a_540 = 0 then
              let strong a_541: loaded integer = pure(Specified(0)) in
              pure(conv_loaded_int('signed int', a_541))
            else
              let strong a_542: loaded integer = pure(Specified(1)) in
              pure(conv_loaded_int('signed int', a_542))
        | Unspecified(_: ctype) =>
            pure(undef(<<UB_CERB004_unspecified__conditional>>))
      end
    ) in
  run ret_513(conv_loaded_int('signed int', a_543)) ;
  pure(Unit) ;
  save ret_513: loaded integer (a_544: loaded integer:= Specified(0)) in
    pure(a_544)

