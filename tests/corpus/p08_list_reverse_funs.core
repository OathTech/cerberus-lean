-- Aggregates
def struct node :=
  head: 'signed int'
  tail: 'struct node*'

-- Fun map
proc list_reverse (p: pointer): eff loaded pointer :=
  let strong acc: pointer = create(Ivalignof('struct node*'), 'struct node*') in
  store('struct node*', acc, Specified(NULL(struct node))) ;
  save while_787: unit (acc: pointer:= acc) in
    let strong a_789: loaded integer =
      bound(
        let weak a_792: loaded pointer =
          let weak a_791: pointer = pure(p) in
          load('struct node*', a_791) in
        case a_792 of
          | Specified(a_793: pointer) =>
              let weak a_794: boolean =
                memop(PtrEq, a_793, NULL(struct node*)) in
              pure(if a_794 then Specified(1) else Specified(0))
          | _: loaded pointer =>
              pure(undef(<<UB_CERB004_unspecified__equality_ptr_vs_NULL>>))
        end
      ) in
    let strong a_788: boolean =
      case a_789 of
        | Specified(a_790: integer) =>
            pure(if not(a_790 = 1) then True else False)
        | Unspecified(_: ctype) =>
            nd(pure(True), pure(False))
      end in
    if a_788 then
      let strong t: pointer =
        create(Ivalignof('struct node*'), 'struct node*') in
      let strong a_795: loaded pointer =
        bound(
          let weak a_801: pointer =
            let strong a_796: loaded pointer =
              let weak a_800: pointer = pure(p) in
              load('struct node*', a_800) in
            case a_796 of
              | Unspecified(_: ctype) =>
                  pure(undef(<<UB_CERB004_unspecified__memberofptr>>))
              | Specified(a_797: pointer) =>
                  pure(member_shift(a_797, node, .tail))
            end in
          load('struct node*', a_801)
        ) in
      store('struct node*', t, a_795) ;
      let strong _: loaded pointer =
        bound(
          let weak (a_802: pointer, a_809: loaded pointer) =
            unseq(
              let strong a_803: loaded pointer =
                let weak a_807: pointer = pure(p) in
                load('struct node*', a_807) in
              case a_803 of
                | Unspecified(_: ctype) =>
                    pure(undef(<<UB_CERB004_unspecified__memberofptr>>))
                | Specified(a_804: pointer) =>
                    pure(member_shift(a_804, node, .tail))
              end
            ,
              let weak a_808: pointer = pure(acc) in
              load('struct node*', a_808)
            ) in
          let weak _: unit = neg(store('struct node*', a_802, a_809)) in
          pure(a_809)
        ) in
      pure(Unit) ;
      let strong _: loaded pointer =
        bound(
          let weak (a_810: pointer, a_812: loaded pointer) =
            unseq(
              pure(acc)
            ,
              let weak a_811: pointer = pure(p) in
              load('struct node*', a_811)
            ) in
          let weak _: unit = neg(store('struct node*', a_810, a_812)) in
          pure(a_812)
        ) in
      pure(Unit) ;
      let strong _: loaded pointer =
        bound(
          let weak (a_813: pointer, a_815: loaded pointer) =
            unseq(
              pure(p)
            ,
              let weak a_814: pointer = pure(t) in
              load('struct node*', a_814)
            ) in
          let weak _: unit = neg(store('struct node*', a_813, a_815)) in
          pure(a_815)
        ) in
      pure(Unit) ;
      kill('struct node*', t) ;
      pure(Unit) ;
      save continue_784: unit (acc: pointer:= acc) in  pure(Unit) ;
      pure(Unit) ;
      run while_787(acc)
    else
      pure(Unit) ;
  save break_785: unit (acc: pointer:= acc) in  pure(Unit) ;
  pure(Unit) ;
  let strong a_817: loaded pointer =
    bound(
      let weak a_816: pointer = pure(acc) in
      load('struct node*', a_816)
    ) in
  kill('struct node*', acc) ;
  run ret_783(a_817) ;
  kill('struct node*', acc) ;
  pure(Unit) ;
  save ret_783: loaded pointer (a_818: loaded pointer:= undef(<<UB088_reached_end_of_function>>)) in
    pure(a_818)

proc main (): eff loaded integer :=
  let strong n: pointer = create(Ivalignof('signed int'), 'signed int') in
  let strong hd: pointer = create(Ivalignof('struct node*'), 'struct node*') in
  let strong r: pointer = create(Ivalignof('struct node*'), 'struct node*') in
  let strong a_536: loaded integer =
    bound(
      let weak a_547: pointer =
        let weak a_537: loaded pointer =
          let weak (a_540: loaded pointer, a_542: loaded integer) =
            unseq(
              let weak a_545: pointer = pure(choices) in
              pure(Specified(array_shift(a_545, 'signed int', 0)))
            ,
              pure(Specified(0))
            ) in
          case (a_540, a_542) of
            | (Specified(a_541: pointer), Specified(a_543: integer)) =>
                pure(Specified(array_shift(a_541, 'signed int', a_543)))
            | _: (loaded pointer,loaded integer) =>
                pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
          end in
        case a_537 of
          | Specified(a_538: pointer) =>
              let weak a_539: boolean =
                memop(PtrValidForDeref, 'signed int', a_538) in
              pure(
                if a_539 then
                  a_538
                else
                  undef(<<UB043_indirection_invalid_value>>)
              )
          | Unspecified(_: ctype) =>
              pure(undef(<<UB043_indirection_invalid_value>>))
        end in
      load('signed int', a_547)
    ) in
  store('signed int', n, conv_loaded_int('signed int', a_536)) ;
  store('struct node*', hd, Specified(NULL(struct node))) ;
  let strong i: pointer = create(Ivalignof('signed int'), 'signed int') in
  let strong a_549: loaded integer =
    bound(
      let weak (a_551: loaded integer, a_552: loaded integer) =
        unseq(
          let weak a_550: pointer = pure(n) in
          load('signed int', a_550)
        ,
          pure(Specified(1))
        ) in
      pure(
        case (a_551, a_552) of
          | (Specified(a_553: integer), Specified(a_554: integer)) =>
              Specified(catch_exceptional_condition_sub('signed int', conv_int('signed int',
              a_553), conv_int('signed int', a_554)))
          | _: (loaded integer,loaded integer) =>
              undef(<<UB036_exceptional_condition>>)
        end
      )
    ) in
  store('signed int', i, conv_loaded_int('signed int', a_549)) ;
  save while_556: unit (i: pointer:= i, n: pointer:= n, hd: pointer:= hd,
  r: pointer:= r) in
    let strong a_558: loaded integer =
      bound(
        let weak (a_560: loaded integer, a_561: loaded integer) =
          unseq(
            let weak (a_566: loaded integer, a_567: loaded integer) =
              unseq(
                let weak a_565: pointer = pure(i) in
                load('signed int', a_565)
              ,
                pure(Specified(0))
              ) in
            case (a_566, a_567) of
              | (Specified(a_568: integer), Specified(a_569: integer)) =>
                  pure(
                    if conv_int('signed int', a_568) >=
                    conv_int('signed int', a_569) then
                      Specified(1)
                    else
                      Specified(0)
                  )
              | _: (loaded integer,loaded integer) =>
                  pure(Unspecified('signed int'))
            end
          ,
            pure(Specified(0))
          ) in
        pure(
          case (a_560, a_561) of
            | (Specified(a_562: integer), Specified(a_563: integer)) =>
                if conv_int('signed int', a_562) = conv_int('signed int', a_563) then
                  Specified(1)
                else
                  Specified(0)
            | _: (loaded integer,loaded integer) =>
                Unspecified('signed int')
          end
        )
      ) in
    let strong a_557: boolean =
      case a_558 of
        | Specified(a_559: integer) =>
            pure(if not(a_559 = 1) then True else False)
        | Unspecified(_: ctype) =>
            nd(pure(True), pure(False))
      end in
    if a_557 then
      let strong slot: pointer =
        create(Ivalignof('signed int'), 'signed int') in
      let strong a_571: loaded integer =
        bound(
          let weak a_588: pointer =
            let weak a_572: loaded pointer =
              let weak (a_575: loaded pointer, a_577: loaded integer) =
                unseq(
                  let weak a_580: pointer = pure(choices) in
                  pure(Specified(array_shift(a_580, 'signed int', 0)))
                ,
                  let weak (a_582: loaded integer, a_583: loaded integer) =
                    unseq(
                      pure(Specified(1))
                    ,
                      let weak a_587: pointer = pure(i) in
                      load('signed int', a_587)
                    ) in
                  pure(
                    case (a_582, a_583) of
                      | (Specified(a_584: integer), Specified(a_585: integer)) =>
                          Specified(catch_exceptional_condition_add('signed int', __conv_int__('signed int', a_584), __conv_int__('signed int', a_585)))
                      | _: (loaded integer,loaded integer) =>
                          undef(<<UB036_exceptional_condition>>)
                    end
                  )
                ) in
              case (a_575, a_577) of
                | (Specified(a_576: pointer), Specified(a_578: integer)) =>
                    pure(Specified(array_shift(a_576, 'signed int', a_578)))
                | _: (loaded pointer,loaded integer) =>
                    pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
              end in
            case a_572 of
              | Specified(a_573: pointer) =>
                  let weak a_574: boolean =
                    memop(PtrValidForDeref, 'signed int', a_573) in
                  pure(
                    if a_574 then
                      a_573
                    else
                      undef(<<UB043_indirection_invalid_value>>)
                  )
              | Unspecified(_: ctype) =>
                  pure(undef(<<UB043_indirection_invalid_value>>))
            end in
          load('signed int', a_588)
        ) in
      store('signed int', slot, conv_loaded_int('signed int', a_571)) ;
      let strong _: loaded integer =
        bound(
          let weak (a_589: pointer, a_625: loaded integer) =
            unseq(
              let strong a_590: pointer =
                let weak a_591: loaded pointer =
                  let weak (a_594: loaded pointer, a_596: loaded integer) =
                    unseq(
                      let weak a_599: pointer = pure(arena) in
                      pure(Specified(array_shift(a_599, 'struct node', 0)))
                    ,
                      let weak a_601: pointer = pure(slot) in
                      load('signed int', a_601)
                    ) in
                  case (a_594, a_596) of
                    | (Specified(a_595: pointer), Specified(a_597: integer)) =>
                        pure(
                          Specified(array_shift(a_595, 'struct node', a_597))
                        )
                    | _: (loaded pointer,loaded integer) =>
                        pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
                  end in
                case a_591 of
                  | Specified(a_592: pointer) =>
                      let weak a_593: boolean =
                        memop(PtrValidForDeref, 'struct node', a_592) in
                      pure(
                        if a_593 then
                          a_592
                        else
                          undef(<<UB043_indirection_invalid_value>>)
                      )
                  | Unspecified(_: ctype) =>
                      pure(undef(<<UB043_indirection_invalid_value>>))
                end in
              pure(member_shift(a_590, node, .head))
            ,
              let weak a_624: pointer =
                let weak a_602: loaded pointer =
                  let weak (a_605: loaded pointer, a_607: loaded integer) =
                    unseq(
                      let weak a_610: pointer = pure(choices) in
                      pure(Specified(array_shift(a_610, 'signed int', 0)))
                    ,
                      let weak (a_612: loaded integer, a_613: loaded integer) =
                        unseq(
                          let weak (a_617: loaded integer, a_618: loaded integer) =
                            unseq(
                              pure(Specified(1))
                            ,
                              let weak a_622: pointer = pure(n) in
                              load('signed int', a_622)
                            ) in
                          pure(
                            case (a_617, a_618) of
                              | (Specified(a_619: integer),
                              Specified(a_620: integer)) =>
                                  Specified(catch_exceptional_condition_add('signed int', __conv_int__('signed int', a_619), __conv_int__('signed int', a_620)))
                              | _: (loaded integer,loaded integer) =>
                                  undef(<<UB036_exceptional_condition>>)
                            end
                          )
                        ,
                          let weak a_623: pointer = pure(i) in
                          load('signed int', a_623)
                        ) in
                      pure(
                        case (a_612, a_613) of
                          | (Specified(a_614: integer),
                          Specified(a_615: integer)) =>
                              Specified(catch_exceptional_condition_add('signed int', __conv_int__('signed int', a_614), __conv_int__('signed int', a_615)))
                          | _: (loaded integer,loaded integer) =>
                              undef(<<UB036_exceptional_condition>>)
                        end
                      )
                    ) in
                  case (a_605, a_607) of
                    | (Specified(a_606: pointer), Specified(a_608: integer)) =>
                        pure(Specified(array_shift(a_606, 'signed int', a_608)))
                    | _: (loaded pointer,loaded integer) =>
                        pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
                  end in
                case a_602 of
                  | Specified(a_603: pointer) =>
                      let weak a_604: boolean =
                        memop(PtrValidForDeref, 'signed int', a_603) in
                      pure(
                        if a_604 then
                          a_603
                        else
                          undef(<<UB043_indirection_invalid_value>>)
                      )
                  | Unspecified(_: ctype) =>
                      pure(undef(<<UB043_indirection_invalid_value>>))
                end in
              load('signed int', a_624)
            ) in
          let weak _: unit =
            neg(store('signed int', a_589, conv_loaded_int('signed int', a_625))) in
          pure(conv_loaded_int('signed int', a_625))
        ) in
      pure(Unit) ;
      let strong _: loaded pointer =
        bound(
          let weak (a_626: pointer, a_640: loaded pointer) =
            unseq(
              let strong a_627: pointer =
                let weak a_628: loaded pointer =
                  let weak (a_631: loaded pointer, a_633: loaded integer) =
                    unseq(
                      let weak a_636: pointer = pure(arena) in
                      pure(Specified(array_shift(a_636, 'struct node', 0)))
                    ,
                      let weak a_638: pointer = pure(slot) in
                      load('signed int', a_638)
                    ) in
                  case (a_631, a_633) of
                    | (Specified(a_632: pointer), Specified(a_634: integer)) =>
                        pure(
                          Specified(array_shift(a_632, 'struct node', a_634))
                        )
                    | _: (loaded pointer,loaded integer) =>
                        pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
                  end in
                case a_628 of
                  | Specified(a_629: pointer) =>
                      let weak a_630: boolean =
                        memop(PtrValidForDeref, 'struct node', a_629) in
                      pure(
                        if a_630 then
                          a_629
                        else
                          undef(<<UB043_indirection_invalid_value>>)
                      )
                  | Unspecified(_: ctype) =>
                      pure(undef(<<UB043_indirection_invalid_value>>))
                end in
              pure(member_shift(a_627, node, .tail))
            ,
              let weak a_639: pointer = pure(hd) in
              load('struct node*', a_639)
            ) in
          let weak _: unit = neg(store('struct node*', a_626, a_640)) in
          pure(a_640)
        ) in
      pure(Unit) ;
      let strong _: loaded pointer =
        bound(
          let weak (a_641: pointer, a_650: loaded pointer) =
            unseq(
              pure(hd)
            ,
              let weak (a_642: loaded pointer, a_644: loaded integer) =
                unseq(
                  let weak a_647: pointer = pure(arena) in
                  pure(Specified(array_shift(a_647, 'struct node', 0)))
                ,
                  let weak a_649: pointer = pure(slot) in
                  load('signed int', a_649)
                ) in
              case (a_642, a_644) of
                | (Specified(a_643: pointer), Specified(a_645: integer)) =>
                    pure(Specified(array_shift(a_643, 'struct node', a_645)))
                | _: (loaded pointer,loaded integer) =>
                    pure(undef(<<UB_CERB004_unspecified__pointer_add>>))
              end
            ) in
          let weak _: unit = neg(store('struct node*', a_641, a_650)) in
          pure(a_650)
        ) in
      pure(Unit) ;
      save __cerb_continue1: unit (slot: pointer:= slot, i: pointer:= i,
      n: pointer:= n, hd: pointer:= hd, r: pointer:= r) in
        let strong _: loaded integer =
          bound(
            let weak (a_651: pointer, a_658: loaded integer) =
              unseq(
                pure(i)
              ,
                let weak (a_653: loaded integer, a_654: loaded integer) =
                  unseq(
                    let weak a_652: pointer = pure(i) in
                    load('signed int', a_652)
                  ,
                    pure(Specified(1))
                  ) in
                pure(
                  case (a_653, a_654) of
                    | (Specified(a_655: integer), Specified(a_656: integer)) =>
                        Specified(catch_exceptional_condition_sub('signed int', conv_int('signed int',
                        a_655), conv_int('signed int', a_656)))
                    | _: (loaded integer,loaded integer) =>
                        undef(<<UB036_exceptional_condition>>)
                  end
                )
              ) in
            let weak _: unit =
              neg(store('signed int', a_651,
              conv_loaded_int('signed int', a_658))) in
            pure(conv_loaded_int('signed int', a_658))
          ) in
        pure(Unit) ;
      kill('signed int', slot) ;
      pure(Unit) ;
      save continue_532: unit (i: pointer:= i, n: pointer:= n, hd: pointer:= hd,
      r: pointer:= r) in
        pure(Unit) ;
      pure(Unit) ;
      run while_556(i, n, hd, r)
    else
      pure(Unit) ;
  save break_533: unit (i: pointer:= i, n: pointer:= n, hd: pointer:= hd,
  r: pointer:= r) in
    pure(Unit) ;
  pure(Unit) ;
  kill('signed int', i) ;
  pure(Unit) ;
  let strong a_659: loaded pointer =
    bound(
      let strong ((a_661: loaded pointer,
      (a_662: ctype, a_663: [ctype], a_664: boolean, a_665: boolean)),
      a_667: loaded pointer) =
        unseq(
          let strong a_660: loaded pointer =
            pure(Specified(Cfunction(list_reverse))) in
          pure((a_660, cfunction(a_660)))
        ,
          let weak a_668: pointer = pure(hd) in
          load('struct node*', a_668)
        ) in
      if not(params_length(a_663) = 1) then
        pure(undef(<<UB038_number_of_args>>))
      else
        if a_664 \/ not(are_compatible ('struct node*', a_662)) then
          pure(undef(<<UB041_function_not_compatible>>))
        else
          let strong a_666: pointer =
            let a_669: ctype = params_nth(a_663, 0) in
            if not(are_compatible ('struct node*', a_669)) then
              pure(undef(<<UB041_function_not_compatible>>))
            else
              let weak a_670: pointer =
                create(Ivalignof('struct node*'), 'struct node*') in
              let weak _: unit = store('struct node*', a_670, a_667) in
              pure(a_670) in
          let strong a_671: loaded pointer =
            ccall('struct node* (*) (struct node*)', a_661, a_666) in
          kill('struct node*', a_666) ;
          pure(a_671)
    ) in
  store('struct node*', r, a_659) ;
  let strong i: pointer = create(Ivalignof('signed int'), 'signed int') in
  let strong a_672: loaded integer = bound(pure(Specified(0))) in
  store('signed int', i, conv_loaded_int('signed int', a_672)) ;
  save while_673: unit (i: pointer:= i, n: pointer:= n, hd: pointer:= hd,
  r: pointer:= r) in
    let strong a_675: loaded integer =
      bound(
        let weak (a_677: loaded integer, a_678: loaded integer) =
          unseq(
            let weak (a_684: loaded integer, a_685: loaded integer) =
              unseq(
                let weak a_682: pointer = pure(i) in
                load('signed int', a_682)
              ,
                let weak a_683: pointer = pure(n) in
                load('signed int', a_683)
              ) in
            case (a_684, a_685) of
              | (Specified(a_686: integer), Specified(a_687: integer)) =>
                  pure(
                    if conv_int('signed int', a_686) <
                    conv_int('signed int', a_687) then
                      Specified(1)
                    else
                      Specified(0)
                  )
              | _: (loaded integer,loaded integer) =>
                  pure(Unspecified('signed int'))
            end
          ,
            pure(Specified(0))
          ) in
        pure(
          case (a_677, a_678) of
            | (Specified(a_679: integer), Specified(a_680: integer)) =>
                if conv_int('signed int', a_679) = conv_int('signed int', a_680) then
                  Specified(1)
                else
                  Specified(0)
            | _: (loaded integer,loaded integer) =>
                Unspecified('signed int')
          end
        )
      ) in
    let strong a_674: boolean =
      case a_675 of
        | Specified(a_676: integer) =>
            pure(if not(a_676 = 1) then True else False)
        | Unspecified(_: ctype) =>
            nd(pure(True), pure(False))
      end in
    if a_674 then
      let strong a_690: loaded integer =
        bound(
          let weak (a_692: loaded integer, a_693: loaded integer) =
            unseq(
              let strong a_711: loaded integer =
                let weak (a_697: loaded integer, a_698: loaded integer) =
                  unseq(
                    let weak (a_702: loaded integer, a_703: loaded integer) =
                      unseq(
                        let weak a_708: loaded pointer =
                          let weak a_707: pointer = pure(r) in
                          load('struct node*', a_707) in
                        case a_708 of
                          | Specified(a_709: pointer) =>
                              let weak a_710: boolean =
                                memop(PtrEq, a_709, NULL(struct node*)) in
                              pure(if a_710 then Specified(1) else Specified(0))
                          | _: loaded pointer =>
                              pure(
                                undef(<<UB_CERB004_unspecified__equality_ptr_vs_NULL>>)
                              )
                        end
                      ,
                        pure(Specified(0))
                      ) in
                    pure(
                      case (a_702, a_703) of
                        | (Specified(a_704: integer), Specified(a_705: integer)) =>
                            if conv_int('signed int', a_704) =
                            conv_int('signed int', a_705) then
                              Specified(1)
                            else
                              Specified(0)
                        | _: (loaded integer,loaded integer) =>
                            Unspecified('signed int')
                      end
                    )
                  ,
                    pure(Specified(0))
                  ) in
                pure(
                  case (a_697, a_698) of
                    | (Specified(a_699: integer), Specified(a_700: integer)) =>
                        if conv_int('signed int', a_699) =
                        conv_int('signed int', a_700) then
                          Specified(1)
                        else
                          Specified(0)
                    | _: (loaded integer,loaded integer) =>
                        Unspecified('signed int')
                  end
                ) in
              case a_711 of
                | Specified(a_712: integer) =>
                    if a_712 = 0 then
                      let strong a_741: loaded integer =
                        let weak (a_713: loaded integer, a_714: loaded integer) =
                          unseq(
                            let weak (a_718: loaded integer,
                            a_719: loaded integer) =
                              unseq(
                                let weak a_728: pointer =
                                  let strong a_723: loaded pointer =
                                    let weak a_727: pointer = pure(r) in
                                    load('struct node*', a_727) in
                                  case a_723 of
                                    | Unspecified(_: ctype) =>
                                        pure(
                                          undef(<<UB_CERB004_unspecified__memberofptr>>)
                                        )
                                    | Specified(a_724: pointer) =>
                                        pure(member_shift(a_724, node, .head))
                                  end in
                                load('signed int', a_728)
                              ,
                                let weak a_740: pointer =
                                  let weak a_729: loaded pointer =
                                    let weak (a_732: loaded pointer,
                                    a_734: loaded integer) =
                                      unseq(
                                        let weak a_737: pointer =
                                          pure(expected) in
                                        pure(
                                          Specified(array_shift(a_737, 'signed int', 0))
                                        )
                                      ,
                                        let weak a_739: pointer = pure(i) in
                                        load('signed int', a_739)
                                      ) in
                                    case (a_732, a_734) of
                                      | (Specified(a_733: pointer),
                                      Specified(a_735: integer)) =>
                                          pure(
                                            Specified(array_shift(a_733, 'signed int', a_735))
                                          )
                                      | _: (loaded pointer,loaded integer) =>
                                          pure(
                                            undef(<<UB_CERB004_unspecified__pointer_add>>)
                                          )
                                    end in
                                  case a_729 of
                                    | Specified(a_730: pointer) =>
                                        let weak a_731: boolean =
                                          memop(PtrValidForDeref, 'signed int',
                                          a_730) in
                                        pure(
                                          if a_731 then
                                            a_730
                                          else
                                            undef(<<UB043_indirection_invalid_value>>)
                                        )
                                    | Unspecified(_: ctype) =>
                                        pure(
                                          undef(<<UB043_indirection_invalid_value>>)
                                        )
                                  end in
                                load('signed int', a_740)
                              ) in
                            pure(
                              case (a_718, a_719) of
                                | (Specified(a_720: integer),
                                Specified(a_721: integer)) =>
                                    if not(conv_int('signed int', a_720) =
                                    conv_int('signed int', a_721)) then
                                      Specified(1)
                                    else
                                      Specified(0)
                                | _: (loaded integer,loaded integer) =>
                                    Unspecified('signed int')
                              end
                            )
                          ,
                            pure(Specified(0))
                          ) in
                        pure(
                          case (a_713, a_714) of
                            | (Specified(a_715: integer),
                            Specified(a_716: integer)) =>
                                if not(conv_int('signed int', a_715) =
                                conv_int('signed int', a_716)) then
                                  Specified(1)
                                else
                                  Specified(0)
                            | _: (loaded integer,loaded integer) =>
                                Unspecified('signed int')
                          end
                        ) in
                      pure(conv_loaded_int('signed int', a_741))
                    else
                      let strong a_742: loaded integer = pure(Specified(1)) in
                      pure(conv_loaded_int('signed int', a_742))
                | Unspecified(_: ctype) =>
                    pure(undef(<<UB_CERB004_unspecified__conditional>>))
              end
            ,
              pure(Specified(0))
            ) in
          pure(
            case (a_692, a_693) of
              | (Specified(a_694: integer), Specified(a_695: integer)) =>
                  if conv_int('signed int', a_694) =
                  conv_int('signed int', a_695) then
                    Specified(1)
                  else
                    Specified(0)
              | _: (loaded integer,loaded integer) =>
                  Unspecified('signed int')
            end
          )
        ) in
      let strong a_689: boolean =
        case a_690 of
          | Specified(a_691: integer) =>
              pure(if not(a_691 = 1) then True else False)
          | Unspecified(_: ctype) =>
              nd(pure(True), pure(False))
        end in
      if a_689 then
        let strong a_749: loaded integer =
          bound(
            let weak (a_743: loaded integer, a_744: loaded integer) =
              unseq(
                pure(Specified(1))
              ,
                let weak a_748: pointer = pure(i) in
                load('signed int', a_748)
              ) in
            pure(
              case (a_743, a_744) of
                | (Specified(a_745: integer), Specified(a_746: integer)) =>
                    Specified(catch_exceptional_condition_add('signed int', __conv_int__('signed int', a_745), __conv_int__('signed int', a_746)))
                | _: (loaded integer,loaded integer) =>
                    undef(<<UB036_exceptional_condition>>)
              end
            )
          ) in
        kill('signed int', i) ;
        kill('signed int', n) ;
        kill('struct node*', hd) ;
        kill('struct node*', r) ;
        run ret_531(conv_loaded_int('signed int', a_749))
      else
        pure(Unit) ;
      let strong _: loaded pointer =
        bound(
          let weak (a_750: pointer, a_757: loaded pointer) =
            unseq(
              pure(r)
            ,
              let weak a_756: pointer =
                let strong a_751: loaded pointer =
                  let weak a_755: pointer = pure(r) in
                  load('struct node*', a_755) in
                case a_751 of
                  | Unspecified(_: ctype) =>
                      pure(undef(<<UB_CERB004_unspecified__memberofptr>>))
                  | Specified(a_752: pointer) =>
                      pure(member_shift(a_752, node, .tail))
                end in
              load('struct node*', a_756)
            ) in
          let weak _: unit = neg(store('struct node*', a_750, a_757)) in
          pure(a_757)
        ) in
      pure(Unit) ;
      save __cerb_continue2: unit (i: pointer:= i, n: pointer:= n,
      hd: pointer:= hd, r: pointer:= r) in
        let strong _: loaded integer =
          bound(
            let weak (a_758: pointer, a_765: loaded integer) =
              unseq(
                pure(i)
              ,
                let weak (a_759: loaded integer, a_760: loaded integer) =
                  unseq(
                    let weak a_764: pointer = pure(i) in
                    load('signed int', a_764)
                  ,
                    pure(Specified(1))
                  ) in
                pure(
                  case (a_759, a_760) of
                    | (Specified(a_761: integer), Specified(a_762: integer)) =>
                        Specified(catch_exceptional_condition_add('signed int', __conv_int__('signed int', a_761), __conv_int__('signed int', a_762)))
                    | _: (loaded integer,loaded integer) =>
                        undef(<<UB036_exceptional_condition>>)
                  end
                )
              ) in
            let weak _: unit =
              neg(store('signed int', a_758,
              conv_loaded_int('signed int', a_765))) in
            pure(conv_loaded_int('signed int', a_765))
          ) in
        pure(Unit) ;
      pure(Unit) ;
      save continue_534: unit (i: pointer:= i, n: pointer:= n, hd: pointer:= hd,
      r: pointer:= r) in
        pure(Unit) ;
      pure(Unit) ;
      run while_673(i, n, hd, r)
    else
      pure(Unit) ;
  save break_535: unit (i: pointer:= i, n: pointer:= n, hd: pointer:= hd,
  r: pointer:= r) in
    pure(Unit) ;
  pure(Unit) ;
  kill('signed int', i) ;
  pure(Unit) ;
  let strong a_780: loaded integer =
    bound(
      let strong a_770: loaded integer =
        let weak a_767: loaded pointer =
          let weak a_766: pointer = pure(r) in
          load('struct node*', a_766) in
        case a_767 of
          | Specified(a_768: pointer) =>
              let weak a_769: boolean =
                memop(PtrEq, a_768, NULL(struct node*)) in
              pure(if a_769 then Specified(1) else Specified(0))
          | _: loaded pointer =>
              pure(undef(<<UB_CERB004_unspecified__equality_ptr_vs_NULL>>))
        end in
      case a_770 of
        | Specified(a_771: integer) =>
            if a_771 = 0 then
              let strong a_778: loaded integer =
                let weak (a_772: loaded integer, a_773: loaded integer) =
                  unseq(
                    pure(Specified(1))
                  ,
                    let weak a_777: pointer = pure(n) in
                    load('signed int', a_777)
                  ) in
                pure(
                  case (a_772, a_773) of
                    | (Specified(a_774: integer), Specified(a_775: integer)) =>
                        Specified(catch_exceptional_condition_add('signed int', __conv_int__('signed int', a_774), __conv_int__('signed int', a_775)))
                    | _: (loaded integer,loaded integer) =>
                        undef(<<UB036_exceptional_condition>>)
                  end
                ) in
              pure(conv_loaded_int('signed int', a_778))
            else
              let strong a_779: loaded integer = pure(Specified(0)) in
              pure(conv_loaded_int('signed int', a_779))
        | Unspecified(_: ctype) =>
            pure(undef(<<UB_CERB004_unspecified__conditional>>))
      end
    ) in
  kill('signed int', n) ;
  kill('struct node*', hd) ;
  kill('struct node*', r) ;
  run ret_531(conv_loaded_int('signed int', a_780)) ;
  kill('signed int', n) ;
  kill('struct node*', hd) ;
  kill('struct node*', r) ;
  pure(Unit) ;
  save ret_531: loaded integer (a_781: loaded integer:= Specified(0)) in
    pure(a_781)

