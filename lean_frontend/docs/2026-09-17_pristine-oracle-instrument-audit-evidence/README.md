# Evidence — pre-merge audit of `arc/pristine-oracle-instrument` (WP-O), 2026-09-17

Companion to [`../2026-09-17_pristine-oracle-instrument-audit-premerge.md`](../2026-09-17_pristine-oracle-instrument-audit-premerge.md). Everything here was produced by the independent auditor [AGENT auditor] at head `586b550b8` in `worktrees/cerberus-lean-audit/pristine-oracle-instrument`; the tree under audit was never modified (plants monkeypatch the register PATH in-process or write only under the git-ignored `.tmp/audit/`). Outputs are verbatim transcripts; the scripts are the exact ones that produced them (paths inside them are absolute to that worktree).

| file | what |
|---|---|
| `adversarial_plants.py` / `.out.txt` | 54 auditor plants against `compare()`, `load_register`, `project_diagnostics`/`FRAME_POSITION`, `capture()` (real `kill -9`, real `sleep`/timeout, real `SIGSEGV`), real both-crash captures of `immaculate/nolibc/g4-bswap64-overflow` from BOTH oracles mutated one character at a time, stdout-projection checks, silent/malformed/missing captures, loader citation extras, and the C-program-exit-value probe. Sections A–G; summary line at the end (the one "FAIL", D6, is a mis-specified expectation — `OSError` → `INCOMPLETE`, stricter than `difference`). |
| `lane_end_to_end_plants.py` / `.out.txt` | 7 runs through the lane's `main()` with `test_upstream_oracle.REGISTER` pointed at a doctored copy: the `node` row withheld (a REAL one-sided pristine timeout), a forged fork-124 admitting row, a row naming an absent case, an empty `--only`, an empty `--shard 1000/1000`, the three tray rows with their rows (`subset_passed; reviewed_difference: 3`), a missing manifest. |
| `ensure_plants.sh` / `.out.txt` | 14 plants against `scripts/ensure_independent_oracle.py`: static grep for delete/overwrite primitives; empty dir; garbage manifest (sha before/after); three corrupted COPIES of the real manifest; `--no-build`; bad `--lem-repo`; `CERB_LEM_REPO`/`CERB_CERBERUS_REPO` at a non-repo; `CERB_INDEPENDENT_MANIFEST`; the real build (idempotent, mtime unchanged); a directory named `manifest.json`; `bash -n` + `set -e` of the hook; the live hook lines vs the recorded diff. |
| `check_deleted_rows_evidence.py` / `.out.txt` | the auditor's OWN frame tokenizer over the worker's `o1-raw-diffs-*.txt` for the 21 deleted register rows (20 present there; `BAD count: 0`). |
| `check_deleted_rows_fresh.py` / `.out.txt` | the same tokenizer over FRESH captures of the 21 rows (`--corpus all --only …` at the head; that run's verbatim tail: `Independent oracle scope: subset (--only); 21 rows in 5.0s; source unchanged: True` / `Independent oracle: subset_passed; {'matching_failure': 21}; …/.tmp/audit/only21/report.json`; its per-row lane statuses are the `lane=` column of the check output): equal statuses, empty stdout, equal stderr line counts, every differing line a frame differing only in position (`BAD: 0`). |
| `check_record_quotes.py` / `.out.txt` | every verbatim gate/build line quoted in the WP-O record found in a file of its evidence dir (`quoted lines: 44 missing: 0`). |
| `gate-chain.sh`, `gate-chain2.sh`, `gate-chain3.sh`; `chain.log`, `chain2.log`, `chain3.log` | the sequential (one heavy job at a time) gate runners and their logs with start/end times and load averages. |
| `row10-full.txt`, `row10-plant.txt`, `corpus-ci.txt`, `row12-chvalid.txt` | LADDER Tier B row 10, its `--plant`, Tier C `--corpus ci`, Tier B row 12 — transcripts at `586b550b8`. |
| `with-lean-subset.txt` | `--with-lean --only` over 8 cases: the three-engine column reproducing 6 of the record's 40 Lean≠fork rows token for token; rc 0 (report-only). |
| `release-fast.txt`, `release-fast-2.txt` | `python3 scripts/release.py --mode fast` twice: the first with `Source unchanged: False` (this evidence directory appeared untracked mid-run — `source_identity` hashes untracked files), the second with no tree writes during the run. 14/14 lanes PASSED in both. |

Reproduce: from the worktree root, `scripts/ce python3 <script>` for the Python plants (they import `scripts/test_upstream_oracle.py`), `bash ensure_plants.sh`, `bash gate-chain.sh`. The plants need the standing pristine build at `.validation-foundations/independent-oracle-v2` (built by the container hook when the worktree was created; log `/home/dev/projects/cerberus-lean-proj/.tmp/audit-worktree.log`, quoted in the report §2.4).

## `o6/` — the delta re-read of O6 (`1abadc396`, `46e5d2f19`; report §A)

Produced with `46e5d2f19`'s tree materialised in the audit worktree (the ten files the two commits touch checked out; `git diff 46e5d2f19` empty outside the audit paths; lane-script sha `aa431f7cd873dfc8`), then restored to HEAD before committing.

| file | what |
|---|---|
| `delta_plants.py` / `.out.txt` | 30 auditor plants against the O6 code: M1 (registered cases judged only by their row — the committed `node`/`arr-2-2-return` rows under fork-124, both-124 (hermetic and REAL `sleep` timeouts), pristine-124, moved sha, stale agreement, 137 either side, a `diagnostic-text` row pinning pristine 124, an empty-dict row; unregistered both-124/one-sided/137) and N1 (untracked ignored / untracked non-ignored / `..` / out-of-range / inverted / line-0 / last-line / `#anchor` / deleted-tracked citations; git unusable → `OSError`). |
| `gate-chain.sh`, `chain.log`, `row10-full.txt`, `row10-plant.txt`, `corpus-ci.txt` | row 10, `--plant` (45 rows, `plant_ok` 43), `--corpus ci` at `46e5d2f19`'s tree, sequential. |
| `with-lean-capped.txt`, `with-lean-capped-command-heads.txt` | `--with-lean --only` over 7 cases: the recorded `.command.json` heads show `env CERB_MEM_MAX=4G capped timeout <bound>` on the bridge AND the Lean driver for libc_exec / immaculate / uri rows and a bare `timeout` on minimal / multi_tu rows (M5). |
| `check_record_quotes_o6.out.txt` | every verbatim gate/build line quoted in the amended record (48) found in its evidence dir. |
