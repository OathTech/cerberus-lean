# Pre-merge audit — `arc/semantics-audit-repairs` (2026-09-15)

**Range:** `54f007187..5b9ada782` (14 commits: the charter `40bb7fe77`, D0 `dbdf36a5e`, D1 `c807ce603`, resumption note `c7dd0ba29`, D1b `146179d24`, D2 `a43abba65`, D3 `dbe633ec5` + `83dc6ba00`, second resumption note `15dd162e9`, D4 `3d98f695e`, D5 `535389a3d`, the orchestrator's 097 re-pin `18e917709`, the orchestrator's review `5b9ada782`).
**Head audited:** `5b9ada78230189ffb4b0d5363ba2f89d66cf44a6`.
**Date:** 2026-09-15.
**Who:** an INDEPENDENT pre-merge auditor (Claude, Fable 5.1) — [AGENT auditor] throughout. I wrote none of the range; the orchestrator and the worker did. Every claim below that I mark *reproduced* was produced by my own runs in this worktree; every judgment is mine and is labelled [AGENT auditor]. Quoted outputs are verbatim; tallies are labelled derived.

**What I ran.** Pre-flight `bash tools/check_driver_fresh.sh --check` → `check_driver_fresh: oracle OK (bin 28fcf2c842a88046e7f7066ca0ae2af1cc9553bb6d819bf08c263cb8aea05f82, src 19de18ed9f03a529067ec7103f58917d116933da22bc317546408b2f7d66043e)` / `check_driver_fresh: lean OK (bin e36af96d9eed60cfac55414d675d354edff4268b0bdfad6031c121137dc3be84, src a0ed1133a00bda9cc6ab4c84b2d534548f50f1ace248c61654fcd9ac253036bb)`; `git status --porcelain` empty. Read in the prescribed order: the charter (§0–§8), the record (all sections incl. the orchestrator's review), `VALIDATION.md` §0–§3, `scripts/LADDER.md`, the whole-project audit, then every diff in the range. Ran: 105 self-generated floating literals through Python, gcc, the fork oracle and Lean at the BYTE level (all 8 bytes of the parsed double, three packed `int` returns per literal); 29 byte-path probes through the real `--cabs-json` exporter and the real Lean importer; 15 two-file cross-TU struct-value programs on fork oracle / Lean / pristine upstream / gcc; the R5 pin on all five references (fork, Lean, pristine, gcc, Python, plus the OCaml 5.4.0 toplevel); the `minimal/097` program on both oracles with stderr hashes; `./scripts/check_fork_drift.sh`; `python3 scripts/test_observations.py`; `python3 scripts/test_release.py`; `python3 scripts/release.py --list`; the two new unit exes directly; LADDER rows 6 and 6b and the tray corpus under `full`; the pristine lane `python3 scripts/test_upstream_oracle.py` ONCE. Scratch lived under `.tmp/audit-scratch/` (git-ignored) and is deleted. Nothing outside this file was edited; no build was invoked by me directly (see F7 for the one indirect relink).

## Findings

### F1 — P2 — `scripts/LADDER.md` Tier A row 1 says `8/8 exes`; `test_unit.sh` runs 9

**Claim audited:** row 1's unit-exe count against what `scripts/test_unit.sh` actually runs (task item 7).
**Reproduction:** `scripts/test_unit.sh` `UNIT_TESTS=(` lists, verbatim, `"effects-proof-test" "totality-proof-test" "core-parser-test" "fresh-int-test" "pp-test" "fuel-exemplar-test" "monadic-failstop-test" "float-literal-test" "are-compatible-test"` — nine entries (derived count). The record's own gate lines at D3 and later read `Total: 9 passed, 0 failed` (record §D3(d), §D5, orchestrator review `A1: Total: 9 passed, 0 failed`). The range edited this very cell in D1b (`7/7` → `8/8`, commit `146179d24`) and then added the ninth exe in D3 (`dbe633ec5`) without updating it. `git diff 54f007187..HEAD -- scripts/LADDER.md` shows the cell now reads `8/8 exes (incl. 280 parser tests …)`.
**Remedy:** `8/8` → `9/9` on row 1 (the `280 parser tests` in the same cell is also stale per the record — the exe reports 292 — pre-existing; fix together).

### F2 — P2 — a matched-mode consequence of the repair on the chartered RETURN path is unobserved, unpinned and unrecorded: an incompatible struct value RETURNED and then STORED into a caller local now dies on both fork engines with the pure-failure class

**Claim audited:** the record's D3 coverage ("Results change only on multi-TU programs where the guard previously rejected a compatible value or the typo previously accepted an incompatible one — … the latter is reachable in matched mode only on the RETURN path") and draft 39's account of where the store-side consult is reached (its "related observation 2" reports it only under `--switches=inner_arg_temps`, for arguments). The seven tray cases exercise the RETURN path only through `PEmemberof` (`mk().a[0]`, `mk().n`, `ident(n).v`); no case stores the returned value.
**Reproduction** (two files, `struct S { int a[1]; }; struct S mk(void) { struct S s; s.a[0] = 7; return s; }` / `struct S { int a[2]; }; struct S mk(void); int main(void) { struct S s = mk(); return s.a[0]; }`, matched mode `--nolibc --exec --batch --mode=exhaustive tu1.c tu2.c`), verbatim first lines:

```
-- fork:
internal error: can_advance: Step_error2 ==> c1_store_1_2/tu2.c:1:61-79the value of a store(struct S) didn't match the lvalue type: Specified((struct S){.a= {7}})
cerberus: internal error, uncaught exception:
          Failure("internal error: can_advance: Step_error2 ==> c1_store_1_2/tu2.c:1:61-79the value of a store(struct S) didn't match the lvalue type: Specified((struct S){.a= {7}})")
rc=125
-- lean:
PANIC at _private.LemLib.0.failwithIImpl LemLib:168:2: can_advance: Step_error2 ==> c1_store_1_2/tu2.c:1:61-79the value of a store(struct S) didn't match the lvalue type: Specified((struct S){.a= {7}})
rc=134
-- pristine:
Defined {value: "Specified(7)", stdout: "", stderr: "", blocked: "false"}
rc=0
-- gcc:
gcc exit=7
```

The compatible twin (`int a[2]` in both, same shape) is `Defined {value: "Specified(7)", …}` rc 0 on fork oracle, Lean and pristine, gcc 7. Mechanism [AGENT auditor]: `struct S s = mk();` is a `store` → `core_run.lem:544` `memValueFromValue (Ctype [] (unatomic_ ty)) cval` → the `Struct/OVstruct` arm's consult (`core_aux.lem:196-202`) → with the typo fixed, `false` → `Nothing` → `Step_error2` — the same crash class draft 39 records for `inner_arg_temps`, but reached in the DEFAULT switch set, on the RETURN path the charter named. So the typo repair moves this program from `Defined 7` (pristine/pre-D3 fork; the typo accepted the incompatible value) to a both-crash pair (oracle exit 125 / Lean abort 134) — class (a) under VALIDATION §1, the two fork engines agree, the program has UB by §6.2.7#2, and this IS the intended effect of the fix. It is not a Lean bug and not a wrong answer. But it is a behaviour change of the repaired predicate in matched mode that no record, README, draft or pin carries, and its presentation is a crash, not the `Illformed_program` diagnostic the tray rows show.
**Remedy (documentary, before merge):** add the shape to the record's D3 (a third RETURN-path shape: return + store), to `tests/multi_tu_tray/README.md` (a record-only row — the multi-TU lane has no crash class, so it cannot be a row-6b case; the charter's D3(g) rule for "a crash class" is "pin them in the record only"), and to draft 39's related observations (the store-side consult is reached in the default switch set via return-then-store, not only under `inner_arg_temps`). No code change.

### F3 — P3 — `Ctype_aux.are_compatible_aux` ignores member alignment specifiers, so the new `PEmemberof` consult accepts struct definitions whose layouts differ

**Claim audited:** the semantic question of task item 3 — is there a pair of struct types the predicate accepts whose values have different layouts.
**Reproduction:** `frontend/model/ctype_aux.lem:141` compares members as `((ident1, (_, _(*TODO alignment*), qs1, ty1)), (ident2, (_, _(*TODO alignment*), qs2, ty2)))` — the `maybe alignment` component is discarded. C11 §6.2.7#1 requires "if one member of the pair is declared with an alignment specifier, the other is declared with an equivalent alignment specifier". Program: TU1 `struct S { char c; int x; }; struct S mk(void) { struct S s; s.c = 1; s.x = 7; return s; }`, TU2 `struct S { char c; _Alignas(16) int x; }; struct S mk(void); int main(void) { return mk().x; }`:

```
-- fork:    Defined {value: "Specified(7)", stdout: "", stderr: "", blocked: "false"}   rc=0
-- lean:    Defined {value: "Specified(7)", stdout: "", stderr: "", blocked: "false"}   rc=0
-- pristine: Error {msg: "ill-formed program: `PEmemberof(struct) ==> mismatched tags: Symbol(536, SD_Id("S")) vs Symbol(502, SD_Id("S"))'"}   rc=1
-- gcc exit=0
```

gcc's 0 (not 7) shows the two definitions really have different layouts (`x` at offset 4 vs 16). Inside Cerberus no wrong read occurs [AGENT auditor]: `PEmemberof` looks the member up BY NAME on the value's own member list (no offset is involved), and the store side builds `Mem.struct_mval tag_sym1 xs` with the DESTINATION tag (`core_aux.lem:200`), so store and subsequent loads use one layout; the same program with `struct S s = mk(); return s.x;` is also `Specified(7)` on both fork engines. The result is a Defined answer to a UB program (§6.2.7#2 — permitted), but the predicate is weaker than the clause the record's intent argument cites, and the D3 consult is what makes this observable on the fork (pristine rejects on exact tags). Pre-existing upstream gap (the `TODO alignment` is the authors' own); both fork engines agree; not a Lean bug. Bit-fields are not reachable (`error: feature not yet supported: bit-fields` on every engine); qualifier differences, member-name differences, `int`/`unsigned`, enum-vs-int, inner member order and the FAM shape are all REJECTED (reproduced); anonymous union members and nested named unions inside a struct are accepted and give the right value (reproduced: 7 / 1 / 7 on both fork engines and gcc).
**Remedy:** a `TODO.md` row (or a question-tier tray draft in the shape of 41) naming the alignment clause and this reproducer; one sentence in the record's intent argument that the lem's §6.2.7#1 reading omits alignment.

### F4 — P3 — counts made stale by this range in the normative documents

**Reproduction** (derived counts from the tree): `tests/float` has 93 files and `scripts/exec_float_baseline.txt` 93 rows; `tests/minimal` has 111 files and `scripts/exec_baseline.txt` 111 rows; `scripts/gcc_oracle_baseline.txt` has 1997 non-comment rows. Stale text: `scripts/LADDER.md` Tier A row 4b `(69/69 MATCH baseline, arc-10 S3b; …)`; Tier B row 7 `the 1,963-row skip ledger`; `lean_frontend/VALIDATION.md` §5 lanes table `test_exec.sh --check-baseline | upstream tests/minimal | 106/106 at the pinned baseline`, `test_core.sh | … | … 106/106 minimal`, `test_gcc_oracle.sh --check-baseline | … (1,963 final-candidate rows)`; `scripts/exec_float_baseline.txt` header `# corpus: tests/float (69 files, …)`. Each was true before this range and is false after it.
**Remedy:** one docs commit updating the five numbers (93, 111, 1997) — with F1.

### F5 — P3 — two register/lane texts left incomplete by R5 (both already noted by the worker as out-of-fence residuals)

`lean_frontend/VALIDATION.md` §2, the R4 paragraph: "today the markers are `CerbDecode.lean` R1/R2 (`grep "ISO-fix register R"`)" — false since D1b: `grep -rn -- '-- ISO-fix register R5' lean_frontend/*.lean` → `lean_frontend/CerbFloat.lean:142` (reproduced). `scripts/test_immaculate.sh` header comment `:22-27` enumerates R1–R3 only while its OK message line (edited in D4) names R5. **Remedy:** two one-line edits.

### F6 — P3 — latent, unreachable seam divergences in `CerbFloat.of_string`

[AGENT auditor, from source; not reachable from either feeding site, so no reproduction is possible through C.] (a) The suffix strip (`s.endsWith "f" || … "F" || … "l" || … "L"` → `dropRight 1`) runs BEFORE the `0x` check, so a hexadecimal string with no binary exponent ending in the hex digit `f` (e.g. `0x1f`, which `caml_float_of_hex` accepts as 31.0 — the exponent is optional there, floats.c `exp = 0`) would read as `0x1` = 1.0. The C grammar makes the binary exponent mandatory and the lexer separates the suffix (`cabs_json.ml:121` `json_of_cabs_floating_constant (s, suffix_opt)`), and `CoreParser.lexNumLit` emits decimals, so no caller can produce it. (b) OCaml's `caml_float_of_string` also accepts `_` digit separators and `inf`/`infinity`/`nan` spellings, on which Lean panics; the module comment already states the inf/nan case. **Remedy:** a comment line naming (a) as an unreachable difference, or check `0x` before stripping the suffix; nothing else.

### F7 — P3 — process disclosure: my lane runs relinked the oracle binary (no source change)

Running LADDER rows 6/6b via `./scripts/test_multi_tu.sh` (to reproduce the projection claims) invoked `build_cerberus` from `scripts/common.sh` (a `dune build` of `main.exe`), although the brief asked me not to build. Afterwards `bash tools/check_driver_fresh.sh --check` → `check_driver_fresh: oracle OK (bin 1c7caf84141b9a3e8e52e0e4f3e6f1e335554370752865825729125f55c4239f, src 19de18ed9f03a529067ec7103f58917d116933da22bc317546408b2f7d66043e)` — the bin hash moved from `28fcf2c8…`, the source hash did not; `git status --porcelain` stayed empty. This is exactly the link non-reproducibility the record lists as its open question 2 (three bin hashes with one source hash). No semantic effect; recorded so the stamp history reads correctly. The pristine lane was run once, as permitted.

## Verified clean (checked and found correct — by my own reproduction unless marked "by reading")

**1. Floating-literal conversion (`lean_frontend/CerbFloat.lean`).** Re-derived from IEEE 754-2019 §3.4 (p = 53, w = 11, bias 1023, emin −1022, subnormal quantum 2^−1074, roundTiesToEven) and checked branch by branch [AGENT auditor, by reading]: `roundToBinary64Bits` — binade `E` from `log2 num − log2 den` with the one `atLeast` correction is exact (num/den lies in `(2^(a−b−1), 2^(a−b+1))`); quantum `qe = max(E−52, −1074)` is the format's; `q = ⌊num·2^−qe/den⌋` with remainder `r`, `2r > d` up / `2r < d` down / tie to even `q` — correct; `q < 2^52` is exactly the subnormal case (a normal binade always yields `q ≥ 2^52`) and encodes exponent field 0 with fraction `q` (q = 0 → signed zero; q rounding up to 2^52 → min normal, exponent field 1 — checked); carry `q = 2^53` → `(2^52, qe+1)`; `expField = qe + 52 + 1023`, `≥ 2047` → ±inf, which also makes the tie at `2^1024 − 2^970` overflow (odd max-finite significand → even → carry → inf, per §7.4). `scaledToBits` guards, for base 2 AND base 10 with `m ≥ 1` and `m < base^mBound`: `e > 1024` ⇒ `m·base^e ≥ 2^1025 > 2^1024` ⇒ inf is correct; `e + mBound < −1075` ⇒ `m·base^e < base^(e+mBound) ≤ 2^(e+mBound) < 2^−1075` (negative exponent, base ≥ 2) ⇒ 0 is correct, and the tie at exactly 2^−1075 is NOT caught by the guard (it falls through to the exact rounding, which gives 0 by ties-to-even — reproduced row 32). `parseDecimal`/`parseHex` produce `(m, e, bound)` with the right value and bound (leading zeros only loosen the bound); missing hex exponent = `p0` mirrors `floats.c:293`; exponent marker without digits is malformed as in `strtod`/`floats.c:420-421`; sign and one suffix handled. `Float.ofBits` is `@[extern "lean_float_of_bits"] opaque` (`Init/Data/Float.lean:111`) — a pre-existing boundary primitive, no new class. No `Float` arithmetic precedes `Float.ofBits`. The only numerals are the named §3.4 parameters. **Battery (below): 103 conversion literals, 0 Lean-vs-reference disagreements; 3 oracle-vs-reference disagreements, all in R5's declared scope.**

**2. Byte path.** `json_of_bytes` maps `b < 0x80` to itself and `b ≥ 0x80` to `0xC0|b>>6, 0x80|b&0x3F` = the UTF-8 of U+0080..U+00FF, exactly one code point per byte for all 256 values [by reading]; reproduced through the REAL exporter+importer: a literal of the 128 bytes `0x80..0xFF` → `cabs-json rc=0 UTF-8 OK | fragments=128 codepoints 80,81,…,fe,ff` in order, Lean and oracle both `Defined {value: "Specified(129)", …}`; the 29 control bytes `0x01–0x1F` (minus CR/LF) → 29 fragments, code points `01…1f`, both `Specified(30)`; `"\x7f\x80"` → `7f, 80`, both 3; a lone invalid-UTF-8 byte `"\xff"` → both 2; `"\0é"`, `"é\0"`, `"\x00\xff\x00"` → escape text + raw bytes as separate fragments (`5c30, c3, a9` etc.), both 4; `'é'` character constant → body `c3 a9`, both 4; `'\xff'` → both 4; `L"é"` / `u8"é"` prefixes carried, both 12 / 3; adjacent-literal concatenation `"\xc3" "\xa9"` → two parts, both 3; a raw NUL byte in source is rejected by the preprocessor on both (`null character(s) preserved in literal [-Werror]`); `(unsigned char)"é"[1]` → both crash at the decoder (oracle `Failure("decode_character_constant: invalid char constant ==> \169")` exit 125; Lean `PANIC … CerbDecode:121:11 … (decode.ml:199…` abort) — class (a). Exporter sites (all 15 `` `String``/`json_of_string`/`json_of_bytes` occurrences enumerated by grep): the three byte sites (`:129` character constant, `:146` string-literal fragments, `:324` asm fragments) are the ONLY byte carriers, and `_Static_assert`'s message goes through `json_of_cabs_string_literal` (`:632`) so it is covered (reproduced: `_Static_assert(1, "é")` → UTF-8 OK, both 5); TEXT sites (`:13/:14` tags, `:30` file, `:44` Loc_other, `:63` identifier, `:114/:121` constant digit text, `:600/:602` attribute strings, `:657` magic text) are text. Importer: `getByteStr` at the three matching sites (`:249`, `:268`, `:746`); the remaining `getStr` sites are file, Loc_other, identifier, integer/floating constant text, attribute args/tokens, `EDecl_magic` — text. The VALIDATION §3(b) residual row is TRUE as written: `[[cerb::magic("\xff")]]` → `cabs-json rc=0` but `NOT UTF-8: 'utf-8' codec can't decode byte 0xff in position 92970`; Lean `rc=1` `uncaught exception: Tried to read file 'q5_attr_ff_c.json' containing non UTF-8 data.`; oracle `Defined {value: "Specified(4)", …}` — loud refusal where the oracle proceeds, exactly the recorded class. A `0xff` in an identifier is rejected by the lexer on both (`error: invalid symbol`); in a comment it is stripped. `scripts/test_cabs_bytes_probe.py` asserts the same three properties fail-closed (read).

**3. Shared-model edits.** `git diff 54f007187..HEAD -- frontend/model/ctype_aux.lem frontend/model/core_eval.lem` is exactly the one-token `(n1_opt, n1_opt)` → `(n1_opt, n2_opt)` and the struct-arm guard `tag_sym <> tag_sym' && not (Ctype_aux.are_compatible …)` plus a 9-line comment; nothing else (the union arm is untouched). The generated trees carry it (`ocaml_frontend/generated/ctype_aux.ml:87` `match (n1_opt, n2_opt)`, `lean_frontend/generated/Ctype_aux.lean:100` `match n1_opt, n2_opt`). The Lean guard is `(… tag compare …) && not ((are_compatible0 _lemReader_tagDefs) …)` and Lean's `&&` is `Bool.and`, `@[macro_inline] def Bool.and (x y : Bool) : Bool := match x with | false => false | true => y` (`Init/Prelude.lean:1210`: "short-circuiting evaluation: if `x` is `false` then `y` is not evaluated") — so equal tags never evaluate the consult, on both targets; a single-TU struct value carries its one definition's tag, so single-TU programs never reach it (structural; and every single-file lane held per the record and the orchestrator's battery). Qualifiers ARE compared (`qs1 = qs2`, `ctype_aux.lem:92`); member names by string (`idEqual`); the two-engine agreement held on all 15 of my cross-TU programs up to symbol numbering. `Symbol.from_same_translation_unit` is a digest compare (`symbol.lem:301-302`), as the unit test asserts.

**4. Projection.** `scripts/observations.py`: `SYMBOL_NUMBER = re.compile(rb'Symbol\([0-9]+, ')`, substituted to `Symbol(_, ` only when `v.kind in ('Error', 'Undefined')`, every other verdict kind byte-identical; `--projection` choices gain `failure-class`; the default stays `full`. `grep -rn projection scripts/*.sh scripts/*.py`: the only script passing `--projection "$PROJECTION"` is `test_multi_tu.sh`, whose `PROJECTION=full` unless `--failure-class-projection` is given; the other lanes pass `values` (ci_sweep, gcc) or `pin` (verify, speclab) as before. `python3 scripts/release.py --list` → `A6 ./scripts/test_multi_tu.sh` / `A6b ./scripts/test_multi_tu.sh --failure-class-projection tests/multi_tu_tray` — row 6b is the only row with the flag. `python3 scripts/test_observations.py` → `Ran 23 tests … OK`; the plant test replaces the tag NAME, the arm and the description and asserts each still differs, and a `Defined` token spelling `Symbol(7, x)` is untouched (read). `python3 scripts/test_release.py` → `Ran 16 tests … OK`. Lane runs: row 6b → `PROJECTION: failure-class (OPT-IN, LADDER Tier A row 6b only) — …` / `SUMMARY: total=7 match=7 fail=0` / `ALL PASSED`; row 6 → `PROJECTION: full (complete verdict tokens)` / `SUMMARY: total=2 match=2 fail=0`; the tray under `full` → `[2] MISMATCH arr-1-2-return:` / `[6] MISMATCH fam-vs-array-return:` / `SUMMARY: total=7 match=5 fail=2` — the two rows differ on numbering alone, as claimed.

**5. Registers and pins.** R5 against §2's criteria: (i) `tools/n1570.json` `§6.4.4.2#3` reads, verbatim, "… For hexadecimal floating constants when FLT_RADIX is a power of 2, the result is\n    correctly rounded." — the register row and charter §7 quote it correctly and the decimal latitude in the same paragraph indeed does not apply; (ii) second oracles reproduced: `gcc -std=c11 -O0 -w` on the pin → `gcc exit=1`; Python `float.fromhex('0x8000000000000BFp-1082') == float.fromhex('0x1.0000000000002p-1023')` → `True 0x8000000000001`, and the two-step model `math.ldexp(float(0x8000000000000BF), -1082)` → `0x8000000000000` (the oracle's value); OCaml 5.4.0 toplevel → `false 0x0.8p-1022 0x0.8000000000001p-1022`; fork oracle `Defined {value: "Specified(0)", …}` rc 0; pristine `Defined {value: "Specified(0)", …}` rc 0; Lean `Defined {value: "Specified(1)", …}` rc 0; (iii) tray `ocaml/01` + `40` exist with the runs; (iv) `tests/immaculate/baseline.txt` row `r5-hex-subnormal-double-rounding DIFF | L=VAL:{value: "Specified(1)", stdout: "", stderr: "", blocked: "false"}` + gcc row `AGREE -`; (v) marker `-- ISO-fix register R5` once, `CerbFloat.lean:142`; ruling quoted verbatim and identically in charter §6, record §D1b and VALIDATION §2. The `minimal/097` re-pin: my runs → fork `rc=125`, `f097.err` sha `a31c366c46156430652c5a0aef898db54acc145d0f88e9ae0889652eb2fa3333`; upstream `rc=125`, `u097.err` sha `0cbf405bb92a175bd2453c546cf4c86f8f129c559e4b92dc87daebf62aa372b2`; both stdout `e3b0c442…` (empty); `diff` of the two stderrs with `lines? N(-M)?, characters A-B` stripped → identical — the rationale ("line/character positions only") is TRUE; `python3 scripts/test_upstream_oracle.py` → `Independent oracle: passed; {'semantic_agreement': 738, 'reviewed_difference': 1, 'matching_failure': 11, 'interface_agreement': 2}`. This is a changed EXISTING pin row, made by the orchestrator in a dedicated, dated commit with rationale (`18e917709`) after the worker's fenced STOP — the practice LADDER "Conventions" prescribes. `scripts/fork_drift_manifest.txt`: exactly five hash moves (`[source-content]` `cabs_json.ml` `34f2ddcf…→5f64fc9c…`, `core_eval.lem` `e1fc98ed…→4ade27ce…`, `ctype_aux.lem` `5a657d68…→d7e2ece8…`; `[expected-semantic]` `ctype_aux.ml` `afcc21e3…→977ee22d…`, `core_eval.ml` `1b3c441a…` ADDED) plus two dated header notes; `./scripts/check_fork_drift.sh` → `check_fork_drift: OK — layer 1: 76 oracle-surface files = manifest …; layer 2: 23 differing generated files, all hash-pinned (merge-base b9aeedcb4dd438763b0eef7f95ac19e93875d7de; lem-pin f6542f8 = lem -v)`. Baselines, line by line from `git diff`: `exec_baseline.txt` +5 rows (107–111) appended; `exec_float_baseline.txt` +24 rows (081–097, 099–105) appended; `gcc_oracle_baseline.txt` +34 rows (24 float, 4 `f3-*`, 1 `r5-*`, 5 minimal) inserted in key order; `tests/immaculate/baseline.txt` +5 rows (4 `f3-*`, 1 `r5-*`) + 14 header comment lines — NO existing row changed in any of the four. `git diff --stat 54f007187..HEAD -- scripts/fuel_forms_pending.txt scripts/fuel_hypotheses.txt scripts/failure_reach_register.txt tests/multi_tu/ frontend/model/core_aux.lem lean_frontend/CerbDecode.lean lean_frontend/CerbMem.lean lean_frontend/Core_eval_lemMeasureProofs.lean` → empty.

**6. Policy.** `grep -E 'axiom|sorry|native_decide|bv_decide|ofReduce|partial def|opaque|implemented_by|maxHeartbeats|maxRecDepth|set_option'` over `CerbFloat.lean`, `FloatLiteralTest.lean`, `AreCompatibleTest.lean` → nothing; the `CabsImport.lean` diff adds no `partial`/`opaque`/`axiom`/`sorry`/`implemented_by`/`set_option` line (its `partial def`s pre-exist). No fuel numerals: the conversion's numerals are `53`, `11`, `1023`, `1 − emax`, `p − 1`, `2^w − 1`, each cited. No `2>/dev/null` added under `scripts/`. Both unit tests are `IO` executables comparing bit patterns / booleans (`float-literal-test` → `42 literal pins checked` / `All float-literal pins passed` rc 0; `are-compatible-test` → 8 `ok` lines / `All are_compatible pins passed` rc 0, run directly) — no theorem, no `rfl`/`decide` at a program literal. Provenance: the [USER 2026-09-15] ruling text is identical in the charter, record, VALIDATION §2 and the baseline header; the charter's operator quotes carry dates; the record labels tallies "derived" and judgments [AGENT]; acceptance statements quote observed lines. Hand-written `CerbFloat.lean`/`CabsImport.lean` are byte-identical to their `generated/` copies (`cmp`). All 13 commits in the range carry the `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` trailer.

**7. Documents.** Draft 39: upstream `ctype_aux.lem:80` is the typo line (`deps/cerberus-upstream/frontend/model/ctype_aux.lem:77-84` read, `b9aeedcb4`); `core_run.lem:947-950` upstream = the `SW_inner_arg_temps` pointer wrapping; `core_run.lem:527` upstream = the store-side `memValueFromValue`; fork `:964-967`/`:544` likewise; the three-engine lines match my re-runs of the tray cases (`545/502`, `558/502`, `536/502`, `533/502` oracle; `63/19`, `50/19` Lean). Draft 40 / `ocaml/01`: `impl_mem.ml:2523-2524` `let str_fval str = float_of_string str` ✓; `util/cerb_floating.ml:8-16` strips one `f` ✓; `floats.c:355` `f = (double) (int64_t) m;` and `:369` `if (exp != 0) f = ldexp(f, exp);` ✓ (read in `_opam/.opam-switch/sources/ocaml-compiler.5.4.0/runtime/floats.c`); the exact-arithmetic argument (191/256 quanta; 63 < 64 rounds down; tie at 2^−1075; ties-to-even) is correct and my battery shows two further instances (rows 33 and 40) inside the stated scope. Draft 41: `ctype_aux.lem:137-151` — member-count test, then the `(flexible_opt1, flexible_opt2)` match whose mixed cases are `false` ✓; the reproducer's three-engine lines reproduced. Draft 42: fork `translation.lem:1530-1559` / upstream `:1525-1554` `A.AilEunary A.Minus e` → `Caux.mk_op_pe C.OpSub zero_pe …` with `zero_pe = Caux.mk_floating_value_pe Mem.zero_fval` ✓; `impl_mem.ml:2519` `zero_fval = 0.0`, `:2533` `FloatSub -> fval1 -. fval2` ✓; my battery rows 88/89 (`-0x0p0`, `-0.0` as C expressions) reproduce the class: oracle = Lean = `+0`, gcc/Python `−0`. INDEX rows 38–42 present, numbering per charter §8 item 8, the `ocaml/` pointer in "Other upstreams". `node_a.c` header restated correctly (pristine `rc=124` reproduced on the `node` case by the record; I did not re-run the 60 s timeout). `TODO.md` block: every RESOLVED pointer names the right commit; the union-twin reproducer shape is the one I ran (`c11_union_twin`: `PEmemberof(union) ==> mismatched tags` on all three engines, gcc 7). VALIDATION §3(b) residual row TRUE (item 2 above); §5 row 6b consistent with LADDER; §9 blind-spot paragraph present. `python3 scripts/test_release.py` accepts the ladder.

## The battery — 105 literals (103 conversion literals + 2 unary-minus-on-zero C expressions), reference = Python 3.12 `float()`/`float.fromhex()` cross-checked against `gcc 13.3.0 -std=c11 -O0` native (column `gcc`: `same` = gcc's 8 bytes equal Python's; 0 disagreements), engines = fork oracle `main.exe --nolibc --exec --batch --mode=exhaustive` and `LEAN_ABORT_ON_PANIC=1 cerberus-lean --batch` on `--cabs-json`, read as all 8 little-endian bytes of `union { double d; unsigned char b[8]; } u; u.d = LIT;` via three packed `int` returns. Long literals are elided in the display column only (`…(N chars)…`).

| # | literal | Python ref (LE bytes) | gcc | oracle | Lean | verdict |
|---|---|---|---|---|---|---|
| 1 | `0x1p0` | `000000000000f03f` | same | 000000000000f03f MATCH | 000000000000f03f MATCH | ok |
| 2 | `0X1.8P1` | `0000000000000840` | same | 0000000000000840 MATCH | 0000000000000840 MATCH | ok |
| 3 | `0x.8p1` | `000000000000f03f` | same | 000000000000f03f MATCH | 000000000000f03f MATCH | ok |
| 4 | `0x10p-4` | `000000000000f03f` | same | 000000000000f03f MATCH | 000000000000f03f MATCH | ok |
| 5 | `0X1P0` | `000000000000f03f` | same | 000000000000f03f MATCH | 000000000000f03f MATCH | ok |
| 6 | `0x1.Fp0` | `000000000000ff3f` | same | 000000000000ff3f MATCH | 000000000000ff3f MATCH | ok |
| 7 | `0xA.Bp-3` | `000000000060f53f` | same | 000000000060f53f MATCH | 000000000060f53f MATCH | ok |
| 8 | `0x1.00000000000008p0` | `000000000000f03f` | same | 000000000000f03f MATCH | 000000000000f03f MATCH | ok |
| 9 | `0x1.00000000000018p0` | `020000000000f03f` | same | 020000000000f03f MATCH | 020000000000f03f MATCH | ok |
| 10 | `0x1.000000000000080000000000001p0` | `010000000000f03f` | same | 010000000000f03f MATCH | 010000000000f03f MATCH | ok |
| 11 | `0x1.00000000000007fffffffffffffp0` | `000000000000f03f` | same | 000000000000f03f MATCH | 000000000000f03f MATCH | ok |
| 12 | `0x1.fffffffffffff8p0` | `0000000000000040` | same | 0000000000000040 MATCH | 0000000000000040 MATCH | ok |
| 13 | `0x1.ffffffffffffe8p0` | `feffffffffffff3f` | same | feffffffffffff3f MATCH | feffffffffffff3f MATCH | ok |
| 14 | `0x123456789abcdef0123456789abcdefp-100` | `dfbc9a7856343241` | same | dfbc9a7856343241 MATCH | dfbc9a7856343241 MATCH | ok |
| 15 | `0xffffffffffffffffp0` | `000000000000f043` | same | 000000000000f043 MATCH | 000000000000f043 MATCH | ok |
| 16 | `0x1ffffffffffffffp0` | `0000000000008043` | same | 0000000000008043 MATCH | 0000000000008043 MATCH | ok |
| 17 | `0x100000000000000000…(269 chars)…000000p-1040` | `000000000000f03f` | same | 000000000000f03f MATCH | 000000000000f03f MATCH | ok |
| 18 | `0x100000000000000000…(1008 chars)…000000p-3996` | `000000000000f03f` | same | 000000000000f03f MATCH | 000000000000f03f MATCH | ok |
| 19 | `0xffffffffffffffffff…(308 chars)…ffffffp-1200` | `000000000000f03f` | same | 000000000000f03f MATCH | 000000000000f03f MATCH | ok |
| 20 | `0x1000000000000000000001p-80` | `0000000000003040` | same | 0000000000003040 MATCH | 0000000000003040 MATCH | ok |
| 21 | `0x1.fffffffffffffp1023` | `ffffffffffffef7f` | same | ffffffffffffef7f MATCH | ffffffffffffef7f MATCH | ok |
| 22 | `0x1.fffffffffffff8p1023` | `000000000000f07f` | same | 000000000000f07f MATCH | 000000000000f07f MATCH | ok |
| 23 | `0x1.fffffffffffff7ffffffffffffp1023` | `ffffffffffffef7f` | same | ffffffffffffef7f MATCH | ffffffffffffef7f MATCH | ok |
| 24 | `0x1p1024` | `000000000000f07f` | same | 000000000000f07f MATCH | 000000000000f07f MATCH | ok |
| 25 | `0x1p99999999999` | `000000000000f07f` | same | 000000000000f07f MATCH | 000000000000f07f MATCH | ok |
| 26 | `0x0.00000001p1055` | `000000000000e07f` | same | 000000000000e07f MATCH | 000000000000e07f MATCH | ok |
| 27 | `0x1p-1022` | `0000000000001000` | same | 0000000000001000 MATCH | 0000000000001000 MATCH | ok |
| 28 | `0x0.fffffffffffffp-1022` | `ffffffffffff0f00` | same | ffffffffffff0f00 MATCH | ffffffffffff0f00 MATCH | ok |
| 29 | `0x0.ffffffffffffffp-1022` | `0000000000001000` | same | 0000000000001000 MATCH | 0000000000001000 MATCH | ok |
| 30 | `0x1.00000000000008p-1022` | `0000000000001000` | same | 0000000000001000 MATCH | 0000000000001000 MATCH | ok |
| 31 | `0x1p-1074` | `0100000000000000` | same | 0100000000000000 MATCH | 0100000000000000 MATCH | ok |
| 32 | `0x1p-1075` | `0000000000000000` | same | 0000000000000000 MATCH | 0000000000000000 MATCH | ok |
| 33 | `0x1.0000000000000001p-1075` | `0100000000000000` | same | 0000000000000000 ORACLE≠REF | 0100000000000000 MATCH | SEE |
| 34 | `0x1.8p-1075` | `0100000000000000` | same | 0100000000000000 MATCH | 0100000000000000 MATCH | ok |
| 35 | `0x1.8p-1074` | `0200000000000000` | same | 0200000000000000 MATCH | 0200000000000000 MATCH | ok |
| 36 | `0x3p-1075` | `0200000000000000` | same | 0200000000000000 MATCH | 0200000000000000 MATCH | ok |
| 37 | `0x1p-99999999999` | `0000000000000000` | same | 0000000000000000 MATCH | 0000000000000000 MATCH | ok |
| 38 | `0x8000000000000BFp-1082` | `0100000000000800` | same | 0000000000000800 ORACLE≠REF | 0100000000000800 MATCH | SEE |
| 39 | `0x8000000000000C0p-1082` | `0100000000000800` | same | 0100000000000800 MATCH | 0100000000000800 MATCH | ok |
| 40 | `0x8000000000000BEp-1082` | `0100000000000800` | same | 0000000000000800 ORACLE≠REF | 0100000000000800 MATCH | SEE |
| 41 | `0x1.0000000000000fp-1030` | `0000000000100000` | same | 0000000000100000 MATCH | 0000000000100000 MATCH | ok |
| 42 | `0x1.00000000000003p-1040` | `0000000004000000` | same | 0000000004000000 MATCH | 0000000004000000 MATCH | ok |
| 43 | `0x0p0` | `0000000000000000` | same | 0000000000000000 MATCH | 0000000000000000 MATCH | ok |
| 44 | `0x0.0p0` | `0000000000000000` | same | 0000000000000000 MATCH | 0000000000000000 MATCH | ok |
| 45 | `0x0p99999999` | `0000000000000000` | same | 0000000000000000 MATCH | 0000000000000000 MATCH | ok |
| 46 | `0x0p-99999999` | `0000000000000000` | same | 0000000000000000 MATCH | 0000000000000000 MATCH | ok |
| 47 | `0.0` | `0000000000000000` | same | 0000000000000000 MATCH | 0000000000000000 MATCH | ok |
| 48 | `.0` | `0000000000000000` | same | 0000000000000000 MATCH | 0000000000000000 MATCH | ok |
| 49 | `0e0` | `0000000000000000` | same | 0000000000000000 MATCH | 0000000000000000 MATCH | ok |
| 50 | `0e999999999` | `0000000000000000` | same | 0000000000000000 MATCH | 0000000000000000 MATCH | ok |
| 51 | `0.1` | `9a9999999999b93f` | same | 9a9999999999b93f MATCH | 9a9999999999b93f MATCH | ok |
| 52 | `0.2` | `9a9999999999c93f` | same | 9a9999999999c93f MATCH | 9a9999999999c93f MATCH | ok |
| 53 | `0.3` | `333333333333d33f` | same | 333333333333d33f MATCH | 333333333333d33f MATCH | ok |
| 54 | `1e23` | `f64ae1c7022db544` | same | f64ae1c7022db544 MATCH | f64ae1c7022db544 MATCH | ok |
| 55 | `9007199254740993.0` | `0000000000004043` | same | 0000000000004043 MATCH | 0000000000004043 MATCH | ok |
| 56 | `9007199254740995.0` | `0200000000004043` | same | 0200000000004043 MATCH | 0200000000004043 MATCH | ok |
| 57 | `2.2250738585072011e-308` | `ffffffffffff0f00` | same | ffffffffffff0f00 MATCH | ffffffffffff0f00 MATCH | ok |
| 58 | `2.2250738585072012e-308` | `0000000000001000` | same | 0000000000001000 MATCH | 0000000000001000 MATCH | ok |
| 59 | `4.9406564584124654e-324` | `0100000000000000` | same | 0100000000000000 MATCH | 0100000000000000 MATCH | ok |
| 60 | `2.4703282292062327e-324` | `0000000000000000` | same | 0000000000000000 MATCH | 0000000000000000 MATCH | ok |
| 61 | `2.4703282292062328e-324` | `0100000000000000` | same | 0100000000000000 MATCH | 0100000000000000 MATCH | ok |
| 62 | `1.7976931348623157e308` | `ffffffffffffef7f` | same | ffffffffffffef7f MATCH | ffffffffffffef7f MATCH | ok |
| 63 | `1.7976931348623158e308` | `ffffffffffffef7f` | same | ffffffffffffef7f MATCH | ffffffffffffef7f MATCH | ok |
| 64 | `1.7976931348623159e308` | `000000000000f07f` | same | 000000000000f07f MATCH | 000000000000f07f MATCH | ok |
| 65 | `1e400` | `000000000000f07f` | same | 000000000000f07f MATCH | 000000000000f07f MATCH | ok |
| 66 | `1e-400` | `0000000000000000` | same | 0000000000000000 MATCH | 0000000000000000 MATCH | ok |
| 67 | `1e999999999` | `000000000000f07f` | same | 000000000000f07f MATCH | 000000000000f07f MATCH | ok |
| 68 | `1e-999999999` | `0000000000000000` | same | 0000000000000000 MATCH | 0000000000000000 MATCH | ok |
| 69 | `123456789012345678901234567890.0` | `3e376cff90eef845` | same | 3e376cff90eef845 MATCH | 3e376cff90eef845 MATCH | ok |
| 70 | `0.30000000000000004` | `343333333333d33f` | same | 343333333333d33f MATCH | 343333333333d33f MATCH | ok |
| 71 | `1.000000000000000111…(55 chars)…680908203125` | `000000000000f03f` | same | 000000000000f03f MATCH | 000000000000f03f MATCH | ok |
| 72 | `1.000000000000000111…(56 chars)…809082031251` | `010000000000f03f` | same | 010000000000f03f MATCH | 010000000000f03f MATCH | ok |
| 73 | `5e-324` | `0100000000000000` | same | 0100000000000000 MATCH | 0100000000000000 MATCH | ok |
| 74 | `8.5e-324` | `0200000000000000` | same | 0200000000000000 MATCH | 0200000000000000 MATCH | ok |
| 75 | `7.4109846876186982e-324` | `0200000000000000` | same | 0200000000000000 MATCH | 0200000000000000 MATCH | ok |
| 76 | `007.5` | `0000000000001e40` | same | 0000000000001e40 MATCH | 0000000000001e40 MATCH | ok |
| 77 | `1.` | `000000000000f03f` | same | 000000000000f03f MATCH | 000000000000f03f MATCH | ok |
| 78 | `1E+2` | `0000000000005940` | same | 0000000000005940 MATCH | 0000000000005940 MATCH | ok |
| 79 | `1e-0` | `000000000000f03f` | same | 000000000000f03f MATCH | 000000000000f03f MATCH | ok |
| 80 | `3.141592653589793238…(64 chars)…582097494459` | `182d4454fb210940` | same | 182d4454fb210940 MATCH | 182d4454fb210940 MATCH | ok |
| 81 | `1.5f` | `000000000000f83f` | same | 000000000000f83f MATCH | 000000000000f83f MATCH | ok |
| 82 | `0.5F` | `000000000000e03f` | same | 000000000000e03f MATCH | 000000000000e03f MATCH | ok |
| 83 | `2.0l` | `0000000000000040` | same | 0000000000000040 MATCH | 0000000000000040 MATCH | ok |
| 84 | `3.25L` | `0000000000000a40` | same | 0000000000000a40 MATCH | 0000000000000a40 MATCH | ok |
| 85 | `1e1F` | `0000000000002440` | same | 0000000000002440 MATCH | 0000000000002440 MATCH | ok |
| 86 | `0x1.8p1f` | `0000000000000840` | same | 0000000000000840 MATCH | 0000000000000840 MATCH | ok |
| 87 | `0x1p-1074L` | `0100000000000000` | same | 0100000000000000 MATCH | 0100000000000000 MATCH | ok |
| 88 | `-0x0p0` | `0000000000000080` | same | 0000000000000000 ORACLE≠REF | 0000000000000000 LEAN≠REF | SEE |
| 89 | `-0.0` | `0000000000000080` | same | 0000000000000000 ORACLE≠REF | 0000000000000000 LEAN≠REF | SEE |
| 90 | `9.3288515364639567e-194` | `f7eefe8e693ddb17` | same | f7eefe8e693ddb17 MATCH | f7eefe8e693ddb17 MATCH | ok |
| 91 | `3.9737295848771364e-195` | `adf1b558a5909217` | same | adf1b558a5909217 MATCH | adf1b558a5909217 MATCH | ok |
| 92 | `5.8014873391049894e-283` | `e42075e538915505` | same | e42075e538915505 MATCH | e42075e538915505 MATCH | ok |
| 93 | `6.8255350919034949e1` | `8e9061ab57105140` | same | 8e9061ab57105140 MATCH | 8e9061ab57105140 MATCH | ok |
| 94 | `9.0609931047976448e158` | `01ae4095bf7ff060` | same | 01ae4095bf7ff060 MATCH | 01ae4095bf7ff060 MATCH | ok |
| 95 | `1.5768034388618686e-237` | `fb668df41289c40e` | same | fb668df41289c40e MATCH | fb668df41289c40e MATCH | ok |
| 96 | `3.2146595624762460e-101` | `1826ddc20200122b` | same | 1826ddc20200122b MATCH | 1826ddc20200122b MATCH | ok |
| 97 | `5.0201405046214136e207` | `8d9b9403e0450f6b` | same | 8d9b9403e0450f6b MATCH | 8d9b9403e0450f6b MATCH | ok |
| 98 | `0x85f3d96a84939ep-491` | `7492502d7bbeb024` | same | 7492502d7bbeb024 MATCH | 7492502d7bbeb024 MATCH | ok |
| 99 | `0x5450a34a627546p143` | `529d98d22814454c` | same | 529d98d22814454c MATCH | 529d98d22814454c MATCH | ok |
| 100 | `0x8bd809c130636bp-882` | `6d0c2638017b410c` | same | 6d0c2638017b410c MATCH | 6d0c2638017b410c MATCH | ok |
| 101 | `0x3e78dfc4593917p-351` | `8c9c2ce26f3c5f2d` | same | 8c9c2ce26f3c5f2d MATCH | 8c9c2ce26f3c5f2d MATCH | ok |
| 102 | `0x743577d9af49031980c62d0abb9fa0p261` | `41d26bf65d0dad57` | same | 41d26bf65d0dad57 MATCH | 41d26bf65d0dad57 MATCH | ok |
| 103 | `0x636e3f3f7059605c5ed6724d76757bp744` | `5816dccf8fdbd875` | same | 5816dccf8fdbd875 MATCH | 5816dccf8fdbd875 MATCH | ok |
| 104 | `0x38dde74efeb0d2d90569766f505813p-62` | `69587fa7f36e6c43` | same | 69587fa7f36e6c43 MATCH | 69587fa7f36e6c43 MATCH | ok |
| 105 | `0xcc272042c863ee33c78b8538a81f1fp-177` | `7e0c5908e484593c` | same | 7e0c5908e484593c MATCH | 7e0c5908e484593c MATCH | ok |

DERIVED TALLY: literals=105 lean_vs_ref_disagree=2 oracle_vs_ref_disagree=5 gcc_vs_python_disagree=0

**Reading of the tally [AGENT auditor].** `literals=105`: rows 1–87 and 90–105 are the 103 conversion literals — **0 Lean-vs-reference disagreements**; **3 oracle-vs-reference disagreements**, rows 33 (`0x1.0000000000000001p-1075`, a 65-bit mantissa whose sticky bit is lost by the 53-bit pre-rounding, then the `ldexp` tie goes to 0), 38 (the R5 pin) and 40 (`…BEp-1082`, 190/256 quanta, same mechanism) — every one a subnormal hexadecimal result with more than 53 significant bits whose 53-bit pre-rounding lands on a tie, i.e. exactly R5's declared scope (VALIDATION §2); on all three Lean is correctly rounded (`MATCH` against Python and gcc). Rows 88–89 (`-0x0p0`, `-0.0`) are NOT literal conversions on the C path: the lexer yields the unsigned constant and `-` is elaborated as `0.0 − x` (draft 42); there oracle = Lean = `+0` against gcc/Python `−0` — the tally's `lean_vs_ref_disagree=2` is entirely these two rows, where Lean mirrors the oracle as the rule requires. Hex ties at bit 54 in both directions (rows 8/9, 12/13), sticky above/below the tie (10/11), 128-, 260-, 1000-, 1200-bit mantissas (14, 17–19), the overflow boundary including the tie-to-inf (21–24) and a `strtol`-overflowing exponent (25, 37), min normal / max subnormal / min subnormal / the 2^−1075 tie and its neighbours (27–36, 41–42), positive-zero forms with huge exponents (43–50), decimal ties at 2^53 and 1e23 (54–56), the subnormal decimal midpoints (57–61, 73–75), the DBL_MAX midpoint (62–64), exact 55/56-digit midpoints (71–72), 64-digit π (80), all four suffixes on decimal and hex (81–87), upper-case `X`/`P`/hex digits (2, 5–7), and 8 random 17-digit decimals + 8 random 14/30-hex-digit literals (90–105) all MATCH on both engines.

## VERDICT

**MERGE-READY AFTER P2 FIXES** — F1 (LADDER row 1 `8/8` → `9/9`) and F2 (record / tray README / draft 39 carry the return-then-store shape and its crash class). No wrong answer, no unsound step, no policy violation and no silently moved baseline row were found; the one changed existing pin (`minimal/097`) is a dated, rationale-carrying orchestrator re-record whose rationale I reproduced as true.
