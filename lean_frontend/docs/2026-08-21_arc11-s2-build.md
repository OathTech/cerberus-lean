# Arc-11 / S2 — BUILD SLICE record: the T5 climb (engine prong + the env-lookup family; PARKED AT EVIDENCE GRADE, position advanced)

Date: 2026-08-21/22. Provenance: [AGENT:arc11-S2] throughout. Work
order: D2 (decision log) — S2 GO per the S0 plan §12.6 + the charter;
D3-class stop rule armed. All commands capped `CERB_MEM_MAX=40G`;
probe scratch untracked (`ProbeT5S4*.lean`, `scratch/probet5s4*.out`
— every claim below reproducible from them).

## Outcome (headline)

**T5 is NOT landed this session; the climb is PARKED AT EVIDENCE
GRADE with the position ADVANCED past the arc-9 park and the
resumption toolchain transformed.** Concretely:

* the arc-9 44/79 park round (census R65) is BROKEN — the k=0
  iteration walk now clears **45/79** (stuck goal at `fuel + 34`;
  was `fuel + 35`), and the wall there was NOT a missing law: the
  named "continuation-lambda advance" suspect dissolved under the
  trace instrument into three measured EMITTER budget/robustness
  defects, all fixed (below);
* the SYMBOLIC-j hbody walk (the real `iter_compose` obligation,
  succ case) is LIVE: 3/79 rounds fire at symbolic j with the
  hypothesis-mediated fact route WORKING end-to-end (`kwf: eq-fact
  HIT hlkN` — a family lookup fact consumed mid-chase by kernel
  matching), at ~6 s per probe cycle (the arc-9 diagnosis cycle was
  ~10 min);
* the §11.2 ENV-LOOKUP FAMILY is PROVED and COMMITTED
  (`relsem/RelSem/T5Iter.lean`, in-build): `envL_built`,
  `envL_lookup_n` (whole-chain, stuck-seed keys via hdig/hseed +
  the slate j-bound), `envL_lookup_i`/`envL_lookup_s`, the
  comparator helpers — the semantic engine hbody needs, banked.

## 1. The engine prong (D2 step 1) — the stuck round dissolved

The trace instrument (S1) + a diagnosis lane run on the stuck census
round R65 (`tau[Ewseq Eannot]`). Findings, each measured then fixed:

* **F-S2-1 (silent aborts).** A candidate-attempt ABORT (heartbeat
  trip inside unification) was indistinguishable from a law mismatch:
  `attempt`'s debug prints used `logInfo`, which the caller's
  `restoreState` ROLLS BACK. Fixed (dbg_trace); the law loop's
  attempt now surfaces aborts in the trace lane. Root diagnosis
  became possible only after this.
* **F-S2-2 (the R65 wall itself).** `advance_tau_misc` WAS found and
  applicable — the crossing is `Step_tau2 "Ewseq Eannot" TSK_Misc
  {arena := apply_ctx …}` (dumped verbatim in the record's probe
  outputs; NO lambda-headed step, the arc-9 suspect class does not
  occur here). The walls were emitter-side, in order of discovery:
  (a) `normStateV2`'s UNCAPPED elaborator whnf on the post-state's
  `apply_ctx` arena redex burned the candidate window → whole
  `normAppState` normalize+seal now capped with a RIDE-RAW fallback
  (give-up-keeps-compact-spelling); (b) the TERMINAL RHS check
  (`withReducible isDefEq`) burns the window on a NEGATIVE deep
  defeq exactly when the mid-iteration state approaches the target
  family → capped (a terminal that cannot be decided fast is not
  terminal), state restored on the miss; (c) the ROUND SEAL used
  `mkAuxTheorem` bare — its elaborator-side re-verification tripped
  after (a)/(b) were cleared → `mkAuxThmRobust` (capped primary +
  the raw-`addDecl` kernel-only fallback, the `mkAuxRfl` pattern
  generalized to arbitrary proof values, fvar/level closure over
  type AND value). With (a)-(c): R65 FIRES (ping trace: `hyps ok →
  proof assembled → state normalized → bridge done → round sealed`)
  and the k=0 walk reaches 45/79.
* **F-S2-3 (selection payloads).** `kWhnfSelection`: kernel-whnf of
  a selection fact stops at `Option.some payload` WHNF leaving the
  payload non-ctor-headed; planted selections now kernel-whnf the
  payload one level (kernel-canonical; the aux-rfl certificate
  re-derives it) so advance-law DiscrTree/unification see the Step
  constructor.

## 2. The symbolic hbody route (D2 step 3, brought forward)

At symbolic k the `arenaL` family is stuck on its `Nat` match —
hbody splits by cases: k=0 (concrete-arena block, the iter0 walk)
and k=j+1 (arena = the pinned `whileBody`, everything else symbolic
— the period-uniform representative). The succ-case walk at symbolic
j exposed the real semantic frontier, exactly the §11.2 design: EVERY
env operation at the family is non-computational (the recursive
`envL` base is stuck at the symbolic index; tree inserts/lookups
cannot reduce) — the hypothesis-mediated route is not optional, it
is the only route. Mechanisms built to serve it (all engine-side,
ordinary certificates):

* **F-S2-4 (equation-fact chase).** `kWhnfWithFacts` GENERALIZED
  from decide-facts to arbitrary EQUATION facts: at a stuck chase
  level, a context fact `h : L = v` whose LHS matches rewrites the
  stuck form to `v` and reduction resumes (certificate = the fact
  under a defeq type hint; kernel-checked at declaration end).
  Deterministic first match. Chase depth 24 → 48 (the env chain is
  23 inserts deep — a structural bound, not a heartbeat budget).
* **F-S2-5 (recursor-stuck head relaxation).** A recursor/
  matcher-stuck form has lost its def-level head (kernel-whnf
  unfolded `fmapLookupBy` into `Fmap.rec`), so def-headed facts can
  only match by defeq — the head filter is relaxed for exactly the
  stuck classes.
* **F-S2-6 (KERNEL fact matching — the breakthrough).** The
  elaborator's capped defeq cannot afford the fact-vs-stuck-form
  comparison (e.g. the extern-symmap lookup through the seal chain:
  every capped attempt missed). CLOSED facts now decide by
  `Kernel.isDefEq` (heartbeat-free; kernel errors are misses).
  Measured result: `kwf: eq-fact HIT hlkN` — the family lookup fact
  consumed mid-chase, the symbolic walk advanced 1 → 3 rounds,
  5.6 s wall.
* **F-S2-7 (seal-transparent context matching).** The assumption
  lane's head filter now computes a SEAL-TRANSPARENT effective head
  (registry-driven unfold+beta, A-F6's registry doing structural
  work) so crossings whose LHS head is a sealed step_m closure can
  match facts stated at real semantic heads. Companion:
  `unsealHead` before the computed-value ctor unification
  (seal-headed values otherwise silently defeat
  `Result (Defined ?th', ?rs')` assignment).

## 3. The env-lookup family (committed: `relsem/RelSem/T5Iter.lean`)

Wave-1 groundwork, proved through Kit/Map's `FmapBuilt` + lookup
engine and in-build since this commit: `symCmpO_ne_of_id`/
`symCmpO_self` (comparator facts via `symCmpO_eq_iff`); `e0_built`
(rfl), `eIns_built`, `envL_built` (induction; `iterate`-chained);
`lookup_eIns_ne`/`lookup_eIns_eq` (the family-spelling law
instances); `envL_lookup_n` (whole-chain induction: 27 concrete-key
skips by `decide`d id-inequalities + the TWO stuck-seed keys
(`unitSym (2j)`, `unitSym (2j+1)`) discharged from `hdig`/`hseed`
with the slate bound `j ≤ 100` via `omega` — §11.2's design
executed verbatim; the k=0 base is pure kernel computation, `rfl`);
`envL_lookup_i`/`envL_lookup_s` (chain-head hits). The seed linkage
is definitional: `seedT5 = 1048577 := hfresh` typechecks directly
(`initial_core_run_state` seeds `sym_supply` from the
`runEffectful` draw — generated Core_run_aux.lean:395).

## 4. The park (position + next unknowns, precisely)

* **k=0 iteration walk: 45/79** (`fuel + 34`), the five k=0 memory
  facts + hn/hb/hfresh in context. Was 44/79 at arc-9 close and at
  S1.
* **Symbolic-j hbody (succ case): 3/79** (`fuel + 76`) with the
  family facts; the fact route VALIDATED (hlkN consumed).
* **Next unknowns (named, with evidence in the probe outputs):**
  1. the round-4 crossing (`RSK_eval "eval operands of Load"`) —
     the chase exhausts at `exceptM.rec`-stuck forms whose majors
     carry AVATAR-BLOCKED sealed closures (`t0.casesOn` under
     `walkVal_aux` seals): the sealed step_m closures capture
     abstraction avatars — the localized residue of the arc-9
     "sealStates not yet coherent" note, now with an exact
     reproducer (3 engine iterations did not clear it; per the
     park-don't-grind doctrine it goes to the register, not to a
     fourth improvisation);
  2. the INTERSTITIAL fact spellings: mid-iteration lookups run over
     prefix-extended envs (the iteration's own inserts over
     `envL n (j+1)`); the first two interstitial facts were stated
     but did not match the walk's actual spellings — deriving the
     canonical interstitial spelling from the trace (or teaching the
     chase the Kit skip-law as a rewrite rule) is the next mechanism
     decision — an OPERATOR-VISIBLE design choice (per-round fact
     families vs a law-driven chase), flagged for D3;
  3. the remaining iteration structure (NEG draws, stores at
     interstitial memory, the Erun jump with the labeled-map lookup)
     — all designed classes (§11.2/§12.5), none yet reached at
     symbolic j.
* **Stop-rule assessment (honest):** NO new wall class was hit —
  every wall this slice fell to the standing toolkit (emitter caps/
  robustness, kernel-side matching, the chase generalization), and
  the frontier items above are designed-class work. What ends the
  slice is CAPACITY against the remaining volume (~75 symbolic
  rounds at a measured nonzero engineering cost per round-class,
  plus exit/post/composition/statement), not a wall — the arc-9 D4
  precedent. The toolchain delta this slice: diagnosis cycle
  ~10 min → ~6 s; every future round-class lands against the same
  instrument.

## 5. Regression bar (verbatim tails at the batch commit)

* capped full `lake build` (T5Iter now in-build): `Build completed
  successfully (611 jobs).`; `RelSem audit sweep: 3021 declarations
  across RelSem.* modules, all within the declared axiom boundary
  (0 recorded sorryAx exceptions)` — T1 calibration, `entry5_walk`,
  E7-E10, and all 54 kit pins re-elaborated under the final S2
  engine.
* `test_unit.sh`: `Total: 7 passed, 0 failed`; `test_verify.sh`:
  `test_verify: 29 passed, 0 failed (5 fixtures, 18 harness
  points)`.
* exec-minimal: `SUMMARY: total=106 match=85 ub_match=18 ub_diff=0
  mismatch=0 fail=0 crash=0 lean_error=0 timeout=0 cerb_skip=3
  cerb_inconsistent=0` (zero movement).
* Probe positions banked (untracked scratch, final engine): k=0
  iter0 **45 fired / stuck `fuel + 34`** (`probet5s4-final.out`);
  symbolic-j succ block **3 fired / stuck `fuel + 76`** with
  `eq-fact HIT hlkN` (`probet5s4c6/7.out`).

## Register items (new)

* R-S2-1: sealed step_m closures capture abstraction avatars —
  emitter-coherence item (the round-4 blocker); reproducer:
  `ProbeT5S4c.lean` + `scratch/probet5s4c9.out`.
* R-S2-2: interstitial-env fact spelling — mechanism decision
  (fact families vs law-driven chase), operator-visible.
* R-S2-3: `walk_diag`-class instruments live in untracked probes;
  the committed debug surface (`app_walk_norm?`) does not yet print
  the eff-head/stuck-form dumps (kept trace-lane-only this slice).
