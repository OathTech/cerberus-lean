# Pin bump 3c88f0d — slice record (cerberus-lean onto the lem-lean parity-fix mainline)

Date: 2026-09-03. Branch `arc/pin-bump` (worktree
`worktrees/cerberus-lean-arc/pin-bump`, off mainline `f95ef8d9c`).
Worker: [AGENT] pin-bump worker; every decision below is [AGENT] unless
marked; every quoted output is verbatim (derived tallies are labelled).
Consumer manifest: `docs/2026-09-03_pin-bump-change-manifest.md`.
Lem-side record: lem-lean `doc/lean-backend/2026-09-03_parity-fix-record.md`
(§5 CONSUMER-IMPACT was the worklist; §6 the audit addendum).

Operator rule in force [USER 2026-09-03, relayed by the orchestrator]:
zero Lean-vs-oracle execution discrepancies; the differential battery
is the gate; any movement must be enumerated and attributed to a named
lem fix; anything unattributable is a STOP.

## 0. Summary

- lem-lean `bc1bae7 → 3c88f0d` (the parity-fix arc) is pinned on every
  pin surface (§1). The `.lem` model is untouched; the OCaml generated
  tree is BYTE-IDENTICAL before/after (§2); the Lean generated tree
  changes in 49 of 193 files (§3).
- Six hand-written sites adapted (§4), each mirroring the OCaml or
  stating the port-side contract; no semantics change beyond what the
  new lem forces.
- Battery (§5): every gate green at the COMMITTED baselines — zero
  moved rows. The predicted movements (§6) did not materialise as
  baseline changes; the one measured movement is outside every gate
  and toward the oracle (`--pp-core` enumeration order, F10), found by
  a cross-version probe.
- mem-scale (§5.4): one non-gating row changed SURFACING, not class —
  `d_loop_1000000` lean-first, the recorded process-stack ceiling, now
  parks in the runtime's overflow handler (HANG) instead of aborting;
  reproduced 2+2 against the old binary; flagged for the orchestrator.
- Census (§7): the brief's `CerbMem.lean:1352` Z-class row is NOT a
  divergence — the oracle has the same zero guard (impl_mem.ml:2479-2480,
  quoted); registered as an erratum against the lem record §5 instead.
  The real, already-recorded neighbour (IntRem_t/IntRem_f on a zero
  divisor, unreachable behind UB045) is restated as the census row.

## 1. Pin evidence

opam (the shared local switch `cerberus-lean/_opam`, sanctioned by the
orchestrator for this slice; `deps/lem-pinned` had been advanced to
`3c88f0d` by the orchestrator beforehand). `opam upgrade --switch=.
--no-depexts -y lem`, verbatim tail:

```
[NOTE] Ignoring uncommitted changes in /home/dev/projects/cerberus-lean-proj/deps/lem-pinned (`--working-dir' not active).
[lem.2026-05-01] synchronised (git+file:///home/dev/projects/cerberus-lean-proj/deps/lem-pinned#cerberus-pin)
  ↻ recompile lem 2026-05-01*
⊘ removed   lem.2026-05-01
∗ installed lem.2026-05-01
Done.
```
(the "uncommitted changes" are three untracked build artefacts in
`deps/lem-pinned/ocaml-lib/` — `install_lem`, `install_num`,
`install_zarith` — not source; `git status --short` there shows only
`??` rows.) Afterwards: `lem -v` → `Lem 3c88f0d`; `opam show --switch=.
lem -f source-hash` → `3c88f0d7e5556491d733932c503c1637ae186f54`
(before the upgrade: `Lem 045dcb0` / `045dcb0d57a171eb4fb3a6eb5abe288c227270ce`).

Lake: `lean_frontend/lakefile.toml` `rev` moved `045dcb0d…` →
`3c88f0d7e5556491d733932c503c1637ae186f54` (comment records the bump);
`lake update LemLib` run (capped, `CERB_MEM_MAX=32G`) in
`lean_frontend`, `lean_frontend/speclab`, `tests/mem-scale-probes/micro`.
Verbatim (lean_frontend): `info: LemLib: URL has changed; deleting
'…/lean_frontend/.lake/packages/LemLib' and cloning again` / `info:
LemLib: checking out revision '3c88f0d7e5556491d733932c503c1637ae186f54'`
(the URL note is the septract→OathTech spelling already in the lakefile;
resolution is the local `deps/gitconfig` redirect either way). All three
manifests now read `"rev": "3c88f0d7e5556491d733932c503c1637ae186f54"`
and `"inputRev": "3c88f0d7e5556491d733932c503c1637ae186f54"` for LemLib.

Two-repo invariant at close: lem-lean `mdd/lean-backend` head
`3c88f0d7e5556491d733932c503c1637ae186f54` = `deps/lem-pinned` HEAD =
opam source-hash = the three Lake `rev`/`inputRev`.

Freshness stamps on the battery binaries (`driver_fresh.*.sha256`,
recorded by `build_cerberus`/`build_lean`):
```
lean:   bin 8c484a477dc862bc255ba8a46f1b6791cb994a18a8b42e1fe77689aa82a1ff6a  src 60f9b3efb82e380130da3727a74627602f476cc0828a0db67dcc33a387bdf091
oracle: bin 8dc62fa2e9e1157a2b8fe6ec999305d17cfaa4944a83a68cbf9a57b277f25c68  src c9c1a7067139b3ceb4eb0ad6870b93d8d0dbbaa9bd39e0397f11e8c975737a3b
```
lem-sync stamps: OCaml `src 4f2e089b…0c54 gen 295e4f82…8100` (UNCHANGED
across the bump — the generated OCaml is the same content); Lean `src
4f2e089b…0c54 gen 41781645302ba346ab11977956c6eb2e82f5de95678bc3d1091b1ae9e9eab3f8`
(was `49ad8b2c…eee00`).

## 2. OCaml byte-identity proof

Method: snapshot `ocaml_frontend/generated` + its stamp (as generated by
the `045dcb0` lem, stamp-verified `check_lem_sync: OK`) to
`.tmp/prebump/ocaml_generated`; then `rm -rf lean_frontend/generated &&
make clean-prelude-src prelude-src` with the `3c88f0d` lem; then
`diff -rq`. Verbatim:

```
check_lem_sync: recorded ocaml_frontend/lem_sync.sha256 (src 4f2e089b39d5b371973513b3350f81d1b89871976f77df9ba4a25da3421d0c54, gen 295e4f8291c9ffd57a4061dd38e8ec273f18d6c1cfe3a0465291f1a4bcff8100)
OCAML GENERATED TREE BYTE-IDENTICAL
```
(`diff -rq .tmp/prebump/ocaml_generated ocaml_frontend/generated` printed
nothing.) The `sibylfs/generated` tree (also lem-generated OCaml) was
compared against the primary checkout's old-lem output: `diff -rq`
empty — `SIBYLFS GENERATED BYTE-IDENTICAL to main checkout's`.

Oracle rebuilt cache-disabled (`DUNE_CACHE=disabled`, `build_cerberus`):
`check_lem_sync: OK (src 4f2e089b…, gen 295e4f82…)`, stamp recorded
(bin `8dc62fa2…`). Fork-drift gate in `test_unit.sh` (§5): `check_fork_drift:
OK — layer 1: 71 oracle-surface files = manifest; layer 2: 22 differing
generated files, all hash-pinned (merge-base b9aeedcb4dd438763b0eef7f95ac19e93875d7de)`
— NO manifest change.

## 3. The Lean generated tree

`diff -ru` of the pre-bump snapshot vs the regenerated tree: 7265 diff
lines; 49 of 193 files differ (derived). Token census on `+`/`-` lines
(derived): `compare_derived` +456/-0; `Pset` +270/-0; `lemListFoldr`
+56 (71 sites in the tree); `lemListZip` +26; `lemIntegerDiv` +7;
`lemNatDiv`/`lemNatMod` +1 each; `setFromListBy`/`setToList`/`setFold`
rename-neutral (equal +/-); `Int32`/`Int64` 0; `LemUnsupported` 0;
`-  deriving BEq, Ord` 55, `+  deriving BEq, Ord` 0; `ctor_rank_ocaml`
definitions 35 → 90. Largest movers: Cmm_csem (565 changed lines),
Undefined (520), Errors (433), Constraint (381), Cn (363), Mem_common
(342) — all derived-instance shape (F4). Classification by lem fix is in
the manifest §1-§3. `make lean-prelude-src` completed with no refusal;
`check_handwritten_sync: OK (23 hand-written files …)`.

## 4. Seam adaptations (build_lean red → green)

First `build_lean` after regeneration: FAILED in `CerbMem` —
`generated/CerbMem.lean:126:16: Unknown identifier 'Std.TreeMap'` (and
:132, :1797 + the cascade). Cause: LemLib `045dcb0` opened with `import
Std.Data.TreeMap` (its Fmap internals); `3c88f0d` does not (Fmap is the
Pmap port), and CerbMem.lean's `MemState` maps are `Std.TreeMap` (arc-6
S3, mirroring impl_mem.ml:93's `IntMap = Map.Make(Z)`). This site was
NOT on the lem record's impact list (the list was built from grep of
LemLib NAMES; a transitive import is invisible to it) — noted for the
lem-side record.

| # | Site | Adaptation | Mirrors / contract |
|---|---|---|---|
| 1 | `Main.lean` `loadLibc` assembly, 4 sites (tagDefs/funs/extern/funinfo) | `fmapOfSpine cmp l` → new `private def libcMapFromAssoc cmp l := l.foldl (fun acc (k, v) => fmapAddBy cmp k v acc) fmapEmpty` | OCaml `read_core_object`'s `map_from_assoc compare = List.fold_left (fun acc (k, v) -> Pmap.add k v acc) (Pmap.empty compare)` (backend/common/pipeline.ml:653-654, used at :672-679). Left fold, later key wins (the removed `fmapOfSpine` was a foldr — first key won); the four lists are duplicate-free by construction (`libcInsertChecked`, the `funs` `find?` dedup, `fmapElements` of a map), so the direction is unobservable and the OCaml's is taken |
| 2 | `CerbCall.lean:77` `funSymsNamed` | `Lem_Set.filter p dom` → `setFilterBy (fun s1 s2 => ordCompare s1 s2) p dom` | `Lem_Set.filter` is commented out in LemLib/Set.lean:116; `setFilterBy` = pset.ml `filter`, keyed by the same `Ord0 sym` comparator as the `fmapDomainBy` above it. Port-side harness entry (no OCaml counterpart, per the file header) |
| 3 | `CerbFunMapInstances.lean` header + one arm comment | "THE REQUIREMENT IS PHANTOM" → "THE VALUE-COMPARATOR LEG IS UNREACHABLE" | `Lem_Map_extra.fold` (LemLib/Map_extra.lean:36) is now `setFold … (fmapToSetBy (pairCompare setElemCompare setElemCompare) m)`: the instance IS passed; `pairCompare` reaches the value leg only when two bindings' keys compare EQ under `SetType sym` = `symbol_compare` (Symbol.lean:285-287, the model's own instance at priority 1000 beating the auto trio at 500), the comparator every `Fmap sym _` captures — bindings are pairwise distinct under it. Fold order ascending = `Pmap.fold`. Instance BODY unchanged |
| 4 | `CerbUtils.lean` `set_fold` | DELETED (dead; premise "Lem sets are sorted lists" false) | No generated module referenced it. RESIDUE: `frontend/model/core_linking.lem:61-63` declares the unused `val set_fold` with `declare lean target_rep function set_fold = \`CerbUtils.set_fold\`` — now a dangling declare on an unused val (lem emits no code for it; a future use fails loudly at Lean build). Not edited: a `.lem` change is outside this slice's OCaml-byte-identity contract even if its OCaml rendering would be empty. Registered in §8 |
| 5 | `CerbMem.lean:9` | `import Std.Data.TreeMap` added (comment cites the cause) | representation import, no behaviour |
| 6 | `test/Unit/FuelExemplar.lean` (`driver2_done`) | `import LemLibTheorems`; `dsimp only [List.map, List.foldr]` → `dsimp only [List.map]; rw [LemLibTheorems.lemListFoldr_eq]; dsimp only [List.foldr]` | the generated `nd_mapM` (Nondeterminism.lean:254) folds with `lemListFoldr` (F7, `l.toArray.foldr`), which does not `dsimp`-reduce on a literal list; LemLib ships the kernel-checked equation. Found by `test_unit.sh` (`Unit.FuelExemplar` build failure, verbatim: `error: test/Unit/FuelExemplar.lean:334:4: Type mismatch … runOne (lemListFoldr (fun m m' => nd_bind m …`). The theorem's axiom cone stays within the standard three (the FUEL arc leg of `check_theorem_axioms` re-ran green, §5) |

Second `build_lean`: `Build completed successfully`, `check_driver_fresh:
recorded lean stamp (bin 8c484a47…, src 60f9b3ef…)`; `real 0m34.522s`
after the 2m13s first (failed) pass — no grind.

## 5. Battery (verbatim gate lines; fresh stamped binaries)

### 5.1 Tier A (`scripts/LADDER.md`)

| Row | Verbatim |
|---|---|
| 1 `test_unit.sh` | first run rc=1 (the FuelExemplar site, §4 #6); after the fix rc=0: `✓ effects-proof-test PASSED` `✓ totality-proof-test PASSED` `✓ core-parser-test PASSED` `✓ fresh-int-test PASSED` `✓ pp-test PASSED` `✓ fuel-exemplar-test PASSED`; `check_theorem_axioms: OK (effect-retirement C2 bar: zero axiom declarations anywhere; entry cones ⊆ the standard three)` (C2 ratchet: `291 files scanned recursively: 0 axioms, 0 runEffectful, seam population = the 66 pinned path-qualified counted rows exactly`; FUEL leg `28 contract lemmas + drive_lemFuel + the ∀-fuel exemplar and its instances, every cone ⊆ [propext, Classical.choice, Quot.sound]`); `check_sorry_token: OK (256 files scanned comment-stripped — generated 193, hand-written+test 29, LemLib 34; 0 sorry tokens)`; `test_fuel_classifier: 18 fixtures, ALL OK`; `check_lem_sync: OK` + `lean OK`; `check_fork_drift: OK — layer 1: 71 oracle-surface files = manifest; layer 2: 22 differing generated files, all hash-pinned`; `check_fixture_freeze: OK (16 fixture files match the pinned manifest; name set exact)`; `test_renumber_plants: OK (12 plants: refusals refuse, admits admit with declared class)` |
| 2 minimal | `SUMMARY: total=106 match=85 ub_match=18 ub_diff=0 mismatch=0 fail=0 crash=0 fuel=0 lean_error=0 timeout=0 hang=0 cerb_skip=3 cerb_floor=0 cerb_inconsistent=0` / `Baseline check: 0 regression(s), 0 improvement(s)` / `BASELINE OK` |
| 3 coverage | `SUMMARY: total=199 match=174 ub_match=12 ub_diff=0 mismatch=0 fail=0 crash=0 fuel=0 lean_error=0 timeout=0 hang=0 cerb_skip=13 cerb_floor=0 cerb_inconsistent=0` / `Baseline check: 0 regression(s), 0 improvement(s)` / `BASELINE OK` |
| 4 debug | `SUMMARY: total=90 match=66 ub_match=20 ub_diff=0 mismatch=0 fail=0 crash=0 fuel=0 lean_error=0 timeout=0 hang=0 cerb_skip=4 cerb_floor=0 cerb_inconsistent=0` / `BASELINE OK` |
| 4b float | `SUMMARY: total=69 match=69 ub_match=0 … cerb_inconsistent=0` / `BASELINE OK` |
| 4c bytes | `SUMMARY: exec_match=9 neg_pinned=5 fail=0` / `ALL AT COMMITTED EXPECTEDS` |
| 5 libc_exec | `SUMMARY: match=7 diff=0` / `ALL MATCH RECORDED BASELINE` |
| 6 multi_tu | `SUMMARY: total=2 match=2 fail=0` / `ALL PASSED` |
| 7 parse | `Success rate:   100% (of cerberus successes)` / `ALL PASSED` |
| 8 core | `Success rate:   100% (of cerberus successes)` / `ALL PASSED` |
| 9 elab | `SUMMARY: total=106 same=103 diff=3 ocaml_fail=0 lean_fail=0` (the recorded state; rc 0) |
| 10 libxml2 uri | `[lean-nolibc] exit=1 …: Error {msg: "Illformed_program: calling an unknown procedure: Symbol(968, SD_Id("memset"))"}` / `[lean+libc] EXACT MATCH with ORACLE_LIBC (16/16 URI corpus)` / `GATE PASS: all lane expectations pinned-green + baseline unchanged (16/16)` |
| 11 cn_coverage | `BASELINE OK (213 entries, exact match)` |

Tier A rows 2-11 ran in 2m32s wall total (06:35:16-06:37:41 UTC).

### 5.2 Tier B (06:40:41-07:20:54 UTC; box load ≈ 26 during the libxml2 slices — other agents; every row rc=0)

| Row | Verbatim |
|---|---|
| 1 `test_libxml2.sh` | `SUMMARY: total=4 match=4 fail=0 (points: 1354, 22 observations each)` / `ALL PASSED` (12m42s wall on the loaded box) |
| 2 parse tests/ci | `ALL PASSED` (the two `REJECTED (exit 1): Undefined {ub: "UB060_block_scope_function_with_storage_class" …` rows are the lane's classified rejections) |
| 3 core tests/ci | `Lean parse:     128 ok, 0 failed` / `Success rate:   100% (of cerberus successes)` / `ALL PASSED` |
| 4 verify | `test_verify: 117 passed, 0 failed (23 fixtures, 22 call points, 14 corpus fixtures, 21 corpus points)` |
| 5 immaculate | `OK: lane matches the committed post-S1 baseline (mostly MATCH; the intended non-MATCH rows: g5-decode-question ORACLE_CRASH/L=63 and g5-escape-roundtrip DIFF/L=127 are oracle-wrong — upstream-tray #10/#11 — and g6 is …` |
| 6 speclab | `--selftest`: `test_speclab: PASS (both pipelines agree on Specified(0))`; `--plant`: `test_speclab: PASS (both pipelines agree on Specified(2))`; gates: `test_speclab_divmod: PASS (--gate)` `test_speclab_bytearr: PASS (--gate)` `test_speclab_list: PASS (--gate)` `test_speclab_tree: PASS (--gate)` `test_speclab_seed: PASS (--gate)` (each `…GateTest: ALL PASSED`; the speclab package rebuilt against LemLib 3c88f0d: `Build completed successfully (271 jobs).`) |
| 7 gcc second oracle | `Baseline check: 0 regression(s), 0 improvement(s)` / `gcc second-oracle lane OK` (06:59:25-07:18:xx, box load fell to ≈2 during the run — the TIMEOUT-class caveat did not bite) |
| 8 plants | `test_hang_plant: all plants read as expected (sleep→HANG, busy→TIMEOUT, both lanes; missing record→harness error)`; `test_kill_plant: all plants read as expected (cap breach -> OOM-KILLED witness; ci_sweep LEAN_KILL, libc_exec KILL, immaculate KILL, uri/libxml2 FAIL-killed; SIGKILL stub NOT the cap class; native exit(137) still compare…`; `test_fuel_plant: ALL PLANTS OK (FUEL classification live in exec/gcc/ci_sweep/cn_coverage/measure; negatives not FUEL)` |

`git status` after Tier A+B: only the ten pin/seam files and the two new
docs — NO baseline, pin or TSV file was modified by any lane.

### 5.3 Tier C — csmith corpus shards

Grind-tripwire note, written BEFORE the run [AGENT]: the six shards of
`test_csmith_corpus.sh --check-baseline --shard K/6` are a ~2.7 h
sequential differential-corpus MEASUREMENT sweep (LADDER.md Tier C:
"Full pass ~2.7 h: run --shard K/6 sharded"), requested by the brief
("csmith shards") — the category the tripwire names as qualifying
("measurement sweeps over differential corpora may qualify"); it is not
a build or proof pass and no code is iterated while it runs. Started
07:21:21 UTC after Tier B; shards run one at a time (box discipline).

Run: shard 1 in the harness-backgrounded runner (07:21:21-08:00:43);
the runner was then KILLED by the agent harness mid shard 2 (129/279 —
an environment event, not a lane failure; the partial log is kept as
`C-csmith-shard-2.killed-partial.log`), and shards 2-6 re-ran from
scratch in a detached runner (08:23:11-10:01:23; shard 2 restarted in
full — the lane is deterministic). Every shard `--check-baseline`
rc=0. Verbatim per shard:

| Shard | SUMMARY (verbatim) | Baseline |
|---|---|---|
| 1/6 | `SUMMARY: total=279 match=127 ub_match=0 ub_diff=0 mismatch=0 fail=0 crash=0 fuel=0 lean_error=0 timeout=1 hang=0 cerb_skip=151 cerb_floor=0 cerb_inconsistent=0` | `Baseline check: 0 regression(s), 0 improvement(s)` / `BASELINE OK` |
| 2/6 | `SUMMARY: total=279 match=159 ub_match=0 ub_diff=0 mismatch=0 fail=0 crash=0 fuel=0 lean_error=0 timeout=3 hang=0 cerb_skip=117 cerb_floor=0 cerb_inconsistent=0` | same |
| 3/6 | `SUMMARY: total=279 match=144 ub_match=0 ub_diff=0 mismatch=0 fail=0 crash=0 fuel=0 lean_error=0 timeout=2 hang=0 cerb_skip=133 cerb_floor=0 cerb_inconsistent=0` | same |
| 4/6 | `SUMMARY: total=279 match=234 ub_match=0 ub_diff=0 mismatch=0 fail=0 crash=0 fuel=0 lean_error=0 timeout=0 hang=0 cerb_skip=45 cerb_floor=0 cerb_inconsistent=0` | same |
| 5/6 | `SUMMARY: total=279 match=268 ub_match=0 ub_diff=0 mismatch=0 fail=0 crash=0 fuel=0 lean_error=0 timeout=1 hang=0 cerb_skip=10 cerb_floor=0 cerb_inconsistent=0` | same |
| 6/6 | `SUMMARY: total=274 match=229 ub_match=0 ub_diff=0 mismatch=0 fail=0 crash=0 fuel=0 lean_error=0 timeout=2 hang=0 cerb_skip=43 cerb_floor=0 cerb_inconsistent=0` | same |

Derived tally over the union (= the full 1669-file corpus): match 1161,
mismatch 0, fail 0, crash 0, fuel 0, lean_error 0, hang 0, timeout 9,
cerb_skip 499, cerb_floor 0; every shard at the committed
`scripts/exec_csmith_corpus_baseline.txt` with 0 regressions and 0
improvements. (The `timeout` rows are the baseline's recorded
TIMEOUT-class rows — wall-clock sensitive by the lane's own caveat; they
did not move.)

### 5.4 mem-scale corpus (non-gating instrument; `tests/mem-scale-probes/run_all.sh`)

Configuration = the committed `2026-09-02_s2-parity.tsv` sweep's
(`--engines oracle-random,lean-first`, 600 s, cap 32G) so rows compare
like-for-like; 10:01:23-10:40:35 UTC for the nolibc classes, then the
libc class after `scripts/libc_prep.sh --jsons .tmp/memscale/libcjson`
(the first pass had printed `measure.sh: libc jsons missing at …
(scripts/libc_prep.sh --jsons)` for every `--libc` row and continued —
the instrument is fail-noisy, non-gating; the e_*/z_base libc rows were
then run with `--only`). Result TSV committed as
`tests/mem-scale-probes/results/2026-09-03_pin-bump-sweep.tsv` (70
distinct (probe, mode, engine) rows; z_base nolibc appears twice,
identical).

Comparison [AGENT, `.tmp/pinbump-logs/memscale_compare.py`: same key ⇒
compare exit, verdict, note] against the two recorded sweeps:

vs `2026-09-02_s2-parity.tsv` (recorded at the 10^6 fuel budget):
`MEMSCALE COMPARE: common=70 same=63 moved=7 only_old=0 only_new=0`. The
seven, attributed:
- `a_zero_global_10000000 lean-first`: HANG both times; only the cpu
  figure inside the note differs (3.23 s → 3.19 s). NOT a movement.
- `b_zero_local_1000000` / `b_zero_local_10000000 lean-first`: exit 134,
  1.10 s both times; the note gained `FUEL(panic);` — the FUEL arc's
  classifier (added after s2-parity). Identical to the fuel-arc record's
  own rows (`134 1.10 … FUEL(panic); 0.02`). NOT a pin-bump movement.
- `d_loop_100000` and `e_memcpy_100000 lean-first`: 134 → 0 with the
  oracle's value (`VAL:Specified(-97)`, `VAL:Specified(7)`): the FUEL arc's
  10^8 budget (the fuel-arc record shows these completing at
  `18ceb18b2`+budget); identical to the fuel-arc record rows. NOT a
  pin-bump movement.
- `e_memcpy_1000000 lean-first`: 134 both times; the note gained
  `STACK_OVERFLOW;` (measure.sh marker added at `f95ef8d9c`, after the
  fuel-arc rows were recorded); wall 99.84 s → 42.35 s is load. Same
  class (the recorded process-stack ceiling). NOT a movement.
- `d_loop_1000000 lean-first`: **MOVED — same limit, different
  surfacing.** Fuel-arc record: `134 44.58 … NONE - ` ("Stack overflow
  detected. Aborting."); now `124 600.08 2183036 NONE HANG(cpu 36.66s of
  600.08s wall; timeout 600s); 36.66`. Oracle: `0 110.61 … VAL:Specified(63)`.
  Reproduced four times at a 120 s timeout [AGENT]: NEW binary ×2 →
  `124 120.10 … TIMEOUT(120s); 33.56` / `… 33.40` (parks after ~33.5 s
  CPU — the same event the 600 s run classifies HANG); OLD binary
  (primary checkout, mainline `f95ef8d9c`, via the loud
  `CERB_LEAN_BIN_OVERRIDE` hook — a runtime probe, not a semantics
  claim) ×2 → `134 39.06 … STACK_OVERFLOW; 37.98` / `134 39.16 …
  STACK_OVERFLOW; 38.08` with `Stack overflow detected. Aborting.` on
  stderr. So: deterministic per binary; the pin bump changed the
  outcome of the SAME process-stack overflow from a clean abort to the
  runtime overflow handler's deadlock (the mechanism VALIDATION.md
  already documents for the >7 M-element aggregates: guard-page SIGSEGV,
  then FUTEX_WAIT inside the handler; upstream report drafted). Both
  outcomes are the recorded resource-limit class (b) — Lean fails where
  the oracle succeeds, TODO.md "Step-runner execution ceiling" — neither
  is agreement, neither is a verdict change. Attribution: the bump
  re-renders the driver loop's recursion (`nd_mapM` folds with
  `lemListFoldr`, F7; the thread-state `Fmap`s are the Pmap port,
  F3/F10), which moves WHICH frame hits the guard page; which way the
  runtime handler then goes (abort vs deadlock) is the handler's known
  position-dependence, not a named lem fix — flagged for the
  orchestrator as the one movement in the whole battery whose
  attribution is mechanism-level rather than fix-level. Registered in
  TODO.md (the ceiling item).

vs the fuel-arc record's rows (lean engines at the 10^8 budget, 6
lean-first rows in common): `same=4` (`b_zero_local_1000000`,
`b_zero_local_10000000`, `d_loop_100000`, `e_memcpy_100000`) and the two
rows discussed above (`d_loop_1000000` surfacing; `e_memcpy_1000000`
note only).

Every oracle-vs-Lean VERDICT agreement in the corpus is unchanged: all
rows where both sides complete agree on the value (a/b/c classes to the
sizes the recorded ceilings allow, d_loop ≤ 10^5, e_memcpy ≤ 10^5,
z_base both modes); the rows where Lean fails are the recorded (b)-class
rows only.

## 6. Moved-row enumeration — the predicted movements vs. what happened

Every committed baseline is UNMOVED: the enumeration of moved rows is
EMPTY. Per predicted class (the brief's (a)-(d)):

- (a) derived-Ord ranking (F4) → `Set.choose`/`topo_order` observables:
  no gate row moved. No row was MATCH→DIFF (the stop condition) and no
  DIFF/ADDR row became MATCH. F4 moved the INSTANCE SHAPE (55 types,
  manifest §2) without moving a gated output on these corpora.
- (b) right-to-left draw order (F6) → Lean-side renumbering: the one
  pin embedding a Lean symbol id, `tests/libxml2/uri_baseline.txt`'s
  C1-F2 row, still reads `Symbol(968, SD_Id("memset"))` — unmoved;
  `test_elab.sh`'s `same=103 diff=3` is the recorded state. No other
  lane observes Lean-side symbol ids (grep of the committed pins for
  `Symbol(`: the uri baseline is the only hit), so whether F6 renumbered
  anything else is NOT observable in this battery; the cross-version
  probe below saw no id-bearing line change either (it prints names).
- (c) division by zero (D1): the 7 generated `lemIntegerDiv` sites all
  divide by the literal 2 and the `lemNatDiv`/`lemNatMod` site's base is
  a digit base; no lane row moved to a Lean panic. The hand-written
  `CerbMem.lean` sites are untouched by the bump (hand-written code is
  not regenerated) — §7.
- (d) `set 'a` = Pset, ascending iteration: no enumerating output moved.

**The one measured movement — outside every gate, TOWARD the oracle
(attributed: F10, map iteration order).** Cross-version probe [AGENT]
(`.tmp/pinbump-logs/ppcore_probe.sh`, measurement only): the OLD Lean
driver (primary checkout, mainline `f95ef8d9c`, stamped `bin fd035977…`,
LemLib `045dcb0`) vs the NEW one, `--pp-core` over all 106
`tests/minimal` cabs-jsons. Verbatim: `PPCORE PROBE: total=106 same=0
diff=106 cabs_skip=0`; every one of the 106 diffs is a LINE PERMUTATION
(`line-permutation-only: 106 / 106`): `ppCoreSignature` (Main.lean:459)
walks `fmapElements coreFile.funs`, whose order was the old rep's
newest-first spine and is now `Pmap.bindings` ascending (pmap.ml:308;
LemLib.lean:1089) — exactly reversed on every file. Against the oracle's
`--pp=core` on the multi-function tests: 016-func-simple / 056-func-ptr /
090-unseq-func-args-ok all print `proc add proc main`; NEW Lean prints
`proc add proc main`; OLD Lean printed `proc main proc add`. No gate
consumes this ordering (test_parse.sh runs `--pp-core` for the exit
class only), so no baseline moved; recorded here as the enumeration's
one row: `pp-core function/tagdef enumeration order: old = reverse of
oracle → new = oracle order; lem fix F10 (Pmap port, ascending
bindings)`.

Nothing else moved; nothing unattributable. The instrument commits the
brief anticipated ("ONE instrument commit per lane family") are not
needed — no baseline file changed in this slice.

## 7. Census rows for the zero-discrepancy arc

**The brief's Z-class row is an erratum, not a bug.** The brief (from
the lem record §5) states that `CerbMem.lean:~1352` `if n2 == 0 then 0
else integerDiv_t n1 n2` "still returns 0 where the oracle raises".
Verified against the oracle source [AGENT], `memory/concrete/impl_mem.ml:2479-2480`
verbatim:

```
      | IntDiv ->
          IV (combine_prov prov1 prov2, Z.(if equal n2 zero then zero else div n1 n2))
```

The oracle has the SAME explicit zero guard returning zero; the Lean line
(now `CerbMem.lean:1358`, comment `impl_mem.ml:2479-2480: explicit zero
guard, then TRUNCATING Z.div`) mirrors it exactly. There is no
divergence at this site; nothing to register as BUG-FIX. Per the house
rule that audit findings are claims to be verified before their remedy
is implemented, the remedy (a census row) is NOT registered; instead:

- ERRATUM (for the lem-lean record `2026-09-03_parity-fix-record.md` §5,
  "Division-by-zero panic sites" paragraph, and its audit-response F4
  note): the `CerbMem.lean:1352` sentence is false — the oracle raises
  nowhere at that site. Also in that paragraph: "`CerbMem.lean` (22)
  now render through `lemNatDiv`/…" is inaccurate — CerbMem.lean is
  hand-written and is not re-rendered by lem; its `/`/`%` sites are
  unchanged by the bump. Filed here for the orchestrator to carry to
  the lem-lean repo (out of this worktree's scope).

**The genuine neighbour row (already recorded, restated for the arc's
census):** `CerbMem.lean:1359-1360` `IntRem_t`/`IntRem_f` render as
`Int.tmod`/`Int.emod`, which return the Lean defaults on a zero divisor,
where impl_mem.ml:2481-2484 `Z.integerRem_t n1 n2` / `Z.integerRem_f`
(zarith `Z.rem` / `Big_int_Z.mod_big_int`) RAISE `Division_by_zero`.
Documented in place (CerbMem.lean:1323-1327): "unreachable behind Core's
division-by-zero UB guards (UB045)… deliberate divergence on an
unreachable input". For the zero-discrepancy census: class = failure-vs-
success on an input the model guards upstream; status = recorded
deliberate divergence; a mover (if the arc wants the site oracle-shaped
regardless of reachability): `if n2 == 0 then failwithI "Division_by_zero"
else …`, mirroring the raise, with a probe that reaches the site only by
bypassing UB045 (none exists in the corpora).

Related, new with this bump and on the trust side (not a divergence):
the 21 generated `lemIntFromInteger` sites now fail loudly outside the
OCaml 63-bit `int` range exactly where the reference raises `Failure
"int_of_big_int"` (lem record §6 N4/X3) — a matched-failure-class
improvement in principle; no corpus input reaches it (no row moved).

## 8. Registered residue (for TODO.md / the next lem slice)

1. `frontend/model/core_linking.lem:61-63`: unused `val set_fold` with
   an OCaml `Pset.fold` rep and a Lean rep pointing at the deleted
   `CerbUtils.set_fold`. Remove all three lines in a slice that may
   touch the `.lem` (expected OCaml rendering delta: none — a
   target_rep'd val emits no definition — but the source hash and the
   fork-drift review must be re-run to say so).
2. Lem record §5 errata (§7 above): the CerbMem:1352 claim; the "22
   CerbMem sites re-render" claim; the impact list's blind spot for
   transitive imports (`Std.Data.TreeMap`, §4) and for proof-term
   dependence on `List.foldr` reduction (§4 #6); the `Utils.default0`
   prediction (manifest §5 — it did not revert to `default`).

## 9. Worktree state at close

Branch `arc/pin-bump`, two commits on top of mainline `f95ef8d9c`:
commit 1 (pin + seams + manifest + record draft + TODO, on Tier A+B
green) and commit 2 (this record's Tier C sections, the committed
mem-scale sweep TSV, the TODO ceiling note). No merge, no push. Working
tree clean apart from the ephemeral `.tmp/` (gitignored): the pre-bump
snapshots, per-row lane logs, the probe scripts and the run
artefacts — deleted at slice end per the container rule once the
orchestrator has re-verified. The shared opam switch carries lem
`3c88f0d` (a machine-visible effect of this slice, sanctioned in the
brief; the primary checkout `cerberus-lean/` on mainline still has
generated trees and binaries from `045dcb0` — its next `make
prelude-src` will regenerate byte-identical OCaml, and its Lean tree
must be regenerated + rebuilt before use).
