# Arc 15 / S1 — R1 "hello template" on division/mod (record)

Date: 2026-08-22. Provenance: [AGENT:arc15-laneA-S1] throughout.
Charter slice: S1 (R1 scalar rung). Grounding: the ratified template
note (`notes/2026-08-22_harness-statement-template.md`), S0 record
(`2026-08-22_arc15-s0-preliminaries.md`). Registers: the S1 entries
in `2026-08-22_arc15-spec-register.md` (S1-E1..E6) and
`2026-08-22_arc15-proof-register.md` (S1-P1..P5).
Worktree `cerberus-lean-spec-lab`, branch `spec-lab`; commits
`2e3481929` (batch 1), `6e4538c62` + `88f6ffa70` (batch 2), batch 3 =
this record + registers + proofs lib.

## Targets (real external code, clean-room deps/cn, BSD-2)

`deps/cn/tests/cn/division.c` and `deps/cn/tests/cn/mod.c`, CN specs
verbatim (also cited in every harness header and DivMod.lean):

```c
int division (int x, int y)
/*@ requires y != 0i32;
    ensures return == x/y; @*/
{
    return x / y;
}

int mod (int x, int y)
/*@ requires y != 0i32;
    ensures return == x % y; @*/
{
    return x % y;
}
```

Model: `Int.tdiv`/`Int.tmod` (C11 6.5.5 truncation/sign semantics).
`Wf` = i32 ranges + `y ≠ 0` + ¬(INT_MIN, -1) — the corner beyond
CN's `requires` (spec register S1-E3; honesty note in DivMod.lean).

## The full pipe, per the template

`modelFn` → S0 codecs extended with the i32/i8 two's-complement
layer (kernel round trips + canonicity) → `mkHarness` templates
(Form 1 / 1b / 2 / kernel-instance Form 1u-i8, all with `choices[]`
+ `expected[]` compiled in, expected computed PURE-SIDE) → the
differential lanes of `scripts/test_speclab_divmod.sh`.

### (a) Differential sweeps — pipeline vs pipeline vs pure model

Edge set: 13 operand values (0, ±1, ±2, ±10, 127, -128, i32 maxima,
-2147483647, 123456789) crossed, Wf-filtered → 155 samples (all
sign boundaries, both maxima, the excluded corner verified filtered).
Summary lines VERBATIM (per-sample logs: sweep transcripts, one line
per sample, all `OK`, e.g.
`[form1] x=0 y=-1 oracle=Specified(0) lean=Specified(0) predict=Specified(0) OK`):

```
SWEEP SUMMARY [form1]: samples=155 red=0
test_speclab_divmod: PASS (--sweep)
SWEEP SUMMARY [form1b]: samples=155 red=0
test_speclab_divmod: PASS (--sweep)
SWEEP SUMMARY [i8]: samples=169 red=0
test_speclab_divmod: PASS (--i8sweep)
```

(i8 sweep = 13² i8 edge pairs on the kernel-instance template,
re-run after the block-scope template change.) Form 2 (libc mode, 8
samples, stdout asserted against the pure `render3` prediction):

```
[form2] x=0 y=1 OK
    O: Defined {value: "Specified(0)", stdout: "000,000,000,000,000,000,000,000,\n", stderr: "", blocked: "false"}
    L: Defined {value: "Specified(0)", stdout: "000,000,000,000,000,000,000,000,\n", stderr: "", blocked: "false"}
    P: stdout="000,000,000,000,000,000,000,000,\n" value=Specified(0)
...
FORM2 SUMMARY: samples=8 red=0
test_speclab_divmod: PASS (--form2)
```

FINDING (first Form 2 run was 8/8 RED with EMPTY stdout on both
pipelines): cerberus libc stdout is line-buffered with no exit
flush; the serialization newline is part of the statement. Register
S1-E1.

### (b) Fuzz + shrink smoke

Deterministic byte-stream fuzz (awk srand, seed 20260822), Form 1,
byte-wise shrinker armed (shrink-to-minimal-program on divergence):

```
FUZZ SUMMARY: requested=200 run=200 invalid_skipped=0 divergences=0 (seed=20260822)
test_speclab_divmod: PASS (--fuzz)
```

0 divergences — the shrinker never fired (its divergence predicate
is exercised only by construction; the plant lane below is the
red-path witness).

### (c) Plant test (wrong operator: division's `/` → `*`)

VERBATIM (`--plant`; predictions are pure-side `plantVerdict`):

```
[plant:form1] x=7 y=2 oracle=Specified(0) lean=Specified(0) predict=Specified(0)
[plant:form1-plant] x=7 y=2 oracle=Specified(1) lean=Specified(1) predict=Specified(1)
[plant:form1-plant] x=-7 y=3 oracle=Specified(1) lean=Specified(1) predict=Specified(1)
[plant:form1b] x=7 y=2 oracle=Specified(0) lean=Specified(0) predict=Specified(0)
[plant:form1b-plant] x=7 y=2 oracle=Specified(1) lean=Specified(1) predict=Specified(1)
[plant:i8] x=7 y=2 oracle=Specified(0) lean=Specified(0) predict=Specified(0)
[plant:i8-plant] x=7 y=2 oracle=Specified(1) lean=Specified(1) predict=Specified(1)
[plant:i8-plant] x=-5 y=3 oracle=Specified(1) lean=Specified(1) predict=Specified(1)
PLANT SUMMARY: all wrong-operator plants RED with predicted indexes; healthy twins green
```

Form 2 plant (the witness-carrying red), VERBATIM:

```
== form2-plant (7,2) oracle ==
Defined {value: "Specified(0)", stdout: "014,000,000,000,001,000,000,000,\n", stderr: "", blocked: "false"}
== healthy prediction ==
003,000,000,000,001,000,000,000,\n
== form2-plant (7,2) lean ==
Defined {value: "Specified(0)", stdout: "014,000,000,000,001,000,000,000,\n", stderr: "", blocked: "false"}
```

Theorem face of the plant: `divmodI8PlantFile` is pinned;
`SpecLabProofs.plantClaim_refuted_of_run` (kernel-checked) turns any
future `HarnessRunsTo divmodI8PlantFile 1` exec fact into
`¬ DivModI8PlantHealthyClaim`; the gate exe already checks the
verdict-1 fact EXECUTABLY (below). The full in-logic refutation
waits on the same parked exec equations as the positive theorems
(proof register S1-P1) — at R1 the "unprovable theorem" leg is:
statement pinned + refutation schema kernel-checked + refuting run
machine-checked executably + red differential demonstrated.

### (d) The pinned statement layer + kernel theorems

Pinned fixtures (tests/speclab/): `divmod_i8_{a,b,d,c,plant}.{c,core}`
= mkHarness i8 instances (7,2)/(-5,3)/(-6,3)/(-128,-1)/plant(7,2);
oracle exec verdicts VERBATIM at pinning: `Specified(0)` ×4,
`Specified(1)` (plant). Term layer: `speclab-emit-core` →
`SpecLab/DivModCore.lean` (parametric `mainParamDecl`, spec register
S1-E5); assembly + statements: `SpecLab/DivModFiles.lean`. Gate exe
VERBATIM (`test_speclab_divmod.sh --gate`):

```
  PASS  drift gate: re-emitted module byte-identical
  PASS  param pin [a]: mainParamDecl == parsed main
  PASS  param pin [b]: mainParamDecl == parsed main
  PASS  param pin [d]: mainParamDecl == parsed main
  PASS  param pin [c]: mainParamDecl == parsed main
  PASS  exec [a] (7,2): Specified(0)
  PASS  exec [b] (-5,3): Specified(0)
  PASS  exec [d] (-6,3): Specified(0)
  PASS  exec [c] (-128,-1): Specified(0)
  PASS  exec [plant]: Specified(1) — the wrong-operator plant is RED in-logic
CoreGateTest: ALL PASSED
```

STATEMENTS (SpecLab lib, statement-TCB-gated; exactly what each
quantifies):

* `DivModI8SampleStatement` — ∀ m in the EXPLICIT 4-element sample
  set {(7,2),(-5,3),(-6,3),(-128,-1)}, every runND outcome of
  `drive (divmodI8FileOf m) … ["cmdname"]` is Active Specified(0).
  FINITE sample quantification, labeled; the i8-family-∀ and full
  i32-family-∀ are the priced follow-on (S1-P1).
* `DivModI8SampleStreamStatement` — the same over the 4 encoded
  STREAMS through the stream-indexed file.
* `DivModI8PlantHealthyClaim` — the claim the plant refutes.

KERNEL-CHECKED at S1 (axiom cones VERBATIM in `SpecLabAudit.lean` +
`proofs/SpecLabProofs.lean` as in-build `#guard_msgs` pins; captured
output, abridged to the S1-new theorems):

```
'SpecLab.DivMod.model_forall_iff_stream_forall' depends on axioms: [propext, Quot.sound]
'SpecLab.DivMod.divmod_reconstruction' depends on axioms: [propext]
'SpecLab.DivMod.modelDiv_inRange' depends on axioms: [propext, Classical.choice, Quot.sound]
'SpecLab.DivMod.fileOfStream_encode' depends on axioms: [propext, Classical.choice, Quot.sound]
'SpecLab.DivMod.sample_model_iff_stream' depends on axioms: [propext, runEffectful, Classical.choice, Quot.sound]
'SpecLab.DivMod.DivModI8SampleStatement' depends on axioms: [propext, runEffectful, Classical.choice, Quot.sound]
'SpecLab.DivModCore.mainParamDecl' does not depend on any axioms
'SpecLabProofs.specifiedInt_injective' depends on axioms: [propext]
'SpecLabProofs.harnessRunsTo_exclusive' depends on axioms: [propext, runEffectful, Classical.choice, Quot.sound]
'SpecLabProofs.plantClaim_refuted_of_run' depends on axioms: [propext, runEffectful, Classical.choice, Quot.sound]
```

(31 further pins in SpecLabAudit.lean, same discipline: classical
trio subsets for the pure layer; + `runEffectful` — the declared
boundary — exactly where a statement quotes the drive substrate; AST
terms axiom-free. No sorryAx, no ofReduce* anywhere.)

The EXEC EQUATIONS at the concrete streams (the walk itself) are
PARKED-PRICED — proof register S1-P1 has the measured grounds and
the resumption route.

## Gates (all in-build / fail-closed, this slice)

* speclab statement-TCB: grep floor (7 files clean) + the NEW
  in-build twin (`SpecLabAudit.lean`): transitive Prop-def constant
  walk banning Iris/RelSem roots, wrapper-hole NEGATIVE TEST
  in-build; axiom-pin machinery plant-tested both directions
  (doctored pin → build FAIL; revert + rebuild → green).
* Drift: emitter re-emission byte-identical; param pins ×4;
  in-Lean exec ×5 (speclab-core-test, `--gate` lane).
* Validation battery at each commit: speclab plain `lake build`
  (capped) green; speclab-test 20/20; `test_speclab.sh`
  --selftest/--plant PASS; `test_unit.sh` `Total: 7 passed, 0
  failed`.

## Parked / follow-ons (priced)

* THE EXEC-EQUATION CAMPAIGN (S1-P1): L, its own slice, after Lane
  B/T5 — upgrades the sample-∀ statements to proved theorems and the
  plant refutation to unconditional; `mainParamDecl` makes family-∀
  the natural endpoint.
* Codec `Canonical` contract in the idiom library (S1-E2): S.
* Form 2 leak-conjunct/teardown observable: R1 has no heap allocs;
  the leak conjunct enters at R3 per charter.
* `.s1-probes/` scratch deleted before the batch-3 commit (S0
  convention).
