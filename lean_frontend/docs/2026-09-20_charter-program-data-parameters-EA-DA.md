# Charter — program-data parameters, E-A + D-A: the enum's compatible type and the translation unit's digest as program data (2026-09-20)

**Status:** CHARTER for one Claude Fable subagent worker, written by the orchestrator [AGENT]; branch `arc/program-data-parameters` in the primed worktree `worktrees/cerberus-lean-arc/program-data-parameters` (base: mainline `e283bed77`, which landed S0.5 — lem pin `4307dc5`, the N-ary `reader_seed` rule; the code files this charter cites are unchanged since `b7e45d55e`, so the S0 record's cites hold). This is the arc's main slice: the two last effect-erased reads — `CerberusImpl.typeof_enum` (an `IO.Ref` registry) and `CerberusFresh.digest` (an `@[extern]` read of a C global) — become READER PARAMETERS carried as program data, and the two seams, their allowlist rows, the effect-erasure invariant page and the one kept `panic!` retire. Inputs: the design note `2026-09-18_program-data-parameters-design-note.md` (§3 routes, §8 decisions), the S0 record `2026-09-19_program-data-parameters-S0-record.md` (§1.6 rules 1–7, §2 inventory, §3 the digest rule, §4 errata E1–E11, §5 Q-A/Q-C–Q-F), the S0.5 record `2026-09-19_program-data-parameters-S0.5-record.md` §10 and the S0.5 audit's N1. ONE slice, TWO phases (E-A then D-A), a commit and an orchestrator boundary after each; one record; ONE consumer re-pin note at the end.

## 0. Rulings (verbatim), decisions, standing constraints

- [USER 2026-09-19]: *"I'm interested in making decisions that are consequential in some way but for the other kinds of decisions, which are really more implementation-focused, I think you can make the calls."* Q2: *"yeah, I agree on Q2, let's roll it together"* — E-A and D-A are one arc, one consumer re-pin.
- [USER 2026-09-04] (the supply-threading precedent, cited in the S0 record §1.6): *"we don't change the lem structure for ocaml"* — accepted exceptions are parameters DEAD on the OCaml target (the supply, the N-ary seed's leading parameters) and shared bodies both targets compute identically.
- [USER 2026-09-16]: *"cerberus-sl is our main upstream customer at the moment."* The consumer note at the end of this slice is for them; the operator relays it.
- [USER 2026-09-11]: Fable-class workers; the orchestrator re-verifies every gate. [USER 2026-09-08] verbatim: *"we should \*NOT\* be building anything new out-of-policy"* — no new enumeration/literal/semantics-evaluation proofs; the kernel tests below are the design note §4's named ones.
- Design decisions in this charter, all [AGENT] under the delegation and FLAGGED to the operator in the launch message (§0 of the record restates them with provenance):
  - **D1 — where the enum map lives.** `A.sigma` gains `enum_definitions : list (Symbol.sym * Ctype.integerType)` (`frontend/model/ail/ailSyntax.lem:307-322`, beside `tag_definitions`), and the Core `file` gains `enumDefs : map Symbol.sym Ctype.integerType` beside `tagDefs` (`core.lem:417-426`) — the run needs the map from the LINKED program and the consumer's `Program` is `file × supply` (`cerberus-sl Lang.lean:74-83`), so the file must carry it. The Core TEXT printer (`ocaml_frontend/pprinters/pp_core.ml:748`) is NOT changed and `CoreParser` defaults `enumDefs := fmapEmpty` (`CoreParser.lean:2329,2660,2986` and the `pCoreFile` result): this MIRRORS the oracle, whose `.core` input and `read_core_object` paths (`pipeline.ml:666`) leave the registry empty. Consequence, an ERRATUM to the design note §3.1 "OCaml — one tiny target_rep, NOTHING else": every non-generated OCaml site constructing a FULL `Core.file` record literal must gain the dead field (compiler-forced; in the driver build graph: `ocaml_frontend/rewriters/core_peval.ml:865`, `ocaml_frontend/rewriters/remove_unspecs.ml:106`, `backend/common/pipeline.ml:288,517`, `backend/ocaml/runtime/rt_ocaml.ml:348`, `backend/ocaml/driver/core_opt.ml:58,77`, `backend/ocaml/driver/main.ml:75`; outside it, only if `dune build` compiles them: `backend/bmc/bmc_utils.ml:413`, `backend/web/instance.ml:334`, `backend/absint/src/cfg.ml:1025`) — one line each, `enumDefs = Pmap.empty …` or `= file.enumDefs`, dead on OCaml (the oracle reads its registry). No non-generated OCaml constructs a full `A.sigma` literal (grep: none). In Lean, the full-file literals gain the field: `Main.lean:833-840`, `test/Unit/FuelExemplar.lean:120`, `speclab/SpecLab/{CnSeed,DivMod,ByteArr(×2),TreeRot,ListAppend}Files.lean`.
  - **D2 — the layout functions (S0 record Q-D): normalise FIRST, one consumer.** `Implementation.normalise_integerType` becomes the ONE `reader_consumer` of the enum map (`declare {lean} reader_consumer val normalise_integerType`; its Lean rep `CerberusImpl.normalise_integerType` takes the three readers in sorted order — `(digest : String) (enumDefs : Fmap sym integerType) (tagDefs : …)` — and its `.Enum0 tag_sym` arm is `lookup tag_sym enumDefs`, a `failwithI` with the OCaml text on a miss). The four layout vals become SHARED one-line lem wrappers over renamed reps: `let sizeof_ity ity = sizeof_ity_norm (normalise_integerType ity)` (same for `alignof_ity`, `is_signed_ity`, `precision_ity`), where `val sizeof_ity_norm` carries today's target_reps — OCaml `(Ocaml_implementation.get()).sizeof_ity` UNCHANGED (its own normalisation re-runs on an already-normalised type: idempotent — `Common.normalise_integerType_` `ocaml_implementation.ml:37-66` maps `Signed/Unsigned` through the alias table (identity on a resolved type), `Enum` through the registry (absent after the first pass), the four named types to `Signed/Unsigned` (fixed points)), Lean `CerberusImpl.sizeof_ity` etc. with their `.Enum0`/`typeof_enum` paths (`CerberusImpl.lean:84-92,150-160,169-175,186-190`) replaced by a `failwithI` leaf "un-normalised Enum reached the layout rep" (UNREACHABLE-BY-INVARIANT: the wrapper normalises first — state the lemma `normalise_integerType d e t ity ≠ .Enum0 _` if it is cheap). WHY: one consumer instead of five; the consumer's 33 uses of `CerberusImpl.sizeof_ity/is_signed_ity/alignof_ity` as functions of the type alone (`cerberus-sl Repr.lean:233-289` and elsewhere) keep their shape; the OCaml oracle is untouched; the enum→type step is exactly one kernel-visible place. `max_ival`/`min_ival` (`mem.lem:283-284,297-300`, reps `CerbMem.maxIval/minIval` `CerbMem.lean:1424,1447`) call `typeof_enum` today: declare them `reader_consumer` too (their lem callers `core_eval.lem:30-31,69-70,101-102,635,643` lift — most already are), and the reps normalise through `CerberusImpl.normalise_integerType d e t`. `typeof_enum` keeps a SHARED lem body (`match Map.lookup s (enum_definitions ()) with Just ity -> ity | Nothing -> failwith "Ocaml_implementation.typeof_enum: '…' was not registered"`) — no lem caller exists (`ailTypesAux.lem:798-802` is commented out), it stays as the named lookup. `register_enum` keeps its OCaml rep (the registry write, `ocaml_implementation.ml:130-142`); its Lean rep becomes the pure `fun _ _ => true` (the duplicate case is caught by the `Map.member tag_sym st.tag_definitions` check just before, `cabs_to_ail_effect.lem:1778-1783`, so both engines take the same branch).
  - **D3 — desugar-time reads see the registry-so-far.** On OCaml the registry is filled as enums are registered (`cabs_to_ail_effect.lem:1787-1794`) and read by whatever the desugarer normalises before the sigma exists. A reader is ONE value for a whole lifted computation, so `desugar`'s enum reader cannot be "the enums registered so far". Rule: every desugar-time path from `cabs_to_ail.lem`/`cabs_to_ail_effect.lem` into `normalise_integerType`/the layout wrappers/`max_ival`/`min_ival` is seeded from the DESUGAR STATE at the `desugM` site that reaches it — `enum_defs_of_state st` = the map `Enum_definition ns ↦ enum_compatible_type ns` over `st.tag_definitions` (the S0 record §5 Q-A, generalised from the mini-run to every such site) — through an N-ary `reader_seed` def whose leading parameters are the seeds in sorted order (`digest`, `enum_defs`, `tds`: the digest seed is the caller's `Symbol.digest ()`, the tds seed is the enclosing binder's value `Ctype_aux.tagDefs ()`; OCaml-dead, the S0.5 precedent). E-A1 below INVENTORIES the reaching sites first (static + dynamic); if none exists beyond the mini-run (`mini_pipeline.lem:70-78,185-187`), D3 reduces to Q-A — record which. The typing entry (`annotate_program`, `genTyping.lem:25-30` reads `precision_ity`/`is_signed_ity`) and `translate` receive the COMPLETE per-TU map derived from the sigma (`Map.fromList sigm.A.enum_definitions`); the run receives the LINKED map `runFile.enumDefs` (`core_linking.lem:294` merges it as `tagDefs`: `f1.enumDefs union f2.enumDefs`). The Lean run map therefore equals what today's process-global Lean registry holds after frontending the same TUs (metadata TUs included in libc mode) — zero movement on every lane by construction.
  - **D4 — the digest (D-A).** `symbol.lem:44-46`: `declare {lean} reader val digest` (one line; the OCaml rep `Cerb_fresh.digest` stays). `Main.lean`: `frontendTU` gains `(digest : String)` and passes it as the FIRST reader to every lifted entry (`desugar` `:521`, `annotate_program` `:533` once it lifts, `translate` `:563`); the `forceIO` barriers `:520,533,562` and the `setDigestIO` sets `:616,637,961` are DELETED (their only purpose was the digest global — `:496-499`); the run's value = the LAST program TU's digest (S0 record §3, pinned: `tunits` in command-line order; libc dump/metadata digests are set earlier and never win) passed to `initial_driver_state` (`:1025`, which LIFTS — E11) and to `drive`/`CerbCall.driveCall` (`:1037,1039`); `CerbCall.lean:156` uses the binder; the libc dump's `md5Hex dumpContent` (`:616`) no longer seeds anything — `CoreParser.mkSym`'s `""` digest (`CoreParser.lean:242-243`, Q-E) is untouched, `md5Hex` STAYS (a tested pure extern, `FreshIntTest.lean:72-105`; its OPAQUE_WANT/allowlist rows stay); `CerberusFresh.lean:60-71,73-121,123-167`: delete `digestIO`, `setDigestIO`, `digestPure`, `digest_impl`, `digest`, `forceThunkIO`, `forceIO_impl`, `forceIO`; `native/md5.c:149-170`: delete `cerb_digest`, `cerb_digest_get/set`, `cerb_force_thunk` (keep `cerb_md5_hex`), then `make lean-native-obj`; `test/Unit/FreshIntTest.lean:113-145` `testDigestGlobal` → the kernel-visibility test (D4-T below).
  - **D5 — order of readers everywhere:** `digest`, `enum_definitions`, `tagDefs` (S0 record §1.6 rule 1: sorted by NAME; erratum E1 to the design note). Name the lem val `enum_definitions` exactly.
- Standing constraints: NEVER modify machine-global state (no opam changes; `_opam` is the SHARED switch); NO `git push`, ever; never commit on `mdd/cerberus-lean`, `master`, or lem-lean's mainlines; no lem-lean change (a backend refusal you cannot satisfy in the `.lem` is stop rule S4); no merge/rebase; fail-closed/fail-noisy (no `2>/dev/null` on a build or gate step); [AGENT]/[USER] provenance; quoted outputs verbatim, derived tallies labelled; every `lake`/`lean` through `scripts/capped`; gate scripts through `scripts/ce`; ONE heavy job at a time; the ~45-minute step tripwire (the full Lean rebuilds after regeneration and the Tier B battery at the end are EXPECTED real content — report wall times; past ~1 h STOP and report); never end the turn on a wait; a committed phase record ENDS the phase — Phase 2 starts only on the orchestrator's re-launch.

## 1. Facts (verified by the orchestrator at `e283bed77`; cite, do not re-derive; errata to the record)

- **Readers today:** ONE — `ctype_aux.lem:36` `tagDefs`. `reader_seed`: `mini_pipeline.lem:78` `run_const_expr_driver tds dr_st` (`:70-77`; caller `:185-187` passes `Translation.translate_tag_definitions sigm.A.tag_definitions`). `reader_consumer`: SIXTEEN in `mem.lem` — `:96 allocate_object`, `:97 load`, `:98 store`, `:163 diff_ptrval`, `:176 validForDeref_ptrval`, `:182 isWellAligned_ptrval`, `:213 array_shift_ptrval`, `:217 member_shift_ptrval`, `:225 eff_array_shift_ptrval`, `:229 eff_member_shift_ptrval`, `:239 memcpy`, `:243 memcmp`, `:247 realloc`, `:306 offsetof_ival`, `:310 sizeof_ival`, `:327 alignof_ival` — each `CerbMem.lean` implementation gains two leading parameters (rule 2/3: a stub takes ALL readers, sorted). `supply`: `symbol.lem:242` `fresh_int`. The backend rules: S0 record §1.6 rules 1–7 (binder order `[LemFuel]`, `[Inhabited]`, readers, supply, own args; consumer stubs identifier-form; seeds' callers pass VALUES; non-lifted positions fail-closed; no `_lemReader_*` names; no two readers with one unqualified name) + the S0.5 audit N1: a seed def with ≥ N parameters silently takes its own leading parameter as the N-th seed — every seed def gets a per-seed VALUE pin.
- **Lean reader-passing sites today** (S0 record §2(b)): `Main.lean:521` (`desugar fmapEmpty …`), `:563` (`translate fmapEmpty …`), `:1025` (`initial_driver_state supply addressSpaceTop runFile fsState` — lifts under D-A), `:1037` (`drive runFile.tagDefs …`), `:1039` (`CerbCall.driveCall runFile.tagDefs …`); `CerbCall.lean:258,261,262,267,274,290,292,302,305,316,318` (+ the digest read `:156`); `test/Unit/FuelExemplar.lean` 17 sites (`:146,318,364,391,425,428,443,455,473,499,504,520,546,548,550,580,584`); `test/Unit/MonadicFailstop.lean:41,43`; `speclab/test/SLUnit/{ByteArrGateTest:47, CoreGateTest:45, ListGateTest:55, SeedGateTest:46, TreeGateTest:53}.lean`; `tests/immaculate/illtyped-store.lean:49`. New under this slice: `Main.lean:533` (`annotate_program`, if it lifts — D3 says it will), every `CerbMem` internal call that reaches `maxIval`/`minIval`/`normalise_integerType` (e.g. `CerbMem.lean:2786-2787`), and whatever else the regenerated tree's binders demand.
- **The enum seam:** `CerberusImpl.lean:55` `initialize enumRegistryRef`, `:57-59` `symKeyEq`, `:61-70` `typeof_enum_impl` (the `panic!` at `:69` — the ONE kept panic after seam hygiene, `failure_reach_register.txt` 0 panic rows / `observations.py:76-81` `IMMACULATE_PANICS` = exactly this origin / `test_observations.py:333-345` its plant), `:73-75` `opaque typeof_enum`, `:84-92` `is_signed_ity` (normalises `.Enum0` itself), `:150-160` `normalise_integerType` (`.Enum0 tag_sym => typeof_enum tag_sym` `:153`), `:169` `sizeof_ity` (via `normalise_integerType`), `:186` `precision_ity`, `:209` `alignof_ity := sizeof_ity`, `:267-285` `register_enum_impl`/`opaque register_enum`. `CerbMem.lean:1411-1458` `maxIval`/`minIval` (`typeof_enum` at `:1424,:1447`; the "Enum after typeof_enum" leaves `:1437,:1458` = register rows 103/105). OCaml: `ocaml_implementation.ml:125-150` (`DefaultImpl.registered_enums`, `register_enum` GCC rule `:130-136`, `typeof_enum` `:144-150` with the failwith text), the record fields `.mli:27-28`, `MorelloImpl`/`HafniumImpl` reuse `DefaultImpl.register_enum/typeof_enum` (`:317-318,392-393`), `get ()` `:413-424`; `Common.normalise_integerType_` `:37-66`. Lem: `implementation.lem:12-14` the three vals, `:30-35` their reps; `cabs_to_ail_effect.lem:1775-1800` `register_tag_definition` (the `register_enum` call `:1790`), `:2522-2548` `mk_current_ail_sigma` (the `Enum_definition _ -> acc` arm `:2538-2539`); `translation.lem:4245` (`core_tagDefs = translate_tag_definitions sigm.A.tag_definitions`), `:4543`, `:4578`; `core_linking.lem:294`; `mini_pipeline.lem:42` (a full `A.sigma` literal in lem — gains the field). Lem callers of the layout vals: 35 sites in `defacto_memory_aux.lem`, `defacto_memory.lem`, `ail/ailTypesAux.lem` (`:28` the alias `let is_signed_ity = Implementation.is_signed_ity`, `:294-295,792-796` `normalise_integerType`), `ail/genTyping.lem:25-30`.
- **The digest seam:** S0 record §2(c) (15 lem-generated reads: `symbol.lem:249,253,257,262,278,281`, `cabs_to_ail_effect.lem:657-677`, `translation.lem:942,4435`; `CerbCall.lean:156`), §3 (the rule and what is observable — nothing prints a digest; the value is behaviour only through `symbol_compare`, Z-28 libc-mode lanes), `CerberusFresh.lean` (`:57-58 md5Hex` KEEP; `:64-65 digestIO`, `:70-71 setDigestIO`, `:110-121 digestPure/digest_impl/digest`, `:154-167 forceThunkIO/forceIO_impl/forceIO` — DELETE), `Main.lean:487-570` `frontendTU`, `:605-645` `loadLibc` (dump `:616`, metadata TUs `:637`), `:946-964` the program TU loop (`:961`), `:1018-1042` the run entry; `FreshIntTest.lean:113-145`; `native/md5.c:131-170`.
- **Registers and gates that move:** `scripts/unsafebaseio_allowlist.txt` KEEP rows `:32-33` (enum) and PIN rows `:121-123,125-131` (digest, 10) + `:132-137` (enum, 6) retire; `:124 md5Hex` stays; `scripts/check_theorem_axioms.sh:205-233` `OPAQUE_WANT` 12 → 4 (`md5Hex`, `bounded_integer`, `fuelExhaustedLoc`, `modelFailStopLoc`; trail comment updated); `scripts/check_exec_purity.sh:33-44` header comments; `scripts/failure_reach_register.txt` rows 103/105 (text moves), 130-132 (may move), NEW rows for the new `failwithI` leaves (the `normalise_integerType` lookup miss — REACHABLE, mirrors `ocaml_implementation.ml:146-149` on Core-text inputs; the `*_norm` reps' un-normalised-Enum leaves — UNREACHABLE-BY-INVARIANT), resealed by the gate's documented recipe (`check_failure_reach.sh --emit` then review; never a blind re-record); `scripts/observations.py:76-81,221` `IMMACULATE_PANICS` → no kept origin (the panic is gone; keep the mechanism fail-closed: an empty set, the branch intact) and `scripts/test_observations.py:333-345` plants adjusted (an unreviewed panic under immaculate must STILL be rejected); `scripts/fork_drift_manifest.txt`: layer-2 rows MOVE for `cabs_to_ail_effect.ml` (`:429`), `mini_pipeline.ml` (`:434`), `translation.ml` (`:440`), `ailSyntax.ml` (`:443`, expected-cosmetic → reclassify to expected-semantic: a field), `core.ml` (`:446`, same); NEW rows for `implementation.ml` and `core_linking.ml` (today byte-identical to upstream's generated tree — confirm before claiming); `[files]` (layer 1) gains every non-generated OCaml file newly touched (the D1 literal sites, `ocaml_implementation.ml/.mli`) with the review classification the manifest header prescribes; ALL by single-row edits + one dated NOTE (the S0.5 practice; never `--refresh`). `lean_frontend/handwritten_copy.manifest`: no new hand-written module unless a test helper is added (then list it). `lakefile.toml` `[[lean_exe]]` + `scripts/test_unit.sh:25-38`'s list: register any new unit test.
- **Zero movement is the expectation everywhere:** the OCaml oracle's behaviour is unchanged (dead fields, an idempotent second normalisation, a registry→map function nobody's output depends on); the Lean values are identical by construction (the same GCC rule, the same lookup, the same digest per TU). Tier B row 10 (pristine vs fork, 855 cases) is unchanged by construction; row 4 (`test_verify.sh` pin provenance) is unchanged because the Core text is not changed. ANY lane movement is a finding (stop rule S2), never a re-baseline.
- **The consumer (cerberus-sl @ `035f12c`, read 2026-09-20; derived counts):** 498 `.tagDefs`-passing sites across 30 files under `CerberusIris/`; 33 uses of `CerberusImpl.{sizeof_ity,is_signed_ity,alignof_ity}` as functions of the type (kept stable by D2); `Program := ⟨file, supply⟩` (`Lang.lean:74-83`, unchanged by D1); three full Core-file literals `CertP.lean:67,77,89` (gain `enumDefs := F.enumDefs`); `SymFresh.lean:70-75` states the minting-digest property as UNPROVABLE on their pin — provable by `rfl` on the reader binder after D-A; prose mentions of `CerberusFresh.digest`/`set_digest` (`HeapNeg.lean:34,135`, `SymFresh.lean:72-73`) become stale.

## 2. Deliverables

### Phase 1 — E-A (the enum map) + the D-A lem line + every signature ripple (both readers at once)

**E-A1 — The reach inventory (first; a record section, not a separate slice).** (a) Static: add the two declares (`declare {lean} reader val enum_definitions`, `declare {lean} reader val digest`) and the D2 consumer declares, regenerate (`scripts/ce make prelude-src lean-prelude-src`), and READ the generated tree: which defs gained binders that did not have `tagDefs`' — in particular every `Cabs_to_ail*.lean` def, `GenTyping.annotate_program`, `Translation.translate`, `Driver.initial_driver_state`; list them with the reason (the consumer they reach). (b) Dynamic: with the desugar entry seeded by `fmapEmpty` for `enum_definitions`, run Tier A rows 2–4b and `tests/immaculate` (`test_immaculate.sh`) — every lookup-miss `failwithI` names the tag and the site; that is the list of desugar-time reads D3 must seed. (c) Decide per site: seed from the desugar state (D3) or, if the reaching computation is the mini-run only, Q-A alone. Write E-A1's table into the record BEFORE building further; if a reaching site cannot be expressed as an N-ary seed (stop rule S1), STOP.

**E-A2 — lem.** `implementation.lem`: `val enum_compatible_type: list integer -> Ctype.integerType` (GCC's rule, shared body, cite `ocaml_implementation.ml:130-136`); `val enum_definitions: unit -> map Symbol.sym Ctype.integerType` + `declare {lean} reader val enum_definitions` + OCaml rep `Ocaml_implementation.enum_definitions`; `typeof_enum` shared body over `enum_definitions ()` (OCaml failure text preserved); `register_enum` Lean rep → `CerberusImpl.register_enum` as a plain `def … := true` (OCaml rep unchanged); `normalise_integerType` → `declare {lean} reader_consumer`; the four layout wrappers over `*_norm` reps (D2). `ail/ailSyntax.lem` sigma field; `cabs_to_ail_effect.lem:2538-2539` the arm → `(ident, enum_compatible_type ns)` into `A.enum_definitions` (also `:594`/`mini_pipeline.lem:42` literals); `core.lem` file field; `translation.lem:4245,4543,4578` carry `Map.fromList sigm.A.enum_definitions`; `core_linking.lem:294` merge; `mini_pipeline.lem:70-78,185-187`: `run_const_expr_driver dg en tds dr_st` (N-ary seed; caller passes `(Symbol.digest ()) (enum_defs_of_state st) (Translation.translate_tag_definitions …)`) — Q-A/Q-C; `mem.lem:283-284` `max_ival`/`min_ival` → `reader_consumer`; `symbol.lem:44-46` the digest declare; the D3 seeds at the E-A1 sites. NO other lem change; no Core-text grammar change.

**E-A3 — OCaml.** `ocaml_implementation.ml`: `let enum_definitions () = List.fold_left (fun m (s, ity) -> Pmap.add s ity m) (Pmap.empty Symbol.symbol_compare) DefaultImpl.registered_enums` (module level; `.mli` exports it; `DefaultImpl.registered_enums` is what Morello/Hafnium share too — verify `:317-318,392-393`); the D1 dead-field literals, compiler-forced only. Build: `scripts/ce opam exec --switch=. -- dune build backend/driver/main.exe cerberus-lib.install cerberus.install` (or `scripts/common.sh build_cerberus`).

**E-A4 — Lean seams.** `CerberusImpl.lean`: delete `enumRegistryRef`/`symKeyEq`/`typeof_enum_impl`/`typeof_enum`/`register_enum_impl`/`initialize`; `register_enum` plain; `normalise_integerType (digest) (enumDefs) (tagDefs) : integerType → integerType` (the `.Enum0` arm = `lookup` + `failwithI` with the OCaml text; every other arm unchanged); `sizeof_ity`/`is_signed_ity`/`precision_ity`/`alignof_ity` on normalised types (their `.Enum0` paths → the UNREACHABLE leaf); header prose re-written to describe what IS (shop-window doctrine). `CerbMem.lean`: the 16 consumer stubs + `maxIval`/`minIval` gain `(digest : String) (enumDefs : Fmap sym integerType)` leading parameters and thread `enumDefs` to every internal normalisation; `:1411-1458` prose updated. `Main.lean`, `CerbCall.lean`: +2 arguments at every reader site (§1) — for Phase 1 the digest VALUE passed is a placeholder read of the still-existing `CerberusFresh.digest ()` at each site (so Phase 1 is behaviour-identical and Tier A can certify it alone); `frontendTU` derives and passes the sigma map to typing/translation; `runFile.enumDefs` at the run. Tests/speclab/immaculate: +2 arguments at every §1 site (the tests pass `""`/`fmapEmpty` where they passed `fmapEmpty`; the exemplar theorem `FuelExemplar.lean:146` gains two binders — its statement stays `∀ fuel top`). `handwritten_copy.manifest` untouched unless a file is added.

**E-A5 — Kernel tests (design note §4; NOT new artefact surface — they are the named checks).** `test/Unit/EnumDataTest.lean` (`[[lean_exe]] enum-data-test`, listed in `test_unit.sh`): `CerberusImpl.normalise_integerType d e t (.Enum0 s) = .Unsigned .Int_` by `rfl` given `e = fmapFromList [(s, .Unsigned .Int_)]`; `Implementation.sizeof_ity d e t (.Enum0 s) = some 4` and `alignof_ity … = some 4` by `rfl`/`decide`; `enum_compatible_type [0, 1] = .Unsigned .Int_` and `enum_compatible_type [-1, 1] = .Signed .Int_` by `rfl`; `register_enum s ns = true` by `rfl`; a NEGATIVE control by `#guard_msgs`: the old `typeof_enum`/`enumRegistryRef` names no longer elaborate. The differential witness is the immaculate lane's `s9-enum-*.c` cases and the exec baselines (zero movement).

**E-A6 — Registers and gates (Phase 1 part).** Allowlist −2 KEEP −6 PIN (enum); `OPAQUE_WANT` 12 → 10; failure-reach register resealed; `observations.py`/`test_observations.py` (the kept panic is gone); `check_exec_purity.sh` comment; fork-drift manifest rows (single-row + NOTE). Tier A green. **Commit ONCE** (all of Phase 1), quote the head, STOP and report (the orchestrator re-verifies, then re-launches).

### Phase 2 — D-A (the digest value) on the orchestrator's re-launch

**D-A1 — Main/CerbCall plumbing (D4):** `frontendTU (digest : String)` per TU; the placeholders of E-A4 become the real per-TU values; `lastDigest` through the TU loop → `initial_driver_state` (lifted) and `drive`/`driveCall`; libc metadata TUs seeded from their cabs-json digests; `:616` `setDigestIO` deleted (keep a provenance `say` of `md5Hex dumpContent` if you like — it is informational); the `forceIO` barriers and every `setDigestIO` deleted; `CerbCall.lean:156` the binder.

**D-A2 — Deletions:** `CerberusFresh.lean` (D4 list; `md5Hex`, `digest_compare`, `string_of_digest` stay), `native/md5.c` (keep `cerb_md5_hex`), `make lean-native-obj`; `FreshIntTest.lean:113-145` → **D4-T:** `(fresh_given_int d n).1 = d`-shaped kernel pin on the generated `Symbol.fresh_given_int` (by `rfl` on the binder), the entry-shape pin (the digest a cabs-json carries is 32 lowercase hex digits and `≠ ""`, checked on a committed fixture), and a pure `runDigest : List (String × translation_unit) → String` helper in `Main.lean` with the pin `runDigest tunits = (tunits.getLast h).1` by `rfl`/`simp` — the executable statement of the S0 record §3 rule; the run entry USES `runDigest`.

**D-A3 — Registers, gates, docs:** allowlist −10 PIN (digest); `OPAQUE_WANT` 10 → 4; `check_exec_purity.sh` comments; `VALIDATION.md:1023-1026` the digest-boundary bullet → "LEFT the boundary 2026-09-20 (program-data parameters E-A/D-A): the digest is a reader parameter; `md5Hex` remains the one pure extern"; `docs/2026-08-22_arc14-effect-erasure-invariant.md` gains a top header "RETIRED 2026-09-20 — both seams are reader parameters (E-A/D-A record); this page is history"; `lean_frontend/CLAUDE.md` key-files rows (`CerberusImpl.lean`, `CerbMem.lean`, `CerberusFresh.lean`, `Main.lean`) describe the system as it IS; `TODO.md` closes the corresponding items; the design note is NOT edited (errata live in the record).

**D-A4 — Gates:** Tier A; then the Tier B battery via `scripts/ce python3 scripts/release.py --mode full` (~80 min; pre-authorised real content — report the wall time; if a single lane exceeds ~1 h on its own, report) including row 10 (pristine, unchanged), row 12, `test_verify.sh`, `test_immaculate.sh`, the speclab gates, `test_gcc_oracle.sh --check-baseline`. Zero movement everywhere.

**D-A5 — The record** `lean_frontend/docs/2026-09-20_program-data-parameters-EA-DA-record.md`: §0 decisions D1–D5 restated with provenance; E-A1's reach table; every file touched by phase with the reason; the kernel tests' verbatim output; both phases' gate tails verbatim; the register/manifest movements (before/after counts: allowlist rows, OPAQUE_WANT 12 → 4, register rows, manifest rows); the fork-drift NOTEs' text; errata to the design note / S0 record (D1's OCaml literal sites; anything else found); the **consumer re-pin note** for cerberus-sl (the exact signature changes: two leading arguments `digest : String`, `enum_definitions : Fmap sym integerType` before `tagDefs` on every reader-taking signature; `initial_driver_state`, `desugar`, `translate`, `annotate_program` newly reader-taking; `Program` unchanged; `file.enumDefs` new; `CertP` literals; `CerberusImpl.*_ity` signatures unchanged; `CerberusFresh.digest` gone — their `SymFresh` property now `rfl`; the S0.5 audit N1 note on seeds). **Commit ONCE** (Phase 2), quote the head, STOP.

## 3. Fence, forbidden, stop rules

**Fence (may edit):** lem `frontend/model/{implementation,symbol,mini_pipeline,cabs_to_ail_effect,cabs_to_ail,core,core_linking,translation}.lem`, `frontend/model/ail/ailSyntax.lem`, and ONLY for D3 seeds `frontend/model/ail/ailTypesAux.lem`; OCaml `ocaml_frontend/ocaml_implementation.{ml,mli}` and the D1 literal sites (compiler-forced; a site outside the D1 list is stop rule S3); Lean hand-written `CerberusImpl.lean`, `CerbMem.lean`, `CerberusFresh.lean`, `Main.lean`, `CerbCall.lean`, `CoreParser.lean` (ONLY the `enumDefs := fmapEmpty` defaults), `native/md5.c`, `test/Unit/*`, `speclab/test/SLUnit/*`, `speclab/SpecLab/*Files.lean` (the literals), `tests/immaculate/illtyped-store.lean`, `lakefile.toml` (`[[lean_exe]]` only), `handwritten_copy.manifest` (only if a file is added); scripts/registers: `unsafebaseio_allowlist.txt`, `check_theorem_axioms.sh` (the `OPAQUE_WANT` block + its trail), `check_exec_purity.sh` (comments), `failure_reach_register.txt` (via the recipe), `observations.py` + `test_observations.py`, `fork_drift_manifest.txt` (single rows + NOTE), `test_unit.sh` (the exe list); docs: the record, `VALIDATION.md` (the one bullet), the effect-erasure page header, `lean_frontend/CLAUDE.md` rows, `TODO.md`. **Forbidden:** `pp_core.ml` or any Core-text grammar change; ANY baseline/expectation file (`scripts/exec_*_baseline.txt`, `tests/**/baseline.txt`, `expectations.txt`, `corpus/`, fixture pins — movement is a finding, S2); `.gitignore`; lem-lean / `deps/lem-pinned` / the Lake pin files; `CerbGlobal`, the allocator, the address-space parameter; any semantics change beyond the data threading; `2>/dev/null` on builds/gates; commits on any mainline; `git push`; uncapped Lean; more than one heavy job; a wholesale manifest `--refresh`.

**Stop rules:** **S1** E-A1 finds a desugar-time read that an N-ary `reader_seed` cannot express (report the shape and the site); **S2** ANY lane movement — a MISMATCH/DIFF, a baseline delta, a pristine-lane class change, a `test_verify` pin failure (report verbatim; do not re-baseline); **S3** an OCaml compile error at a site outside the D1 list + `ocaml_implementation.{ml,mli}` (report the list; wait for the orchestrator's fence ruling); **S4** a lem generation refusal from the reader machinery that needs a lem-lean change (RC-rel scope in an indreln/assert, an instance, a reserved name you cannot rename around); **S5** a step past ~45 minutes (the regeneration-triggered full rebuilds and the Tier B battery excepted; wall times reported; any single step past ~1 h reported); **S6** a rule conflict; **S7** each phase ends at its commit — Phase 2 waits for the re-launch.

## 4. Gates

**Phase 1:** `scripts/ce make prelude-src lean-prelude-src` (+ `make lean-native-obj` if `native/` moved); `scripts/common.sh build_cerberus` + `build_lean` (`DUNE_CACHE=disabled`); Tier A rows 1–12 incl. 4b/4c/6b (`scripts/LADDER.md`), every tail verbatim; `test_unit.sh` incl. the new `enum-data-test`; the register/manifest gates green with the reviewed counts; `git status` clean after the commit. **Phase 2:** the same, plus the Tier B battery (`release.py --mode full`: rows 1–12, `test_upstream_oracle.py` `passed; {'semantic_agreement': 822, 'matching_failure': 28, 'reviewed_difference': 3, 'interface_agreement': 2}` unchanged, chvalid 4/4), `check_theorem_axioms.sh` population 4, allowlist row counts as §1 predicts, `check_failure_reach.sh` OK with the resealed register, `#print axioms` on the kernel tests = the trio or fewer.

## 5. Record and reporting

Interim status pings get an immediate reply. Each phase's report to the orchestrator: the head hash, every gate tail verbatim, the register/manifest movements (before → after), the E-A1 reach table (Phase 1), deviations from this charter with reasons, and — Phase 2 — the consumer note's final text. Errata to this charter go into the record. Then stop.
