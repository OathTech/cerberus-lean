# Pre-merge audit — `arc/pristine-oracle-instrument` (WP-O, 2026-09-17)

**Range:** `0c78635cb..586b550b8` — seven commits (`1770e112d` charter; `0ce67f2b6` O3; `3debab120` O1; `57a0e4ee0` O2; `664f9d0a7` O4; `419c572a1` record; `9cbd9b86f` O5; `586b550b8` record amendment). Base = mainline `mdd/cerberus-lean` `0c78635cb` (the mainline has since moved to `6da619f69`; not rebased here — the orchestrator's rebase/re-gate rule applies).
**Head audited:** `586b550b8`, checked out as branch `audit/pristine-oracle-instrument` in `worktrees/cerberus-lean-audit/pristine-oracle-instrument` (created by the container hook under audit — its first end-to-end exercise; log quoted in §2.4).
**Who:** an INDEPENDENT pre-merge auditor (Claude, Fable 5.1) — [AGENT auditor] throughout. I wrote none of the range. Every claim marked *reproduced* is my own run in this worktree; quoted outputs are verbatim; tallies are labelled derived. The two operator rulings the range implements — [USER 2026-09-17] *"(1) agree, (2) agree, (3) redundant per 2. Agree wiht the audit as proposed"* (record §7.1; the same text is in my brief) — are not questioned here; whether the code implements EXACTLY them is.
**Evidence:** [`2026-09-17_pristine-oracle-instrument-audit-evidence/`](2026-09-17_pristine-oracle-instrument-audit-evidence/) (README there; my plant scripts and their verbatim outputs, the gate transcripts, the two tokenizer checks, the record-quote check). Scratch under the git-ignored `.tmp/audit/`.

**What I ran (all at `586b550b8`, one heavy job at a time, box load 1.0–4.1 during my Tier B runs — other agents took it to 33 at 02:01 during `release.py`'s last lane).** Read in the prescribed order: charter, record + evidence dir, container `CLAUDE.md`, `lean_frontend/CLAUDE.md`, then the full range diff. Reproduced: LADDER Tier B row 10 (`python3 scripts/test_upstream_oracle.py`, 116.5 s) and `--plant`; Tier C `--corpus ci`; Tier B row 12 `--corpus libxml2_chvalid`; Tier A `python3 scripts/release.py --mode fast` twice (the first run's `Source unchanged: False` was MY doing — the untracked evidence directory appeared mid-run; `release.source_identity` hashes untracked files, `scripts/release.py:124-125` — so I re-ran it with no tree writes); a `--with-lean --only` 8-case subset (the Lean column reproduces 6 of the record's 40 Lean≠fork rows verbatim); a fresh `--corpus all --only` over the 21 deleted register rows and the 3 tray rows; 54 auditor-written hermetic/real plants against `compare()`, the loader, `project_diagnostics`, `capture()` and the two oracles; 7 end-to-end lane plants with the register path monkeypatched (never the tree); 14 plants against `ensure_independent_oracle.py`; a mechanical check of the worker's raw-diff evidence and of fresh captures with my OWN frame tokenizer. Every `lake`/`lean` invocation: none by me (the Lean driver binary is the primed one; `--with-lean` runs it as the lane does). **Process disclosure:** `check_driver_fresh --check-oracle` reported the oracle binary as `7336b2e3…` for my first three gates (the worker's own hash, `o1-measure-chvalid.txt`) and `5108b37b…` afterwards — `release.py` A1 relinks `main.exe` with no source change (`src 98ad48b5…` throughout), exactly the 2026-09-15 audit's F7 disclosure; no verdict depends on which link ran.

## 0. Verdict

**MERGE-WITH-FIXES.** No MAJOR finding. The two rulings are implemented as stated for every case they name; every fail-open shape I planted (one-sided timeouts with and without forged rows, 137 on either side, exception text differing by one character, positions outside frames, stdout beside identical crashes, silent success, missing or malformed captures, empty selections, a missing manifest, a row naming an absent case) reads RED or `INCOMPLETE` rc 1; the 21 deleted register rows differ ONLY in OCaml backtrace frame positions (reproduced with my own tokenizer on fresh captures — item 2's MAJOR risk is cleared); the register's three rows are explained by drafts 37/38/39 and their pinned signatures reproduce; the orchestrator's four gate lines reproduce verbatim; no pre-existing baseline row moved (14/14 Tier A lanes PASSED, twice). Five MINOR findings, all small, all fixable in one commit followed by a row-10 + `--plant` re-gate: **M1** a sub-case the ruling did not name where the code chose the fail-open reading of the charter's stale-row rule (a registered case whose FORK side also times out is `matching_incomplete`, not RED; Tier A row 6b backstops it) — an operator call, flagged; **M2** two corpora (`libc_exec`, `libxml2_uri`) run at 30 s where their owning lanes bound the oracle at 300 s, against the new doc claim "each with its owning lane's per-case timeout" — with `matching_incomplete` non-failing this is a fail-open shape (dormant today: the slowest such case takes 1.2 s); **M3/M4** doctrine overclaims ("every corpus the fork-vs-Lean lanes walk" — `test_ci_sweep.sh`'s suites are not walked or named; "the ONLY permitted list of fork≠pristine behaviours … exactly 3 rows" without the observability caveat and the cross-reference to `fork_drift_manifest.txt`, which is where unobservable fork≠upstream source deltas live); **M5** `--with-lean` runs the Lean driver uncapped where four owning lanes wrap it in `CAPPED_TEST`, against "exactly as the owning lane does". Seven NOTEs. Recommended before merge: M1 (once the operator picks the reading), M2, M3, M4, N2 — then row 10 + `--plant` re-gated and the record amended; M5, N1 may follow in the S1 charter.

## 1. Findings

Grades: MAJOR = a trust gap, a hidden real difference, or a ruling not implemented; MINOR = a fail-open shape, an overclaim in a normative document, or a mirror deviation, each with a small fix; NOTE = precision, process, or a residual the merge need not wait for. Severities are [AGENT auditor] judgments.

### M1 — MINOR — `compare()` lets a BOTH-sides timeout bypass the register's stale-row rule (fail-open on a sub-case the ruling did not name)

**Where:** `scripts/test_upstream_oracle.py:243-244` (the both-124 early return precedes the `if exception:` pin check at `:255-259`); plant `:649` (`compare/both-sides-timeout+row-irrelevant` → `matching_incomplete`) bakes the reading in; record §7.1 states it: *"both 124 → `matching_incomplete` (a row is irrelevant …)"*.
**Claim audited:** charter O1, *"Two-direction fail-closed: a register row whose case no longer exists or no longer differs is RED (stale exception), as the baseline lanes do"*; ruling (1), *"both sides status 124 at the lane's own bound → `matching_incomplete` … no register row for matching timeouts"*. The ruling addressed UNREGISTERED both-sides timeouts (the `tests/ci` and csmith members); it did not name the case of a REGISTERED row whose fork side moves into a timeout. On that sub-case the two rules conflict and the code took the fail-open reading without raising it (charter S6: rule conflicts are raised, fail closed meanwhile).
**Reproduction** (`adversarial_plants.out.txt`, plant A5; hermetic — `compare(timeout_, timeout_, 'batch', node_row)` with the committed `multi_tu_tray/node` row, which pins fork `status: 0` / `Specified(7)`):
```
PLANT NOTE A5 registered node row, fork ALSO 124 (pin fork.status=0 NOT reproduced): 'matching_incomplete' — charter O1 stale-row rule says a row whose case no longer differs is RED; the code returns this (non-failing)
```
Consequence: if the fork lost draft 37's fix (the shared `.lem` regressing to non-termination on `node`), row 10 would still exit 0 with `matching_incomplete: 1` — the gate whose purpose is the fork≠pristine inventory would not go RED on the one row that inventory names for non-termination. Backstop [AGENT auditor]: Tier A row 6b (`test_multi_tu.sh:151-154`, `cerb_exit -ge 124 → FAIL`) fails on any fork timeout in the tray, so the ladder as a whole catches it; the hole is in row 10's own terms.
**Fix:** in `compare()`, when `exception` is present and both statuses are 124 return `('difference', 'reviewed difference pin moved: the registered fork signature is not reproduced (both sides timed out)')`; flip plant `:649` to expect `difference`; one sentence in VALIDATION §0/§3 and LADDER row 10 ("a registered case whose fork side times out is RED — the pin moved"). The loader's refusal of fork 124/137 rows (`:170-171`) and the unregistered both-124 class are untouched, so ruling (1) is preserved. **Operator call:** if the literal reading (any both-124 → `matching_incomplete`, rows ignored) is the intended one, say so in the record and this finding closes as a NOTE.

### M2 — MINOR — `libc_exec` and `libxml2_uri` run at the default 30 s where their owning lanes bound the oracle at 300 s; the new doctrine claims every corpus carries its owning lane's per-case timeout

**Where:** `corpus()` `:301-320` (six Tier A folders incl. `libc_exec`, `timeout=None` → `--timeout` 30) and `:369-379` (`libxml2/uri-{libc,nolibc}`, same); owning lanes `scripts/test_libc_exec.sh:37` `TIMEOUT_SECS="${TIMEOUT_SECS:-300}"` (the oracle run `:85`) and `scripts/test_libxml2_uri.sh:58` `TIMEOUT_SECS="${TIMEOUT_SECS:-300}"` (`:108`). Claims: LADDER row 10 `:67` *"855 cases, each with its owning lane's flags and per-case timeout (cited in `corpus()`)"*; VALIDATION §4 `:472-473` *"Each corpus runs with the flags, exclusions and per-case timeout of the lane that owns it"*; record §3.2 lists the Tier A block as "unchanged" and cites `test_libc_exec.sh:87` for flags only.
**Reproduction** (derived from `.tmp/audit/row10-full/report.json` `membership`): timeouts per corpus at the head = `{bytes,cli,cn,corpus,coverage,debug,float,libc_exec,libxml2_uri,minimal,multi_tu,multi_tu_tray,verify}: 30`, `immaculate: 60` — `libc_exec` and `libxml2_uri` are the two whose owning lane says 300. The default is pre-existing (base `:203`/`:268`); the doc claim is new (O4).
**Why it matters now:** before O5 a both-sides 124 was RED, so an under-mirrored bound could only make the lane louder. Since ruling (1) a both-124 is counted and NOT failing, so a `libc_exec`/`uri` case that both engines complete in 30–300 s would read `matching_incomplete` here (rc 0) while the owning lane compares it — a fail-open shape. Dormant today: the slowest such rows are `libxml2/uri-libc` (1.238 s / 1.217 s) and `libc_exec/011-decimal-sweep.c` (0.316 s / 0.308 s) — 25× under the bound.
**Fix:** pass `300` in the two `add(...)` calls (cite `test_libc_exec.sh:37` and `test_libxml2_uri.sh:58`), or strike "per-case timeout" from the two sentences and name the two exceptions. The `cli/*` rows (no owning lane) keep `--timeout`.

### M3 — MINOR — "every corpus the fork-vs-Lean lanes walk" overclaims: `test_ci_sweep.sh`'s twelve suites are neither walked nor named as excluded; "except the two below" is three

**Where:** VALIDATION §0 `:122-123` (*"pristine vs fork over every corpus the fork-vs-Lean lanes walk"*), §4 `:465-466`; LADDER row 10 `:67` (*"over EVERY corpus the fork-vs-Lean lanes walk except the two below"*); the lane's docstring `:11-12`.
**Reproduction (by reading):** `scripts/test_ci_sweep.sh:90-102` — `add_suite` over `tests/gcc-torture/breakdown/{success,fail,limbus,undefined,invalid,not_std_compliant,not_supported}`, `tests/tcc`, `tests/suite`, `tests/pnvi_testsuite`, `tests/hacl-star`, `tests/freebsd` (all present under `tests/`); LADDER Tier C row C4 calls it a "differential sweep" of fork vs Lean (2,186 files). None of these directories appears in `corpus()` or in the report's `not_applicable` list. The charter's O1 enumeration did not include them, so the WORKER followed the charter; the DOCTRINE text (O4) generalised beyond it. Separately, row 10's "except the two below" — the other selections are three (`libxml2_chvalid` row 12, `ci` C6, `csmith` C7).
**Fix:** in §0/§4 and row 10 write "every corpus the fork-vs-Lean GATE lanes walk, plus the `tests/ci`/csmith reporting corpora; the `test_ci_sweep.sh` suites (Tier C, no baseline) are not walked" — and name them in `not_applicable` or as a future `--corpus ci_sweep` selection; "two" → "three".

### M4 — MINOR — "the ONLY permitted list of fork≠pristine behaviours … the inventory is exactly 3 rows" lacks the observability caveat and the cross-reference to where unobservable fork≠upstream deltas ARE recorded

**Where:** VALIDATION §0 `:117-127` (*"the register … is the ONLY permitted list of fork≠pristine behaviours … Today the inventory is exactly 3 `shared-model-fix` rows"*); §3 `:393-397`.
**Claim audited:** the register can only contain deviations the WALKED corpora observe. The fork's source-level deviations from upstream are recorded in `scripts/fork_drift_manifest.txt` (gate `check_fork_drift.sh`, VALIDATION §6 `:626`): its header records three moved `[source-content]` pins — drafts 38/39 (D3, observable: the three rows), draft 43's `many`/`many1` restatement (D1, "EXTENSIONALLY IDENTICAL … the OCaml's results are unchanged on every input" — unobservable by construction) and the `cabs_json.ml` byte-preserving exporter (D2, fork-side only) — and 24 differing generated files. §0 does not mention the drift manifest (0 occurrences of "drift" in `:108-141`), so a reader takes "exactly 3 rows" as the complete fork≠pristine inventory rather than its behavioural projection on 855 + 4 + 242 + 1669 inputs. (My brief asked about "the allocator fix planned in tray draft 44": no draft 44 exists at this head — the tray ends at `43-many-many1-input-indexed-recursion-proposal.md`; the point stands generically for any fix whose reproducer is not in a walked corpus.) §3's new block is correctly scoped to "the fork OCaml's reviewed deviations from pristine" as OBSERVED by row 10; §0's summary sentence is the one that overreaches.
**Fix:** one sentence in §0: "… the ONLY permitted list of fork≠pristine behaviours OBSERVED on the walked corpora; the fork's source-level deltas from upstream are the reviewed `scripts/fork_drift_manifest.txt` (§6, `check_fork_drift.sh`), of which the register is the behavioural projection — a delta with no register row is either unobservable on the corpora or a missing case, and the S1 charter's three-way report is how a slice shows which."

### M5 — MINOR — `--with-lean` runs the Lean driver (and the `--cabs-json` bridge) without `CAPPED_TEST` on rows whose owning lane caps both engines; the record says "exactly as the owning fork-vs-Lean lane does"

**Where:** `run_lean:537-554` — `capture(directory / 'lean', [LEAN_BIN, *args], lean_env, limit)` is a bare `timeout`; owning lanes that cap: `test_libc_exec.sh` (4 uses of `CAPPED_TEST`), `test_immaculate.sh` (4), `test_libxml2.sh` (4), `test_libxml2_uri.sh` (3) — `common.sh:401` `CAPPED_TEST=(env "CERB_MEM_MAX=$TEST_MEM_MAX" "$CAPPED_BIN")`; lanes that do not: `test_exec.sh`, `test_multi_tu.sh`, `test_cn_coverage.sh`, `test_verify.sh` (0 each; derived counts). Record §4 and LADDER C5: *"exactly as the owning fork-vs-Lean lane does (… recipes cited per corpus)"*.
**Why it matters:** report-only column, so no gate moves; but on the libc/immaculate/chvalid rows an uncapped Lean driver can drive the box to the kernel OOM killer instead of the lanes' cgroup cap with its `OOM-KILLED` witness (the kill class the 2026-09-06 audit VF-01 made load-bearing), and the container's box discipline ("suspect box OOM on any unexplained session death") is exactly about this. Mirror-OCaml doctrine: a mirror deviation is a defect unless documented in-code as deliberate — it is not.
**Fix:** carry a `capped: bool` in the Lean recipe for the four capping lanes and run the Lean side (and the bridge, as those lanes do) through `env CERB_MEM_MAX=<TEST_MEM_MAX> scripts/capped`; or document the deviation at `run_lean` with the reason. Can follow in the S1 charter (C5 is that slice's mandatory report).

### N1 — NOTE — `citation_exists` accepts an untracked, git-ignored file and an out-of-range line reference

**Where:** `:122-129` — `(ROOT / path).is_file()` only. Plants E7/E8 (`adversarial_plants.out.txt`): a citation `.tmp/audit/plants/work/untracked-citation.md` (a file I created under the ignored `.tmp/`) is `ACCEPTED`; `tests/multi_tu_tray/README.md:99999` is `ACCEPTED`. A `..` path is not structurally rejected either (my E6 was rejected only because `ROOT/../lem-lean/README.md` does not exist from a two-level worktree; from the primary checkout it would). The rule the loader enforces is "an EXISTING citation explains the difference"; a scratch file is not a record. **Fix:** require `git ls-files --error-unmatch <path>` (tracked at HEAD), reject `..` components, and check `:N[-M]` ≤ the file's line count.

### N2 — NOTE — "16 doctored registers rejected at load" is 15 + the positive control

**Where:** LADDER row 10 `:67`, VALIDATION §5 `:525`, record §3.4 `:222`. `o5-gate-plant.txt` has 16 `PLANT OK   register/…` lines: 15 rejections (`schema-1, missing-class, unknown-class, missing-citation, empty-citation, nonexistent-citation, unknown-iso-fix-id, unknown-key, empty-rationale, identical-signatures, fork-timeout, fork-kill, pristine-kill, pristine-timeout-under-diagnostic-text, malformed-sha`) + `register/committed-loads` (the committed register LOADS). The record's own enumeration lists 15. Plant arithmetic otherwise checks: 16 + 9 `compare/` + 6 timeout-matrix + 6 `projection/` + 1 withheld-row = 38 `plant_ok` (derived). **Fix:** "15 doctored registers rejected + the committed register loads".

### N3 — NOTE — charter §1 erratum the worker did not list: "two CLI cases" were already three at the base

`git show 0c78635cb:scripts/test_upstream_oracle.py:166-168` has `cli/core-dump`, `cli/typecheck-core`, `cli/args`; the record §3.2 correctly says "the 3 CLI rows — unchanged" and §7 lists four charter errata without this one. Cosmetic.

### N4 — NOTE — a capture whose `.status` FILE disagrees with its record is not detected by `compare()`

Plant D4: record status 125, `.status` file `abc` → `load_capture` raises `ValueError` → the projection path → `matching_failure`. Both are written by `capture()` from one value (`:225-227`), so only tampering produces the inconsistency; pre-existing shape. Recording it because `matching_failure` is a non-failing class. No fix required for merge; a one-line consistency check in `record_of`/`compare` would close it.

### N5 — NOTE — the FULL gate (Tier A+B) ran at `664f9d0a7`, before O5 changed the lane, the register and the docs

Record §6/§7.1 say so honestly and the orchestrator re-ran row 10, `--plant`, `--corpus ci`, `test_unit.sh` and `--mode fast` at the head. Row 12 (`--corpus libxml2_chvalid`, Tier B) shares O5's changed `compare()`/projection and was not recorded as re-run at the head by anyone — I ran it (§2.7): `Independent oracle: passed; {'semantic_agreement': 4}` — 505.9 s wall (other agents had the box at load 34.7 when it started; slice 00 took 93.3 s pristine / 79.0 s fork against the 300 s bound, the other three 54–57 s). Green at the head; this note closes as information.

### N6 — NOTE — signal statuses other than 124/137 are "completed" statuses and fall to `difference`

Plant A6/B6: a real `SIGSEGV` child → status 139; two identical 139 records → `difference` (not in the `matching_failure` set `(1, 125, 134)`). Fail-closed; no change needed; documented so the class is not mistaken for a gap.

### N7 — NOTE — the C program's own exit value does not leak into the oracle's exit status under `--batch` (no 124/137 ambiguity)

Plant G: `int main(void){return 124;}` / `137` / `1` → both oracles exit 0 with `Defined {value: "Specified(124)", …}` etc., `semantic_agreement`. So `status == 124` is unambiguously the `timeout` tool's verdict for every walked case. Recorded because ruling (1) rests on it.

## 2. The scope items — what I did, verbatim outputs, verdict

### 2.1 Admission rules (`scripts/test_upstream_oracle.py`)

**Read in full** (915 lines) and diffed against the base. The loader (`load_register:132-178`) refuses: schema ≠ 2, top-level keys ≠ `{schema, cases}`, row keys ≠ `{class, citation, rationale, upstream, fork}`, unknown class, empty/non-string/nonexistent citation, empty rationale, non-int/out-of-range status, malformed sha, identical signatures, fork 124/137, pristine 137, pristine 124 outside `resource`/`shared-model-fix`. `compare():234-282`: 137 either side → `incomplete`; both 124 → `matching_incomplete` (ruling 1); fork-only 124 → `incomplete`; pristine-only 124 → `reviewed_difference` only via an admitting row whose BOTH signatures match, else `incomplete`; a present row that does not match → `difference` ("pin moved"); batch → shared codec verdict equality, else `matching_failure` only when status ∈ {1,125,134}, equal status/stdout-sha/diagnostic-sha, EMPTY stdout, non-empty stderr; non-batch → full signature equality with status 0 and non-empty stdout (typecheck exempt) → `interface_agreement`; everything else `difference`. `main():894-897`: `failed` = any `difference`/`incomplete`/`plant_failed` row, or the library probe failing, or the source identity moving — `matching_incomplete` is never in it (ruling 1). Exceptions: `compare` catches `(ProtocolError, OSError, ValueError)` from the codec and converts them to a reason that can only reach `matching_failure` under the guarded conjunction; `main` catches `(ValueError, OSError, KeyError, CalledProcessError)` → `INCOMPLETE` rc 1; anything else propagates (rc 1). **No swallowed exception, no absorbing default found.** `--with-lean` is a separate `row['lean']` dict; `row['status']` never depends on it (report-only — verified by reading and by the 8-case run below: rc 0 with 6 `LEAN≠FORK` rows). `--corpus` selects from `CORPORA:60-67`; the register's cases are checked against the UNION of all corpora (`:773-775`) in both directions; `--shard:490-499` is `test_csmith_corpus.sh:88-95`'s arithmetic exactly (`per = ceil(n/M)`, slice `[(K-1)per, K·per)`); `--only`/`--shard`/non-`all` selections report `subset_passed`, never `passed`; an empty selection raises (`:806-807`).

**Corpus cites checked line by line** (all resolve to what they claim): `test_exec.sh:403-406, :442-451, :169`; `test_libc_exec.sh:75-76, :85-106, :37`; `test_multi_tu.sh:139, :149-150, :175, :192, :66`; `test_immaculate.sh:151-173, :191-193, :201-203, :210-214, :63`; `test_bytes.sh:79-81, :88`; `test_verify.sh:57-65, :75-79, :105, :128, :160-163, :209, :238, :269`; `test_libxml2.sh:110-116, :121-124, :136, :144, :162-163, :204, :214-215, :65`; `test_csmith_corpus.sh:53-68, :88-95, :51`; `test_cn_coverage.sh:230-239, :86`; `test_libxml2_uri.sh:186, :193, :198-204, :221, :58`; `common.sh:319, :401`. Exclusions mirrored: `.syntax-only.c`/`.exhaust.c` (Tier A and `tests/ci`), the two in-Lean immaculate probes, the `--call` rows and `--pp=core` derivations of `test_verify.sh`, `lean_frontend/corpus` main-mode stems = `test_verify.sh:269`'s seven. Deviations found: the per-case timeout for `libc_exec`/`libxml2_uri` (M2); `test_ci_sweep.sh`'s suites absent (M3).

**My plants (54 hermetic/real + 7 end-to-end; `adversarial_plants.out.txt`, `lane_end_to_end_plants.out.txt`) — all fail-closed shapes hold.** Verbatim summary lines:
```
54 checks, 1 FAIL: ['D6 capture files missing, records differ -> difference']
```
(the one "FAIL" is my mis-specified expectation: missing captures raise `OSError` → `main()` → `INCOMPLETE` rc 1, STRICTER than `difference`; D5 confirms neither passing class is reached.) End-to-end through `main()` with the register path monkeypatched (the tree untouched):
```
--- node-row-withheld: rc=1 status=failed counts={'incomplete': 1}          (a REAL one-sided pristine timeout, 31.3 s, no row)
--- forged-fork-124-row: rc=1 status=incomplete counts=None                  INDEPENDENT ORACLE INCOMPLETE: register row 'multi_tu_tray/node': a fork-side incomplete process (status 124) is never admissible
--- row-names-absent-case: rc=1 status=incomplete counts=None                INDEPENDENT ORACLE INCOMPLETE: reviewed difference refers to a case absent from the corpus: multi_tu_tray/does-not-exist
--- empty-only: rc=1 status=incomplete counts=None                           INDEPENDENT ORACLE INCOMPLETE: empty independent oracle selection
--- empty-shard: rc=1 status=incomplete counts=None                          INDEPENDENT ORACLE INCOMPLETE: empty independent oracle selection
--- subset-with-row: rc=0 status=subset_passed counts={'reviewed_difference': 3}
--- missing-manifest: rc=1 status=incomplete counts=None                     INDEPENDENT ORACLE INCOMPLETE: [Errno 2] No such file or directory: …/no-such-manifest.json
```
Real processes: `kill -9 $$` under `capture()` → 137 → `incomplete` against a completed side (B1/B3); `sleep 5` at limit 1 → 124 (B2); real both-crash captures of `immaculate/nolibc/g4-bswap64-overflow` from the two oracles → `matching_failure` with raw stderr shas differing and diagnostic shas equal (C1/C1b); the fork's exception payload line `Z.Overflow` with ONE character changed → `difference` (C2); a frame's FILE name changed → `difference` (C4); one frame dropped → `difference` (C5); stdout beside the identical crash → `difference` (C6); positions in a Cerberus-style diagnostic line, in a `Called from` line lacking ` in file "…"`, inside a `Failure("…")` payload, and in a frame-shaped line with trailing text → all `difference` (C7–C10); a frame-shaped line in STDOUT differing only by position → `difference` for batch and for `core` kinds (C11/C12; stdout is never projected — `project_diagnostics(` has exactly two occurrences: its definition and the one `.stderr` call in `record_of:207`). Silent success (both 0, empty stdout+stderr) → `difference` for batch and core, `interface_agreement` for `typecheck` as declared (D1–D3).

**Coverage of the brief's required plants by the worker's 38:** covered hermetically — one-sided pristine timeout no row; fork timeout with a row (load + compare); 137 either side (load + compare); exception text differing by one character (`offset:4`→`offset:0`); frame positions only; a non-frame position (`internal error: failed at line 5/6`); stdout beside a crash; nonexistent citation; pristine 124 under `diagnostic-text`. **Not covered by the worker's plants:** an EMPTY corpus/selection; MISSING capture files; any REAL process (a real 137, a real 124, a real one-sided timeout end to end); a frame whose FILE name differs or a DROPPED frame; silent success; a citation to an untracked file (which the loader wrongly accepts — N1). All are in my evidence; I recommend adopting the empty-selection, missing-capture and real-137 plants into `--plant`.

### 2.2 The register (`scripts/upstream_oracle_differences.json`, 3 rows) and the 21 deleted rows

**The 3 rows.** Citations resolve (`load_register` accepts the committed file: `register/committed-loads: 3 rows, every class/citation valid`). Draft 37 explains `node` (`Ctype_aux.are_compatible` recursing on the self-referential struct in two TUs; verbatim `rc=124 (60 s; draft 37's non-termination)` at draft 38 `:127`); draft 38 `:132-139` records verbatim the two rejections `mismatched tags: Symbol(558, SD_Id("S")) vs Symbol(502, …)` and `Symbol(536, …)` for `arr-2-2-return` / `arr-incomplete-ptr-return`, and draft 39 the array-bound typo whose fix (with 38's consult) yields the fork's `Specified(7)`; `tests/multi_tu_tray/README.md:87-95` carries the same three-engine table (`node` D7/D7/rc=124; `arr-2-2-return` D7/D7/E(558,502); `arr-incomplete-ptr-return` D7/D7/E(536,502); the four others agree). The rationales match the drafts (ISO C11 §6.2.7#1, gcc 7, "retires when upstream fixes 37/38/39"). **Pinned signatures reproduce** — *reproduced*, `lane_end_to_end_plants.out.txt`:
```
Independent oracle scope: subset (--only); 3 rows in 31.4s; source unchanged: True
Independent oracle: subset_passed; {'reviewed_difference': 3}; …/.tmp/audit/plants/e2e/subset-with-row/report.json
```
and with the `node` row withheld the same case is `incomplete` rc 1 (above); the lane's own `--plant` withholds `arr-2-2-return`'s row → `difference` (below).

**The 21 deleted rows (`o5-register-rows-deleted.txt`).** Two independent checks with MY OWN tokenizer (`^\s*(Raised at|Raised by primitive operation at|Called from|Re-raised at)\s+(.*?) in file "([^"]*)"( \(inlined\))?, lines? (\d+(?:-\d+)?), characters (\d+-\d+)$`, groups 1–4 must be equal, only 5–6 may differ):
1. Over the worker's evidence `o1-raw-diffs-tray-immaculate.txt` + `o1-raw-diffs-ci.txt` (zero-context unified diffs; `check_deleted_rows_evidence.out.txt`): 20 of 21 present, every changed line pair a frame differing only in position, equal changed-line counts, stdout diff empty/none — `BAD count: 0`; `minimal/097-null-ptr-arith.undef.c` is not in those two files (its O1 citation was the 2026-09-06 record's `d5-pristine-097-diagnostic-diff.txt`).
2. Over FRESH captures at the head — *reproduced*: `python3 scripts/test_upstream_oracle.py --corpus all --only '^(…the 21 ids…)$'` → `Independent oracle: subset_passed; {'matching_failure': 21}` (5.0 s); then per case: equal status (125), empty stdout both sides, equal stderr line counts, every differing line a frame differing only in position (`check_deleted_rows_fresh.out.txt`), e.g.
```
OK  minimal/097-null-ptr-arith.undef.c: lane=matching_failure status=125 lines=29 changed=15 payload='Failure("TODO(pure shift a null pointer should be undefined'
OK  immaculate/nolibc/g4-bswap64-overflow: lane=matching_failure status=125 lines=18 changed=10 payload='Z.Overflow'
OK  ci/0258-array-function-type.error.c: lane=matching_failure status=125 lines=37 changed=6 payload='internal error: AilTypesAux.is_complete: called an a function type'
rows: 21 BAD: 0 counts: {'matching_failure': 21}
```
**Verdict:** the normalisation hides no exception-text difference in any of the 21; the deletion is sound. The O1-era register (`git show 3debab120:…json`, 24 rows) is exactly the 21 + the 3 (statuses 125/125 on all 21).

### 2.3 The normalisation (`FRAME_POSITION:193-195`, `TIME_SPENT:192`, `project_diagnostics:198-199`)

Regex adversarial lines (`adversarial_plants.out.txt` F1–F16): normalised — the four frame keywords with `line N`, `lines N-M`, ` (inlined)`; NOT normalised — a Cerberus diagnostic quoting a source location (`tests/ci/0120.c:3:5: error: … line 3, characters 4-5`), a batch `Error {msg: …}` line on stderr, a Lean `PANIC at … Cerberus.Mem:123:45:` line, a `Failure("…Raised at … in file …")` payload, the old-style `Raised at file "…"` form (no ` in file`), trailing text after the position, a CRLF line, a keyword not at line start, `Time spent: … seconds (user)`. The anchor set is `^ *KEYWORD … in file "…"[ (inlined)], line(s) N[-M], characters A-B$` under `re.M`: a match requires the line to BEGIN with a frame keyword and END with the position, so the only way a non-frame line is normalised is if a program deliberately prints an OCaml-frame-shaped line to the ORACLE's stderr — and under `--batch` the program's own stderr is inside the `Defined {… stderr: "…"}` record on STDOUT, which is never projected (C11–C13, F16). The exception text, frame function and file names, frame COUNT and every non-frame byte are compared untouched (C2, C4, C5, C7–C10 → `difference`). **Verdict:** implements ruling (2) exactly; stdout is never projected.

### 2.4 The standing build (`scripts/ensure_independent_oracle.py`) and the container hook

Reads: it `import`s `validate_build` from the lane (`:34`) — no second validator; no delete/overwrite primitive in the file (`E0`: `grep -nE "rmtree|unlink|remove\(|rename|replace\(|write_text|write_bytes|open\("` → none); discovery `discover():49-72` — explicit flag > env var > container candidates, each tested with `git cat-file -e <rev>^{commit}`, `SystemExit` naming every candidate tried. **Plants (`ensure_plants.out.txt`), verbatim first lines:** an existing EMPTY directory → `REFUSED — … exists but has no manifest.json … Nothing was deleted or overwritten` rc 1 (dir left); a garbage manifest → `REFUSED — existing build at … is INVALID: Expecting value: line 1 column 1 (char 0)` rc 1, `file intact`; a COPY of the real manifest with `status` flipped, with the pin doctored, and verbatim in another directory (archives absent) → `REFUSED … INVALID: independent build is incomplete or has wrong source pins` ×2 and `… [Errno 2] No such file or directory: …/cerberus.tar`, rc 1 each, nothing removed; `--no-build` on an absent path → `ABSENT … (--no-build: not building)` rc 1, `no directory created`; `--lem-repo /nonexistent`, `CERB_LEM_REPO=/tmp`, `CERB_CERBERUS_REPO=/tmp` → rc 1 before any `mkdir`, each naming the rev and the reason; `CERB_INDEPENDENT_MANIFEST` → the garbage copy → `REFUSED` via the parent; a directory named `manifest.json` → `REFUSED`; the real standing build → `VALID standing build: … (cerberus b9aeedcb4, lem 3802cb0)` rc 0, `manifest untouched` (mtime). **Hook** (`scripts/new-worktree.sh:86-90`, container-side): `bash -n` OK; `set -euo pipefail` at `:12` so the `[[ -n "${CERB_SKIP_INDEPENDENT_ORACLE:-}" ]] || "$PROJ/scripts/ce" python3 "$DST/scripts/ensure_independent_oracle.py"` list failing exits the script nonzero AFTER the worktree exists — as the hook's own comment says; the live lines are byte-identical to the recorded `o3-new-worktree-hook.diff` (`E14: hook lines identical to the recorded diff`). Its first end-to-end exercise is THIS worktree — `/home/dev/projects/cerberus-lean-proj/.tmp/audit-worktree.log`, verbatim tail:
```
ensure_independent_oracle: ABSENT — building the pristine oracle into /home/dev/projects/cerberus-lean-proj/worktrees/cerberus-lean-audit/pristine-oracle-instrument/.validation-foundations/independent-oracle-v2
  lem-repo:      /home/dev/projects/cerberus-lean-proj/lem-lean
  cerberus-repo: /home/dev/projects/cerberus-lean-proj/worktrees/cerberus-lean-audit/pristine-oracle-instrument
lem-compiler: passed (7.503s)
lem-libraries: passed (1.747s)
lem-runtime-build: passed (4.969s)
lem-runtime-install: passed (0.152s)
cerberus-generation: passed (22.603s)
cerberus-build: passed (10.209s)
Independent upstream oracle: …/.validation-foundations/independent-oracle-v2/manifest.json
ensure_independent_oracle: BUILT+VALID standing build: …/manifest.json (cerberus b9aeedcb4, lem 3802cb0)

Worktree ready: /home/dev/projects/cerberus-lean-proj/worktrees/cerberus-lean-audit/pristine-oracle-instrument
```
(47.2 s of build steps, derived.) Not exercised by me: the opt-out and the failing-build path end to end — both would mean creating worktrees, outside my remit; verified by reading. **Verdict:** as chartered (O3 (a)(b)(c)); fail-closed throughout.

### 2.5 Doctrine vs facts

Checked against code/evidence: 855 = 752 + 7 + 64 + 32 (derived from the measurement table; membership in my report.json = 855); 213 CN rows (`grep -cvE '^\s*(#|$)' tests/cn_coverage/manifest.txt` → 213); 7 `lean_frontend/corpus` stems (`CORPUS_MAIN_STEMS` = `test_verify.sh:269`); 3 CLI rows; 242 `tests/ci` cases; 11 → 28 `matching_failure` and 102 → 106 in C6 (11 + 17 = 28; 102 + 4 = 106, derived — matches my runs); 4 chvalid slices at 44.6/53.0/52.6/54.7 s pristine (`o1-measure-chvalid.txt`; "~45-55 s per engine", "406.7 s" ✓; "~7 min" ≈ 430.7 s B12 ✓); "~2 min warm" (116.5 s ✓); csmith 1669 (checked in `corpus():485`); shard 1/34 = 50 ✓. The `[USER]` quotations: VALIDATION §0 `:108-110` quotes *"right now we have the ocaml-cerberus as an oracle, but we're also changing it. There's some danger there!"* — a verbatim substring of the charter §0's first ruling (which begins "Thinking about the changes we're making, …"); "(1) agree" / "(2) agree" are verbatim fragments of the §7.1 ruling. LADDER row 6b `:38` now says the tray IS walked; row 10 `:67`, row 12 `:69`, C5–C7 `:94-96` match the code's selections and my verdict lines; `release.py --list` shows `B12`, `C5`, `C6`, `C7` (the membership hash moved with LADDER.md — `release.py` untouched, as the record says). `tests/multi_tu_tray/README.md:17-28` corrected paragraph: accurate (three rows; "the other four cases agree on all three engines" holds under row 6b's `failure-class` projection — `arr-1-2-return` is `E(545,502)` fork = pristine, `E(63,19)` Lean; `with-lean-subset.txt` reproduces `lean_agreement` for it). §8 names rows 10 and 12 ✓. **Overclaims found:** M2 (per-case timeout), M3 (every corpus / "two below"), M4 (ONLY list / inventory without the observability caveat), N2 (16 vs 15).

### 2.6 The three-engine column

By reading: `--with-lean` adds `row['lean']` only; gating (`:894-897`) never reads it; `lean_bridge_failed`/`lean_incomplete`/`lean_both_undecodable`/`lean_difference` are all printed `LEAN≠FORK` and listed in `report.json` `lean_differences`; the Lean binary is a checked prerequisite (`:750-753`, `check_driver_fresh.sh --check-lean`). *Reproduced* on 8 cases (`with-lean-subset.txt`; rc 0):
```
Three-engine report (Lean column, NOT gating): {'lean_agreement': 2, 'lean_difference': 5, 'lean_both_undecodable': 1}; Lean≠fork rows: 6: minimal/073-exit.libc.c, minimal/097-null-ptr-arith.undef.c, debug/ub-static-reject.c, bytes/byte_is_not_char.c, immaculate/nolibc/g5-decode-question, immaculate/libc/g5-escape-roundtrip
Independent oracle: subset_passed; {'semantic_agreement': 4, 'matching_failure': 4}; …
```
with the three tokens per row exactly as in `o2-with-lean-tier-b.txt` (e.g. `073`: fork `Symbol(549, SD_Id("exit"))` vs Lean `Symbol(66, …)`; `g5-escape-roundtrip`: fork 87 vs Lean 127; `g5-decode-question`: fork crash vs Lean `Specified(63)`). **S2 sample (≥ 8) against the owning baselines — by my own `grep`:** `tests/immaculate/baseline.txt` — `f3-raw-high-byte-int MATCH | L=CRASH`, `g2-memcmp-uninit MATCH | L=CRASH`, `g4-bswap64-overflow MATCH | L=CRASH`, `offsetof-union-member MATCH | L=CRASH`, `zd-z2f04-closedir MATCH | L=CRASH`, `zd-z2m01-aligned-alloc-zero-zero MATCH | L=CRASH`, `g5-decode-question ORACLE_CRASH | L=VAL:{value: "Specified(63)", …}`, `s4b-memcmp-hugesize ORACLE_CRASH | L=UB:{ub: "UB_CERB002a_out_of_bound_load", …}`, `zd-e2-ptr-string-literals ORACLE_CRASH | L=VAL:{value: "Specified(0)", …}`, `zd-z2m01-aligned-alloc-zero[-nolibc] ORACLE_CRASH | L=UB:{ub: "DUMMY(align_alloc)", …}`, `g5-escape-roundtrip DIFF | L=VAL:{value: "Specified(127)", …}`, `r5-hex-subnormal-double-rounding DIFF | L=VAL:{value: "Specified(1)", …}` (13 rows, all as the join table says); `scripts/exec_baseline.txt` — `073-exit.libc.c CERB_SKIP`, `074-abort.libc.c CERB_SKIP`, `097-null-ptr-arith.undef.c CERB_SKIP`; `exec_coverage_baseline.txt` — the 10 named rows `CERB_SKIP`; `exec_debug_baseline.txt` — the 4 named rows `CERB_SKIP`; `tests/bytes/` — the five front-end-reject fixtures have `.elab` pins and no `.exec` (the lane's NEG pins). Caveat the record itself states and I confirm: the `CERB_SKIP` rows pin the ORACLE's skip, not a Lean verdict — the Lean side on them is a first observation, and the record classes it under §1(a) (symbol-id / front-end rejection on stderr vs `Error {msg: "desugaring failed at …"}`) rather than "pinned". That reading is correct against VALIDATION §1(a) `:157`; the S2 claim is accurate as "recorded classes", slightly loose as "recorded pins". Lean recipes: cites verified (§2.1); mirror deviation M5 (capping).

### 2.7 Sequential invariance — the gates at `586b550b8`, *reproduced* (`gate-chain.sh`, `gate-chain2.sh`, `gate-chain3.sh`; transcripts in the evidence dir)

Row 10 (`row10-full.txt`, started 01:49:59Z at load 1.00):
```
Independent oracle scope: tier-b; 855 rows in 116.5s; source unchanged: True
Independent oracle: passed; {'semantic_agreement': 822, 'matching_failure': 28, 'reviewed_difference': 3, 'interface_agreement': 2}; …/.tmp/audit/row10-full/report.json
```
`--plant` (`row10-plant.txt`):
```
Independent oracle scope: plant; 40 rows in 1.4s; source unchanged: True
Independent oracle: plants_passed; {'semantic_agreement': 1, 'plant_rejected': 1, 'plant_ok': 38}; …/.tmp/audit/row10-plant/report.json
```
`--corpus ci` (`corpus-ci.txt`; the two members `22/242 matching_incomplete: ci/0023-jump1.c (pristine 30.0s, fork 30.0s)`, `24/242 … ci/0025-jump3.c`):
```
Independent oracle scope: ci; 242 rows in 137.0s; source unchanged: True
Independent oracle: passed; {'semantic_agreement': 134, 'matching_incomplete': 2, 'matching_failure': 106}; …/.tmp/audit/corpus-ci/report.json
```
All three equal the orchestrator's lines in my brief (counts identical; wall 116.5 vs 120.2 s). Row 12 `--corpus libxml2_chvalid` (`row12-chvalid.txt`): 
```
Independent oracle scope: libxml2_chvalid; 4 rows in 505.9s; source unchanged: True
Independent oracle: passed; {'semantic_agreement': 4}; …/.tmp/audit/row12-chvalid/report.json
```
(the record's B12 at `664f9d0a7`: `4 rows in 430.5s`, same counts.)
`python3 scripts/release.py --mode fast` (Tier A, 14 commands) — first run `release-fast.txt` (01:54–02:01Z): every lane `PASSED` (`A1 164.8s, A2 30.1s, A3 51.3s, A4 22.9s, A4b 24.2s, A4c 3.1s, A5 24.2s, A6 2.3s, A6b 3.6s, A7 10.1s, A8 9.0s, A9 16.8s, A10 17.7s, A11 78.3s`) but
```
fast: incomplete; 14/14 selected commands completed successfully.
Source unchanged: False. Complete tier selection: True.
```
— my untracked evidence directory appeared during the run (`source_identity` hashes `git ls-files --others`). Second run with no tree writes (`release-fast-2.txt`): 
```
fast: passed; 14/14 selected commands completed successfully.
Source unchanged: True. Complete tier selection: True.
Release certification: incomplete: reporting/adoption/audit exits require separate evidence.
```
(`PASSED A1 158.4s, A2 32.2s, A3 59.4s, A4 26.9s, A4b 27.3s, A4c 3.6s, A5 26.8s, A6 2.4s, A6b 3.8s, A7 11.0s, A8 9.3s, A9 17.9s, A10 19.0s, A11 61.4s`; rc 0; 02:10:43–02:18:24Z) — the orchestrator's `passed; 14/14` reproduces; the last line is the runner's standing text for a Tier-A-only selection, not a lane failure.
**No pre-existing baseline row moved:** every baseline lane (A2–A4c, A5, A6, A6b, A9–A11) reports its committed state (`BASELINE OK`, `SUMMARY … fail=0`, `GATE PASS`, `213 entries, exact match`); row 10's three `reviewed_difference` rows are exactly the register's; its 28 `matching_failure` = the base's 11 + the 17 formerly-pinned tier-b rows (derived).

### 2.8 Record integrity

`check_record_quotes.out.txt`: every one of the 44 verbatim gate/build lines quoted in the record (O3 build, O3/O1/O2/O4/O5 gates, the three-engine line, the FULL-gate summary, B7's regression and re-run tails, C6/C7) exists in a file of `…-record-evidence/` — `quoted lines: 44 missing: 0`. Derived tallies are labelled "derived" (§3.1, §3.5, §7.1). Provenance: [AGENT] on every placement, the signature change, the codepoint-order divergence, the hook's fail-noisy design; [USER] only on the quoted rulings (§0, §7.1) — the §7.1 quotation matches my brief character for character (including "wiht"). Charter §1 errata: four stated (record locations `:133`/`:1776`; the 2026-09-06 record's line shifts; `test_observation_lanes.py` has no row-10 plant; `build_independent_oracle.py` line ranges) — one missed (N3). Open items honest and still open: the csmith shard order's locale dependence (fix belongs to `test_csmith_corpus.sh`); the hook's end-to-end exercise (now done — this worktree); `TODO.md` untouched; `release.py` untouched; the FULL gate at `664f9d0a7` not the head (N5). Undone items the record does NOT list: M1–M5, N1, N2.

## 3. Verified clean (checked and found correct; by my own reproduction unless marked "by reading")

- Ruling (1): both-124 → `matching_incomplete`, counted, non-failing; one-sided 124 either side and 137 either side → `incomplete`, fatal; the loader refuses fork 124/137 rows — plants A1–A4, A6, B1–B5, E3, end-to-end `node-row-withheld`/`forged-fork-124-row`. (Sub-case not named by the ruling: M1.)
- Ruling (2): position normalisation inside the four frame forms only; exception text, frame function/file names, frame count, non-frame lines, stdout untouched; raw stderr retained — C1–C13, F1–F16, the 21 fresh cases.
- The register's 3 rows: cited, explained by drafts 37/38/39, signatures reproduce; the `node` row is the only admitted pristine-side incomplete; a withheld row is RED (the lane's own plant and mine).
- The 21 deleted rows: frame-position-only, twice (evidence + fresh), with my own tokenizer.
- `ensure_independent_oracle.py`: one validator (imported), refuses present-but-invalid, never deletes/overwrites, fail-loud discovery; the hook diff is what is live; the hook built this worktree's oracle in ~47 s.
- The gates: row 10, `--plant`, `--corpus ci`, row 12, `--mode fast` 14/14 — all at the head, all matching the orchestrator's lines; no baseline row moved.
- `--with-lean`: report-only; recipes cite the owning lanes correctly (bridge flags, `--batch`/`--first`, `--args`, libc pin + 12 metadata TUs count-checked, `failure-class` for the tray and uri-nolibc); the S2 sample (13 immaculate + 17 exec-baseline + 5 bytes rows) matches the owning files.
- Record quotes 44/44; provenance; derived labels.
- Fence: only fenced files changed (`git diff --stat`: the lane, the register, the new ensure-script, LADDER, VALIDATION, tray README, the charter/record/evidence); no `.lem`/`.ml`/`.lean`/Makefile/lakefile/dune, no other baseline, `CERBERUS_REV`/`LEM_REV` unchanged (`build_independent_oracle.py` untouched).
- "Nothing new out of policy" ([USER 2026-09-08]): the range adds no semantics, no proofs, no artefact class — an instrument only (by reading of the diff).

## 4. What I did not check

- The FULL Tier A+B gate at the head (B1, B4–B9, B11 were run at `664f9d0a7` by the worker; O5 touched none of their inputs). I ran Tier A (twice) and the three row-10-family lanes plus row 12 at the head. B7 (`test_gcc_oracle.sh`, ~24 min) not re-run: no input of it changed in the range.
- `--corpus csmith --shard 1/34` (11 min; the record's `subset_passed; 26/3/21` and the count arithmetic were checked from `o5-gate-csmith.txt`, not re-run) and the full `--with-lean` Tier B report (4 min; 8-case subset reproduced instead).
- The hook's opt-out and failing-build paths end to end (creating worktrees is outside my remit; read).
- Tampering with the standing build's artefacts to exercise `validate_build`'s artefact-hash checks (would modify the standing build; the validator is the 2026-09-06 audited one, unchanged in this range; manifest-level corruptions were planted).
- Whether upstream has since fixed drafts 37/38/39 (network; the rows' "retire when upstream fixes" is a standing condition, not a claim about today).
- A rebase of the range onto `6da619f69` (the orchestrator's step; three commits moved the mainline after `0c78635cb`).

## 5. Provenance

[AGENT auditor] throughout; no [USER] ruling is created or revised here. Quotations marked `[USER 2026-09-17]`/`[USER 2026-09-16]` are copied from the charter/record/brief. All measurements 2026-09-17 01:49–02:18 UTC on the audit worktree at `586b550b8`, box load recorded per run in the chain logs. Scratch under `.tmp/audit/` (git-ignored) may be deleted; everything cited is in the evidence directory beside this report. Nothing was pushed, merged or committed on any mainline; the audited branch and the primary checkout were not touched.
