# Fuel-parameter arc, cerberus half, slice C1 — pre-merge audit (2026-09-04)

Auditor [AGENT] (independent of the C1 worker and of the orchestrator).
Audited range: mainline `mdd/cerberus-lean` @ `1b57bcf26` → branch
`arc/fuel-parameter-cerberus` @ `43360304b` (5 commits: `509698371`,
`bed02f577`, `20655ae7b`, `f436dba72`, `43360304b`). Built, stamped tree
read and probed in `worktrees/cerberus-lean-arc/zero-discrepancy` (nothing
there edited); this document lives on `audit/c1-premerge` (worktree
`worktrees/cerberus-lean-audit/c1-premerge`). Nothing merged, nothing
pushed. Every quoted output is verbatim; derived tallies are labelled
DERIVED. Scheduling constraint honoured: all reading first; no lane and no
Lean run until the orchestrator's battery log
(`.tmp/c1-reverify.log`) ended with `=== DONE`; then the three named lanes
serially, then the plants and the kernel probe.

## 0. Verdict

**MERGE-WITH-FIXES.** No MAJOR finding. Three MINOR findings (M1–M3),
two of them ten-minute fixes recommended before the ff-only merge (M2: the
no-numerals gate misses the natural literal-fuel spelling of an
instance-carrying worker and six non-idiomatic spellings, while
VALIDATION.md/the manifest describe it as "no fuel numeral in the Lean
text"; M3: duplicated `[LemFuel] [LemFuel]` binders in speclab). M1 (the
totality-parametricity list is a static snapshot whose generator is
uncommitted) may be a follow-up row. NOTES N1–N9 are record-integrity and
context items; none changes the verdict. The operator decides which fixes
land before the merge (the audit ask is the working practices'; the
waiver is the operator's).

What holds, verified (§2–§7): the `.lem` diff is declares-only and the
OCaml generated tree is byte-identical to the mainline primary checkout's
(`diff -rq` empty, 86 files, same lem-sync `gen` hash); the only fuel
numeral in the Lean text is `Main.lean`'s `defaultFuel` (+ the `letI` that
consumes it); every fuel'd callee starts from the full ambient (0
cross-block counter passes in the generated tree); the driver builds the
instance once and refuses `--fuel 0`/non-numerals/missing/out-of-position
(exit 2/1) and refuses to run without `LEAN_ABORT_ON_PANIC` (so the
panic-form exhaustion is fail-closed); the three `fuel_measure`
obligations are stated as the fuel-measure contract and proved for all
inputs with cones ⊆ {propext, Classical.choice, Quot.sound} and no option
bumps; the measured wrappers are fuel-free and kernel-computable
(`decide`/`rfl` on closed terms); the two new gates go red on their
plants; `test_unit.sh`, `test_verify.sh`, `test_immaculate.sh` green on
the stamped tree; the orchestrator's battery (all Tier A + Tier B rows)
rc 0 with zero baseline movement.

## 1. Findings

### MINOR

**M1 — `TotalityProofTest.lean` Part 1 is a static snapshot; its generator is uncommitted; coverage drift is silent.**
Cite: `lean_frontend/test/Unit/TotalityProofTest.lean:38` ("`-- 64 ambient
wrappers (generated by .tmp/scratch/gen_totality_part1.py from the
generated tree)`"); the generator exists only in the ephemeral
`worktrees/cerberus-lean-arc/zero-discrepancy/.tmp/scratch/gen_totality_part1.py`
(read: it regex-reads every `def f … [LemFuel] : … := f_lemFuel LemFuel.fuel`
line and `assert`s the worker head exists — fail-closed for a missing
WORKER, but nothing in the committed tree re-derives the list). Measured
today (DERIVED, `grep` over the 170 non-seam generated modules): 64
ambient wrappers = 64 `example`s — consistent. But a fuel'd wrapper added
by a later `.lem`/lem change is silently unpinned: the test stays green,
so the record's claim "fuel parametricity for all 64 wrappers" is true at
this head and unguarded afterwards (a coverage fail-open, not a soundness
one — each `rfl` that IS there still fails on binder drift). Fix: commit
the generator under `scripts/` and have `test_unit.sh` (or a small gate)
compare the generated wrapper set against the example list (set equality,
both directions), or generate Part 1 at build time from the tree.

**M2 — `check_no_fuel_numerals.sh` misses the natural literal-fuel spelling of an instance-carrying worker, and six non-idiomatic spellings; VALIDATION.md/the manifest overstate what it guarantees.**
Cite: `scripts/check_no_fuel_numerals.sh:100-105` (F1–F6 regexes);
`lean_frontend/VALIDATION.md:151` ("**no fuel numeral in the Lean text a
consumer reasons against**"); manifest §1 ("the ONLY fuel numeral in the
repository's Lean text"). Plants on a scratch COPY of the scan set (the
gate script copied beside it; nothing in any tree touched), verbatim
(`.tmp/plant_evasions.sh`):

```
BASELINE (unplanted copy):
check_no_fuel_numerals: OK (267 files scanned comment-stripped; no lemDefaultFuel/driverFuel/ndDefaultFuel, no LemFuel instance, no literal fuel (F1-F6); allowed Main.lean sites seen: 4 of 4 (hand-written + generated copy))
rc=0
--- the brief's four plants
PLANT [numeral in a fuel position in a seam (F3)] -> rc=0: check_no_fuel_numerals: OK (267 files scanned comment-stripped; no lemDefaultFuel/driverFuel/ndDefaultFuel, no
PLANT [⟨N⟩ applied to a fuelled name (F5)] -> rc=1: check_no_fuel_numerals: FAIL (F5): fuel numeral shape found:
PLANT [instance : LemFuel (F2)] -> rc=1: check_no_fuel_numerals: FAIL (F2): fuel numeral shape found:
PLANT [lemDefaultFuel reference (F1)] -> rc=1: check_no_fuel_numerals: FAIL (F1): fuel numeral shape found:
--- auditor's evasion probes (non-idiomatic spellings of a fuel numeral)
PLANT [E1 hex literal ⟨0x5F5E100⟩] -> rc=0: check_no_fuel_numerals: OK (267 files scanned comment-stripped; no lemDefaultFuel/driverFuel/ndDefaultFuel, no
PLANT [E2 ascribed literal ⟨(100000000 : Nat)⟩] -> rc=0: check_no_fuel_numerals: OK (267 files scanned comment-stripped; no lemDefaultFuel/driverFuel/ndDefaultFuel, no
PLANT [E3 arithmetic ⟨10^8⟩] -> rc=0: check_no_fuel_numerals: OK (267 files scanned comment-stripped; no lemDefaultFuel/driverFuel/ndDefaultFuel, no
PLANT [E4 structure-instance letI : LemFuel := { fuel := 100000000 }] -> rc=0: check_no_fuel_numerals: OK (267 files scanned comment-stripped; no lemDefaultFuel/driverFuel/ndDefaultFuel, no
PLANT [E5 indirection through a non-fuel-named constant] -> rc=0: check_no_fuel_numerals: OK (267 files scanned comment-stripped; no lemDefaultFuel/driverFuel/ndDefaultFuel, no
PLANT [E6 LemFuel.mk (10^8)] -> rc=0: check_no_fuel_numerals: OK (267 files scanned comment-stripped; no lemDefaultFuel/driverFuel/ndDefaultFuel, no
PLANT [E7 worker at a hex literal] -> rc=0: check_no_fuel_numerals: OK (267 files scanned comment-stripped; no lemDefaultFuel/driverFuel/ndDefaultFuel, no
```

The planted lines were (each appended to `lean_frontend/CerbND.lean`
unless stated): the brief's F3 plant `def auditP1 := @driver2_lemFuel
⟨fuelVar⟩ 5 fmapEmpty false` — GREEN: for the 26 workers that carry the
instance (`driver2`, the substitution family, `step_eval_pexpr`, …) the
explicit form puts `⟨inst⟩` between `_lemFuel` and the numeral, so F3's
`_lemFuel[[:space:]]+[1-9]` never sees it; this is the shape a proof text
naturally writes (`@driver2_lemFuel ⟨n⟩ 5 …`). The F5 plant `def auditP2
:= @driver2 ⟨100000000⟩ fmapEmpty false` — RED (F5). The F2 plant
`instance : LemFuel := ⟨100000000⟩` in `generated/Driver.lean` — RED (F2).
The F1 plant `def auditP4 : Nat := lemDefaultFuel` in `CerbMem.lean` —
RED (F1). E1–E7 as labelled (E5 = `def auditBudget : Nat := 100000000`
then `@driver2 ⟨auditBudget⟩ …`; E7 = `@driver2_lemFuel ⟨fuelVar⟩
0x5F5E100 …`) — all GREEN. The gate's own 13-plant `--selftest` is red on
every idiomatic shape (§6 below), so the gate CAN go red and is not
vacuous — it is a speedbump against the idiomatic spellings, not the
guarantee the shop-window text states. Fix (pre-merge recommended, one
regex each): F3 → also `_lemFuel[[:space:]]*⟨[^⟩]*⟩[[:space:]]+(0x[0-9a-fA-F]+|[1-9][0-9]*)`
and hex; F5 → `⟨[[:space:]]*(\(?[[:space:]]*)?(0x[0-9a-fA-F]+|[1-9][0-9]*)`
(covers `⟨(N : Nat)⟩` and hex); F4 → add `LemFuel[[:space:]]*:=[[:space:]]*\{`
and `LemFuel\.mk[[:space:]]*\(`; add the new shapes to the selftest.
E3/E5/E6-style arithmetic and indirection are not regex-closable; say so
in VALIDATION.md:151 ("the idiomatic shapes F1–F6; arithmetic/indirect
spellings are review discipline") and in the manifest §1.

**M3 — duplicated instance binders in speclab.**
Cite: `lean_frontend/speclab/SpecLab/DivModFiles.lean:102` `def
divmodI8FileOf [LemFuel] [LemFuel] (m : Input) …` and `:118` `def
divmodI8FileOfStream [LemFuel] [LemFuel] (s : Stream) …` (the only two
such sites: `grep -rn "\[LemFuel\] \[LemFuel\]"` over `lean_frontend`
and `tests`). Harmless at every implicit call site (both binders resolve
to the one ambient instance), but an explicit `@divmodI8FileOf ⟨n⟩ ⟨m⟩`
admits two different fuels — a surface wart for any consumer of the
speclab package, and a mechanical-edit artefact (the worker's
`addbinder.py` ran twice). Fix: drop one binder on each line.

### NOTE

**N1 — record §0 miscounts and dangles.** `2026-09-04_fuel-parameter-C1-record.md:49`
says "re-pinned (31 names incl. the 3 obligations)"; `FUEL_THMS` has 34
entries (DERIVED count of `scripts/check_theorem_axioms.sh:838-852`; the
gate prints "34 contract lemmas", §4.4/§5.1/§7 of the record say 34).
§0 cross-references "§11" twice (`:29`, `:50`); the record has no §11 —
the battery is §5. §3 numbers its subsections `3` then `3.5`. Cosmetic;
fix in the doc commit.

**N2 — the record's bit-identical-binary claim is not reproduced; the load-bearing invariant is.**
Record §2.5: "the SAME binary hash as pass 1's cache-disabled build (`bin
4b00af5c…`): byte-identical OCaml sources give a bit-identical
executable". The orchestrator's re-verify rebuild on the same sources
stamped `check_driver_fresh: oracle OK (bin
bc1bee7b9e1e2fb21d825e1929b2fbaeedf3a8735847764abb67ca71d6d06a13, src
7c9a3b9dbc6d1cbc124dd986ef3239d5159456128138b3df116e1e77bd210b09)` (the
same `src`, a third `bin`). Binary-hash reproducibility is not a property
this repository can rely on (link/cache nondeterminism); the invariant
that matters — the generated OCaml tree's byte identity — I verified
independently (§2). Suggest the record soften the sentence.

**N3 — stale `.lem` comments still name the deleted `lemDefaultFuel`.**
`frontend/model/defacto_memory.lem:2678`, `formatted.lem:327`,
`monadic_parsing.lem:113` (comments only). Left alone correctly under the
byte-identity rule (lem emits comments into the OCaml text), but they now
describe a constant that no longer exists. Fix at the next sanctioned
OCaml-text move (or a comment-only delta with the stamp re-recorded, by
operator ruling); a TODO row would keep it visible.

**N4 — the hand-written `CerbMem` layout family shares one counter across its `mutual` block.**
`CerbMem.lean:343-503`: `memberAlign_lemFuel`/`offsetsofMembers_lemFuel`/
`offsetsof_lemFuel`/`sizeofCtype_lemFuel`/`alignofCtype_lemFuel` pass the
DECREMENTED `lemFuel` to each other. This is the block's own recursion
(the generated all-or-none-per-block convention, lem design R1 item 2),
pre-existing and unchanged by C1, not a cross-callee restart; every
caller from outside the block starts it at `LemFuel.fuel`. In the
generated tree the same analysis (DERIVED, script over the 170 non-seam
modules: `X_lemFuel lemFuel` with `X` ≠ the enclosing def) finds 5 sibling
passes, all inside one `mutual` block, 0 across blocks. C2 converts the
layout family to measured (manifest §3).

**N5 — the obligations are STABILITY, not "sentinel unreachable"; the record says so.**
Per lem-lean fuel-measure record §2.2 the generated statement is
fuel-stability above the measure; it does not assert the `fuelExhausted`
arm is unreachable at `μ`. The three proofs' structure (constructor
positivity `1 ≤ lemSize`, descent `lemSize child < lemSize parent`, the
`f = 0` case closed by `omega`) gives the operational fact but no theorem
states it — the documented residual gap, correctly described in the
record §3.5. Not a defect of the slice.

**N6 — the gate's allowlist is by exact line INCLUDING the numeral.**
`check_no_fuel_numerals.sh:52` allowlists `def defaultFuel : Nat :=
100000000` verbatim: changing the harness default requires editing the
gate (F6 fires otherwise). By design ("allowlisted by exact line"), noted
so the next person is not surprised. F5 (`⟨N⟩`) may also false-positive on
an unrelated anonymous-constructor numeral — fail-closed, acceptable.

**N7 — derived counts.** Reproduced from the tree (DERIVED, `grep`/`python3`):
397 `[LemFuel]` binders in the 170 non-seam generated modules; 64 ambient
wrappers `:= f_lemFuel LemFuel.fuel`; 67 generated `theorem f_lemFuel_zero`
(incl. the nine driver/ND/memory names at `Nondeterminism.lean:215/339/344`,
`Driver.lean:275/391/428`, `Defacto_memory.lean:821/833/917`); 3 measured
wrappers (`Ctype.lean:442`, `Core.lean:222`, `Defacto_memory_aux.lean:70`);
`inductive monTrace [LemFuel]` at `Cmm_op.lean:411`; `initial_driver_state
[LemFuel]` (`Driver.lean:491`), `drive [LemFuel]` (`:563`). Seam binders:
CerbMem 40, CerbND 3 defs (+3 comment mentions), CerbCall 3, Main 3 defs
(+1 comment) — the manifest's "53" counts text occurrences; fine. The
unit lane's `C2 ratchet OK (301 files scanned …)` vs the record's "295":
an unpinned count (the LemLib copy), not a population movement.

**N8 — `CERB_TEST_FUEL` is read by shell only.** `scripts/common.sh:331`
defines it; readers: the five `test_speclab_*.sh` gate legs and
`test_immaculate.sh:229`; in Lean text the string appears only inside
usage messages (`SLUnit/Fuel.lean`, `illtyped-store.lean`, `Micro.lean`).
No product code reads it; the differential lanes do not pass it (the
driver's own `--fuel` default applies). Within the ruling's test-suite
exception; record D-C1-3 asks the operator to confirm.

**N9 — the consumer's (A)/(B)/(C) requirement stays open for the layout family, by sequencing.**
Review §2 asks that every fuel'd function on the execution path be
structural, absorbing, or unreachable; `sizeofCtype`/`alignofCtype`/
`offsetsof*`/`memValueToBytes`/`reconstructValue`/`typeofMval`/
`unqualifyAndUnatomic` are still ambient with the opaque
`fuelExhaustedWith` sentinel (loud at runtime — `Main.lean:1106` refuses
to run without `LEAN_ABORT_ON_PANIC`, so no default value ever reaches a
verdict; in the logic the arm is opaque). Scoped to C2 by the operator
("go ahead with the merges as proposed, then work on this as you
suggest"); manifest §3 says so. Not a C1 finding; recorded so the
consumer is not surprised at the re-pin.

## 2. `.lem` discipline and OCaml byte identity (scope item 1)

`git diff 1b57bcf26..43360304b -- frontend/model/` (read in full):
`core.lem +5`, `ctype.lem +9`, `defacto_memory_aux.lem +5` — each ONE
``declare {lean} fuel_measure val … = `lemSize …` `` line plus its
comment; `mem.lem +32` — 19 `declare {lean} fuel_consumer val …` lines
plus one 13-line comment; `driver.lem -9` — the 5-line "FUEL arc budget
commit" comment + the four `declare {lean} fuel val X = 100000000` lines;
`nondeterminism.lem -6` — the 5-line comment + `declare {lean} fuel val
nd_bind = 100000000`. No other hunk; no body, type, `val` or `instance`
line touched. The mem.lem comment sits between `declare lean target_rep
function alignof_ival` and `declare {lean} fuel_consumer val alignof_ival`
(swallowed by the following `{lean}` declare on OCaml — the record's
§2.2 quirk), which the identity check below confirms.

Byte identity, measured by me (the OCaml generated tree is NOT committed —
`git ls-tree 1b57bcf26 ocaml_frontend/generated` is empty — so the
comparison is against the primary checkout's tree, parked on mainline
`1b57bcf26`, stamped from the pre-arc lem):

```
=== primary checkout HEAD + stamp
1b57bcf26
src 4f2e089b39d5b371973513b3350f81d1b89871976f77df9ba4a25da3421d0c54
gen 295e4f8291c9ffd57a4061dd38e8ec273f18d6c1cfe3a0465291f1a4bcff8100
=== ZD stamp
src 03c176935c3e37a0f5b9a00192796ddf42dd6bd09ebf3bb3a41c028c25f8f10c
gen 295e4f8291c9ffd57a4061dd38e8ec273f18d6c1cfe3a0465291f1a4bcff8100
=== diff -rq primary generated vs ZD generated
diff rc=0  (primary files: 86, ZD files: 86)
=== sibylfs
sibylfs diff rc=0
```

`tools/check_lem_sync.sh --check` in the primary: `check_lem_sync: OK (src
4f2e089b…, gen 295e4f82…)`; in the branch worktree (unit lane, §6):
`check_lem_sync: OK (src 03c17693…, gen 295e4f82…)`. The `gen` hash is
`sha256` over the NUL-sorted `ocaml_frontend/generated/*.ml`
(`tools/check_lem_sync.sh:27,102-109`): same hash, empty `diff -rq`, 86
files each side. The record's §2.2/§2.5 evidence (`OCAML GENERATED TREE
BYTE-IDENTICAL (86 files)`) is corroborated independently. Oracle
`src` moves with the `.lem` text by construction (record §2.2 note) —
correct.

## 3. No magic values (scope item 2)

Gate on the tree (unit lane, §6): `check_no_fuel_numerals: OK (267 files
scanned comment-stripped; … allowed Main.lean sites seen: 4 of 4
(hand-written + generated copy))`. The four allowed sites are two lines ×
two copies: `Main.lean:1070` `def defaultFuel : Nat := 100000000  --
FUEL-DEFAULT (the one allowed fuel numeral)` (the CLI default, with its
docstring `:1055-1069` stating it is a harness choice no theorem depends
on) and `Main.lean:1304` `let code ← (letI : LemFuel := ⟨fuel⟩;
runPipeline …` (the one instantiation). Deleted names in code: `grep` over
`lean_frontend scripts tests frontend tools` (docs excluded) finds
`driverFuel|ndDefaultFuel|drive_lemFuel|lemDefaultFuel|drive_wrapper_defeq|
driverFuel_eq|exemplar_run_one_kernel|exemplar_certified_shipped_one` only
in comments/docstrings/the gate's own F1 pattern and three stale `.lem`
comments (N3). `.lean` files outside the gate's scan set: only
`lean_frontend/docs/upstream-tray/lean4/repro/*.lean` (docs). Plants: §1
M2 (the brief's four: F5/F2/F1 red, F3-with-instance green) and §6 (the
gate's 13-plant selftest, all red). `CERB_TEST_FUEL`: N8.

## 4. The `[LemFuel]` surface (scope item 3)

- Ambient starts: 64 generated wrappers `:= f_lemFuel LemFuel.fuel`; the
  seams `runND`/`runND1`/`runND1Trace` (`CerbND.lean:163-165, 241-243,
  292-295`) and the twelve `CerbMem` wrappers read `LemFuel.fuel`; 0
  cross-block counter passes in the generated tree (N4). No `_lemFuel
  (n - 1)`-style restart anywhere outside a worker's own block.
- `drive`'s only fuel source: the generated `drive [LemFuel]`
  (`Driver.lean:563`) — `#check @drive : [LemFuel] → Fmap … → Bool →
  generic_file … → List String → ndM …` (probe, §7); the hand-written
  mirror `drive_lemFuel` and `drive_wrapper_defeq` are gone (`grep`).
- One instance at the entry: `Main.lean:1304` inside `main`, around
  `runPipeline`; `frontendTU`/`loadLibc`/`runPipeline` take `[LemFuel]`
  (`:563,663,791`); no other `letI`/`instance` (gate F2/F4 green; the
  `--parse-core` branch runs the hand-written `CoreParser`, which is not
  fuel'd, before the instantiation — `--fuel` there is refused as
  out-of-position, exit 2, measured §7).
- `⟨0⟩`: the runners' leaf `runNDFuel_zero`/`runND1Fuel_zero`/
  `runND1TraceFuel_zero` (kept, axiom-gated); the CLI refuses `--fuel 0`
  (exit 2, §7); the exemplar's `exemplar_certified_shipped_zero` and
  `exemplar_killed_at_one` state the kills at 0 and 1.
- The deleted names are gone (§3); the generated `_zero` lemmas for the
  driver family exist (N7 line cites).

## 5. The three obligations (scope item 4)

Statements, generated (`Ctype_auxiliary.lean:45`, `Core_auxiliary.lean:65`,
`Defacto_memory_aux_auxiliary.lean:37`), verbatim for the first:

```
theorem ctypeEqual_measure_sufficient (c : ctype) (c0 : ctype) (lemFuel : Nat) (lemMeasureLe : (ctype.lemSize c) ≤ lemFuel) :
    ctypeEqual_lemFuel lemFuel c c0 = ctypeEqual c c0 :=
  Ctype_lemMeasureProofs.ctypeEqual_measure_sufficient c c0 lemFuel lemMeasureLe
```

— exactly the fuel-measure contract (`f_lemFuel lemFuel xs = f xs` under
`μ ≤ lemFuel`, lem-lean fuel-measure record §2.2), the auxiliary shells
`import <Module>_lemMeasureProofs` (`:6` in each) and apply the constant
at the stated type (a missing/mistyped theorem fails the build; the
roots gate makes the shells un-skippable, §6). The hand-written proofs
(`lean_frontend/{Ctype,Core,Defacto_memory_aux}_lemMeasureProofs.lean`)
each prove the universally quantified stability lemma `∀ c c0 f g, μ c ≤
k → μ c ≤ f → μ c ≤ g → W f c c0 = W g c c0` by induction on `k`, then
instantiate `g := μ c` — ALL inputs, no special case; tactics are
`cases`/`rcases`/`obtain`/`simp only`/`rw`/`omega`/`congr`/`exact`/
`apply`/`rename_i`; `grep` for `maxHeartbeats|maxRecDepth|native_decide|
decide := native|bv_decide|sorry|admit|ofReduce` over the three files,
the tests and the seams: no hits (`CerbCall.lean:83` and
`CerbMem.lean:1999` are comments). Axiom cones (unit lane FUEL leg and my
probe, identical):

```
'Ctype_lemMeasureProofs.ctypeEqual_measure_sufficient' depends on axioms: [propext, Classical.choice, Quot.sound]
'Core_lemMeasureProofs.eq_core_base_type_measure_sufficient' depends on axioms: [propext, Classical.choice, Quot.sound]
'Defacto_memory_aux_lemMeasureProofs.fake_mem_value_eq_measure_sufficient' depends on axioms: [propext, Quot.sound]
'ctypeEqual_measure_sufficient' depends on axioms: [propext, Classical.choice, Quot.sound]
'eq_core_base_type_measure_sufficient' depends on axioms: [propext, Classical.choice, Quot.sound]
'fake_mem_value_eq_measure_sufficient' depends on axioms: [propext, Quot.sound]
```

Sufficiency of a FIRST-argument measure, argued against the sources:

- `ctypeEqual` (`ctype.lem:135-179`; worker `generated/Ctype.lean:438`):
  recursion in `Array0/Array0` (child `ty1` of `c`), `Function/Function`
  (return type `ty1` of `c`, and `paramsEqual` over `List.zip params1
  params2` — every pair's FIRST component is a member of `c`'s parameter
  list; zip truncation only drops pairs, never introduces one whose first
  component is outside `params1`), `FunctionNoParams`, `Atomic`; every
  other arm is fuel-free. `ctype.lemSize (Ctype _ ty) = 1 + ctype_.lemSize
  ty`, with `Function (_, x1) x2 _ ↦ 1 + lemSize x1 + lemSize_aux1 x2` and
  `lemSize_aux1 ((_, x1, _) :: xs) = 1 + lemSize x1 + lemSize_aux1 xs`
  (`Ctype.lean:298-312`), so each recursive argument's size is < the
  parent's and the counter `lemSize c` is decremented exactly once per
  level: the first argument's size bounds the depth regardless of `c0`
  (the worker only ever recurses on pairs whose first component descends
  from `c`; a longer `c0` is truncated by the zip, a shorter one ends the
  `all`). The proof's `ctype_param_lt` + `List.of_mem_zip` is exactly
  this argument.
- `eq_core_base_type` (`core.lem:59-89`; worker `Core.lean:215`):
  `BTy_list` (direct child) and `BTy_tuple` via `listEqualBy (W f) bTys1
  bTys2` — `listEqualBy` (`LemLib.lean:239-242`) recurses on both lists in
  lockstep, so every recursive call's first argument is a member of
  `bTys1`; `core_base_type.lemSize_aux1` gives the strict decrease. The
  proof's `listEqualBy_congr` quantifies over members of the FIRST list
  only — sufficient.
- `fake_mem_value_eq` (`defacto_memory_aux.lem:27-35`; worker
  `Defacto_memory_aux.lean` via `Defacto_memory_types.lean:693-714`): the
  one recursive arm `MVarray/MVarray` over `List.all (lemListZip mvals1
  mvals2) (uncurry (W f))` — same zip argument; `impl_pointer_value.lemSize_aux3`
  is the list helper; the `error` arm is a `failwithI` leaf (no fuel).

Kernel computability of the measured wrappers (probe, §7): `decide` and
`rfl` close closed instances (`ctypeEqual cArr cArr = true := rfl`,
`eq_core_base_type (BTy_tuple [BTy_unit, BTy_boolean]) (BTy_tuple
[BTy_unit]) = false := by decide`, …), `#eval` prints `true`/`false`/`4`,
and `#check @ctypeEqual : ctype → ctype → Bool` shows no `[LemFuel]`; the
`Eq0 ctype` instance is fuel-free (`Ctype.lean:447-449`).

## 6. Gates (scope item 5) and lanes (scope item 6)

Run by me in the branch worktree after `=== DONE`, serially, `SKIP_BUILD=1
CERB_MEM_MAX=16G TMPDIR=<audit .tmp>`, via `scripts/ce`; freshness first:

```
check_driver_fresh: oracle OK (bin bc1bee7b9e1e2fb21d825e1929b2fbaeedf3a8735847764abb67ca71d6d06a13, src 7c9a3b9dbc6d1cbc124dd986ef3239d5159456128138b3df116e1e77bd210b09)
check_driver_fresh: lean OK (bin 444f1c285e398e025b3b1d080619c29902dda332612d5d2d4454199264451f69, src cea37e5754f6ba308c26ccd09ac068dc9c881b70a8a281ce9841fc57cfce3d37)
```

`./scripts/test_unit.sh` → rc 0. Verbatim lines:

```
✓ effects-proof-test PASSED
✓ totality-proof-test PASSED
✓ core-parser-test PASSED
✓ fresh-int-test PASSED
✓ pp-test PASSED
✓ fuel-exemplar-test PASSED
Total: 6 passed, 0 failed
check_exec_purity: CLEAN (11 modules)
check_theorem_axioms: generated-tree census OK (196 files: 0 axioms, boundary-opaque population = the 26 registered rows exactly-once (incl. CerbFuel.fuelExhaustedLoc), 0 unsafeCast)
check_theorem_axioms: C2 ratchet OK (301 files scanned recursively: 0 axioms, 0 runEffectful, seam population = the 66 pinned path-qualified counted rows exactly incl. the extern class; lem tests/ scaffolds asserted outside the surface)
check_theorem_axioms: C2 entry census OK (9 entries, every cone ⊆ [propext, Classical.choice, Quot.sound])
'FuelExemplar.exemplar_killed_at_one' depends on axioms: [propext, Classical.choice, Quot.sound]
'FuelExemplar.exemplar_certified_shipped_forall' depends on axioms: [propext, Classical.choice, Quot.sound]
check_theorem_axioms: FUEL arc leg OK (34 contract lemmas — 9 generated _zero + the CerbND runner leaves/parametricity pins + the ∀-fuel exemplar and its instances + the 3 fuel_measure sufficiency obligations (generated statement + hand-written proof), every cone ⊆ [propext, Classical.choice, Quot.sound])
check_theorem_axioms: OK (effect-retirement C2 bar: zero axiom declarations anywhere; entry cones ⊆ the standard three)
check_sorry_token: OK (262 files scanned comment-stripped — generated 196, hand-written+test 32, LemLib 34; 0 sorry tokens)
test_fuel_classifier: 18 fixtures, ALL OK
check_no_fuel_numerals: SELFTEST — planting F1-F6 into a scratch copy of the scan set (loud plant banner; nothing in the tree is touched)
  PLANT OK   [F1 deleted default named in code] -> check_no_fuel_numerals: FAIL (F1): fuel numeral shape found:
  PLANT OK   [F1 deleted driverFuel in a seam] -> check_no_fuel_numerals: FAIL (F1): fuel numeral shape found:
  PLANT OK   [F2 global instance] -> check_no_fuel_numerals: FAIL (F2): fuel numeral shape found:
  PLANT OK   [F2 global instance (where)] -> check_no_fuel_numerals: FAIL (F2): fuel numeral shape found:
  PLANT OK   [F3 worker at a literal fuel] -> check_no_fuel_numerals: FAIL (F3): fuel numeral shape found:
  PLANT OK   [F3 parenthesised literal] -> check_no_fuel_numerals: FAIL (F3): fuel numeral shape found:
  PLANT OK   [F4 LemFuel.mk numeral] -> check_no_fuel_numerals: FAIL (F4): fuel numeral shape found:
  PLANT OK   [F4 letI outside Main] -> check_no_fuel_numerals: FAIL (F4): fuel numeral shape found:
  PLANT OK   [F5 anonymous-constructor literal] -> check_no_fuel_numerals: FAIL (F5): fuel numeral shape found:
  PLANT OK   [F6 fuel-named numeral constant] -> check_no_fuel_numerals: FAIL (F6): fuel numeral shape found:
  PLANT OK   [F6 in a speclab gate test] -> check_no_fuel_numerals: FAIL (F6): fuel numeral shape found:
  PLANT OK   [F6 in a generated copy of Main] -> check_no_fuel_numerals: FAIL (F6): fuel numeral shape found:
  PLANT OK   [F5 in an immaculate Lean probe] -> check_no_fuel_numerals: FAIL (F5): fuel numeral shape found:
check_no_fuel_numerals: SELFTEST OK (13 plants red with the declared label; unplanted set green)
check_no_fuel_numerals: OK (267 files scanned comment-stripped; no lemDefaultFuel/driverFuel/ndDefaultFuel, no LemFuel instance, no literal fuel (F1-F6); allowed Main.lean sites seen: 4 of 4 (hand-written + generated copy))
check_lakefile_roots: SELFTEST — planting on a scratch copy of lakefile.toml (loud plant banner; nothing in the tree is touched)
  PLANT OK   [dropped root Core_aux_auxiliary] -> check_lakefile_roots: FAIL — generated module(s) NOT a Lake root (their obligations would never build):
  PLANT OK   [phantom root Phantom_auxiliary] -> check_lakefile_roots: FAIL — Lake root(s) with no generated module:
  PLANT OK   [unrooted generated Orphan_auxiliary] -> check_lakefile_roots: FAIL — generated module(s) NOT a Lake root (their obligations would never build):
check_lakefile_roots: SELFTEST OK (3 plants red, baseline green)
check_lakefile_roots: OK (195 roots = 195 generated modules + the exe root Main; 85 auxiliary modules all built)
check_exec_totality: CLEAN (22 generated modules + hand-written CerbND, 0 allowlisted)
check_lem_sync: OK (src 03c176935c3e37a0f5b9a00192796ddf42dd6bd09ebf3bb3a41c028c25f8f10c, gen 295e4f8291c9ffd57a4061dd38e8ec273f18d6c1cfe3a0465291f1a4bcff8100)
check_lem_sync: lean OK (src 03c176935c3e37a0f5b9a00192796ddf42dd6bd09ebf3bb3a41c028c25f8f10c, gen d1931404d27d3004cd8305c3d2d7fea770f3da3de075d9dbe168a513f3280ecb)
check_fork_drift: OK — layer 1: 71 oracle-surface files = manifest; layer 2: 22 differing generated files, all hash-pinned (merge-base b9aeedcb4dd438763b0eef7f95ac19e93875d7de)
check_fixture_freeze: OK (16 fixture files match the pinned manifest; name set exact)
test_renumber_plants: OK (12 plants: refusals refuse, admits admit with declared class)
```

`./scripts/test_verify.sh` → rc 0: `test_verify: 127 passed, 0 failed (25
fixtures, 28 call points, 14 corpus fixtures, 21 corpus points)`.

`./scripts/test_immaculate.sh` → rc 0: `  KILL           illtyped-store`
(the in-Lean probe at `--fuel $CERB_TEST_FUEL`) and `OK: lane matches the
committed baseline (MATCH except the ISO-fix register pins R1
g5-decode-question/zd-e2-ptr-string-literals ORACLE_CRASH, R2
g5-escape-roundtrip DIFF, R3 s4b-memcmp-hugesize ORACLE_CRASH —
VALIDATION.md 'ISO-fix register' — and the in-Lean probes g6 TRIPWIRE /
illtyped-store KILL).`

Gate-design reads: `check_lakefile_roots.sh` parses the `CerberusLean`
lib's `roots` and compares both directions against `generated/*.lean`
minus `Main` (`MIN_ROOTS=150` vacuity guard) — an `_auxiliary` shell
cannot be dropped silently. `check_theorem_axioms.sh:838-852` lists 34
names; the leg fails closed on any name that does not produce exactly one
`#print axioms` line (`:866-873`) and on any axiom outside the allowlist
(`:874-891`). `test_fuel_plant.sh:145-190`: the real driver through a
`CERB_LEAN_BIN_OVERRIDE` stub splicing `--fuel 1` after the mode flag,
asserted fresh first; accepts `FUEL:(kill|panic), exit (1|134)`; the
default → MATCH; `--fuel 0`/`abc` exit 2; out-of-position and missing
argument; the `else` branch (override already set) sets `fail=1`. The
`FuelExemplar` rewrite: no `set_option` in the file; the div/mod finding's
workaround is `theorem alignofIval_signed_int (k) : @CerbMem.alignofIval
⟨Nat.succ k⟩ fmapEmpty signed_int = CerbMem.integerIval 4 := rfl` + one
`rw` — a kernel-checked rewrite, not an axiom; cones above. Totality
Part 1: M1.

Tier B, cited from the orchestrator's battery log
(`.tmp/c1-reverify.log`, not re-run by me), verbatim lines: `test_verify:
127 passed, 0 failed (…)`; immaculate `OK: lane matches the committed
baseline (…)`; `test_hang_plant: all plants read as expected (…)`;
`test_kill_plant: all plants read as expected (…)`; `test_fuel_plant: ALL
PLANTS OK (FUEL classification live in exec/gcc/ci_sweep/cn_coverage/measure;
negatives not FUEL; the real driver at --fuel 1 reads FUEL and at the
default MATCH; --fuel 0/non-numeral/out-of-position/missi…`; libxml2
`SUMMARY: total=4 match=4 fail=0 (points: 1354, 22 observations each)` /
`ALL PASSED`; parse/core `tests/ci` `Lean parse:     128 ok, 0 failed` /
`ALL PASSED`; gcc `SUMMARY: total=1963 compared=1885 agree=1873 agree_nd=0
triaged=12 disagree=0 o2_agree=190 …` / `Baseline check: 0 regression(s),
0 improvement(s)` / `gcc second-oracle lane OK`; every `--- rc=0`; the log
ends `=== DONE`. Tier A rows 2–11 in the same log all `BASELINE OK` /
`ALL PASSED` (e.g. `SUMMARY: total=106 match=85 ub_match=18 ub_diff=0
mismatch=0 fail=0 crash=0 fuel=0 …` / `Baseline check: 0 regression(s), 0
improvement(s)`).

## 7. Driver and kernel probes (scope items 3, 7)

The real binary (`lean_frontend/.lake/build/bin/cerberus-lean`, stamp
above) on the oracle's cabs-json of `tests/minimal/001-return-literal.c`,
`LEAN_ABORT_ON_PANIC=1` unless stated (arguments are separate argv
tokens; paths elided):

```
== [--batch min.json] rc=0: Defined {value: "Specified(42)", stdout: "", stderr: "", blocked: "false"}
== [--batch min.json --fuel 1] rc=134: lem: fuel exhausted|backtrace:
== [--batch min.json --fuel 2] rc=134: lem: fuel exhausted|backtrace:
== [--batch min.json --fuel 3] rc=134: lem: fuel exhausted|backtrace:
== [--batch min.json --fuel 0] rc=2: cerberus-lean: refused — --fuel 0: the fuel must be a positive integer (fuel 0 kills at the first bind: never a verdict; see VALIDATION.md, fuel)
== [--batch min.json --fuel abc] rc=2: cerberus-lean: refused — --fuel abc: not a decimal numeral (the fuel is a positive integer; default 100000000; see VALIDATION.md, fuel)
== [--batch min.json --fuel 0x10] rc=2: cerberus-lean: refused — --fuel 0x10: not a decimal numeral (the fuel is a positive integer; default 100000000; see VALIDATION.md, fuel)
== [--batch min.json --fuel 1000000000] rc=0: Defined {value: "Specified(42)", stdout: "", stderr: "", blocked: "false"}
== [--fuel 5 --batch min.json] rc=2: cerberus-lean: refused — --batch: known flag out of its canonical position (`--batch`, `--pp-core` or `--parse-core` must be argv[0]; `--first` must immediately follow `--batch`/`--pp-core
== [--batch min.json --fuel] rc=1: cerberus-lean: --libc/--libc-tu/--call/--call-args/--args/--fuel require an argument
== [--fuel 1, LEAN_ABORT_ON_PANIC unset]
rc=2 : cerberus-lean: refused — LEAN_ABORT_ON_PANIC is not set: a Lean panic! (this port's fail-stop mirror of every OCaml failwith/assert/uncaught exception) would
== [--parse-core 001-return-literal.c --fuel 5]
rc=2 : cerberus-lean: refused — --fuel: unknown flag; this port accepts only --batch | --pp-core | --parse-core (argv[0]), --
```

Matches the record §4.5 row for row (the fuel 1–3 exhaustion is the
front end's panic form — record F-C1-4 — and cannot reach a verdict
because the binary refuses to run without abort-on-panic).

Kernel probe (`.tmp/AuditProbe.lean`, `capped lake env lean`, 16G cap),
verbatim output beyond the cones quoted in §5:

```
'FuelExemplar.exemplar_certified_shipped_forall' depends on axioms: [propext, Classical.choice, Quot.sound]
'FuelExemplar.exemplar_killed_at_one' depends on axioms: [propext, Classical.choice, Quot.sound]
'CerbND.runND_eq' does not depend on any axioms
ctypeEqual : ctype → ctype → Bool
eq_core_base_type : core_base_type → core_base_type → Bool
fake_mem_value_eq : impl_mem_value → impl_mem_value → Bool
inferInstance : Lem_Basic_classes.Eq0 ctype
true
false
4
@CerbND.runND : {a info err cs st : Type} →
  [LemFuel] → ndM a info err cs st → st → List (nd_status a err st × List String × st)
@drive : [LemFuel] →
  Fmap sym (CerbLocation.Loc × tag_definition) →
    Bool →
      generic_file Unit core_run_annotation →
        List String → ndM driver_result step_kind driver_error (mem_constraint CerbMem.IntegerValue) driver_state
```

(the nine `example … := by decide` / `:= rfl` closed instances elaborated
without error; `#eval ctypeEqual cArr cArr` → `true`, `#eval ctypeEqual
cArr cPtrInt` → `false`, `#eval ctype.lemSize cArr` → `4`). The
manifest's binder-order claim for `runND` (`{a info err cs st} [LemFuel]`)
is confirmed by the `#check`.

## 8. Record/manifest integrity (scope item 7)

- `[LemFuel]` surface vs code: the manifest §2 rows checked against
  `CerbND.lean:373-408` (`driver2_wrapper_defeq (n) : @driver2 ⟨n⟩ =
  @driver2_lemFuel ⟨n⟩ n`, `nd_bind_wrapper_defeq`, `liftND_/liftAction_`,
  `runND_eq/runND1_eq/runND1Trace_eq` with the stated binder orders),
  `CerbMem.lean` (the 12 wrappers + 4 workers + the 21 further entries
  carry `[LemFuel]`; 40 binders, DERIVED), `CerbCall.lean:258,274,302`,
  `Main.lean:563,663,791`, speclab (34 constructors/runners, `git diff`),
  `illtyped-store.lean`/`Micro.lean` (`--fuel N` required, exit 2 / return
  2 otherwise). Consistent, modulo M3.
- Deleted-names list: consistent with the code (§3).
- The measured trio "fuel-free, kernel-computable": verified (§5, §7).
- csmith premise correction (record F-C1-5): `scripts/exec_csmith_corpus_baseline.txt:1177`
  `sia_csmith_477.c TIMEOUT`, `:1469` `sia_csmith_769.c MATCH`;
  `scripts/gcc_oracle_baseline.txt:1147/1439` `AGREE` — as stated; the §6
  measurement is not re-run here.
- Ratchet re-pin reasons (record §7): the only pinned LIST that changed is
  `FUEL_THMS` (deletions ordered by the brief + the obligations);
  populations 26/66/9/22/71/16 unchanged — matches the unit lane (§6).
- WIP branch `wip/fuel-parameter-C1-scratch` = `b0f718eda` exists, its
  message says "WIP — NOT GATED, NOT MERGEABLE … Kill-loss containment
  only"; referenced as history only. Two-repo invariant: lem-lean
  `mdd/lean-backend` = `deps/lem-pinned` = `ecf75b4`; all three
  `lake-manifest.json` `rev` = `ecf75b4172b1f5b9838eb8bcda5928c5b05dee9b`.
- Branch worktree `git status --short` empty at audit time.
- Record integrity slips: N1, N2, N7 (derived count).

## 9. What I did not check

- Tier B lanes beyond `test_verify.sh`/`test_immaculate.sh` (cited from
  the orchestrator's log, not re-run); Tier A rows 2–11 likewise.
- The record §6 csmith re-runs at 10^9 (not re-run).
- The lem-lean side (`ecf75b4`) — audited separately
  (`2026-09-04_d2-enablers-audit-premerge.md`); I took its generated
  output as given and checked only what it produced here.
- The mem-scale sweep and the csmith shards (Tier C; not asked, not run).
- refined-cerberus consumption of the manifest (not in scope).
- Bisectability of the five commits (the record §12 says the intermediate
  trees are not individually buildable; not tested).
- Perf (record §6 reports "costs nothing measurable"; not re-measured).

## 10. Provenance

[AGENT] (auditor): every finding, grade, measurement and recommendation
above. [USER 2026-09-03/04] rulings are quoted as relayed in the brief and
the record §1; no decision here is the user's. Scratch: the audit
worktree's gitignored `.tmp/` (`AuditProbe.lean`, `plant_evasions.sh`,
lane logs, `min.json`) — ephemeral, not committed.
