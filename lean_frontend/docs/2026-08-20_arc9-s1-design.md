# Arc 9 / S1 — THE PLAN: the WP-workbench tactic-set design

Date: 2026-08-20. Provenance: [AGENT:S1] throughout; operator inputs
marked [USER]. Charter: `docs/2026-08-20_arc9-wp-tactics-charter.md`
(S1 = this design doc; NO tactic code this slice — none written).
Designed against the BUMPED iris pin `34390a0133986385c62bf59a6eb01938945b48ec`
(D1, decision log; verified in `lean_frontend/lakefile.toml:30`).

Inputs (all read in full for this slice): the S0 survey
(`docs/2026-08-20_arc9-s0-survey.md` — gap matrix G1-G12 §4, OwnP map
§1.3, brick-wp taxonomy §2, golean walker/lessons §3, T1-T4 baseline
§5, parked questions §6), the arc-7/8 proof layer
(`relsem/RelSem/{Machine,FuelHooks,IrisLang,IrisState,IrisRules,
IrisAdequacy,SlateWP,Call,RunND,T1,T1AppEq,T4AppEq,...}.lean`), the
donor sources at file:line (brick-wp `theories/WpTactics.v`, golean
`proofs/GoLeanProofs/Tactics/GoWalk.lean`, iris-lean
`Iris/Iris/ProgramLogic/OwnP.lean` at the bumped rev), and the pinned
T5 fixture (`tests/verify/t5_sum.{c,core}`).

Design-only discipline: every artifact below is NAME + CONTRACT
(goal shape in → goal shape out, failure behavior), no code. Statement
sketches are design notation, not source. No elaboration probes were
run this slice: each feasibility claim below is instead anchored to an
ALREADY-PROVED in-tree lemma of the same shape (cited inline); the two
genuinely open feasibility points are declared as S2 probes P1/P2 with
fallbacks (§8), not assumed.

---

## 0. Design summary (one screen)

* **Layering**: L0 = iris-lean consumed as-is (post-bump; OwnP adopted,
  ~380 lines of hand-rolled twin retired, §1.1); L1 = the Cerberus
  step layer — six LEMMA KITS at Layer 2 (app-equation land, no Iris),
  extracted from the T1-T4 hand proofs golean-style and generalized to
  COMPUTED-RHS round lemmas (§1.2); L2 = a goal-guarded, law-table
  app-equation walker (`app_walk` quartet) in the golean `go_walk`
  architecture with brick-wp's dispatch discipline (§1.3).
* **The loop rule** (G1, T5's anchor): a PURE Nat-induction composition
  lemma `iter_compose` over fuel-offset app-block equations + a
  per-fixture iteration block proved by the walker; exit and post-loop
  composed OUTSIDE the induction (brick-wp `wp_frame_exit` shape).
  No new axioms; the conclusion is an ordinary app equation consumed by
  the UNCHANGED adequacy chain (§2).
* **Transcription kill**: computed-RHS round lemmas + walker RHS
  synthesis eliminate the intermediate-configuration def layer and the
  hand `.trans` chains; acceptance = golean's form, calibrated by
  re-proving T1's dnms segment (§3).
* **Proof-size bar**: per slate example, the counted proof file ≤ 250
  lines and ≤ 40 manual (non-walker) proof steps, all-semantic;
  measured by a gate script; kit files fixture-free by grep-gate
  (mega-lemma counter) (§4).
* **Slate**: T5 sum-loop, T6 nested loops, T7 array fill+sum, T8 early
  exit, T9 two-function call composition, T10 chvalid cameo (stretch,
  park-clause) (§5).
* All seven parked S0 questions are ANSWERED (§7); one operator-flag
  section (§9) — nothing requiring a stop under the charter's
  autonomy protocol.

---

## 1. The tactic/lemma set

### 1.1 L0 — iris-lean primitives consumed as-is (post-bump)

The bump to `34390a0` landed and re-gated in D1, so L0 is designed
against head. Consumed WITHOUT modification:

| artifact (iris-lean cite) | role here |
|---|---|
| the IPM tactic set (`Iris/ProofMode/Tactics/`, survey §1.2) | all entailment plumbing in Layer 3; unchanged from arc 7 |
| `wp_value'` (WeakestPre.lean) | `wp_done` stays a one-line reuse (IrisRules.lean:43) |
| `OwnPGS`/`OwnPGpreS` (OwnP.lean:23/:31) | replace `CerbGS`/`CerbGpreS` |
| `ownP` (OwnP.lean:58) | replaces `stateIs` |
| `ownPG_irisGS` (OwnP.lean:48) | replaces `instStateInterpDrive` + `instIrisGSDrive` |
| `ownP_eq` (OwnP.lean:107) | replaces `stateIs_agree` |
| `ownP_timeless` (OwnP.lean:121) | the `▷`-strip the survey priced (§1.3) |
| `ownP_lift_atomic_det_step_no_fork` (OwnP.lean:225) | the lifting shell under `wp_app_active/_killed` |
| `ownP_adequacy` (OwnP.lean:64) | replaces `callAdequate_of_wp` leg 1's alloc/split/defeq-bridge dance |
| `adequate` + `adequate_result` (Adequacy.lean) | leg 2, unchanged |
| `iloeb`, `bi_least_fixpoint` (survey §1.2) | AVAILABLE, deliberately unused (G1 verdict: no WP traffic at our granularity); recorded so the reuse-honesty audit sees the decision, not an omission |
| `big_sepM2` (head, #582) | AVAILABLE, not planned; noted for struct-field resource bundles if a Q4-granularity future wants them |

**The OwnP adoption plan (Q1 answered: ADOPT WHOLESALE, S2-early, own
commit).** Signature-by-signature disposition of the ~380-line
hand-rolled twin (survey §1.3 map, verified against
IrisState/IrisRules/IrisAdequacy as read this slice):

RETIRES (replaced by the L0 rows above):

* `CerbGpreS`, `CerbGS` (IrisState.lean:48-64) → `OwnPGpreS
  driver_state GF`, `OwnPGS driver_state GF`.
* `stateIs` (IrisState.lean:71) → `abbrev stateIs σ := ownP σ` during
  S2 (so `wp_callND`, `SlateWP`, and the T1-T4 route files re-elaborate
  without textual churn), the abbrev deleted and call sites renamed at
  S4 consolidation.
* `instStateInterpDrive` (IrisState.lean:75), `instIrisGSDrive`
  (IrisState.lean:83) → `ownPG_irisGS`.
* `stateInterp_eq` (IrisState.lean:94) → gone; the interpretation
  shape is the library's, and its unfolding happens inside the OwnP
  lemmas.
* `stateIs_agree` (IrisState.lean:99) → `ownP_eq`.
* `stateIs_update` (IrisState.lean:104) → gone: the ghost update is
  performed inside `ownP_lift_step` (OwnP.lean:145, `iOwn_update_op`);
  no rule of ours hand-rolls it anymore.
* `wp_app_active`/`wp_app_killed` bodies (IrisRules.lean:52-130, ~40
  IPM lines each) → SAME SIGNATURES, re-proved as ~10-line wrappers:
  `ownP_lift_atomic_det_step_no_fork` with `Hsafe :=
  reducible_of_app_active/killed h` (RelSem/IrisLang.lean:104, already
  proved) and `Hdet := step_running_active/killed_inv h`
  (Machine.lean:561/:581 — the survey's observation that our
  determinism inversion IS OwnP's premise, verified: `Hdet` wants
  `σ₂' = σ₂ ∧ toVal e₂' = some v₂ ∧ eₜ' = []`, which is exactly the
  configuration equality those inversions produce, split by
  `injection`, plus `primStep_inv`'s empty-forks component
  (IrisLang.lean)). The `▷` on the triple's precondition is supplied
  by later-intro from `ownP σ` (numLatersPerStep = 0 both sides;
  `ownP_timeless` available if a strip is ever needed on the
  consuming side).
* `callAdequate_of_wp` LEG 1 (IrisAdequacy.lean:55-87: the
  `ghost_var_alloc` + `Qp.half_add_half` split + the
  `irevert/istop/entails_wand` instance-defeq bridge) →
  `ownP_adequacy` applied at `DriveExpr`. Leg 2 (the
  `callOutcomes_sound`/`steps_erased`/`adequate_result` composition,
  IrisAdequacy.lean:89-90) is Cerberus-specific and UNCHANGED.

STAYS (Cerberus-specific, no upstream analog):

* `RelSem/IrisLang.lean` whole (the `Language DriveExpr driver_state
  Empty DriveVal` instance, `primStep_inv`, `steps_erased`) — the
  coupling itself.
* `CerbS` (IrisState.lean:114) — REWORKED, not retired: index 4 swaps
  `GhostVarF driver_state` → `ownPRF driver_state` (= `constOF
  (ExclAuthR (stateO driver_state))`, OwnP.lean:20) and
  `instCerbGpreS_CerbS` becomes the `OwnPGpreS driver_state CerbS`
  witness (`ElemG` at index 4). iris-lean ships no closed bundles;
  a concrete GF for the final discharge stays ours.
* `wp_callND`/`wp_callND_killed` (IrisRules.lean:146-174) — thin
  instantiations at `callND`, kept verbatim (they are two-line
  applications of `wp_app_active/_killed`).
* `wp_done`, all of `SlateWP.lean`, the ExecModel-shaped corollaries
  (`callHarnessAdequate_of_wp`, `callUBFree_*`), and everything at
  Layer 2 — untouched.

Expected line effect (derived, from the files' measured sizes 134 +
177 + 145): the ~380 lines the survey scoped shrink to ~150
(bundle + wrappers + call instantiations); the qualitative win is
bigger than the count — the two 40-line IPM lifting proofs and the
adequacy defeq-bridge (the three most delicate hand artifacts in
Layer 3) become library calls.

Gating: the adoption is S2's FIRST commit after a capped probe of the
two priced risks (the `ElemG` witness at the reworked `CerbS`; the
texan-triple → wand-form bridge in the wrapper). Fallback (pre-declared,
survey §1.6 pattern): if the probe shows nonlocal breakage, the
adoption is parked with the probe transcript recorded and the arc
proceeds on the arc-7 layer — every L1/L2 artifact below is
Layer-2-side and INDEPENDENT of this choice (the app-equation interface
`app … = (NDactive v, σ')` is the boundary, and both state layers
consume it identically).

### 1.2 L1 — the Cerberus step layer (lemma kits, Layer 2, no Iris)

Provenance tags used below: [IN-HOUSE] = exists arc-7, promoted;
[KIT-EXTRACT] = golean s1/s2 lift-wave pattern (survey §3.3: extract
from consumers, pin, delete duplicates); [BRICK-B<n>] = design lifted
from the survey's §2.1 taxonomy row; [NEW] = build-new per gap-matrix
row cited.

The load-bearing design move (the transcription killer, Q4): round
lemmas are restated with a **COMPUTED RHS** — the post-state is a
FUNCTION of the pre-state and the round's decomposition hypotheses,
never a per-fixture transcribed term. In-tree feasibility evidence
that hypothesis-mediated generic stepping elaborates at default
budgets: `aux2_step`/`aux2_done` (T1AppEq.lean:718-756 — generic in
pe/env/mem/file with `hpull`/`hstep`/`hnv` crossings),
`liftCore_run_defined` (T1AppEq.lean:895-904), `perform_unfold`/
`ars_load_unfold` (T1AppEq.lean:579-600), `eubind_defined`/
`stub_defined` (T1AppEq.lean:648-656), and the whole fuel-generic
bind/lift family (Machine.lean:302-401). What is NOT yet evidenced is
generic reduction through `dnms`'s redex discovery (`get_ctx`) with a
symbolic continuation — S2 probe P1 (§8), fallback: shape-indexed
lemma families (one lemma per arena-context shape, still
fixture-generic).

**Kit/AppEq.lean** [IN-HOUSE, re-export surface] — the monadic spine,
already proved in Machine.lean and left there (it is audited);
this file only re-exports + simp-tags: `app_nd_return`, `app_kill`,
`app_nd_get/put/update/read`, `app_print_debug`, `app_nd_guard_true/false`,
`app_pick_singleton` (Machine.lean:435-482), `app_bind_active/_killed`
(:314/:502), `app_liftND_active/_killed` (:362/:394), `app_ifM`,
`app_addConstraints` (:215/:221).
Contract (all): closed `app`-computation equations, `rfl`-provable,
registered `@[app_eq]` for the walker.

**Kit/Mem.lean** [KIT-EXTRACT + BRICK-B1/B3; gap G4] — the memory-op
block kit at MemState level (pure, no driver types). Extracted from
T1AppEq (allocErr_eq/storeErr_eq/loadX_eq/reconX_eq/roundtrip_arith +
the bmD3 fetch family) and T4AppEq (createS/storeU/storeA/storeB/
loadV/loadA + layout facts), generalized:

* `mem_alloc_block (tid pref align ty addr aid) (hlast : …) (halign : …) :
  app (CerbMem.allocateObject …) mem = (NDactive (mkPtr aid addr), memAfterAlloc mem …)`
  — the fresh-allocation equation with the address/id computed from
  the pre-state fields; named hypothesis args in the brick-B3
  convention (each `h*` a small closed fact the walker discharges by
  `rfl`/`simp [app_norm]`).
* `mem_store_block (ty ptr mval) (hget hbounds hcompat hatomic hbytes) :
  app (CerbMem.storeM …) mem = (NDactive (FP .W a n), memStoreAt mem a bytes)`
  — where `memStoreAt` spells the bytemap in the COLLAPSED canonical
  form (Q3, §7): same-key inserts collapsed, one insert per byte
  address over the canonical base.
* `mem_load_block (ty ptr) (hget hbounds hatomic hdead hbytes hrecon) :
  app (CerbMem.loadM …) mem = (NDactive (FP .R a n, mval), mem)`.
* `bytes_int_roundtrip (w : width) (x) (h1 h2) :
  bytesToInt (intBytes w x) signed = some x` — `roundtrip_arith`
  (T1AppEq.lean:243) generalized over the byte width family actually
  used by the slate (4-byte now; the statement is width-indexed so T7's
  array elements and any future `char`/`long` reuse it).
* `recon_int_block` — `reconX_eq`'s generic form (fuel by the
  literal-successor jump, as now).
* `bytemap_insert_collapse (k v v' m) : (m.insert k v').insert k v = m.insert k v`
  and the `readBytesFrom`-over-inserts fetch lemmas — the TreeMap
  normal-form maintenance family (Q3). At concrete keys these are
  kernel computations; the collapse lemma is the one PROPOSITIONAL
  fact (S2 probe P2 checks whether `Std.TreeMap` gives it structurally
  or via its lawful-map API; either lands in this kit).
* The layout-fact family: `sizeof_fact`/`alignof_fact`/`shift_fact`
  schemas with the `htags` hypothesis convention (T4AppEq.lean:31-67
  pattern — the tag-global hypothesis stays a hypothesis, brick-B13).

Failure behavior (kit-wide): a lemma whose side hypothesis does not
discharge by `rfl`/`app_norm`-simp is NOT applied by the walker — the
hypothesis surfaces as a goal only under explicit `app_walk_step`
(§1.3), mirroring golean's "a tactic that guessed at those would be
guessing at the semantics" (GoWalk.lean:52-56).

**Kit/Round.lean** [NEW, computed-RHS; gaps G4/G6; brick-B3/B5 shapes]
— the driver-round layer: fused step_ctx+advance equations over
`drive_nonmemory_steps_aux2_lemFuel` (`dnms` generically: symbolic
`tagDefs`, `acc`, `tids`, frame-parametric in mem/rs/trace/counter as
T1's round lemmas already are, T1AppEq.lean:514-576). One lemma per
ROUND CLASS observed on the slate corpus:

* `round_pure_eval (harena : th.arena ≈ C[Epure pe]) (heval : eval-crossing) :
  app (dnms (fuel+1) …) (mkDrv th mem rs tr n) = app (dnms fuel …) (mkDrv (thUpd th C v) mem rs tr (n+1))`
  — covers T1's round0/round7 class; the eval-crossing hypothesis is
  discharged through Kit/Eval.
* `round_tau_wseq` / `round_tau_sseq` (hmatch : match_pattern pat v = binding-result)
  — the Ewseq/Esseq tau binds (round1/round5 class); RHS env is
  `update_env pat v th.env`, computed.
* `round_tau_strip` — Ebound/Eannot stripping (round4 class).
* `round_load` / `round_store` / `round_create` / `round_kill` —
  action-request rounds: aid draw + `perform_unfold` +
  `ars_*_unfold` + the Kit/Mem block through the `liftMem` lens
  (`app_liftND_active`), trace event consed, counter NOT bumped
  (round3's verified shape). `round_kill` is new (t5's `kill('signed
  int', i)` object-deallocation rounds — present in the pinned t5 Core,
  absent from T1-T4's paths).
* `round_run_jump (hlabel : find_labeled_continuation … = some …) :`
  the Erun/Esave jump (round6's jump component, separated from its
  eval component; G6's early-exit rounds are THIS lemma at the
  `break_500`/`ret_498` labels — t5_sum.core's label structure,
  verified in the pinned dump: `run while_501(i, s)`,
  `save break_500 …`, `run ret_498(…)`).
* `round_unseq` — the one_step_unseq dispatch on `unseq(…)` nodes
  (t5's guard/body use `Ewseq … unseq` heavily; T1-T4 never crossed
  it, but the S0/arc-7 trace evidence covers T5: still one
  bind-collapsed NDactive node, Machine.lean §Coverage-by-need).
* `round_terminal` — the no-can-advance exit returning the
  accumulated steps map (round8 class).
* `driver_iter_exit` / `driver_iter_wakeup` — the `driver2` iteration
  glue (driver2_iter's two scheduling branches, T1AppEq.lean:995-1021,
  generalized: the opaque execution-mode Bool dispatched by cases,
  as now).
* `prefix_block` / `finish_block` — the callND prefix walk
  (prefix_a/prefix_b generalized over file/fname/args: resolution +
  injection + errno via Kit/Mem blocks) and the finalize tail.

Contract (all round lemmas): IN a goal
`app (dnms (fuel+1) acc tids) σ = ?rhs` (or an explicit RHS to be
matched), with σ's thread arena exposing the round's head shape; OUT
the same goal one round later with the state components rewritten by
the computing functions and normalized by `app_norm`; FAIL (lemma does
not apply — wrong head shape, or a side hypothesis that is not
`rfl`/`app_norm`-dischargeable) leaves the goal untouched.

**Kit/Eval.lean** [IN-HOUSE promote + KIT-EXTRACT] — the pure-eval
crossing layer: `eubind_defined`, `stub_defined`, `eumapM_one`,
`liftCore_run_defined`, `aux2_step`, `aux2_done` (all proved,
T1AppEq.lean:648-904 — MOVED here, T1AppEq re-imports), plus:

* `evalconv_int_spec (v ty) (hrange) : eval_pexpr_aux2 … (convPE-shape v ty)
  = Result (Defined (Sum.inr (loaded v)))` — the composed
  conv_loaded_int chain (T1's s0-s4/z0-z4 residual ladder, proved ONCE
  generic in the loaded value; kills the five z-transcriptions per
  fixture). The range check is where the `hrange` hypothesis enters —
  the walker SURFACES it, never proves it (G12 division of labor).
* `evalcmp_int_spec` / `evaladd_checked_spec` — the `<`/`=` comparison
  and `catch_exceptional_condition_add` chains t5/T6-T9's guards and
  bodies evaluate (same ladder pattern; the add's no-overflow
  hypothesis surfaces exactly like T2's).

**Kit/Loop.lean** [NEW; gaps G1/G2] — the loop rule + fuel algebra.
Contents in §2 (the loop story); headline names: `iter_compose`,
`iter_compose_exit` (early-exit variant), `fuel_split_at_default`,
`dnms_fuel_cast`.

**Kit/CallSpec.lean** [NEW; gap G5; BRICK-B4 shape] — the callee-spec
form + composition for T9:

* `structure CalleeBlock (fsym) (pre : args → Prop) (post : args → value → MemDelta → Prop)`
  — the `call_ready`/`wp_fptr_of_spec` analog at app-equation
  granularity: a record packaging "under `pre`, the callee's
  proc-body app-block equation from the ccall-entry configuration to
  the return configuration, with `post` on the result and the memory
  delta".
* `round_ccall_push (hresolve) :` the caller's Eccall round — stack
  push, callee arena installed (NEW round class: T1-T5 contain no
  in-Core proc calls; `conv_loaded_int` etc. are `PEcall` pure calls
  evaluated inline by Kit/Eval).
* `round_return_pop :` the callee's `run ret_*` + stack pop back into
  the caller's context.
* `ccall_compose (cb : CalleeBlock …) (hpre) :` consumes a proved
  `CalleeBlock` at a caller's ccall round, yielding the fused
  block equation — ONE lemma, applied per call site (twice in T9).
  The `_from_hyp` Löb variant (brick WpTactics.v:1915) is
  deliberately NOT designed — no recursion in the slate (survey B4
  verdict: "no traffic until recursion enters the slate").

### 1.3 L2 — automation (the walker)

Architecture = golean's `go_walk` law-table core (GoWalk.lean:1-124),
retargeted from IPM/WP goals to PURE app-equation goals — the
simplification the survey §3.2 predicted: no resource threading, no
modality dance, no `iframe` renames; the hardest parts of golean's v1
do not exist here. Discipline = brick-wp's measured lessons: dispatch
is GOAL-GUARDED by head shape before any apply (WpTactics.v:2276-2282
— ungated failing applies measured "~4 s per failed attempt and 85% of
wp_auto's total runtime"), and the mechanical/semantic division is
absolute (WpTactics.v:2195-2198: the user provides ONLY semantic
steps).

Components:

* `register_simp_attr app_norm` — THE state normal form (Q3): the
  simp set that maintains the canonical spelling after every step
  (update_env unfolding at concrete patterns, TreeMap insert-collapse
  at concrete keys, `mkDrv`/thread-update record normalization,
  List/trace cons forms). Confluence requirement as golean's
  `go_walk_simp` (GoWalk.lean:130-134). Invariant: every registered
  `@[app_eq]` lemma's LHS and RHS are stated IN this normal form —
  brick-B14's lesson ("the wrong choice makes every kit lemma's LHS
  miss") made structural.
* `@[app_eq]` attribute — registers a kit lemma into a DiscrTree
  keyed on its conclusion's LHS `app <computation> <state>` pattern
  (the golean `lawKeys` move, GoWalk.lean:186-195: metavariable
  telescope so lemma variables index as wildcards). Most-specific-first
  candidate order + per-candidate try under saved state with a small
  budget (golean's two ordering deltas, GoWalk.lean:111-116).
  DELIBERATELY UNREGISTERED (the walk stops in front of them):
  `iter_compose`/`iter_compose_exit` (invariant-carrying — their `St`
  family is a Lean-level ∀-obligation no goal determines,
  GoWalk.lean:59-61), `ccall_compose` (consumes a CalleeBlock the
  human names), and any lemma with a non-mechanical side condition
  (range checks, overflow bounds — G12).
* `app_walk` / `app_walk n` — the loop: match head, apply the one
  registered lemma, discharge its side hypotheses by
  `rfl`/`assumption`/`simp [app_norm]` (and ONLY those), normalize,
  repeat. Goal shape in: `app C σ = R` where R is a metavariable, a
  stated term, or an `app C' σ'` continuation (the `.trans`-chain
  shapes of every AppEq file). Goal shape out: the same equation
  advanced to the first configuration where no registered lemma fires
  — a semantic obligation, an unregistered-rule site, or `rfl`-closable
  (then closed). Failure behavior: stops with the goal untouched by
  the failed attempt, reporting the stuck head shape; never a
  half-applied lemma (golean saved-state discipline, GoWalk.lean:66-69).
  RHS synthesis mode (Q4): when R is a metavariable, each step
  instantiates it through the computed-RHS round lemma + `app_norm`,
  and the final assignment is the synthesized post-state — this is
  what deletes the transcription defs.
* `app_walk_step lem as [x, h, …]` — apply ONE named lemma
  (registered or not) with its semantic hypotheses supplied/named;
  the manual-step primitive the bar counts (§4).
* `app_walk_finish h` — walk until the goal matches the block
  equation `h` supplies, close with `h` (the honest segment end,
  golean GoWalk.lean:45-48).
* `app_walk?` — one step, reporting which lemma fired (golean
  `go_walk1` + brick `wp_step_debug`, WpTactics.v:2366-2374; the B7
  probe-driven-development lesson). Debugging only, banned from
  committed proofs (grep-checked by the size gate, §4).

Meta-code residency rules: the walker is metaprogramming, not
proof-facing semantics (golean's framing, GoWalk.lean:117-123) —
`partial` allowed INSIDE `TacticM` only; every proof it produces is
kernel-checked as usual and swept by the in-build audit; the D14 ban
(no ofReduce* carriers) applies to its outputs and is already enforced
by the axiom gate.

WP-level tactics: NONE this arc (Q6, §7). SlateWP + the L0 rules are
the whole Layer 3 story; iris-lean's `WpGoal`/`runTacticWp`/`iWpPure`
scaffolding (HeapLang/ProofMode.lean:160-186 + the #657 refactor) is
recorded as the METHOD file for the Q4-granularity future, nothing
more.

Derived counts (for the record): L0 ≈ 12 consumed families (table
above); L1 ≈ 45-55 named lemmas across 6 kit files (AppEq ~12
re-exports, Mem ~12, Round ~14, Eval ~9, Loop ~5, CallSpec ~4);
L2 = 5 tactics + 2 attributes.

---

## 2. The loop story (T5's invariant rule, G1)

Ground truth about the object (pinned `tests/verify/t5_sum.core`,
verified this slice): the C for-loop compiles to a labeled
continuation — `save while_501: unit (i, s) in <guard eval> ; if a_502
then <body ; save __cerb_continue0 … ; run while_501(i, s)> else
pure(Unit)` with `break_500`/`ret_498` labels after. So an iteration
is a FIXED sequence of dnms rounds (guard unseq/loads/compare →
if-split on the guard boolean → body loads/add/store → i-increment
block → `run while_501` jump), and the whole run — prefix, all n
iterations, exit, post-loop, terminal — is still ONE bind-collapsed
`app` computation (arc-7 trace evidence covers T5, Machine.lean
§Coverage-by-need). The loop rule therefore lives at Layer 2, exactly
as the gap matrix concluded: iris-lean has no loop rule at either rev
(survey §1.2, ABSENT), and at one-atomic-WP-step granularity there is
no WP traffic for `iloeb`/fixpoint machinery to carry — the rule is a
Nat-induction over app-block equations.

**The rule, as a contract (Q2 answered: option (a), split into a pure
composition core + walker-proved premises):**

```
-- Kit/Loop.lean, fully generic (no driver types):
iter_compose {α σ : Type} {C : Nat → σ → α} {St : Nat → σ} {k n : Nat}
  (hbody : ∀ i, i < n → ∀ fuel,
     C (fuel + k) (St i) = C fuel (St (i+1))) :
  ∀ fuel, C (fuel + k * n) (St 0) = C fuel (St n)
```

* HYPOTHESES read as the classic triple: `St` is THE INVARIANT (a
  pure, closed-form state family — brick-B9's derivation-indexed
  contract, deliberately kept: for t5, `St i = mkDrv (thLoop i)
  (memLoop (sVal i) i) rs (trL i) (n0 + r*i)` with
  `sVal i = i*(i-1)/2`); `i < n` is THE GUARD's semantic content
  (hbody is provable only while the compiled guard evaluates true —
  the `i < n` hypothesis discharges the comparison chain via
  Kit/Eval's `evalcmp_int_spec`); `hbody` is BODY-PRESERVES (one
  iteration block: k rounds, proved by `app_walk` per fixture,
  ∀-quantified in i and fuel).
* CONCLUSION: the composed k·n-round block equation from `St 0` to
  `St n` — still relative fuel, still an ordinary app equation.
* `k` is the CONSTANT per-iteration round count (constant because the
  body is fixed code; this is what makes Q5's offset algebra
  sufficient, §7).
* Proof obligation of the rule itself: Nat-induction on n over
  equation transitivity — pure Lean, zero axioms; nothing about the
  semantics is assumed. Soundness path: unchanged. The rule
  manufactures an `app`-equation; that equation feeds the UNCHANGED
  chain `wp_of_app_active` → `callHarnessAdequate_of_wp` →
  `wp_adequacy_gen`/`ownP_adequacy` → `callOutcomes_sound` +
  `steps_erased` + `adequate_result` (SlateWP.lean:31-69,
  IrisAdequacy.lean:44-110) — the cone stays exactly [propext,
  runEffectful, Classical.choice, Quot.sound], enforced by the
  in-build audit as today.

**Exit framing (brick-B8, `wp_frame_exit` WpTactics.v:2257-2274
adapted):** the invariant proof never carries the post-loop
continuation. The T5 composition is four separate blocks chained
OUTSIDE the induction:

```
prefix_block ⋯ entry_block          -- create s/i, store 0s, save label: c₀ rounds
  |>.trans (iter_compose … n)        -- k·n rounds, St 0 → St n
  |>.trans exit_block                -- guard false at i = n: c₁ rounds
  |>.trans post_block                -- break/kill/load s/run ret: c₂ rounds → NDactive
```

`exit_block`'s comparison discharges from `¬(n < n)`; early-exit slate
entries (T8) replace `iter_compose` with `iter_compose_exit`:

```
iter_compose_exit … (hbody : ∀ i, i < j → …) (hexit : exit-block at St j) :
  -- j ≤ n the exit index; the family only covers 0..j
```

**Fuel instantiation (G2/Q5):** the round lemmas are already
fuel-generic (`fuel+1` shapes throughout T1AppEq/Machine); only the
final instantiation at the executable's `lemDefaultFuel` needs
arithmetic:

```
fuel_split_at_default (c : Nat) (h : c ≤ lemDefaultFuel) :
  dnms lemDefaultFuel acc tids = dnms ((lemDefaultFuel - c) + c) acc tids
```

by `congrArg` + `Nat.sub_add_cancel` — with `c = c₀ + k*n + c₁ + c₂`
and slate bounds n ≤ 100, k ≲ 30, `c ≤ ~3,100 ≪ 999,999` discharges by
`omega`/`decide` on the `n ≤ 100` hypothesis. No fuel-monotonicity
family is needed (§7 Q5). The same split handles the driver2/nd_bind
fuels via the existing wrapper-defeq hooks (FuelHooks.lean — cited,
never unfolded).

**The T5 statement** (fixed now so S2 states it verbatim): identical
form to T1-T4 —

```
T5Statement : ∀ n : Int, 0 ≤ n ∧ n ≤ 100 →
  CallHarnessAdequate t5File.tagDefs t5File "sum" [intValue n] t5Fs
    (fun r => r.dres_core_value = intValue (n * (n - 1) / 2))
```

interpreter-only (CallHarnessAdequate = the CerbND-shaped headline,
Call.lean:322), plus the `T5_ubFree`/`T5Outcomes` faces as T1
(T1.lean:152-171). Statement-TCB gate untouched. NOTE the closed form:
sum of 0..n-1 = n(n-1)/2 (the fixture's loop adds i for i < n); the
`sVal` family's side conditions (`sVal i` in int range for i ≤ 100)
are omega-trivial at these bounds — the stack-ceiling register item
(~1.5k iterations) stays out of scope as chartered.

**Design finding to record in advance** (charter risk "LOOP-RULE
SOUNDNESS"): nothing here assumes new Machine.lean structure — the
rule composes EXISTING app equations, so the charter's
Machine-refactor contingency is not triggered by this design. The
delicate point moved instead into hbody's provability through generic
round lemmas (probe P1).

---

## 3. The transcription-kill plan

The measured cost center (survey §5): 5,966 Layer-2 lines / 378 lemmas
/ 523 defs / 621 `.trans` steps for four loop-free theorems, the defs
"overwhelmingly hand-TRANSCRIBED intermediate configurations". What
kills what:

| cost component (measured share) | killer | mechanism |
|---|---|---|
| intermediate-configuration defs (arenas/threads/mems/run-states — the bulk of the 523; e.g. T1's arena1-8, th1-8, z0-z4, memErrAlloc ≈ 45 of its 71 defs) | Kit/Round computed-RHS lemmas + `app_walk` RHS synthesis (Q4) | the post-state is computed and normalized, never stated; a def survives only if it appears in a STATEMENT, an induction family (`St`), or the fixture pin |
| hand `.trans` chains (621) | `app_walk` | the walker chains internally; explicit `.trans` remains only at the 3-5 block boundaries of §2's composition |
| per-fixture eval residual ladders (T1's s0-s4 + z0-z4 pattern, replicated in T2-T4) | Kit/Eval `evalconv_int_spec`/`evalcmp_int_spec`/`evaladd_checked_spec` | proved once, generic in the value |
| per-fixture memory-op equations (allocErr_eq/storeErr_eq/loadX_eq × 4 files) | Kit/Mem blocks | proved once, generic in address/value/type with side-fact hypotheses |
| per-round fixture lemmas (round0-8 × 4 files, T4's ~40 rounds) | Kit/Round | the round classes are ~14; fixtures stop owning round lemmas entirely |

**Acceptance bar (golean's form, honestly adapted).** golean's
recorded acceptance: "the existing n=1 summit re-proves in ≤ ~100
lines with identical statement and axiom set", refined to "line counts
are advisory; the REAL acceptance is 'statement and axiom set
identical to the hand proof'" (survey §3.3, verbatim). Ours:

* CALIBRATION (S2, before T5): `t1_app_eq`'s dnms segment
  (`dnms_chain` + its 9 rounds + arenas/threads/envs, ≈ 700 of
  T1AppEq's 1,038 lines) re-proves through Kit + `app_walk` with
  **identical statement and axiom set** in ≤ ~150 lines — a ~4.5x
  measured-shape reduction on the segment, honest to our shapes
  (brick-wp's own calibration point, survey §2.2: with a full library
  their single-while-loop proof is 337 lines — "short" means "every
  line is semantic", not tiny). The old T1AppEq text is then DELETED
  in the same commit (golean lift-wave rule: extract, pin, delete
  duplicates — no parallel twins).
* PROJECTION (the ratio promised): T5 by hand would exceed ~2,500
  lines (survey §5 growth signal: T1's shape × ~n iteration copies);
  through the library the bar is ≤ 250 hand lines — **≥ 10x under the
  unautomated projection for the loop case**, and 4-9x against the
  measured T1/T4 baselines for straight-line content. Per-proof
  targets: `.trans` ≤ 15 (vs 73-280 measured), non-fixture defs ≤ 25
  (vs 71-229 measured).
* HARD CRITERIA (per golean's refinement, the numbers advisory):
  statement form identical (CallHarnessAdequate, statement-TCB gate),
  cone exactly [propext, runEffectful, Classical.choice, Quot.sound],
  and the all-semantic reading (§4).

---

## 4. The proof-size bar (final)

Refining the survey §5 first cut — ADOPTED with the measurement
convention made concrete:

**File convention.** Each slate example `T<n>` ships exactly two
example-owned files: `T<n>Fixture.lean` — the pinned program term
(instrument-emitted Core), symbols, addresses, and NOTHING with a
`by`-block beyond `rfl`-pins (NOT counted); and `T<n>.lean` — the
statement, the spec function, the invariant/state family, the side
conditions, and the proof (COUNTED WHOLE, including its defs: the `St`
family is hand content and must fit the bar — hiding transcription in
the counted file's defs is not an escape, hiding it in the fixture
file is caught below).

**The bar.** Per slate example: `T<n>.lean` ≤ **250 lines** total and
≤ **40 manual proof steps**, where a manual step = any tactic line in
a `by`-block that is not an `app_walk`/`app_walk_finish` invocation
(i.e. `app_walk_step`, `refine`, `omega`, `simp`, case splits… all
count; the walker does not). Line counts ADVISORY per golean; a breach
is a MISSING-RULE FINDING triggering the S3 stop-extract-redo loop
(charter), never a push-through.

**Measurement.** A new gate script `scripts/check_proof_size.sh`,
added to the Tier A ladder (run from `test_unit.sh` like the other
gate scripts): for each registered slate proof file (list in the
script, updated per example), report `wc -l` + the manual-step count
(grep-derived over by-blocks minus walker lines) and FAIL over
250/40; additionally FAIL on: `app_walk?` in any committed proof
file, and any fixture-symbol reference (`t[0-9]+File`, fixture sym
names) inside `Kit/*.lean` — the MEGA-LEMMA COUNTER, mechanized: a
"kit lemma" that names a fixture is bar-gaming by construction. The
S4 audit scope (c) remains the semantic check (read what the lines
SAY); the script is its mechanical floor. Tallies land verbatim in
the results doc (golean derivation-anchored convention, survey §3.4).

**Kit exactness surface (G9).** Every PUBLIC Kit lemma gets a verbatim
`#print axioms` pin via `#guard_msgs` in `Kit/Audit.lean` (golean
Audit/Kit.lean pattern, survey §3.4 — exactness on top of the existing
in-build sweep; private internals unpinned; re-baselining requires the
docstring reason in the same commit). In-build: imported by
RelSem/Audit.lean so `lake build` enforces it.

---

## 5. The graded example slate (S3)

Six entries, each with a DISTINCT reasoning demand; T5 is S2's, T6-T9
are S3's core (charter success condition 2 needs ≥ 3 beyond T5 incl.
one nested/compound loop and one call composition), T10 is the
stretch. Fixture recipe is uniform (the tests/verify pattern, already
gated): `tests/verify/t<n>_<name>.c` + pinned `.core` via the oracle
(`--pp=core`; the pin-provenance check in test_verify.sh re-derives
byte-identically, fail-closed — automatic for new fixtures since the
script iterates `*.c`) + `expectations.txt` rows (concrete
Specified/UB points incl. boundary args) + the emit-lean-core
instrument for the program term (`T<n>Core`/`T<n>File` per arc-7 §8,
drift-gated by emit-lean-core-test).

* **T5 — `sum` (bounded for-loop)** [S2; the anchor]. C: the pinned
  t5_sum.c. Statement: §2. Exercises: `iter_compose`, the full
  Kit/Round set (incl. `round_unseq`, `round_kill` — both NEW vs
  T1-T4), `evalcmp_int_spec`, `evaladd_checked_spec` (overflow side
  condition discharged from n ≤ 100 by omega), fuel algebra,
  store-collapse normal form. Expectations rows: n ∈ {0, 1, 10, 100}
  → Specified(n(n-1)/2).
* **T6 — `tri` (nested loops)**. C sketch: `int tri(int n) { int c =
  0; for (i = 0; i < n; i++) for (j = 0; j <= i; j++) c = c + 1;
  return c; }`, 0 ≤ n ≤ 30. Statement: Specified(n(n+1)/2). Demand:
  loop-rule COMPOSITION — the inner loop's `iter_compose` conclusion
  is one round-block inside the outer `hbody`; nested fuel accounting
  (k_outer itself contains k_inner·(i+1), so the outer family's fuel
  offset is i-dependent — the one place `iter_compose` needs its
  variable-k generalization `iter_compose_var (k : Nat → Nat)`,
  designed into Kit/Loop from the start).
* **T7 — `sumarr` (loop over an array)**. C sketch: `int sumarr(int
  n) { int a[8]; int s = 0; for (i = 0; i < 8; i++) a[i] = n + i;
  for (i = 0; i < 8; i++) s = s + a[i]; return s; }`, range-bounded n.
  Demand: memory-invariant interplay — the `St` family carries a
  bytemap with 8 SYMBOLIC-valued cells at concrete addresses
  (array-element addressing through the create'd array object;
  Kit/Mem at element granularity); sequential-loop composition (two
  iter_compose blocks chained).
* **T8 — `firstge` (early exit)**. C sketch: `int firstge(int n) {
  for (int i = 0; i < 100; i++) { if (i * i >= n) return i; } return
  100; }` (or `break`+return — final choice at fixture time by which
  Core shape exercises `run break_500` vs `run ret_*` mid-loop; the
  pinned t5 Core shows both label classes exist). Demand:
  `iter_compose_exit` (the family covers 0..j only), `round_run_jump`
  out of the loop body (G6), a spec with an ∃/least-index flavor
  stated as a pure closed form.
* **T9 — `dbl2` (two-function call composition)**. C sketch: `int dbl
  (int x) { return x + x; } int dbl2(int x) { return dbl(dbl(x)); }`,
  range-bounded so no overflow. Demand: `CalleeBlock` for `dbl`
  proved ONCE, `ccall_compose` consumed TWICE at the caller's Eccall
  rounds; `round_ccall_push`/`round_return_pop` (stack discipline —
  genuinely new vs T1-T5, whose only calls are inline pure PEcalls).
* **T10 — chvalid cameo (STRETCH, park-clause armed)**. Real-code
  target: a `xmlIsBlank_ch`-class predicate over the chvalid tables
  (the arc-5/6 differential corpus). Demand: GLOBALS — driver_globals
  walking a real static-const initializer into memory (T1-T9's
  prefix crosses an empty globals list), then table-lookup loads.
  This is a new prefix-block class with real cost; per the charter's
  per-example park clause it is attempted LAST and parked with
  pricing if the globals walk resists the kit.

Bounds discipline: all loop bounds ≤ 100 (stack-ceiling register item
stays untriggered, charter risk note).

---

## 6. File/module structure + regression wiring

```
relsem/RelSem/
  Machine.lean, RunND.lean, Call.lean, Cerberus.lean, ExecModel.lean,
  FuelHooks.lean, IrisLang.lean            -- unchanged (audited substrate)
  IrisState.lean, IrisRules.lean,
  IrisAdequacy.lean, SlateWP.lean          -- OwnP-adopted in place (§1.1;
                                           -- same files, same public names)
  Kit/AppEq.lean                           -- L1 re-export + attribute tags
  Kit/Mem.lean                             -- memory blocks + roundtrip + layout
  Kit/Round.lean                           -- driver-round computed-RHS lemmas
  Kit/Eval.lean                            -- eval crossings + composed chains
  Kit/Loop.lean                            -- iter_compose family + fuel algebra
  Kit/CallSpec.lean                        -- CalleeBlock + ccall composition
  Kit/Audit.lean                           -- per-public-lemma #guard_msgs pins (G9)
  Tactics/AppWalk.lean                     -- L2: attributes + the walker quartet
  T1..T4 (files as-is; T1AppEq shrinks at the S2 calibration,
          T2-T4 AppEq files migrate opportunistically in S3 waves —
          same statements, same cones, golean delete-duplicates rule)
  T5Fixture.lean / T5.lean                 -- S2
  T6..T10 Fixture/proof pairs              -- S3
  Audit.lean                               -- extended: imports Kit/Audit,
                                           -- pins T5+ cones, negative tests
```

Import discipline (enforced by inspection + the audit): `Kit/*` and
`Tactics/*` import NO Iris and NO fixture files (fixture-free is
grep-gated, §4); `Tactics/AppWalk` imports `Lean` + `Kit` only;
fixture files import Kit but not Tactics (fixtures are data);
example proof files import both.

Regression wiring (charter: "the full slate re-proves on every library
change"): automatic — `RelSem` is in `defaultTargets`
(lakefile.toml:5), so every capped `lake build` re-elaborates the
whole slate + the in-build audit + Kit/Audit pins; a kit/tactic change
that breaks any example is a build failure. Additions to the gates:
`scripts/check_proof_size.sh` (§4) into test_unit.sh (Tier A);
`check_theorem_axioms.sh` cone list extended per new theorem;
expectations.txt rows per fixture (test_verify, Tier A). The improvement
loop's regression rule (charter S3): a tactic change that reddens any
slate proof = the improvement is wrong — reverted, not patched around.

---

## 7. The seven parked S0 questions — answers

* **Q1 (OwnP adoption depth): ADOPT WHOLESALE**, S2's first commit,
  probe-gated with a recorded fallback (§1.1). Rationale: REUSE-FIRST
  is the arc's prime directive and the survey's price is ~zero
  (signature-compatible wrappers keep every consumer stable; the
  boundary interface — the app equation — is untouched); the
  counterweight (churning a proven chain mid-arc) is answered by
  sequencing: the swap happens BEFORE any tactic work builds on
  either form, exactly the D1 bump logic one level up. Not
  operator-blocking: the charter pre-authorized the bump flow and
  names OwnP adoption as the delta's payoff; the flag section (§9)
  records it anyway.
* **Q2 (loop-rule form): (a)** — the generic composition core
  (`iter_compose` + `_exit` + `_var`), with the semantics pushed into
  walker-proved per-fixture `hbody` blocks (§2). Option (b)
  (per-fixture Nat-induction) is subsumed as the degenerate use if P1
  forces shape-indexed lemmas; option (c) (dnms-level induction
  principle against the generated driver) is REJECTED: it reaches
  into generated-code internals for no capability (a)+walker lacks,
  and sits closest to the no-lem-changes tripwire.
* **Q3 (walker normal form): the COLLAPSED canonical spelling**,
  owned by `app_norm`: value-indexed closed-form state families at
  concrete keys, TreeMap same-key inserts collapsed
  (`bytemap_insert_collapse`), update_env unfolded at concrete
  patterns. This deliberately REVISES T1AppEq's "spelled as the
  store's OVERWRITE" convention (T1AppEq.lean:350-356): that spelling
  was honest for a 2-store straight line but is unusable under
  induction — the post-iteration state must be SYNTAX-IDENTICAL to
  `St (i+1)`, so overwrite chains growing per iteration are
  disqualified. The revision is a documented, deliberate
  normal-form choice (B14), probe P2 checks the collapse lemma's
  route over the T3/T4 store shapes as the survey asked.
* **Q4 (walker synthesizes intermediate states): YES, bounded**:
  computed-RHS round lemmas + metavar-RHS synthesis inside walk
  segments (§1.2/§1.3); NAMED states survive only at
  statement/induction boundaries. Blow-up control: synthesis is
  per-round through registered lemmas + `app_norm` only (never bare
  whnf on the whole computation — the D7/D8 discipline), each
  candidate under a small budget. Feasibility anchored to the proved
  generic crossings cited in §1.2; residual risk = probe P1 with the
  shape-indexed fallback.
* **Q5 (fuel parametricity scope): the offset algebra suffices** —
  G2 is ~5 lemmas (`iter_compose*` count separately; here:
  `fuel_split_at_default`, `dnms_fuel_cast`, and the `+k`-shape
  restatement conventions), NOT 25. Grounds: per-iteration round
  count k is CONSTANT (fixed body code), all round lemmas are already
  `fuel+1`-generic, and total consumption ≤ ~3,100 ≪ lemDefaultFuel
  at slate bounds, so `Nat.sub_add_cancel` + omega covers every
  instantiation; no monotonicity family, no `∀ fuel ≥ floor`
  quantification. (Monotonicity would return only if a slate demanded
  input-DEPENDENT iteration cost with unbounded inputs — excluded by
  the n ≤ 100 slate design.)
* **Q6 (WP-level tactic layer): NOT BUILT this arc** — deliberate,
  recorded for the reuse-honesty audit: every slate demand lives at
  Layer 2; the per-theorem WP glue is ~15 lines through SlateWP and
  shrinks further under OwnP; a DriveExpr WpGoal instantiation would
  automate the smallest, most stable cost in the system. The middle
  layer of the charter's layering IS SlateWP + the L0 rules. The
  HeapLang scaffolding stays the documented method donor for the
  Q4-granularity refinement (gen_heap instantiation), where WP-level
  traffic would first exist.
* **Q7 (slate composition): §5** — T5 sum, T6 nested, T7 array, T8
  early exit, T9 call composition, T10 chvalid stretch; distinct
  demands and fixture recipes as tabled.

---

## 8. Slice-2/3 execution plan

**S2 — minimal for T5** (sequenced worker batches, one commit each on
green gates):

1. **OwnP swap** (§1.1): capped probe (ElemG witness at reworked
   CerbS; wrapper bridge), then the adoption commit — full gate incl.
   T1-T4 re-elaboration + cone re-pin. Fallback: park, record,
   proceed on arc-7 layer.
2. **Probes P1/P2** (scratch, capped, reverted; transcripts verbatim
   into the S2 record): P1 — one generic computed-RHS round lemma
   (`round_tau_sseq` shape) elaborates against symbolic
   continuation/env at default budgets; fallback = shape-indexed
   families. P2 — `bytemap_insert_collapse` route over the T3/T4
   store shapes (structural vs lawful-map API); either route lands
   in Kit/Mem.
3. **Kit extraction wave 1** (Kit/AppEq, Mem, Eval + `app_norm`):
   extract from T1-T4, pin in Kit/Audit, THE CALIBRATION — T1's dnms
   segment re-proved ≤ ~150 lines, identical statement + axiom set
   (§3), old text deleted same commit.
4. **Kit/Round + Tactics/AppWalk v1**: the round classes T5 needs +
   the walker quartet; unit-style exercise file under test/ per the
   walker's contract table.
5. **Kit/Loop + T5**: fixture emission (T5Fixture from the pinned
   core, expectations rows), statement (§2 verbatim), proof through
   the library; FIRST measurement against the bar +
   check_proof_size.sh lands in the same commit.

**S3 — the slate + the improvement loop**: per example, the cycle is
fixture (mechanical; Opus-eligible) → statement + proof (Fable) → on
bar breach STOP, extract the missing rule into the right Kit file
with contract + Kit/Audit pin + exercise, REDO the proof short →
full capped build (= full-slate regression, §6) at every library
change. T2-T4 AppEq migration runs as background waves when an
extraction naturally subsumes their content (never as a goal in
itself). T6 before T8/T9 (it stresses the loop rule hardest, earliest);
T10 last under the park clause. Worker batching: sequential within
the single stream (shared worktree state, doctrine); kit/walker/rule
work on Fable-grade workers, fixture emission + expectations batches
Opus-eligible; the orchestrator independently re-runs gates at every
batch boundary per doctrine.

---

## 9. Operator flags (park-don't-improvise)

None of these stops the arc under the charter's autonomy protocol;
they are flagged so the operator can redirect at any checkpoint:

* **F1 — OwnP adoption churns a proven adequacy chain** (Q1). Charter
  pre-authorizes the bump-and-reuse flow and the survey names the
  ~380-line twin as reuse debt; the design sequences the swap before
  tactic work with a recorded fallback. Flagged because "rebase the
  adequacy discharge on a 1-day-old upstream file" is a
  risk-tolerance call the operator may wish to override toward
  keep-and-document (the Q1 alternative); the arc proceeds with
  adopt-wholesale unless redirected.
* **F2 — the Q3 normal-form revision** changes a recorded arc-7
  honesty convention (states spelled as-computed → collapsed
  canonical). It is a documented design choice with in-record
  rationale (§7 Q3), not a silent drift; the S4 audit should check
  the collapse lemmas rather than the old spelling convention.
* **F3 — T10's globals machinery** is the one slate entry whose cost
  is not bounded by existing evidence (new prefix-block class); it is
  park-clause-armed and last in order. If parked, charter success
  conditions are still met by T5-T9.

---

## 10. S1 exit summary

Tactic set designed in three layers with names, contracts, and
provenance (L0: 12 consumed iris-lean families incl. the full OwnP
retirement plan; L1: ~45-55 kit lemmas across 6 files; L2: 5 tactics
+ 2 attributes on the golean law-table architecture with brick-wp
dispatch discipline). Loop rule contracted as a pure composition core
+ walker-proved iteration blocks, exit framed outside the induction,
soundness through the unchanged adequacy chain. Transcription-kill
plan with a measured calibration gate (T1 segment, identical
statement + axiom set, ≥ ~4.5x; loop case ≥ 10x vs projection).
Proof-size bar fixed at ≤ 250 lines / ≤ 40 manual steps per counted
slate file with a mechanized gate + mega-lemma counter. Six-example
slate with per-fixture recipes. Seven S0 questions answered, three
operator flags recorded, S2/S3 sequenced. No code written this slice
(docs-only commit; `git status` clean before it).
