# Part-two pre-merge audit evidence (2026-09-18)

Audited head: `88c5a827b1fc7195d85e923c9569e56fbefb4065`.
All fresh runs occurred before adding this directory and the audit document.
No implementation or baseline was edited. Full raw lane captures remain under
`.tmp/audit-address-space-part-two/` in the audited worktree; that directory is ignored.

- `fast-console.txt`: fresh complete fast run, 16/16, unchanged source.
- `focused-console.txt`: fresh B5/B10 selection, 3/3 successful; deliberately reports
  `full: incomplete` because it is not a complete Tier A/B selection.
- `tiny-lane.txt`, `tiny-selftest.txt`, `immaculate.txt`, `oracle-summary.txt`:
  verbatim outputs / selected verbatim pristine-oracle summary lines from those runs.
- `verification-metadata.json`: compact extraction of source identity, selections,
  lane results, and output hashes from both fresh reports and the historical full report.
- `verification-summary.txt`: audit check of the previous full run, including hashes
  of all 78 retained output streams; the previous run was not repeated in full.
- `allocator_arithmetic.py` and `allocator-arithmetic.txt`: a reconstruction of the
  corpus allocation schedules using the pristine allocator equation and Euclidean
  division. This is not execution of an old engine at changed bounds.
- `expectation_plants.py` and `expectation-plants.txt`: extract and exercise the
  exact branch `check_expectations` function on EOF mutations, with a fixed observation table.
- `real-lane-eof.txt`: the actual tiny-bound lane accepts a phantom final row
  without a newline. Generated by the following command after running the plant script:
  `SKIP_BUILD=1 <ce> bash scripts/test_address_space.sh --expectations .tmp/audit-address-space-part-two/phantom-no-newline.txt`.
- `extra_differential.py` and `extra-differential.txt`: 40 additional full-token
  comparisons of real fork/Lean observations (20 positive bounds, two programs),
  plus ten CLI checks; the candidate `malloc(9)` case kills at top 32 in both fixed engines.
  Raw per-engine captures are in the ignored `extra/` directory.
- `EntryParam.lean` and `entry-param.txt`: three universal state-projection lemmas
  about the actual generated constructors, plus printed exemplar signatures.
  Compiled with the repository's capped `lean_probe.sh` recipe. These do not prove
  the missing bound-quantified execution theorem.
- `vip-build.txt`: `opam exec --switch=. -- dune build --root . @memory/vip/all`, exit 0.

Commands ran from the part-two worktree through
`/home/dev/projects/cerberus-lean-proj/scripts/ce` (denoted `<ce>` above).
The Python scripts assume the repository root as cwd and use the ignored scratch
path above; create that directory first when reproducing. To rerun the Lean probe,
copy it into `lean_frontend/.tmp/audit-address-space-part-two/`, then from
`lean_frontend/` run `<ce> ../scripts/lean_probe.sh .tmp/audit-address-space-part-two/EntryParam.lean`.
The exploratory ICE probes in scratch were not evidence for a finding and are
not included here.
