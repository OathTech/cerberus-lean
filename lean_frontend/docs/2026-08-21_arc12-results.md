# Arc-12 results — the honest oracle

Date: 2026-08-21. Branch `arc/honest-oracle` (cerberus-lean only; ZERO
lem-lean changes, zero `.lem` edits — verified §6). Mode: long-cycle
autonomous, parallel stream with arc 11. Slice records:
`2026-08-21_arc12-s0-floor-design.md` (+S1 addendum),
`…_arc12-s1-floor-record.md`, `…_arc12-s2-s3-record.md`,
`…_arc12-renumbering-case.md`, decision log D1–D3.

## Headline

**The F-D declaration-layout corruption family is CLOSED-BY-FLOOR.**
The fork oracle now FAIL-STOPS (`CERB_FRESH_FLOOR_VIOLATION`, exit 70,
harness class `CERB_FLOOR`) on any translation unit whose
desugar-threaded symbol ids can overlap ambient ids, instead of
silently corrupting verdicts. All **35 F-D witnesses (+ 1 latent + 34
coincidentally-correct corpus rows) are loud; zero silently-wrong
oracle verdicts remain** across every witness and swept corpus
(charter success condition 1). Symbol numbering of every in-margin
program is bit-for-bit unchanged: pin-provenance 29/29 twice,
generated-Lean AND generated-OCaml trees unmoved (drift-gate
layer-2 hashes unchanged — gate-verified), `-d5` pp byte-identical on
minimal 106/106 + ci 128/128 + sample 86/86 (condition 2). The drift
gate is green with a refreshed manifest documenting the repair
(condition 3). F-A/F-B upstream filing drafts are ready with un-forked
re-verified repros (condition 4). No forbidden surface touched; no lem
change; branch gate-green (condition 5, §6).

## S0 — mechanism confirmed (probes only)

- Duplicate-(digest,num) scanner over `--pp core -d5`: **20/20 probed
  F-D artifacts collision-positive; upstream 0/2 control**; every
  first collision is a desugar symbol vs a translation label/temp
  (`ret_/break_/continue_/while_N`, `a_N`) — e.g.
  `DUP num=594: ['__cerb_continue21', 'ret_594']`. Run-time: 11,206
  `symbolEqual` "suspicious equality" hits in ONE witness execution.
- Exact synthetic margin: first collision at **N=464** top-level
  declarations (desugar id 483 = main's `ret_483` label); ambient
  window base **483** in every mode (std.core+impl draws).
- **Attribution CORRECTED** (D1): the mechanism is the April desugar
  threading (`8923d6436`, 0-based `fresh_sym_supply`) — NOT the arc-10
  "prime suspect" arc-2 run supply, which is exonerated on all probed
  witnesses (its ids seed at ~5,000–8,700, above every program
  symbol). Addenda placed in the arc-10 campaign record + results.
- Margin dynamics: mode/input-dependent but runtime-known via the
  single `Cerb_fresh.int` chokepoint → NO replan tripwire; a per-TU
  dynamic floor is sound in every mode.
- **Honest S0 error, corrected by addendum at S1:** S0 claimed a
  ~11,250 libc-mode margin. WRONG — `.co` loading is marshalled
  (`read_core_object`) and draws nothing; the measured `a_11253` ids
  were libc.co's baked symbols. The libc-mode margin is the same 483.
  This error shaped D1's expectations and surfaced at S1 as the
  libc/uri conflict (below).

## S1 — the floor (verbatim tallies)

Implementation (hand-OCaml only): `util/cerb_fresh.ml` two-check
per-TU dynamic floor (forward: every ambient draw > desugar hwm;
backward: hwm < the TU's first ambient draw — covers mid-desugar
const-expr-seed composition); `backend/common/ail_sym_hwm.ml` complete
no-wildcard Ail fold (v2 = SYMBOLS ONLY — the v1 supply-nat noting
floored ~half the corpus with zero live collisions; recorded
deviation); `pipeline.ml` post-desugar hook; `test_exec.sh`
`CERB_FLOOR` bucket (fail-closed both directions). Plant-tested
(hook disabled → silent corruption returns; restored → loud).

Ladder, all exit 0, `cerb_floor=0` on every standing lane:

```
minimal : SUMMARY: total=106 match=85 ub_match=18 ub_diff=0 mismatch=0 fail=0 crash=0 lean_error=0 timeout=0 cerb_skip=3 cerb_floor=0 cerb_inconsistent=0
ci      : SUMMARY: total=242 match=91 ub_match=23 ub_diff=0 mismatch=0 fail=0 crash=0 lean_error=0 timeout=0 cerb_skip=110 cerb_floor=0 cerb_inconsistent=18
coverage: SUMMARY: total=199 match=174 ub_match=12 ub_diff=0 mismatch=0 fail=0 crash=0 lean_error=0 timeout=0 cerb_skip=13 cerb_floor=0 cerb_inconsistent=0
float   : SUMMARY: total=69 match=69  (…all zero…)
debug   : SUMMARY: total=90 match=66 ub_match=18 … mismatch=1 … cerb_skip=4 cerb_floor=0 cerb_inconsistent=1   (baselined)
bytes   : SUMMARY: exec_match=9 neg_pinned=5 fail=0
multi_tu: SUMMARY: total=2 match=2 fail=0
```

chvalid (pre-scanned in-margin: desugar max 220 vs 492): 4/4 MATCH.
test_verify 29/29 (twice). test_unit / test_core / test_parse ALL
PASSED. Witness battery: **35/35 + sia_976 + 3 sampled flips, all
exit 70, one floor line each** (16 corpus + 4 committed + 15
seed-regenerated incl. all 8 B1 seeds).

Full-corpus sweep + re-baseline (verbatim):

```
SUMMARY: total=1669 match=1038 ub_match=0 ub_diff=0 mismatch=0 fail=0 crash=2 lean_error=0 timeout=5 cerb_skip=108 cerb_floor=516 cerb_inconsistent=0
```

Movement fully decomposed (all justified, baseline header + S1
record): 15 DIFF→FLOOR (the F-D rows), 2 TIMEOUT→FLOOR (sa_190 +
LATENT sia_976), **34 MATCH→FLOOR** (coincidentally-correct class —
S0's sampled estimate 25–40; rows enumerated), 465 CERB_SKIP→FLOOR
(rank-neutral relabel of already-failing files), 2 jitter
improvements. **mismatch=0**; comparisons 1070→1038 (−3%). Fresh
shard checks vs the new baseline: 0 regressions.

## The conflict and its resolution (D2/D3 narrative)

The S0 libc-margin error meant S1 discovered, rather than predicted:
4/12 of the fork's OWN libc source TUs are beyond-margin (desugar hwm
stdio 856 / stdlib 673 / internal 682 / vfscanf 521), three with LIVE
collisions (**214/58/106** duplicate keys, e.g. `__uflow` =
`ret_526`), and `uri.c` is at hwm **1798** with **252** live
collisions (incl. `__xmlLastError`) — so the floor correctly refused
`libc_prep`'s metadata exports, any libc.co rebuild, and the uri
gate's oracle lane. Charter "libc/uri zero movement" vs D1's
"--cabs-json floors too" became irreconcilable → PARKED per
park-don't-improvise with options priced. **D2 ruled B+C conditional**
(operator-flagged bar amendment, veto window OPEN through the merge
ask — a veto reverts the lane re-greens to honest-red; the floor
itself is unaffected):

- **B (verified-sound export exemption).** Condition DISCHARGED (D3):
  `main.ml:246` discards the desugar result (`(cabs_tunit, _)`); the
  serializer is a pure function of the pre-desugar Cabs tree (no
  symbol numbers, no mutable state); empirically the 4 beyond-margin
  libc TU exports are BYTE-IDENTICAL between collision-possible
  (base 0) and collision-impossible (scratch base 2^20) builds.
  Implemented as `CERB_FRESH_FLOOR_WARNING (cabs-json export)` —
  loud, once per TU, never silent. Residual on record: a corrupted
  const-expr can only flip the export's hard-ERROR verdict class.
- **C (grandfather register G1–G4,** S2+S3 record§2): pinned libc.co
  (exposure-carrying), metadata jsons, uri elaboration+expectations,
  export residual — each validated-by-agreement with the protected
  Lean side, each with mover = **renumbering-era re-derivation**;
  the grandfather env (`CERB_FRESH_FLOOR_GRANDFATHER=1`) is contained
  to exactly the two documented uri-lane invocations (grep-verified);
  honesty addenda appended to the arc-5/6 records.
- Lanes re-greened (verbatim): libc_exec `SUMMARY: match=7 diff=0` /
  `ALL MATCH RECORDED BASELINE`; uri `GATE PASS … baseline unchanged
  (16/16)` + `[lean+libc] EXACT MATCH with ORACLE_LIBC (16/16 URI
  corpus)`.
- **D (renumbering) deferred with its now-strongest case recorded**
  (`…_arc12-renumbering-case.md`): margin ~483 in every mode; the
  fork cannot regenerate its own libc under the floor; ~31% of the
  csmith corpus beyond-margin; the uri gate always ran exposed.
  Post-arc-13 agenda item; retiring the G-entries is its mover.

## S2/S3

S2: findings manifest updated (F-D REPAIRED — FAIL-STOP + attribution
addendum); arc-10 records cross-referenced by addendum; exploration
spot batch (20 fresh B1 seeds):
`SUMMARY: total=20 match=2 … mismatch=0 … cerb_skip=2 cerb_floor=16`
(exit 0). S3: filing drafts **08** (F-A nested-initializer internal
error, TRUE BUG, exit-125 uncaught Failure at
`generated/translation.ml:3257`; 2-D control `Specified(4)`) and
**09** (F-B `&obj.field` address constants rejected; the
subscript-vs-member boundary isolated) in `notes/upstream/` with the
operator filing checklist; repros re-verified un-forked on
`b9aeedcb4` with fresh native references (gcc clean; 8/8/11). F-D not
filed (fork-side); draft 07 updated with the arc-12 pointer.

## Register movements (this arc)

| entry | movement |
|---|---|
| F-D corruption family (35 witnesses) | **CLOSED-BY-FLOOR** (fail-stop; zero silent wrongs; renumbering removes the refusal class later) |
| Renumbering | TEMPORAL entry, case now strongest-yet (design note); mover for all G-entries |
| G1–G4 grandfathered artifacts (libc.co, metadata jsons, uri elaboration+expectations, export residual) | NEW (S2+S3 record §2) — validated-by-agreement, loud warnings, movers recorded |
| sia_csmith_976 | latent F-D CONFIRMED (97 live collisions under a perf-timeout label) → CERB_FLOOR |
| Coincidentally-correct class | NEW, enumerated: 34 corpus rows + the grandfathered libc/uri surfaces — collision-present, correct-by-layout-luck |
| Arc-10 F-D attribution | CORRECTED by addendum (April desugar threading; arc-2 supply exonerated; O1 non-escape narrowed to run-vs-run) |
| Fold-v2 residual (transient mid-desugar syms) | NEW, documented in ail_sym_hwm.ml |
| Perf-gap re-measure mover | LIVE for the 5 surviving TIMEOUT rows |
| S0 libc-margin error | Honest finding, corrected by addendum (records-integrity: original text preserved) |

## §6 Surfaces + gates at close

Whole-arc diffstat (`mdd/cerberus-lean..arc/honest-oracle`,
21 files): oracle OCaml `util/cerb_fresh.ml`,
`backend/common/{ail_sym_hwm.ml,pipeline.ml}`, `backend/driver/main.ml`;
scripts `test_exec.sh`, `test_libxml2_uri.sh`,
`fork_drift_manifest.txt`, `exec_csmith_corpus_baseline.txt`; docs
(11 files incl. addenda) + `tests/csmith_findings/README.md` +
`lean_frontend/CLAUDE.md`. **Zero `.lem`, zero `lean_frontend/`
outside docs/CLAUDE.md, zero forbidden surfaces** (relsem/,
tests/verify/, lakefile/manifest, test_unit.sh, check_proof_size.sh,
lem-lean untouched — grep-verified). Final full bar: see the merge
checklist. D2 veto window: OPEN through the merge ask.
