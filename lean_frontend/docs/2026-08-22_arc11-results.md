# Arc 11 results — workbench v2 (trace/replay engine, context laws, the package rehearsal; T5 parked at the improved grade)

Date: 2026-08-22. Branch: `arc/workbench-v2` (cerberus-lean only;
**zero lem changes** — no lem-lean branch pair exists for this arc).
Base: mainline `mdd/cerberus-lean` @ `a8da194b2`; 13 commits at
close-out-doc time (`74acb0482..08c12d28f` + the S5 docs commits).
Charter: `2026-08-21_arc11-workbench-v2-charter.md`; decision log:
`2026-08-21_arc11-decision-log.md` (D1–D4). Parallel stream alongside
arc 12 (the honest oracle); serialization facts in
`2026-08-22_arc11-merge-checklist.md`.

**The arc closes per D3's capacity clause, ratified in D4**: the
deliverables are the v2 ENGINE (S1 + the S2 hardening) and the PACKAGE
REHEARSAL (S4); T5 — charter exit criterion 1 — is **NOT proved** and
parks at a substantially improved grade with a named, fixture-
independent first move (R-S2-1). This document states both halves
plainly.

The 2-agent adversarial audit (charter S5 scopes a–d) ran after this
record; findings + dispositions are in §10 (all findings FIXED at the
two audit-fix commits).

## 1. The v2 engine inventory (S1 + S2; records: `2026-08-21_arc11-s1-build.md`, `2026-08-21_arc11-s2-build.md`)

All engine work is in `relsem/RelSem/Tactics/` (`WalkTrace.lean` new;
`AppWalk.lean`, `AppEqAttr.lean` extended); every certificate remains
an ordinary kernel-checked declaration — the v2 additions add ZERO
trust surface (preview is negative-tested; replay re-emits through
the same per-stage emitter).

* **Trace IR + structured events** (S1 batch 1): `WalkTrace.lean` —
  `Residual`/`CandFate`/`CandTrace`/`NormLane`/`SealKind`/`SealEv`/
  `HypEv`/`Ledger`/`FiredTrace`/`RoundTrace`/`Outcome`/`WalkTrace`,
  the `TraceSt` builder, the engine event buffer, `engineRev`
  (now 2). Every candidate gets a fate (incl. the added
  `CandFate.aborted` — a grammar §12.1 extension recorded as a
  deviation); discharge lanes, decide-fact hits, seal events, and
  per-round `Ledger` rows (ms + settled heartbeats) are recorded.
  Rode along: the A-F4 cleanup slate (dead lanes, stale docstrings,
  `normCompute` params) and the A-F6 sealed-aux REGISTRY
  (`registerSealedAux`, replacing suffix heuristics at the expose/
  seal decision points).
* **Sealing-as-default** (F12-4, D1-ratified): `normWalkCfg` (norm +
  seal facts/rounds/states) is `app_walk_norm`'s configuration;
  `app_walk_norm!` RETIRED; `nostates` modifier committed with the
  measured rationale (entry 21/21 nostates vs 16/21 sealed; iteration
  44/79 sealed vs 38/79 nostates — the default stands on the corpus
  that matters).
* **Preview + the E9 negative layers** (S1 batch 2):
  `WalkCfg.preview` guards every `goal.assign` site and skips round-
  proof assembly; `app_walk_preview` ALWAYS fails with a
  deterministic summary. Three enforcement layers: (1) structural —
  goal assignment unreachable under preview; (2) always-fails; (3)
  `check_proof_size.sh` ban (plant-tested both directions). E9 pins
  the discriminating pair verbatim: the exact goal `app_walk` closes,
  under preview, fails with `outcome closed-rfl` reported — reported
  closability, no closure. Recorded amendment vs the §12.2 wording:
  fact-level aux theorems and seal definitions DO occur under preview
  (they are what makes discovery feasible); the user's goal mvar is
  never assigned.
* **Checked replay + fingerprints** (S1 batch 3): `storedTracesExt` +
  `app_walk_rec` (normal sealed walk, recorded + fingerprint-stamped)
  and `app_walk_replay` (fingerprint refusal on engineRev/kit-
  registry/goal-statement mismatch; then exactly the recorded law
  sequence with `WalkCfg.replayOnly` — choice removed, checking
  identical; divergence is a loud error, NO fallback to search). E10
  pins record→replay (kernel-checked twice) + the goal-mismatch
  refusal verbatim. Fingerprint components `gen`/`fixture` DEFERRED
  (recorded deviation — those surfaces are pinned by the standing
  fork-drift + emit-lean-core gates).
* **The bench** (`relsem/bench/WalkBench.lean`, Tier C). Columns,
  verbatim:

  ```
  BENCH B1 T1-dnms discovery (preview to semantic stop): 2350ms
  BENCH B2 entry full walk + closure (recorded): 39985ms
  BENCH B3 entry checked replay + closure: 2593ms
  ```

  The ~15x replay economics is the climb's tooling thesis, measured.
  (Derived, labeled as such: the kernel final-check share ≈ ~2 s =
  whole-file wall 46.6 s minus tactic walls; the per-`addDecl` kernel
  split is deferred — register.)
* **Context queries + typed residuals + linter** (S1 batch 4):
  `@[app_eq]` required-fact metadata — `(fact := binder keyPos)`
  (SOFT) and `(fact! := binder keyPos)` (GATE), telescope-checked at
  registration; `queryRequiredFact` is DETERMINISTIC (newest-first,
  first key-match COMMITS to one capped full-type defeq, no
  backtracking); ambiguity-is-error both STATIC (equal key path +
  equal priority at registration) and DYNAMIC (same-priority sibling
  passing the gate at fire time errors naming both). `hypFailed`
  fates and stuck outcomes carry typed residual classifications
  (semantic/arithmetic/defeqBridge/missingLaw/missingFact).
  `#app_eq_lint` over the full kit registry, verbatim:

  ```
  app_eq_lint: 17 laws, 0 identical-key pair(s), 0 literal-pinned LHS position(s)
  ```

  Exemplar registration: `mem_load_block` with `(fact := hget 3)`,
  SOFT mode, behavior-compatible on every existing walk.
* **S2 engine hardening** (the findings that broke the arc-9 park —
  all measured then fixed, S2 record §1–2):
  - **Three emitter defects fixed** (F-S2-1/2/3): silent candidate
    aborts (rolled-back `logInfo` debug prints made heartbeat trips
    look like law mismatches); the R65 wall itself — `normStateV2`'s
    UNCAPPED whnf on the post-state arena redex (now capped with a
    ride-raw fallback), the TERMINAL RHS deep-defeq burn (now capped
    — a terminal that cannot be decided fast is not terminal), and
    the bare `mkAuxTheorem` round seal (now `mkAuxThmRobust`: capped
    primary + raw-`addDecl` kernel-only fallback); selection-payload
    whnf one level deeper (F-S2-3) so advance laws see the Step
    constructor.
  - **The kernel-side closed-fact matching breakthrough** (F-S2-6):
    CLOSED context facts decide by `Kernel.isDefEq` (heartbeat-free)
    where every capped elaborator attempt missed — measured
    `kwf: eq-fact HIT hlkN`, the family lookup fact consumed
    mid-chase at ~6 s wall. Supporting: the equation-fact chase
    generalization (F-S2-4, arbitrary `h : L = v` facts resume a
    stuck chase, certificate kernel-checked), recursor-stuck head
    relaxation (F-S2-5), seal-transparent context matching +
    `unsealHead` (F-S2-7).

## 2. The T5 climb — honest narrative

Charter exit criterion 1 was **T5 proved**. It is **not proved**; the
proof-size gate's registered `T5.lean` row prints, honestly and
verbatim at every `test_unit.sh` run:

```
check_proof_size: T5.lean — not present yet (registered, pending)
```

The flip-to-enforcing NEVER happened — no gate row was flipped, no
bar was changed. What the climb DID produce, in sequence:

* **Arc-9 close position**: 44/79 iteration rounds (k=0 walk), park
  suspect named "the continuation-lambda advance law"; entry
  certificate evidence-grade with a session-scratch recipe that did
  not survive the worktree prune (the D1 honest gap).
* **The suspect DISSOLVED** (S2 engine prong): the trace instrument
  showed census R65 is an ordinary `Ewseq Eannot` tau with
  `advance_tau_misc` found and applicable — NO lambda-headed step
  occurs there. The walls were the three emitter defects above, all
  fixed. The arc-9 park diagnosis was wrong in the useful direction:
  no new law was needed.
* **45/79** k=0 (stuck `fuel + 34`; was `fuel + 35`), and the entry
  theorem is no longer evidence-grade scratch: **`entry5_walk` is a
  committed in-build kernel-accepted theorem** (T5Prefix.lean —
  21-round entry walk at symbolic n and fuel, `app_walk_norm 25
  nostates; app_defeq`, ~8 s; the S1 batch-3 exit test, closing the
  D1 reproduction gap via record→replay).
* **The symbolic route is LIVE**: the succ-case hbody walk (the real
  `iter_compose` obligation) fires 3/79 rounds at symbolic j with
  the hypothesis-mediated fact route working end-to-end — the §11.2
  design executed. Diagnosis cycle: arc-9 ~10 min → ~6 s.
* **T5Iter proved and committed** (`relsem/RelSem/T5Iter.lean`,
  in-build): the env-lookup family — `envL_built`, `envL_lookup_n`
  (whole-chain induction, 27 decided concrete-key skips + the two
  stuck-seed keys from `hdig`/`hseed` with the slate bound via
  `omega`), `envL_lookup_i`/`envL_lookup_s`, comparator helpers.
* **The final push measured its own limit** (D3-authorized single
  session, S2 record §6): BOTH R-S2-2 routes (skip-law-as-rewrite
  AND per-round fact families) consume premises that must
  kernel-match the walk's env spelling, and that spelling is exactly
  the R-S2-1 artifact — sealed step_m closures capturing abstraction
  avatars (kernel `isDefEq` returns FALSE, not error — discriminated
  by a purpose-built trace lane; evidence `scratch/probet5s4c10.out`,
  0 KERNEL-ERR). No third attempt was made; the session pivoted
  in-session to S4 per D3's capacity clause.
* **The park**: k=0 45/79; symbolic-j 3/79 with the fact route
  validated; no NEW wall class was ever hit (the stop rule never
  fired — every wall fell to the standing toolkit; what ended the
  climb is capacity against ~75 symbolic rounds plus exit/post/
  composition/statement).
* **The named first move is R-S2-1** (sealed-closure avatar
  coherence in the emitter) — an ENGINE-side item,
  **fixture-independent by design**: it deliberately does not touch
  T5 fixtures or pinned Core dumps, so it is compatible with the
  operator-ordered RENUMBERING arc's re-pin of those fixtures
  (D3/D4). After R-S2-1, both R-S2-2 routes unblock and the climb
  resumes post-renumbering; a landed T5 would then re-record
  mechanically via checked replay (the D3 renumbering-compatibility
  argument — replay's fingerprint refusal makes stale re-use loud,
  never silent).

## 3. The package rehearsal (S4; record: `2026-08-22_arc11-s4-package-rehearsal.md`)

Delivered complete (charter success condition 5): `relsem/` is its
own Lake package (path-require on the semantics package; git deps
shared via `packagesDir = "../.lake/packages"`, offline-safe); the
in-build gates (DAEMON absence, statement-TCB, axiom sweep, 54
Kit/Audit pins) re-homed to the relsem package's plain `lake build`,
fail-closed, PLANT-TESTED (corrupted pin ⇒ package build fails);
RelSem-importing exes + bench re-homed to `relsem/test/Unit/` +
`relsem/bench/`; scripts package-aware. The TWO STRUCTURAL FINDINGS
are the rehearsal's real product — the design data for any future
repo split:

1. **The exec-facing boundary is forced**: `Main.lean` imports
   `RelSem.Call`; the verify-harness closure {Call, Machine, RunND,
   ExecModel, Cerberus} cannot leave the root package (require
   cycles forbidden) — now the root-side `RelSemCore` lib
   (`relsemcore/`, git-mv, module + declaration names unchanged,
   sweep count 3021 unchanged). A real split must draw the line
   here or invert the driver dependency.
2. **Root-module prefix resolution**: Lake resolves imports by
   root-module PREFIX; a same-prefix split across packages is
   impossible — the relsem lib enumerates its 34 leaf-module roots
   (aggregator `RelSemAll`).

Chartered deviation (flagged to the S5 audit, scope d): "plain root
build elaborates everything" is satisfiable only per-package — the
preserved property is root plain build green AND proof gates
in-build fail-closed on the relsem package's plain build, with
`test_unit.sh` building BOTH as explicit fail-closed steps.

## 4. Per-slice verbatim tallies (regression-bar tails at each slice's final commit; full per-batch tails in the slice records)

S1 (batch-4 tree, `12977f0c9`):

```
Build completed successfully (610 jobs).
RelSem audit sweep: 3016 declarations across RelSem.* modules, all within the declared axiom boundary (0 recorded sorryAx exceptions)
Total: 7 passed, 0 failed
test_verify: 29 passed, 0 failed (5 fixtures, 18 harness points)
SUMMARY: total=106 match=85 ub_match=18 ub_diff=0 mismatch=0 fail=0 crash=0 lean_error=0 timeout=0 cerb_skip=3 cerb_inconsistent=0
```

S2 (`817635f2f`, T5Iter in-build):

```
Build completed successfully (611 jobs).
RelSem audit sweep: 3021 declarations across RelSem.* modules, all within the declared axiom boundary (0 recorded sorryAx exceptions)
Total: 7 passed, 0 failed
test_verify: 29 passed, 0 failed (5 fixtures, 18 harness points)
SUMMARY: total=106 match=85 ub_match=18 ub_diff=0 mismatch=0 fail=0 crash=0 lean_error=0 timeout=0 cerb_skip=3 cerb_inconsistent=0
```

S4 (`1a36cb0eb`, the two-package structure; audit-gate lines from the
relsem package's plain build):

```
Build completed successfully (367 jobs).
Build completed successfully (363 jobs).
info: RelSem/Audit.lean:144:0: RelSem DAEMON absence gate: no constant named DAEMON or DAEMON1 exists in the environment; neither is allowlisted
info: RelSem/Audit.lean:490:0: RelSem statement gate: 16 slate statements fuel-opsem-clean (negative tests: t1_wp and the wrapper-hole probe correctly rejected)
info: RelSem/Audit.lean:541:0: RelSem audit sweep: 3021 declarations across RelSem.* modules, all within the declared axiom boundary (0 recorded sorryAx exceptions)
```

(367 = root plain build incl. the driver; 363 = relsem package plain
build. `test_unit.sh` / `test_verify.sh` / exec-minimal tails at S4
match the S2 lines above — quoted in the S4 commit message.)

Probe positions banked (untracked scratch, final S2 engine —
derived tallies from the trace dumps, labeled): k=0 iter0 45 fired /
stuck `fuel + 34` (`probet5s4-final.out`); symbolic-j succ block 3
fired / stuck `fuel + 76` with `eq-fact HIT hlkN`
(`probet5s4c6/7.out`).

## 5. Charter success conditions — scorecard

1. T5 proved, gate-enforcing: **NOT MET** — parked at the improved
   grade per D3/D4 (the gate row honestly PENDING; §2).
2. Trace/replay with acceptance gates: **MET with registered
   deviation** (grade corrected at the audit disposition, B-F2):
   preview negative-tested E9; metrics separated in the bench; BUT
   the metric table's per-`addDecl` kernel split has now been
   deferred TWICE — the S1 batch-3 record deferred it "to S5
   refinement" and S5 shipped without it (a lapsed promise, stated
   plainly), and the bench has no elaboration-only column. The
   deviation carries in the register (§8) with the resumption as
   mover.
3. ≥T6 additionally proved: **NOT MET** — T6+ deferred per D3 (out
   of capacity scope; the charter's park clauses applied).
4. Context queries deterministic, trace-recorded, no backtracking:
   **MET** (audit-checkable; §1).
5. Package rehearsal: **MET** (§3).
6. Zero movement / zero forbidden-surface / zero lem: **MET**
   (diffstat §7; exec/verify/unit tails §4).
7. Records complete, branch gate-green, merge checklist ready:
   **MET** (this doc + `2026-08-22_arc11-merge-checklist.md`).

## 6. Decision log summary (full text in the log)

* **D1**: S0 verified; the four §12.7 design flags ratified
  (footprint incremental-IN; preview trust = layered guards;
  ambiguity static+same-prio-dynamic; sealing-as-default). The one
  honest S0 gap (entry recipe was session-scratch) upgraded to S1
  batch-3's acceptance test — subsequently closed by `entry5_walk`.
* **D2**: S1 verified (bench thesis-numbers accepted; deviations
  recorded); S2 GO.
* **D3**: S2 verified; the park judged evidence-grade (suspect
  dissolved, symbolic route live); ONE final climb session
  authorized with the renumbering timing as the deciding factor;
  R-S2-2 ruled (skip-law-as-rewrite route); capacity-end without T5
  = pivot to S4, no third climb.
* **D4**: the final session honored every bound (measured the
  R-S2-1 blockage, pivoted in-session); S4 complete with the two
  structural findings banked; ARC CLOSES per D3's capacity clause;
  S5 GO; merge ask to be presented JOINTLY with arc-12's.

## 7. Forbidden-surface confirmation (for the audit)

Whole-arc diff `mdd/cerberus-lean..arc/workbench-v2` at close-out-doc
time = 32 files (before the S5 docs commits; S5 adds docs +
`lean_frontend/CLAUDE.md` only): `.gitignore` (+1 line:
`relsem/.lake/`), `lean_frontend/CLAUDE.md`, 6 docs (5 new arc-11
records + the arc-9 S1-design addendum), `lean_frontend/lakefile.toml`,
4 relsem package files (lakefile/manifest/toolchain/`RelSemAll.lean`
rename), 6 engine/kit/slate `.lean` (Kit/Mem, T5Iter new, T5Prefix,
Tactics/AppEqAttr, Tactics/AppWalk, Tactics/WalkTrace new — count
corrected from "5" at the audit disposition, B-F1: the enumeration
was always these six), `relsem/bench/WalkBench.lean` (new), 4
test-exe git-mvs → `relsem/test/Unit/`, 5 core-module git-mvs →
`relsemcore/RelSem/` (R100, names unchanged), and 2 DECLARED
scripts (`check_proof_size.sh`, `test_unit.sh`) plus
`test_verify.sh` as a DISCLOSED S4-FORCED CONSEQUENCE (path-only
change from the S4 re-homing, within the S4 chartered touch-point
but not on the charter's declared-script list — wording corrected
at the audit disposition, B-F3). Precise grep over the diff for
`^(frontend/|util/|backend/|tests/)`, `\.lem$`, `baseline`,
`fork_drift`, `csmith`, `CerbMem`, `CerbND`: **zero hits**. Root
`lean_frontend/lake-manifest.json`: zero diff (LemLib rev unchanged
= `11d4b4c3…` = `deps/lem-pinned` HEAD; the relsem package manifest
pins the same revs). lem-lean: no branch for this arc exists.

## 8. Register (movers named)

* **R-S2-1** — sealed step_m closures capture abstraction avatars
  (emitter coherence). THE resumption's first move;
  fixture-independent by design (renumbering-compatible). Reproducer:
  `ProbeT5S4c.lean` + `scratch/probet5s4c9.out`; the kernel-false
  evidence `scratch/probet5s4c10.out`. Mover: the T5 resumption
  (post-renumbering).
* **R-S2-2** — interstitial-env fact spelling; RULED (D3) =
  skip-law-as-rewrite, MEASURED BLOCKED behind R-S2-1 (S2 §6).
  Mover: unblocks mechanically when R-S2-1 lands.
* **R-S2-3** — `walk_diag`-class dumps live only in untracked
  probes; the committed debug surface does not print eff-head/
  stuck-form dumps. Mover: fold into `app_walk_norm?` at the
  R-S2-1 resumption.
* **T6–T9 deferred** per D3 (capacity; charter park clauses) — the
  slate resumes after T5.
* **Deferred metric/fingerprint components** (S1 batch 3, recorded
  deviations): per-`addDecl` kernel split — deferred TWICE (batch 3
  promised it "to S5 refinement"; S5 shipped without it — the
  lapsed promise is why scorecard condition 2 reads "MET with
  registered deviation", audit B-F2); the bench also lacks an
  elaboration-only column; `gen`/`fixture` fingerprint components
  (covered meanwhile by fork-drift + emit-lean-core gates). Mover:
  the T5 resumption's v2 refinement pass, explicitly.
* **Process note (audit A-F2, standing)**: the batch checklist in
  the build-record conventions now includes "engineRev bump on
  behavioral engine change" — the S2 hardening changed engine
  behavior without a bump; engineRev is 3 as of the audit-fix
  commit (rationale in WalkTrace.lean's history comment).
* **sealStates entry-store early stop** (S1 batch-3 archaeology):
  the sealed lane breaks one store-round class (entry R16) while
  clearing the other; `nostates` is the committed workaround.
  Mover: R-S2-1 (same seal-coherence family).
* **Async record/replay contract** (S1 batch 3): recording/replaying
  declarations sit under `set_option Elab.async false`; elabs WARN
  when async is on. Determinism/visibility only, never soundness —
  audit visibility item.
* **Plain-build deviation** (S4, chartered): proof gates ride the
  relsem package's plain build; flagged to the S5 audit scope (d).

## 9. Untracked probe scratch — disposition

PRESERVED in this worktree for the R-S2-1 resumption (deliberately
NOT committed — the proof-size gate's ban policy exempts untracked
scratch, and the probes use debug surfaces by design). A merge does
not carry these; the worktree (or a copy of the files below) must
survive until the resumption. Inventory:

* Probe drivers (`lean_frontend/relsem/RelSem/`, untracked):
  `ProbeT5S3.lean`, `ProbeT5S3b.lean`, `ProbeT5S3c.lean`,
  `ProbeT5S4.lean`, `ProbeT5S4b.lean`, `ProbeT5S4c.lean`.
* Outputs (`lean_frontend/scratch/`): `LintProbe.lean`,
  `t5probe-arc11-s0.out`, `walkbench-b3.out`,
  `probet5s3-arc11-s0.out`, `probet5s3b-arc11-s0.out`,
  `probet5s3b2-arc11-s0.out`, `probet5s3b3-arc11-s0.out`,
  `probet5s3-b1.out`, `probet5s3-b4.out`, `probet5s3c.out`,
  `probet5s3c2.out`, `probet5s3c3.out`, `probet5s4-diag.out`,
  `probet5s4-diag2.out` … `probet5s4-diag6.out`,
  `probet5s4-fix1.out` … `probet5s4-fix5.out`,
  `probet5s4-symk1.out` … `probet5s4-symk5.out`,
  `probet5s4c1.out` … `probet5s4c10.out`, `probet5s4-final.out`.

Key evidence files: `probet5s4-final.out` (45/79),
`probet5s4c6/7.out` (symbolic route + hlkN hit), `probet5s4c9.out` +
`probet5s4c10.out` (the R-S2-1 avatar evidence).

## 10. The adversarial audits (S5 scopes a–d) — findings + dispositions

Two independent auditors (A: gates/engine; B: records/scorecard) ran
after the S5 close-out. This section is the disposition record,
written at the AUDIT-FIX commits; the finding summaries below are
DERIVED (labeled as such) from the orchestrator's disposition
hand-off, not verbatim report text — the plant outputs quoted are
this worker's own runs, verbatim. All findings were dispositioned
FIX and are CLOSED at the two audit-fix commits (code:
`149616b31`; docs: this commit).

### Auditor A (gates + engine) — derived summaries

* **A-F1 (the real one): the T5 gate hole.** The T5 chain
  (T5Fixture/T5Prefix/T5Iter) was built by the relsem package but
  was NOT in RelSem/Audit.lean's import closure — `entry5_walk` and
  the T5Iter family (§1–2's headline theorems) sat OUTSIDE the
  axiom sweep and had no exactness pins; the sweep's tail line
  ("across RelSem.* modules") overstated its scope. FIXED:
  `import RelSem.T5Iter` added; sweep re-baselined **3021 → 3348**
  and the success line is now **#guard_msgs-pinned** (exact count,
  in-build — the closure can no longer shrink silently; on a green
  build the pin swallows the line, so its absence from build logs
  is expected); tail line now states the closure scope honestly;
  five flagship pins added (`entry5_walk`, `envL_built`,
  `envL_lookup_n/i/s`), each exactly `[propext, runEffectful,
  Classical.choice, Quot.sound]`. Plant-tested — a sorried theorem
  appended to T5Iter.lean fails the relsem build, verbatim:

  ```
  error: RelSem/Audit.lean:585:0: RelSem audit sweep FAILED — declarations with axioms outside the declared boundary (a `sorry`, a non-kernel decision procedure, or new postulate?):
    plant_audit_af1 depends on sorryAx
  ```

  (plus the #guard_msgs mismatch error; `error: build failed`).
  Revert verified byte-identical to the pre-plant backup.
* **A-F2: engineRev not bumped for the S2 hardening.** The S2
  engine changes (emitter defect fixes F-S2-1/2/3, kernel-side
  closed-fact matching F-S2-6, chase generalization) were
  behavioral but `engineRev` stayed 2 — a pre-hardening rev-2 trace
  would replay against a changed engine without refusal. FIXED:
  engineRev **2 → 3** with the rationale in the history comment;
  the process rule ("engineRev bump on behavioral engine change" on
  every batch checklist) is banked in §8.
* **A-F3: the debug-surface ban regex was evadable.** The
  `^[^-]*` prefix (a comment-skip attempt) let a banned token hide
  behind ANY earlier dash on the line. FIXED in
  `check_proof_size.sh`: `--` line comments are stripped before
  matching; the scan-surface policy (Tactics/, relsem/test/ (E9),
  relsem/bench/ excluded) is recorded in the script header. The
  evasion plant re-run, verbatim (old regex: no hit, exit 1; new
  gate):

  ```
  check_proof_size: FAIL — debug-only walker surface in committed files:
  /home/dev/projects/cerberus-lean-proj/worktrees/cerberus-lean-arc/workbench-v2/scripts/../lean_frontend/relsem/RelSem/T5Prefix.lean:639:theorem plantEvade (a b : Nat) : a - b = a - b := by app_walk_preview
  ```

  A `--` comment mentioning a banned token correctly passes; plants
  reverted (git diff empty).
* **A-F4: replay accepted fingerprint-less traces.** `app_walk_replay`
  silently skipped ALL staleness checks when `fp = none`. FIXED:
  a fingerprint-less trace now REFUSES loudly ("has NO fingerprint
  — refusing to replay an unstamped trace; re-record").

### Auditor B (records + scorecard) — derived summaries

* **B-F1**: §7 said "5 engine/kit/slate .lean" while enumerating
  six files. Corrected in place (count → 6).
* **B-F2**: scorecard condition 2 graded plain "MET" while the
  metric table's per-`addDecl` kernel split had been deferred TWICE
  (batch 3 promised it "to S5 refinement"; S5 lapsed) and the bench
  lacks an elaboration-only column. Re-graded **MET with registered
  deviation** (§5); the register entry (§8) names the lapse and the
  resumption as mover.
* **B-F3**: §7's "3 DECLARED scripts" overcounted — the charter
  declared 2; `test_verify.sh` was an S4-forced path-only
  consequence, disclosed but not charter-declared. Corrected in
  place.
* **B-F4**: the S1 batch-3 archaeology enumerated the log range
  `65e162efc..HEAD` as "the A-F1 debug-repro fix + batch 1" — batch
  2 was also in that range. Corrected in the S1 record (labeled
  correction; the zero-closure-relevant-drift conclusion is
  unchanged).
* **B-F5** (cosmetic): `test_unit.sh`'s relsem-build `tail -3` cut
  the DAEMON absence gate line from unit output; now `tail -4`
  (verified present in the run).

### Not a finding, recorded for honesty

The auditors' probe leftovers named in the hand-off
(`ProbeAuditA*.lean`) were NOT present in the worktree at fix time
(the auditors evidently removed them); the standing untracked probe
scratch (§9 inventory) was left untouched. This audit-fix pass also
amends the merge-checklist gate row 2 (the sweep line is now pinned,
not printed, when green).
