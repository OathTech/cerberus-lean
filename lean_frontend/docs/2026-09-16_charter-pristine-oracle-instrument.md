# Charter — WP-O: the pristine-oracle instrument (2026-09-16)

**Status:** CHARTER for one Claude Fable subagent worker, written by the orchestrator [AGENT] from the [implementation plan R1 §R1.7](2026-09-16_lean-only-outcomes-implementation-plan.md); executed on branch `arc/pristine-oracle-instrument` in the primed worktree `worktrees/cerberus-lean-arc/pristine-oracle-instrument` (base: mainline `0c78635cb`). The worker commits its own work per deliverable; the orchestrator independently re-verifies every gate at every boundary (worker-claimed green is never accepted). A committed record ENDS the slice.

## 0. Rulings and standing constraints (verbatim; nothing here is the worker's to revisit)

- [USER 2026-09-16]: *"Thinking about the changes we're making, I wonder if there's some way to build the upstream cerberus as a separate oracle? right now we have the ocaml-cerberus as an oracle, but we're also changing it. There's some danger there!"* — then: *"let's add improving the upstream oracle as a thing to do before we execute on further work."* — then: *"Great, let's do a light review, land with minor fixes (pause if major), then execute on WP-O as proposed"* (the proposal = plan R1 §R1.7, reproduced as §2 below).
- [USER 2026-09-11] on execution mode: *"in this case you should launch them as claude fable class subagents. Can you do that? We want to keep in control of this bit of the work"*.
- [USER 2026-09-08], verbatim: *"Is the work on the enumerative methods actually valuable? These methods are out of policy and work on them has zero value to the project. The alternative is (1) we delete them, (2) we do this properly."* Then: *"I sort of don't care how we sequence this. It's fine if stuff lands if it'll be deleted per policy. The main point is we should \*NOT\* be building anything new out-of-policy. So if it's easiest, let's just land it, then make a plan to delete all the out-of-policy stuff the referee identified for elimination"*. Applied here: no new enumeration/literal/semantics-evaluation proofs, no new artefact-class surface; this slice is a validation INSTRUMENT, it adds no semantics and no proofs.
- [USER 2026-09-03] the four aims, verbatim in `lean_frontend/VALIDATION.md` §0 (`:34-40`): mirror the oracle exactly absent STRONG ISO evidence; fix-and-log on clear evidence; consumer-useful; fail-closed. The zero-execution-discrepancy rule (VALIDATION §0/§1): every Lean-vs-oracle execution difference is a bug unless it is a reviewed row of an exception class.
- Standing constraints (container and repo `CLAUDE.md`): NEVER modify machine-global state (no opam switch changes, no `~/.gitconfig`, no shell profiles, no shared caches); NO `git push`, ever, under any reading; never commit on `mdd/cerberus-lean` or `master`; merges are the orchestrator's (ff-only, on explicit per-merge operator sign-off after the unconditional pre-merge audit ASK); fail-closed and fail-noisy everywhere (no `2>/dev/null` on build/install steps; no defaults that absorb failures); [AGENT]/[USER] provenance on every decision; quoted outputs verbatim, derived tallies labelled derived; never run `lake`/`lean` uncapped (`scripts/capped`; `CERB_MEM_MAX` to lower it); ONE heavy job at a time; the grind tripwire — any single step approaching ~45 min is STOP-AND-REPORT with the measurement, nothing over ~1 hr without written justification in advance; status pings get an immediate interim reply; never end your turn on a wait (poll foreground in ≤10-minute steps).

## 1. Facts (verified by the orchestrator 2026-09-16 at mainline `0c78635cb`; cite, do not re-derive)

**The lane.** LADDER Tier B row 10 (`scripts/LADDER.md:67`): `python3 scripts/test_upstream_oracle.py` + `--plant`; "Pristine source `b9aeedcb4` built with upstream Lem `3802cb0`, compared with fork OCaml". Its PREREQUISITE text, verbatim: *"the prepared independent build — `scripts/ce python3 scripts/build_independent_oracle.py --lem-repo <lem-lean checkout> --cerberus-repo <cerberus-lean checkout> --out .validation-foundations/independent-oracle-v2` (~50 s–1 min; the output directory must be new) — whose `manifest.json` the lane reads from that default path or from `CERB_INDEPENDENT_MANIFEST`. Without a valid built manifest the lane FAILS CLOSED (no build, no skip, no vacuous pass)"*. Recipe and first review: `lean_frontend/docs/2026-09-06_independent-oracle-and-fork-pins.md` (its scope statement `:73-79`; its fail rules `:89-91`: *"Unknown differences, timeouts, cap kills, malformed success and silent success fail. `--only` reports a subset; it cannot certify the full lane."*; fork-only flags `--cabs-json`, `--call`, `--batch-alloc-census` have no pristine counterpart, `:103-104`). Recorded build timings (audit-repairs record `:128-133`): `cerberus-generation: passed (19.368s)`, `cerberus-build: passed (12.602s)`.

**The lane script** `scripts/test_upstream_oracle.py`: `validate_build:29-51` (fail-closed manifest/artefact/hash check; revs from `build_independent_oracle.CERBERUS_REV`/`LEM_REV`); `corpus:112-169` — walks `tests/{minimal,coverage,debug,float,bytes,libc_exec}` (`rglob('*.c')`, excluding `.syntax-only.c`/`.exhaust.c`; `--exec --batch`, plus `--nolibc --mode=exhaustive` outside `libc_exec`), `tests/multi_tu/*` (batch), the `tests/cn_coverage/manifest.txt` rows, the libxml2 `uri` harness ×2 modes, two CLI cases; `compare:78-109` — classes `incomplete` (either side status 124/137: *"No exception may admit an incomplete process or silent success"*), `reviewed_difference` (a register row whose `upstream`/`fork` signatures — `status`, `stdout_sha256`, `stderr_sha256` — both match, with a `rationale`), `difference` ("pin moved" or unexplained), `semantic_agreement` (equal verdict lists through `observations.load_capture`), `matching_failure` (same failure under the diagnostic projection, status ∈ {1,125,134}, empty stdout), `interface_agreement`; `--only` (subset; never certifies), `--plant` (a real pair + a fork-verdict mutation that must be rejected), `--timeout` default 30 s; failure = any `difference`/`incomplete`/`plant_failed` row (`:286`). Register `scripts/upstream_oracle_differences.json`: `schema: 1`, ONE row (`minimal/097-null-ptr-arith.undef.c`: both status 125, backtrace-layout difference; re-pinned 2026-09-15 with rationale). Last recorded full verdict verbatim (audit-repairs record `:1776`): `B10.1: Independent oracle: passed; {'semantic_agreement': 738, 'reviewed_difference': 1, 'matching_failure': 11, 'interface_agreement': 2}`.

**What the lane does NOT walk, and why the tray was kept out.** LADDER row 6b (`scripts/LADDER.md:38`), verbatim: *"7 cross-TU struct-value cases kept OUT of `tests/multi_tu/` because the pristine lane (Tier B row 10) enumerates that directory and cannot admit upstream's non-termination (`node`) or exact-tag rejection (upstream-tray drafts 37/38/39 …)"*. Also not walked: `tests/immaculate/{nolibc,libc}/*.c` (Tier B row 5, `test_immaculate.sh`, baseline `tests/immaculate/baseline.txt`), `tests/ci/*.c` (Tier C, `test_exec.sh --write-baseline=scripts/exec_ci_baseline.txt tests/ci`), `tests/verify/*.c` + `lean_frontend/corpus/` (Tier B row 4, `test_verify.sh`; its `--call` rows are fork-only), the csmith corpus (Tier C, `test_csmith_corpus.sh`, 1669 files, prefixed materialisation + kit header shim, `--shard K/M`, baseline `scripts/exec_csmith_corpus_baseline.txt`), libxml2 `chvalid` (Tier B row 1, `test_libxml2.sh`, ~8 min on the fork lane, baseline `tests/libxml2/chvalid_baseline.txt`).

**Consequence (the exposure).** The fork's two shared-model semantic fixes (`ctype_aux.lem:100` array-bound typo, draft 39; `core_eval.lem` PEmemberof compatibility consult, draft 38; both landed `eaa2066e9`) make the fork answer `Specified(7)` where pristine loops (`node`: rc 124 at 60 s) or rejects (`arr-2-2-return`, `arr-incomplete-ptr-return`: `Error {msg: "ill-formed program: \`PEmemberof(struct) ==> mismatched tags: …"}` rc 1) — upstream-tray draft 38 `:132-139` records the verbatim runs. No lane records these fork≠pristine deviations; the register has one failure-text row. Every other execution lane compares fork OCaml with Lean, which a shared `.lem` change moves together. No lane runs pristine, fork and Lean on one input. Correctly shaped already: the Lean-only R5 hex-float fix is a deliberate Lean≠fork DIFF pin (`tests/immaculate/baseline.txt:121`).

**The build.** `scripts/build_independent_oracle.py:69-130`: `--lem-repo`, `--cerberus-repo` (any checkout whose history contains the pinned revs — the fork repos `lem-lean` and `cerberus-lean` do), `--out` (resolved; `mkdir(exist_ok=False)` — an existing directory is REFUSED); archives at `LEM_REV`/`CERBERUS_REV` with `GIT_CEILING_DIRECTORIES` and `DUNE_CACHE=disabled`; the manifest records ABSOLUTE paths for every artefact and the `PATH`/`OCAMLPATH` environment. Default manifest location: `<checkout>/.validation-foundations/independent-oracle-v2/manifest.json` (`test_upstream_oracle.py:197-198`; `.gitignore:77` `/.validation-foundations/`), or `CERB_INDEPENDENT_MANIFEST`. NO built pristine oracle exists anywhere in the container at charter time (the earlier builds were per-worktree and left with those worktrees). The container script `scripts/new-worktree.sh` (container-level, NOT under version control) primes `.lake`, `_opam` (symlink), `_build`, `generated/`, `native/`, stamps — and nothing for `.validation-foundations/`. `release.py` (`--mode fast|full|reporting`, `--lane <id>` repeatable, `--out`) reads the lane tables from `scripts/LADDER.md` and does not build the pristine oracle (by design).

## 2. Deliverables (plan R1 §R1.7 "as proposed"; order of execution O3 → O1 → O2 → O4 → record)

**O3 — Standing build (first, because everything else needs it).** A repo-side script `scripts/ensure_independent_oracle.sh` (or `.py`; one entry point) that (a) validates the manifest at the default path exactly as the lane does (reuse `validate_build`; no second validator), (b) if ABSENT builds it with `build_independent_oracle.py` into the default path (a NEW directory; `--lem-repo`/`--cerberus-repo` default to the container siblings `../../lem-lean`-style discovery or explicit flags — fail loud if not found), (c) if PRESENT BUT INVALID refuses with the validator's reason and names the directory for the operator to remove — it never deletes or overwrites anything. The lane and `release.py` keep their PREREQUISITE stance (they never build; loud fail on a missing manifest stays). Then a ONE-LINE opt-out hook in the container's `scripts/new-worktree.sh` calling the script after priming (e.g. `CERB_SKIP_INDEPENDENT_ORACLE=1` to skip) — this is a container-side change: record it VERBATIM (the diff) in the record, since the container is not a repo. Relocation of an existing build is NOT attempted (absolute paths; rebuild is ~1 min).

**O1 — Corpus and register.** Extend `corpus()` so row 10 walks every corpus the fork-vs-Lean lanes use, with the SAME exclusions and flags those lanes apply per corpus (read each lane script; mirror its flags; cite the line): `tests/multi_tu_tray/*` (the 7 tray cases; multi-TU batch like row 6b's engine invocation), `tests/immaculate/nolibc/*.c` (`--nolibc`) and `tests/immaculate/libc/*.c` (libc mode), `tests/ci/*.c` (with `test_exec.sh`'s exclusions), `tests/verify/*.c` and the `lean_frontend/corpus/` `.c` sources in MAIN mode only (`--call` rows are fork-only; name the exclusion in the report as `:103-104` does), libxml2 `chvalid` (the 4 slices' oracle invocations), and the csmith corpus (its materialisation recipe from `test_csmith_corpus.sh`; `--shard K/M` mirrored). MEASURE each added corpus's pristine+fork wall-clock with `--only` FIRST and record the table; adopt into the Tier B row only corpora that keep the row under ~10 minutes warm; larger ones (expected: csmith, possibly chvalid) become their own rows with `--shard` or a Tier C reporting row — a documented [AGENT] decision with the measurement, and the orchestrator is told before it is baked in (a status message, then continue). Register schema 2 for `scripts/upstream_oracle_differences.json`: every row carries `class` ∈ {`diagnostic-text`, `resource`, `missing-feature`, `shared-model-fix`}, `citation` (an upstream-tray draft path, an ISO-fix register id from VALIDATION §2, or a dated record), `rationale`, and the two signatures; a pristine-side `incomplete` (status 124) is admissible ONLY through a reviewed row of class `resource` or `shared-model-fix` whose citation explains the non-termination (draft 37 for `node`), and NEVER for the fork side or for the plant; the existing "pin moved → `difference`" behaviour is preserved; the schema-1 row migrates to class `diagnostic-text` with its citation. The worker writes register rows ONLY for differences that an EXISTING citation explains (drafts 37/38/39 for the tray; anything else is a STOP — §3). Plants: the existing `--plant` stays; add a plant that a fork≠pristine behavioural difference with NO register row is RED, and a plant that a row with a missing/unknown `class` or `citation` is rejected at load (fail-closed schema). Two-direction fail-closed: a register row whose case no longer exists or no longer differs is RED (stale exception), as the baseline lanes do.

**O2 — Three engines.** A report-only Lean column: `test_upstream_oracle.py --with-lean` runs the Lean engine on each batch case through the fork's `--cabs-json` path exactly as `test_exec.sh`/`test_multi_tu.sh`/`test_libc_exec.sh` do (mirror their recipes; cite lines; `cerberus-lean` built ONLY via `scripts/capped`), decodes with the shared codec (`scripts/observations.py`, no codec change), and reports per case `pristine | fork | lean` verdict tokens plus the class. GATING stays pristine-vs-fork plus the register (O1); Lean-vs-fork is already gated by its own lanes and is NOT re-gated here — but any Lean≠fork difference seen in the report is a STOP (§3, S2). Add ONE Tier C reporting row for the three-engine report over the full pristine corpus; the S1 charter will make it that slice's mandatory report.

**O4 — Doctrine.** `lean_frontend/VALIDATION.md`: §0 gets the reference doctrine (pristine upstream `b9aeedcb4` = THE reference; the fork's OCaml = the shared model's mirror twin, whose deviations from pristine are exactly the reviewed register's rows; the register is the ONLY permitted list of fork≠pristine behaviours; a shared-model slice runs the three-way over the full corpora and may add register rows only with a citation); §4 "What is compared, against what" and §5 lane list updated for the widened row 10, the new rows and the three-engine report; §3's per-difference table gains the register's shared-model-fix rows as fork≠pristine entries. `scripts/LADDER.md` row 10 text and new rows updated (the membership hash `release.py:353` follows). `lean_frontend/README.md` only if it states the oracle roles.

## 3. Fence, forbidden, stop rules

**Fence (may edit):** `scripts/test_upstream_oracle.py`; `scripts/upstream_oracle_differences.json` (schema 2 + CITED rows only); `scripts/ensure_independent_oracle.{sh,py}` (new); `scripts/build_independent_oracle.py` ONLY for argument defaults/discovery (the `CERBERUS_REV`/`LEM_REV` pins are FROZEN); `scripts/release.py` only if lane IDs/timeouts require it; `scripts/LADDER.md`; `lean_frontend/VALIDATION.md`; `lean_frontend/README.md` (oracle roles only); `scripts/test_observation_lanes.py` only where its row-10 plant needs the new schema; the record `lean_frontend/docs/2026-09-16_pristine-oracle-instrument-record.md` (new) + `…-record-evidence/`; the container `scripts/new-worktree.sh` ONE-LINE hook (diff recorded verbatim).

**Forbidden:** any change to `.lem`, `.ml`/`.mli`, `.lean`, `Makefile`, `lakefile.toml`, `dune` files; any other lane's baseline or register (`exec_*_baseline.txt`, `tests/immaculate/baseline.txt`, `tests/libxml2/*_baseline.txt`, `fuel_*`, `failure_reach_register.txt`, `fork_drift_manifest.txt` — and never `check_fork_drift.sh --refresh`); moving `CERBERUS_REV`/`LEM_REV` or the merge-base; register rows without an existing citation; a validator/decoder duplicated instead of reused; `2>/dev/null` on any build/install step; deleting or overwriting anything outside this worktree's own `.tmp/` and a build directory this worker created; opam/switch changes; anything machine-global; `git push`; commits on any mainline; uncapped Lean; more than one heavy job at a time.

**Stop rules (STOP = commit what is green, write the interim state into the record, end the turn with a report):** **S1** a pristine≠fork difference that no EXISTING citation explains (a new semantics finding — the orchestrator adjudicates; the worker never writes the row); **S2** any Lean≠fork difference in the O2 report (a zero-discrepancy-rule violation); **S3** a step measured or projected past ~45 minutes (report the measurement and the sharding proposal); **S4** the pristine build fails or its manifest fails validation for a reason other than "absent"; **S5** any design question that changes gate semantics beyond §2 (e.g. admitting an incomplete result in a new way); **S6** any conflict between rules (raise it; fail closed meanwhile).

## 4. Gates and commit discipline

Work in `worktrees/cerberus-lean-arc/pristine-oracle-instrument` (`scripts/ce <cmd>` for anything needing the OCaml environment; the primary checkout and other worktrees are read-only to you). Build the pristine oracle at the worktree's default path FIRST (O3's script is its own first consumer). One commit per deliverable on `arc/pristine-oracle-instrument`, on green only; the message states what ran with its verbatim verdict lines. FAST-GATE per commit: `python3 scripts/test_upstream_oracle.py` (full lane, not `--only`) + `--plant`, and `python3 scripts/test_observation_lanes.py` if touched. FULL gate at the end, once: `python3 scripts/release.py --mode full` (Tier A + B, including the widened row 10 and the new rows) with the verbatim per-lane tails in the record; zero movement of any existing baseline row. The orchestrator re-verifies every gate independently before any merge conversation; you do not merge, rebase, or push.

## 5. The record (ends the slice)

`lean_frontend/docs/2026-09-16_pristine-oracle-instrument-record.md`: §0 this charter's rulings by pointer; per deliverable — what was built, the files, the verbatim gate tails; the register's new rows with class/citation/rationale and the raw pristine-vs-fork diffs in the evidence dir; the measurement table (per corpus: cases, pristine wall-clock, fork wall-clock, adopted row/tier and the [AGENT] decision); the three-engine report's summary line and any S2 finding; the container-side `new-worktree.sh` diff verbatim; the doctrine text as landed; open items and errata; the one-line rule handed to the S1 charter ("a shared-model slice reports pristine | fork | lean over the full pristine corpus and may add register rows only with a citation"). Commit it; then STOP — the orchestrator takes over (audit ask, sign-off, ff-only merge).
