# Arc-10 results: register burn-down + csmith at scale ("robustness") [AGENT:S5]

Date: 2026-08-21. Branch pair `arc/robustness` (CERB + LEM), run as a
PARALLEL STREAM alongside arc 9 (the workbench; still OPEN at this
close — the merge checklist carries both serialization orders).
Charter: `docs/2026-08-20_arc10-robustness-charter.md` (+ ADDENDUM);
decisions D1–D6 in `docs/2026-08-20_arc10-decision-log.md`; slice
records: `docs/2026-08-20_arc10-s0-triage.md`,
`docs/2026-08-20_arc10-s3-pp-and-dead-corpora.md`,
`docs/2026-08-20_arc10-s4-csmith-campaign.md` (S1/S2 records live in
their commit messages `3b82fac70` / `442a52334` `eec935634` and lem
`98d19fb` `a9387fb`, summarized below); reproducer artifact index:
`tests/csmith_findings/README.md`.

Verbatim-transcript doctrine: quoted blocks are literal harness/tool
output (sourced from the cited slice records, worker commit messages,
and the S5 close-out runs); derived tallies are labeled DERIVED.

## Headline

The substrate hardened on every axis the charter named, and the
differential instrument passed its hardest honesty test to date:

- **tests/ci mismatches are ZERO — 114/114 comparable agree** (was
  110/114 at arc open; finding 11 + the pp-placeholder text class both
  CLOSED by real OCaml mirrors, not baseline edits).
- **The 1134-site sorried-comparison debt is GONE: 1134 → 0** real
  `:= sorry` comparison bodies in the generated tree. The lem backend
  now derives real structural BEq/Ord/SetType/Eq0/Ord0 comparisons
  with OCaml-polymorphic-compare parity, fail-closed (fn-carrying
  types get loud failwithI residuals, never sorry); the
  set-comprehension sorry stub is now a generation-time error.
- **3,169 differential programs (csmith campaign + in-tree corpus):
  ZERO Lean-side semantic defects.** The one Lean semantic defect the
  campaign surfaced (register finding 8, eqPtrval msum provenance
  fork, S0-found) was fixed CerbMem-only and re-swept clean.
- **THE F-D REATTRIBUTION (the campaign's headline):** the
  declaration-layout-sensitive oracle corruption family (spurious UB /
  internal errors / silent wrong values, 35 witnesses [CORRECTION
  2026-08-21, audit fix-batch: originally "24" — recount in the
  register row below and decision-log correction C1]) is NOT an
  upstream cerberus bug — un-forked upstream is correct on every
  tested witness. It is a CERBERUS-LEAN FORK REGRESSION (prime
  suspect: the arc-2 S1 threaded sym_supply +
  description-insensitive symbolEquality). Without the [USER]
  root-cause directive we would have filed OUR OWN bug upstream. The
  repair is parked as the TOP next-arc candidate (priced M).
- Two dead corpora came alive: tests/float wired 69/69 MATCH first
  sweep; tests/bytes wired against its committed upstream expecteds
  (9 exec + 5 negative pins). A new deterministic 1669-file
  csmith-corpus lane is committed with a fully classified baseline.
- Zero commits on forbidden surfaces; standing baselines moved ONLY by
  classified, justified improvements (finding 11, pp class, finding 8).

## Per-slice record

### S0 — register triage + csmith shakedown (CERB `a0f3706ab`, D1)

Record: `docs/2026-08-20_arc10-s0-triage.md`. The full open register
(arcs 4–8) enumerated into 28 evidence-cited rows (DERIVED verdict
tally: 4 slice-assigned fixes + 1 S4-committed, 7 S4-if-forced, 7
parked/priced, 2 forbidden-surface, 2 out, 4 boundary/doc-delib, 1
close-as-mitigated). Kit shakedown: the arc-4 csmith kit runs
unmodified; the deferred creduce leg completed
(`scripts/creduce_interestingness.sh` + `scripts/creduce` wrapper —
env-rot fix, machine-global state untouched). Shakedown found the
arc's first differential hit (seed 930005 → finding-8 signature) and
re-probed the stack ceiling: the arc-6 register text was STALE — onset
today between 16384 and 24576 plain loop iterations, uniformly LOUD
exit 134 (5/5 probes), not the recorded ~1.4k quiet-death (presumed
mover: arc-7 CerbND fuel-totalization). Register text refreshed in the
table below; the item stays forbidden-surface parked (CerbND = arc-9
turf).

### S1 — finding 11: read-only allocations (CERB `3b82fac70`, D2)

`CerbMem.allocateObject` now marks read-only allocations at creation,
mirroring impl_mem.ml:1304-1333 (init_opt=None → IsWritable;
Core create_readonly → IsReadOnly with kind by prefix:
PrefStringLiteral/PrefTemporaryLifetime/const-qualified). The
store-path MerrWriteOnReadOnly check already existed; this was the one
missing marking site. Movement: EXACTLY the finding-11 class —
tests/ci `0086-literal_access.undef.c` DIFF → UB_MATCH
(`UB:UB033_modifying_string_literal`, matches oracle) and coverage
`mem3-004-string-write-attempt` DIFF → UB_MATCH (ratified in D2: the
file's own header cites select_ro_kind impl_mem.ml:1704-1710 — a
finding-11 test by construction; the S0 triage's pp-class attribution
of it was inference, corrected). Verbatim (S1 commit, ci
check-baseline):

```
SUMMARY: total=242 match=88 ub_match=23 ub_diff=0 mismatch=3 fail=0 crash=0 lean_error=0 timeout=0 cerb_skip=110 cerb_inconsistent=18
```

ci scoreboard 110/114 → **111/114 comparable agree** (DERIVED).
Everything else zero movement. Finding 11 CLOSED.

**The S1 lesson (surfaced at S2, D4):** S1's inline nested match in
allocateObject pushed equation-lemma generation past maxRecDepth and
LATENTLY broke `simp only [CerbMem.allocateObject]` in the frozen
relsem T1AppEq proofs — missed by both the worker and the
orchestrator because neither ran the capped DEFAULT-TARGET build at
that boundary. Fixed in-scope by the named-helper hoist
(`readonlyStatusForAlloc`, semantics identical, citations preserved;
CERB `442a52334`; relsem untouched). STANDING RULE since (D4): every
boundary verification includes the capped default-target build.

### S2 — lem C-tier batch: the comparison-sorry elimination (LEM `98d19fb` + `a9387fb`; CERB `442a52334` + pin-bump `eec935634`, D4)

The arc's largest slice, Fable-grade per the charter risk note.

**S2a (R1):** real `BEq (Sum α β)` / `Ord (Sum α β)` in LemLib with
OCaml-polymorphic-comparison parity cited in-code (Left/Right block
tags in declaration order → structural (=), inl/Left < inr/Right).
LemLibTest pins labeled as untrusted-evaluator #guard tests.

**S2b:** the backend's `generate_beq_ord_instances` non-deriving
sorry branch (lean_backend.ml:4185-4222 at arc open) is REPLACED by a
derivation mechanism: total mutual `beq_derived`/`compare_derived`
per homogeneous-parameter mutual block (`termination_by structural` —
the WF fallback blew default heartbeat budgets on the 31-type Cabs
family; structural elaborates at DEFAULT budgets, no bump anywhere),
generated List/Option/Sum/tuple helpers (the CerbStepInstances
pattern), parameterized deriving types get the real bridge trio with
`[BEq tv]`/`[Ord tv]` bounds (the arc-8 S1 bounded-derivation pattern
applied to comparisons). PARITY CONVENTION documented in the emitted
header: nullary constructors below non-nullary, declaration order
within each class, fields left-to-right — OCaml polymorphic compare.
FAIL-CLOSED: fn-carrying types (where OCaml compare raises) and their
referencers get loud greppable failwithI residual bodies — NO sorry
emission path remains; the set-comprehension sorry stub is now a loud
generation-time error (D1 ruling 3; negative probe neg_setcomp.lem).
Probe-first: 29 semantic pins [CORRECTION 2026-08-21, audit fix-batch:
originally "28" — direct recount: 29 `assert dc_*` pins in
test_derived_comparison.lem] + 8/8
negative probes; comprehensive suite fully green; cerberus-scale
regeneration + capped default-target build at the same slice (the
standing April lesson).

**The census (before/after, DERIVED, method in the S0 record §census
and re-confirmed independently at D4):**

| population | before (S0) | after (S2) |
|---|---|---|
| real sorried comparison-field sites in generated/ | **1134** (524 instance blocks [CORRECTION 2026-08-21, audit fix-batch: originally "525" — the triage census's own per-class column sums 79+79+122+122+122 = 524], 122 types, 25 files) | **0** |
| types with real derived comparisons | — | 58 |
| deriving-bridge types with real bounded SetType/Eq0/Ord0 | — | 43 |
| fn-residual types (loud failwithI bodies, greppable) | — | 21 (210 failwithI sites) |
| tv-fallback sites (priority-50 failwithI for dictionary-erased lem default-instance sites) | — | 494 (counted, labeled) |

Registered residuals from S2 (in the register table below): the
`deriving Ord` MIXED-variant leaf order (Lean flat constructor-index
vs OCaml two-class rank — pre-existing deriving-path divergence, not
introduced here) and the fn-residual/tv-fallback populations (loud,
counted). Zero differential movement at the pin bump; the lakefile
touch-point handled per the parallel-stream discipline (own commit).

### S3 — pp-placeholder text class CLOSED (CERB `e0d3ad1f7`, D5)

Record: `docs/2026-08-20_arc10-s3-pp-and-dead-corpora.md`. Real
OCaml-printer mirrors, file:line-cited at every definition: the ctype
chain + symbol/provenance/mem-value printers (CerbMem), exact %.12g /
%.<prec>f float formatting on the IEEE-754 decomposition against an
OCaml 5.4.0 reference transcript (CerbFloat — closes the
format_string_of_float stub, triage row 11 #29-class),
Pp_core.pp_value (CerbPP), and batch_drive's exit selection
(Main.batchExitValue). `stringFromMemValue` partial → TOTAL (half of
register row 21). The four in-class files flipped, verbatim:

```
Baseline check: 0 regression(s), 3 improvement(s)    (ci)
Baseline check: 0 regression(s), 1 improvement(s)    (coverage)
```

and at the updated committed baselines ci shows

```
SUMMARY: total=242 match=91 ub_match=23 ub_diff=0 mismatch=0 fail=0 crash=0 lean_error=0 timeout=0 cerb_skip=110 cerb_inconsistent=18
```

**ci mismatch count 0 — 114/114 comparable agree (DERIVED). MILESTONE.**
Enumerated residual: 25 placeholder functions, per-line-tagged in
CerbPP ([AIL] 6, [CORE-PP] 8, [CABS] 3, [DEFACTO] 4, [CMM] 2,
[PRETTY] 2), all "<...>"-bracketed so any leak into compared text is
an honest mismatch. New `pp-test` unit exe; test_unit.sh → 6 exes.

### S3b — dead corpora wired ([USER] addendum; CERB `57fe96ab4` + `65f77606c`, D5)

**FLOAT:** tests/float (69 files, copied arc-4, never wired) is now a
Tier-A test_exec.sh lane with committed baseline. First sweep, before
any fixes (none needed), verbatim:

```
SUMMARY: total=69 match=69 ub_match=0 ub_diff=0 mismatch=0 fail=0 crash=0 lean_error=0 timeout=0 cerb_skip=0 cerb_inconsistent=0
```

The upstream float-mul boundary class did NOT surface — this corpus
exercises the concrete model's op_fval on both sides, not the buggy
lem-level Cerb_floating.mul; the baseline header banks the standing
classification rule (an oracle-indicting float differential is
boundary-entry evidence, never "fixed" to match).

**BYTES:** tests/bytes (14 files) wired as an ORACLE-INDEPENDENT
micro-lane (`scripts/test_bytes.sh`): the committed upstream
`.exec`/`.elab` records are the reference. 9 exec files MATCH at the
committed expecteds + 5 front-end-reject negative pins; fail-closed
both directions (doctored-expected probe + vacuous-pass refusal).
Recorded residual: the Lean desugar's own byte-typing rejections are
unprobed by this corpus (needs an oracle parse-only mode or Lean-side
C parsing — out of scope).

### S4 — the csmith campaign (CERB `fb36810a8` `bdb9f1967` `82be0b3b8` `b3944a7ac` `855224925` `ac509e962`, D6)

Record: `docs/2026-08-20_arc10-s4-csmith-campaign.md` (verbatim
evidence; artifact index tests/csmith_findings/README.md). In brief:

- **Configuration exploration ([USER] directive):** 21 oracle-only
  configs × 200 seeds (4,210 programs) with measured yield + construct
  coverage. THE DISCOVERY: csmith's default `--max-array-dim 3`
  wasted ~52% of generated programs on one upstream initializer
  defect (F-A); `--max-array-dim 2` recovers both yield AND construct
  density. Evidence-based 5-lane PORTFOLIO (P1_full / P2_value /
  P3_ub / P4_argc / P5_noptr), deterministic disjoint seed ranges.
- **Sweep:** 3 batches × 100 seeds × 5 lanes (1500 programs) + the
  1669-file in-tree corpus lane + 1 seeded prototype reproducer =
  **3169 differential programs, 100% of non-agreements classified,
  ZERO Lean-side semantic defects.**
- **Finding 8 FIXED** (CerbMem-only, `bdb9f1967`; the D1 park line
  never tripped): eqPtrval now mirrors the oracle's provenance-fork
  msum; coverage ptr3-006 DIFF → MATCH (classified baseline
  improvement); re-confirmed closed by the sweep.
- **F-D REATTRIBUTED** ([USER] root-cause directive): the fork's
  oracle — not upstream — corrupts execution on
  declaration-layout-sensitive witnesses. Decisive experiment: the
  un-forked upstream cerberus (prototype checkout @ 866be5254)
  returns the gcc/Lean-agreeing value on every tested witness
  (6000098 → 117, 6000018 → 100, sa_csmith_168 → 28). Mechanism
  evidence (campaign record §root-cause): alpha-equivalent Core,
  identical memory traces for ~397 actions, then a wrong-continuation
  jump in the Esave/Erun label machinery; head-vs-tail declaration
  insertion predictions TESTED (head shifts symbol numbers and
  changes the verdict; tail does not). Prime suspect: the arc-2 S1
  threaded sym_supply (core_run_aux.lem:233-247,287 — its own comment
  concedes the undischarged sym non-escape obligation) ×
  description-insensitive symbolEquality. The Lean side is unaffected
  (its supply threading is pure and collision-free — WHY the
  differential caught this). Repair = model-.lem surface, outside
  this arc's blessed scope: **PARKED as the TOP next-arc candidate**,
  priced M, with the full analysis attached (campaign record
  §root-cause + tests/csmith_findings/).
- **The record-integrity lesson (banked):** before the root-cause
  directive, F-D sat in the upstream tray. We were one step from
  filing OUR OWN regression as an upstream bug report. The upstream
  tray is now corrected (below), and the lesson generalizes: an
  oracle defect is not upstream-attributable until reproduced on
  un-forked upstream.

### S5 — close-out (this slice)

Housekeeping: stray untracked `platform.info` (csmith scratch — its
standard platform-probe output) removed; both worktrees clean.

**Harness fix (own commit `b4768cabf`):** the D6-queued full
corpus-lane baseline pass must run sharded (the external watchdog
kills ~2.5h background jobs; the lane is ~2.7h), but
`--check-baseline` composed only with full runs. test_csmith_corpus.sh
now checks partial runs against the committed baseline RESTRICTED to
the run's files, FAIL-CLOSED on corpus/baseline drift (count
equality), so the union of shards 1..M checks the entire committed
baseline exactly once. Plant-tested (deleted baseline line → refusal
rc 1 before any test runs; baseline restored md5-identical).

**The corpus-lane full baseline pass (D6's queued item):** all 6
shards (deterministic boundaries: the script's sorted-list partition,
279+279+279+279+279+274 files), foreground-sequenced, TIMEOUT_SECS=15,
SKIP_BUILD=1, capped 8G, at the committed
`scripts/exec_csmith_corpus_baseline.txt`. Verbatim per-shard tails:

```
csmith corpus lane: 279 of 1669 files (shard='1/6')
SUMMARY: total=279 match=93 ub_match=0 ub_diff=0 mismatch=7 fail=0 crash=0 lean_error=0 timeout=1 cerb_skip=178 cerb_inconsistent=0
Baseline check: 0 regression(s), 0 improvement(s)
BASELINE OK
csmith corpus lane: 279 of 1669 files (shard='2/6')
SUMMARY: total=279 match=137 ub_match=0 ub_diff=0 mismatch=5 fail=0 crash=0 lean_error=0 timeout=2 cerb_skip=135 cerb_inconsistent=0
improvement: sia_csmith_041.c baseline=TIMEOUT current=MATCH
Baseline check: 0 regression(s), 1 improvement(s)
BASELINE OK
csmith corpus lane: 279 of 1669 files (shard='3/6')
SUMMARY: total=279 match=127 ub_match=0 ub_diff=0 mismatch=2 fail=0 crash=0 lean_error=0 timeout=2 cerb_skip=148 cerb_inconsistent=0
improvement: sia_csmith_139.c baseline=TIMEOUT current=MATCH
Baseline check: 0 regression(s), 1 improvement(s)
BASELINE OK
csmith corpus lane: 279 of 1669 files (shard='4/6')
SUMMARY: total=279 match=223 ub_match=0 ub_diff=0 mismatch=0 fail=0 crash=0 lean_error=0 timeout=0 cerb_skip=56 cerb_inconsistent=0
Baseline check: 0 regression(s), 0 improvement(s)
BASELINE OK
csmith corpus lane: 279 of 1669 files (shard='5/6')
SUMMARY: total=279 match=267 ub_match=0 ub_diff=0 mismatch=0 fail=0 crash=1 lean_error=0 timeout=0 cerb_skip=11 cerb_inconsistent=0
Baseline check: 0 regression(s), 0 improvement(s)
BASELINE OK
csmith corpus lane: 274 of 1669 files (shard='6/6')
SUMMARY: total=274 match=225 ub_match=0 ub_diff=0 mismatch=1 fail=0 crash=1 lean_error=0 timeout=2 cerb_skip=45 cerb_inconsistent=0
Baseline check: 0 regression(s), 0 improvement(s)
BASELINE OK
```

(Each 4/5-line block is the verbatim tail of its shard run; blocks
concatenated here. Per-file lines in the S5 run logs.)

DERIVED roll-up: 1669 files, **0 regressions across all 6 shards** —
match 1072, mismatch-counter (the baseline's classified DIFF class)
15, timeout 7, crash 2, cerb_skip 573 (sums check: 1669). The 2
reported improvements (sia_csmith_041/139, TIMEOUT → MATCH) are the
registered TIMEOUT_LEAN_PERF class finishing just inside the 15 s cap
on this run — boundary-timing jitter within an already-classified
class, non-regressing; the committed baseline records the S4
observation and is NOT updated for run-to-run timing jitter (the
improvement path is reported-not-fatal by design). Every other file —
including all 15 F-D DIFFs and both CEILING_FUEL crashes —
reproduced its committed classified status exactly. D6's queued item
is DISCHARGED.

## Scoreboard deltas (arc open → arc close)

| lane | arc open (fb091fc93) | arc close | movement |
|---|---|---|---|
| tests/ci exec (Tier C scoreboard) | 88 MATCH + 22 UB_MATCH + 3 MISMATCH + 1 DIFF — 110/114 comparable agree | 91 MATCH + 23 UB_MATCH, **0 mismatch — 114/114 comparable agree** | finding 11 (0086 DIFF→UB_MATCH) + pp class (0006/0007/0046 MISMATCH→MATCH), all classified improvements |
| tests/coverage exec | 172 MATCH + 11 UB_MATCH + 2 MISMATCH + 1 DIFF — 183/186 comparable agree | 174 MATCH + 12 UB_MATCH, **0 mismatch — 186/186 comparable agree** | mem3-004 DIFF→UB_MATCH (finding 11), mem-006 MISMATCH→MATCH (pp class), ptr3-006 MISMATCH→MATCH (finding 8) |
| tests/minimal exec | 103/106 (3 oracle-side skips) | unchanged (zero movement at every commit) | — |
| tests/debug exec | 66 MATCH + 18 UB_MATCH, 1 recorded DIFF | unchanged | — |
| tests/float exec | NOT RUN (dead corpus) | **69/69 MATCH**, committed baseline | new lane (S3b) |
| tests/bytes | NOT RUN by anyone (survey) | **9/9 exec at committed expecteds + 5/5 neg pins** | new oracle-independent lane (S3b) |
| csmith corpus (1669 in-tree) | no lane | **committed classified baseline: 1070 MATCH / 573 CERB_SKIP / 15 DIFF (all F-D fork-oracle) / 9 TIMEOUT (Lean perf) / 2 LEAN_CRASH (fuel ceiling)** | new lane (S4); full baseline pass re-verified at S5 |
| csmith generated lanes | kit only, 12% oracle yield (arc-4 record) | 5-lane portfolio, 1500 programs swept, yield up to 47.5%/lane | campaign (S4) |
| libc/uri/chvalid/multi_tu/verify | green | green, zero movement | — |

(Row values DERIVED from the committed baseline files + slice records;
the underlying SUMMARY lines are verbatim in the cited records.)

## Register (full disposition at close)

Numbering = the S0 triage table (`docs/2026-08-20_arc10-s0-triage.md`).

**CLOSED this arc:**

| # | item | disposition |
|---|---|---|
| 1 | finding 11 — read-only allocations | FIXED S1 (CerbMem mirror impl_mem.ml:1304-1333); ci 0086 + coverage mem3-004 → UB_MATCH |
| 2 | pp-placeholder ctype text class | FIXED S3 (real printer mirrors); 25-function enumerated residual, honest-bracketed; ci mismatches → 0 |
| 3 | finding 8 — eqPtrval msum provenance fork | FIXED S4 fix-batch 1 (CerbMem-only, `bdb9f1967`); ptr3-006 + seed-930005 class → MATCH; re-swept clean |
| 11 (#29-class) | format_string_of_float stub | FIXED S3 (CerbFloat exact %.12g/%.<prec>f mirrors vs OCaml reference transcript) |
| 12 | R1 — LemLib `Ord (Sum a b)` (+ missing BEq) | FIXED S2a (real instances, parity cited) |
| 13 | sorried BEq/Ord/SetType/Eq0/Ord0 comparison bodies | ELIMINATED S2b: 1134 → 0; mechanism general + fail-closed |
| 14 | set-comprehension sorry stub | CLOSED S2b (loud generation-time error, negative-probed) |
| 21 (half) | stringFromMemValue partial | TOTALIZED S3 (structural mutual recursion). Gate-scan-extension half stays parked (below) |
| 28 | quadratic allocation retention (perf) | CLOSED AS MITIGATED (arc-6 TreeMap swaps; no standing corpus perf-bound; successor perf items are the NEW entries below) |

**NEW entries this arc:**

| item | evidence | disposition |
|---|---|---|
| **F-D fork-oracle regression** (declaration-layout-sensitive corruption: spurious UB / internal errors / silent wrong values) | 35 witnesses [CORRECTION 2026-08-21, audit fix-batch: originally "24" — recount from the campaign record's own per-witness lists: 11 generated-lane (P1 2 + P2 3 + P3 1 + P4 1 + P5 4) + 16 corpus (15 DIFF rows + sa_csmith_190 under a perf timeout) + 8 B1-exploration internal-error seeds (3016022/44/49/51/58/74/82/99); decision-log correction C1]; root-cause analysis campaign record §root-cause; artifacts tests/csmith_findings/ | **TOP NEXT-ARC CANDIDATE.** Repair surface: frontend/model core_run_aux.lem:233-247,287 + friends (arc-2 threading region), priced M (probe-first lem work + full validation ladder). Until repaired: affected files sit classified in baselines as oracle-side (Lean+gcc agree); F-D suppresses differential VALUE on those files, never corrupts a green baseline (a wrong oracle verdict vs independent correct Lean = visible non-MATCH) |
| Lean interpreter perf gap 2.4–3x (worst 50x on the fork-noise case sa_csmith_190) | 15 TIMEOUT_LEAN_PERF entries (6 generated + 9 corpus), uncapped sequences verified identical | REGISTERED (perf). Mover: interpreter perf arc / after F-D repair re-measure |
| CEILING_FUEL: loud `lem: fuel exhausted` on ≥tens-of-thousands-iteration goto-loops | sia_csmith_477/769 | REGISTERED; fuel policy = CerbND/driver seam (forbidden this arc). Harness now captures the marker |
| F-E channel split (fork `--cabs-json` hard-errors on desugar UB081 where `--exec` defers to a verdict) | 3 CERB_INCONSISTENT | fork-side, COSMETIC register note (exporter channel); the UB081 class itself is upstream-shared (tray question) |
| S2b comparison-parity caveat set [broadened 2026-08-21, auditor A F4]: (a) `deriving Ord` mixed-variant leaf order (Lean flat constructor-index vs OCaml two-class rank); (b) `Ord Float` NaN leaf divergence (Lean's total Float compare vs OCaml polymorphic compare's NaN convention); (c) fn-residual failwithI raises where OCaml polymorphic compare short-circuits on physical equality (`x == y` succeeds before the closure raise) | (a) S2b commit (pre-existing deriving-path divergence, made explicit); (b)+(c) audit A F4/F5 review of the derivation seam | REGISTERED (lem C-tier); all three unreachable today: (a) only via leaf instances of mixed simple variants, (b) no Float-keyed comparison in any compared path, (c) fires only on physically-identical fn-carrying values, where OCaml would raise on any structural difference anyway |
| S2b residual populations: 21 fn-residual types (210 loud failwithI bodies) + 494 tv-fallback sites | S2 census (D4) | REGISTERED, counted; loud + greppable, not sorry. Mover: none needed unless a corpus demands comparison on fn-carrying types (OCaml raises there too — parity) |

**Refreshed / parked (movers stand):**

| # | item | state |
|---|---|---|
| 4–7, 9–11 | findings 9/13/14/16/24/25 + the 27/30 extract tail (CerbMem seam classes; the #29-class member of row 11 closed in S3) | S4-IF-FORCED never forced — 3169 programs, zero hits in their classes. PARKED as priced in the arc-4/S0 records |
| 8 | finding 23 — constraint pruning (CerbND) | FORBIDDEN surface this arc; parked. csmith UB_DIFF classes did not force it |
| 15 | polymorphic Let_defs binders | PARKED (unreachable-loud; pricing stands) |
| 16 | column-0 continuation emission | PARKED (D1 ruling 4: no scope creep) |
| 17 | R2 — 4.28 eager closed-term init (lem-suite) | PARKED (mover: lem-suite toolchain bump) |
| 18 | lem tests/backends leantests srcDir breakage | PARKED (S2 did not touch that harness) |
| 20 | stack ceiling | REFRESHED: onset 16384 < N ≤ 24576 plain loop iterations, uniformly LOUD exit 134 (S0 re-probe, 5/5) — the arc-6 "~1.4k, quiet-death" text is superseded. Presumed mover of the onset: arc-7 totalization. Item stays parked (CerbND, arc-9 turf); distinct from the S4 CEILING_FUEL bucket (fuel policy, loud marker) |
| 21 (half) | CerbMem into the totality-gate scan | PARKED (D1 ruling 5: arc-9 coordination; post-merge candidate) |
| 22 | iris-lean mirror in deps/mirrors | OUT (operator network-window item) |
| 23 | CoreParser totalization | PARKED (priced, arc-7) |
| 25 | finding 18b — enum registry stub | DOC-DELIB; the S4 watch did not trip (no enum-class sweep hit) |
| 26 | float-mul mirrors-diverges + tags.c + CerbStepInstances leaf caveat | DOC-DELIB; float stayed off in the lanes; the S3b float lane's classification rule banked in its baseline header |
| 27 | harness caveats (both-sides-timeout invisibility; stdout spoofing) | DOC-DELIB, restated in the S4 record's classification rules |

## Upstream tray disposition

- **F-A (initializer desugar, AilEinvalid on nested braced
  initializers — killed ~52% of default-flag csmith programs):
  READY.** Genuinely upstream (reproduced VERBATIM on un-forked
  upstream); minimal un-forked repros committed
  (tests/csmith_findings/oracle/init_array_3d.c,
  init_struct_depth3.c). SHARED-MODEL note: the Lean pipeline
  inherits the same defect through the shared .lem desugar.
- **F-B (ISO-legal address-constant initializer rejected): READY.**
  Upstream-reproduced; repro init_addr_const.c; same strictness
  family as the libxml2 probe #1 / wireguard edit-2.
- **F-D: PULLED from the tray** (fork regression, see above). The
  wireguard scoping survey's §2a "oracle exec-driver defect" is
  re-attributed by addendum in that note.
- **F-E: DISSOLVED** — (a) an upstream-shared UB081
  scalar-initializer question (probably a Desugaring_init
  false-positive, F-A's neighborhood — tray as a QUESTION, not a
  report) surfacing through two fork channels (the hard-vs-deferred
  channel split is fork-side cosmetic, registered); (b) a trivial
  stage difference (cabs-json never runs translation, so F-A cannot
  fire there).

## Success conditions (charter, machine-checkable) — verdicts

1. ci scoreboard strictly improved, finding-11 files → MATCH class;
   standing baselines zero-movement at every commit — **MET**
   (110/114 → 114/114; the only baseline movements are the three
   classified fix improvements, each its own justified commit).
2. Sorry census: comparison bodies eliminated, counts recorded — **MET,
   at the strong end** (1134 → 0; residuals loud + counted; no new
   sorry/axiom/opaque — absence gates green throughout).
3. csmith swept at the S0-chosen scale, N reported, 100% of mismatches
   classified with committed artifacts, fixes re-swept — **MET**
   (N=3169 differential + 4210 oracle-only exploration; ledger in the
   campaign record; finding-8 fix re-swept clean).
4. pp text class implemented + validated, register item closed or
   enumerated-residual — **MET** (closed WITH a 25-item enumerated
   residual).
5. Zero forbidden-surface commits; lakefile touch-point per discipline
   — **MET** (worker diffstat checks at every boundary; audit scope
   (d) re-verifies from the diffstat).
6. lem changes probe-first with comprehensive coverage incl. negative
   probes; every lem checkpoint cerberus-scale-validated; pins aligned
   at close — **MET modulo the closing pin dance** (Lake pin =
   lem branch head a9387fb now; opam pin moves at merge — checklist
   steps 5–7).
7. Records complete, branches gate-green, merge checklist ready,
   mainlines untouched — **MET** (this doc; checklist
   `docs/2026-08-21_arc10-merge-checklist.md`; merge is operator-gated
   and SERIALIZES with arc 9).

## Close-out gate evidence (S5, at the final commit)

Full set re-run at the close-out head (all rc 0; D4 standing rule:
capped default-target build included, CERB_MEM_MAX=40G while the
arc-9 lane is live). One verbatim line/fragment per gate:

| gate | verbatim fragment |
|---|---|
| capped default-target `lake build` | `Build completed successfully (593 jobs).` |
| test_unit.sh | `Total: 6 passed, 0 failed` (all gate scripts green: sync 21 files, censuses, purity, totality CLEAN, theorem-axiom cones DAEMON-free) |
| exec tests/minimal | `SUMMARY: total=106 match=85 ub_match=18 ub_diff=0 mismatch=0 fail=0 crash=0 lean_error=0 timeout=0 cerb_skip=3 cerb_inconsistent=0` + `BASELINE OK` |
| exec tests/coverage | `SUMMARY: total=199 match=174 ub_match=12 ub_diff=0 mismatch=0 fail=0 crash=0 lean_error=0 timeout=0 cerb_skip=13 cerb_inconsistent=0` + `BASELINE OK` |
| exec tests/ci | `SUMMARY: total=242 match=91 ub_match=23 ub_diff=0 mismatch=0 fail=0 crash=0 lean_error=0 timeout=0 cerb_skip=110 cerb_inconsistent=18` + `BASELINE OK` |
| exec tests/debug | `SUMMARY: total=90 match=66 ub_match=18 ub_diff=0 mismatch=1 fail=0 crash=0 lean_error=0 timeout=0 cerb_skip=4 cerb_inconsistent=1` + `BASELINE OK` (the 1 mismatch is the recorded baseline DIFF) |
| exec tests/float | `SUMMARY: total=69 match=69 ub_match=0 ub_diff=0 mismatch=0 fail=0 crash=0 lean_error=0 timeout=0 cerb_skip=0 cerb_inconsistent=0` + `BASELINE OK` |
| test_bytes.sh | `SUMMARY: exec_match=9 neg_pinned=5 fail=0` + `ALL AT COMMITTED EXPECTEDS` |
| test_libc_exec.sh | `SUMMARY: match=7 diff=0` + `ALL MATCH RECORDED BASELINE` |
| test_multi_tu.sh | `SUMMARY: total=2 match=2 fail=0` + `ALL PASSED` |
| test_verify.sh | `test_verify: 29 passed, 0 failed (5 fixtures, 18 harness points)` |
| csmith corpus lane (full, sharded) | 6/6 shards `BASELINE OK`, 0 regressions (verbatim tails in the S5 section above) |

The S5 harness commit's own evidence (shard-aware baseline check,
positive + plant-test) is in `b4768cabf`'s commit message.

Post-merge (orchestrator): container CLAUDE.md / ROADMAP updates (arcs
line, register/queue lines, the F-D top-candidate call-out) — outside
this stream's write surface.

---

## ADDENDUM (arc-12 S2, 2026-08-21)

The register row "F-D fork-oracle corruption … root-caused (fork
regression, arc-2 threading suspect)" carries two arc-12 updates:
(1) ATTRIBUTION corrected — the mechanism is the April desugar
threading (8923d6436), the arc-2 supply exonerated on all probed
witnesses (campaign-record addendum); (2) STATUS: REPAIRED-BY-
FAIL-STOP — all 35 witnesses now loud (CERB_FLOOR), corpus
re-baselined, renumbering deferred with the case recorded
(arc-12 records). The perf-gap row's "after F-D repair re-measure"
mover is NOW LIVE for the surviving TIMEOUT rows (5 after
re-baseline).
