# Charter — program-data parameters, S1.5: fuel'd truly-mutual blocks compose with reader lifting (the second paired lem-lean slice) and the cerberus re-pin (2026-09-20)

**Status:** CHARTER for one Claude Fable subagent worker (Part A: the S0.5 lem-lean worker resumed — it knows the seed/reader machinery), written by the orchestrator [AGENT]. Same shape as S0.5 (`2026-09-19_charter-program-data-parameters-S0.5.md`): Part A in lem-lean, an orchestrator boundary (pin move + `make rebuild-lem`), Part B the cerberus pin bump with byte-identical regeneration. It exists because E-A Phase 1 (`2026-09-20_charter-program-data-parameters-EA-DA.md`) stopped at its rule S4 on the FIRST regeneration with the enum reader declared, verbatim (`lean_frontend/lem.log:384-386` at `e110d7db2` + the Stage A lem edits):

```
File "frontend/model/ail/ailTypesAux.lem", line 784, character 1 to line 862, character 51 processed by: compile_faux_seplist, mk_case_exp
  Error: Lean backend: 'declare {lean} fuel val' in a mutual block combined with reader lifting (unsupported; extend when needed)
```

- **Part A (lem-lean):** branch `mutual-fuel-readers` in the primed worktree `worktrees/lem-lean-mutual-fuel-readers` (base: lem-lean mainline `mdd/lean-backend` = `4307dc5` = the pin). Lift the refusal: a fuel'd (and `fuel_measure`d) TRULY-MUTUAL block whose members are reader-lifted must be emitted — workers, wrappers, obligations — with the reader binders, exactly as the single-def fuel × reader case is today.
- **Orchestrator boundary:** re-verify Part A; `git -C deps/lem-pinned reset --hard <A head>`; `make rebuild-lem`; confirm `lem -v`; re-launch.
- **Part B (cerberus-lean):** on `arc/program-data-parameters` (head `e110d7db2`; the E-A worker's uncommitted Stage A edits are saved as a patch and reverted first), the Lake pin bump to Part A's head in `be1cebe36`'s four files + the fork-drift `lem-pin` row + NOTE (the S0.5 practice), both generated trees BYTE-IDENTICAL to their `e110d7db2` state (no fuel'd mutual block is reader-lifted at the current `.lem` — the guard never fires today, so the fix must be invisible), Tier A. Then E-A Phase 1 resumes in the same worktree.

## 0. Rulings (verbatim), decisions, standing constraints

- [USER 2026-09-19] delegation: *"I'm interested in making decisions that are consequential in some way but for the other kinds of decisions, which are really more implementation-focused, I think you can make the calls."* This slice is implementation-focused: the semantics does not move; a backend limit is lifted so that the already-decided E-A route can be generated.
- [USER 2026-09-04]: *"we don't change the lem structure for ocaml"* — the `are_compatible` blocks (`ail/ailTypesAux.lem:784-862`, `ctype_aux.lem:283-297`) need the enum map on BOTH targets (enum ↔ compatible-type compatibility is semantics), so no lem-source reshaping is available; the backend must compose the two mechanisms.
- [USER 2026-09-11]: Fable-class workers; the orchestrator re-verifies. [USER 2026-09-08] verbatim: *"we should \*NOT\* be building anything new out-of-policy"* — tests and probes only.
- Standing constraints: NEVER modify machine-global state (no opam changes; `make rebuild-lem` is the orchestrator's step); NO `git push`; never commit on `mdd/lean-backend`, `mdd/cerberus-lean`, or either `master`; no merge/rebase; fail-closed/fail-noisy (no `2>/dev/null` on a build/gate step); [AGENT]/[USER] provenance; verbatim outputs, derived tallies labelled; every `lake`/`lean` capped (the comprehensive Makefile's `LAKE`); gate scripts through `scripts/ce`; ONE heavy job; the ~45-minute step tripwire (Part B's full Lean rebuild excepted up to ~1 h, wall time reported); never end the turn on a wait; Part A ends at its commit.

## 1. Facts (verified by the orchestrator; cite, do not re-derive; errata to the record)

- **The guard:** `src/lean_backend.ml:4604-4606` at `4307dc5`: `| Some _ when is_truly_mutual && lifted -> raise … "Lean backend: 'declare {lean} fuel val' in a mutual block combined with reader lifting (unsupported; extend when needed)"`, introduced by `a618b9c` "Lean backend: fuel composes with mutual blocks (arc 3, B2)" — READ that commit's diff and record for the ORIGINAL reason the composition was left out (the record must state it and say why it no longer holds, or what you did about it). The only probe of the guard is `tests/comprehensive/negative/neg_fuel_mutual_lifted.lem` (`neg_rping`/`neg_rpong`, reader `neg_env`, fuel `1`/`2`; EXPECT "in a mutual block combined with reader lifting").
- **What already composes (single defs):** the comment `:4608-4612` "fuel x reader composes (arc 3, B1): the worker's fuel counter is emitted BEFORE the reader binders, so the point-free wrapper 'worker LemFuel.fuel' has the reader-prefixed type and lifted callers inject into the wrapper as for any lifted def"; the wrapper head `:4741-4778` (`reader_arrows` when `lifted`, `supply_arrows`, `[LemFuel]`, `[Inhabited]`, `: readers -> supply -> result := worker LemFuel.fuel`); the `fuel_measure` obligation `:4836-4866` is generic — `reader_binders`/`reader_args` sit between `inhabited_binder_output` and `arg_binders`, and today's generated obligations already carry `_lemReader_tagDefs` (`lean_frontend/generated/{Defacto_memory,Core_eval,Driver,Core_aux}_auxiliary.lean`). Tests: `tests/comprehensive/test_fuel_param.lem:46-60` (`climb`, fuel × reader × seed) and `:78-81` (`mping`/`mpong`, a fuel'd mutual block WITHOUT readers); `test_fuel_measure.lem:89-98` (`mev`/`modd`, a measured truly-mutual block, all-or-none).
- **The mutual emission:** `:4228` `groups, is_truly_mutual`; `:4396-4399` fuel all-or-none in a mutual block; `:4405-4415` `workers_need_fuel` (self/sibling calls pass the counter and are excluded from the ambient test); `:4427-4432` `fuel_measure` all-or-none (FM-mutual); `:4935-4942` the `mutual … end` rendering with the fuel wrappers after `end`; `:4503-4505` `reader_seed` in a mutual block refused (STAYS — out of scope); `:4570-4572` supply lifting in a truly-mutual block refused (STAYS — out of scope; note it in the record as the remaining composition gap).
- **The cerberus consumer of the fix (for the record's forward pointer, not for this slice):** `ail/ailTypesAux.lem:784-862` `are_compatible`/`are_compatible_params_aux`/`are_compatible_params` (fuel `:1336-1338`, `fuel_measure` `:1348-1350`; proofs `lean_frontend/AilTypesAux_lemMeasureProofs.lean`) and `ctype_aux.lem:283-297` `are_compatible_aux`/`are_compatible_params_aux`/`are_compatible_params` (measures via `CerbCtypeMeasure`; proofs `Ctype_aux_lemMeasureProofs.lean`). After the fix both blocks' obligations gain the reader binders (`digest`, `enum_definitions`, `tagDefs`) and the hand-written proofs are restated (E-A Phase 1's job, fence extended there).
- **The pin triple today:** lem-lean mainline `mdd/lean-backend` = `deps/lem-pinned` (`cerberus-pin`) = `scripts/ce lem -v` `Lem 4307dc5` = Lake `LemLib` rev `4307dc5108bbe71de2f323712443cab6c2b3c0da` (`lean_frontend/lakefile.toml:54`, `lake-manifest.json`, `speclab/lake-manifest.json`, `tests/mem-scale-probes/micro/lake-manifest.json`) = `scripts/fork_drift_manifest.txt` `[meta] lem-pin=4307dc5` (the cross-check `check_fork_drift.sh:169-175`). `lean-lib/` must stay byte-identical (this is a backend-only change).

## 2. Deliverables

### Part A — lem-lean (worktree `worktrees/lem-lean-mutual-fuel-readers`, branch `mutual-fuel-readers`)

**A0 — The before-snapshot in cerberus** (first action; the E-A worker has already reverted its lem edits when you start — confirm `git -C <arc wt> status --short` shows only the untracked record draft): `scripts/ce lem -v` → `Lem 4307dc5`; if the trees are not freshly derived, `scripts/ce make clean-prelude-src clean-sibylfs-src; rm -rf lean_frontend/generated; scripts/ce make prelude-src lean-prelude-src` (a WIPED derivation — the S0.5 lesson: `make prelude-src` alone is a timestamp no-op); hash both trees into the arc worktree's `.tmp/s15/gen-before.sha256` (record the count; 305 expected).

**A1 — The backend rule.** Build lem (`scripts/ce make` at the worktree root → `./lem`). Read `a618b9c` and state the original gap. Then make a fuel'd truly-mutual block whose members are reader-lifted emit correctly: every member's WORKER takes `[LemFuel]`-if-needed, `[Inhabited]`, the fuel counter, the reader binders, (no supply — that composition stays refused), its own arguments; sibling calls pass the decremented counter AND inject the reader binders (as any lifted call does — confirm the injection path is generic); every member's WRAPPER after `end` is `def f [LemFuel] … : readers -> result := f_lemFuel LemFuel.fuel` (point-free, reader-prefixed type) or, under `fuel_measure`, the measured wrapper binding the parameters with the reader binders in the generic position; every member's obligation carries `reader_binders`/`reader_args` as the single-def path does; the `_zero` exhaustion lemma states the worker at literal 0 on its own binders INCLUDING the readers (check how the single-def `_zero` lemma treats readers and mirror it). Delete the guard at `:4604-4606`; keep the `reader_seed`-in-mutual and supply-in-mutual refusals. If the fix needs more than the emission of binders (e.g. the `workers_need_fuel`/`fuel_lifted` fixpoint interacting with `reader_lifted` for mutual groups), do it and record why — but if it needs a LemLib change or a grammar change, STOP (S1).

**A2 — Tests.** `tests/comprehensive/test_fuel_mutual_reader.lem` (+ `lean-test/TestFuelMutualReaderCheck.lean`, `TestFuelMutualReaderExec.lean`, a `lean_exe test-fuel-mutual-reader`, a Makefile phase `lean-fuel-mutual-reader` in the `lean:` list, the `.lem` in the `LemComprehensiveTest` roots): a reader `amb : unit -> nat`, a truly-mutual pair `rping`/`rpong` where BOTH read the reader (and a variant where only ONE member reads it — the other must still lift, all-or-none by the fixpoint), fuel'd with sentinels; a SECOND truly-mutual pair under `fuel_measure` (measures over the parameters; the proofs in `lean-test/Test_fuel_mutual_reader_lemMeasureProofs.lean`, kernel-only tactics, the C2 template); a `fuel_consumer`/fuel-lifted callee inside one member so `workers_need_fuel` is exercised WITH readers; kernel pins: signatures (`[LemFuel]`, readers, own args — quote the generated heads in the record), value pins where the reader value changes the result, the `_zero` lemmas' statements, `#print axioms` = the trio or fewer; the Exec leg re-asserts compiled. `negative/neg_fuel_mutual_lifted.lem` becomes the positive family's ancestor: DELETE it (its EXPECT text no longer exists) and record the deletion; add `negative/neg_fuel_mutual_supply.lem` if no probe pins the supply-in-mutual refusal yet (check first; `EXPECT: supply lifting in a (truly) mutual block`). `invariance/inv_fuel_mutual_reader.lem` (the `{lean}` declares stripped leave ocaml/hol/isa/coq byte-identical).

**A3 — The byte-identity sweep** (S0.5 A3, same corpora, `-outdir` scratch dirs under `.tmp/`): pinned `scripts/ce lem` (`Lem 4307dc5`) vs the new `./lem` over `tests/comprehensive/test_*.lem`, the `tests/backends` `leantests` set, `examples/ppcmem-model` (10 files together), `examples/cpp/cmm.lem`: IDENTICAL everywhere except the new test (which the pinned lem must REFUSE with the guard's text — quote it); any other difference is stop rule S2.

**A4 — Docs.** `doc/lean-backend/DESIGN.md` (the fuel row's composition sentence and the mutual-block paragraph: fuel × reader now composes for truly-mutual blocks; supply × mutual and seed × mutual remain refused); a dated record `doc/lean-backend/2026-09-20_fuel-mutual-reader-record.md` (the original gap from `a618b9c`, what changed, the emitted shapes verbatim, the tests, the sweep, every gate tail verbatim, the cerberus follow-up: the two `are_compatible` blocks' proofs restated in E-A).

**A5 — Commit ONCE**, quote the head hash, `git diff --stat 4307dc5 -- lean-lib` (empty), `git status` clean. STOP; report.

### Orchestrator boundary (not the worker's)
Rebuild + re-run the suite and the sweep independently; `git -C deps/lem-pinned reset --hard <A5 head>` (look at the worktree's status first); `scripts/ce make rebuild-lem`; `scripts/ce lem -v` = `Lem <A5 short>`; re-launch for Part B.

### Part B — cerberus-lean (the E-A worker, in `worktrees/cerberus-lean-arc/program-data-parameters`, on the re-launch)
**B1** Lake pin bump to Part A's FULL hash in the four files (`scripts/ce ../scripts/capped lake update LemLib` from `lean_frontend/`; confirm the fetched package HEAD and `lean-lib` identity `4307dc5..<A>`); **B2** `scripts/fork_drift_manifest.txt` `[meta] lem-pin=<A short>` + one dated NOTE (single-row; never `--refresh`); **B3** WIPED regeneration under the new lem, hash → `.tmp/s15/gen-after.sha256`, `diff` against `gen-before.sha256` EMPTY (quote both counts), `tools/check_lem_sync.sh --check` + `--check-lean` PASS without re-record; **B4** build + Tier A rows 1–12 incl. 4b/4c/6b, tails verbatim (`check_fork_drift: OK … lem-pin <A> = lem -v`); **B5** the record `lean_frontend/docs/2026-09-20_program-data-parameters-S1.5-record.md` (Part A cited, the pin triple, the identity witness, the tails, the charter errata) and ONE commit (5 files + record). Then re-apply the Stage A patch and CONTINUE E-A Phase 1 per its charter — the `are_compatible` blocks now generate; `AilTypesAux_lemMeasureProofs.lean` and `Ctype_aux_lemMeasureProofs.lean` are added to that charter's fence for the restatement (orchestrator ruling, recorded in the E-A record's errata).

## 3. Fence, forbidden, stop rules

**Fence (Part A):** `src/lean_backend.ml` (the mutual-block fuel/reader emission and the guard — nothing else); `tests/comprehensive/test_fuel_mutual_reader.lem`, `lean-test/TestFuelMutualReader{Check,Exec}.lean`, `lean-test/Test_fuel_mutual_reader_lemMeasureProofs.lean`, `lean-test/lakefile.lean` (roots + one exe), `tests/comprehensive/Makefile` (one phase + list lines), `negative/neg_fuel_mutual_lifted.lem` (delete), `negative/neg_fuel_mutual_supply.lem` (add if absent), `invariance/inv_fuel_mutual_reader.lem`; `doc/lean-backend/DESIGN.md`, `README.md` (only if it states the refusal), the record. **Fence (Part B):** the four pin files, the manifest row + NOTE, the record. **Forbidden:** `lean-lib/`; the `.ott` grammar; the ocaml/hol/isa/coq emitters; the supply-in-mutual and seed-in-mutual refusals; any cerberus `.lem`/hand-written Lean/script/baseline in Part B (the Stage A patch is re-applied only AFTER the Part B commit); `deps/lem-pinned` and `make rebuild-lem` (orchestrator only); opam/switch; machine-global state; `git push`; mainline commits; uncapped Lean; more than one heavy job; a wholesale manifest `--refresh`.

**Stop rules:** **S1** the fix needs LemLib or a grammar change; **S2** a non-identical output in the A3 sweep (other than the new test) or in B3 — a finding; **S3** a step past ~45 minutes (Part B's rebuild excepted to ~1 h); **S4** a rule conflict; **S5** Part A ends at A5's commit.

## 4. Gates

**Part A:** `scripts/ce make` (root; `./lem -v`); `scripts/ce make -C tests/comprehensive lean` — every phase green incl. the new `lean-fuel-mutual-reader`, `lean-negative` (the deleted probe gone, the supply probe present), `lean-invariance` incl. the new witness, `lean-no-sorry-proofs` (the new proofs module scanned), `lean-parity`; the A3 sweep; `#print axioms` on the new test's wrappers and obligations = the trio or fewer; `git diff --stat 4307dc5 -- lean-lib` empty; `git status` clean. **Part B:** the pin triple quoted; the B3 hash diff empty with both counts; both lem-sync checks without re-record; Tier A rows 1–12 (+4b/4c/6b) at their LADDER bars; `git status` clean after the commit.

## 5. Record and reporting

Part A's report: the head hash, `./lem -v`, every phase tail, the sweep verdict per corpus with the pinned refusal quoted, the generated heads of the new test's workers/wrappers/obligations (verbatim), `a618b9c`'s original reason in one sentence, deviations. Part B's report: the head, the triple, the empty diff, the Tier A tails, deviations — then E-A Phase 1 continues under its own charter's reporting.
