# Charter — address-space bound, PART TWO: the witnesses' gating pin, the bound as a quantified entry parameter (route A), and the fork-only OCaml flag (2026-09-17)

**Status:** CHARTER for one Claude Fable subagent worker, written by the orchestrator [AGENT]; branch `arc/address-space-bound-part-two` in the primed worktree `worktrees/cerberus-lean-arc/address-space-bound-part-two` (base: mainline `e64819de7` = part one landed). This is the SECOND HALF of `2026-09-16_charter-allocator-soundness-address-bound.md` (read it first — its §0 rulings, §1 facts and §3 rules carry over unless amended here); part one (C1/C1b/C1c) landed at `e64819de7` with the record `2026-09-16_allocator-soundness-address-bound-record.md`, whose §C2 states why the original route needed a wider fence and lists the options; the operator chose route (A). The worker commits its own work per deliverable, on green only; the orchestrator re-verifies every gate independently; the committed record ENDS the slice.

## 0. Rulings (verbatim; additions to part one's §0)

- [USER 2026-09-17]: *"yes, agreed regarding landing C1 as-is and then working on C2/C3 separately"* — given to the orchestrator's proposal that C2 take route A with an extended fence (below).
- [USER 2026-09-17]: *"I have confirmation from cerberus-sl that 'part 1' is exactly what they need to close their proof gap. And 'part 2' when landed will have modest blast radius for them"* — the consumer re-pin note for this slice lists the exact old→new signatures and stops there.
- [USER 2026-09-17]: *"Makes sense, Let's do the sequence as you propose. We should do this soon but it's not a blocker"* — to: part two first, then the enum-registry slice (design note first), then the concurrency landing. The enum registry (`CerberusImpl.typeof_enum`) is NOT in this slice.
- Part one's §0 rulings apply verbatim: the bound is a quantified parameter whose matched-mode instance is upstream's value; the fork-only OCaml flag is wanted; the no-magic-values rule; the Fable-subagent mode; the [USER 2026-09-08] ban; the standing constraints (never machine-global state; NO push; no mainline commits; no merge/rebase by the worker; fail-closed/fail-noisy; [AGENT]/[USER] provenance; verbatim outputs; capped Lean; one heavy job; the ~45-minute stop; ≤10-minute foreground polling; never end the turn on a wait).

## 1. Facts (verified by the orchestrator 2026-09-17 at `e64819de7`; cite, do not re-derive; errata to the record)

**Part one's residue.** The two witnesses `tests/minimal/112-allocator-exhausted-single-request.c` and `113-allocator-exhausted-single-request-overlap.c` are rows of `scripts/exec_baseline.txt` with class `CERB_SKIP`: `scripts/test_exec.sh:553-558` classes an oracle `Error {` line as an oracle-side non-comparison and never samples Lean. Row 10 pins them (two `shared-model-fix` register rows); the three-engine report shows `lean_agreement` on both; NO fork-vs-Lean lane GATES them. `scripts/gcc_oracle_baseline.txt` has no rows for them (the gcc lane reported them "new file (not in baseline, not fatal)"; Z1 hand-inserted its rows sorted by key, header lines 6-7). The immaculate lane (`scripts/test_immaculate.sh`, Tier B row 5, `tests/immaculate/{nolibc,libc}/*.c`, baseline `tests/immaculate/baseline.txt`) tokenises `Error {msg: "M"}` as `ERR:{msg: "M"}` (`:94`, `:129`) and records both-sides classes (`MATCH | L=…`, `:106`); `--record-baseline` rewrites the file (`:59-79`).

**The bound today** (unchanged from part one §1): `memory/concrete/impl_mem.ml:503-516` `initial_mem_state` value with `:508` `last_address= Z.of_int 0xFFFFFFFFFFFF`; `memory/vip/impl_mem.ml:170`; `memory/symbolic/impl_mem.ml:569`, `memory/cheri-coq/impl_mem.ml:271` (no cursor); interface `ocaml_frontend/memory_model.ml:39` `val initial_mem_state: mem_state`; `frontend/model/mem.lem:41-45`; Lean `CerbMem.lean:153` (`lastAddress : Address := 0xFFFFFFFFFFFF`), `:1780` `initialMemState : MemState := {}`; generated `Driver.lean` `layout_state := CerbMem.initialMemState`.

**The consumers of the driver-state builder** (`frontend/model/driver.lem:1520-1543`: `initial_driver_state_with run_st file fs_state` with `:1526` `layout_state= Mem.initial_mem_state`; wrappers `initial_driver_state file fs_state` `:1535` and `initial_driver_state_given sup file fs_state` `:1540`):
- OCaml, ladder-built: `backend/common/driver_ocaml.ml:157` and `:198` (`batch_drive`, the non-batch driver; `driver_conf` `.ml:11-16`/`.mli:3-8`).
- OCaml, NOT ladder-built: `backend/web/instance.ml:635` (package `cerberus-web`, `backend/web/dune:10-19`), `backend/ocaml/runtime/rt_ocaml.ml:355` (no dune file). `scripts/common.sh build_cerberus` builds only `backend/driver/main.exe cerberus-lib.install` (`:193`) and `cerberus.install` (`:231`); `backend/bmc/` (package `cerberus-bmc`) does not call the builder.
- The const-expr mini-run: `frontend/model/cabs_to_ail.lem:1137` `Mini_pipeline.evalIntegerConstantExpression sup loc core_env sigm ty_opt expr` (inside the desugarer, with the desugar supply `E.get_fresh_sym_supply`) → `frontend/model/mini_pipeline.lem:201-206` → `:163` `Driver.initial_driver_state_given sup_after_elab dummy_core_file Fs.fs_initial_state`. No bound is in scope there.
- Lean, non-generated: `lean_frontend/Main.lean:1019` (`initial_driver_state supply runFile fsState`), `test/Unit/FuelExemplar.lean`, `test/Unit/FuelFormsTool.lean`, `speclab/test/SLUnit/{TreeGateTest,CoreGateTest,ByteArrGateTest,SeedGateTest,ListGateTest}.lean` (e.g. `CoreGateTest.lean:41` `initial_driver_state 0 f CerbFS.fs_initial_state`); `CerbFunMapInstances.lean:13` mentions it in a comment only.

**The desugar state and its entry** (route A's carrier): `frontend/model/cabs_to_ail_effect.lem:224` `type state = <| aux_supply; scope_id_supply; …; fresh_sym_supply: nat (:240); …; tag_definitions; function_declarations; … |>`; `:577-578` `val initial_state: nat -> (map string Symbol.sym) * Core.fun_map unit * Core.impl -> Cn_desugaring.init_scope -> state_with_markers` / `let initial_state fresh_sym_supply_seed core_eval_stuff cn_eval_stuff = <| … fresh_sym_supply= fresh_sym_supply_seed (:582) … |>`; the getter pattern `get_fresh_sym_supply` (`cabs_to_ail.lem:1135`). The entry: `frontend/model/cabs_to_ail.lem:4973-4975` `val desugar: nat -> (map string Symbol.sym) * Core.fun_map unit * Core.impl -> Cn_desugaring.init_scope -> string -> translation_unit -> Exception.exceptM (E.fin_markers_env * ail_program unit * nat) Errors.error` / `let desugar sup core_eval_stuff cn_eval_stuff startup_str (TUnit edecls) = …`. Its callers: `backend/common/pipeline.ml:206` `Cabs_to_ail.desugar 0 (ailnames, core_stdlib_fun_map, core_impl) cn_init_scope …`; `lean_frontend/Main.lean:521` `desugar fmapEmpty supply coreEvalStuff cnInit "main" tunit` (reader-lifted; `fmapEmpty` is the reader argument).

**Precedents** (part one §1): fuel (`Main.lean:1150` `defaultFuel … -- FUEL-DEFAULT`, `:1251-1279` parsing/refusal; `scripts/check_no_fuel_numerals.sh` `ALLOW_MAIN` exact-line allowlist, `--selftest` plants); the fork-only flag `--batch-alloc-census` (`backend/driver/main.ml:521-524`, wired `:99/:219/:566`); unit-test registration; drift-manifest single-row edits with a dated header note (rows for all four `impl_mem.ml`; `driver_ocaml.ml{,i}`, `main.ml`, `memory_model.ml`, `pipeline.ml` are layer-1 pinned too — check `scripts/fork_drift_manifest.txt` for each file you touch).

**WP-O doctrine** (landed): row 10 walks `tests/minimal` and `tests/immaculate`; the register is the only list of OBSERVED fork≠pristine behaviours; a fork-only flag has no pristine counterpart and is named as such (part one's register rationales are the model).

## 2. Deliverables — three commits, in this order

**C0 — The witnesses' gating pin and the gcc ledger (instrument only; no semantics).** (a) Add the two witness programs to `tests/immaculate/nolibc/` (copies with the same content; header comments cite tray 44, part one's record and this charter; names in the directory's style) and re-record the immaculate baseline with the lane's own `--record-baseline`, then confirm by `diff` that the ONLY change is two new rows of class `MATCH | L=ERR:{msg: "MerrOther "Concrete.allocator: failed (out of memory)""}` (or the lane's exact token — quote it) — NO existing row moves, or STOP (S1). Row 10 then walks the two new files too: add their two `shared-model-fix` register rows (same citation/rationale as the `tests/minimal` twins; fresh signatures). (b) `scripts/gcc_oracle_baseline.txt`: hand-insert rows for `minimal/112-…` and `minimal/113-…` at their observed statuses (run `test_gcc_oracle.sh` on them; the ledger's sorted-by-key convention and header-note style of Z1, lines 6-7), so `--check-baseline` no longer reports them as new; a dated header note. Gate: `test_immaculate.sh` (rc 0 at the new baseline), row 10 + `--plant` (expect `reviewed_difference` 7), `test_gcc_oracle.sh --check-baseline` (rc 0, no "new file" lines), `test_unit.sh`, Tier A rows 2–4b unmoved.

**C2 — The bound as an explicit entry parameter, route A.** Design decision [AGENT], agreed [USER 2026-09-17]: the bound enters the semantics at its TWO entry points — the execution driver and the desugarer (for the const-expr mini-run) — from ONE command-line default, both `∀ top`-quantifiable; never a reader constant, never a literal in a definition.
(a) Lem, signatures only (the rationale is the ruling): `mem.lem:41` `val initial_mem_state: integer -> mem_state`; `driver.lem` `initial_driver_state_with address_space_top run_st file fs_state` (`:1526` `layout_state= Mem.initial_mem_state address_space_top`), `initial_driver_state address_space_top file fs_state`, `initial_driver_state_given address_space_top sup file fs_state`; `cabs_to_ail_effect.lem` `state` gains `address_space_top: integer` (beside `fresh_sym_supply`), `initial_state` takes and seeds it (`val initial_state: nat -> integer -> …`), a getter `get_address_space_top` beside `get_fresh_sym_supply`; `cabs_to_ail.lem:4973-4975` `desugar sup address_space_top core_eval_stuff …` seeding `E.initial_state`; `cabs_to_ail.lem:1137` reads the getter and passes it to `Mini_pipeline.evalIntegerConstantExpression` (which gains the argument, `mini_pipeline.lem:201-206`) → `:163` `initial_driver_state_given address_space_top sup_after_elab …`. NOTHING else in the model changes; `drive` untouched.
(b) OCaml: `memory_model.ml:39` `val initial_mem_state: Z.t -> mem_state`; all four `memory/*/impl_mem.ml` take the argument — concrete and vip set `last_address` from it (the literal LEAVES `:508`/`:170`); symbolic and cheri-coq accept and ignore it with a one-line comment; `driver_conf` gains `address_space_top: Z.t` (`.ml`/`.mli`), `driver_ocaml.ml:157`/`:198` pass it; `backend/driver/main.ml` fills it from ONE named default `address_space_top_default = Z.of_int 0xFFFFFFFFFFFF` (the one OCaml numeral, commented as such, citing the removed `impl_mem.ml:508`) and passes it to `Cabs_to_ail.desugar` at `pipeline.ml:206` (the pipeline's desugar entry gains the parameter; the driver passes the same value to both entries). The two NOT-ladder-built callers (`web/instance.ml:635`, `rt_ocaml.ml:355`) get the mechanical edit (pass the named default) with a comment that they are unbuilt — [AGENT] decision: keep them compiling in principle rather than knowingly broken; record it.
(c) Lean: `MemState.lastAddress : Address` (NO default); `CerbMem.initialMemState (addressSpaceTop : Int) : MemState := { lastAddress := addressSpaceTop }`; `Main.lean`: `def defaultAddressSpaceTop : Int := 0xFFFFFFFFFFFF  -- ADDRESS-SPACE-DEFAULT (the one allowed address-space numeral)`, `--address-space-top N` parsed exactly like `--fuel` (`:1251-1279` shape; absent → default; non-numeral or `≤ 0` → the same refusal shape naming the default), passed to BOTH `desugar` (`:521`) and `initial_driver_state` (`:1019`); `CerbCall.lean` if it builds a state (§1 found no site — confirm).
(d) The gate: `scripts/check_no_fuel_numerals.sh` scans for the bound numeral too (`0xFFFFFFFFFFFF`, `0xffffffffffff`, `281474976710655`, and a `lastAddress :=`/`last_address=` literal pattern) across the same trees, allowlisting ONLY the Main.lean lines by exact content (extend `ALLOW_MAIN`); `--selftest` gains plants (a literal in `CerbMem.lean`, in a test, in speclab; the allowlisted Main.lean line accepted).
(e) Lean callers take a test-chosen value (the ruling allows it): `FuelExemplar.lean`, `FuelFormsTool.lean`, the five speclab SLUnit tests, `MonadicFailstop.lean:24` (uses `initialMemState`); the FuelExemplar theorem's statement now quantifies the bound too — say so in its docstring.
(f) Regenerate both trees (`make prelude-src lean-prelude-src`), rebuild both engines; every Tier A/B baseline row unmoved (the parameter is identity at its default).
(g) Docs: `DESIGN.md` §4 (the bound joins fuel; the two entry points; `∀ top`); `VALIDATION.md` §0/§7 sentence where fuel's CLI default is described; `lean_frontend/CLAUDE.md` key-files rows; the CONSUMER RE-PIN NOTE in the record — old→new for `initial_driver_state`, `initial_driver_state_given`, `initialMemState`, `MemState.lastAddress`, `desugar` (Lean signature) — addressed to cerberus-sl (pin `scripts/semantics-pin.env`), nothing more (the operator: "modest blast radius"). Drift rows: single-row edits for every touched layer-1 file, one dated header note.
Gate: `test_unit.sh` (incl. the extended gate + selftest), Tier A full (`release.py --mode fast`), row 10 + `--plant` (zero new rows: the parameter is identity at its default).

**C3 — The fork-only OCaml flag and the differential witness lane.** (a) `backend/driver/main.ml`: `--address-space-top N` (`Arg.(value & opt … address_space_top_default & info ["address-space-top"] ~doc)`, the `--batch-alloc-census` shape), documented FORK-ONLY in `--help`; wired into `driver_conf.address_space_top` and the desugar call. (b) `scripts/test_address_space.sh` + `tests/address_space/` (a handful of small programs allocating a few objects): both engines at tiny bounds (`--address-space-top 64`, `32`, and one bound exhausted by the second object) in batch mode, complete observations compared through the shared codec (`scripts/observations.py`, no codec change); a committed expectations file, fail-closed both directions; `--selftest`: the lane must REJECT an expectations file edited to the PRE-FIX verdict of an exhausted case (an `active` allocation where the kill is expected; the pre-fix values are in draft 44) and a missing/truncated expectations file; no second oracle build of any kind. (c) LADDER: the lane as a Tier A row (seconds); VALIDATION §5 lane list; row 10's fork-only-flag exclusion list gains `--address-space-top` (WP-O named `--cabs-json`, `--call`, `--batch-alloc-census` — follow that list's home). (d) Drift rows for `main.ml`, `driver_ocaml.ml{,i}`, `pipeline.ml`. (e) OPTIONAL tray PROPOSAL draft 45 ("the address-space top as a driver parameter") only if free. Gate: the new lane green + `--selftest`; `release.py --mode full` once (Tier A + B); `test_upstream_oracle.py --with-lean` over the full pristine corpus (the flag is never passed there; ZERO new rows).

## 3. Fence, forbidden, stop rules

**Fence (may edit):** `frontend/model/{mem,driver,cabs_to_ail,cabs_to_ail_effect,mini_pipeline}.lem` — SIGNATURES and the named threading sites ONLY (`mem.lem:41` + target_reps; `driver.lem` the three `initial_driver_state*` defs and `:1526`; `cabs_to_ail_effect.lem` the `state` field, `initial_state`, one getter; `cabs_to_ail.lem` the `desugar` entry and `:1137`; `mini_pipeline.lem` `evalIntegerConstantExpression`'s signature and `:163`); `ocaml_frontend/memory_model.ml:39`; `memory/{concrete,vip,symbolic,cheri-coq}/impl_mem.ml` (`initial_mem_state` signature only — the allocator is part one's and is CLOSED); `backend/common/{driver_ocaml.ml,driver_ocaml.mli,pipeline.ml}`; `backend/driver/main.ml`; `backend/web/instance.ml:635` and `backend/ocaml/runtime/rt_ocaml.ml:355` (the mechanical edit only); `lean_frontend/{CerbMem,Main}.lean` (CerbMem: `MemState.lastAddress` and `initialMemState` ONLY), `test/Unit/{FuelExemplar,FuelFormsTool,MonadicFailstop}.lean`, `speclab/test/SLUnit/{TreeGateTest,CoreGateTest,ByteArrGateTest,SeedGateTest,ListGateTest}.lean`; `tests/immaculate/nolibc/` (two new files), `tests/immaculate/baseline.txt` (re-record: two new rows only), `scripts/gcc_oracle_baseline.txt` (two new rows + header note), `scripts/upstream_oracle_differences.json` (two new rows, same class/citation as the twins); `scripts/{check_no_fuel_numerals.sh,test_unit.sh,fork_drift_manifest.txt (single rows + one header note),test_address_space.sh (new),LADDER.md}`, `tests/address_space/` (new); `scripts/test_upstream_oracle.py` ONLY the fork-only-flag exclusion entry; docs: `DESIGN.md` §4, `VALIDATION.md` §0/§5/§7, `lean_frontend/CLAUDE.md` key-files, the record `lean_frontend/docs/2026-09-17_address-space-bound-part-two-record.md` (+ evidence dir), optional draft 45 + INDEX entry.

**Forbidden:** any other `.lem` body change (`drive` untouched; no allocator edits; nothing in the desugarer beyond the field/seed/getter/threading); `CerberusImpl.lean` and the enum registry (a separate slice); any existing baseline row moving anywhere (`exec_*`, immaculate's existing rows, libxml2, csmith, ci, gcc's existing rows), `fuel_*`, `failure_reach_register.txt`; `check_fork_drift.sh --refresh`; moving `CERBERUS_REV`/`LEM_REV`; lem-lean changes; WP-O's files beyond the one exclusion entry; a default literal anywhere but the allowlisted Main.lean lines and the ONE named OCaml default; `2>/dev/null` on build/install steps; opam/switch changes; anything machine-global; `git push`; commits on any mainline; uncapped Lean; more than one heavy job.

**Stop rules (STOP = commit what is green, write the interim state into the record, end the turn with a report):** **S1** any existing baseline row moves (incl. the immaculate re-record touching anything but the two new rows); **S2** the desugar-state threading ripples beyond the sites named in §1/§2 (a caller or a `.lem` site not listed) — list it and stop; **S3** an `impl_mem.ml` implementation or an unbuilt backend cannot take the new signature mechanically; **S4** any Lean≠fork difference at ANY bound in the new lane or the three-engine report; **S5** a step past ~45 minutes; **S6** any rule conflict.

## 4. Gates and commit discipline

Worktree `worktrees/cerberus-lean-arc/address-space-bound-part-two` (primed; standing pristine build present — `scripts/ensure_independent_oracle.py` validates it). `scripts/ce <cmd>` for the OCaml environment; both engines rebuilt via the `scripts/common.sh` recipes (never a stale `generated/` copy — the sync gate refuses). Per commit (FAST-GATE): `./scripts/test_unit.sh`, `./scripts/test_exec.sh --check-baseline` and the coverage/debug/float rows, `./scripts/test_immaculate.sh` (from C0), row 10 + `--plant`, and from C3 the new lane. Once, before the record (FULL): `python3 scripts/release.py --mode full` (Tier A + B) and the three-engine report; verbatim per-lane tails in the record; zero movement anywhere except the rows C0 names. One commit per deliverable (C0, C2, C3), each message stating what ran with its verbatim verdict lines. You never merge, rebase or push.

## 5. The record (ends the slice)

`lean_frontend/docs/2026-09-17_address-space-bound-part-two-record.md`: §0 rulings by pointer; C0 — the two immaculate rows verbatim, the gcc-ledger rows, the register rows, the row-10 line; C2 — the design (route A; the two entry points; the DESIGN.md cites), the signature list old→new (the CONSUMER RE-PIN NOTE for cerberus-sl, and nothing more), the gate extension and its selftest verdicts, the zero-movement evidence, the unbuilt-backend note; C3 — the flag, the lane's expectations file and selftest verdicts, the LADDER row; the FULL gate tails verbatim; measurements; errata to §1; open items (draft 45 if not drafted; anything the enum-registry slice should know). Commit it; then STOP.
