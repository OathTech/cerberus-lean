# Arc-12 S2+S3: reconciliation, the D2 grandfather register, upstream filing drafts

Date: 2026-08-21. Worker: arc-12 (same worker S0→S3). Follows D2
(decision log) and batch 4 (`ad36aa4e5`, the verified-sound exemption).

## 1. D2 execution summary

- **B-soundness (the ruling's condition): VERIFIED, B implemented.**
  Code: `main.ml:246` binds `(cabs_tunit, _)` — the desugar+typing
  result is discarded; `pipeline.ml:242-246` produces `cabs_tunit`
  from `parse` before `desugar`; `backend/lean_export/cabs_json.ml`
  is a pure function of the Cabs tree (identifiers are
  `(loc, string)`; no symbol numbers, no mutable state, no
  Tags/Cerb_fresh reads). Empirical: the 4 beyond-margin libc TU
  exports are BYTE-IDENTICAL between counter base 0
  (collision-possible desugar; 1 warning each) and a scratch base-2^20
  build (collisions impossible; 0 warnings) — stdio/stdlib/internal/
  vfscanf, `cmp` clean; scratch probe reverted and re-verified.
  Residual on record: a collision-corrupted const-expr can still flip
  the export's hard-ERROR verdict class — refusal-shaped, never wrong
  bytes.
- **Modes implemented** (util/cerb_fresh.ml header carries the full
  soundness note): `CERB_FRESH_FLOOR_WARNING (cabs-json export …)` —
  set only by the driver's `--cabs-json` branch; `CERB_FRESH_FLOOR_
  WARNING (grandfathered lane …)` — opt-in env
  `CERB_FRESH_FLOOR_GRANDFATHER=1`, used ONLY by the two documented
  invocations in `test_libxml2_uri.sh` (ORACLE_LIBC + OCAML_NOLIBC;
  the second preserves the pinned mirrored-failure pair). One warning
  per TU, never silent. Any other use of the env is a finding.
- **Lanes re-greened (verbatim):** test_libc_exec `SUMMARY: match=7
  diff=0` / `ALL MATCH RECORDED BASELINE` (exit 0); uri lane
  `GATE PASS: all lane expectations pinned-green + baseline unchanged
  (16/16)` with `[lean+libc] EXACT MATCH with ORACLE_LIBC (16/16 URI
  corpus)` (exit 0).
- **Full bar at batch 4 (all exit 0):** test_unit; minimal
  (`cerb_floor=0`); ci (0 regressions); corpus shard 2/12 vs the new
  baseline (0 regressions; `cerb_floor=104` as baselined); test_verify
  29/29; witness spot (csmith_6000098) still floors on the exec path.

## 2. The D2 grandfather REGISTER (Option-C mechanics)

| # | entry | exposure (measured) | validation | mover |
|---|---|---|---|---|
| G1 | **Pinned libc Core objects** (`_build` libc.co + `tests/libc/libc.core` pin) | built from beyond-margin TUs stdio/stdlib/internal/vfscanf (desugar hwm 856/673/682/521 vs margin 483); LIVE duplicate (digest,num) keys: stdio 214, stdlib 58, internal 106 (e.g. `__uflow` = `ret_526`); vfscanf supply-only | agreement: libc_exec 7/7 vs the protected Lean side; libc.co pin drift-checked every lane run | **renumbering-era re-derivation** (`2026-08-21_arc12-renumbering-case.md`); libc.co REBUILDS stay floored until then |
| G2 | **libc metadata cabs-jsons** (libc_prep --jsons, 12 TUs) | 4 TUs warn on export (same TUs as G1) | B-soundness (§1): export bytes provably pre-desugar | retire the export warnings with renumbering |
| G3 | **uri gate oracle elaboration + pinned expectations** (`tests/libxml2/uri_baseline.txt`) | uri.c desugar hwm 1798; 252 live collisions incl. `__xmlLastError`/`__xmlGenericError`/`__xmlStructuredError` | byte-agreement 16/16 vs Lean each run (the gate itself); explicit env in exactly two invocations | retire grandfather env + re-derive expectations at renumbering |
| G4 | export-mode residual (const-expr → wrong hard-error verdict class on beyond-margin TUs) | not observed; theoretical window | refusal-shaped only | closed by renumbering |

Honesty addenda appended (never rewrites): `2026-08-19_arc6-results.md`
(the uri gate's exposure asterisk), `2026-08-19_arc6-s1-libc-load.md`
(libc.co exposure + grandfather), `2026-08-19_arc5-results.md`
(libc-mode qualifier). Renumbering case recorded for the post-arc-13
agenda: `2026-08-21_arc12-renumbering-case.md`.

## 3. S2 reconciliation (chartered items vs what landed where)

- Baselines: the corpus re-baseline landed in S1 batch 3 (movement
  table in the baseline header; 51 rank-changing rows justified; shard
  checks green). ci/coverage/float/debug/minimal: zero movement,
  re-verified. uri/libc: re-greened under D2 (this record).
- `tests/csmith_findings/README.md`: family status updated — F-D
  REPAIRED — FAIL-STOP (renumbering deferred with pointer) + the
  ATTRIBUTION CORRECTION addendum (April desugar threading is the
  mechanism; arc-2 supply exonerated on all probed witnesses).
- Arc-10 records cross-referenced BY ADDENDUM: campaign record
  (§root-cause correction + repair status) and arc-10 results
  (register-row updates; the perf-gap re-measure mover now live for
  the 5 surviving TIMEOUT rows).
- Affected-lane reruns: csmith corpus FULL pass (S1); ci + coverage
  re-run (batch 4 bar); **exploration-lane spot batch** (20 fresh B1
  seeds 3030001-3030020), verbatim:
  `SUMMARY: total=20 match=2 ub_match=0 ub_diff=0 mismatch=0 fail=0
  crash=0 lean_error=0 timeout=0 cerb_skip=2 cerb_floor=16
  cerb_inconsistent=0` (exit 0) — mismatch-free; the 16 floors are the
  measured beyond-margin density of B1-config csmith (the honest
  exploration-yield cost priced in the renumbering case).

## 4. S3 — upstream filing drafts (F-A, F-B): READY

Tray: `notes/upstream/` (project-level), INDEX updated with the two
new drafts + the operator filing checklist. Both repros re-verified
UN-FORKED against `deps/cerberus-upstream` @ b9aeedcb4 TODAY, with
fresh native references (gcc -std=c11 compiles clean; returns 8/8/11):

- **08-desugar-nested-init-internal-error.md** (F-A) — TRUE BUG.
  3-D scalar-array + struct-nesting braced initializers → AilEinvalid
  → uncaught `Failure("internal error: Translation called on Ail
  program with an invalid node")`, exit 125 with OCaml backtrace
  (generated/translation.ml:3257); 2-D control fine (`Specified(4)`);
  ~52% csmith-default kill rate; two-level remedy (diagnostic
  instead of failwith; then desugar support). Includes the
  upstream-shared UB081 false-positive neighborhood note (the F-E(a)
  channel split itself is fork-side and NOT filed).
- **09-address-constant-member-rejected.md** (F-B) — TRUE BUG.
  `static int *g_q = &g_arr[1][0].f3;` rejected ("initializer element
  is not a compile-time constant", §6.7.9#4 cited, exit 1) while the
  member-free `&g_arr[1][0]` on the previous line is ACCEPTED — the
  sharp array-subscript-vs-member boundary isolated; C11 §6.6p9
  classification argued; remedy proposed.

FILING ITSELF IS OPERATOR-GATED (network + judgment) — the checklist
is in the tray INDEX. F-D is NOT filed (fork-side, repaired); the
upstream-facing symbol-identity design question remains draft 07
(updated with an arc-12 pointer).

## 5. Open items handed to S4 / close-out

1. Operator veto window on D2's charter-bar amendment (flagged in the
   decision log) — if vetoed, revert batch 4's lane re-greens to
   Option A (the floor itself is unaffected).
2. The 2-agent adversarial audit (charter S4) — suggested scopes
   unchanged plus: the D2 exemption soundness argument, the
   grandfather-env containment (grep: exactly two uses), fold-v2
   completeness (plant a leaf-type symbol and check the fold sees it).
3. Merge checklist prep (serialize with arc 11).
