# Arc-11 / S1 — BUILD SLICE record: the engine (trace/replay + context queries)

Date: 2026-08-21. Provenance: [AGENT:arc11-S1] throughout. Work order:
the S0 design addendum (`docs/2026-08-20_arc9-s1-design.md` §12,
execution plan §12.6) under the D1 ratifications (decision log).
Standing regression bar per batch: full capped `lake build` (in-build
audits + 54 kit pins), `test_unit.sh`, `test_verify.sh`, exec-minimal
zero-movement, T1 calibration, and the S0 probe baselines (entry
21/21 mechanical + iter0 44/79 — untracked scratch, reproducible).
All commands capped `CERB_MEM_MAX=40G`.

## Batch 1 — lane consolidation + A-F4/A-F6 cleanup + engineRev + structured events + sealing-default (F12-4)

### What landed

* **New module `Tactics/WalkTrace.lean`** (trace IR types, §12.1):
  `Residual`/`CandFate`/`CandTrace`/`NormLane`/`SealKind`/`SealEv`/
  `HypEv`/`Ledger`/`FiredTrace`/`RoundTrace`/`Outcome`/`WalkTrace` +
  the `TraceSt` builder and its emission API (`trHyp`/`trFate`/
  `trResetHyps`/`trCloseRound`/`trOutcome`), the low-level engine
  event buffer (`pushEngineEv`/`drainEngineEv`, enabled only during a
  traced walk), `engineRev := 1`, and the SEALED-AUX REGISTRY
  (A-F6): `registerSealedAux`/`isSealedAuxName`. Scope notes recorded
  in-file: residual-classification slots are `Option Residual`
  (batch 4 populates them); `Fingerprint`/`GoalKey` land in batch 3;
  the nested hyp-trace of the design grammar is flattened to
  depth-tagged `HypEv` rows (serialization detail); a `CandFate.
  aborted` constructor was ADDED vs the §12.1 grammar (an attempt-
  window abort is a real fate the design missed — every candidate
  gets a fate, per the survey rank-3 requirement).
* **A-F4 cleanup executed** (`Tactics/AppWalk.lean`): the dead
  `verbose && false` raw-attempt block in `walkOnce` DELETED; the
  vestigial `if true then` in `dischargeHyp` replaced by a nested
  `do` (identical semantics, zero re-indentation — the arc-9
  indentation-incident lesson applied); `normCompute`'s unused
  `atoms`/`d` parameters removed (2 call sites updated); the
  WIP-stale docstrings on the LANDED v3 flags + `normAppState`
  rewritten to describe the D3 emitter as landed.
* **A-F6 fix**: `mkStateBridge`'s expose filter and both
  `sealCtorLeaves` already-sealed checks now consult the REGISTRY
  first (every `mkAuxRfl`/`mkAuxTheorem`/`mkAuxDefinition` creation
  site registers its constant); the legacy `_aux`/`kwSt`/`walkSt`
  suffix check remains as a documented fallback for imported-module
  auxes only (it can only widen discovery-side unfolding — the
  kernel-checked content is unaffected).
* **Structured event emission**: `WalkCfg.traceRef : Option TraceRef`;
  `walkOnce` records every candidate's fate (lhsMismatch /
  hypFailed i / residualMvars / aborted / fired) via a per-candidate
  fate cell; `dischargeHyp` records discharge lanes (computed with
  its normalization lane, scalarPf, assumption with the fact name,
  lawFired, rflClosed, enum verbatim-plant) with cert names;
  `kWhnfWithFacts` records decide-fact hits (fact name + polarity);
  seal sites record `SealEv`s (value/state/round/cert kinds with
  depth before/after); `walkLoopCore` closes each round with a
  `Ledger` row (ms + settled heartbeats — the F-S3-6 accounting made
  inspectable) and records the outcome (closedTerminal/closedRfl/
  stuck/budget). `walkLoop` is now a tracing-aware wrapper
  (try/finally restores the engine-event flag on every exit path).
* **Sealing-as-default (F12-4, ratified)**: `normWalkCfg` — norm +
  seals (facts/rounds/states) — is `app_walk_norm`'s configuration;
  `app_walk_norm!` is RETIRED (syntax deleted); `app_walk_norm?`
  mirrors the default config + verbose/trace and prints the
  STRUCTURED trace dump. Ban-list update in the same commit
  (`scripts/check_proof_size.sh`: `app_walk_norm!` row dropped with
  the policy note; header + tail message updated) + the
  `lean_frontend/CLAUDE.md` row.

### Regression bar (verbatim tails)

* capped full `lake build`: `Build completed successfully (610 jobs).`
  with `RelSem statement gate: 16 slate statements fuel-opsem-clean
  (negative tests: t1_wp and the wrapper-hole probe correctly
  rejected)` and `RelSem audit sweep: 2869 declarations across
  RelSem.* modules, all within the declared axiom boundary (0
  recorded sorryAx exceptions)` (T1AppEq — the calibration — and all
  54 Kit/Audit pins re-elaborated in-build).
* `test_unit.sh` exit 0 (fail-closed; incl. `app-walk-test` — E7/E8
  now exercise the SEALED default); gate tails:
  `check_proof_size: debug-surface ban OK
  (app_walk?/app_walk_norm?/dnms_kwalk/app_defeq_diag)` /
  `check_proof_size: OK`.
* `test_verify.sh`: `test_verify: 29 passed, 0 failed (5 fixtures,
  18 harness points)`.
* exec-minimal: `SUMMARY: total=106 match=85 ub_match=18 ub_diff=0
  mismatch=0 fail=0 crash=0 lean_error=0 timeout=0 cerb_skip=3
  cerb_inconsistent=0` (zero movement).
* S0 probe baselines at parity under the new engine (scratch
  `probet5s3-b1.out`): iter0 44 `dnms_round` firings by BOTH counts
  — the legacy dbg_trace count AND the new structured trace
  (`fired=RelSem.Kit.dnms_round` grep: 44) — stuck goal at
  `fuel + 35` (identical position); the structured dump reads
  `walk trace: 45 round(s), outcome
  RelSem.Tactics.Outcome.stuck none` (44 fired + the stuck round
  with per-candidate fates). Entry: 21/21 rounds mechanical; the
  boundary-closure gap (§12.0 item 5) UNCHANGED — deep recursion at
  field 16 of the sealed boundary, batch-3's archaeology target, not
  a regression.

### Design deviations (recorded)

1. `CandFate.aborted` added (see above) — grammar §12.1 extension,
   faithful to "every candidate gets a fate".
2. The §12.2 preview wording "no declaration is ever constructed"
   is AMENDED for batch 2 (recorded here ahead of the build):
   faithful discovery NEEDS the value/state seal definitions and
   fact certificates (they are what makes the walk feasible at
   scale — the S3 measured walls). Preview therefore: never assigns
   ANY goal metavariable, never assembles a round/goal proof
   (bridge + round seal + goal.assign skipped), always fails as a
   tactic — but fact-level aux theorems and seal definitions do
   occur. They cannot close the user's theorem (the goal mvar stays
   unassigned). The three §12.2 enforcement layers stand unchanged.

## Batch 2 — preview mode + the negative test + the gate extension

### What landed

* **`WalkCfg.preview`** (per the batch-1 amendment above): guards
  EVERY goal-assignment site — `walkOnce`'s terminal and chain
  `goal.assign`s and `walkLoopCore`'s stuck-rfl `g.assign` (round 0's
  `g` IS the user's goal) — plus the round-proof assembly (state
  bridge + round seal skipped; the continuation value `lemRhsN` is
  all progression needs). The continuation metavariable drives the
  next round's discovery but is never connected to the user's goal.
* **`app_walk_preview` / `app_walk_preview n`**: runs discovery under
  `normWalkCfg + preview + traceRef`, then ALWAYS FAILS with a
  deterministic summary (counts + outcome tag only, no timings —
  `#guard_msgs`-stable); the full ledger dump stays on the
  `app_walk_norm?`/programmatic-`traceRef` surfaces. Trust layers
  documented at the elab site: (1) structural — `goal.assign`
  unreachable under `preview := true` (S5 audit scope); (2)
  always-fails; (3) the gate ban.
* **E9, the discriminating pair** (test/Unit/AppWalkTest.lean): the
  EXACT e2 goal (closed by `app_walk` above it, kernel-checked) under
  `app_walk_preview`, `#guard_msgs`-pinned to fail with, verbatim:

  ```
  error: app_walk_preview: preview only — goal intentionally left unsolved (4 round(s) previewed, 3 fired, outcome closed-rfl)
  ```

  — the pin shows preview's discovery reached REPORTED closability
  (outcome closed-rfl) on a goal the walk closes, and still failed.
  (Labeled a TEST per doctrine; the trust layers are 1 and 3.)
* **Gate extension**: `app_walk_preview` added to
  `scripts/check_proof_size.sh`'s debug-surface ban (header policy
  note; §12.2 enforcement layer 2). PLANT-TESTED both directions,
  verbatim: tracked plant (`example : True := by app_walk_preview`
  appended to T5Prefix.lean) →
  `check_proof_size: FAIL — debug-only walker surface in committed
  files:` naming the line, rc=1; untracked plant (same line in
  ProbeT5S3.lean) → `check_proof_size: OK`, rc=0.

### Regression bar (verbatim tails)

* capped full `lake build`: `Build completed successfully (610
  jobs).`; `RelSem audit sweep: 2875 declarations across RelSem.*
  modules, all within the declared axiom boundary (0 recorded
  sorryAx exceptions)`; statement gate 16 clean.
* `test_unit.sh`: `Total: 7 passed, 0 failed` (AppWalkTest ALL
  PASSED with E9); `check_proof_size: debug-surface ban OK
  (app_walk?/app_walk_norm?/dnms_kwalk/app_defeq_diag/
  app_walk_preview)`.
* `test_verify.sh`: `test_verify: 29 passed, 0 failed (5 fixtures,
  18 harness points)`.
* exec-minimal: `SUMMARY: total=106 match=85 ub_match=18 ub_diff=0
  mismatch=0 fail=0 crash=0 lean_error=0 timeout=0 cerb_skip=3
  cerb_inconsistent=0` (zero movement). Non-preview lanes are
  behavior-neutral by construction (`preview := false` defaults);
  the S0 probe baselines were re-validated at batch 1 and the
  preview guards do not touch those paths.

## Batch 3 — checked replay + fingerprints + bench; THE ENTRY THEOREM LANDS (exit test)

### Archaeology (the D1 lead, executed first)

* Engine drift since the arc-9 D3-continuation commit (`65e162efc`,
  the "entry theorem GREEN ~5 s" state): `git log 65e162efc..HEAD --
  …AppWalk.lean` = exactly the A-F1 debug-repro fix (trace-lane
  only) + arc-11 batch 1. ZERO closure-relevant engine drift — the
  gap was the scratch RECIPE, not the engine.
* Measured re-derivation (probe `ProbeT5S3c.lean`, traced): under the
  SEALED default the entry walk stops at 16/21 (trace: R16 stuck,
  `dnms_round:hyp-4-failed` — the hadv premise — at the census
  store-i round; REGISTER ITEM for S2: the sealStates lane breaks
  one store-round class while clearing the other), and `app_defeq`'s
  whole-side KERNEL check bridges the residual 5 rounds — fragile
  (one identical-statement repetition deep-recursed under parallel
  elaboration). Under `nostates` (sealFacts+sealRounds only) the
  walk fires 21/21 and the boundary closes stably. On the ITERATION
  corpus the order flips: iter0 reaches 44/79 sealed vs 38/79
  nostates — so the SEALED default STANDS (F12-4, baseline
  preserved) and the entry uses the explicit modifier.
* Async finding (register item + recorded CONTRACT): concurrent
  proof elaboration (`Elab.async`, default on) shares the
  process-global heartbeat ledger across walks (nondeterministic
  stop points — the observed identical-statement divergence) and
  does not propagate tactic-time env-extension writes across
  declarations. Recording/replaying declarations therefore sit under
  `set_option Elab.async false` (file- or decl-scoped); the elabs
  WARN when async is on. Determinism/visibility contract only —
  never a soundness surface.

### What landed

* **`app_walk_norm … nostates`** — state-sealing-off modifier on the
  committed surface (docstring carries the measured 44-vs-38 /
  16-vs-21 rationale).
* **Fingerprints** (`WalkTrace.Fingerprint`): kit-registry hash
  (name-sorted `@[app_eq]` registry: name, key path, prio, statement
  hash — via the new `appEqAll` flat registry), `engineRev`, and the
  goal-statement key (pretty-printed-goal hash; raw `Expr.hash` is
  fvar-id-sensitive). Recorded deviation vs §12.1: the
  `gen`/`fixture` components are DEFERRED — those surfaces are
  pinned by the standing fork-drift and emit-lean-core gates, and
  staleness surfaces as a loud replay divergence anyway.
* **Stored traces**: persistent env extension (`storedTracesExt`) +
  `app_walk_rec name (n)?` (a NORMAL sealed walk that also records,
  fingerprint-stamped) and `app_walk_replay name` (CHECKED replay:
  fingerprint refusal on engineRev/kit/goal mismatch; then exactly
  the recorded law sequence via `WalkCfg.replayOnly` — choice
  removed, checking identical; divergence = loud error naming the
  step, NO fallback to search).
* **E10** (AppWalkTest): record → replay on the e2 contract
  (kernel-checked twice) + the goal-mismatch NEGATIVE pinned
  verbatim (`app_walk_replay: trace 'e10_tr' was recorded for a
  DIFFERENT goal statement; re-record`). Probe-level (Q5,
  `ProbeT5S3c`): the same refusal on the entry trace against a
  fuel+20 variant statement.
* **`bench/WalkBench.lean`** — the metrics instrument (Tier C, run
  on demand, exit-1-by-design: the preview lane fails by
  construction). Columns, verbatim:

  ```
  BENCH B1 T1-dnms discovery (preview to semantic stop): 2350ms
  BENCH B2 entry full walk + closure (recorded): 39985ms
  BENCH B3 entry checked replay + closure: 2593ms
  ```

  (B2 is cold-cache discovery + certification; B3 is warm replay —
  the honest reading is "replay removes search and re-uses nothing
  else"; the kernel FINAL-check share is not tactic-visible and is
  reported DERIVED: whole-file wall 46.6 s minus tactic walls ≈
  ~2 s. Recorded deviation vs the §12.2 metric table: the
  per-`addDecl` kernel split is deferred to S5 refinement.)
* **THE EXIT TEST — `entry5_walk` COMMITTED** (T5Prefix.lean): the
  21-round entry theorem (post-prefix state → `StT5 n 0`, symbolic
  n and fuel) as an ordinary kernel-accepted in-build theorem —
  `app_walk_norm 25 nostates; app_defeq` under the async-off pin;
  builds in ~8 s inside the module. The §12.0-item-5 reproduction
  gap is CLOSED: the closing configuration is re-derived, recorded
  (probe Q3/Q4: record → replay both kernel-accepted), and BANKED
  as committed tree content — the arc-9 evidence-grade park is now
  a theorem.

### Regression bar (verbatim tails)

* capped full `lake build` (now INCLUDING `entry5_walk` in-build):
  `Build completed successfully (610 jobs).`; `RelSem audit sweep:
  2940 declarations across RelSem.* modules, all within the declared
  axiom boundary (0 recorded sorryAx exceptions)`; statement gate 16
  clean.
* `test_unit.sh`: `Total: 7 passed, 0 failed` (AppWalkTest ALL
  PASSED incl. E9 + E10); all check_* gates OK.
* `test_verify.sh`: `test_verify: 29 passed, 0 failed (5 fixtures,
  18 harness points)`.
* exec-minimal: `SUMMARY: total=106 match=85 ub_match=18 ub_diff=0
  mismatch=0 fail=0 crash=0 lean_error=0 timeout=0 cerb_skip=3
  cerb_inconsistent=0` (zero movement).
* S0 probe baselines: the default-lane behavior is unchanged
  (batch-3 deltas are the inert-when-unset `replayOnly` filter and
  the additive `nostates` syntax); the sealed-default iter0 44/79
  baseline stands from the batch-1 parity run (`probet5s3-b1.out`;
  not re-run this batch — the sealed lane is untouched), and the
  batch-3 probes bank entry 21/21 under `nostates` + iter0 38/79
  under `nostates` (the comparison datum, `probet5s3c3.out`).

## Batch 4 — context queries + typed residuals + linter + ambiguity checks

### What landed

* **`@[app_eq]` required-fact metadata** (§12.3): `(fact := binder
  keyPos)` (SOFT: query-first discharge, mechanical-lane fallback on
  a miss) and `(fact! := binder keyPos)` (GATE: applicability — no
  key-match or a failed commit makes the law inapplicable, residual
  `missingFact`/`defeqBridge`). Registration resolves the binder
  against the lemma telescope (index + LHS head + key-position range
  checked, LOUD failures — the Lithium `[instance]`-generator
  discipline); `RequiredFact` rides in the law registry.
* **The query engine** (`queryRequiredFact`): DETERMINISTIC —
  hypotheses newest-first; key argument compared syntactically then
  under a small capped defeq; the FIRST key-match COMMITS to one
  capped full-type defeq (assigning the premise — the Islaris
  `findM(a)` move: the fact determines the law's undetermined
  arguments); no further scanning, no backtracking. Ungrounded keys
  (mvar-carrying after LHS unification) never query.
* **Ambiguity-is-error** (survey rank-3 acceptance gate): STATIC —
  registering a law with an identical DiscrTree key path AND equal
  priority to an existing law is an attribute ERROR naming both
  (verified non-tripping over the whole existing kit registry);
  DYNAMIC — when a candidate fires and a SAME-priority sibling also
  passes the applicability gate (LHS unification + gating facts),
  the walk ERRORS naming both laws (never silent first-wins).
* **Typed residuals populated**: candidate `hypFailed` fates now
  carry a classification (`semantic` with the computation head /
  `arithmetic` / `defeqBridge` / `missingLaw` / `missingFact`), and
  the stuck outcome classifies the goal (missing law vs semantic) —
  the S3 stop-extract-redo loop's work-order format, structured.
* **`#app_eq_lint`** (on-demand registry linter): identical-key-path
  pairs (would-be ambiguities) + literal-pinned VALUE argument
  positions in conclusion LHS keys (the `addrOpt` bug class;
  Sort-typed arguments skipped — type instantiations are benign).
  Run over the full kit registry, verbatim:

  ```
  app_eq_lint: 17 laws, 0 identical-key pair(s), 0 literal-pinned LHS position(s)
  ```

  (a clean registry — the arc-9 `addrOpt` generalization already
  removed the one known instance of the class).
* **The exemplar**: `mem_load_block` registered with
  `(fact := hget 3)` — the allocation-lookup premise keyed on the
  allocations MAP argument, SOFT mode (behavior-compatible: every
  existing walk discharges hget through the normal lanes on a
  query miss; a context fact about the same map now commits first
  and determines allocId/alloc).
* `engineRev` bumped to 2 (batches 3-4 are behavioral).

### Regression bar (verbatim tails; run at the final batch-4 tree)

* capped full `lake build`: `Build completed successfully (610
  jobs).`; `RelSem audit sweep: 3016 declarations across RelSem.*
  modules, all within the declared axiom boundary (0 recorded
  sorryAx exceptions)` — T1 calibration, `entry5_walk`, and all 54
  kit pins re-elaborated under the batch-4 walker (dynamic ambiguity
  check + the mem_load_block soft fact live on every fired round:
  zero trips).
* `test_unit.sh`: `Total: 7 passed, 0 failed`; `test_verify.sh`:
  `test_verify: 29 passed, 0 failed (5 fixtures, 18 harness
  points)`.
* exec-minimal: `SUMMARY: total=106 match=85 ub_match=18 ub_diff=0
  mismatch=0 fail=0 crash=0 lean_error=0 timeout=0 cerb_skip=3
  cerb_inconsistent=0` (zero movement).
* S0 probe baseline under the batch-4 engine (`probet5s3-b4.out`):
  iter0 44 `dnms_round` firings, stuck at `fuel + 35` — EXACT
  parity — and the stuck outcome now classified, verbatim:

  ```
  walk trace: 45 round(s), outcome RelSem.Tactics.Outcome.stuck (some (RelSem.Tactics.Residual.semantic `drive_nonmemory_steps_aux2_lemFuel))
  ```

  (the typed residual protocol reporting on the real corpus: the
  stuck crossing is SEMANTIC — the dnms advance premise — which is
  precisely the S2 resumption's named territory).

## S1 close state

All four batches landed and gate-green; S2 (the T5 climb) is next
per the coordinator's sequencing, reported at this boundary. The S2
work order inherits: the trace instrument on the stuck round
(engine prong §12.5-1 first), the continuation-lambda advance law +
`round_run_jump` (both now context-query-capable — the gating mode
is built), `entry5_walk` as the proof's first banked segment, and
the register items recorded above (sealStates entry-store-round
early stop; the async ledger contract).
