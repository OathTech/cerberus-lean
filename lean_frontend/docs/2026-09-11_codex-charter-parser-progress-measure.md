# Codex charter — `many`/`many1` as input-indexed recursion: the shared-body restatement, the progress hypothesis, and the last two rows of the fuel-pending register (2026-09-11)

**Branch:** `arc/parser-progress-measure` · **worktree:** `worktrees/cerberus-lean-arc/parser-progress-measure` (primed from the mainline `54f007187` = `e30810be7` + the audit's docs-only commit; both driver stamps OK on 2026-09-11; LIBRARY target included in the prime) · **author of this charter:** the orchestrator [AGENT], under the rulings in §0 · **lem-lean pin:** `f6542f8` (unchanged; the lem tool is NOT edited — `fuel_measure … assuming` and FM-mutual are already supported) · **worker:** Codex, launched by the operator in nono · **sequencing:** intended to run AFTER `arc/semantics-audit-repairs` lands (shared registers: `scripts/fork_drift_manifest.txt`, `lean_frontend/TODO.md`, `lean_frontend/VALIDATION.md` §7, `test/Unit/TotalityProofTest.lean`, the tray `INDEX.md`); D0 says what to do if the mainline moved.

## 0. The property, in one sentence, and the rulings behind it

> **`Monadic_parsing.many` and `many1` compute exactly what they compute today on every input, in both targets, but their recursion descends an explicit input parameter, so the two fuel'd workers ship MEASURED under a progress hypothesis on the inner parser (the C4 form), the fuel-pending register becomes EMPTY, the printf parser family loses its ambient `[LemFuel]`, and the fork's restatement is manifested and tray-filed as a proposal.**

Rulings this rests on, verbatim:

- [USER 2026-09-11]: "D3 agree. Go ahead" — on the orchestrator's D3 explanation: three routes for the last two pending workers (change the shared Lem body so the input becomes an explicit parameter, measured by input length under a progress hypothesis on the inner parser; a lem-lean backend hoist through the parser wrapper; or leave the two rows permanently), with the recommendation "the first … the same argument you accepted for the compatibility accumulator: the more regular design, expressed in the shared source, with the smallest trust surface." The operator agreed with the first route.
- [USER 2026-09-10], the precedent for a shared lem body change with no behavioural difference on any previously-terminating input: "Agreed with all of this. It sounds like this is actually a more regular design than the current lem? … go ahead with the charter" (`docs/2026-09-10_codex-charter-are-compatible-assumed-set.md` §0).
- [USER 2026-09-03], zero Lean-vs-oracle execution discrepancies; the shared `.lem` changes so the fork's OCaml oracle changes IDENTICALLY with Lean; here the OCaml change is a restatement with the SAME results on every input (extensional equality of the parsers — D1's argument), so no lane may move at all. No ISO-fix register row (not an ISO matter). [USER 2026-09-03], no magic values: the measure is a function of the input, never a numeral budget.
- [USER 2026-09-05] (master plan §0): faithfulness = the authors' intent — the restatement changes the recursion's PARAMETERISATION, not its meaning; the tray draft is a PROPOSAL to upstream (termination-checkable formulation), not a bug report.
- The register's own rule (`scripts/fuel_forms_pending.txt` header): "the register can only shrink by proving/absorbing, and grow by an explicit, reviewed line". The 2026-09-08 close-out's D2 text in that register — "an eta-expansion through `ParserM` in the lem text would be a body change (forbidden)" — was a CHARTER-scoped restriction of that slice; the operator has now ruled for the body change.
- [USER 2026-09-08], carried verbatim into every charter: "we should *NOT* be building anything new out-of-policy" — program proofs are derivations through the rules; no `rcases … <;> rfl` over literal ranges, no `rfl`/`decide` at a program literal as a load-bearing step; no new enumeration/literal/artefact surface. (This slice's proofs are about the semantics' own workers and combinators, not about programs.)
- Standing: fail-closed, fail-noisy; never run a Lean build uncapped; the grind tripwire; "Profile before optimizing" and the eager-measure lesson (`docs/2026-09-07_fuel-measure-cost-record.md`); record integrity (verbatim quotes, [AGENT]/[USER] provenance).

## 1. What is known (facts you may rely on; verify the ones you touch)

| Fact | Where |
|---|---|
| **The module.** `frontend/model/monadic_parsing.lem` (115 lines): `type parserM 'a = ParserM of (list char -> list ('a * list char))` (`:4-5`) — a LIST-of-results parser; `parse (ParserM p) = p` (`:8`); `parse_return a = ParserM (fun cs -> [(a, cs)])` (`:12`); `parse_bind p f = ParserM (fun cs -> List.concatMap (fun (a, cs') -> parse (f a) cs') (parse p cs))` (`:16-18`); `mzero` (`:22`); `parse_mplus p1 p2 = ParserM (fun cs -> parse p1 cs ++ parse p2 cs)` (`:25`); `item` (`:39-43`); **`(<|>) p1 p2 = ParserM (fun cs -> match parse (mplus p1 p2) cs with [] -> [] \| (x::_) -> [x] end)`** (`:48-52`) — deterministic choice = the HEAD of the concatenation; `sat`, `char`, `anyChar`, `string` (`:65-91`; `string` is list-structural, `termination_argument automatic` `:98`). **The block:** `let rec many p = many1 p <|> return [] and many1 p = p >>= fun a -> many p >>= fun _as -> return (a::_as)` (`:100-106`); the fuel declares `declare {lean} fuel val many = \`fuelExhausted (ParserM (fun _ => []))\`` and the same for `many1` (`:114-115`); the comment `:108-113` still says "lemDefaultFuel dominates any real parse depth" — STALE since the fuel-parameter arc (no default constant exists). `return`, `bind`, `mplus`, `>>=`, `>>`, `<|>`, `option`, `char` are `let inline`. | source, read 2026-09-11 |
| **Generated Lean today** (`lean_frontend/generated/Monadic_parsing.lean:126-145`): `many_lemFuel {a} (lemFuel : Nat) (p : parserM a) : parserM (List a)` with the zero case `fuelExhausted (ParserM (fun _ => []))` and the succ case the inlined `<|>` body over `many1_lemFuel lemFuel p`; `many1_lemFuel` likewise via `parse_bind`; wrappers `many {a} [LemFuel] := many_lemFuel LemFuel.fuel`, `many1` likewise; `_zero` lemmas by `rfl`. `generated/Monadic_parsing_auxiliary.lean` exists (the obligation shell; empty of measured obligations today). | source |
| **Why no measure exists today** (`scripts/fuel_forms_pending.txt`, the parser block; `docs/2026-09-08_fuel-pending-closeout-record.md` §2 rows `many`/`many1`, §7): the recursion descends the INPUT `cs`, which is bound by the lambda INSIDE the `ParserM` constructor in the bodies of `<|>` and `>>=`, never by the head; the only parameter of `many`/`many1` is the parser `p`, a function; a measured wrapper fixes its counter before the input arrives. The depth on an input is the number of characters `p` consumes on that input. | register; record |
| **The restatement that makes the input a parameter, and why it is the SAME parser** (D1's content; the worker re-derives and writes it): unfolding the `inline`s, `parse (many1 p) cs = List.concatMap (fun (a, cs') -> List.map (fun (_as, cs'') -> (a::_as, cs'')) (parse (many p) cs')) (parse p cs)` (bind's `concatMap` over `p`'s results; the inner `>>= return` is a `map`), and `parse (many p) cs = match parse (many1 p) cs ++ [([], cs)] with [] -> [] \| (x::_) -> [x] end` (mplus then head). So two mutually recursive functions ON THE INPUT, `many_run p cs` and `many1_run p cs`, with those two bodies, and `many p = ParserM (many_run p)`, `many1 p = ParserM (many1_run p)`, denote the same parsers — for EVERY `p`, including one that returns several results or none, and including a non-consuming `p` (both formulations then recurse forever in OCaml). Every hop `many_run cs → many1_run cs → many_run cs'` passes through one `p`-result `(a, cs')`. | derivation [AGENT], 2026-09-11 |
| **The progress hypothesis and the measure.** If `p` CONSUMES — every result `(a, cs')` of `parse p cs` has `List.length cs' < List.length cs` — then from `many_run p cs` the block's depth is at most `2 · List.length cs + 2` (two hops per consumed character, plus the final `many_run` on the exhausted input and its `many1_run`, which returns `[]` when `p` fails); from `many1_run p cs` at most `2 · List.length cs + 1`. Hypothesis form: a Prop over the worker's binder `p`, e.g. `CerbParserProgress.Consumes p` with `def Consumes {a} (p : parserM a) : Prop := ∀ cs r, r ∈ parse p cs → r.2.length < cs.length` in a Props-only seam module (the `CerbCoreShape` pattern: `lean_frontend/CerbCoreShape.lean`, listed in `handwritten_copy.manifest:45` and `lakefile.toml:152`). The measure mentions the binder `cs` and the qualified global `List.length` — exactly the renderer's vocabulary (`to_pures`'s `List.length l + 1`, `core_aux.lem:2590`). | analysis [AGENT]; the C4/close-out precedents |
| **The declare syntax** (lem-lean `doc/lean-backend/2026-09-05_measure-hypothesis-record.md` §2.1-2.2): `declare {lean} fuel_measure val f = \`<measure>\` assuming \`<H>\`` — the WRAPPER is fuel-free and hypothesis-free (`def f xs := f_lemFuel (μ) xs`); the OBLIGATION gains the binder NAMED `lemHyp` immediately before `lemFuel`: `theorem f_measure_sufficient (xs…) (lemHyp : H) (lemFuel : Nat) (lemMeasureLe : μ ≤ lemFuel) : f_lemFuel lemFuel xs… = f xs… := <Module>_lemMeasureProofs.f_measure_sufficient xs… lemHyp lemFuel lemMeasureLe`. The `fuel` sentinel declares STAY (they supply the zero case; `assuming` on them is a parse error). FM-mutual: one shared counter, all-or-none — both members get a `fuel_measure` declare. Precedents: `core_aux.lem:2589-2590` (`to_pure`/`to_pures`, mutual, both under hypotheses; proofs `lean_frontend/Core_aux_lemMeasureProofs.lean`, template header `:20`), `formatted.lem:338` (`showNonNegativeWithBasis_aux`, `assuming \`2 ≤ b\``; proof `Formatted_lemMeasureProofs.lean:37-80`, the `_stable_aux` + `_measure_sufficient` shape). | lem-lean record; source |
| **The hypothesis register** (`scripts/fuel_hypotheses.txt`): TAB-separated `<worker> <exact hyp text> <invariant with a .lem:<line> cite> <reviewer>`; the gate (`scripts/check_fuel_forms.sh:147-167`) fails BOTH ways (an unregistered hypothesis; a stale row) and requires the `.lem:<line>` cite and a reviewer; rows carry "[USER] sign-off at merge". The `showNonNegativeWithBasis_aux` row is the shape for a "every caller passes …" invariant. | source |
| **Where `many`/`many1` are used — the parsers passed, all consuming:** `formatted.lem:90` and `:97` `many digit` (`digit = char #'0' <|> nonzero`, `:83-85`; `nonzero` a `sat`); `:103` `many (char #'-' <|> char #'+' <|> char #' ' <|> char #'#' <|> char #'0')`; `:170-171` `many ((many1 (sat (fun z -> z <> #'%')) >>= fun str -> return (F_text str)) <|> (conversionSpecification >>= fun cs -> return (F_conv cs)))` — the left alternative consumes because `many1` of a consuming `sat` consumes; the right alternative begins with `char #'%'` (`conversionSpecification`, `:~150`) followed by parsers that never lengthen the input. `sat`/`char`/`item` consume exactly one character on success (`:39-43`, `:65-68`). No other module uses `many`/`many1` (`grep -rn "many" frontend/model/*.lem`). | source; re-grep |
| **What loses its ambient fuel:** the generated printf parser family carries `[LemFuel]` ONLY because it calls `many`/`many1`: `Formatted.nonnegativeDecimalInteger`, `decimalInteger`, `flags0`, `fieldWidth`, `output_precision`, `conversionSpecification`, `format0` (`generated/Formatted.lean:310-391`). After this slice their heads lose `[LemFuel]` — a generated SIGNATURE change. `printf`/`vprintf`/`vsnprintf`/`convert`/`load_character_array` KEEP theirs (`load_character_array_aux` is an ABSORBING fuel'd worker). `Formatted_lemMeasureProofs.lean` mentions none of the parser family (its theorems are `lemNatDiv_of_pos`, `showNonNegativeWithBasis_aux_*`). | grep 2026-09-11 |
| **The pins that move:** `lean_frontend/test/Unit/TotalityProofTest.lean:55-71` — "16 ambient wrappers", generated by `scripts/gen_fuel_parametricity.py --emit`, pinned as a SET both ways by `--check` in `test_unit.sh`; the `many`/`many1` lines (`:66-67`) go, the count becomes 14; the header comment `:21` carries the historical counts ("64 at C1; 22 at C4; 19 at the 2026-09-08 …") and the risk-map follow-up (`TODO.md` "Stale counts") asks that the CURRENT count be stated. `check_fuel_forms.sh` partition today: `60 MEASURED (10 under a hypothesis) + 13 ABSORBING + 2 ambient-reachable + 6 ambient-unreachable = 81`; expected after: `62 MEASURED (12 under a hypothesis) + 13 + 0 + 6 = 81` — the WORKER NAMES change (`many_lemFuel`/`many1_lemFuel` → the new recursive functions' `_lemFuel` forms) while the count is preserved; the two pending rows leave `scripts/fuel_forms_pending.txt`, which becomes header-only. Check that the gate accepts an EMPTY register (its `grep -c`/`wc -l` reads under `set -e`; a fail-closed misread of "0 rows" as an error is a gate defect — fix it, plant-tested, if so). | source; `check_fuel_forms.sh:107-200` |
| **Fork-drift** (`scripts/fork_drift_manifest.txt`): `[source-content]` row `:320` `frontend/model/monadic_parsing.lem`; `monadic_parsing.ml` is in NEITHER layer-2 list today (byte-identical to upstream's generation) — after D1 it is a NEW `[expected-semantic]` row (the restatement changes the generated OCaml's code; the note records that it is an extensionally identical restatement). Layer-2 count 22 → 23, or 23 → 24 if `arc/semantics-audit-repairs` landed first (its `core_eval.ml` row). Single-row edits plus a dated header note (never `--refresh`; 2026-09-10 erratum). | source |
| **Cost.** The measured wrapper computes `2 · List.length cs + 2` ONCE per application of `many`/`many1` to an input (the recursion runs inside the fuel'd worker at the counter); `cs` is the remaining printf format string — tens of characters. No paired CPU study is required; the printf-heavy lanes (`test_libc_exec.sh`, `test_immaculate.sh`, `test_libxml2*.sh`, the timing rows of Tier B) must show zero movement. | analysis [AGENT] |
| **The consumer** (refined-cerberus) proves over a fragment without `printf` (`KNOWN-OPEN-ITEMS.md` A1: "the other six are unreachable for the fragment (struct/union values, printf, the rewriter)"); the signature changes above do not reach it. Still: list every changed generated head in the record's consumer note (the 2026-09-08 close-out's §8 shape). | refined-cerberus `docs/KNOWN-OPEN-ITEMS.md` |
| **Build/battery mechanics:** identical to `docs/2026-09-10_codex-charter-are-compatible-assumed-set.md` §1 (last rows) and `docs/2026-09-11_codex-charter-semantics-audit-repairs.md` §1: OCaml oracle rebuilt after any `.lem` change (`build_cerberus` or the `CLAUDE.md` recipe); `make lean-prelude-src`; `scripts/capped lake build CerberusLean cerberus-lean` (LIBRARY target too); `tools/check_driver_fresh.sh --check` both OK before any lane counts; Tier A = `python3 scripts/release.py --mode fast` + `./scripts/test_unit.sh`; full = `--mode full` + the four `test_exec.sh` baseline lanes directly. The pristine oracle build (for Tier B row 10) as in those charters. | those charters |

## 2. Deliverables — ordered; one at a time; each ends with a gate and a commit

### D0 — Snapshot and base
If `git log --oneline -1 mdd/cerberus-lean` is not `54f007187`, the mainline moved (the repair slice landed): `git rebase mdd/cerberus-lean` (the branch holds only this charter — trivial), then regenerate BOTH trees and rebuild both engines before anything else (the primed build products are from `e30810be7`). Then Tier A once, green, verdict lines quoted as the zero-movement baseline. Build the pristine oracle. Record the `check_fuel_forms` partition line and the `gen_fuel_parametricity.py --check` line verbatim.

### D1 — The restatement (shared lem body) — same parsers, input-indexed recursion
**Goal (fixed):** in `frontend/model/monadic_parsing.lem`, replace the block `:100-106` by two mutually recursive functions on the input, `many_run`/`many1_run` (`val many_run: forall 'a. parserM 'a -> list char -> list ('a * list char)`, likewise `many1_run`), whose bodies are EXACTLY the unfoldings in §1 ("The restatement …") — `many_run p cs` = head-or-empty of `many1_run p cs ++ [([], cs)]`; `many1_run p cs` = `List.concatMap (fun (a, cs') -> List.map (fun (_as, cs'') -> (a::_as, cs'')) (many_run p cs')) (parse p cs)` — and the two ORIGINAL names as non-recursive definitions `let many p = ParserM (many_run p)` and `let many1 p = ParserM (many1_run p)` with their `val`s unchanged (`:100-101`). Move the two `fuel` sentinel declares to the new recursive functions with the sentinel `fuelExhausted []` (the codomain is now the result list); rewrite the comment `:108-113` (why the recursion is on the input, what bounds it, no "lemDefaultFuel"). Every other definition in the module is untouched. Use only `List.concatMap`/`List.map`/`++`/`match` forms lem already renders in this module (they are what `parse_bind`/`parse_mplus`/`<|>` render to — read `generated/Monadic_parsing.lean:66-95` first so the OCaml and Lean outputs stay in the module's existing vocabulary).

**The correctness argument (write it into the record, your own words, ≤ one page):** for every `p` and `cs`, `parse (many p) cs` and `parse (many1 p) cs` are the same lists before and after — by unfolding the `inline`s (`<|>`, `mplus`, `>>=`, `return`) exactly once each; the two formulations have the same hop structure, so they terminate on the same inputs in OCaml and, at equal fuel, in Lean.

**Steps:** edit; `make prelude-src`; rebuild the OCaml oracle; `make lean-prelude-src`; `scripts/capped lake build CerberusLean cerberus-lean` (the new workers are still `fuel`-declared, ambient — the build must be green; `check_fuel_forms` will now report the two NEW ambient names unregistered — that single expected red is allowed in D1 only, named in the commit message, cleared in D2; `gen_fuel_parametricity.py --check` will likewise be red on the wrapper-set drift until D3); `tools/check_driver_fresh.sh --check`.

**Acceptance:** (a) the unified diff of `ocaml_frontend/generated/monadic_parsing.ml` and of `lean_frontend/generated/Monadic_parsing.lean` before/after, in the record, with sha256 after (both trees are untracked build outputs); (b) Tier A rows 2–11 green with ZERO movement (the printf-heavy lanes 5, 10 and the immaculate rows especially); the only reds are the two named gate reds above; (c) `git diff --stat` shows exactly `frontend/model/monadic_parsing.lem`. Commit.

### D2 — The measure under the progress hypothesis
**Goal (fixed):** a new Props-only seam `lean_frontend/CerbParserProgress.lean` (`Consumes` as in §1, plus the leaf lemmas of D4 if you fold them in — no `partial`, no `opaque`, no `implemented_by`), named by `declare {lean} extra_import \`CerbParserProgress\`` in `monadic_parsing.lem`; two `declare {lean} fuel_measure val … = \`2 * List.length cs + 2\` assuming \`CerbParserProgress.Consumes p\`` lines (`+ 1` for `many1_run`; spell the measures over the GENERATED binders — re-read them after regeneration); proofs in `lean_frontend/Monadic_parsing_lemMeasureProofs.lean` (new) in the `_stable_aux` + `_measure_sufficient` shape of `Formatted_lemMeasureProofs.lean:37-80` / the mutual template of `Core_aux_lemMeasureProofs.lean` (`to_pure`/`to_pures`); both new modules in `handwritten_copy.manifest` and as `lakefile.toml` roots (`check_lakefile_roots.sh`). The `fuel` sentinel declares stay.

**Register:** two rows in `scripts/fuel_hypotheses.txt` — worker, the exact `hyp=` text as the classifier prints it, the invariant "every exec-path caller passes a parser that consumes ≥ 1 character on every success" with the FOUR call-site cites (`formatted.lem:90`, `:97`, `:103`, `:170-171`) and the definitions they rest on (`digit` `:83-85`; `sat`/`char`/`item` `monadic_parsing.lem:39-43,65-74`; `conversionSpecification`'s leading `char #'%'` with its line), reviewer field "[AGENT worker 2026-09-11]; [USER] sign-off at merge"; the two rows leave `scripts/fuel_forms_pending.txt` (header rewritten: the parser class is CLOSED by this slice, record cited; the file may be header-only — see the empty-register check in §1).

**Acceptance:** `check_fuel_forms` OK with the partition `62 MEASURED (12 under a hypothesis) + 13 ABSORBING + 0 ambient-reachable + 6 ambient-unreachable = 81` (quote it; if the total is not 81, explain every name that moved); the two `_measure_sufficient` theorems' cones ⊆ {propext, Classical.choice, Quot.sound}; kernel-only tactics, no option bumps; `check_theorem_axioms` OK; Tier A green except the parametricity-set red cleared in D3. Commit.

### D3 — Pins, callers, consumer manifest
`scripts/gen_fuel_parametricity.py --emit` → replace `TotalityProofTest.lean`'s Part 1 block (expect 14 wrappers; update the header comment's count with today's date, keeping the history); `--check` green; `test_unit.sh` green. List every generated head whose signature changed (the `Formatted.*` family losing `[LemFuel]`; anything else the diff shows) in the record's consumer-note section, the 2026-09-08 close-out §8 shape; if a hand-written module (`CerbFS`, `Main`, a `*_lemMeasureProofs`) fails to build because a callee lost its binder, repair the call and list it. Tier A fully green. Commit.

### D4 — Discharge lemmas and the equivalence theorem (bounded; stop-if-not-closing)
1. In `CerbParserProgress.lean`: kernel-checked `Consumes` for the concrete call-site parsers where the proof is a direct unfolding — `Consumes (sat pred)`, `Consumes (parse_char c)`, `Consumes item`, `Consumes (p1 <|> p2)` from both (the generated `<|>` form), and `Consumes (many1 p)` from `Consumes p` (the head-of-results structure) — and therefore `Consumes Formatted.digit`, the flags alternative, `Consumes (sat (· ≠ '%'))`. For the `:170-171` right alternative (`conversionSpecification >>= …`) a general `NonExpanding` toolbox (results never lengthen the input; closed under every combinator in the module) plus "bind of a consuming left with a non-expanding right consumes" is the honest route: do it if it closes in ordinary time; otherwise the register row's cite stands and the record says exactly which call site rests on the cite alone.
2. The equivalence theorem: keep the OLD generated `many_lemFuel`/`many1_lemFuel` text (from D1's before-diff) as `many_old_lemFuel`/`many1_old_lemFuel` in `lean_frontend/test/Unit/ManyRestatementTest.lean` and prove, for every `n`, `p`, `cs`: `parse (many_old_lemFuel n p) cs = many_run_lemFuel n p cs` and the `many1` twin (the hop structures coincide, so induction on `n` should close it; the zero cases are both sentinels applied to different shapes — if they are not definitionally related, state the theorem for `0 < n` on the succ case and say so). This turns D1's prose argument into a kernel fact. Register the exe like the other unit tests. If it will not close in ordinary time: STOP that proof, keep D1's argument, record why.
Commit.

### D5 — Manifest, tray, documents
1. Fork-drift: `[source-content]` row for `monadic_parsing.lem` moves; `[expected-semantic]` gains `monadic_parsing.ml` (single-row edits + dated header note stating "extensionally identical restatement; the OCaml's results are unchanged on every input"); any OTHER hunk is a STOP; quote the `check_fork_drift` OK line with the layer-2 count.
2. Tray: a NEW draft, next free number, PROPOSAL tier (the INDEX's question/proposal wording, drafts 06/07 as the shape): "`many`/`many1` as input-indexed recursion — a termination-checkable formulation, extensionally identical", the `.lem` diff as the patch, the equivalence argument, D4.2's theorem if it closed; INDEX row with a slotting note and the AI-provenance note.
3. `lean_frontend/TODO.md`: the C2-follow-ups block's `many`/`many1` mention and the close-out record's "what remains" pointer → RESOLVED with this record; the "Stale counts" risk-map item's TotalityProofTest line → done. `lean_frontend/VALIDATION.md` §7: the `PENDING | 2 | …` row → `0` with the history sentence extended ("… and `many`/`many1` on 2026-09-11 — by the input-indexed restatement, measured under `Consumes p`"); the (A) row's count and "under a hypothesis" count updated. `lean_frontend/CLAUDE.md`: only the sentence naming the 2026-09-08 count/pending state, if present (grep `pending`, `many`).
Commit.

### D6 — Full battery and the record
`python3 scripts/release.py --mode full` plus `./scripts/test_unit.sh` and the four `test_exec.sh` baseline lanes directly; every verdict line verbatim in `lean_frontend/docs/2026-09-11_parser-progress-measure-record.md` (`=== lane` / `rc=` pairs, `Baseline check:` lines, the `check_fuel_forms` partition, `check_fork_drift` OK, `gen_fuel_parametricity --check`, the pristine lane's `status`). The record also carries: the `.lem` diff verbatim; the correctness argument (D1); the measure and why it bounds the depth under the hypothesis, in prose; the two register rows and the discharge status per call site (D2/D4); the consumer note (D3); the manifest hunks; open questions for the orchestrator. Evidence directory `…-evidence/` (plain text, ≤ 1 MB). Commit. STOP: no merge, no push, no rebase beyond D0's; the orchestrator reviews externally.

## 3. Rules (binding)

- **File fence.** `frontend/model/monadic_parsing.lem`; `lean_frontend/CerbParserProgress.lean` (new), `lean_frontend/Monadic_parsing_lemMeasureProofs.lean` (new), `lean_frontend/handwritten_copy.manifest`, `lean_frontend/lakefile.toml` (roots + the D4.2 test exe only); `lean_frontend/test/Unit/TotalityProofTest.lean` (Part 1 block + header count), `lean_frontend/test/Unit/ManyRestatementTest.lean` (new), `scripts/test_unit.sh` (only if unit exes are registered there); `scripts/fuel_forms_pending.txt`, `scripts/fuel_hypotheses.txt`, `scripts/check_fuel_forms.sh` (ONLY the empty-register fix of §1, plant-tested); `scripts/fork_drift_manifest.txt` (single-row edits + header note); hand-written callers that fail to build because a callee lost `[LemFuel]` (list each); `docs/upstream-tray/<new>.md`, `INDEX.md` (the new row only); `lean_frontend/TODO.md`, `lean_frontend/VALIDATION.md` §7, `lean_frontend/CLAUDE.md` (D5 items only); the record and evidence directory. Generated trees change only as the OUTPUT of `make prelude-src` / `make lean-prelude-src`.
- **Forbidden:** any edit under `/home/dev/projects/cerberus-lean-proj/lem-lean/` or `deps/`; Lake/opam pin changes; changing or re-recording ANY existing baseline row in ANY lane; edits to `formatted.lem` or any other `.lem` (the call sites are cited, not changed); a behavioural change to `many`/`many1` on ANY input (a guard that stops a non-consuming parser is a semantics change — not this slice); `_zero` lemma changes beyond the mechanical move to the new worker names; `axiom`, `sorry`, `native_decide`, `bv_decide`, `ofReduce*`, `partial def`/`opaque`/`implemented_by` in the new modules; heartbeat/recursion-depth option bumps; fuel or budget numerals; `macro`s standing in for measures; `2>/dev/null` on any build/install step; building, editing or committing in the primary checkout or in any other worktree (in particular `worktrees/cerberus-lean-arc/semantics-audit-repairs`).
- **Environment.** `source /home/dev/projects/cerberus-lean-proj/scripts/env.sh` (or `scripts/ce`). Every `lake`/`lean` invocation through `scripts/capped` with `CERB_MEM_MAX=48G`; never `none`; if the sandbox denies the cgroup, stop and report. One heavy job at a time — and NOT concurrently with another worktree's builds or batteries (the operator sequences the slices).
- **Snapshot discipline.** D0's Tier A pre-run is the baseline. Before each commit, `git status --porcelain` lists only the fence. After every regeneration build the LIBRARY target too.
- **Tripwire.** Any single build/proof/lane pass approaching one hour: stop that pass, record it, report. Full-battery Tier B lanes are pre-justified. Proof passes are not: if a sufficiency proof will not close in ordinary time, the measure's shape is wrong — a bigger, simpler bound is fine (hypothesis-carrying, sufficient, cheap); if D4's proofs will not close, stop THOSE and record (D4 is bounded by design).
- **Provenance.** Quote outputs verbatim; label derived tallies; your judgments are [AGENT]; operator lines only from §0; acceptance statements in your record state observed or proved facts, never predicted verdicts. Commit messages state exactly what was verified.
- **Stop rules.** Stop and report (record + commit, no merge) when: D6 is done; or ANY existing baseline row moves in ANY lane at any step (the change must be invisible to every corpus — a moved row is a defect in the restatement or a finding; quote before/after, never re-record); or the fork-drift manifest needs a hunk outside the two enumerated; or the partition total is not 81 and the record cannot name every moved worker; or a hand-written module outside the fence must change for a reason other than a lost `[LemFuel]` binder; or a gate is red for a reason outside the fence; or the tripwire fires.

## 4. What success looks like, in the record's own words

A reader of `2026-09-11_parser-progress-measure-record.md` can see: (1) the `.lem` diff and a one-page argument that both parsers are unchanged on every input, plus (if it closed) the kernel theorem relating the old and new workers at every fuel; (2) the `check_fuel_forms` line `62 (12 under a hypothesis) + 13 + 0 + 6 = 81` and the two theorem names with their cones; (3) the two register rows with their four call-site cites and, per call site, whether `Consumes` is a theorem or a cite; (4) the 14-wrapper parametricity pin and the list of generated heads that lost `[LemFuel]`; (5) the manifest's two hunks and the layer-2 count; (6) the full A + B battery's verdict lines with zero movement; (7) the tray proposal; (8) the questions you could not answer.

## 5. Reading list
`CLAUDE.md` (repo) · `lean_frontend/CLAUDE.md` · `lean_frontend/VALIDATION.md` §7 · `scripts/LADDER.md` · `frontend/model/monadic_parsing.lem` (whole) and `lean_frontend/generated/Monadic_parsing.lean` (whole) · `frontend/model/formatted.lem:80-180` · `scripts/fuel_forms_pending.txt`, `scripts/fuel_hypotheses.txt`, `scripts/check_fuel_forms.sh` header · `scripts/gen_fuel_parametricity.py` · `lean_frontend/test/Unit/TotalityProofTest.lean:1-75` · `lean_frontend/CerbCoreShape.lean` (the Props-only seam) · `lean_frontend/Formatted_lemMeasureProofs.lean` and `lean_frontend/Core_aux_lemMeasureProofs.lean` (the `_stable_aux`/`_measure_sufficient` and mutual-under-hypothesis templates) · `lean_frontend/CerbMeasureLemmas.lean` (the shared toolbox — `to_congr` for list traversals) · lem-lean `doc/lean-backend/2026-09-05_measure-hypothesis-record.md` §2 · `docs/2026-09-05_fuel-parameter-C4-record.md` §7–§8 · `docs/2026-09-08_fuel-pending-closeout-record.md` §2, §7, §8 · `docs/2026-09-10_codex-charter-are-compatible-assumed-set.md` (§1 mechanics rows, §7 errata) and its record · `docs/2026-09-11_codex-charter-semantics-audit-repairs.md` (the sibling slice; its fence tells you what NOT to touch) · `docs/upstream-tray/INDEX.md`, `README.md`, drafts 06/07 (the proposal shape).

## 6. Pre-launch note (orchestrator [AGENT], 2026-09-15)

- **The mainline moved.** `mdd/cerberus-lean` is `eaa2066e9` (the semantics-audit repairs landed 2026-09-15, 17 commits over `54f007187`). D0's rebase clause applies: `git rebase mdd/cerberus-lean` (this branch holds only the charter and this note), then regenerate BOTH trees and rebuild BOTH engines before the Tier A baseline — the primed build products in this worktree are from `e30810be7`.
- **Facts this charter's text predates**, corrected here: `scripts/test_unit.sh` now runs NINE exes and `scripts/LADDER.md` Tier A row 1 says `9/9`; D4.2's new exe makes it ten — fence addition: `scripts/LADDER.md` Tier A row 1, the count only. The tray has drafts 39–42 and an `ocaml/` target directory: D5.2's proposal draft is **43**. Fork-drift layer 2 is **23** (`core_eval.ml` added 2026-09-15); expected after D1 = 24 (`monadic_parsing.ml`). `tests/multi_tu_tray/` and LADDER row 6b exist (not yours; do not touch). The unit-exe registration pattern to copy is `lean_frontend/test/Unit/FloatLiteralTest.lean` + its `lakefile.toml` `[[lean_exe]]` stanza + the `scripts/test_unit.sh` list. VALIDATION §7's PENDING row and `scripts/fuel_forms_pending.txt`'s header still name `many`/`many1`, as §1 says.
- **Two lessons from the sibling slice, binding here.** (i) Any ISO clause you cite is quoted from `tools/n1570.json`, never from memory. (ii) Every negative or lane-classification expectation is trialled on the CURRENT engines before it enters your plan (§8 of the sibling charter). Also: the pristine lane's reviewed-diagnostic pins (`scripts/upstream_oracle_differences.json`) hash the FULL fork stderr including OCaml backtrace line numbers of generated files; a `.lem` body change that shifts `monadic_parsing.ml`'s lines could move a pin whose backtrace passes through it. If Tier B row 10 fails ONLY on such a pin (both engines still status 125, same message, positions only), that is the orchestrator's landing-time re-pin, forbidden to you — STOP at D6 with the diff recorded, as the sibling worker did.
- **Worker identity.** The worker is a Claude Fable subagent in fresh context ([USER 2026-09-11] "launch them as claude fable class subagents"); "Codex"/"the worker" in this text is you.

## 7. Post-execution errata (orchestrator [AGENT], 2026-09-15)

- **§1 "The restatement …" / D1:** the charter's `val many_run: forall 'a. parserM 'a -> list char -> list ('a * list char)` was a typo for `list (list 'a * list char)` (a run returns the LIST of parsed items with the remaining input); the worker used the correct type as forced (record §0 item 1).
- **D2 "named by `declare {lean} extra_import`":** impossible without an import cycle — the hypothesis module states `Consumes` over `Monadic_parsing.parserM`/`parse`, so it must import the generated module; the obligation shell reaches `CerbParserProgress` through `Monadic_parsing_lemMeasureProofs`, which is what lem's generated `_auxiliary` already imports. Rule for future charters: a hypothesis seam over a generated type is imported by the PROOFS module, not named by `extra_import` (record §D2 "Why there is no extra_import").
- **§1 "Check that the gate accepts an EMPTY register" / §3 fence "`check_fuel_forms.sh` ONLY the empty-register fix, plant-tested":** the `policy` function needed no fix; the selftest's plants P2/P6/P7 named `many`/`many1` and had to be retargeted — the orchestrator reads that as inside the fence (the same three properties, plant-tested, on real names). Rule: a charter that empties or moves a register lists the gate's SELFTEST plants that name its rows.
- **§6 pre-launch note "regenerate both trees and rebuild both engines before the Tier A baseline":** superseded at launch — the worktree was re-primed from the primary rebuilt at `eaa2066e9` (the launch prompt said so); D0 ran the baseline directly.
- **D4.2 "prove, for every `n`, …":** not provable unconditionally — LemLib's `fuelExhaustedWith` is `opaque`, so the two sentinel shapes are unrelated kernel values and that leaf is reached at every fuel by a deep enough parse; the honest statements are the sentinel-hypothesis theorem at every fuel and the unconditional agreement at fuel ≥ measure (record §D4.2). Rule: an equivalence between two fuel'd formulations is stated at sufficient fuel, or under an explicit sentinel-agreement hypothesis, never bare `∀ n`.
- **§6 fence:** `lean_frontend/CLAUDE.md`'s Key-files table (new modules) was outside the fence; a landing edit by the orchestrator.
- **(pre-merge audit 2026-09-16, F2)** D1 acceptance (c) / D5.1: the fork-drift manifest hunks were performed at D1 and D2, not at D5, because the drift gate runs inside Tier A row 1 and D1's "Tier A green" needed it (record §0 item 3) — a forced reordering inside the fence, omitted from the first errata list.
- **(audit F3)** D4.2's `_old` text is the pre-change generated code modulo whitespace, not byte-verbatim; the test header now says so.
- **(audit F4)** two off-by-one cites in §1 and D2's register text: `digit` is `formatted.lem:82-84` (not `:83-85`); `many_run` spans `monadic_parsing.lem:119-123` (not `:119-122`). Fixed in the register rows and the seam header at landing; left as written above.
