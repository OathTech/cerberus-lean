# Charter — landing `feature/concurrency` (Phase 0, the SC instance): rebase, the soundness theorem, the agreement lemma and its empirical twin, the trust story (2026-09-19)

**Status:** CHARTER for one Claude Fable subagent worker, written by the orchestrator [AGENT] from the landed scoping note `2026-09-17_concurrency-landing-scoping-note.md` (mainline `6da619f69`, erratum `bb09cb745`); branch `arc/concurrency-landing` in the primed worktree `worktrees/cerberus-lean-arc/concurrency-landing`, created FROM `feature/concurrency` @ `086d8762d` (the other agent's Phase-0 work, slices S0–S7, preserved as the record; this branch carries it to the mainline). The base for the rebase is the mainline head the orchestrator names at launch (after the seam-hygiene slice lands). The worker commits its own work per deliverable, on green only; the orchestrator re-verifies every gate at every boundary; the committed record ENDS the slice.

## 0. Rulings (verbatim) and standing constraints

- [USER 2026-09-04] (Phase-0 charter §1): *"One way of approaching this would be to make the executable memory model parametric, and initially support just an SC / strongly interleaved semantics. This is very easy to implement, and would support formal verification via Iris. It's an artifical model so the trick would be building it so that we \*don't\* commit to it as the long term path. …"*; *"I think even landing a working SC model on main would be a major success …"*.
- [USER 2026-09-16]: *"I think we will also own landing the concurrency work. So let's schedule it after the allocator slice. We'll need to discuss our trust story here because it's much more tricky to know exactly what the 'right' behavior is. A key question is whether we are supporting reasoning, so we'll want to land this carefully to avoid any issues later (or issues if we switch the concurrency model)"*.
- [USER 2026-09-16]: *"We will eventually need concurrency - the overall north star is the same, i.e Linux system code."*
- [USER 2026-09-17]: *"i would imagine the cerberus-sl project would eventually want to use the same model for everything. and the model with concurrency is in some sense the 'real' model, even if we're running in a sequential context"*.
- [USER 2026-09-17]: *"Great, I agree with all of this"* — to the scoping note's §3 trust story and §4 deliverables (reproduced in §2 below), and *"Go ahead and land the notes as you suggest"*.
- [USER 2026-09-11] execution mode: Fable subagents; [USER 2026-09-08] verbatim: *"we should \*NOT\* be building anything new out-of-policy"* — the theorems below are GENERAL statements proved from the model's definitions; no enumeration over literals, no new artefact-class surface.
- The seam-hygiene slice's doctrine (landing before this): no seam default is kernel-visible; the switch arms are explicit; `CerbMem.oomKill` names the OOM kill. The WP-O doctrine: pristine upstream is the reference; a fork-only flag has no pristine counterpart and is named as such.
- Standing constraints: NEVER modify machine-global state (no opam changes — `_opam` is a symlink to the SHARED switch); NO `git push`, ever; never commit on `mdd/cerberus-lean` or `master`; the worker does the REBASE this charter orders (L1) and nothing else of that kind — no merge; fail-closed/fail-noisy (no `2>/dev/null` on build/install steps); [AGENT]/[USER] provenance; verbatim outputs; every `lake`/`lean` through `scripts/capped`; ONE heavy job at a time; the ~45-minute stop (the FULL battery excepted, polled in ≤10-minute steps); never end the turn on a wait; kernel-only proofs (no `native_decide`/`decide`-on-literals as load-bearing steps, no option bumps, no `partial`, no `sorry`).

## 1. Facts (verified by the orchestrator 2026-09-17/19; the worker re-checks at the rebase base)

**The branch.** `feature/concurrency` @ `086d8762d`: 13 commits over merge base `31eba718e` (2026-09-05); records `2026-09-04_concurrency-{scoping,phase0-sc-charter,S0-record,S1-S2-record,S1-interface-review-*}.md`, `2026-09-05_concurrency-{S3,S4,S5,S6-mergeable,S7}-record.md` and change manifests; the pre-merge audit `2026-09-05_concurrency-premerge-audit.md` lives on `audit/concurrency-premerge` @ `c0a926707` (MERGE-WITH-FIXES; F1/F2 fixed in S7; M1: the agreement lemma needs the `sc_fragment_ok` premise). Mainline is 162 commits past the base at `0457732e1` (more once seam hygiene lands). Files changed on BOTH sides (non-docs, at `0457732e1`): `backend/common/driver_ocaml.ml{,i}`, `backend/driver/main.ml`, `backend/web/instance.ml`, `frontend/model/{core_reduction,driver,mini_pipeline}.lem`, `lean_frontend/{DESIGN.md,README.md,TODO.md,VALIDATION.md,Main.lean,handwritten_copy.manifest,lakefile.toml}`, `lean_frontend/test/Unit/FuelExemplar.lean`, `lean_frontend/speclab/test/SLUnit/{ByteArr,Core,List,Seed,Tree}GateTest.lean`, `scripts/{fork_drift_manifest.txt,LADDER.md,test_unit.sh}` — the S7 precedent for conflicts is "keep mainline's form" (S7 record §0). Tray collisions: the branch's `36-atomic-qualifier-form-dropped.md`, `37-driver-scheduler-duplicate-blocked-steps.md`, `38-concurrency-flag-is-broken.md` collide with mainline's 36/37/38 (mainline ends at 44) → renumber to 45/46/47 with INDEX entries and every internal cross-reference updated.

**What the branch delivers (measured).** The selector `Core_run_aux.concurrency_model = CM_sequential | CM_sc` (`core_run_aux.lem:37-39`), threaded from `drive`; `--concurrency=sc` on both CLIs (`backend/driver/main.ml:524` cmdliner enum; `lean_frontend/Main.lean:1184-1189`; a bare `--concurrency` or unknown model refused on both); `sc_fragment_ok`/`epar_free` (`driver.lem:237-241`); the SC scheduler, the candidate-execution construction (S3 record :12-20: `sb`/`asw` from the reduction's annotations; `rf` = the most recent committed same-location write; `mo` = the writes at each ATOMIC location in commit order; `sc` = the seq_cst actions in commit order; `lo`/`tot` empty), the end-of-trace verdict: `data_races` (`cmm_csem.lem:1431`) → `UB005_data_race`, and on race-free traces the BRIDGE check `sc_accesses_consistent_execution` (`cmm_csem.lem:2077-2094`: `assumptions`, `tot_empty`, `well_formed_threads`, `well_formed_rf`, `locks_only_consistent_{locks,lo}`, `consistent_mo`, `sc_accesses_consistent_sc`, `consistent_hb`, `consistent_rf` = {`det_read`, `consistent_non_atomic_rf`, `consistent_atomic_rf`, `coherent_memory_use`, `rmw_atomicity`, `sc_accesses_sc_reads_restricted`}) — a failing leaf is a typed internal error (plant-tested by corrupting `rf`). The litmus lane `scripts/test_litmus.sh` (30 programs, `tests/litmus/`, three-way: oracle set = Lean set = the row's target in `tests/litmus/expectations.txt`, targets derived by `tests/litmus/sc_reference.py` and cross-checked against upstream's recovered 2017 sets in `backend/common/tests.ml:77-89`; `--plant` vacuity plants); `CM_sequential` refuses a spawn (`DErr_model_refused MR_spawn_under_sequential`) where upstream silently serialised; the six `SequentialIdentity` lemmas (the walker part of the agreement). Recorded limits (DESIGN.md on the branch, "temporal boundary; mover = Phase 1"): loads take their value AT ISSUE from concrete memory (the C11 operational model defers it); non-seq_cst orders/fences/Linux atomics/`wait` refused; locations are pointer values (mixed-size overlaps are not races); the S5 record's own scale warning on the agreement lemma, verbatim: *"the whole statement is a proof over the ND trace monad: reasoning-era-scale, not a Phase-0 slice (the grind ban applies; the cheap parts alone prove nothing a consumer needs)"*. Pre-existing on both engines (audit N4): `x++` on an atomic crashes the front end — a tray item, not a landing blocker. No external oracle: `herd7`/`cvc5` absent; `/usr/bin/z3` present; `backend/bmc/` not built.

**The trust story we land under** (scoping note §3, agreed): (a) the consumer's model is `CM_sc`; `CM_sequential` is the oracle-comparison instance — every differential lane runs with concurrency OFF, so the AGREEMENT LEMMA is the transfer principle carrying the sequential evidence to the consumer's model; (b) an empirical twin lane (the exec corpora under `--concurrency=sc` on both fork engines, verdicts equal to the sequential runs, fragment refusals a class); (c) the CLI default stays `CM_sequential`; (d) the soundness theorem — every race-free `CM_sc` trace satisfies the SC-accesses tree — in two layers; (e) interface vs instance stated separately; (f) DRF-SC (the converse) NAMED as the model-switch precondition, proved later; (g) `herd7` requested for the next network window.

## 2. Deliverables (commits in this order; the record last)

**L1 — Rebase and reconcile (one commit range = the 13 replayed commits + one reconciliation commit).** Rebase `arc/concurrency-landing` onto the base the orchestrator names; resolve every conflict by "keep mainline's form" (S7 precedent) — a resolution that changes SEMANTICS rather than form is STOP S4; reconcile with what landed since the base: part one/two of the allocator slice (`initial_driver_state*` now take `address_space_top`; `CerbMem.allocator` fixed; `oomKill`), WP-O (row 10's fork-only-flag exclusion list gains `--concurrency`; the register is untouched — pristine cannot run any concurrent case), seam hygiene (`failwithI` leaves, explicit switch arms, `CerbSwitch`), the S1 outcomes work if any has landed (it has not). Renumber the three tray drafts (45/46/47) + INDEX. Regenerate both trees (`make prelude-src lean-prelude-src`), rebuild both engines. Gate: the litmus lane `./scripts/test_litmus.sh --check` + `--plant` (30/30 three-way at the rebased head — anything else is STOP S3), `./scripts/test_unit.sh`, the FULL battery `python3 scripts/release.py --mode full` (Tier A + B) — every PRE-EXISTING row unmoved (sequential invariance: with concurrency off the fork engines must be byte-identical to the mainline — the audit's §1 method), row 10 + `--plant` unchanged, the three-engine report unchanged. LADDER: the litmus lane becomes a Tier A row (seconds).

**L2 — The soundness theorem (two commits).** (i) `lean_frontend/CerbSCExecution.lean`: an abstract theory of executions generated from a strict total order `T` on actions with `sb, asw ⊆ T`, `rf r` = the `T`-latest same-location write before `r`, `mo` = `T` restricted to atomic writes per location, `sc` = `T` restricted to seq_cst actions, `lo`/`tot` empty: under `data_races = ∅` each leaf of `sc_accesses_consistent_execution` holds — one general lemma per leaf (`hb ⊆ T` hence acyclic; `mo` agrees with `hb`; an SC read reads the last SC write in `sc`; coherence leaves as order embeddings; RMWs read their `mo`-predecessor because the scheduler performs them atomically; the locks leaves vacuous). (ii) `lean_frontend/CerbSCExecutionProofs.lean` (or beside): the SPECIFICATION lemma — the driver's recorded execution at trace end (the `Cmm_op.symState` pre-execution and witness the S3 construction builds) equals the abstract construction on the trace's commit order — and the theorem: for every fragment program and every `CM_sc` trace, `data_races = ∅ → sc_accesses_consistent_execution` holds on the recorded execution. Canon-first: characterise, never unfold the generated tree in a goal; kernel-only; `#print axioms` = the classical trio. STOP S2 if (ii) does not close in a bounded effort: land (i) as a proved theory, keep the runtime bridge check as the plant-tested witness, record (ii) OWED with the exact blocking goal — the operator rules on landing with it owed. Consequence to record either way: `VALIDATION.md` claim 5 ("checked per trace, not assumed; sampled") is upgraded to PROVED only if (ii) lands.

**L3 — The agreement lemma (with the fragment premise) and its empirical twin (two commits).** (a) The lemma, stated as the audit's M1 requires: for every Core file `f` with `sc_fragment_ok f = true` and `epar_free f = true`, `runND (drive tds CM_sequential f args)` and `runND (drive tds CM_sc f args)` yield the same verdict list at equal fuel. Layers: the walker part (the existing six `SequentialIdentity` lemmas), the single-thread tail (`data_races = ∅` for one thread; the single-thread recorded execution is SC-consistent — a corollary of L2(i) with `T` = the trace order), the trace-monad glue (the recording is dead state on the verdict; the refusal scan is the identity on fragment programs). The S5 record's scale warning is acknowledged: this is bounded effort with STOP S2 — if a layer needs reasoning-era grind, land what closed, record the remainder OWED with the blocking goal, and let the twin lane carry the transfer EMPIRICALLY; the operator rules. (b) The twin lane `scripts/test_exec_sc.sh` (or a `--concurrency=sc` mode of `test_exec.sh` — mirror its recipe exactly): for the exec corpora (`tests/minimal`, `coverage`, `debug`, `float`, `libc_exec`, `immaculate`), run BOTH fork engines under `--concurrency=sc` and require each case's verdict list to equal the SEQUENTIAL run's (compare captures through `scripts/observations.py`; no codec change), with fragment refusals (a non-seq_cst atomic, a fence, …) reported as their own class and pinned in a committed expectations file, fail-closed both directions, `--selftest` plants (a forged agreement, a missing/truncated file, a phantom row, a refusal mis-classed). LADDER Tier B row. Pristine cannot join (no concurrency upstream): say so in the lane header and in row 10's exclusion note.

**L4 — The trust story in the shop window.** `VALIDATION.md`: §0 — the consumer's model is `CM_sc`, `CM_sequential` the oracle-comparison instance, the agreement lemma (or its owed status, honestly) the transfer principle, the twin lane its empirical form; §1/§3 — the register class "behaviour absent upstream" for every `--concurrency=sc` observation (the fork-only flag named beside `--address-space-top`); the interface-vs-instance contract (what ANY instance guarantees: a thread pool, action granularity, typed refusal outside the fragment, race = UB, the selector total and kernel-transparent on `drive`; what is true ONLY of `CM_sc`: immediate loads, seq_cst only, pointer-valued locations); claim 5 per L2's outcome; the missing external oracle stated. `DESIGN.md` §4/the model section: the parameter, the temporal boundary (mover Phase 1), DRF-SC NAMED as the precondition of any model switch (a fault-avoiding logic proves race freedom; DRF-SC makes SC proofs transfer to a weak instance). `TODO.md` + the master plan (a rev-11 row): DRF-SC by name; `herd7` for the next network window; the `x++`-on-atomic tray item. The CONSUMER NOTE (record): `drive`'s selector argument (a signature change for cerberus-sl's pin), the interface contract they may rely on, the lemma's status, the model-switch rule.

**L5 — The record** (`lean_frontend/docs/2026-09-19_concurrency-landing-record.md` + evidence dir) and STOP.

## 3. Fence, forbidden, stop rules

**Fence (may edit):** everything the 13 replayed commits touch (the rebase); the 23 shared files (conflict resolution, mainline's form); the three tray drafts (renumber) + INDEX; `lean_frontend/CerbSCExecution.lean`, `lean_frontend/CerbSCExecutionProofs.lean`, the agreement-lemma module (new; add to `handwritten_copy.manifest`), `test/Unit/*` for their hermetic tests, `lakefile.toml`, `scripts/test_unit.sh`; `scripts/test_exec_sc.sh` (new) + its expectations file, `scripts/LADDER.md`, `scripts/test_upstream_oracle.py` ONLY the fork-only-flag exclusion entry; `VALIDATION.md`, `DESIGN.md`, `TODO.md`, `lean_frontend/docs/2026-09-05_master-plan.md` (one rev-11 row), `lean_frontend/CLAUDE.md` key-files; `scripts/fork_drift_manifest.txt` (single-row edits for the replayed OCaml/lem files, one dated header note); the record + evidence.

**Forbidden:** any `.lem` body change beyond the branch's own 13 commits (no new model changes; no weak-memory work; no deferral of read values; no change to `Cmm_op`/`cmm_csem`); any register row (`upstream_oracle_differences.json`) — pristine runs no concurrent case; any pre-existing baseline row of any lane; `check_fork_drift.sh --refresh`; lem-lean changes; a resolution that changes semantics; `sorry`, `native_decide`, `decide`-on-literals as load-bearing steps, option bumps, `partial`, new `unsafe`/`opaque`/`implemented_by`; opam/switch changes; anything machine-global; `git push`; commits on any mainline; uncapped Lean; more than one heavy job.

**Stop rules (STOP = commit what is green, write the interim state into the record, end the turn with a report):** **S1** any pre-existing baseline row moves with concurrency off; **S2** L2(ii) or an L3 layer does not close in a bounded effort — land what closed, record the remainder OWED with the blocking goal; **S3** the litmus lane is not 30/30 three-way at the rebased head; **S4** a rebase conflict whose resolution would change semantics rather than form; **S5** a step past ~45 minutes (the FULL battery excepted); **S6** any rule conflict.

## 4. Gates and commit discipline

Worktree `worktrees/cerberus-lean-arc/concurrency-landing` (primed from the mainline; the branch's generated trees differ — regenerate first; the standing pristine build appears after the rebase brings `scripts/ensure_independent_oracle.py` — run it then). `scripts/ce <cmd>`; the `scripts/common.sh` build recipes; `make lean-prelude-src` after every seam edit. Per commit (FAST-GATE): `./scripts/test_unit.sh`, `./scripts/test_litmus.sh --check` + `--plant`, Tier A rows 2–4b, `./scripts/test_immaculate.sh`, row 10 + `--plant`; from L3(b) the twin lane + its `--selftest`. FULL (`release.py --mode full`) TWICE: at the rebased head (L1) and at the final head; per-lane tails verbatim in the record; zero movement of pre-existing rows. You never merge or push; the rebase in L1 is the only history rewrite you perform.

## 5. The record (ends the slice)

§0 rulings by pointer; L1 — the rebase (old→new hashes of the 13 commits), every conflict and its resolution, the renumbering, the FULL tails at the rebased head, the litmus lane's 30/30 line; L2 — the theory's lemma list, the specification lemma or its blocking goal, `#print axioms`; L3 — the lemma's status per layer, the twin lane's expectations and plant verdicts; L4 — the doctrine text as landed; the FULL tails at the final head; errata to §1; open items (DRF-SC, herd7, `x++`, `arc/validation-foundations-concurrency`'s 17 unmerged commits checked for anything still needed); the CONSUMER NOTE. Commit it; then STOP — the orchestrator takes over (audit ask, sign-off, ff-only merge).
