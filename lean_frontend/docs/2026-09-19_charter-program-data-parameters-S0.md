# Charter — program-data parameters, S0: the multi-reader probe and the inventory (2026-09-19)

**Status:** CHARTER for one Claude Fable subagent worker, written by the orchestrator [AGENT]; branch `arc/program-data-parameters` in the primed worktree `worktrees/cerberus-lean-arc/program-data-parameters` (base: mainline `b7e45d55e`). This is the first slice of the arc designed in `2026-09-18_program-data-parameters-design-note.md` (§3 routes E-A and D-A; §8 the four decisions). S0 changes NO repository code: it is a PROBE of the lem-lean backend's multi-reader support at the pinned lem, an INVENTORY of the seams and call sites the arc will touch, and a source-level pinning of the multi-translation-unit digest behaviour. Its record ends the slice and is the input to the E-A/D-A charter.

## 0. Rulings (verbatim) and standing constraints

- [USER 2026-09-19]: *"I'm interested in making decisions that are consequential in some way but for the other kinds of decisions, which are really more implementation-focused, I think you can make the calls."* Then on Q2: *"yeah, I agree on Q2, let's roll it together"* — the digest reader (D-A) and the enum sibling map + reader (E-A) are ONE arc; one consumer re-pin.
- [USER 2026-09-17]: *"Makes sense, Let's do the sequence as you propose. We should do this soon but it's not a blocker"*; [USER 2026-09-19]: the concurrency pause — *"just get sequential cerberus-lean to the best possible state … we have a backlog our customer needs"*.
- The design note §8 [AGENT] decisions within that delegation: Q1 E-A (sibling map + reader; OCaml layout functions untouched); Q3 this probe first; Q4 the fork's OCaml keeps its registry and digest global.
- [USER 2026-09-08] verbatim: *"we should \*NOT\* be building anything new out-of-policy"* — S0 builds nothing but probe files and a record.
- Standing constraints: NEVER modify machine-global state (no opam changes — `_opam` is a symlink to the SHARED switch); NO `git push`, ever; never commit on `mdd/cerberus-lean`, `master`, or lem-lean's `mdd/lean-backend`/`master`; no merge/rebase; fail-closed/fail-noisy; [AGENT]/[USER] provenance; verbatim outputs; every `lake`/`lean` through `scripts/capped`; one heavy job; the ~45-minute stop; never end the turn on a wait.

## 1. Facts (verified by the orchestrator; cite, do not re-derive; errata to the record)

- **The reader mechanism** (lem-lean `doc/lean-backend/DESIGN.md:240-274`; `src/lean_backend.ml` `reader_lifted`, `reader_binder`, `reader_seed_param`, `reader_params_cache : (cref, param name) list`): a def is lifted if it transitively uses a reader constant; `declare {lean} reader_consumer val f` makes `f`'s generated call sites pass ALL declared reader parameters as extra leading arguments in the GLOBAL SORTED reader order; `declare {lean} reader_seed val f` overrides the injected reader name inside `f`'s body with its first argument (lexically scoped); readers and `reader_seed` compose; binder order is `[Inhabited]`, readers, supply. Existing invariance witness: `lem-lean/tests/comprehensive/invariance/inv_reader_consumer.lem` (`cfg : unit -> nat` reader; `scaled` a `reader_consumer` target_rep'd to `TestReaderConsumerImpl.scaled`; `seed_entry` a `reader_seed`). Pinned lem: `deps/lem-pinned` @ `f6542f8` (= the opam pin in `cerberus-lean/_opam` = the Lake `LemLib` rev); the lem binary is the switch's `lem`.
- **Today's readers in the model:** ONE — `declare {lean} reader val tagDefs` (`frontend/model/ctype_aux.lem:36`; `val tagDefs: unit -> map Symbol.sym (Loc.t * tag_definition)` `:11`); `reader_consumer` declares: `frontend/model/mem.lem:89-91` (`allocate_object`, `load`, `store`), `:156` (`diff_ptrval`), `:169` (`validForDeref_ptrval`) — and any added since (inventory them); `reader_seed`: `frontend/model/mini_pipeline.lem:78` (`run_const_expr_driver`). Lean entry seeding: `Main.lean` passes `fmapEmpty` during desugar/typing/elaboration and `runFile.tagDefs` at `drive` (`:521`, `:1019` region — verify lines at the base); `CerbCall.lean`'s `driveCall`; the tests and speclab that call `drive`/`initial_driver_state` (the allocator part-two record lists nine Lean callers).
- **The two seams to retire:** `CerberusImpl.lean:55-75, 264-285` (`enumRegistryRef`, `typeof_enum`/`register_enum` via `implemented_by`/`opaque`); `CerberusFresh.lean:110-121` (`digestPure`/`digest_impl`/`digest`) and `setDigestIO` (`Main.lean:616/637/961`); allowlist rows `scripts/unsafebaseio_allowlist.txt` (the enum rows `:29-30`; the digest rows — inventory). Lem: `symbol.lem:44-46` `val digest: unit -> digest` (target_reps `Cerb_fresh.digest` / `CerberusFresh.digest`), `:247-262` `fresh/fresh_pretty/fresh_cn/fresh_pretty_with_id` read `digest()`, `:276-278` `fresh_given_int n = Symbol (digest()) n SD_None`; `implementation.lem:12-13,30-33` `register_enum`/`typeof_enum`. Generated reads of `CerberusFresh.digest`: `Symbol.lean` 8, `Cabs_to_ail_effect.lean` 5, `Translation.lean` 2, `CerbCall.lean` 2 (at `e64819de7`; recount).
- **The digest on the OCaml side:** `util/cerb_fresh.ml:88-95` `digest = ref ""`, `set_digest filename` (`Digest.file`); called at `backend/common/pipeline.ml:185` (`c_frontend`, per TU), `:280` (`core_frontend`), `backend/driver/main.ml:270`; run-time minting via `Fork_renumber.fresh_symbol'` (`ocaml_frontend/fork_renumber.ml:49`) → `Symbol.fresh ()` → `digest ()` = the global's CURRENT value at the time of minting. Multi-TU: the driver processes the TUs in argument order, each `c_frontend` setting the digest; the run mints after linking → with the LAST processed TU's digest (to be confirmed from `main.ml`'s TU loop and `pipeline.ml` — S0 pins it from source on BOTH engines: Lean's `Main.lean` per-TU `setDigestIO` at `:637`/`:961` and what precedes `drive`).
- **The enum data path:** the desugar state's `tag_definitions` map holds `Enum_definition of list integer` (`cabs_to_ail_aux.lem:12`; `cabs_to_ail_effect.lem:1762-1788` `register_tag_definition`), dropped at sigma construction (`cabs_to_ail_effect.lem:2518-2528`, `Enum_definition _ -> acc`); the Core file carries `tagDefs: core_tag_definitions` (`core.lem:420`), assembled in `translation.lem:4505-4511` and merged in `core_linking.lem`; the OCaml GCC rule `ocaml_implementation.ml:130-135`.

## 2. Deliverables (S0 is docs + probe files only)

**P1 — The multi-reader probe (the arc's go/no-go).** In a SCRATCH directory under this worktree's `.tmp/` (never in the tree), write a small lem program in the style of `inv_reader_consumer.lem` with THREE reader constants of the arc's shapes — `tagDefs : unit -> map nat nat`, `digest : unit -> string`, `enum_defs : unit -> map nat nat` (names as the arc will use them; `unit -> T` each) — a `reader_consumer` val target_rep'd to a hand-written Lean stub that takes all three leading arguments, a `reader_seed` def, a supply-lifted def (`declare {lean} supply val`) in the same cone, and lifted defs that use one, two and all three readers; generate it with the PINNED lem (`scripts/ce lem -lean …`; record `lem -v`), compile the generated Lean against LemLib at the pin (a scratch Lake project pointing at `lem-lean/lean-lib` @ `f6542f8`, or `scripts/lean_probe.sh`'s recipe — under `scripts/capped`), and READ the generated code: the binder order (the global sorted reader order — which is it? alphabetical by name? by declaration? document what the backend does and cite `lean_backend.ml`), the `reader_consumer` call sites (all three passed, in that order), the `reader_seed` scoping with three readers, the interaction with the supply binder ([Inhabited], readers, supply). Then the NEGATIVE controls: a def using a reader inside a `reader_seed` body; a reader read in a non-lifted position (an error is the expected fail-closed behaviour — quote it); a `reader_consumer` whose Lean stub takes the readers in the WRONG order (the build must fail — quote it). Verdict: SUPPORTED AS-IS (with the exact rules the E-A/D-A charter must follow), or NEEDS A LEM-LEAN FIX (the failing case verbatim, the smallest fix you can name, and the paired-slice note) — either is a valid S0 outcome; a fix is NOT made here.

**P2 — The inventory at the base.** (a) Every `reader`/`reader_consumer`/`reader_seed` declare in `frontend/model/*.lem` (file:line); (b) every Lean site that passes the reader today (the `fmapEmpty`/`runFile.tagDefs` arguments in `Main.lean`, `CerbCall.lean`, `test/Unit/*`, `speclab/test/SLUnit/*`, `tests/immaculate/*.lean`, `tests/mem-scale-probes/*`) — these gain two more leading arguments; (c) every generated read of `CerberusFresh.digest` and `CerberusImpl.typeof_enum` by module and count; (d) the allowlist rows and `OPAQUE_WANT` rows that E-A/D-A retire; (e) the OCaml sites that would change: NONE for the layout functions (Q1/Q4); the two tiny target_reps (`enum_definitions ()` from `!registered_enums`; nothing for the digest) — confirm by reading `ocaml_implementation.ml` and `cerb_fresh.ml`.

**P3 — The multi-TU digest, pinned from source.** On the OCaml side: the exact order of `Cerb_fresh.set_digest` calls for `cerberus a.c b.c` (the TU loop in `backend/driver/main.ml`, `pipeline.ml:185/280`) and which digest `Fork_renumber.fresh_symbol'` therefore mints with at run time; on the Lean side: `Main.lean`'s `setDigestIO` sequence (`:616` libc dump, `:637` metadata TUs, `:961` program TUs) and the value in force when `drive` runs. State the rule D-A must mirror ("the run's reader value = the digest of TU <which>") and whether any lane could observe a wrong choice (symbol digests are not printed in batch output; say what IS observable — e.g. `--pp-core` symbol printing, the `CERB_FRESH_FLOOR_VIOLATION` backstop). If the two engines differ today in which digest the run mints with, that is a FINDING (a zero-discrepancy question) — report it, do not fix it.

**P4 — The E-A/D-A charter's §1, pre-verified.** Re-check every cite in the design note §2 against the base `b7e45d55e` (line numbers moved with part two and seam hygiene); list errata. Size the arc's slices from P1–P3: E-A (lem: `implementation.lem` two vals + a shared body; `cabs_to_ail_effect.lem:2525` one arm; the sigma/Core-file field; `translation.lem`/`core_linking.lem`; Lean: `CerberusImpl.lean` ~16 sites, `CerbMem.lean` 6, the seeding sites of P2(b); gates: census/allowlist counts) and D-A (`symbol.lem` one declare; the Lean seeding of the digest per TU and at `drive`; the deletion of `digestPure`/`digest_impl`/`setDigestIO` and their rows; the `forceIO` question — what else uses it).

**P5 — The record** `lean_frontend/docs/2026-09-19_program-data-parameters-S0-record.md` (+ evidence dir with the probe files, the generated Lean, `lem -v`, the build logs, the negative controls' verbatim errors): P1's verdict and rules; P2's tables; P3's pinned rule and any finding; P4's errata and sizing; open questions for the E-A/D-A charter. Commit ONCE (the record + evidence), then STOP.

## 3. Fence, forbidden, stop rules

**Fence (may edit):** the record and its evidence dir under `lean_frontend/docs/`; scratch files under this worktree's `.tmp/` only. **Forbidden:** ANY change to repository code, lem, OCaml, scripts, tests, baselines, the allowlist, the register, lakefiles; any change in `lem-lean` or `deps/lem-pinned` (read-only; a fix is a separate paired slice); opam/switch changes; anything machine-global; `git push`; commits on any mainline; uncapped Lean; more than one heavy job. **Stop rules:** **S1** a probe result that contradicts the design note's route (e.g. readers cannot compose with the supply binder) — report it as P1's verdict, do not work around it; **S2** a step past ~45 minutes; **S3** any rule conflict.

## 4. Gates

None beyond the probe's own build (the scratch Lake project compiles the generated Lean; `#print axioms` on the probe's lifted defs = the trio or fewer) and `git status` clean but for the record + evidence. No lane runs.
