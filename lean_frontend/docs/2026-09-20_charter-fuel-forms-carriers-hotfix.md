# Charter — hotfix: the fuel-forms gate builds its carrier modules; the memory-model measure proofs repaired after seam-hygiene H1 (2026-09-20)

**Status:** CHARTER for one Claude Fable subagent worker, written by the orchestrator [AGENT]; branch `fix/fuel-forms-carriers` in the primed worktree `worktrees/cerberus-lean-fix/fuel-forms-carriers` (base: mainline `e283bed77`). A hotfix-class slice on its own branch, to land on the mainline BEFORE the program-data-parameters E-A slice, because the mainline's Tier A row 1 is vacuous on six reviewed register rows today.

## 0. The finding (F-1), verified by the orchestrator on the mainline checkout

- `lean_frontend/CerbMem_lemMeasureProofs.lean` (a Lake root, `lakefile.toml:178`; listed in `handwritten_copy.manifest:64`; imported by NOTHING — `grep -rn "import CerbMem_lemMeasureProofs" lean_frontend` → none) does NOT compile at `e283bed77`. Reproduction (`scripts/ce ../scripts/capped lake build CerbMem_lemMeasureProofs` from `lean_frontend/` of the primary checkout), verbatim:
  ```
  error: generated/CerbMem_lemMeasureProofs.lean:921:8: Tactic `rewrite` failed: Did not find an occurrence of the pattern
    panicWithPosWithDecl ?m ?d ?l ?c ?msg
  in the target expression
    x ∈ (failwithI "CerbMem.offsetsof: unknown tag (OCaml: Pmap.find Not_found)").fst
  ```
  and the same at `:1016:18`. Both sites are `rw [panic_eq_default]` (`:883` the lemma; `:921` in `offsetsof_types`; `:1016` in the union arm of `reconstructValue_stable_aux`'s neighbourhood) on goals whose leaf is now LemLib's OPAQUE `failwithI` — seam-hygiene H1 (`fce1de9f8`, 2026-09-19) replaced the 98 registered `panic!` sites (incl. `CerbMem.lean:430` "CerbMem.offsetsof: unknown tag …" and the union-member leaf) by `failwithI`, whose whole point is that no kernel equation like `panic_eq_default` exists for it.
- The module was never rebuilt after H1: the primary checkout's `.lake/build/lib/lean/CerbMem_lemMeasureProofs.olean` (+ `.hash`) was dated **2026-09-15 18:25** (pre-H1) until the orchestrator's reproduction deleted it; `CerbMem.olean` 2026-09-15 18:24 there, `CerbTagsWf.olean` 2026-09-05 (the primary is a parking checkout, but primed worktrees COPY its `.lake`). `scripts/common.sh build_lean` builds the `cerberus-lean` exe = Main's closure, which does not include this root; `scripts/check_lakefile_roots.sh` compares NAMES only; `scripts/check_fuel_forms.sh:96-104` lists the carriers (`generated/*_auxiliary.lean`, `generated/*_lemMeasureProofs.lean`) but BUILDS only `fuel-forms-tool` (`:100`) and then `importModules` them at RUNTIME (`test/Unit/FuelFormsTool.lean:376`) — a stale `.olean` is imported without any source check.
- Consequence: the MEASURED verdicts for the six `CerbMem` rows of `scripts/fuel_hypotheses.txt:60-65` (`sizeofCtype_lemFuel`, `alignofCtype_lemFuel`, `memberAlign_lemFuel`, `offsetsof_lemFuel`, `offsetsofMembers_lemFuel`, `reconstructValue_lemFuel`) and the three hand-written seam obligations (`typeofMval`, `unqualifyAndUnatomic`, `memValueToBytes`) rested on a pre-H1 compiled artifact from 2026-09-19 to today; every Tier A row 1 since (the seam-hygiene pre-merge audit's, S0.5's, S1.5's) passed on them VACUOUSLY — a gate vacuity of the P0-F2/F3 class (`docs/2026-09-05` whole-project audit F2/F3: a gate that does not exercise what it certifies). Found by the E-A Phase 1 worker (its record §3.6) on the first explicit build of the module; the primary's olean is now gone, so the mainline's row 1 reads red honestly.
- What the failure means for the proofs: the two failing sites used `panic_eq_default` to know that the failure value "has no members" (`pot_struct_none = 2`, `oPot_of_none = 1` encode it). Five of the six layout obligations and `memValueToBytes` are unaffected in substance (their unknown-tag arms are `failwithI = failwithI`, `rfl`-shaped after H1); `reconstructValue_measure_sufficient` under `CerbTagsWf.Acyclic ambient` (row 6, `:65`) is the one whose Struct/Union arms recurse into the now-opaque list — the E-A worker's analysis says it is no longer provable under that hypothesis alone. Whether that is so is THIS slice's H3 probe; the register move it may imply is the OPERATOR's ruling (options: strengthen to `Acyclic ∧ Closed` — "every tag the type mentions is in the table", plausibly a real invariant at the load site; reclassify ABSORBING; make those two leaves transparent again). Until the ruling arrives, `scripts/fuel_hypotheses.txt` is NOT edited.

## 1. Rulings and standing constraints

- [USER 2026-09-19] delegation (implementation calls are the orchestrator's; consequential ones the operator's): the gate hardening and the mechanical repairs are implementation; the row-6 hypothesis is a register move → operator. [USER 2026-09-08] verbatim: *"we should \*NOT\* be building anything new out-of-policy"* — plants and repairs only. Working-practice rules that apply verbatim: "Gates are plant-tested (vacuity must be loud)"; "Certification integrity … verified-vs-loaded artifact gaps are findings"; "Fail-closed, fail-noisy. Everywhere."
- Standing constraints: NEVER modify machine-global state; NO `git push`; never commit on `mdd/cerberus-lean`/`master`; no merge/rebase; fail-closed/fail-noisy (no `2>/dev/null` on a build/gate step); [AGENT]/[USER] provenance; verbatim outputs, derived tallies labelled; every `lake`/`lean` through `scripts/capped`, gate scripts through `scripts/ce`; ONE heavy job; ~45-minute step tripwire (a full Lean rebuild excepted to ~1 h, wall time reported); never end the turn on a wait; a committed record ENDS the slice.

## 2. Deliverables

**H1 — The gate builds what it loads.** `scripts/check_fuel_forms.sh` `table_of_tree`: before running the tool, `lake build` EVERY carrier module it will import (the `aux` list — every `generated/*_auxiliary` and `generated/*_lemMeasureProofs` — minus `FUELFORMS_EXCLUDE_MODULES`), under `scripts/capped`, fail-closed with the log tail on failure (the same shape as the tool build at `:100`); the `--selftest` gains a PLANT: a carrier module that does not compile (a scratch copy with a broken proof, or a temporarily excluded-and-replaced carrier in the existing P11 style) must make the gate FAIL naming the module — the vacuity of F-1 reproduced in-plant so it cannot recur silently. Also decide and implement ONE of: (a) `scripts/common.sh build_lean` builds `CerberusLean` (every Lake root) and the exe — measure the extra wall time on a warm tree; if it is minutes, not tens, take it (no root can go stale unnoticed again; the roots are the manifest's hand-written modules); or (b) leave `build_lean` and rely on H1's carrier build — record the measurement and the choice. `test_unit.sh` invokes the gate as today.

**H2 — The mechanical repairs.** In `lean_frontend/CerbMem_lemMeasureProofs.lean`: everything that repairs WITHOUT a hypothesis change — restate `offsetsof_types` (`:915-925`) and the neighbouring union-arm reasoning so they no longer need `panic_eq_default` where the obligation's own hypothesis already excludes the arm (an unknown tag under `AcyclicPair`/`Acyclic` — check: does `Ranked`/`lookup` give `lookup tagDefs t ≠ none` for the tags the type mentions? if not, this is exactly the H3 question), delete `panic_eq_default` (`:883`) if nothing needs it, confirm the five layout obligations + `memValueToBytes` + `typeofMval` + `unqualifyAndUnatomic` compile, kernel-only tactics, no option bumps. Every obligation the generated `CerbMem`… no: these are hand-written seam obligations — their STATEMENTS must not change (the fuel-forms gate classifies them by shape; `scripts/fuel_hypotheses.txt` rows 60-64 unchanged).

**H3 — The row-6 probe (evidence for the operator, no register edit).** In a SCRATCH copy under `.tmp/` (or a clearly-marked `#exit`-guarded section that is NOT committed), attempt `reconstructValue_measure_sufficient` under `Acyclic ambient ∧ Closed ambient ty` with a `Closed` predicate you define in the scratch (every tag `refsOf`-reachable from the type — through members, transitively — has a `lookup` in the table; `CerbTagsWf.refsOf`/`memberTypes`/`Ranked` are the vocabulary, `CerbTagsWf.lean:93-154`): does it prove? how large is the change? what is the load-site invariant to cite (`loadM`'s type comes from the Core program's `ctype`s, whose tags the translation recorded — `translation.lem:4245` `core_tagDefs`; is every struct/union tag in a loaded type in `tagDefs` by the typing rules? cite `ail/ailTypesAux.lem` completeness or say what is missing)? Alternatively state the ABSORBING reclassification's cost (which callers lose kernel computability; whether `check_fuel_forms.sh`'s (B) form fits). Report both in the record §3 with verbatim proof-state evidence; the orchestrator relays to the operator; the register/row-6 edit is a SECOND commit after the ruling (or a follow-up slice) — not this commit.

**H4 — Docs.** Record `lean_frontend/docs/2026-09-20_fuel-forms-carriers-hotfix-record.md`: §0 the finding with the verbatim evidence (the orchestrator's mainline reproduction above + your own build), the window (H1 landing 2026-09-19 → this fix) and every Tier A run that passed vacuously on these rows (list the records: seam-hygiene audit/landing, S0.5 Part B, S1.5 Part B); §1 H1 (the gate change, the plant, the `build_lean` measurement and choice); §2 H2 (what changed in the proofs, statements unchanged); §3 H3 (the probe's evidence and the options with costs); §4 gates verbatim; §5 errata — to `docs/2026-09-18_seam-hygiene-record.md` (its §10/§11 Tier A claims were vacuous on these rows; state it there as a dated erratum paragraph, do not rewrite history) and to `VALIDATION.md`'s `check_fuel_forms.sh` row (the gate now builds its carriers). `lean_frontend/CLAUDE.md` `fuel-forms-tool` bullet + the `CerbMem_lemMeasureProofs.lean` row updated to what IS.

**H5 — Commit ONCE** (scripts + proofs + docs; message states what was verified), quote the head hash, STOP and report.

## 3. Fence, forbidden, stop rules

**Fence:** `scripts/check_fuel_forms.sh`, `scripts/common.sh` (`build_lean` only, if (a)), `scripts/test_unit.sh` (only if the invocation must change), `lean_frontend/CerbMem_lemMeasureProofs.lean` (H2; statements of the obligations unchanged), `lean_frontend/CerbTagsWf.lean` ONLY inside the scratch probe (not committed) until the ruling, the record, `docs/2026-09-18_seam-hygiene-record.md` (one dated erratum paragraph), `VALIDATION.md` (the one row), `lean_frontend/CLAUDE.md` (two rows). **Forbidden:** `scripts/fuel_hypotheses.txt` (until the operator's ruling — a second commit); `CerbMem.lean` or any semantics/seam file; `failure_reach_register.txt`; baselines; lem; the pin files; `.gitignore`; `2>/dev/null` on builds/gates; mainline commits; `git push`; uncapped Lean; more than one heavy job.

**Stop rules:** **S1** a repair in H2 needs a hypothesis change after all (report which obligation and why — it joins H3's evidence; do not edit the register); **S2** any Tier A lane movement outside row 1 (there should be none — no semantics moved); **S3** a step past ~45 minutes (the one full rebuild excepted, wall time reported); **S4** a rule conflict.

## 4. Gates

`scripts/ce ./scripts/check_fuel_forms.sh --selftest` (the new plant among them, "PLANT OK" naming the uncompilable carrier) and the gate itself (`check_fuel_forms: OK (81 fuel'd workers: … )` — the population must be the reviewed 81/62/13/0/6 unless a repair honestly changes it, then say so); Tier A rows 1–12 incl. 4b/4c/6b (`scripts/LADDER.md`), every tail verbatim; row 1's `test_unit.sh` fully green (the fuel-forms gate no longer vacuous); the `build_lean` wall-time measurement if (a); `git status` clean after the commit.

## 5. Record and reporting

The report to the orchestrator: the head hash; the plant's verbatim line; the gate's OK line; the `build_lean` measurement and the (a)/(b) choice; the H2 diff summary (which proofs changed, statements unchanged); the H3 evidence — proves / does not prove under `Closed`, the invariant citation or the gap, the ABSORBING alternative's cost — in a form the operator can rule on in one reading; every Tier A tail; deviations. Then stop.
