# Arc 15 — "the spec lab" RESULTS (Lane A, S0-S5)

Date: 2026-08-23. Provenance: [AGENT:arc15-laneA-S5] (close-out
slice), on the S0-S4 workers' records. Charter:
`2026-08-22_arc15-spec-lab-charter.md` (BLESSED 2026-08-22). Branch
`spec-lab`, worktree `cerberus-lean-spec-lab`; cerberus-lean only
(zero lem changes, as the charter anticipated — no pair re-charter
was needed). Lane B (T5 resumption) runs separately on the preserved
workbench-v2 worktree and is NOT reported here.

MERGE STATUS: NOT MERGED — the orchestrator owns the audit ask and
the merge ask (this doc deliberately contains no merge ask; skeleton
checklist at the end).

## What landed, per slice (with commits)

**S0 — preliminaries + scaffold** (`6c38469d1`): the speclab Lake
package (third-layer rehearsal, one-way require), mkHarness v1 +
S0 codecs with kernel round trips, plant-test runner, the statement
grep gate; three probes — golean structure-parameterization
(adopt/differ table, attributed), argv parity (mechanism B =
GO-pending-one-driver-flag, parked S), CN magic-comment filtering
(confirmed, mechanism pinned). Record:
`2026-08-22_arc15-s0-preliminaries.md`.

**S1 — R1 divmod, "hello template"** (`2e3481929`, `6e4538c62` +
`88f6ffa70`, `3a557efca` + `8dddd2a33`): division.c + mod.c; four
statement forms side by side (Form 1/1b/2/kernel-instance i8);
sweeps 155+169, fuzz 200, wrong-operator plants with predicted
indexes; pinned i8 fixtures + the FIRST parametric statement term
(`mainParamDecl`, digit-run zip) + sample-∀/stream-∀ statements +
kernel bridge; the in-build audit twin (statement walk + axiom
pins); the proofs lib (relsem one-way) with the refutation-schema
layer. Findings: the Form-2 newline discipline; the INT_MIN/-1
Wf-vs-CN honesty shape; the bridge's two-codec-law price. Record:
`2026-08-22_arc15-s1-r1-divmod.md`.

**S2 — R2 byte-blaster** (`61f2057b4`, `cab9086a1`, `95710f807`):
memcpy.c + get_from_arr.c; the containment-side verbatim-bytes
verdict; Canonical adopted as the second codec law; the mutation-
readback + canary + one-index-space idioms; plant blind spots
demonstrated green; malformed-stream totality arms; THE HOISTING
EMITTER (pinned terms are trees of defs — the code-generator depth
wall); getarr = the first our-spec-exceeds-CN point. Record:
`2026-08-22_arc15-s2-r2-bytearr.md`.

**S3 — R3 list, first real builder** (`356b3f3fc`, `016e0a06d`,
`72cc59fcd`): IntList_append; the builder/walker/comparator trio +
build-only harness (builder correctness as a runnable program); THE
LIVE LEAK CONJUNCT (final-allocation observable, driver baseline
measured + pinned, wrong-link plant at baseline+1 predicted by a
pure lemma); pointer-selection prototype (`--at`); structural-vs-
content plant signatures (the length arm); allocator-proxy closure +
first tag defs; the capacity-corner live catch (out[68]). Record:
`2026-08-22_arc15-s3-r3-listappend.md`.

**S4 — R4 tree rotation, THE REFERENCE INSTANCE** (`09e746d04`,
`5c3d8ea6f`, `f6ca947ab`): fresh-authored rotate_right (corpus has
no rotation; shape from tree_rev01.c); every template element in one
instance; THE SEQUENCED-CALL RULE (measured 3-executions-per-site ND
blowup + fix); pointer selection promoted to a STATEMENT FAMILY
(parent-link write-back); the two-observable plant separation
(leak-free break vs leaking break); the S4-E1 readback-vs-
decomposition verdict; emitter fidelity delta EMPTY (construct
saturation). Records: `2026-08-23_arc15-s4-r4-treerot.md` + the
worked-example doc (folded into the template note at S5).

**S5 — CN-slate seed + close-out** (`c6aac0e6d` + this docs batch):
swap_pair.c + cn_inline.c lookup_size_shift; THE AMORTIZATION
MEASUREMENT (both targets in 31.5 min wall, zero new
codecs/idioms/pp-surfaces — the ladder pays for the corpus,
measured); the Wf-free bridge (full-domain u64 model); the
kernel-characterized plant blind set (`swapPlant_blind_iff`); the
CoreParser ENUM-CTYPE GAP found + parked-priced (the arc's one
oracle-dump-fidelity finding); the CN-vs-us comparison record; the
template note revised (worked example + four normative amendments +
confirmed verdicts); both registers closed with summary heads + the
Linux-scaling memo. Records: `2026-08-23_arc15-s5-cn-seed.md`,
`2026-08-23_arc15-s5-cn-comparison.md`.

## Charter success criteria — status

1. R1-R4 landed, runnable, differentially validated, plant-tested,
   kernel-certified at concrete streams minimum — **MET** (R5 on
   top; ~1,970 differential executions across the arc's lanes,
   derived from the rung records: sweeps+form2 ≈ 1,102, fuzz 800,
   plant/malformed rows 68 — every one agreeing across both
   pipelines and the pure prediction; every rung plant-tested with
   predicted signatures; statement layers pinned with cones
   verbatim, in-build).
2. Twin registers with every experiment recorded incl. abandonments;
   proof register closes with the memo — **MET** (summary heads +
   memo in the registers).
3. Idiom library v1 with golean attribution — **MET** (Codec
   RoundTrip+Canonical u8..u64/arrays; mkHarness; the harness
   phase/comparator/builder/walker/teardown idioms; the emitter
   zip/hoist machinery; attribution in speclab/README.md + S0
   record).
4. Template note revised; CN slate seeded with the comparison
   record — **MET** (this slice).
5. Lane B (T5) — out of Lane A's scope; its status reports
   separately.

## The twin registers' headline findings (one paragraph each)

SPEC-STYLE (full table in the register head): Form 1 default
everywhere, model-∀ headline, codecs ship both laws, verbatim bytes
for buffers, parametric zip at ≥3 instances, readback-equality in
front with decomposition in back, plants with predicted indexes +
demonstrated (at R5: characterized) blind sets + leak-class
separation, Wf surfaces the whole domain, statements label their
quantification. The lab's discipline itself caught three real
defects in its own instruments during the arc (Form-2 empty stdout,
the build-only capacity corner, the fake 15-node capacity corner) —
the lanes work.

PROOF-STYLE (full table + memo in the register): P5 pure-transport
is the graded workhorse (flat per-operation cost, five definitional
collapses of CN postconditions, zero elaboration pressure); P1 is
the priced bridge whose growth axis (call structure, not construct
vocabulary — the pp surface saturated at R3) is exactly the walker's
amortization axis; P2/P3/P4 are staged behind P1's equations with
their first targets named. The P5 trend 7→3→3→8→~5(2.7) is
flat-per-operation with a measured structural reason for the R4
bend (operation count × recursive model, ≈2.5/LOC per operation).

## THE LINUX-SCALING MEMO (compressed; full text = proof register §memo)

* The evidence supports a two-stage architecture: P5 pure models in
  front of everything (property content is nearly free and
  embarrassingly parallel), the P1 walker campaign (+P3 replay) as
  the single bridge obligation per family, P2 modularity behind it
  for kernel-shaped call graphs.
* The binding constraint is the exec-equation campaign, not
  statement supply — statements + parametric terms are complete five
  rungs deep, and the walker's cost structure is now measured
  (construct vocabulary SATURATED at R3; growth is call structure).
* Ranked next investments: (1) the exec-equation campaign with
  replay wired in (amortizes over every landed and future
  statement); (2) T5 (prerequisite for its loop rungs + all symbolic
  axes); (3) the two S-priced instrument gaps (CoreParser enum arm,
  oracle allocation-census line); (4) the P2 modular layer once
  equations exist.
* The honest long pole for kernel-∀: the shape-parametric walls
  (symbolic arity × recursion depth × path), to be landed one axis
  at a time on T5 tech, with fixed-shape families as honest
  intermediate claims.
* Named unknowns: walker round-growth at five-proc call graphs;
  exhaustive-mode ND economy on real kernel idioms; composition with
  the future cmm schedule-stream design.

## Parked / priced inventory (the arc's complete list)

| Item | Price | Owner surface | Notes |
|---|---|---|---|
| THE EXEC-EQUATION CAMPAIGN (S1-P1..S5-P1) | L (own slice) | speclab+relsem | after Lane B/T5; order R1-i8 → R5-swap → R2 → R3 → R4; upgrades sample-∀ → family-∀ (five parametric endpoints built), plant refutations → unconditional |
| Shape/length/path-parametric ∀ | XL-class, staged | relsem (T5 tech) | the R2/R3/R4 walls: symbolic-arity initializers + recursion depth + path axis; per-shape scoping amendment keeps claims labeled |
| CoreParser `enum TAG` ctype arm | S | SEMANTICS surface | unblocks the lookup pinned layer (fixtures + 2-param zip plan committed); S5-E4 reproducers in tests/speclab |
| Oracle `--batch` allocation-census line | S | fork oracle side | the leak conjunct's differential leg (S3-N item 3; R4 doubles the stakes); upstream-tray candidate |
| Mechanism-B Lean driver `--args` flag | S | driver Main.lean | argv parity verified at S0; strict headline upgrade, never a dependency |
| DivMod local canonicity consolidation | S | speclab | now triply shadowed by Codec.Canonical (u16/u32); churn-isolation deferral, noted in Codec.lean |
| Form-2 leak/teardown observable | — | closed | subsumed: leak = outcome scalar since R3 |
| Structural tree shrinking (fuzz) | S | speclab lanes | byte shrinker never fired in 800 fuzz runs across rungs; revisit on first divergence |
| speclab cross-rung naming convention note | XS | speclab docs | S5-E5: five rungs of namespaces now collide-prone |

## Validation state at close (Lane A surface)

Every commit was gated per the charter battery; at the final batch:
speclab plain capped `lake build` green (statement-TCB gate: 46
statements clean, wrapper-hole negative test detecting; axiom pins
exact, doctored-pin plant test run both directions at every rung);
speclab-test 56/56; `test_speclab.sh` --selftest/--plant PASS; all
five lane scripts (divmod, bytearr, list, tree, seed)
--plant/--gate PASS; `./scripts/test_unit.sh` `Total: 7 passed, 0
failed`. The standard battery rows beyond the speclab surface
(exec zero-movement, core, verify, cn_coverage, immaculate, libc,
uri) were green at slice boundaries per the rung records; the
orchestrator re-runs them independently at the pre-merge gate
(worker-claimed green is never accepted).

## Merge-checklist SKELETON (orchestrator-owned; no merge ask here)

1. Independent re-run of the full Tier-A battery + the five speclab
   lanes (all modes, not just --plant/--gate: --sweep, --fuzz,
   --i8sweep/--form2/--at/--buildsweep rows per lane) from a clean
   rebuild (certification rule 1: cache-disabled where build rules
   changed — the speclab lakefile grew two exes).
2. Slow-ladder rows if the orchestrator judges them contended
   (libxml2, uri) — no semantics-surface files were touched by Lane
   A (verify: `git diff mdd/cerberus-lean..spec-lab --stat` shows
   only speclab/, scripts/test_speclab*, tests/speclab/, docs/,
   + the container-level template note).
3. Pre-merge AUDIT ASK to the operator (unconditional; propose
   scope: the S5 CoreParser-gap disposition, the impl0 hand-pin, the
   register summary heads' faithfulness to the entries).
4. Lane B (T5) coordination: serialize merges per playbook; if Lane
   B moved the mainline, rebase + re-gate + re-ask.
5. ff-only merge on explicit operator sign-off; then the standing
   post-merge certification sweep.
6. Post-merge: workbench-v2 worktree remains preserved (T5); the
   spec-lab worktree prunes only after the orchestrator confirms
   nothing in-flight references it.
