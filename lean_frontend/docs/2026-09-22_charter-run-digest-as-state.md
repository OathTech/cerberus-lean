# Charter — the run's minting digest as run-state data ("D-S"; cerberus-sl hidden-state note items 1 and 6) (2026-09-22)

**Status:** CHARTER for one Fable-class worker (a subagent of this orchestrator or an agent the operator launches — the charter is the same), written by the orchestrator [AGENT]; branch `arc/run-digest` in the primed worktree `worktrees/cerberus-lean-arc/run-digest` (base: the E-A head `e2fc391f2` of `arc/program-data-parameters`, NOT the mainline — §0 "base and landing"). The design is `lean_frontend/docs/2026-09-20_run-digest-as-state-design-note.md` and cerberus-sl ACCEPTED it in `2026-09-20_run-digest-design-response-cerberus-sl.md` (cherry-picked onto `arc/run-digest` at chartering, so the record cites them in-tree). It replaces the blocked reader route "D-A" of the program-data-parameters design note §3.2 (the E-A record §3.1 has the block: a reader on `digest` would lift `Symbol.fresh*`, whose binders would need types from modules downstream of `Symbol`).

## 0. Rulings (verbatim), decisions, base and landing, standing constraints

- [USER 2026-09-19] delegation: *"I'm interested in making decisions that are consequential in some way but for the other kinds of decisions, which are really more implementation-focused, I think you can make the calls."* [USER 2026-09-16]: *"cerberus-sl is our main upstream customer at the moment."* [USER 2026-09-04]: *"we don't change the lem structure for ocaml"* — accepted exceptions: parameters DEAD-equal on the OCaml target (the supply, the address-space top, the N-ary seed) and shared bodies both targets compute identically; this slice adds an entry parameter and a state field whose OCaml value equals the global the OCaml minting already reads. [USER 2026-09-08] verbatim: *"we should \*NOT\* be building anything new out-of-policy"* — the kernel facts below are cerberus-sl's named acceptance facts. [USER 2026-09-22]: *"Go ahead, you can write the charter and setup a worktree for this work"*.
- cerberus-sl's rulings (their `docs/2026-09-20_run-digest-design-response.md` §4): Q1 [AGENT, C3.34] `Program.digest` is a FIELD seeded from the entry (`progOf F supply digest`); Q2 no exported theorem needed (welcome, not depended on); Q3 no theorem of theirs uses the program digest for a program symbol; **Q4 [USER 2026-09-20] "agree, we should wait for the complete solution to land" — ONE re-pin after this slice lands alongside E-A**; Q5 item 7 started separately (`fix/match-pattern-arity`, done). Their §6 remark: `initial_core_run_state dg xs` (the non-`_given` constructor) still draws its supply from `Symbol.fresh_int ()` — on the Lean target that is a SUPPLY draw (threaded), not an ambient read; say so in the record, no action.
- **Base and landing [AGENT]:** based on `e2fc391f2` because the slice touches nine files the E-A branch also changed (`mini_pipeline.lem`, `core_run_aux.lem`, `Main.lean`, `CerbCall.lean`, `backend/web/instance.ml`, the fork-drift manifest, the failure-reach register, `lakefile.toml`, `test_unit.sh`) in the same regions; it LANDS AFTER the E-A arc (ff onto the mainline once E-A is there). If E-A's pending Codex delta audit adds commits to `arc/program-data-parameters`, this branch REBASES onto them (mechanical) before its own audit ask. The E-A worktree and `fix/match-pattern-arity` are READ-ONLY for this worker.
- Standing constraints: NEVER modify machine-global state; NO `git push`; never commit on `mdd/cerberus-lean`/`master`, on `arc/program-data-parameters` or `fix/match-pattern-arity`; no merge/rebase except the rebase named above; fail-closed/fail-noisy (no `2>/dev/null` on a build/gate step); [AGENT]/[USER] provenance; verbatim outputs, derived tallies labelled; every `lake`/`lean` through `scripts/capped`, gate scripts through `scripts/ce`; **ONE heavy job on the machine** — the operator's external reviewer (Codex) may be building and running batteries in `audit/*` worktrees: before every rebuild and every lane run check `ps -ef | grep -E "release.py|lake build|lean |dune build" | grep -v grep | wc -l` and the load, and WAIT (report the wait) if a heavy job is running; ~45-minute step tripwire (full rebuilds and the Tier B battery excepted to ~1.5 h, wall times reported); never end the turn on a wait; a committed record ENDS the slice; the FINAL battery runs on a FROZEN tree (write the record before starting it; touch nothing during; copy `report.json`/`summary.txt` into the evidence dir afterwards — the E-A audit's E3 lesson).

## 1. Facts (verified by the orchestrator at `e2fc391f2`; cite, do not re-derive; errata to the record)

- **Run-time minting is ONE site on the Lean target:** `frontend/model/core_run.lem:116-119` `fresh_symbol'` — `(Symbol.fresh_given_int n, <| run_st with sym_supply= n+1 |>)`; generated `Core_run.lean` `fresh_symbol'` is the only generated caller of `fresh_given_int` (E-A record W7; cerberus-sl confirmed at their pin). OCaml rep: `declare ocaml target_rep function fresh_symbol' = \`Fork_renumber.fresh_symbol'\`` (`core_run.lem:126`; `ocaml_frontend/fork_renumber.ml:49` `let fresh_symbol' run_st = (Symbol.fresh (), run_st)` — ambient, upstream's scheme; UNCHANGED by this slice).
- **`fresh_given_int`:** `symbol.lem:276-278` `val fresh_given_int: nat -> sym` / `let fresh_given_int n = Symbol (digest()) n SD_None` — the one ambient `digest()` read on the run path. `fresh`/`fresh_pretty`/`fresh_cn`/`fresh_pretty_with_id`/`fresh_object_address` (`:247-262, 280-281`) are FRONTEND minting: untouched.
- **The run state:** `core_run_aux.lem:249` `sym_supply: nat;` in `core_run_state`; constructors `:270-271` (vals), `:286-297` `initial_core_run_state_given sup xs`, `:299-300` `initial_core_run_state xs = initial_core_run_state_given (Symbol.fresh_int ()) xs`; the OCaml mirror of the non-`_given` constructor is `fork_renumber.ml:57-63` (`sym_supply = 0`, a documented divergence) — it MUST gain the new field.
- **The driver entries:** `driver.lem:1520` `initial_driver_state_with address_space_top run_st file fs_state`, `:1538` `initial_driver_state address_space_top file fs_state`, `:1543` `initial_driver_state_given sup address_space_top file fs_state` (the address-space top is the FIRST explicit parameter by its slice's rule — keep it first, the digest SECOND). Callers: OCaml `backend/common/driver_ocaml.ml:169,210` (`conf.address_space_top file fs_state`), `backend/web/instance.ml:639`, `backend/ocaml/runtime/rt_ocaml.ml:360`; lem `mini_pipeline.lem:177` (`Driver.initial_driver_state_given sup_after_elab address_space_top dummy_core_file Fs.fs_initial_state` — the const-expr mini-run); Lean `Main.lean:1043` (`initial_driver_state supply addressSpaceTop runFile fsState`), the tests/speclab callers `test/Unit/FuelExemplar.lean`, `speclab/test/SLUnit/{ByteArr,Core,List,Seed,Tree}GateTest.lean`, `test/Unit/FuelFormsTool.lean` (grep `initial_driver_state` — E-A did not change this entry: it is supply-lifted only, W7).
- **The digest the run must receive — the rule both engines follow today** (`2026-09-19_program-data-parameters-S0-record.md` §3, pinned from both sources): the digest of the LAST translation unit the frontend processed — `cerberus a.c b.c` → `Digest.file "b.c"`; Lean `Main.lean:964-979` sets the global per program TU in `tunits` order (`:979 setDigestIO digest`), libc dump (`:629`) and metadata TUs (`:650`) earlier and never winning; `.co`/`.core` inputs set nothing (OCaml `pipeline.ml:666`; a run with NO program TU mints with the global's prior value — `""` if nothing set it). The OCaml callers pass `(Cerb_fresh.digest ())` = exactly that value at run init.
- **Lean sites that read the digest on the run path:** `CerbCall.lean:156` `PrefFunArg callLoc (CerberusFresh.digest ()) n` (a prefix, not an identity — uniformity). `CerberusFresh.digest`/`digestPure`/`digest_impl`/`setDigestIO`/`forceIO` STAY (frontend minting reads them; the allowlist rows `scripts/unsafebaseio_allowlist.txt` and the `OPAQUE_WANT` rows in `check_theorem_axioms.sh` for them are UNCHANGED by this slice); `CoreParser.mkSym`'s `""` (`CoreParser.lean:242-243`) UNCHANGED (library symbols).
- **Fork-drift manifest** (`scripts/fork_drift_manifest.txt` at `e2fc391f2`): layer-2 rows exist for `core_run_aux.ml` (`:488`), `core_run.ml` (`:489`), `mini_pipeline.ml` (`:490`), `symbol.ml` (`:491`), `driver.ml` (`:493`) — all MOVE (single-row re-pins + one dated NOTE); layer 1 `[files]` already lists `backend/common/driver_ocaml.ml` and `ocaml_frontend/fork_renumber.ml`; `backend/web/instance.ml` and `backend/ocaml/runtime/rt_ocaml.ml` are NOT listed — if the gate's layer-1 set requires every fork-touched non-generated file, they are NEW rows with the review classification the manifest header prescribes (read it); state what the gate demanded.
- **cerberus-sl's acceptance (their §5):** `fresh_given_int d n = Symbol d n SD_None := rfl` in their layer; the `MintDigest`-shaped `rfl`; the pin `mintDigest_pin_t11` deleted; `Spikes.T11S.t11_adequacy_concrete_pin`'s cone = `selectAgrees_pin` alone (or the trio if item 7 has landed); their `corpus-check` on the eight quoted modules with the new `digest` field; the run-state invariant `rs.sym_digest = P.digest` preserved (their theorem, one line — no step writes the field). Our record's consumer note states what THEY will see; the S0.5-audit N1 rule (per-seed value pins) is not engaged — this slice adds no seed.

## 2. Deliverables

**D0 — DONE by the orchestrator at chartering:** the two design docs (`b4422df14`, `4d777218e` from `docs/run-digest-design`) are cherry-picked onto `arc/run-digest` (the two commits before this charter's); the worker only confirms `git log` shows them and cites them in-tree.

**D1 — lem (shared bodies; the OCaml value is equal by construction).**
- `symbol.lem:276-278`: `val fresh_given_int: digest -> nat -> sym` / `let fresh_given_int d n = Symbol d n SD_None`. Nothing else in `symbol.lem`.
- `core_run_aux.lem`: `sym_digest: Symbol.digest;` beside `sym_supply` (`:249`, with a comment: "the digest every run-minted symbol carries; seeded by the driver entry; no step writes it"); `val initial_core_run_state_given: nat -> Symbol.digest -> map … -> core_run_state` / `let initial_core_run_state_given sup dg xs = <| …; sym_supply= sup; sym_digest= dg; labeled= xs |>`; `val initial_core_run_state: Symbol.digest -> map … -> core_run_state` / `let initial_core_run_state dg xs = initial_core_run_state_given (Symbol.fresh_int ()) dg xs`.
- `core_run.lem:116-119`: `(Symbol.fresh_given_int run_st.sym_digest n, <| run_st with sym_supply= n+1 |>)`; the `:120-126` comment gains one sentence (the digest is the state's; OCaml rep unchanged, ambient, equal value); the target_rep line UNCHANGED.
- `driver.lem`: `initial_driver_state address_space_top digest file fs_state = initial_driver_state_with address_space_top (Core_run.initial_core_run_state digest (…)) file fs_state`; `initial_driver_state_given sup address_space_top digest file fs_state = ( initial_driver_state_with address_space_top (Core_run.initial_core_run_state_given sup digest (…)) file fs_state, sup+1 )`; `initial_driver_state_with` UNCHANGED; the `:1535-1537` comment names the digest as the SECOND explicit parameter.
- `mini_pipeline.lem:177`: the mini-run passes `(Symbol.digest ())` — the desugarer's current TU digest on both targets (Lean: the frontend global the desugar phase still sets; OCaml: the global) — with a comment citing the S0 record §5 Q-C. (This is the one remaining ambient read on the mini-run's seed path; it is FRONTEND-phase, like `fresh_sym*`, and stays — say so.)
- Every other lem site that constructs a `core_run_state` literal or calls the changed vals: grep `initial_core_run_state\|core_run_state` in `frontend/model/*.lem` and fix compiler-forced sites only; list them in the record.

**D2 — OCaml (dead-equal values; the oracle's behaviour unchanged).** `fork_renumber.ml:57-63` mirror record gains `sym_digest = Cerb_fresh.digest ();` (+ one comment line); `driver_ocaml.ml:169,210`, `instance.ml:639`, `rt_ocaml.ml:360` pass `(Cerb_fresh.digest ())` as the new second argument (compiler-forced; `rt_ocaml.ml` and `instance.ml` are optional backends — edit them anyway: the E-A audit's E2 established that "not compiled by the driver build" is not evidence of validity, and type-check them in isolation as that audit did if the full targets do not build). `Fork_renumber.fresh_symbol'` UNCHANGED. Build: `scripts/common.sh build_cerberus` (the libc.co step included).

**D3 — Lean.** `Main.lean`: a pure `def runDigest : List (String × translation_unit) → String` = the LAST unit's digest, `""` for an empty list (the OCaml global's value when no TU was processed — cite `pipeline.ml:666`/the S0 rule), used at the run entry: `initial_driver_state supply addressSpaceTop (runDigest tunits) runFile fsState` and passed to `CerbCall.driveCall` (which forwards it to the `PrefFunArg` at `CerbCall.lean:156` in place of `CerberusFresh.digest ()`); the `setDigestIO` calls at `:629,:650,:979` STAY (frontend phase) — the comment at `:496-499` and `:964-979` is re-worded to say the RUN no longer reads the global. Every Lean caller of `initial_driver_state`/`initial_driver_state_given` (`FuelExemplar.lean`, the five speclab gate tests, `FuelFormsTool.lean`, any other `grep` hit) gains the digest argument — tests pass a named test digest (a 32-hex string constant, never `""` unless the test is about the empty case); the exemplar theorem `FuelExemplar.lean` (`exemplar_certified_shipped_forall (fuel) (top) …`) gains a `∀ (digest : String)` binder — its statement stays quantified, its proof mechanical. `handwritten_copy.manifest`: only if a hand-written file is ADDED (the new test module below is under `test/Unit/`, not a seam).

**D4 — Kernel tests (cerberus-sl's acceptance facts; a unit exe `run-digest-test`, `test/Unit/RunDigestTest.lean`, registered in `lakefile.toml` and `scripts/test_unit.sh`):** T1 `fresh_given_int d n = Symbol d n SD_None := rfl` on the generated definition; T2 the `MintDigest` shape `∀ m, symDigest (fresh_given_int d m) = d := fun _ => rfl` (or `by intro; rfl`); T3 `runDigest [] = ""` and `runDigest [(d1,u1),(d2,u2)] = d2` by `rfl`/`decide` on concrete lists; T4 the entry-shape pin on a COMMITTED cabs-json fixture: the digest a cabs-json carries is 32 lowercase hex digits and `≠ ""` (find a committed cabs-json under the tests; if none exists, commit ONE tiny fixture produced by the oracle's `--cabs-json` from a one-line C file, with its provenance in the record — a test fixture, in policy); T5 the state pin: `(initial_driver_state sup top d file fs).1.core_run_state.sym_digest = d` by `rfl` on a small `file` literal (the `FuelExemplar.lean:120` literal shape), and `fresh_symbol'` on that state mints `Symbol d sup SD_None` by `rfl`; T6 a NEGATIVE control by `#guard_msgs`: `fresh_given_int n` (the OLD arity) no longer elaborates. `#print axioms` on T1–T5 = the trio or fewer (pinned by `#guard_msgs`, as `MatchPatternArityTest.lean` does).

**D5 — Registers, manifest, docs.** `scripts/fork_drift_manifest.txt`: the five layer-2 rows moved (+ any new layer-1 rows the gate demands) by single-row edits + one dated NOTE (never `--refresh`); `scripts/failure_reach_register.txt`: expected UNCHANGED (no new failure leaf) — run the gate, reseal only if the census moves and say why; `unsafebaseio_allowlist.txt`, `OPAQUE_WANT`: UNCHANGED (frontend seam rows stay). Docs: `docs/2026-08-22_arc14-effect-erasure-invariant.md` — its digest paragraph narrows to FRONTEND minting ("the run's minting digest is run-state data since D-S 2026-09-22; the frontend's `Symbol.fresh*` reads remain the seam"); `VALIDATION.md` §4's digest-boundary bullet likewise; `lean_frontend/CLAUDE.md` rows for `Main.lean` (`runDigest`, the entry's second explicit parameter) and `CerberusFresh.lean` (frontend only); `TODO.md`; the record `lean_frontend/docs/2026-09-22_run-digest-as-state-record.md` with: §0 decisions/provenance, §1 the diff by file with reasons, §2 T1–T6 verbatim, §3 the consumer re-pin note FINAL TEXT for cerberus-sl (their §3 table: `Program.digest` field / `progOf F supply digest`; `initial_driver_state (sup) (top) (digest) (file) (fs)`; `initial_core_run_state_given (sup) (digest) (xs)`; `core_run_state.sym_digest`; `fresh_given_int (d) (n)`; the run-state invariant they add; `MintDigestC` deleted; the multi-unit rule; frontend minting unchanged; plus the E-A changes from the E-A record §9.2 so ONE note covers the one re-pin — E-A's +1 leading reader argument, `file.enumDefs`, the `CertP` literals, the `*_ity` signatures, the `reconstructValue` arm reshape), §4 gates verbatim, §5 errata, §6 evidence (`report.json`, `summary.txt` committed).

**D6 — Gates and commit.** Regenerate (`scripts/ce make prelude-src lean-prelude-src`, lem 38f87d5); `build_cerberus`, `build_lean` (every root), speclab; Tier A rows 1–12 incl. 4b/4c/6b (`Total: 14 passed` with the new exe); then the FROZEN full battery `scripts/ce python3 scripts/release.py --mode full --out <dir under .tmp>`: expected `full: passed; 39/39 … Source unchanged: True. Complete tier selection: True.`, pristine `semantic_agreement: 835` (this base) with `reviewed_difference: 7`, every lane at baseline — ZERO movement (stop rule S2; the digest VALUE every minted symbol carries is unchanged on both engines); the multi-TU lanes (`test_multi_tu.sh`, row 6/6b) are the differential witness of the last-unit rule. ONE commit (lem + OCaml + Lean + tests + registers + docs + record + evidence); quote the head; `git status` clean; STOP and report.

## 3. Fence, forbidden, stop rules

**Fence:** `frontend/model/symbol.lem` (`fresh_given_int` only), `core_run_aux.lem` (the field + the two constructors), `core_run.lem` (`fresh_symbol'` + comment), `driver.lem` (the two entries + comment), `mini_pipeline.lem:177` (one argument) + any compiler-forced `core_run_state` literal in lem; `ocaml_frontend/fork_renumber.ml` (the mirror record), `backend/common/driver_ocaml.ml` (2 sites), `backend/web/instance.ml` (1), `backend/ocaml/runtime/rt_ocaml.ml` (1); `lean_frontend/Main.lean` (`runDigest`, the entry, comments), `CerbCall.lean` (the forwarded digest), `test/Unit/FuelExemplar.lean`, `test/Unit/FuelFormsTool.lean`, `speclab/test/SLUnit/*GateTest.lean` (the entry argument), `test/Unit/RunDigestTest.lean` (new) + `lakefile.toml` + `scripts/test_unit.sh`, ONE committed cabs-json fixture if needed; `scripts/fork_drift_manifest.txt` (rows + NOTE), `scripts/failure_reach_register.txt` (only if the census moves); docs named in D5. **Forbidden:** `Symbol.fresh*`/frontend minting; `CerberusFresh.lean` (its opaques stay); `CoreParser.lean`; `Fork_renumber.fresh_symbol'`; any baseline/expectation/fixture other than the one cabs-json; the pristine register; lem-lean; the pin files; `.gitignore`; the E-A and item-7 worktrees; `2>/dev/null` on builds/gates; commits on any mainline or on the other two branches; `git push`; uncapped Lean; two heavy jobs.

**Stop rules:** **S1** a lem generation refusal or a type error that needs a change outside the fence (report the shape); **S2** ANY lane movement (a MISMATCH/DIFF, a baseline delta, a pristine-lane class change) — report verbatim, never re-baseline; **S3** a step past ~45 minutes (full rebuilds / the battery excepted to ~1.5 h, reported); **S4** a rule conflict; **S5** a heavy job is running when you need one — wait, report the wait; **S6** the E-A branch gains commits (its delta audit) before your battery — rebase onto its new head FIRST, then gate (say so).

## 4. Record and reporting

Report to the orchestrator: the head hash; every lem/OCaml hunk verbatim (they are short); T1–T6 outputs verbatim; every Tier A tail and the battery's summary verbatim with the pristine counts; the manifest rows moved/added; the register/allowlist/opaque counts (unchanged); the consumer note's final text; any wait, any rebase, every deviation with its reason. Then stop for the audit ask.
