# Arc-14 decision log

- **D1 [AGENT] — S0 verified; rulings** (relayed 2026-08-22): plan
  adopted, S1/S2 sequential, '\?' = upstream candidate #10, two
  deferrable prices deferred, sem:S18 partial-overtake accepted;
  worktree lem-sync provisioning executed by the orchestrator + the
  new-worktree.sh priming gap fixed at container level.
- **D2 [AGENT] — S1 boundary verified; S2 GO.** Six batch commits to
  ca59aa4c1; orchestrator re-ran: immaculate lane at committed
  baseline (11/12 probes flipped; g5-decode-question intentionally
  Lean-correct/oracle-wrong), unit 7/7, exec zero movement, core ALL
  PASSED. All 6 GRAVE semantics findings FIXED probe-flipped; new
  upstream candidate #11 (escape round-trip 127→87) ACCEPTED for the
  S3 tray drafts; the recorded-with-price parks (S9/S13/S15/N10)
  accepted. Process notes accepted: the contaminated intermediate
  bars were re-certified on settled trees before commit (recorded
  honestly); the manifest single-line edit over --refresh was right
  (the --refresh header-stripping defect is a known register item).
  Cosmetic for S3: the lane's explanatory message still describes
  the S0 expected-state wording — refresh it with the post-S1
  baseline description. S2 (the backend slice) is GO: batches B1→B6
  per the S0 plan — de-globalization under the byte-neutrality
  protocol, the Ott grammar repair, Set-eq, the instance-priority
  guard (with sem:S2's cross-listed fix), the tail.
