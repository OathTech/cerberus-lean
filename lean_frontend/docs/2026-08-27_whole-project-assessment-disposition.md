# Whole-project assessment — disposition record

Source: the professor-grade whole-project assessment (container
`notes/2026-08-27_professor-whole-project-assessment.md`, 2026-08-27),
commissioned under the operator mandate [USER 2026-08-27, verbatim]:
"Our aim here is to BUILD A VERIFICATION FRAMEWORK SIMILAR TO BRICK
OR REFINEDC. That's the aim. We are NOT executing code at specific
inputs. We are NOT doing enumeration. These are COMPLETELY FORBIDDEN
as proof strategies. Examples should be quantified / 'all input'
properties… the MINIMUM kind of specification we care about."

Assessment headline (professor, quote): the project built "a sound,
kernel-certified, Iris-carried concrete-trace evaluator wearing
program-logic vocabulary" — the substrate cannot cross a
data-dependent branch at a symbolic value; the missing middle
(per-construct symbolic rules, an assertion layer over locals, a
case-splitting executor) is what BRiCk/RefinedC are. Trust discipline
judged better than the donors'.

## 1. THE KILL LIST — RATIFIED [USER 2026-08-27] ("execute on the
kill list"). Normative item list: assessment doc §kill-list (file:line
per item). Summary: 26 registered concrete-input theorems (T6 pair,
T7 pair, all 22 R6 corpus theorems, ~7,900 lines incl. T7Walks); the
4 parked concrete reproducers (X3/Z1/C9/C9T; C9T's ∀-x restatement
and the `within` byte law SALVVAGED per the delta disposition); the
ambient 16-theorem slate + chase machinery (~10,000 lines; absorbs
the standing C5/R7 purge); ~240 per-fixture literal-address supply
entries; the whole-run concrete-anchor mint mode; spec-lab's 23
sample/concrete statement defs; THE EXEC-EQUATION CAMPAIGN IS
CANCELLED, NOT PARKED (the PROOF.md plan naming kernel-proved
concrete executions as "the binding constraint" is the forbidden
strategy). All gate touch points enumerated in the assessment; the
purge is build-fatal-if-partial by design.

EXECUTION NOTE [AGENT, operator-visible]: the kill executes NOW for
every example and every support item nothing KEEP-class needs. Kill
items whose deletion would today break KEEP-class artifacts (expected
sole case: the whole-run mint mode currently carrying the kept T1–T5
proofs) are killed-by-registration: marked for deletion, executed the
moment the B-plan replacement lands (the assessment's own B6
sequencing). Each deferral requires named evidence of the KEEP-class
breakage in the purge record.

## 2. THE CONVERSION TABLE — TO BE RATIFIED
Assessment §conversion: KEEP (Seg fuel algebra, FnSpec, heap RA,
mem-op WP rules, Kit block laws, LawRegistry, adequacy spine, T1–T5
statements, speclab codecs/models, trust gates); CONVERT (`restIs`
decomposition into control token + env points-to + supplies — L, the
load-bearing refactor; per-construct walk rules M–L; RoundEval
chassis → case-splitting symbolic stepper L); DELETE (iter_compose,
the spine template, twin-spelling user vocabulary). Awaiting operator
ratification; no conversion work executes before it.

## 3. THE BUILD PLAN (B0–B6) — TO BE RATIFIED
B0 judgment contract + mechanical concrete-input ban gate (S;
fail-closed, waiver-gated, negative-tested) → B1 assertion layer over
locals (L) → B2 per-construct symbolic rules incl. the PEif/PEcase
case-split rule — the heart (L) → B3 automation (M–L) → B4 calls
after are_compatible totalization (M) → B5 predicate invariants;
acceptance = the old corpus RESTATED ∀-input (M) → B6 purge remainder
(M). If ratified, B0–B6 supersedes the remaining arc-18 ladder
(R6c → B4 prerequisite; R7 ⊂ kill/B6; R8/R9 re-anchor after B5).
Awaiting operator ratification; no build work executes before it.
