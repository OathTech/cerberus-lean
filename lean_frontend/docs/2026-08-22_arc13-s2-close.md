# Arc-13 S2 — close-out record: corpus restoration, exploration yield, grandfather dissolution

Date: 2026-08-22. Worker: arc-13 S1/S2 (D1 GO). Companion records:
`2026-08-22_arc13-s0-scheme-decision.md` (the decision + probes),
`2026-08-22_arc13-s1-build.md` (the scheme + backstop + cascade +
economics). Quoted blocks verbatim; derived tallies labeled DERIVED.

## 1. The corpus re-baseline (S1 step 6) — the 516 FLOOR rows restored

Mechanics: 4 parallel shard writers (`test_csmith_corpus.sh
--write-baseline --shard K/4`, per-shard output via the new
`CSMITH_BASELINE` path override — the committed baseline untouched
until assembly), assembled in shard order (= the harness's
deterministic sorted order), new header documenting the movement.
Shard SUMMARY lines verbatim in §1.1; movement decomposition §1.2;
three-way spot-checks §1.3.

### 1.1 Shard summaries (verbatim)

Shard runs: `--write-baseline --shard K/4`, per-shard `CSMITH_BASELINE`
files, TIMEOUT_SECS=15, oracle @ batch-A commit `0ca08d695`. Verbatim:

```
shard1: SUMMARY: total=418 match=182 ub_match=0 ub_diff=0 mismatch=0 fail=0 crash=0 lean_error=0 timeout=3 cerb_skip=233 cerb_floor=0 cerb_inconsistent=0
shard2: SUMMARY: total=418 match=248 ub_match=0 ub_diff=0 mismatch=0 fail=0 crash=0 lean_error=0 timeout=3 cerb_skip=167 cerb_floor=0 cerb_inconsistent=0
shard3: SUMMARY: total=418 match=364 ub_match=0 ub_diff=0 mismatch=0 fail=0 crash=1 lean_error=0 timeout=0 cerb_skip=53 cerb_floor=0 cerb_inconsistent=0
shard4: SUMMARY: total=415 match=366 ub_match=0 ub_diff=0 mismatch=0 fail=0 crash=1 lean_error=0 timeout=2 cerb_skip=46 cerb_floor=0 cerb_inconsistent=0
```

DERIVED totals: 1669 rows; match=1160, mismatch=0, crash=2, timeout=8,
cerb_skip=499, **cerb_floor=0** (was 516). Fresh shard checks against
the assembled baseline (verbatim): `--shard 3/40`: `SUMMARY: total=42
match=20 … timeout=1 cerb_skip=21 cerb_floor=0` + `Baseline check: 0
regression(s), 0 improvement(s)`; `--shard 29/40`: `SUMMARY: total=42
match=38 … cerb_skip=4 cerb_floor=0` + `Baseline check: 0
regression(s), 0 improvement(s)`.

### 1.2 Movement decomposition (old = arc-12 floor baseline, new = this re-baseline)

Per-row diff of the committed baselines (derived; script over
old/new row sets — 1669 = 1669 files, zero file-set drift):

```
CERB_FLOOR -> CERB_SKIP: 391
CERB_FLOOR -> MATCH:     122
CERB_FLOOR -> TIMEOUT:     3
TIMEOUT    -> MATCH:       2     (sia_csmith_041, sia_csmith_139)
unchanged: 1151
```

- The 516-row floor class dissolves COMPLETELY into real verdicts:
  122 restored MATCHes (the recovered differential surface — incl.
  all 15 former F-D DIFF rows, sa_190, sia_976 and the 34-row
  coincidentally-correct class, now clean and upstream-agreeing per
  the §1.3 spot-checks), 391 oracle-side SKIPs (the standing non-F-D
  csmith classes — internal errors etc. — now honestly classified
  instead of floored), 3 TIMEOUTs (perf class at the 15s cap).
  mismatch=0: restoring 516 rows surfaced ZERO differential
  disagreements.
- The ONLY non-floor movement is the known jitter pair
  sia_csmith_041/139 TIMEOUT->MATCH, re-recorded THIS time under the
  arc-12 audit B-F5 bar: uncapped classification runs, 3x each —
  oracle walls 3.72/3.75/3.79s (041) and 4.50/4.57/4.60s (139),
  identical outputs across runs, verdicts Specified(226) over 984
  executions / Specified(128) over 5038 executions, Lean agreeing
  (the shard MATCH rows carry the full per-execution agreement).

### 1.3 Three-way spot-checks (fork-renumbered vs un-forked upstream b9aeedcb4)

Corpus-lane substitution applied; `--nolibc --exec --batch
--mode=exhaustive`; comparison = full output byte-equality modulo the
`Time spent` line (ALL executions, not just the first). Verbatim:

```
sa_csmith_168 fork_rc=0 FORK==UPSTREAM verdict=Defined {value: "Specified(28)", ...}
sa_csmith_190 fork_rc=0 FORK==UPSTREAM verdict=Defined {value: "Specified(123)", ...}
sa_csmith_19  fork_rc=0 FORK==UPSTREAM verdict=Defined {value: "Specified(159)", ...}
sia_csmith_081 fork_rc=0 FORK==UPSTREAM verdict=Defined {value: "Specified(74)", ...}
sia_csmith_136 fork_rc=0 FORK==UPSTREAM verdict=Defined {value: "Specified(149)", ...}
sia_csmith_976 fork_rc=0 FORK==UPSTREAM verdict=Defined {value: "Specified(201)", ...}
```

DERIVED: the sample covers the F-D witness class (sa_19/168 were DIFF
rows, sia_081/136 coincidentally-correct-class members probed
collision-positive at arc-12 S0), the perf-cap F-D row (sa_190 — old
TIMEOUT label, now COMPLETES with the upstream-agreed value), and the
latent F-D row (sia_976 — 97 live collisions at arc-12, now clean and
upstream-agreed). Plus the committed-witness battery in the S1 record
§5 (6000018/6000038/6000098/ub010 all landing on upstream).

## 2. Exploration yield RESTORED (S1 step 7)

See the S1 record §6 for the full evidence. Headline: the exact
arc-12 spot-batch block (B1 config, seeds 3030001-3030020) moves from
`match=2 cerb_skip=2 cerb_floor=16` to (verbatim)
`SUMMARY: total=20 match=10 ub_match=0 ub_diff=0 mismatch=0 fail=0
crash=0 lean_error=0 timeout=0 cerb_skip=10 cerb_floor=0
cerb_inconsistent=0` — differential yield 2/20 → 10/20 (5x),
floors 16 → 0, zero mismatches.

## 3. Grandfather dissolution — closure evidence index

- G1 (libc.co / libc.core): rebuilt + re-pinned; dump byte-identical
  to upstream's own; scanner 0 duplicates (was 214/58/106). Closure
  addendum: arc-12 S2+S3 record; arc-6 s1-libc-load record.
- G2 (metadata jsons): warn-mode deleted; lanes green (libc_exec 7/7).
- G3 (uri): flag deleted, invocations plain, 16/16 re-verified;
  memset-id baseline row re-recorded (118→1451).
- G4 (export residual): class vacuous (no collisions exist).
- The `--fresh-floor-grandfather` flag: REMOVED (main.ml); grep over
  the tree finds no invocation.
- Records carrying the asterisks updated by APPENDED closure addenda
  (never rewritten): `2026-08-21_arc12-s2-s3-record.md`,
  `2026-08-21_arc12-results.md`, `2026-08-21_arc12-s0-floor-design.md`
  (superseded note), `2026-08-21_arc12-s1-floor-record.md` (superseded
  in part), `2026-08-21_arc12-renumbering-case.md` (EXECUTED),
  `2026-08-19_arc6-s1-libc-load.md` (exposure gone),
  `tests/csmith_findings/README.md` (arc-13 status update).

## 4. Register movements (this arc)

| entry | movement |
|---|---|
| F-D corruption family | **CLOSED-BY-CONSTRUCTION** (single supply; the arc-12 refusal class GONE; witnesses land on upstream byte-level) |
| Renumbering TEMPORAL entry | **RETIRED** (the mover was this arc) |
| G1-G4 grandfathered artifacts | **DISSOLVED** (clean re-derivations, scanner-verified, libc.core == upstream's dump) |
| Coincidentally-correct class (34 rows) | **EMPTY** (no collisions exist; rows restored — §1.2) |
| sia_csmith_976 (latent F-D) | restored + upstream-agreed (§1.3) |
| O1 (run-supply non-escape, narrowed) | **CLOSED on OCaml** (run symbols are ambient draws); remains a Lean-side-only note (unchanged there) |
| S3 SeqRMW draw-order divergence | still order-only vs upstream; under a single counter it cannot create equal ids — recorded caveat for atomics-trace three-ways ONLY |
| WalkBench instrument | NEW finding: fails (`unknown tactic` at timedp sites) in the merged tree; not an arc-13 surface; mover: workbench maintenance at the T5 resumption (S1 record §3.2) |
| Perf-gap movers (5 surviving TIMEOUT rows) | re-measured by the re-baseline (§1.2 shows the TIMEOUT class's new shape) |
| notes/upstream/07 (symbol-identity fragility) | fork side RESOLVED by re-convergence; the filing's upstream ask unchanged — update parked for the operator (notes/ lives outside this worktree's writable surface) |

## 5. The floor's acceptance property (charter success condition 1)

The backstop fired ZERO times across every lane and sweep in this
arc (`cerb_floor=0` on minimal/ci/coverage/debug/float/corpus/
exploration; uri/chvalid/libc flag-free and green), and fires loudly
on the plant (re-threaded supply sim → `window-nodraw`/`window-low`,
exit 70 — S1 record §1.2). Collisions are impossible by construction:
one supply, every (digest, num) a distinct draw of it.
