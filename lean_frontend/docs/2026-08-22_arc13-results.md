# Arc 13 results — the renumbering ("clean numbering": symbol-id rebase + fixture re-pin)

Date: 2026-08-22. Branch `arc/renumbering` (cerberus-lean only; ZERO
lem-lean changes — lem-lean status clean, opam pin + Lake manifest
untouched). Mode: long-cycle autonomous under the orchestrator/worker
doctrine. Slice records: `2026-08-22_arc13-s0-scheme-decision.md` (the
scheme probes + decision), `2026-08-22_arc13-s1-build.md` (the scheme,
backstop, cascade, economics), `2026-08-22_arc13-s2-close.md` (corpus
restoration, yield, dissolution), decision log D1-D2. Quoted blocks
verbatim; derived tallies labeled.

## Headline

**F-D is CLOSED-BY-CONSTRUCTION and the fork oracle's numbering is
upstream's.** Arc-13 D1 (scheme R-B, full form) removed the fork's
split symbol-id streams: desugar and run-time minting are back on the
single ambient `Cerb_fresh.int` on the OCaml target — collisions are
impossible because there is nothing to collide with — and the oracle's
output is **byte-identical to the un-forked upstream oracle**
(S0 probes 11/11 fixtures; the re-pinned `tests/libc/libc.core` ==
upstream's own libc.co dump; D2's independent spot re-confirmed
literal identity). The Lean target is UNTOUCHED (threaded supplies +
2^20 base stay; generated-Lean diff EMPTY, verified by regeneration at
every stage incl. the final head). The arc-12 margin-refusal class is
GONE: the 516 corpus `CERB_FLOOR` rows restored to real verdicts with
**mismatch=0**, exploration yield 5x on the identical seed block, the
grandfather register G1-G4 DISSOLVED, and the floor survives as a
single-supply window backstop that fired zero times on healthy inputs
and loudly on the plant.

## The scheme (S0 decision + S1 step 1)

Three ocaml-only `declare ocaml target_rep` seams
(`cabs_to_ail_effect.lem fresh_sym_int`, `core_run.lem fresh_symbol'`,
`core_run_aux.lem initial_core_run_state`) + the 13-line-of-code shim
`ocaml_frontend/fork_renumber.ml` (upstream-scheme citations; the one
deliberate divergence — `sym_supply = 0`, dead on OCaml — documented).
Zero .lem body changes. Alternatives probed and rejected on evidence
(S0 doc §5): R-A1 (ambient base 2^20, the Lean mirror) = the same full
re-pin without the comparability payoff; R-A2 (desugar at 2^20,
nearly re-pin-free) = pp id-leakage (tag suffixes, glob order) + a
run-supply growth hazard whose fix converges on R-B's surface.
Commit `8923d6436`'s "OCaml path unchanged" claim is finally true;
drift-review finding O6 retires.

## Byte-identity (the three-way instrument's maximal form)

- S0 probes: fork == upstream, `cmp`-level, 11/11 — tests/verify
  t1-t5, decl463/464 margin synthetics, csmith_6000098 pp AND exec,
  uri.c pp (9.86 MB), stdio.c pp (1.1 MB).
- Rebuilt libc.co dump (89,609 lines) == upstream's own.
- Witnesses (exec, all executions): 6000018 `Specified(100)`, 6000038
  `Specified(13)`, 6000098 `Specified(117)`, ub010 honest timeout —
  each == upstream; the F-D corruptions gone.
- Corpus three-way spots 6/6 byte-level (sa_19/168/190,
  sia_081/136/976). [CORRECTED at the S3 audit, B-F3: the sample
  covers the ex-DIFF class (sa_19/168 AND sia_081/136 — all four are
  former F-D DIFF rows), the perf-cap row (sa_190) and the latent
  row (sia_976); the 34-row coincidentally-correct (ex-MATCH) class
  was NOT in this sample — its byte-identity evidence is auditor B's
  own spot-runs on sa_119/123 (both class members, fork == upstream
  byte-level), banked at the audit.]
- Consequence for future findings: fork-vs-upstream triage is a
  byte-diff; the F-A/F-B filings (notes/upstream/08+09) and the 07
  fragility filing gain the re-converged fork as their strongest
  demonstration (07 addendum: orchestrator, container surface).

## The cascade as executed (S1 step 2; every pin self-checking)

| artifact | movement |
|---|---|
| tests/verify/t{1..5}.core | 213 lines; each == upstream output |
| tests/libc/libc.core (+ .co.version) | 42,330/42,303 lines of 89,609; == upstream's dump; rebuilt via `dune build cerberus.install` (the build the arc-12 floor refused) [audit fix B-F5: the .co.version version-string pin (unreproducible '-dirty' describe) is REPLACED by tests/libc/libc.core.sha256 — a content-hash pin of the dump text; version header now informational-only in libc_prep] |
| libc metadata cabs-jsons | id-free, regenerate on demand — no pin movement (consuming lanes green) |
| tests/libxml2/uri_baseline.txt | ONE row (pinned error message's memset id 118→1451) |
| relsem T1Core/SlateCore | re-emitted (4+16 term lines); emit drift gates byte-identical in-build |
| 8 hand proof modules | 1,510 SCRIPTED token replacements (name strings, name-hash literals, def identifiers, comments); zero manual proof edits. [Counting method, labeled at the S3 audit (A-F3): 1,510 = the sum of the rewrite script's own per-module replacement counts (S1 record §3.1); auditor A's independent regex count over the same diff = 1,592 — different token delimitation; both are derived tallies of the same scripted rewrite] |
| tests/fixtures/*/core.txt (10 golden Core dumps) | [ADDED to this inventory at the S3 audit, A-F3] ZERO movement, verified current at the audit-fix batch: all 10 regenerate byte-identical (`cmp`) under the renumbered oracle — Phase-1 goldens predate the April fork threading, so re-convergence restored their original numbering |
| scripts/fork_drift_manifest.txt | 6 semantic hash re-pins, driver.ml cosmetic→semantic, fork_renumber.ml manifested, `renumber=arc13`. [CORRECTED at the S3 audit, B-F4 → decision-log C1: D1's "the deltas shrink toward upstream" phrasing was wrong — what went to ZERO is the OUTPUT/numbering divergence (byte-identity, triple-verified); the seam-code manifest deltas did NOT shrink. Quantified (derived, re-measured at the audit fix, changed-line counts vs upstream): cabs_to_ail 24→24, cabs_to_ail_effect 85→85, core_reduction 30→30, core_run 17→17 unchanged; core_run_aux 16→20 and driver 1→3 GREW — all six justified call-site rewrites to Fork_renumber.*] |
| scripts/exec_csmith_corpus_baseline.txt | the 516-row restoration (below) |
| scripts/canonicalize_ids.py | underscore-name canonicalization (+self-test): cross-side comparisons stay id-insensitive now that oracle/Lean id streams differ by design (a REAL renumbering-caused elab row was found and neutralized; lane back to the standing 103/106) |
| expectations.txt / verdict baselines (minimal, ci, coverage, debug, float, bytes, multi_tu, libc, chvalid) | ZERO movement — id-insensitive, verified by full reruns |

Re-pin completeness first-pass sweep (the S3 audit re-runs it
independently): tree-wide grep for old-first-window tokens
(`ret_483`, `a_484..a_499`, `ret_49x/50x`) over tests/, scripts/,
relsem/, relsemcore/, test/ — sole hit: an inline synthetic Core
snippet in `scripts/extract_core_sig.py`'s self-test (test DATA, not
a pinned oracle artifact; number-insensitive by construction).
`tests/csmith_findings/` retains old-oracle verdict QUOTES as
historical evidence by design (README arc-13 status update marks
them).

## THE ECONOMICS (S1 step 3 — the workbench prediction's first real-world test)

**Full proof-package re-elaboration after total fixture renumbering:
30.5 s wall / 2.0 GB peak (363 jobs), green on the first attempt,
ZERO manual proof edits** — the entire hand cost was a scripted
positional name-map rewrite (1,510 token replacements). Per-module:
T1AppEq 13 s (the dnms_chain full walks), T5Prefix 8.3 s, T4AppEq
4.6 s, T4Defs 3.4 s, everything else ≤ 2.6 s. The T5-family pins
(T5Fixture/T5Prefix/T5Iter incl. `entry5_walk`) re-elaborated in the
same build.

**The replay refinement, stated plainly:** record→replay was NOT
exercised — there were no stored traces to refuse (T1-T4 proofs use
plain `app_walk`; T5 is parked pre-landing), and at T1-T4 scale the
full re-walk is so cheap that replay could not have paid for itself.
The workbench's 15x replay-vs-walk thesis (B2 39,985 ms vs B3
2,593 ms) is REFINED, not refuted: it becomes load-bearing at
T5-walk scale (~75 symbolic rounds), where the arc-11 D3
renumbering-compatibility argument (fingerprint refusal → mechanical
re-record) remains the plan of record — now with the measured datum
that the fixture side of a re-pin is mechanically scriptable.

FINDING (registered): the WalkBench instrument
(`relsem/bench/WalkBench.lean`, Tier-C on-demand) fails in the merged
tree (`unknown tactic` at the three timedp-wrapped walk sites, 0 ms
BENCH lines vs arc-11's 2350/39985/2593 ms). Not an arc-13 surface
(zero tactic/Kit diffs this arc; T1AppEq's own `app_walk` elaborates
green in-build). Mover: workbench maintenance at the T5 resumption.
[CLOSED at the S3 audit fix, A-F2 — root cause + repair: 280
orphaned pre-package-split RelSem build artifacts (32 oleans incl. a
pre-arc-11 `Tactics/AppWalk.olean` with no `timedp`) were stranded
in the ROOT package's `.lake` tree; Lean resolves an import's ROOT
module prefix against the FIRST search-path entry carrying a
`RelSem/` dir, and the root lib precedes the relsem package's own in
`lake env`'s LEAN_PATH — so raw probes silently loaded the stale
tree. Orphan status verified (root package's only current RelSem.*
modules are the five RelSemCore roots; no root source imports the
rest), artifacts deleted, and WalkBench is GREEN again via
`lake lean bench/WalkBench.lean` from `relsem/` — verbatim:
`BENCH B1 T1-dnms discovery (preview to semantic stop): 2238ms` /
`BENCH B2 entry full walk + closure (recorded): 38358ms` /
`BENCH B3 entry checked replay + closure: 2347ms` — the arc-11 shape
(≈16x replay-vs-walk) reproduced. Probe-recipe consequence recorded
in lean_frontend/CLAUDE.md: RelSem probes use `lake lean` FROM
`relsem/` (raw `lake env lean` cannot resolve the two-package
RelSem split for ad-hoc files and now fails LOUD, which is the
correct failure mode); after structural moves, stale root artifacts
must be cleaned. Register entry CLOSED; no T5-resumption work item
remains for this instrument.]

## The backstop (S1 step 4) + acceptance property

`util/cerb_fresh.ml check_ail_window` + the digest-filtered (min,max)
fold (`ail_sym_hwm.ml sym_window_program`, audited walk body
unchanged): every current-digest Ail symbol must lie in
`[tu_first .. last_issued]`; below-window = a re-threaded 0-based
supply (the F-D-era scheme), above-window = ids from nowhere, forward
check = counter re-init. Same token/exit-70/CERB_FLOOR surface.
PLANT-TESTED (re-pointing `fresh_sym_int` at a private 0-based counter
→ `window-nodraw` on decl463, `window-low` on a const-expr TU, both
exit 70; restored build silent). [SCOPE narrowed at the S3 audit,
A-F1: this is a check of the DESUGAR seam only (both directions);
auditor A's run-seam plant (re-pointing `fresh_symbol'`) does NOT
fire it — run-minted symbols pass no hand chokepoint outside the
shims. The run seams' standing defense is the fork-drift gate's
layer-2 hash pins: any source-level re-thread changes the generated
OCaml call sites and fails `check_fork_drift.sh` (documented in the
shim/backstop headers + the drift manifest notes). A cheap dynamic
run-init window extension was assessed and PARKED — see the audit
section below.] **Fired ZERO times on every lane,
sweep and batch in this arc** (`cerb_floor=0` everywhere) — the
charter's acceptance property holds; any future CERB_FLOOR row is a
finding by definition.

## Grandfather dissolution (S1 step 5) — G1-G4 CLOSED

`--fresh-floor-grandfather` + both warn-only modes DELETED (main.ml,
cerb_fresh.ml); uri-lane invocations plain. G1: libc rebuilt
collision-free, dump == upstream, scanner 0 duplicates on
stdio/stdlib/internal/vfscanf (was 214/58/106/supply-only). G2: warn
mode gone, lanes green. G3: uri elaborates with 0 duplicates (was
252), 16/16 re-verified un-grandfathered. G4: vacuous (no collisions
exist). Scanner faithful-reimplementation plant-tested (synthetic
duplicate detected). Closure addenda APPENDED (never rewritten) to:
arc-12 s2-s3 record, arc-12 results, arc-12 s0-floor-design
(superseded), arc-12 s1-floor-record (superseded in part), arc-12
renumbering-case (EXECUTED), arc-6 s1-libc-load, findings README.

## Corpus restoration + exploration yield (S1 steps 6-7)

Full 1669-file re-baseline (4 parallel shard writers via the new
documented `CSMITH_BASELINE` override; assembled; fresh shard checks
0 regressions / 0 improvements). Aggregate (DERIVED from the four
verbatim shard SUMMARYs in the baseline header) [old-side values
CORRECTED at the S3 audit, B-F2 — re-derived from the committed
arc-12 baseline rows]: match 1036 → 1160, cerb_skip 108 → 499,
timeout 7 → 8 (5 surviving old + 3 newly-restored = 8; the jitter
pair left), crash 2 → 2, **cerb_floor 516 → 0, mismatch = 0**.
Per-row movement:

```
CERB_FLOOR -> CERB_SKIP: 391    CERB_FLOOR -> MATCH: 122
CERB_FLOOR -> TIMEOUT:     3    TIMEOUT    -> MATCH:   2   (unchanged: 1151)
```

Restoring 516 refused rows surfaced ZERO differential disagreements.
[This paragraph CORRECTED at the S3 audit, B-F1.] The recovered
differential surface: +122 MATCH rows = 15 ex-DIFF (all the former
F-D DIFF rows) + 34 ex-MATCH (the coincidentally-correct class, now
EMPTY as a class) + 73 ex-SKIP (arc-10 baseline classes; breakdown
re-derived from the committed baselines at the audit fix). sa_190
and sia_976 are NOT MATCH rows: sa_190 restored to CERB_SKIP (times
out under the 15 s cap; COMPLETES uncapped with the upstream-agreed
value — the §1.3 uncapped three-way spot), sia_976 (latent F-D)
restored to TIMEOUT (clean and upstream-agreed at its uncapped
spot). The only non-floor movement is the known jitter pair
sia_041/139 TIMEOUT→MATCH, re-recorded under the arc-12 audit B-F5
bar (3x uncapped runs each: 3.7-4.6 s stable walls, identical
outputs, Lean agreeing).

Exploration (the identical arc-12 spot-batch block, B1 config, seeds
3030001-3030020): `match=2 cerb_skip=2 cerb_floor=16` →
`match=10 cerb_skip=10 cerb_floor=0 mismatch=0` — **differential
yield 5x**, refusals gone.

## Register movements (this arc)

| entry | movement |
|---|---|
| F-D corruption family | **CLOSED-BY-CONSTRUCTION** (single supply; witnesses land on upstream byte-level; the floor's refusal class gone) |
| Renumbering TEMPORAL entry | **RETIRED** (this arc was the mover; forward-design constraint discharged — nothing baked in the old numbering) |
| The floor | now the single-supply window BACKSTOP (permanent; acceptance = never fires; plant-tested) |
| G1-G4 | **DISSOLVED** (closure addenda; flag code removed) |
| Coincidentally-correct class (34 rows) | EMPTY; rows restored |
| sia_csmith_976 (latent F-D) | restored, upstream-agreed |
| O1 run-supply non-escape (narrowed) | **CLOSED on OCaml** (run syms are ambient draws); Lean-side-only note remains |
| S3 SeqRMW draw-order | order-only vs upstream; under one counter it cannot create equal ids; caveat recorded for atomics-trace three-ways only |
| WalkBench instrument | NEW, then **CLOSED at the S3 audit fix (A-F2)**: root cause = stale pre-split RelSem oleans in the root package's `.lake` shadowing probe imports; cleaned, green re-run recorded (economics section addendum); probe recipe fixed in lean_frontend/CLAUDE.md |
| canonicalize_ids underscore extension | NEW (recorded): cross-side id streams differ by design post-renumbering; id-insensitive comparison is the contract (elab lane at standing 103/106) |
| Perf-gap movers (old TIMEOUT rows) | [CORRECTED at the S3 audit, B-F2] the arc-12 baseline had 7 TIMEOUT rows; 2 (the jitter pair) re-recorded MATCH under the arc-12 B-F5 bar; **5 remain TIMEOUT + 3 new TIMEOUTs** from the restored floor class = the 8 current rows (the perf lane's honest state) |
| notes/upstream/07 | fork side RESOLVED by re-convergence; addendum appended by the orchestrator (D2) |

## Decisions

- **D1 [AGENT]**: scheme = R-B full form, on S0 probe evidence
  (11/11 byte-identity, EMPTY generated-Lean diff, libc-scale
  convergence); R-A1/R-A2 rejected with recorded reasons; no operator
  escalation needed — the evidence removed the trade.
- **D2 [AGENT]**: S1/S2 boundary verified (independent byte-identity
  spot; economics finding accepted with the replay refinement
  recorded); S3 GO.

## Surfaces + gates at close (for the audit)

Whole-arc diffstat (`mdd/cerberus-lean..HEAD`): **51 files, +45,529
/ −43,627** [counts CORRECTED at the S3 audit, B-F6/F8: the earlier
"45 files, +44,840/−43,610" was measured before the S3 docs commit;
this figure is measured at the audit-fix final head and includes the
audit-fix batches — file-class prose below unchanged except as
labeled] — oracle OCaml (`util/cerb_fresh.ml`,
`backend/common/{ail_sym_hwm,pipeline}.ml`, `backend/driver/main.ml`,
`ocaml_frontend/fork_renumber.ml` NEW), 3 `.lem` files
(declares+comments ONLY — zero body changes; generated-Lean EMPTY),
10 relsem `.lean` (2 re-emitted + 8 script-renumbered), 5 scripts
(2 harness comment/classifier touches, corpus-baseline override,
canonicalizer, drift manifest), 8 pinned-artifact files, findings
README, lean_frontend/CLAUDE.md, 7 arc-12/arc-6 records (APPENDED
addenda), 6 arc-13 docs. ZERO: lem-lean (status clean; pin 11d4b4c
unmoved), relsem Tactics/Kit, lakefiles/manifests, test_unit.sh,
check_proof_size.sh, native/, memory model. The workbench-v2 worktree
untouched. Gates at the final head: test_unit (both Lake packages,
9 exes, in-build audits) PASS; test_verify 29/29; core/parse ALL;
every exec lane at baseline with `cerb_floor=0`; uri 16/16 plain;
chvalid 4/4; drift gate OK; generated-Lean diff EMPTY (regenerated);
corpus fresh-shard checks green. Full verbatim tails: the S1/S2
records + the batch commit messages (`0ca08d695`, `59781c611`).

## The S3 audit (2-agent adversarial) + dispositions (audit-fix batches, 2026-08-22)

Audited head: `dc09c702d`. Two independent auditors (A, B) ran the
charter's mandatory scopes; every finding got an explicit disposition
from the orchestrator and was executed in two audit-fix commits atop
`dc09c702d` (batch a: code/scripts; batch b: these records). The
in-place corrections above are all bracket-LABELED; nothing was
silently rewritten.

- **A-F1 (backstop coverage overclaim) — FIXED by honest narrowing;
  extension PARKED.** The shim/backstop headers claimed re-threading
  "any of these seams" fails loud; only the DESUGAR seam is
  dynamically checked (A's run-seam plant — re-pointing
  `fresh_symbol'` at a private counter — does not fire it).
  Claims narrowed in `fork_renumber.ml`, `cerb_fresh.ml`, this doc;
  the actual run-seam defense (fork-drift layer-2 hash pins on the
  generated call sites; the hand shim itself manifest-listed and
  review-bound) documented in both headers + the drift-manifest
  notes. The proposed run-init window extension was ASSESSED and
  PARKED (park-don't-improvise): run-minted symbols pass no
  hand-written chokepoint outside the shims — an in-shim assertion
  is deleted by the very re-thread it should catch (the desugar
  backstop works precisely because it lives in the pipeline hook,
  outside the seam), and a real downstream observation point
  requires .lem/generated-surface changes, i.e. exactly the surface
  layer 2 already pins. A's plant scenario stands documented as
  drift-gate-defended.
- **A-F2 (stale-olean shadowing) — FIXED; WalkBench register entry
  CLOSED.** 280 orphaned pre-split RelSem artifacts (32 oleans; A's
  count of 28 was the flat-directory olean census, which includes
  the 5 live RelSemCore modules and misses the 9 Kit/Tactics
  subdirectory oleans) deleted from the root package's `.lake` after
  per-module orphan verification; WalkBench green re-run recorded in
  the economics section addendum; probe recipe + cleanup warning
  added to lean_frontend/CLAUDE.md.
- **A-F3 + B-F6/F8 (inventory + prose) — RECORDED.** The 10
  `tests/fixtures/*/core.txt` goldens added to the re-pin inventory
  with a verified-current note (all 10 `cmp`-identical under the
  renumbered oracle); whole-arc diffstat corrected at the final
  head; the corpus baseline's 391-SKIP class description reordered
  (exhaustive-mode timeouts first, per B's row sample); the 1,510
  replacement figure method-labeled (A's regex count 1,592 noted).
- **B-F1/F2/F3 (restored-row prose) — FIXED as labeled corrections.**
  sa_190/sia_976 removed from the restored-MATCH enumerations
  (CERB_SKIP resp. TIMEOUT, accurately stated); the 122 = 15
  ex-DIFF + 34 ex-MATCH + 73 ex-SKIP breakdown added (re-derived
  from the committed baselines); TIMEOUT accounting corrected
  (arc-12 baseline had 7 rows; 8 = 5 surviving old + 3 new) here
  and in the merge checklist; the §1.3 three-way sample relabeled
  (sia_081/136 are ex-DIFF rows) with B's own sa_119/123
  byte-identity spot-runs cited as the 34-class evidence.
- **B-F4 (comparability claim) — FIXED + decision-log correction
  C1.** D1's "manifest deltas shrink" was false: the OUTPUT
  divergence went to zero; the seam-code manifest deltas did not
  shrink (4 unchanged, 2 grew — quantified in the cascade table
  note, re-measured at the audit fix). D1's text untouched; C1
  appended to the decision log; the S0 doc §6 carries the labeled
  correction at the propagation site.
- **B-F5 (libc.co pin mechanism) — FIXED, the operational one.**
  The version-string pin (`tests/libc/libc.co.version`, an
  unreproducible '-dirty' describe) replaced by a CONTENT-HASH pin
  (`tests/libc/libc.core.sha256`, sha256 of the dump text —
  stronger and rebuild-independent); `libc_prep.sh` verifies the
  committed pin AND the freshly regenerated dump against it, and
  logs the libc.co version header informationally. Re-pinned at the
  current upstream-byte-identical dump
  (`bb0560d94f6383cb8057b8c810f6253ff6cd451c10b29a9e0fcd105c3de62197`).
  Plant-tested both directions (verbatim head of the pin-corruption
  plant): `libc_prep: ERROR: pinned libc.core does not match its
  content-hash pin` (rc=1); the consistent-but-drifted pin pair
  plant refuses at the regen check (`regenerated libc.co dump does
  not match the content-hash pin`, pinned vs rebuilt sha256 lines
  printed); restored check green. The merge checklist's post-merge
  remedy is now the working form: rebuild → `libc_prep.sh --check`
  re-derives → content-hash passes with NO re-pin.
- **B-F7 (audit-plant hygiene) — DOCTRINE, recorded here + checklist
  lessons.** Plant recipes MANDATE a rebuild-after-revert before any
  further measurement (a reverted source with a stale binary is a
  doctored instrument); audit pairs sharing one worktree SEQUENCE
  binary-affecting plants — never interleave them. (Promotion to
  container doctrine is the orchestrator's merge-time action.)
- **[USER] env-trap tweaks — DONE (batch a).** `scripts/capped` is
  self-sourcing (walks up from its real location to the container
  `scripts/env.sh`, idempotent, loud one-line no-op if absent;
  fresh `env -i`-style shell running `scripts/capped lake --version`
  verified rc=0); `scripts/common.sh` gains the fail-fast guard
  (missing lem or GIT_CONFIG_GLOBAL → exit 2, `env not loaded: run
  via scripts/ce or source scripts/env.sh`; plant-tested: bare
  shell rc=2, sourced shell silent with empty stderr).

Gate at the audit-fix final head (batch a tree; batch b is
records-only): test_unit rc=0 (both packages, all script gates incl.
fork-drift OK), test_verify 29/29, exec-minimal SUMMARY at baseline
with `cerb_floor=0`, test_libc_exec 7/7 against the content-hash
pin, `libc_prep.sh --check` OK.
