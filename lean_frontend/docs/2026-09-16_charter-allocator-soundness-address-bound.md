# Charter — allocator soundness (tray 44) + the address-space bound as a quantified parameter + the fork-only OCaml flag (2026-09-16)

**Status:** CHARTER for one Claude Fable subagent worker, written by the orchestrator [AGENT]; branch `arc/allocator-soundness-address-bound` in the primed worktree `worktrees/cerberus-lean-arc/allocator-soundness-address-bound`. SEQUENCING [USER]: this slice runs AFTER the pristine-oracle instrument (WP-O, `arc/pristine-oracle-instrument`) lands; the orchestrator rebases this charter commit onto that mainline before launch, and the worker starts from the rebased head (so WP-O's widened row 10, register schema and doctrine are in the tree). The worker commits its own work — three commits, in the order of §2 — on green only; the orchestrator independently re-verifies every gate at every boundary; the committed record ENDS the slice.

## 0. Rulings and standing constraints (verbatim; not the worker's to revisit)

- [USER 2026-09-16], on the finding (upstream-tray draft 44): *"Yes, we should file it, and I think this is in the 'unambiguously wrong' category where we are allowed to fix ahead of upstream."*
- [USER 2026-09-16], on the bound: *"Btw, I thought we had moved away from concrete bounds? The semantics should be quantified over such bounds so we could (in principle) be running on a tiny machine with a tiny amount of memory to allocate, and the reasoning has to quantify over the bound."*
- [USER 2026-09-16], on this slice and the flag: *"Agree, let's do this as the slice you propose. And I think an ocaml flag is a good idea."* (The proposal: one slice, commit 1 = remedy 1 in both OCaml models and the Lean mirror with a unit test, drift rows and a VALIDATION §3 entry; commit 2 = the address-space bound as a parameter, matched mode at upstream's value, the no-numerals gate extended; plus a fork-only OCaml flag setting the initial cursor so both engines can run at a tiny bound.)
- [USER 2026-09-03], the no-magic-values rule, verbatim (`lean_frontend/DESIGN.md` §4): *"any instance of a value that can be quantified over by a context / theorem is fine. Defaults that are chosen eg. in test suites are fine. Any and all magic values that are hardcoded and can't be quantified over are definitionally bugs (unless they mirror lem or ISO-C design choices)"*; the aim: *"to forbid values that limit the semantics or limit the ways the customer can reason about the semantics"*. The address-space top mirrored upstream's OCaml and was sheltered by the parenthesis; the 2026-09-16 ruling above removes that shelter for this bound: it becomes a quantified parameter whose matched-mode instance is upstream's value.
- [USER 2026-09-11] execution mode: *"in this case you should launch them as claude fable class subagents. Can you do that? We want to keep in control of this bit of the work"*.
- [USER 2026-09-08], verbatim: *"Is the work on the enumerative methods actually valuable? These methods are out of policy and work on them has zero value to the project. The alternative is (1) we delete them, (2) we do this properly."* Then: *"I sort of don't care how we sequence this. It's fine if stuff lands if it'll be deleted per policy. The main point is we should \*NOT\* be building anything new out-of-policy. So if it's easiest, let's just land it, then make a plan to delete all the out-of-policy stuff the referee identified for elimination"*. Applied here: the allocator theorem (§2 C1) is a GENERAL statement over all states, proved from the arithmetic — not an enumeration over literals; the four-state check is a runtime TEST (an exit code), not a proof; no new artefact-class surface.
- The four aims [USER 2026-09-03] (`VALIDATION.md` §0:34-40) and the mirror-OCaml doctrine (container `CLAUDE.md`): every Lean line of the seam cites its OCaml line or documents its divergence in-code.
- Standing constraints: NEVER modify machine-global state (no opam switch changes — the worktree's `_opam` is a symlink to the SHARED switch; no `~/.gitconfig`, profiles, caches); NO `git push`, ever; never commit on `mdd/cerberus-lean` or `master`; no merge/rebase by the worker; fail-closed and fail-noisy (no `2>/dev/null` on build/install steps; no absorbing defaults); [AGENT]/[USER] provenance on every decision; quoted outputs verbatim, derived tallies labelled; every `lake`/`lean` through `scripts/capped` (`CERB_MEM_MAX` to lower it); ONE heavy job at a time; grind tripwire — a step approaching ~45 min is STOP-AND-REPORT, nothing over ~1 hr without written justification in advance; heartbeat/maxRecDepth bumps are defects; no `native_decide`/`bv_decide`/`ofReduce*`; status pings get an immediate interim reply; never end your turn on a wait (poll foreground, ≤10-minute steps).

## 1. Facts (verified by the orchestrator 2026-09-16 at mainline `0c78635cb`; cite, do not re-derive; errata go in the record)

**The defect (tray draft 44, `lean_frontend/docs/upstream-tray/44-…md`, with the verbatim reproductions).** `memory/concrete/impl_mem.ml:9` `let quomod = ediv_rem` (Euclidean); `:1247-1262` `allocator`, rounding line `:1253` `let z' = sub z (if q < zero then neg m else m)` — a truncating-division idiom: when `z = last_address − sz < 0` it ADDS `m`, and for `−align/2 < z < 0` the allocation succeeds at an address in `(0, align)`, overlapping the live object at `last_address` and misaligned. Twin: `memory/vip/impl_mem.ml:8` and `:202-211` (identical text). `allocator_with_address` (`concrete:1267`) does no rounding — unaffected. `memory/cheri-coq` and `memory/symbolic` have no such allocator. Both files are byte-identical to pristine `b9aeedcb4` at these sites. Reproduced on both engines: Zarith `ediv_rem (-1) 4 = (-1, 3)`; cursor 3, request (4, 4) → `active, address 2`; cursor 7, (8, 8) → `active, address 6`; cursor 2, (4, 4) → killed; cursor 8, (4, 4) → active at 4 (the normal regime). Lean mirror: `lean_frontend/CerbMem.lean:2093-2107` (`Int` `/`/`%` are Euclidean on 4.32.2; the mirror is FAITHFUL). The Z2 audit read this site for fidelity only (`docs/2026-09-04_zero-discrepancy-Z2-audit-premerge.md:195-199`).

**Remedy 1 (draft 44; the fix this slice takes), in both models:** fail when the cursor is below the request BEFORE rounding — `if z < zero then fail (MerrOther "Concrete.allocator: failed (out of memory)")`, then `z' = sub z m` (Euclidean `m`, a plain align-down), keeping the existing `z' <= zero` kill; the `q < zero` branch is dead and is deleted. The failure text is unchanged (VIP uses the same string). Consequence to state and prove: for `align > 0`, `sz ≥ 0`, an ACTIVE result `a` satisfies `align ∣ a`, `0 < a`, and `a + sz ≤ last_address` (the new object is aligned and disjoint from everything at or above the cursor).

**The bound today.** `concrete/impl_mem.ml:503-516` `initial_mem_state` (a record VALUE) with `:508` `last_address= Z.of_int 0xFFFFFFFFFFFF; (* TODO: this is a random impl-def choice *)`; `vip/impl_mem.ml:170` likewise; `symbolic/impl_mem.ml:569` and `cheri-coq/impl_mem.ml:271` define their own `initial_mem_state` (no such cursor). The virtual module's interface: `ocaml_frontend/memory_model.ml:39` `val initial_mem_state: mem_state` (`ocaml_frontend/impl_mem.mli` = `include Memory_model.Memory`; `ocaml_frontend/dune:9` `(virtual_modules impl_mem)`; `memory/dune:1` `(dirs cheri-coq concrete symbolic vip)` — every implementation must compile). Lem: `frontend/model/mem.lem:41-45` `val initial_mem_state: mem_state` + the two `target_rep`s (`Impl_mem.initial_mem_state`, `CerbMem.initialMemState`). Its ONE consumer in the model: `frontend/model/driver.lem:1520-1543` — `initial_driver_state_with run_st file fs_state` (`:1526` `layout_state= Mem.initial_mem_state;`), wrapped by `initial_driver_state file fs_state` (`:1535`) and `initial_driver_state_given sup file fs_state` (`:1540`, the const-expr mini-run's explicit-supply variant); `drive` (`:1730`) does not build the state. Callers: OCaml `backend/common/driver_ocaml.ml:157` and `:198` (`Driver.initial_driver_state file fs_state`, inside `batch_drive` and the non-batch driver; `driver_conf` record `:11-16` / `.mli:3-8` = `exec_mode`, `concurrency`, `fs_dump`, `trace`); Lean `lean_frontend/Main.lean:1019` `let (drSt, _supplyFinal) := initial_driver_state supply runFile fsState` (the supply-lifted entry shape). Generated: `lean_frontend/generated/Driver.lean:489` `layout_state := CerbMem.initialMemState`. Lean seam: `CerbMem.lean:153` `lastAddress : Address := 0xFFFFFFFFFFFF` (a structure-field DEFAULT — the literal), `:1780` `def initialMemState : MemState := {}`; the only other provider-side constructor: `test/Unit/MonadicFailstop.lean:24-25` `{ initialMemState with allocations := … }`. The defacto model (`defacto_memory.lem:69` `impl_initial_mem_state`) has no cursor and is out of scope.

**The precedents to mirror.** Fuel: `Main.lean:1150` `def defaultFuel : Nat := 100000000  -- FUEL-DEFAULT (the one allowed fuel numeral)`, `:1251` `| "--fuel" :: v :: rest => …`, `:1271`/`:1279` (absent → default; non-numeral → refusal text naming the default); the gate `scripts/check_no_fuel_numerals.sh` (allowlist `ALLOW_MAIN` at `:69-70`: exact whitespace-trimmed Main.lean lines; F1–F6 plant-tested by `--selftest`). Fork-only OCaml flag: `backend/driver/main.ml:521-524` `let batch_alloc_census = … Arg.(value & flag & info["batch-alloc-census"] ~doc)`, wired at `:99`/`:219`/`:566`; its consumer `driver_ocaml.ml:188-189`. Unit tests: `lean_frontend/lakefile.toml:248-252` (`[[lean_exe]] name = "monadic-failstop-test" srcDir = "test" root = "Unit.MonadicFailstop"`) + `scripts/test_unit.sh:15-…` `UNIT_TESTS=(…)`. Hand-written proof seams beside their subject: `CerbFailProofs.lean`, `CerbNDFuelProofs.lean`. Fork-drift manifest `scripts/fork_drift_manifest.txt`: layer-1 content pins for all four `impl_mem.ml` (rows `:359-362`) — SINGLE-ROW edits with a dated header note, never `--refresh` (`:8-9`, `:17`, `:26`, `:38`, `:44`).

**Observability and the matched-mode instance.** One corpus file prints an address (`%p`); pointer values, UB messages and allocation-census lines can expose addresses. Matched mode therefore instantiates the bound to upstream's value `0xFFFFFFFFFFFF` = 281474976710655 on BOTH engines by default; every existing baseline row must be unmoved.

**Consumers.** cerberus-sl (`/home/dev/projects/cerberus-sl`, read-only to you) uses `initialMemState` in 11 files, `initial_driver_state` in 20, `drive` in 23, `lastAddress` in 4, `MemState := {` in 3 — a signature change here is a consumer re-pin event (like the fuel arc): the record carries the exact old→new signature list for the operator to relay. [USER 2026-09-16]: *"cerberus-sl is our main upstream customer at the moment. I retired refined-cerberus (it got too messy)."* cerberus-sl pins the semantics by COMMIT (`scripts/semantics-pin.env`: `CERBERUS_LEAN_COMMIT="679181d1bbe0…"`, mainline of 2026-09-09; its `scripts/setup-cerberus-dep.sh` primes a workspace `.cerberus-ws/` at the pin, consumed by Lake as a path dependency); re-pins are deliberate, scouted slices recorded in its `docs/DECISIONS.md`, never background updates. The re-pin note in this slice's record is addressed to cerberus-sl's agent (relayed by the operator); the master plan's §6 note is the FORMAT only — refined-cerberus is retired and receives nothing.

**WP-O (landed before you start).** Row 10 is widened and its register (`scripts/upstream_oracle_differences.json`, schema 2: `class`, `citation`, `rationale`, signatures) is the ONLY list of fork≠pristine behaviours observed by a corpus; `VALIDATION.md` §0 carries the reference doctrine and §3 the per-difference table where UNOBSERVABLE deviations are recorded with a citation. This slice's allocator fix is such a deviation (unobservable at upstream's bound; observable at a tiny bound only through the fork-only flag, which pristine does not have). The three-engine report (pristine | fork | Lean) is the mandatory report of a shared-model slice — this is one.

## 2. Deliverables — three commits, in this order

**C1 — Allocator soundness (remedy 1).** (a) `memory/concrete/impl_mem.ml` and `memory/vip/impl_mem.ml`: remedy 1 verbatim as §1 states it; a two-line fork-side comment citing tray 44. (b) `CerbMem.allocator` (`:2093-2107`) mirrors the new body line by line with `impl_mem.ml` cites; the header comment updated. (c) The theorem, in a hand-written seam beside its subject (`CerbMemAllocatorProofs.lean` or inside `CerbMem.lean` if the house style prefers; register it in `handwritten_copy.manifest`): for all `st sz align`, `0 < align → 0 ≤ sz → (allocator sz align).run st = (NDactive (id, a), st') → align ∣ a ∧ 0 < a ∧ a + sz ≤ st.lastAddress ∧ st'.lastAddress = a`; kernel-only tactics over `Int.ediv`/`Int.emod` facts (`Int.emod_nonneg`, `Int.ediv_add_emod`/`Int.emod_add_ediv`, `omega`); axioms ⊆ {propext, Quot.sound, Classical.choice}; NO `native_decide`/`decide` on literals as the load-bearing step. If it does not close with elementary lemmas in a bounded effort → STOP rule S2 (ship the test, record the theorem as OWED with the blocking goal; no grind). (d) The runtime witness `test/Unit/AllocatorSoundnessTest.lean` (lean_exe `allocator-soundness-test`, registered in `lakefile.toml` and `test_unit.sh`): the four states of draft 44 evaluated on the ACTUAL `CerbMem.allocator` — expected after the fix: cursor 3 (4,4) killed, cursor 7 (8,8) killed, cursor 2 (4,4) killed, cursor 8 (4,4) active at 4 with cursor 4 — plus the pre-fix values quoted in a comment as the negative control (exit non-zero on any mismatch). (e) Fork-drift manifest: two single-row edits (concrete, vip) with the dated header note citing tray 44. (f) `VALIDATION.md` §3: the deviation entry (class shared-model-fix; UNOBSERVABLE at upstream's bound; tray 44; the theorem's name; "fork engines agree with each other; pristine differs only in the exhausted regime"). (g) Tray draft 44's "Fork status" gains the LANDED line (commit hash, files) — the patch hunk for upstream is the OCaml diff. Gate: `scripts/test_unit.sh` (all gates), Tier A rows 2–4b unchanged.

**C2 — The address-space bound as an explicit entry parameter (route A; not a reader).** Design decision [AGENT, for the operator's confirmation in the record]: the bound is consumed exactly ONCE, when the initial memory state is built, so it is threaded as an EXPLICIT parameter from the entry point — the sanctioned form ("a caller-passed parameter", DESIGN.md §4) — rather than as a second `declare {lean} reader val` (which would add a leading argument to EVERY `reader_consumer` seam and a binder to every lifted def; `lem-lean/doc/lean-backend/DESIGN.md:250-262`). (a) Lem (shared model, signature only — the rationale is the [USER 2026-09-16] ruling and the no-magic-values rule; recorded as such): `mem.lem:41` `val initial_mem_state: integer -> mem_state`; `driver.lem` `initial_driver_state_with address_space_top run_st file fs_state` with `:1526` `layout_state= Mem.initial_mem_state address_space_top;`, and the two wrappers `initial_driver_state address_space_top file fs_state`, `initial_driver_state_given address_space_top sup file fs_state`; nothing else in the model changes (`drive` untouched). (b) OCaml: `memory_model.ml:39` `val initial_mem_state: Z.t -> mem_state`; all four implementations take the argument — concrete and vip set `last_address` from it (the `0xFFFFFFFFFFFF` literal LEAVES `impl_mem.ml`); symbolic and cheri-coq accept and ignore it with a one-line comment (their state has no cursor); `driver_conf` gains `address_space_top: Z.t` (`.ml` + `.mli`), and `driver_ocaml.ml:157`/`:198` pass `conf.address_space_top`; `backend/driver/main.ml` fills it — in C2 from a single named default `address_space_top_default = Z.of_int 0xFFFFFFFFFFFF` beside the other defaults (the ONE OCaml numeral, with a comment saying so and citing the removed `impl_mem.ml:508`). (c) Lean: `MemState.lastAddress : Address` (NO default), `CerbMem.initialMemState (addressSpaceTop : Int) : MemState := { lastAddress := addressSpaceTop }`; `Main.lean`: `--address-space-top N` parsed exactly as `--fuel` (`:1251-1279` shape: absent → the default; non-numeral or `≤ 0` → the same refusal shape naming the default), `def defaultAddressSpaceTop : Int := 0xFFFFFFFFFFFF  -- ADDRESS-SPACE-DEFAULT (the one allowed address-space numeral)`, passed to `initial_driver_state` at `:1019`; `CerbCall.driveCall` if it builds a state (check; §1 found no site). (d) The gate: `scripts/check_no_fuel_numerals.sh` scans for the bound numeral too (`0xFFFFFFFFFFFF`, `0xffffffffffff`, `281474976710655`, and a `lastAddress :=`/`last_address` literal pattern) across the same trees, allowlisting ONLY the two Main.lean lines by exact content (extend `ALLOW_MAIN`); `--selftest` gains plants for the new patterns (a literal in `CerbMem.lean`, in a test, in speclab; the Main.lean allowlisted line accepted). (e) `test/Unit/MonadicFailstop.lean:24` and any other provider constructor take a TEST-CHOSEN value (allowed by the ruling); the C1 test passes explicit states already. (f) Regenerate both generated trees (`make prelude-src lean-prelude-src`; the lem-sync stamps), rebuild both engines; every Tier A/B baseline row unmoved. (g) Docs: `DESIGN.md` §4 (the bound joins fuel; the entry shape `initial_driver_state top supply file fs`), `VALIDATION.md` §0/§7 sentence where fuel's CLI default is described, `lean_frontend/CLAUDE.md` key-files rows; the CONSUMER RE-PIN NOTE in the record (old→new for `initial_driver_state`, `initial_driver_state_given`, `initialMemState`, `MemState.lastAddress`; what a consumer theorem now quantifies: `∀ top, …`). Gate: `test_unit.sh` (incl. the extended gate and its selftest), Tier A full.

**C3 — The fork-only OCaml flag and the differential witness.** (a) `backend/driver/main.ml`: `--address-space-top N` (`Arg.(value & opt … address_space_top_default & info ["address-space-top"] ~doc)`, the `--batch-alloc-census` shape), documented as FORK-ONLY (no pristine counterpart) in `--help` and in the record; wired into `driver_conf.address_space_top`. (b) The witness lane `scripts/test_address_space.sh` + `tests/address_space/` (a handful of small programs that allocate a few objects, e.g. three `int`s then a `char[16]`; nothing clever): run BOTH engines at tiny bounds (`--address-space-top 64`, `32`, and one bound that exhausts on the second object) in batch mode and compare the complete observations through the shared codec — expected: identical outputs, including the out-of-memory kill where the bound is exhausted; committed expectations file, fail-closed both directions. Plant (`--selftest`, the `test_exec.sh --selftest` pattern): the lane must REJECT an observation file edited to the PRE-FIX verdict of an exhausted case (an `active` allocation where the kill is expected — the pre-fix values are known from draft 44), and must REJECT a missing or truncated expectations file; no second oracle build of any kind, ever. (c) LADDER: the lane as a Tier A row (it is seconds); VALIDATION §5 lane list; row 10's fork-only-flag exclusion list gains `--address-space-top` (only if WP-O landed such a list; otherwise the record names it). (d) Fork-drift rows for `main.ml`, `driver_ocaml.ml{,i}`, `memory_model.ml`, and the four `impl_mem.ml` (C2's signature change) — single-row edits, one dated header note for the slice. (e) A tray PROPOSAL draft 45 for upstream ("make the address-space top a driver parameter; the allocator's exhausted regime is then testable") is OPTIONAL — draft it only if it costs nothing beyond the facts already in the record; slot it like draft 43. Gate: the new lane green; `release.py --mode full`; the WP-O three-engine report over the full pristine corpus with ZERO new rows (the flag is never passed there).

## 3. Fence, forbidden, stop rules

**Fence (may edit):** `frontend/model/mem.lem` (`:41` signature + the two target_reps only) and `frontend/model/driver.lem` (the three `initial_driver_state*` defs and `:1526` only); `ocaml_frontend/memory_model.ml:39`; `memory/{concrete,vip}/impl_mem.ml` (allocator body per remedy 1 + `initial_mem_state` signature), `memory/{symbolic,cheri-coq}/impl_mem.ml` (`initial_mem_state` signature only); `backend/common/driver_ocaml.ml{,i}`; `backend/driver/main.ml`; `lean_frontend/{CerbMem,Main}.lean` (+ `CerbMemAllocatorProofs.lean` new), `test/Unit/{AllocatorSoundnessTest,MonadicFailstop}.lean`, `lakefile.toml`, `handwritten_copy.manifest`; `scripts/{check_no_fuel_numerals.sh,test_unit.sh,fork_drift_manifest.txt (single rows + header note),test_address_space.sh (new),LADDER.md}`; `tests/address_space/` (new); docs: `DESIGN.md` §4, `VALIDATION.md` §3/§5 (+ one sentence in §0/§7), `lean_frontend/CLAUDE.md` key-files table, the record `lean_frontend/docs/2026-09-16_allocator-soundness-address-bound-record.md` (+ evidence dir), tray draft 44 fork-status, optional draft 45 + INDEX entry.

**Forbidden:** any other `.lem` change (no body changes anywhere; `drive` untouched); any allocator arithmetic beyond remedy 1; touching `allocator_with_address`, `allocate_object`, the bytemap, or any other CerbMem function; any baseline or register row of any lane (`exec_*_baseline.txt`, immaculate/libxml2/csmith/ci baselines, `upstream_oracle_differences.json`, `fuel_*`, `failure_reach_register.txt`); `check_fork_drift.sh --refresh`; moving `CERBERUS_REV`/`LEM_REV`; lem-lean changes; WP-O's files (`test_upstream_oracle.py`, `build_independent_oracle.py`, `ensure_independent_oracle.*`) except the fork-only-flag exclusion entry if such a list exists; a second oracle build of any kind; a default literal anywhere but the two allowlisted Main.lean lines and the one named OCaml default; `2>/dev/null` on build/install steps; opam/switch changes; anything machine-global; `git push`; commits on any mainline; uncapped Lean; more than one heavy job at a time.

**Stop rules (STOP = commit what is green, write the interim state into the record, end the turn with a report):** **S1** any existing Tier A/B row moves (a fixed-bound run must be byte-identical to before — the fix is unobservable at upstream's bound and the parameter is identity at its default); **S2** the allocator theorem does not close with elementary `Int` lemmas in a bounded effort — ship the runtime witness, record the theorem as OWED with the open goal, stop (no grind, no option bumps); **S3** an `impl_mem.ml` implementation cannot take the new signature trivially, or the virtual-module change ripples beyond the sites named in §1; **S4** any Lean≠fork difference at ANY bound in the new lane or the three-engine report (a zero-discrepancy violation); **S5** a step measured or projected past ~45 minutes; **S6** any rule conflict (raise it; fail closed meanwhile).

## 4. Gates and commit discipline

Worktree `worktrees/cerberus-lean-arc/allocator-soundness-address-bound`, rebased by the orchestrator onto the post-WP-O mainline before you start (verify: `git log -1` shows this charter on top of WP-O's landing; `scripts/ensure_independent_oracle.*` exists — run it first so row 10 can run). `scripts/ce <cmd>` for the OCaml environment; both engines rebuilt via `scripts/common.sh build_cerberus`/`build_lean` recipes (never a stale `generated/` copy — the sync gate refuses). Per commit (FAST-GATE): `./scripts/test_unit.sh` (all gates incl. `check_no_fuel_numerals.sh --selftest`, `check_fork_drift.sh`, the sync gate), `./scripts/test_exec.sh --check-baseline` and the coverage/debug/float rows (Tier A 2–4b), and from C3 the new lane. Once, before the record (FULL): `python3 scripts/release.py --mode full` (Tier A + B, the widened row 10 with `--plant`) and the three-engine report over the full pristine corpus; verbatim per-lane tails in the record; zero movement anywhere. One commit per deliverable (C1, C2, C3), each message stating what ran with its verbatim verdict lines. You never merge, rebase or push.

## 5. The record (ends the slice)

`lean_frontend/docs/2026-09-16_allocator-soundness-address-bound-record.md`: §0 rulings by pointer; C1 — the OCaml diff (the upstream patch hunk), the mirror cites, the theorem statement and its `#print axioms`, the test's verbatim output before/after (the pre-fix values as the negative control), the drift rows; C2 — the design decision (route A vs the reader route, with the DESIGN.md cites), the signature list old→new (the CONSUMER RE-PIN NOTE, for cerberus-sl and refined-cerberus), the gate extension and its selftest verdicts, the zero-movement evidence; C3 — the flag, the lane's expectations file and plant verdict, the LADDER row; the FULL gate tails verbatim; measurements (wall-clock per lane); errata to §1; open items (the optional draft 45; whether the consumer's `create` rule can now drop its bound assumption — cerberus-sl's option (b)). Commit it; then STOP.
