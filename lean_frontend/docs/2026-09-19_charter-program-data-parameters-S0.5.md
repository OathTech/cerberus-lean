# Charter — program-data parameters, S0.5: N-ary `reader_seed` (the paired lem-lean slice) and the cerberus re-pin (2026-09-19)

**Status:** CHARTER for one Claude Fable subagent worker, written by the orchestrator [AGENT]. Two halves in two repositories, in this order, with a MANDATORY orchestrator boundary between them:

- **Part A (lem-lean):** branch `program-data-parameters` in the primed worktree `worktrees/lem-lean-program-data-parameters` (base: lem-lean mainline `mdd/lean-backend` = `f6542f8` = the pin). The backend fix named by the S0 record §1.6: with N declared readers a `reader_seed` def seeds all N.
- **Orchestrator boundary:** the orchestrator re-verifies Part A, moves `deps/lem-pinned` to Part A's head and runs `make rebuild-lem` (the shared switch's `lem` binary — orchestrator's hands only), then re-launches the worker.
- **Part B (cerberus-lean):** branch `arc/program-data-parameters` in the primed worktree `worktrees/cerberus-lean-arc/program-data-parameters` (head `f417066ff`, the S0 record). The Lake pin bump to Part A's head, regeneration of both generated trees — required BYTE-IDENTICAL to the trees at `f6542f8` — and Tier A.

S0.5 changes NO `.lem` in cerberus and NO hand-written Lean: the model still declares ONE reader (`tagDefs`), so the fixed backend must emit exactly what the pinned one emits. The `.lem`-side use of the new rule (the seed def `run_const_expr_driver` gaining two leading parameters) belongs to the E-A/D-A slice, not here. This charter is the input to that slice's charter together with the S0 record.

## 0. Rulings (verbatim), decisions, standing constraints

- [USER 2026-09-19]: *"I'm interested in making decisions that are consequential in some way but for the other kinds of decisions, which are really more implementation-focused, I think you can make the calls."* Then on Q2: *"yeah, I agree on Q2, let's roll it together"* — D-A and E-A are ONE arc; one consumer re-pin. S0.5 is the backend prerequisite of that arc.
- [USER 2026-09-04] (carried from the supply threading, cited in the S0 record §1.6): *"we don't change the lem structure for ocaml"*.
- [USER 2026-09-11]: *"in this case you should launch them as claude fable class subagents… We want to keep in control of this bit of the work"* — this worker is Fable-class; the orchestrator re-verifies every gate.
- [USER 2026-09-08] verbatim: *"we should \*NOT\* be building anything new out-of-policy"* — S0.5 adds one backend rule, its probes/tests and two records; no new enumeration/literal/semantics-evaluation proofs, no new artefact surface.
- **Q-B decided [AGENT], flagged to the operator** (S0 record §5 Q-B; within the 2026-09-19 delegation, as implementation-focused): **N-ary `reader_seed`** — with N declared readers a seed def's first N parameters are the seeds, positionally in the GLOBAL SORTED reader order (the one order the binders and the consumer stubs already use; `lean_backend.ml:599`). Consequence accepted: in E-A/D-A the `.lem` head of `mini_pipeline.lem:70 run_const_expr_driver tds dr_st` becomes `run_const_expr_driver <digest> <enum_definitions> tds dr_st` — two parameters DEAD on the OCaml target, exactly the precedent of the supply threading (`mini_pipeline.lem:80-86`: *"On the OCaml target the threaded values are DEAD (the mints redirect ambiently)"*; `cabs_to_ail.lem:1131-1133` *"DEAD round-trip on the OCaml target"*), accepted under the same [USER 2026-09-04] rule. The alternative (a partial seed naming the reader it seeds, the seed def lifted for the rest) needs a lifted-AND-seeded def shape the backend has no notion of today (`lean_backend.ml:1189-1191` "reader_seed defs are never lifted"; `:4554-4557` refuses exactly that) — larger, two rules. Revisitable by the operator; if reversed, Part A is the part that changes.
- Standing constraints: NEVER modify machine-global state (no opam package changes — every `_opam` is a symlink to the SHARED switch; `make rebuild-lem` is the orchestrator's step, see §2 boundary); NO `git push`, ever; never commit on `mdd/cerberus-lean`, `master`, lem-lean's `mdd/lean-backend` or `master`; no merge/rebase; fail-closed/fail-noisy (no `2>/dev/null` on a build or gate step); [AGENT]/[USER] provenance; quoted outputs verbatim, derived tallies labelled; every `lake`/`lean` through `cerberus-lean/scripts/capped` (the lem-lean comprehensive Makefile already routes `LAKE` through it, `tests/comprehensive/Makefile:7-9`); ONE heavy job at a time; the ~45-minute step tripwire (Part B's one full Lean rebuild after the rev bump is EXPECTED real content, not grind — report its wall time; past ~1 h STOP and report); never end the turn on a wait; a committed record ENDS the half — working past it without the orchestrator's go is a violation.

## 1. Facts (verified by the orchestrator; cite, do not re-derive; errata to the record)

- **The guard, verbatim** (`src/lean_backend.ml:4459-4464` at `f6542f8`):
  ```
  | Some _ when List.length (get_reader_params ()) <> 1 ->
    (* The seed name overrides EVERY injected reader
       parameter — with more than one reader that would
       silently conflate them (audit finding). *)
    raise (Reporting_basic.err_general true (locn_of_clause_group g)
      "Lean backend: reader_seed requires exactly one declared reader")
  ```
  Its purpose (no conflation) is met by a per-reader association; the guard then has no remaining job. It has NO probe today (`grep -rl "exactly one declared reader" tests doc` → nothing).
- **The failing case, verbatim** (S0 evidence `2026-09-19_program-data-parameters-S0-evidence/probe/probe_seed3.lem` — three readers `tagDefs`/`digest`/`enum_defs`, a consumer, `seed_entry tds x = uses_three x` declared `reader_seed`; `logs/gen_probe_seed3.log`):
  ```
  File "probe_seed3.lem", line 32, character 24 to line 32, character 35
    Error: Lean backend: reader_seed requires exactly one declared reader
    original input: "uses_three x"
  ```
  `lem -v` → `Lem f6542f8` (`logs/lem_version.txt`).
- **The seed machinery, all sites** (`src/lean_backend.ml` at `f6542f8`): `St.reader_seed_param : string option ref` `:341-346` (the [render] comment: the seed name "OVERRIDES the injected reader parameter name at every injection site within (lexically-scoped seeding)"); reset `:429`; `lean_reader_is_seed` `:491`; the annotation-mix guards `:535-548` and `:1275-1278` (supply × reader_seed); "reader_seed defs are never lifted" `:1189-1191`; `get_reader_params` `:3136` (= `lean_reader_get_params` `:589-601`, SORTED by binder name `:599` `List.sort (fun (_, a) (_, b) -> String.compare a b) l`, duplicate-name check `:600`); `reader_consumer_scope_check` `:3162-3166` (reads `!St.reader_seed_param <> None`); `reader_inject_name pname` `:3170-3173` (the lookup to generalise); `reader_args_output` `:3175-3178` (consumer call sites — every reader through `reader_inject_name`); `seed_info` `:4441-4457` (takes the FIRST pattern only; `P_var`/`P_var_annot`; errors "first argument must be a simple variable" `:4450`, "must take the seed as its first argument" `:4453`, multi-clause/mutual `:4456`); the guard `:4459-4464`; the mutual/instance/fuel refusals `:4465-4473` (stay); the reserved-binder check `:4538` (stays); "unexpectedly reader-lifted" `:4554-4557` (stays); the save/restore `:4559-4561`; the two other injection sites `:5819` (applied reader read) and `:5937` (bare reader reference, eta-expanded). Binder order in generated code: `[Inhabited]`, readers (sorted), supply (`doc/lean-backend/DESIGN.md:240-241`; `lean_backend.ml:4703-4712`).
- **The reader test family** (the templates): `tests/comprehensive/test_reader_consumer.lem` (one reader `cfg`, consumer `scaled` → `TestReaderConsumerImpl.scaled`, seed `seed_entry cfgv x`, the `.lem` assert only on the seed-rooted entry); `lean-test/TestReaderConsumerCheck.lean` (kernel `rfl`/signature pins), `lean-test/TestReaderConsumerExec.lean` + `lakefile.lean:113-114` `lean_exe «test-reader-consumer»` + Makefile phase `lean-reader-consumer` `:99-109` (in the `lean:` list `:22`); `negative/neg_*.lem` with the first-line `(* EXPECT: <fragment> *)` header, phase `lean-negative` `:239-254` (a rejection for the WRONG reason is a FAIL); `invariance/inv_reader_consumer.lem`, phase `lean-invariance` `:142-171` (the `{lean}` declares stripped must leave ocaml/hol/isa/coq output byte-identical); `expected_failures.txt` is empty. The lean-test package: `require LemLib from "../../../lean-lib"` (`lakefile.lean:8`), toolchain `v4.28.0`; `lean-lib/.lake` is primed in the worktree, `tests/comprehensive/lean-test/.lake` is NOT (first build compiles the test lib only — LemLib builds in place in `lean-lib/.lake`).
- **Docs to move:** `doc/lean-backend/DESIGN.md:505` row `` `declare {lean} reader_seed val f` | do not lift `f`; its first argument supplies the reader value to lifted callees in its body `` and the compose sentence `:240-241`; the consumer paragraph `:250-275` ("inside a `reader_seed` def the seed's first argument is passed instead of the binder") ; `doc/lean-backend/README.md:136-137` describes reader lifting without the seed (check; touch only if it states the 1-ary rule). Records live in `doc/lean-backend/<date>_<slug>-record.md` (latest: `2026-09-05_tails-and-pmap-laws-record.md`).
- **The pin triple today:** `deps/lem-pinned` (branch `cerberus-pin`) @ `f6542f8` = opam pin `lem.2026-05-01 git git+file:///…/deps/lem-pinned#cerberus-pin` (`scripts/ce opam pin list --switch=.`), `scripts/ce lem -v` → `Lem f6542f8` = Lake `LemLib` rev `f6542f8e6860d12d4655e6648bc4c45dabd1d798` (`lean_frontend/lakefile.toml:48`, `lean_frontend/lake-manifest.json:8`) = lem-lean mainline `mdd/lean-backend` (0 commits ahead). The previous pin bump is the template for Part B's file set: `be1cebe36` "fuel-parameter C4 (1/n): Lake pin bump LemLib d4ba548 -> f6542f8 (measure-hypothesis slice), nothing else" — 4 files: `lean_frontend/lakefile.toml`, `lean_frontend/lake-manifest.json`, `lean_frontend/speclab/lake-manifest.json`, `tests/mem-scale-probes/micro/lake-manifest.json`. LemLib resolves to the local lem-lean checkout via `deps/gitconfig` (`scripts/ce`), so a commit in ANY lem-lean worktree is fetchable offline.
- **Regeneration and the sync stamps:** `scripts/ce make prelude-src` writes the OCaml tree into `$(PRELUDE_SRC_DIR)` (`Makefile:205`), `scripts/ce make lean-prelude-src` writes `lean_frontend/generated/` (`Makefile:307`); neither tree is tracked by git (0 files). The lem-sync stamps (`tools/check_lem_sync.sh`; `Makefile:208-233`, `:335-336` "the lem-sync stamps hash sources + outputs, not the lem version"; `:360-361`) therefore give a second byte-identity witness: after regenerating with the NEW lem the checks must pass WITHOUT `--record`/`--record-lean`. `make rebuild-lem` = `opam upgrade --switch=. --no-depexts -y lem` (`Makefile:337-341`) — it rebuilds the ONE shared switch's `lem` from `deps/lem-pinned`: orchestrator only.

## 2. Deliverables

### Part A — lem-lean (worktree `worktrees/lem-lean-program-data-parameters`, branch `program-data-parameters`)

**A0 — The before-snapshot in cerberus (FIRST action of the slice, before any lem-lean edit).** In the cerberus worktree: confirm `scripts/ce lem -v` → `Lem f6542f8`; run `scripts/ce make prelude-src lean-prelude-src`; hash every file of both generated trees (`find <PRELUDE_SRC_DIR> lean_frontend/generated -type f | sort | xargs sha256sum`) into this worktree's `.tmp/s05/gen-before.sha256` (record the file count). This is Part B's reference; it cannot be taken after the pin moves.

**A1 — The backend rule.** Build lem in the worktree first (`scripts/ce make` at the root → `./lem`). Then, in `src/lean_backend.ml`, seed machinery only:
- `St.reader_seed_param` becomes a per-reader association, `(string * string) list option ref` (reader binder name → seed variable name; `None` = not inside a seed def), lifetime class unchanged ([render]), reset at `:429`; update the header comment (`:341-346`) to state the N-ary rule.
- `seed_info` (`:4441-4457`): with `N = List.length (get_reader_params ())`, take the first N clause patterns, each `P_var`/`P_var_annot` (else the "simple variable" error, reworded to the plural), pair them positionally with the sorted reader list; FEWER than N patterns → a NEW error naming N and the order, e.g. `Lean backend: reader_seed def must take 3 seed arguments (one per declared reader, in the global sorted reader order: digest, enum_defs, tagDefs)` — the fragment `reader_seed def must take` is what the probe pins; `N = 0` → a NEW error `Lean backend: reader_seed declared but no reader is declared (nothing to seed)` (fail-closed: a seed with nothing to seed is a mistake, and today's guard refuses it too).
- Delete the `<> 1` guard (`:4459-4464`) — its comment's concern is met by the association; say so in the record. Keep the mutual/instance/fuel refusals, the reserved-binder check and "unexpectedly reader-lifted" as they are.
- `reader_inject_name pname` (`:3170-3173`) looks `pname` up in the association (falls back to `pname` when not inside a seed def — the lifted-binder case); `reader_consumer_scope_check` (`:3163`) and the save/restore (`:4559-4561`) adapt mechanically. The three injection sites (`:3175-3178`, `:5819`, `:5937`) are untouched by design — they already route through `reader_inject_name`; confirm and cite.
- Nothing else: no grammar change (the declare stays `declare {lean} reader_seed val f`), no change to lifting, to the other emitters, or to `lean-lib/` (a LemLib change is stop rule S1).

**A2 — Tests (the suite grows by one positive family and three probes).**
- `tests/comprehensive/test_reader_multi.lem`: THREE readers whose sorted order differs from declaration order and includes two of the SAME type so the order is observable by VALUE — e.g. declare `gamma : unit -> nat`, `alpha : unit -> nat`, `beta : unit -> string` (sorted: `alpha`, `beta`, `gamma`); a `reader_consumer` `combine` → `TestReaderMultiImpl.combine (alpha : Nat) (beta : String) (gamma : Nat) (x : Nat)` with a value that changes when `alpha` and `gamma` swap (e.g. `alpha * 1000 + beta.length * 100 + gamma * 10 + x`); lifted defs using one, two and all three readers plus the consumer; a supply-lifted def (`declare {lean} supply val`) in the same cone; a seed def `seed3 av bv gv x` (the three seeds in sorted order, then its own arguments) whose body calls the consumer directly AND a lifted callee; a seed-rooted, non-lifted caller `via_seed3 x = seed3 7 "ab" 9 x` asserted from the `.lem` (the only assertable entry, as in `test_reader_consumer.lem`). Compute the expected values by hand in comments.
- `lean-test/TestReaderMultiImpl.lean` (the stub), `lean-test/TestReaderMultiCheck.lean` (kernel pins: the lifted defs' SIGNATURES show `(alpha : Nat) (beta : String) (gamma : Nat)` in that order, then the supply binder after the readers where applicable; `seed3`'s signature is NOT lifted; `rfl` value pins including one that would FAIL under an alpha/gamma swap; the seed pickup through the consumer and through the lifted callee), `lean-test/TestReaderMultiExec.lean` + `lean_exe «test-reader-multi»` in `lakefile.lean` + a Makefile phase `lean-reader-multi` modelled on `:99-109`, added to the `lean:` list (`:22`) and to the `.PHONY` line. Add the `.lem` to the `LemComprehensiveTest` roots as the existing tests are.
- `negative/neg_seed_arity.lem` (`(* EXPECT: reader_seed def must take *)`): the S0 failing shape — three readers, `seed_entry tds x` with ONE seed parameter. `negative/neg_seed_noreader.lem` (`(* EXPECT: reader_seed declared but no reader is declared *)`). `negative/neg_seed_nonvar.lem` (`(* EXPECT: <the plural simple-variable fragment> *)`): a seed def whose second seed position is a non-variable pattern.
- `invariance/inv_reader_multi.lem`: the multi test's shape with the `{lean}` declares (the lean-target `target_rep`s are already target-scoped and stay), per `inv_reader_consumer.lem`.
- The existing `test_reader_consumer.lem` / `TestReaderConsumerCheck.lean` / `TestReaderConsumerExec.lean` / `neg_rc_*` / `neg_fuel_reader_seed.lem` / `inv_reader_consumer.lem` are UNCHANGED and green — the one-reader regression control.

**A3 — The byte-identity sweep (the regression evidence for every other lem program).** For each corpus the suite generates — `tests/comprehensive/test_*.lem` (one file per invocation, the Makefile's `LEMFLAGS`), `tests/backends/*.lem` (as `tests/backends/Makefile`'s Lean invocation does), `examples/ppcmem-model/*.lem` (the 10-file invocation of the root `Makefile:111-123`), `examples/cpp/cmm.lem` — generate Lean output twice into two scratch dirs under the worktree's `.tmp/`: once with the PINNED lem (`scripts/ce lem`, the switch's `f6542f8` binary — confirm `scripts/ce lem -v`) and once with the new `./lem`; `diff -r`. Required: IDENTICAL everywhere except `test_reader_multi.lem`, which the pinned lem must REFUSE (quote its error — the negative control that shows the rule is load-bearing). Any other difference is stop rule S2 (a finding, not something to absorb).

**A4 — Docs.** `DESIGN.md:505` row → the N-ary rule (first N arguments, one per declared reader, global sorted order; too few / no reader refused); `:240-241` and `:250-275` reworded where they say "first argument"; `README.md` only if it states the 1-ary rule; a dated record `doc/lean-backend/2026-09-19_nary-reader-seed-record.md`: the rule and why (the S0 failing case verbatim; the guard's original concern and how the association meets it; the two new refusals; the [AGENT] Q-B decision with the cerberus consequence and the OCaml-dead precedent — provenance exact), the test additions, the sweep result, and every gate tail VERBATIM (§4). Note the cerberus-side follow-up (Part B) and the E-A/D-A `.lem` change as the consumer of the rule.

**A5 — Commit ONCE** (backend + tests + docs; message states what was verified), quote the head hash, then STOP and report to the orchestrator. Do NOT touch `deps/lem-pinned`, do NOT run `make rebuild-lem`, do NOT start Part B.

### Orchestrator boundary (not the worker's)
The orchestrator: rebuilds `./lem` in the lem-lean worktree and re-runs the comprehensive suite + the sweep independently; `git -C deps/lem-pinned reset --hard <A5 head>`; `scripts/ce make rebuild-lem` in the cerberus worktree; confirms `scripts/ce lem -v` → `Lem <A5 short hash>`; re-launches the worker for Part B with that confirmation. The worker treats the re-launch message as the go.

### Part B — cerberus-lean (worktree `worktrees/cerberus-lean-arc/program-data-parameters`, branch `arc/program-data-parameters`)

**B1 — The Lake pin.** Bump `LemLib` to Part A's FULL hash in exactly the previous bump's file set (`be1cebe36`: `lean_frontend/lakefile.toml` — keep the comment trail, add one line naming this slice — `lean_frontend/lake-manifest.json`, `lean_frontend/speclab/lake-manifest.json`, `tests/mem-scale-probes/micro/lake-manifest.json`); `scripts/ce` + `scripts/capped lake update LemLib` from `lean_frontend/` is the sanctioned way (offline via the redirect); if the manifests move by hand, say so. Confirm the fetched package's HEAD equals Part A's hash and that `lean-lib/` is byte-identical between `f6542f8` and Part A's head (`git -C <lem-lean worktree> diff --stat f6542f8 <A5> -- lean-lib` → empty).

**B2 — Regenerate and prove byte-identity.** `scripts/ce lem -v` → Part A's hash (the orchestrator's confirmation, re-checked); `scripts/ce make prelude-src lean-prelude-src`; hash both trees to `.tmp/s05/gen-after.sha256`; `diff .tmp/s05/gen-before.sha256 .tmp/s05/gen-after.sha256` → EMPTY (same file set, same hashes; quote the two file counts). The lem-sync checks (`tools/check_lem_sync.sh --check` and `--check-lean`) PASS WITHOUT re-recording. Any difference is stop rule S2.

**B3 — Build.** Per `scripts/common.sh build_cerberus` / `build_lean` (the hand-written↔generated sync gate runs as its precondition) and the speclab package; ONE heavy job; report the wall time of the full rebuild.

**B4 — Tier A** (`scripts/LADDER.md` rows 1–12, `4b`, `4c`, `6b` included), every tail line VERBATIM in the record. Row 1's `test_unit.sh` includes the lem-sync gate and the fork-drift gate — both must pass with no manifest change (the OCaml tree is identical). No Tier B (the pre-merge audit decides its scope; the change is a pin bump with proven-identical trees).

**B5 — The record** `lean_frontend/docs/2026-09-19_program-data-parameters-S0.5-record.md`: Part A's summary (head hash, the rule, the suite and sweep tails — copied from the lem-lean record, cited), the pin triple after the move (`deps/lem-pinned` HEAD = `opam pin list` target + `lem -v` = Lake rev = lem-lean branch head; what is NOT yet true: lem-lean mainline `mdd/lean-backend` still at `f6542f8` until the lem-lean merge — the pin dance's remaining steps, named), the byte-identity evidence (counts, the empty diff, the sync checks), the Tier A tails, the Q-B [AGENT] decision restated with its flag, and the E-A/D-A charter's inputs (the `.lem` head change, `fork_drift_manifest.txt` re-pin foreseen there). **Commit ONCE** (the 4 pin files + the record), quote the head hash, STOP.

## 3. Fence, forbidden, stop rules

**Fence — Part A may edit:** `src/lean_backend.ml` (the seed machinery named in §1 and A1 — nothing else in the file); `tests/comprehensive/test_reader_multi.lem`, `tests/comprehensive/lean-test/TestReaderMulti{Impl,Check,Exec}.lean`, `tests/comprehensive/lean-test/lakefile.lean` (one root + one exe), `tests/comprehensive/Makefile` (one phase + the two list lines), `tests/comprehensive/negative/neg_seed_{arity,noreader,nonvar}.lem`, `tests/comprehensive/invariance/inv_reader_multi.lem`; `doc/lean-backend/DESIGN.md`, `doc/lean-backend/README.md` (conditionally), the new record. Scratch under the two worktrees' `.tmp/` only. **Part B may edit:** the four pin files of B1 and the record. **Forbidden:** `lean-lib/` (LemLib) in any way; the `.ott` grammar and `language/`; the ocaml/hol/isa/coq emitters; any cerberus `.lem`, hand-written Lean, script, baseline, allowlist, register, `fork_drift_manifest.txt`; `deps/lem-pinned` and `make rebuild-lem` (orchestrator only); opam/switch changes; anything machine-global; `git push`; commits on any mainline; uncapped Lean; more than one heavy job; editing generated trees by hand.

**Stop rules:** **S1** the rule cannot be implemented within the seed machinery (it needs the lifting fixpoint, LemLib, or a grammar change) — report the shape, do not build it; **S2** any non-identical output in the A3 sweep (other than the new test) or in B2 — a finding; **S3** a step past ~45 minutes (Part B's full rebuild excepted up to ~1 h, wall time reported); **S4** any rule conflict; **S5** Part A ends at A5's commit — Part B starts only on the orchestrator's re-launch.

## 4. Gates

**Part A (all verbatim in the lem-lean record):** `scripts/ce make` at the worktree root (builds `./lem`; `./lem -v` quoted); `scripts/ce make -C tests/comprehensive lean` — EVERY phase of the `lean:` list green (`lean-generate` "N passed, 0 failed, 0 skipped", `lean-compile`, `lean-panic`, `lean-tuple-once`, `lean-supply-draws`, `lean-reader-consumer`, the new `lean-reader-multi`, `lean-fuel-param`, `lean-negative` — every probe "OK (rejected as declared)" incl. the three new ones —, `lean-invariance` incl. `inv_reader_multi`, `lean-parity`, `lean-no-sorry-proofs`, `lean-no-fuel-numerals`); `#print axioms` on `via_seed3` and one lifted def of the multi test = the trio or fewer; the A3 sweep: `diff -r` empty for every corpus + the pinned lem's refusal of `test_reader_multi.lem` quoted; `git -C . diff --stat f6542f8 -- lean-lib` empty; `git status` clean after the commit.

**Part B (verbatim in the cerberus record):** the pin triple quoted (`git -C ../../deps/lem-pinned rev-parse HEAD`, `scripts/ce opam pin list --switch=.`, `scripts/ce lem -v`, the `rev` lines of the four files, the fetched package's HEAD); the B2 hash-list diff empty with both counts; `check_lem_sync.sh --check` and `--check-lean` passing without re-record; Tier A rows 1–12 (+4b, 4c, 6b) each at its LADDER bar; `git status` clean but for the five files of B5 before the commit, clean after.

## 5. Record and reporting

Interim status pings get an immediate reply. The Part A report to the orchestrator: the head hash, the phase tails, the sweep verdict, anything that deviated from this charter (with the reason) — then stop. The Part B report: the head hash, the triple, the empty diff, the Tier A tails, deviations — then stop. Errata to this charter (line drift, a wrong cite) go into the records, not into this file.
