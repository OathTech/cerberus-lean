# gcc second-oracle triage ledger — scripts/test_gcc_oracle.sh
#
# Format: <row-key> <TRIAGED_CLASS> <rationale...>
# Classes + triage procedure: design note
# lean_frontend/docs/2026-08-30_gcc-second-oracle-design.md §4.
# FAIL-CLOSED both directions: an entry applies only to a file whose
# current status is DISAGREE (a stale entry is a lane failure, forcing
# cleanup when a divergence disappears); an unlisted DISAGREE is fatal.
#
# [AGENT 2026-08-30] Initial entries from the tier-1 baseline run
# (D2/ADDR, design note §2.3 "allocation addresses" row): each program
# observes object-address bits (low byte / mod / division / alignment
# residue). Cerberus's abstract concrete-allocator addresses and gcc's
# native layout (ASLR disabled via setarch -R, so deterministic) are
# both conforming; neither side is indicted. gcc/lean values below are
# verbatim from the ASLR-off recorded run (gcc=native exit,
# lean=expected byte set).
#
tests/debug/align-01-char-array-addr.c TRIAGED_ADDR observes (unsigned long)&B % 8 of a char[32] global: gcc=1 (native globals 8-aligned), lean={0} (allocator gives a non-8-aligned address; char alignment 1 requires none)
tests/debug/align-02-print-addr.c TRIAGED_ADDR observes &B & 0xFF of a char[32] global: gcc=64 lean={223} — raw address low byte, layout-defined on both sides
tests/debug/align-03-single-global.c TRIAGED_ADDR observes &B & 0xFF of a char[1] global: gcc=17 lean={254}
tests/debug/align-04-no-globals.c TRIAGED_ADDR observes &x & 0xFF of a stack local: gcc=36 lean={244}
tests/debug/intfromptr-05-to-long.c TRIAGED_ADDR observes (int)(long)&a of a stack local: gcc=20 lean={244}
tests/debug/intfromptr-07-check-addr.c TRIAGED_ADDR observes (long)&a % 1000000 of a stack local: gcc=12 lean={244}
tests/debug/intfromptr-08-full-addr.c TRIAGED_ADDR observes (long)&a / 1000000000 of a stack local: gcc=193 lean={130} (native stack ~0x7ffd..., allocator addresses differ)
tests/debug/intfromptr-09-first-alloc.c TRIAGED_ADDR observes (long)&a % 1000000 of a stack local: gcc=12 lean={244}
tests/debug/intfromptr-10-minimal.c TRIAGED_ADDR observes (int)((unsigned long)&a % 1000000) of a stack local: gcc=20 lean={244}
