# fuel_forms_pending.txt — the REGISTER of fuel'd workers that are reachable from
# `drive` (kernel constant closure, mutual blocks included) and are NEITHER
# measured (`<f>_measure_sufficient` proved) NOR absorbing (their `_zero` lemma
# states the monad's absorbing element): the consumer's (A)/(B)/(C) requirement
# (refined-cerberus docs/2026-09-04_review-of-fuel-parameter-design.md §2) is
# NOT yet met for these — each is a fail-open exhaustion on the execution path
# in the kernel (the runtime panic is loud, the kernel value is the sentinel).
# scripts/check_fuel_forms.sh fails on any reachable ambient worker NOT listed
# here and on any listed worker that is no longer reachable-ambient (stale pin):
# the register can only shrink by proving/absorbing, and grow by an explicit,
# reviewed line. Format: <worker>  <reason class>  <route> (free text after the
# first field). Record: lean_frontend/docs/2026-09-04_fuel-parameter-C2-record.md;
# the six point-free rows left at C3 (measured; lean_frontend/docs/2026-09-05_fuel-parameter-C3-record.md).
#
# --- tag-lookup recursion: no data measure; a measure needs a well-formedness
#     (acyclicity) hypothesis on the tag environment, which the generated
#     unconditional obligation cannot carry (C2 record, decisions D-C2-1) ---
CerbMem.sizeofCtype_lemFuel        tag-lookup   hand-written layout oracle (impl_mem.ml sizeof); Struct arm recurses through offsetsof on member types from tagDefs
CerbMem.alignofCtype_lemFuel       tag-lookup   as sizeofCtype (alignof)
CerbMem.offsetsof_lemFuel          tag-lookup   as sizeofCtype (offsetsof)
CerbMem.offsetsofMembers_lemFuel   tag-lookup   as sizeofCtype (member fold)
CerbMem.memberAlign_lemFuel        tag-lookup   as sizeofCtype (_Alignas override)
CerbMem.reconstructValue_lemFuel   tag-lookup   Struct/Union arms reconstruct member types read from tagDefs (offsetsof/UnionDef)
are_compatible_aux_lemFuel         tag-lookup   ctype_aux.lem: Struct/Union members from tagDefs1/tagDefs2 (reached via memValueFromValue)
are_compatible_params_aux0_lemFuel tag-lookup   sibling of are_compatible_aux (mutual block; also point-free tail)
are_compatible_params0_lemFuel     tag-lookup   sibling of are_compatible_aux (mutual block)
# --- pure recursion with no data measure and no absorbing element in the
#     codomain: decisions for the operator (C2 record, D-C2-2 .. D-C2-4) ---
hack_lemFuel                       pure-loop    driver.lem: step-until-value evaluation loop returning a Core `value` (Driver.finalize)
to_pure_lemFuel                    opaque-arg   core_aux.lem: recursion on subst_pattern's RESULT (ill-typed arms are an opaque failwithI value: no provable size bound)
to_pures_lemFuel                   opaque-arg   mutual sibling of to_pure
many_lemFuel                       parser       monadic_parsing.lem: recursion depth is the INPUT's length, which is inside the ParserM lambda, not a parameter; payload = the parser zero, indistinguishable from a normal parse failure
many1_lemFuel                      parser       mutual sibling of many
showNonNegativeWithBasis_aux_lemFuel  precondition  formatted.lem: recursion on n / b terminates only for b >= 2 (callers use 8/10/16)
