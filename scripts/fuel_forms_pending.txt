# fuel_forms_pending.txt — the REGISTER of fuel'd workers that are reachable from
# `drive` (kernel constant closure, mutual blocks included) and are NEITHER
# measured (`<f>_measure_sufficient` proved) NOR absorbing (their `_zero` lemma
# states the monad's absorbing element): the consumer's (A)/(B)/(C) requirement
# (refined-cerberus docs/2026-09-04_review-of-fuel-parameter-design.md §2) is
# NOT yet met for these — each is a fail-open exhaustion on the execution path
# in the kernel (the runtime panic is loud, the kernel value is the sentinel).
# scripts/check_fuel_forms.sh fails on any reachable ambient worker NOT listed
# here and on any listed worker that is no longer reachable-ambient (stale pin):
# the register can only shrink by proving/absorbing, and grow by an explicit,
# reviewed line. Format: <worker>  <reason class>  <route> (free text after the
# first field). Record: lean_frontend/docs/2026-09-04_fuel-parameter-C2-record.md;
# the six point-free rows left at C3 (measured; lean_frontend/docs/2026-09-05_fuel-parameter-C3-record.md);
# the six CerbMem layout rows + showNonNegativeWithBasis_aux left at C4 (measured under a
# hypothesis — scripts/fuel_hypotheses.txt; lean_frontend/docs/2026-09-05_fuel-parameter-C4-record.md);
# hack + to_pure/to_pures left at the fuel-pending close-out (measured under the arena shape
# hypothesis; lean_frontend/docs/2026-09-08_fuel-pending-closeout-record.md).
#
# --- deep-reference recursion (STD §6.2.7#1 structural compatibility): the
#     ctype_aux trio compares two struct/union definitions member by member
#     THROUGH pointer and function types (are_compatible_aux's Pointer/Pointer
#     and Function/Function arms recurse into the pointee/return/parameter
#     types, and the cross-TU Struct/Struct arm re-enters both definitions), so
#     the by-value acyclicity that bounds the layout oracle does NOT bound it:
#     `struct node { struct node *next; }` in two translation units makes the
#     oracle recurse forever (both oracles rc=124 at 60 s; C4 audit §5.1) and
#     makes the Lean driver fail loudly by NATIVE STACK OVERFLOW (rc=134, ~3 s;
#     the recursion is not tail-recursive) before any fuel is consumed.
#     A sufficient hypothesis would be acyclicity through ALL references, which
#     the frontend does not guarantee (self-referential structs are legal C) —
#     a finding, not a fix (C4 record F-C4-1; the lem dry run's proposed
#     `Acyclic p.1 ∧ Acyclic p.2` was insufficient). The six CerbMem tag-lookup
#     rows left the register at C4 (measured under `CerbTagsWf.Acyclic`). ---
are_compatible_aux_lemFuel         deep-ref     ctype_aux.lem: Struct/Union members from tagDefs1/tagDefs2 compared through pointers/functions (reached via memValueFromValue)
are_compatible_params_aux0_lemFuel deep-ref     sibling of are_compatible_aux (mutual block; also point-free tail)
are_compatible_params0_lemFuel     deep-ref     sibling of are_compatible_aux (mutual block)
# --- pure recursion with no data measure and no absorbing element in the
#     codomain (C2 record, D-C2-2 .. D-C2-5). hack / to_pure / to_pures LEFT the
#     register 2026-09-08 (fuel-pending close-out, option C of the pure-failure
#     reachability census): measured UNDER the arena SHAPE hypothesis
#     (CerbCoreShape.IsValuePexpr / IsPureExpr / AllPureExprs) — one hop at
#     every exec-path call site, by prepare_exit (driver.lem:1309-1316);
#     rows in scripts/fuel_hypotheses.txt, record
#     lean_frontend/docs/2026-09-08_fuel-pending-closeout-record.md. ---
# --- parser combinators: the recursion argument is not a parameter (fuel-pending
#     close-out 2026-09-08, D2 — assessed for an honest restatement, none found
#     without a lem body change). monadic_parsing.lem:100-103 `many p = many1 p <|>
#     return []` / `many1 p = p >>= fun a -> many p >>= fun _as -> return (a::_as)`:
#     the recursion descends the INPUT `cs`, which is bound by the lambda INSIDE the
#     `ParserM` constructor in the bodies of `<|>` (:52-56) and `>>=` (:38-40), never
#     by the head — the ONLY parameter of `many`/`many1` is the parser `p`, a
#     function. A measured wrapper fixes its counter `μ p` BEFORE the input
#     arrives, and the depth on an input is the number of characters `p` consumes
#     on THAT input (bounded by its length), so no Nat expression over `p` bounds
#     it and no Prop over `p` alone ("p consumes on success", progress) makes one
#     exist — the bound is a function of the input, not of `p`. The C3 `lemTail`
#     hoist (lem d4ba548) applies to `function` bodies, not to a lambda under a
#     single-constructor wrapper; an eta-expansion through `ParserM` in the lem
#     text would be a body change (forbidden); a backend hoist through the wrapper
#     is a lem-lean item. Reachability: the printf family only (Formatted.format0/
#     decimalInteger/nonnegativeDecimalInteger/flags0), never `drive` itself; the
#     payload is the parser zero, indistinguishable from a normal parse failure;
#     exhausts only at fuel < the longest `many` run in a format string (census Q4). ---
many_lemFuel                       parser       monadic_parsing.lem:100-103: recursion on the INPUT `cs` inside the ParserM lambda (not a parameter); no parameter-level measure exists — see the block note above
many1_lemFuel                      parser       mutual sibling of many (all-or-none, FM-mutual); same blocker
