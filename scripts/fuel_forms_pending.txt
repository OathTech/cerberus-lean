# fuel_forms_pending.txt — the REGISTER of fuel'd workers that are reachable from
# `drive` (kernel constant closure, mutual blocks included) and are NEITHER
# measured (`<f>_measure_sufficient` proved) NOR absorbing (their `_zero` lemma
# states the monad's absorbing element): the consumer's (A)/(B)/(C) requirement
# (refined-cerberus docs/2026-09-04_review-of-fuel-parameter-design.md §2) is
# NOT yet met for these — each is a fail-open exhaustion on the execution path
# in the kernel (the runtime panic is loud, the kernel value is the sentinel).
# scripts/check_fuel_forms.sh fails on any reachable ambient worker NOT listed
# here and on any listed worker that is no longer reachable-ambient (stale pin):
# the register can only shrink by proving/absorbing, and grow by an explicit,
# reviewed line. Format: <worker>  <reason class>  <route> (free text after the
# first field). Record: lean_frontend/docs/2026-09-04_fuel-parameter-C2-record.md;
# the six point-free rows left at C3 (measured; lean_frontend/docs/2026-09-05_fuel-parameter-C3-record.md);
# the six CerbMem layout rows + showNonNegativeWithBasis_aux left at C4 (measured under a
# hypothesis — scripts/fuel_hypotheses.txt; lean_frontend/docs/2026-09-05_fuel-parameter-C4-record.md);
# hack + to_pure/to_pures left at the fuel-pending close-out (measured under the arena shape
# hypothesis; lean_frontend/docs/2026-09-08_fuel-pending-closeout-record.md).
#
# --- deep-reference recursion (STD §6.2.7#1 structural compatibility): the
#     ctype_aux trio (are_compatible_aux / are_compatible_params_aux0 /
#     are_compatible_params0) LEFT the register 2026-09-10 (are-compatible-
#     assumed-set slice, lean_frontend/docs/2026-09-10_are-compatible-assumed-set-record.md):
#     the shared lem body now threads a path-local assumed-compatible list of tag
#     pairs (C11 §6.2.7#1's device for recursive types; upstream-tray draft 37),
#     which makes the block total, and the three workers are MEASURED WITHOUT a
#     hypothesis (lean_frontend/CerbCtypeMeasure.lean: unassumed occurring tag
#     pairs × table member sizes + structural size). The previous text — that
#     `struct node { struct node *next; }` in two translation units made both
#     oracles recurse forever (rc=124 at 60 s) and the Lean driver overflow its
#     native stack (rc=134), and that no frontend-guaranteed hypothesis bounded
#     it (C4 record F-C4-1) — is now history: the body change, not a hypothesis,
#     closed the class. ---
# --- pure recursion with no data measure and no absorbing element in the
#     codomain (C2 record, D-C2-2 .. D-C2-5). hack / to_pure / to_pures LEFT the
#     register 2026-09-08 (fuel-pending close-out, option C of the pure-failure
#     reachability census): measured UNDER the arena SHAPE hypothesis
#     (CerbCoreShape.IsValuePexpr / IsPureExpr / AllPureExprs) — one hop at
#     every exec-path call site, by prepare_exit (driver.lem:1309-1316);
#     rows in scripts/fuel_hypotheses.txt, record
#     lean_frontend/docs/2026-09-08_fuel-pending-closeout-record.md. ---
# --- parser combinators: many/many1 LEFT the register 2026-09-15 (parser-progress-
#     measure slice, lean_frontend/docs/2026-09-11_parser-progress-measure-record.md;
#     [USER 2026-09-11] ruled for the shared lem BODY change): `many`/`many1` are
#     restated as input-indexed recursion — two mutually recursive RUN functions
#     many_run/many1_run on the input `cs` (monadic_parsing.lem:119-132), extensionally
#     the same parsers on every input for every p — and the two workers are MEASURED
#     UNDER THE PROGRESS HYPOTHESIS `CerbParserProgress.Consumes p` (every result of
#     `parse p cs` leaves a strictly shorter rest; measures 2 * List.length cs + 2 /
#     + 1; lean_frontend/CerbParserProgress.lean, proofs
#     lean_frontend/Monadic_parsing_lemMeasureProofs.lean; rows in
#     scripts/fuel_hypotheses.txt). The previous text — that the recursion argument
#     was the input bound INSIDE the ParserM lambda, never a head parameter, so no
#     parameter-level measure existed without a lem body change (close-out 2026-09-08
#     D2) — is now history: the body change made the input a parameter. The parser
#     class is CLOSED; the register is EMPTY (header-only) — the gate accepts an
#     empty register (record D2). ---
