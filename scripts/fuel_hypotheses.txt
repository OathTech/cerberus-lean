# fuel_hypotheses.txt — the REVIEWED REGISTER of the hypotheses under which
# MEASURED fuel'd workers are measured (fuel-parameter arc C4, 2026-09-05; the
# lem-lean measure-hypothesis slice's CONSUMER-SIDE REQUIREMENT, its record
# §2.3/§10 and its pre-merge audit F1: a contradictory hypothesis — `b < b` —
# passes generation and makes an obligation VACUOUSLY provable while the
# fuel-free wrapper ships with the MEASURED mark; satisfiability is not the
# gate's to decide, so every hypothesis in force is named HERE with the
# frontend invariant that guarantees it, and a human reviewer).
#
# scripts/check_fuel_forms.sh reads the classifier's `hyp` column (the type of
# the obligation's reserved `lemHyp` binder, pretty-printed) and fails BOTH
# ways: a MEASURED-under-hypothesis worker without a row here whose hypothesis
# text matches exactly (whitespace-normalized) is RED; a row whose worker is
# not measured under that exact hypothesis is RED (stale); a row without a
# `.lem:<line>` cite in its invariant field or without a reviewer is RED. The
# contradictory-hypothesis decoy of the gate's --selftest (P10) is caught by
# the FIRST rule: a decoy has no row.
#
# Format: TAB-separated, 4 fields —
#   <worker>  <hypothesis (exact `hyp=` text)>  <invariant it rests on, with .lem:line cite>  <reviewer>
# Lines starting with '#' and blank lines are ignored.
#
# THE INVARIANT (rows 1-6): the C frontend's tag environment is acyclic in the
# by-value sense — cabs_to_ail.lem:1512 check_members (STD §6.7.2.1#3) refuses a
# member of function or INCOMPLETE type; ail/ailTypesAux.lem:222 is_complete
# makes a by-value Struct/Union member complete only when its tag is ALREADY in
# sigm.tag_definitions (Array iff its element type is and its size is known,
# Atomic iff its inner type is, Pointer regardless of its target); the flexible
# array member's ELEMENT type is guaranteed by the Ail passes — ailWf.lem:76-80
# (ArrayDeclarationIncompleteType, StructMemberFlexibleArrayInArray) +
# genTyping.lem:2346-2349 (StructMemberFlexibleArray): every by-value edge INTO
# a FAM struct is a constraint violation, so the FAM edge cannot close a cycle
# (audit F-A3); an _Alignas(type) member alignment is completeness-checked by
# alignof_ty on the non-agnostic path (cabs_to_ail.lem:2851-2871) — NOT on the
# character-member `Just LT` path (:2882-2883, ailTypesAux.lem:1291-1292; audit
# F-A2, see below). So definition order is a rank on the by-value reference
# graph; CerbTagsWf.Acyclic states exactly that (any rank).
# Row 4 (offsetsof) takes the two-map form: struct tags resolve in the threaded
# map, union tags in the ambient one (impl_mem.ml:173/:255, the mirrored
# upstream asymmetry); at elaboration time the ambient map is empty and the
# form is Acyclic of the threaded map, at run time both are the linked table.
#
# SCOPE OF THE GUARANTEE: Acyclic holds for every program the frontend ACCEPTS
# CORRECTLY. One known frontend GAP (audit e9730d1c8 F-A2): `struct A {
# _Alignas(struct A) char c; }` — an ISO §6.5.3.4#1 constraint violation (gcc
# rejects) — is ACCEPTED because the character-member path never
# completeness-checks the _Alignas type; its table has a by-value self-edge,
# Acyclic is FALSE for it, BOTH oracles hang on it, and the Lean wrapper fails
# loudly (`CerbMem.memberAlign: fuel exhausted`). The hypothesis is honest there
# (the environment is not acyclic); the gap is an upstream true-bug tray
# candidate (C4 record §9). A hand-authored Core file can likewise violate the
# invariant: the wrapper then may exhaust (the loud sentinel), as the oracle
# loops there.
#
# REVIEW: rows signed by the pre-merge auditor (lean_frontend/docs/
# 2026-09-05_fuel-parameter-C4-audit-premerge.md §3, audit e9730d1c8, with the
# three cite corrections F-A1/F-A2/F-A3 applied below). The operator's explicit
# merge "yes" is the FINAL signature on this register ([USER] sign-off at
# merge); a later change to any row re-opens review.
CerbMem.sizeofCtype_lemFuel	CerbTagsWf.Acyclic ambient	by-value acyclic tag environment: cabs_to_ail.lem:1512 check_members + ail/ailTypesAux.lem:222 is_complete (definition order is a rank); except the _Alignas(type)-on-a-character-member class (audit F-A2: ISO §6.5.3.4#1 CV undiagnosed; the oracle hangs there)	[AGENT auditor 2026-09-05, audit e9730d1c8] SIGNED; [USER] sign-off at merge
CerbMem.alignofCtype_lemFuel	CerbTagsWf.Acyclic ambient	by-value acyclic tag environment: cabs_to_ail.lem:1512 check_members + ail/ailTypesAux.lem:222 is_complete; FAM element: ailWf.lem:76-80 (ArrayDeclarationIncompleteType, StructMemberFlexibleArrayInArray) + genTyping.lem:2346-2349 (StructMemberFlexibleArray) (audit F-A3); except the _Alignas(type)-on-a-character-member class (audit F-A2: ISO §6.5.3.4#1 CV undiagnosed; the oracle hangs there)	[AGENT auditor 2026-09-05, audit e9730d1c8] SIGNED; [USER] sign-off at merge
CerbMem.memberAlign_lemFuel	CerbTagsWf.Acyclic ambient	by-value acyclic tag environment: cabs_to_ail.lem:1512 check_members + ail/ailTypesAux.lem:222 is_complete; _Alignas(type): completeness-checked by alignof_ty on the non-agnostic path (cabs_to_ail.lem:2851-2871) — NOT on the character-member `Just LT` path (:2882-2883, ailTypesAux.lem:1291-1292; audit F-A2): guaranteed by ISO §6.5.3.4#1 on conforming inputs	[AGENT auditor 2026-09-05, audit e9730d1c8] SIGNED; [USER] sign-off at merge
CerbMem.offsetsof_lemFuel	CerbTagsWf.AcyclicPair ambient tagDefs	by-value acyclic tag environment, two-map form (struct tags via the threaded map, union tags via the ambient one — impl_mem.ml:173/:255): cabs_to_ail.lem:1512 check_members + ail/ailTypesAux.lem:222 is_complete; ambient empty at elaboration time (CerbMem.lean layout header); except the _Alignas(type)-on-a-character-member class (audit F-A2)	[AGENT auditor 2026-09-05, audit e9730d1c8] SIGNED; [USER] sign-off at merge
CerbMem.offsetsofMembers_lemFuel	CerbTagsWf.Acyclic ambient	by-value acyclic tag environment: cabs_to_ail.lem:1512 check_members + ail/ailTypesAux.lem:222 is_complete; except the _Alignas(type)-on-a-character-member class (audit F-A2)	[AGENT auditor 2026-09-05, audit e9730d1c8] SIGNED; [USER] sign-off at merge
CerbMem.reconstructValue_lemFuel	CerbTagsWf.Acyclic ambient	by-value acyclic tag environment: cabs_to_ail.lem:1512 check_members + ail/ailTypesAux.lem:222 is_complete (the struct arm's member types come from offsetsof, the union arm's from the definition); except the _Alignas(type)-on-a-character-member class (audit F-A2)	[AGENT auditor 2026-09-05, audit e9730d1c8] SIGNED; [USER] sign-off at merge
showNonNegativeWithBasis_aux_lemFuel	2 ≤ b	every caller passes a literal basis in {8, 10, 16}: formatted.lem:573,581,589,597,605 (showNonNegativeWithBasis, the %d/%i, %o, %u, %x, %X printf arms; :338-343 is _aux's only caller and passes b through; audit F-A1); b = 1 loops in the oracle too	[AGENT auditor 2026-09-05, audit e9730d1c8] SIGNED; [USER] sign-off at merge
hack_lemFuel	CerbCoreShape.IsValuePexpr pexpr1	the arena is a VALUE at hack's only exec-path call site, finalize (driver.lem:1473-1477): driver2 (driver.lem:1369) returns ONLY through process_core_step2's two terminal arms — Step_done2 (driver.lem:1331-1338) and Step_fs2/FS_done (:1352-1356); every other arm re-enters driver2 or is an error, and new_drive_core_threads (:1275-1296) never yields a Nothing step — both rewrite the core state by prepare_exit (driver.lem:1309-1316): arena = Core_aux.mk_value_e cval = Expr [] (Epure (Pexpr [] () (PEval cval))) (core_aux.lem:454-455, :2077-2082), stack = Stack_empty; to_pure of that arena is its PEval pexpr; step_eval_pexpr returns a PEval unchanged (core_eval.lem:583-584) and valueFromPexpr accepts it (core_aux.lem:862-868): ONE iteration. A hand-authored call on another shape is outside the hypothesis (the wrapper may exhaust, loud). Erratum to the census Q4 cite: driver.lem:485-486 is inside the commented-out drive_core_thread2 (:452-510); prepare_exit is the operative mechanism	[AGENT worker 2026-09-08, fuel-pending close-out]; [AGENT orchestrator 2026-09-08] REVIEWED against the cites (prepare_exit driver.lem:1309-1316; process_core_step2 terminal arms; mk_value_e core_aux.lem:2077-2082); [USER] sign-off at merge
to_pure_lemFuel	CerbCoreShape.IsPureExpr g	the arena is Epure of a value at both exec-path call sites — finalize (driver.lem:1473-1477) and driver_globals (driver.lem:1611-1616), each right after driver2 — by prepare_exit (driver.lem:1309-1316; mk_pure_e core_aux.lem:2077-2078): the Epure arm (core_aux.lem:1545-1546) returns at depth 1; the recursion on subst_pattern's RESULT (core_aux.lem:1531-1541) is never entered there	[AGENT worker 2026-09-08, fuel-pending close-out]; [AGENT orchestrator 2026-09-08] REVIEWED against the cites (prepare_exit driver.lem:1309-1316; process_core_step2 terminal arms; mk_value_e core_aux.lem:2077-2082); [USER] sign-off at merge
to_pures_lemFuel	CerbCoreShape.AllPureExprs l	mutual sibling of to_pure (one shared counter, measured all-or-none — lem FM-mutual): never entered on the exec path (to_pure's Eunseq/Ecase arms, core_aux.lem:1570-1575/:1603-1612, need a non-Epure arena, excluded by the to_pure row's invariant); under the hypothesis each element is Epure and the fold (core_aux.lem:1623-1631) needs one inner hop per element at the same counter, bounded by List.length l	[AGENT worker 2026-09-08, fuel-pending close-out]; [AGENT orchestrator 2026-09-08] REVIEWED against the cites (prepare_exit driver.lem:1309-1316; process_core_step2 terminal arms; mk_value_e core_aux.lem:2077-2082); [USER] sign-off at merge
