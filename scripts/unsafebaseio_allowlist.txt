# unsafeBaseIO survivor allowlist — effect-retirement S0 deliverable
# (charter §7.2 grep-ban leg / §8.1 S0 item ii; Q4 ruling [USER 2026-08-31],
# sharpened per-survivor form). This file becomes the C2 ratchet's input:
# after the arc's deletions, `unsafeBaseIO` may occur in hand-written
# lean_frontend sources (and their generated/ copies) ONLY at the rows
# marked KEEP below; any other occurrence fails the gate.
#
# Census provenance: [AGENT] S0 re-verification 2026-08-31 on
# arc/effect-retirement @ 913ee31d1 (worktree grep over
# lean_frontend/*.lean, lean_frontend/generated/*.lean, and LemLib as
# consumed at lean_frontend/.lake/packages/LemLib/lean-lib/**).
# Result: the census MATCHES the charter's Q4 table EXACTLY — zero
# out-of-table sites. Comment-only mentions (not code sites, listed for
# grep-leg design): CerbGlobal.lean:51, CerbTags.lean:28, LemLib.lean:36,
# plus doc mentions of `runEffectful` in CerbTags.lean:6,46,
# CerberusFresh.lean:139, CerbDebug.lean:48. The C2 grep leg must either
# strip comments or count these rows explicitly.
#
# Format: TSV — file<TAB>identifier<TAB>class<TAB>disposition
# Classes are the Q4 ruling's: permanent-declared | temporal(<mover>) |
# C2-convert | in-arc-delete | L2-delete.
# Line numbers are advisory (drift with edits); identifier is normative.
#
# --- survivors after C2 (the standing allowlist) ---
lean_frontend/CerbUtils.lean	timingStackRef	permanent-declared	KEEP (no-op timing ref, intentionally unread; OCaml module-shape parity)
lean_frontend/CerbUtils.lean	logRef	permanent-declared	KEEP (write-only log ref, nothing reads it; shape parity)
lean_frontend/CerbUtils.lean	STD_impl	permanent-declared	KEEP (writes logRef only; value-identity otherwise)
lean_frontend/CerbUtils.lean	boundedIntegerImpl	permanent-declared	KEEP (deterministic stub `pure lo`; documented residual with named mover in-file — RNG threading iff load-bearing)
lean_frontend/CerbGlobal.lean	confRef	temporal(post-arc-parameter-plumbing-slice)	KEEP (config ref; written once by Main at startup, reader-shaped after)
lean_frontend/CerbGlobal.lean	switchesRef	temporal(post-arc-parameter-plumbing-slice)	KEEP (switches ref; same discipline)
lean_frontend/CerbGlobal.lean	getConf	temporal(post-arc-parameter-plumbing-slice)	KEEP (read of confRef)
lean_frontend/CerbGlobal.lean	has_switch_impl	temporal(post-arc-parameter-plumbing-slice)	KEEP (read of switchesRef)
lean_frontend/CerberusImpl.lean	typeof_enum_impl	temporal(reader/supply-machinery-follow-up-slice, explicitly NOT in-arc)	KEEP (enum registry read; genuinely stateful seam mirroring OCaml DefaultImpl ref)
lean_frontend/CerberusImpl.lean	register_enum_impl	temporal(reader/supply-machinery-follow-up-slice, explicitly NOT in-arc)	KEEP (enum registry write during elaboration)
# --- converted at C2 (left the allowlist, 2026-09-01) ---
# lean_frontend/CerberusFresh.lean digest_impl — CONVERTED at C2 to the
# kernel-checked-opaque forceIO/with_tagDefs pattern (Q4 ruling's promoted
# deliverable): unsafe extern opaque `digestPure` (explicit witness,
# never_extract/noinline per the L2-audit armor lesson) + impl + kernel-
# checked `opaque digest := fun _ => ""`. CerberusFresh now has ZERO
# unsafeBaseIO occurrences; differential spot-run byte-identical pre/post
# (C2 record). The new unsafe decls are pinned in the population section.
# --- deleted by this arc (rows retained so the S0→C2 diff is checkable) ---
lean_frontend/CerbTags.lean	tagDefs_impl	in-arc-delete	DELETE (§4 tagDefs loop closure)
lean_frontend/CerbTags.lean	set_tagDefs_impl	in-arc-delete	DELETE (§4)
lean_frontend/CerbTags.lean	with_tagDefs_impl	in-arc-delete	DELETE (§4; boundary-opaque expectation list shrinks)
lean_frontend/CerbTags.lean	reset_tagDefs_impl	in-arc-delete	DELETE (§4)
lean_frontend/CerbDebug.lean	get_level_impl	in-arc-delete	DELETE (§5 CerbDebug level machinery)
lean_frontend/CerbDebug.lean	set_level_impl	in-arc-delete	DELETE (§5)
# --- lem-side (LemLib as consumed; deleted at L2) ---
lem-lean/lean-lib/LemLib.lean	runEffectful_impl	L2-delete	DELETE (the axiom scaffold; §7.1)
# NOTE: generated/ copies of the hand-written files above carry the same
# sites at the same identifiers (sync-gate mirrored); the gate scans both
# trees against the SAME identifier rows.
#
# =====================================================================
# C2 RATCHET POPULATION PIN (charter section 7.2 leg 3, as amended by
# the accepted L2-audit ratchet legs; 2026-09-01). Machine-readable:
# check_theorem_axioms.sh parses the `PIN` lines below and asserts the
# comment-stripped census of @[implemented_by] targets, `unsafe
# def/opaque/abbrev` declarations, and `unsafeBaseIO` occurrences
# (keyed by enclosing declaration) over the ban surface — hand-written
# lean_frontend/*.lean + generated/*.lean + the consumed LemLib copy
# (.lake/packages/LemLib/lean-lib/**, recursive) — EQUALS this set
# exactly, both directions: any NEW site fails naming itself; any
# MISSING pinned row fails as scanner/copy-pipeline drift. This is the
# leg that bans an axiom-free reintroduction of the effect projection
# via opaque + implemented_by + unsafeBaseIO (the L2 audit's accepted
# proposal). Format: PIN <KIND> <file-basename> <identifier>, KIND in
# {IMPLBY, UNSAFEDECL, UNSAFEBASEIO}; hand-written files and their
# generated/ copies share the basename row (sync-gate byte-mirrored).
# =====================================================================
# LemLib survivors (the L2 record's enumerated pair)
PIN UNSAFEDECL LemLib.lean failwithIImpl
PIN IMPLBY LemLib.lean failwithIImpl
PIN UNSAFEDECL LemLib.lean fuelExhaustedWithImpl
PIN IMPLBY LemLib.lean fuelExhaustedWithImpl
# CerberusFresh — the digest/forceIO boundary (post-C2-conversion:
# zero unsafeBaseIO; kernel-checked opaques with native bindings)
PIN UNSAFEDECL CerberusFresh.lean digestPure
PIN UNSAFEDECL CerberusFresh.lean digest_impl
PIN IMPLBY CerberusFresh.lean digest_impl
PIN UNSAFEDECL CerberusFresh.lean forceThunkIO
PIN UNSAFEDECL CerberusFresh.lean forceIO_impl
PIN IMPLBY CerberusFresh.lean forceIO_impl
# CerberusImpl — enum registry (temporal; mover = reader/supply
# machinery follow-up slice, explicitly NOT in-arc)
PIN UNSAFEDECL CerberusImpl.lean typeof_enum_impl
PIN IMPLBY CerberusImpl.lean typeof_enum_impl
PIN UNSAFEBASEIO CerberusImpl.lean typeof_enum_impl
PIN UNSAFEDECL CerberusImpl.lean register_enum_impl
PIN IMPLBY CerberusImpl.lean register_enum_impl
PIN UNSAFEBASEIO CerberusImpl.lean register_enum_impl
# CerbGlobal — config/switch refs (temporal; mover = post-arc
# parameter-plumbing slice) + their read-only safe wrappers
PIN UNSAFEDECL CerbGlobal.lean confRef
PIN UNSAFEBASEIO CerbGlobal.lean confRef
PIN UNSAFEDECL CerbGlobal.lean switchesRef
PIN UNSAFEBASEIO CerbGlobal.lean switchesRef
PIN UNSAFEDECL CerbGlobal.lean getConf
PIN UNSAFEBASEIO CerbGlobal.lean getConf
PIN UNSAFEDECL CerbGlobal.lean backend_name_impl
PIN IMPLBY CerbGlobal.lean backend_name_impl
PIN UNSAFEDECL CerbGlobal.lean current_execution_mode_impl
PIN IMPLBY CerbGlobal.lean current_execution_mode_impl
PIN UNSAFEDECL CerbGlobal.lean using_concurrency_impl
PIN IMPLBY CerbGlobal.lean using_concurrency_impl
PIN UNSAFEDECL CerbGlobal.lean isDefacto_impl
PIN IMPLBY CerbGlobal.lean isDefacto_impl
PIN UNSAFEDECL CerbGlobal.lean isPermissive_impl
PIN IMPLBY CerbGlobal.lean isPermissive_impl
PIN UNSAFEDECL CerbGlobal.lean isAgnostic_impl
PIN IMPLBY CerbGlobal.lean isAgnostic_impl
PIN UNSAFEDECL CerbGlobal.lean isIgnoreBitfields_impl
PIN IMPLBY CerbGlobal.lean isIgnoreBitfields_impl
PIN UNSAFEDECL CerbGlobal.lean has_switch_impl
PIN IMPLBY CerbGlobal.lean has_switch_impl
PIN UNSAFEBASEIO CerbGlobal.lean has_switch_impl
PIN UNSAFEDECL CerbGlobal.lean is_CHERI_impl
PIN IMPLBY CerbGlobal.lean is_CHERI_impl
PIN UNSAFEDECL CerbGlobal.lean is_PNVI_impl
PIN IMPLBY CerbGlobal.lean is_PNVI_impl
PIN UNSAFEDECL CerbGlobal.lean has_strict_pointer_arith_impl
PIN IMPLBY CerbGlobal.lean has_strict_pointer_arith_impl
# CerbUtils — no-op timing/log refs + deterministic stub
# (permanent-declared family, Q4)
PIN UNSAFEDECL CerbUtils.lean timingStackRef
PIN UNSAFEBASEIO CerbUtils.lean timingStackRef
PIN UNSAFEDECL CerbUtils.lean begin_timing_impl
PIN IMPLBY CerbUtils.lean begin_timing_impl
PIN UNSAFEDECL CerbUtils.lean end_timing_impl
PIN IMPLBY CerbUtils.lean end_timing_impl
PIN UNSAFEDECL CerbUtils.lean logRef
PIN UNSAFEBASEIO CerbUtils.lean logRef
PIN UNSAFEDECL CerbUtils.lean STD_impl
PIN IMPLBY CerbUtils.lean STD_impl
PIN UNSAFEBASEIO CerbUtils.lean STD_impl
PIN UNSAFEDECL CerbUtils.lean boundedIntegerImpl
PIN IMPLBY CerbUtils.lean boundedIntegerImpl
PIN UNSAFEBASEIO CerbUtils.lean boundedIntegerImpl
# CerbMem — BEq shortcut (safe perf seam: opaque + implemented_by,
# no unsafeBaseIO; pinned because the population census is total)
PIN UNSAFEDECL CerbMem.lean beqMemValueImpl
PIN IMPLBY CerbMem.lean beqMemValueImpl
